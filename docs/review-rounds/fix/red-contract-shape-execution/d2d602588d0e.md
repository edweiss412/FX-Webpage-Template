# Round filings — fix/red-contract-shape-execution @ d2d602588d0e

## spec — 4 rounds

**Examined:** all four spec rounds of the red verdict-capability contract (`docs/superpowers/specs/2026-08-17-spec-lint-red-verdict-capability.md`): r1 three findings (silent authored decline branch, derived probe executing trailing clauses of compound commands, loose-harness census disagreeing with the normative grammar), r2 one (directory-selector authored marker falsely drawing the zero-token advisory), r3 one (§5.3 catalog row stale against the repaired §5.2), r4 APPROVE with zero.

**Mechanizable:** the r3 shape — a findings-catalog table restating rules whose normative statement lives in another section, drifting one repair later — is the duplicate-statement drift class already filed as `BL-SPECLINT-ENUMERATED-UNIVERSAL-PARITY` (one section owns the rule, every other section references it); no new row filed. The r1 census shape (a probe harness whose match rule differs from the spec's normative grammar) is the probe-mini-review rule already in `docs/agents/spec-self-review.md:23` — applied late here, not missing.

**Judgment:** r1's decline-boundary findings and r2's emptiness rule were genuine design decisions settled only by live probes (trailing-clause execution demonstrated; 657-vs-0 collection on the step3 selector and its typo variant) — not mechanizable, and each converged in one round once probed.

## plan — 4 rounds

**Examined:** all four plan rounds of `docs/superpowers/plans/2026-08-17-red-verdict-capability.md`: r1 five findings (a distillation task with no valid task-local red, an undiscriminated spawn matrix, unwired probe exclusion, unproven verdict independence, a mutant-less docs red), r2 four (a rewrite/guard contradiction, sh -nc vs sh -c spawn-indistinguishability, parse non-observations unexcluded, duplicate `RED_ALREADY_GREEN`), r3 three (authored probe ownership, phase ordering, unbounded diagnostics), r4 APPROVE with zero.

**Mechanizable:** rounds 2-3 were one axis — the plan's spawn/execution test matrix under-covering the spec's normative execution requirements — and the closing move was the same-vector checklist audit (15 cells enumerated from spec §3/§5.1/§5.2, two cells added by the audit itself), which is the `docs/agents/writing-plans.md` same-vector-recurrence rule doing its job at round 3 rather than round 9; nothing new to file. The r1 no-valid-red shape (a fixture task ordered after the implementation it asserts) splits, and only one half is mechanizable. Where such a marker is `red-state=live`, `--exec-red` already names it: the command exits 0 and draws `RED_ALREADY_GREEN`. Where it is `red-state=authored` — which is what r1 actually was — the question is whether the DECLARED failing case could ever exist, and that is truth-of-claim, fenced out of scope by both governing specs (`docs/superpowers/specs/2026-08-15-spec-lint-intent-red-arms.md:19` "mechanizing existence-of-declaration is this arm; mechanizing truth-of-claim is not", and this arc's spec §1.2 "verifying a red command fails for the STATED reason" out of scope). The authored half stays with plan review.

declined: no new ledger row, and NOT on a claim of coverage. The round-2/3 axis is the same-vector-recurrence rule in `docs/agents/writing-plans.md` firing as designed. The r1 no-valid-red shape is half shipped and half fenced: the live case draws `RED_ALREADY_GREEN` under `--exec-red`, and the authored case is truth-of-claim, out of scope by ratified decision in both governing specs — so the residue is judgment for plan review, not unscheduled work. Filing a row for either would assert work nobody should schedule, which is the ledger-filing bar in `AGENTS.md`.

**Judgment:** the spawn-mode discrimination (F2 r2) and ordering (F2 r3) findings each required a live probe of subprocess behavior (`printf` byte counts, module-scope collection cost) — reviewer-probe work, not lint-expressible.
