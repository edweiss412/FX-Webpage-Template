# fix/diagram-tile-states — review-round filing

Base `47e9544e65dd`, the only base this arc has. Rows in the sibling `47e9544e65dd.jsonl`.

## spec — 4 rounds

**Examined:** four spec rounds at this base and four across the arc, which has one base, so the
per-file count and the cross-base total are the same number. Twelve findings: 7, 4, 1, 0, ending
APPROVE at the cap. Every finding was accepted; none was disputed and none was refuted. The subject
is a 524-line spec for a three-state split of one admin component, plus a ruled `object-contain`
change on two surfaces.

**Mechanizable:** one class, and it is already parked. Rounds 2 and 3 both landed on the SAME
sentence — what `background-size` next/image gives a blur placeholder when `object-fit` arrives
through a className — and both times the spec asserted a value derived by READING
`get-img-props` rather than calling it. Round 2's repair corrected the value and left the method
intact, so round 3 found the successor of the same defect. That is `LIM-CLAIM-WITHOUT-ORACLE`:
a load-bearing property with nothing that settles it, so each round's repair is another sentence
and the next round is another reading. This arc is the class's **second** naming; the re-file
trigger is a third distinct arc, so no row is minted. The repair that held here is the one the
slug's owning filing already recommends — structural, not another sentence: a committed probe that
CALLS `getImageProps`, and a `**Settled by:**` line on each of §7's three items naming the oracle
that decides it. The probe carries a discriminating control (a `style` prop that must move the
value) so two identical `cover` readings cannot pass by next having stopped consulting `object-fit`
at all, and its report records the installed next version, because the line numbers the section
quotes live in an untracked package. Round 4 raised nothing on that section.

declined: `LIM-CLAIM-WITHOUT-ORACLE`, whose re-file trigger is a third distinct arc. No row is minted, and the process mint freeze would decline one anyway: this class's done condition is a property of the guard, not a number a product arc would notice moving.

**Judgment:** the rest, and it is where the rounds were well spent. Round 1's seven were design
questions a probe cannot settle — whether the caption may leave the `overflow-hidden` box without
breaking the ratified equal-box contract, which states share the name line, whether the name line
is `aria-hidden` in the live state only. Round 2 found the grid probe printing FAIL and exiting 0
while two sections cited it as a gate, and found AC-7b passing against exactly the wrong
implementation the design invites. Neither is a recognizer question; both are somebody reading
carefully. The five vacuous live-negatives in §5.0 are the same shape: an assertion that a string is
ABSENT from an element the string is about to leave passes forever afterwards, and no lint arm was
going to notice.

**Infra:** the three browser probes could not run in the reviewer's sandbox in rounds 3 and 4
(Playwright `EPERM` creating its temporary directories), so the reviewer inspected their source
instead and said so both times. Every number in spec §2 and §3.4 is therefore author-measured and
was never independently reproduced by the reviewer. The blur probe, which needs no browser, DID run
on the reviewer's side in round 4 and passed on next@16.3.0. Worth knowing rather than worth fixing
here: it means the real-browser claims rest on Tasks 6 and 7 of the plan, which run them in CI,
not on the spec review.

**Repaired by construction, so it cannot recur here:** §7's second half was a cross-reference that
counted to the wrong item in §6's list. Rather than fix the count, §6's peers took slugs
(`grid-density`, `absent-reconcile-focus`, `next-object-fit`, `gallery-failure-copy`) and all four
cross-references name one. A count rots silently when a list is reordered; a slug cannot, and the
reference became a grep instead of an act of counting. Not filed as a class: one occurrence, and the
repair removes the mechanism rather than the instance.

## plan — 4 rounds

**Examined:** four plan rounds at this base and four across the arc, which has one base, so the
per-file count and the cross-base total are the same number. Eleven findings across the four:
6, 3, 1, 1. Every one accepted, none disputed, none refuted, and the rate decayed to one and stayed
there. The subject is a 5-task plan for a
three-state split of one admin component, plus a ruled `object-contain` and border restyle on two
surfaces.

**Mechanizable:** two classes, both already parked, and this arc is a fresh naming of each.

Round 1's sharpest finding is `LIM-PLAYWRIGHT-RED-TESTMATCH` exactly: both Playwright `red=`
commands named `--project=desktop-chromium`, whose `testMatch` excludes
`step3-review-modal.layout`, so both would have exited 0 and reported green forever. Measured the
way that slug's own record measures it — `Total: 0 tests in 0 files` against `Total: 44 tests in
1 file`. **This is the FOURTH arc to name it**, past its three-arc re-file trigger; the trigger
condition is "a product-facing arc measurably blocked", and this arc is product-facing (it closes
three `DIAGRAMTILE-` rows that change what an admin sees) and was measurably slowed by exactly one
review round. **No row is minted regardless**, because this arc files none of any facing under any
exception clause, by its own commission. The trigger state is reported to the orchestrator, whose
call it is.

Rounds 2, 3 and 4 are three faces of `LIM-AUTHORED-RED`, the "cannot be red for its stated reason"
shape, and the second and third faces are worth recording because the slug's write-up has neither
yet. Round 2 was the ordinary form at task granularity: all production change sat in the first four
tasks, so the two browser tasks had nothing left to make red and would have gone green the moment
they were authored. Round 3 was the INVERSE and is not obviously the same class until you see it —
a task whose production change breaks suites it does NOT own. Task 3 deletes the string that
seventeen assertions name while listing only two files, so its own narrow command could report green
with twelve suites left red and no later task owning them. Round 4 is the third face and the only one about GREEN rather than red: Task 2 claimed AC-7b, whose
first clause requires the message to exceed one line-height, while the copy that makes that true
lands in Task 3. The shared 19-character string sits on one line inside the 121.5px tile at 640 and
the 169.5px tile at 1072, so no implementation in Task 2 could have satisfied it. Arithmetic, not
opinion, and the reviewer swept the dependency class across all five markers and found no second
instance.

One root property under all three: a task's declared surface and its actual reach were allowed to
differ. Round 2 left a task nothing to turn red; round 3 left something red that nothing turns
green; round 4 claimed a green that a later task's production change owns. The `red=` marker
grammar checks the first of those and nothing checks the other two, which is the honest statement of
what is mechanizable here and what is not.

declined: `LIM-PLAYWRIGHT-RED-TESTMATCH` and `LIM-AUTHORED-RED`, both parked, and this arc mints
nothing of any facing by its own commission. The process freeze's admission test would decline them
anyway on the second question: their done conditions are properties of the guard, not numbers a
product arc or a human would notice moving.

**Judgment:** the decomposition, and it took three rounds because each repair exposed the next
layer. Round 1 fixed the commands and the claims; round 2 found the shape those correct commands
were arranged in was wrong; round 3 found that the corrected shape still let one task's blast radius
exceed its declared files. No lint arm reaches any of that. The AC-11 correction is the same
character: reading the five live-negative sites one at a time showed each ALREADY fails against a
placeholder, so the spec's §5.0 claim that they "become vacuous" overstates, the plan's original
repair was largely redundant, and round 2 independently showed the mutant proof for it could not
discriminate. That took a reviewer and a reader, not a rule.

**Infra:** none this stage. Every dispatch returned a verdict; no reap, no `no_verdict`, no retry.
