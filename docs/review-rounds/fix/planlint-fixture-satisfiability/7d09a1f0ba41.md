# Review rounds — fix/planlint-fixture-satisfiability @ 7d09a1f0ba41

## spec — 7 rounds

**Examined:** spec rounds 1-7 (r1 BLOCKING 1, r2 BLOCKING 2, r3 BLOCKING 2, r4 BLOCKING 1, r5 BLOCKING 1, r6 BLOCKING 1, r7 NEEDS-ATTENTION 1 — converged, no round 8 owed). Every finding was accepted; none was refuted. All four rounds landed on ONE axis: the verdict semantics of an `expect=green|red` field the spec carried from round 0, and specifically the gap between what vitest's JSON report says and what a verdict about a block needs to know.

The four instances, each probe-backed by the reviewer and independently re-probed by the author before repair:

1. r1 — a skipped test is a present `assertionResults` entry with `status: "skipped"`; a block whose every body would fail exits 0 with zero failures and read clean.
2. r2 — a throwing `afterAll` fails the FILE with `numFailedTests: 0` and the assertion reported `passed`.
3. r3 — the r2 repair's own belt-and-braces conjunct (`status is not failed` added to the clean predicate) made it unsatisfiable, because vitest marks a file failed whenever any test fails; an ordinary `expect=red` block with a real assertion failure then matched no branch at all.
4. r4 — `beforeEach`/`afterEach` failures are recorded on the TEST result, so they arrive as an ordinary-looking failed assertion; in the `beforeEach` case the body never executed and nothing in the report says so.
5. r5 — an empty test body reports `status: "passed"`, so "at least one passed" was not evidence of anything; two live corpus blocks are entirely comment-only.
6. r6 — a `premise()` at module scope throws during COLLECTION, so vitest reports zero test cases and puts the sentinel at file level; the ladder's empty-entries-first order called that "did not run" and suppressed the verdict.

**Mechanizable:** ONE, and it is ours, not the reviewer's. **The review brief's convergence criterion went stale against its own artifact.** The design narrowed across r1-r5 — the `expect=` field deleted, then the clean-observation branch deleted — while bullet 1 of the brief still described the pre-narrowing bound ("passed clean … or declined"). A reviewer scores rounds against the brief, so it was scoring every round against a WIDER claim the spec no longer made: the gap it reported was real against the brief and absent from the artifact. That plausibly bought r6 and possibly r5. The repair is a rule with a cheap mechanical form — **when a repair narrows what the artifact claims, the brief's bound is part of the repair commit, not a later tidy-up** — and the check is a diff-time one: any commit touching a spec's ratified bound section must touch the brief's bound bullet, or say why not. Filed as `BL-REVIEW-BRIEF-BOUND-DRIFT`. This is the transferable lesson; without it "six rounds on one axis" reads as reviewer noise when a share of it was our own brief drift.

The six findings themselves were design defects in a verifier answering a bigger question than the arc asked, and no static check over the spec text would have found any of them — they are properties of vitest's reporter that only a probe reveals. The two NEEDS-ATTENTION findings (r2, r3) WERE mechanizable and are now mechanized: stale ladder ordinals copied into a table, an AC, and seven test titles. The repair is a stated rule (positions appear in §4.3 and nowhere else, spec §4.4) plus `rg -n 'branch [0-9]|condition [0-9]'` run as a closeout sweep (plan §3.2), which is a derivation rather than a re-enumeration.

**Judgment — the r7 finding, and a root cause shared with another arc.** Round 7 confirmed both structural properties (the sentinel-first both-channel ordering separates the two empty-entry shapes; the three predicates are mutually exclusive and exhaustive) and returned one finding: five places where pre-narrowing language survived the r6 sweep. The root cause is worth more than the instances. **One imprecise phrase was doing load-bearing work.** "The block did not run" is a claim about EXECUTION; the arm can only observe that the report carries no TEST CASE. Those two diverge exactly at a skipped body and a throwing hook — which is precisely where §1.1 item 13, §8 item 4 and §8 item 6 appeared to contradict each other, and why §4.3's fallback claimed the block ran when an all-skipped block reaches it with nothing executed. Restating the advisory's meaning once, precisely, dissolved all five instances at their source rather than editing five sentences.

Arc F hit the same shape independently on the same day, in a different artifact. Two arcs, one root cause: a phrase that reads as a claim about the world when the mechanism can only observe a proxy for it. The orchestrator is carrying it to batch closeout. The repair that generalizes: when a term appears in a bound, name what the MECHANISM observes, never what you hope it implies.

The r7 repair was verified by a DERIVED COVER rather than an enumeration — a grep over the phrase class found three instances the reviewer had not cited and then returned zero — which is why no round 8 was owed and none was dispatched.

**Judgment (rounds 1-4):** rounds 1, 2 and 4 were bought, not burned. Each returned a measured property of the vitest reporter that the author's own probing had not reached, each was one ordinary marker edit away from the live corpus (an environment-gated `describe.skip`, one of 27 blocks carrying `afterAll`, an ordinary `beforeEach`), and each would have shipped a silently wrong verdict. Round 3 was charged to the repair, not to the reviewer: a conjunct added defensively without probing what vitest actually reports for a failing file.

**The repair direction was DELETION at every round, and that shape is the other transferable finding.** `expect=` gone; hard codes 8 to 2; ladder branches 8 to 3; four successive versions of a clean-observation branch each deleted rather than patched. Not one round was closed by adding a recognizer, a grammar corner, or a per-shape code — the r6 repair, the only one that added anything, added a second channel to read and an ordering constraint, and strictly NARROWED the advisory's claim. An arc whose finding rate stays flat while its artifact shrinks is converging on the right thing; the same round count with a growing detector would have been the pathology this corpus exists to catch.

The convergence move was NOT a fifth branch. Four consecutive rounds on one axis is the AGENTS.md same-vector recurrence trigger, and the repair direction under recurrence is narrowing, so round 4's repair DELETED the `expect=` field and with it four of the five hard codes. The arm now answers one question — did a stated premise fail — with one hard verdict, one clean reading that requires positive evidence, and one advisory decline that names its reason. Every round-1-through-4 finding is unreachable in the narrowed design rather than patched: there is no verdict left to get wrong. Ladder branches went 8 to 4, codes 8 to 5.

r6 was ruled by the orchestrator rather than answered by another dispatch: repair option 1 (sentinel tested before the empty-entries branch, over both the assertion and file-level channels), fenced in both directions, followed by a confirmation-only r7. The alternative — declining on any zero-test report — was rejected because it would silently drop genuine module-scope premise failures, including the live corpus instance at `docs/superpowers/plans/2026-08-04-guard-premise-reachability.md:1174`.

**Process:** the cap was crossed without the mandatory report to the orchestrator. Rounds 5 and 6 were dispatched at the implementer's own discretion when two consecutive same-axis single-finding rounds were already the orchestrator's disposition call. The halt came from the orchestrator, not from the arc noticing.

**Infra:** none. All six dispatches returned verdicts under the 1380s budget recipe; no `no_verdict`, no reaped run.

## plan — 4 rounds

**Examined:** plan rounds 1-4 (r1 BLOCKING 7, r2 BLOCKING 6, r3 BLOCKING 5, r4 BLOCKING 5). Twenty-three findings, every one accepted, none refuted, and NOT ONE reopened the spec's ratified design — every finding was a defect in the plan as an implementation instruction. Three classes:

1. **Stale cross-references after a restructure** (r2, r3, r4). A task moved and the prose naming it did not.
2. **Test-harness defects that let a wrong implementation pass** (r2, r3, r4). The valuable class, and what plan review is for.
3. **Factual claims asserted without measuring** (r1: registry rows 23 vs the real 36; r4: a corpus census of 216 that reproduces under no spelling — 212 double-quoted at base, 214 with single quotes, 215 on the branch).

**Mechanizable:** two of the three, and both now are.

- **Class 1 is closed by a derivation.** `scripts/plan-self-consistency.mjs` reads the plan's own headings and markers and reports every prose claim contradicting them: a task or gate id no heading defines, a stated count of path-only `red-target=` values the markers do not carry, a claimed live-marker count, and a section reference the document does not define. It reproduced five of round 3's findings on its first run, two of which the round-2 repair had introduced; extended in round 4, it catches the case where the round-3 rewrite **deleted §3.3 outright** while Gate B and the spec went on citing it. Gate B re-runs it.
- **Class 3 is the authored-and-run rule**, which already existed and which this arc violated twice. Recorded under `BL-REVIEW-BRIEF-BOUND-DRIFT`'s sibling discipline rather than as a new row: the rule is not missing, it was not applied. The durable repair shipped here is that every count in the plan is now printed beside the command that produced it, and the section says so in the document rather than in a commit message.
- **Class 2 is not mechanizable and should not be.** "Would this assertion catch a wrong implementation" is the judgment a reviewer is for.

**Judgment:** round 4 is the round worth reading. Two of its five findings were defects in round 3's REPAIRS — a multi-block mapping fixture whose fake reports were declared and never read (so a CORRECT filename-keyed mapper could not produce the asserted result, while the defective first-entry-to-first-block mapper could), and a harness inventing a `run(cmd)` seam beside the landed `spawn(command, cwd, timeoutMs, mode)` one, which a standalone helper could green with the real CLI entirely unwired. Both would have let a broken implementation ship green. That is the fix-round regression rule earning its place, and it is the argument that these rounds were bought rather than burned even at the cap.

**The arc's own tooling caught the sharpest defect.** Dogfooding `spec:lint --exec-red` over this plan drew a hard `RED_SUITE_UNCOLLECTED` from arc B's shipped collection arm on Task 7's red command: `tests/mutation/guardSurfaces.gates.test.ts` lives in `MUTATION_TEST_GLOBS`, an env-gated vitest project, so the bare `vitest run <file>` form collects zero tests and exits non-zero for a collection reason rather than for the key-set mismatch the marker claimed. Arc B's arm found, in arc D's plan, exactly the class arc B was built for.

**Infra:** none. All four dispatches returned verdicts under the 1380s budget recipe.
