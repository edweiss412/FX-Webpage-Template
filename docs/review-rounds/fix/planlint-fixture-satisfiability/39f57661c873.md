# Review rounds — fix/planlint-fixture-satisfiability @ 39f57661c873

The spec and plan stages ran under the previous base and are filed at `7d09a1f0ba41.md`. This base is the post-Stage-0 merge, and every round below is the whole-diff stage.

## diff — 4 rounds

**Examined:** diff rounds 1-4 (r1 NEEDS-ATTENTION 3, r2 NEEDS-ATTENTION 1, r3 NEEDS-ATTENTION 2, r4 APPROVE 0 — converged at the cap, no round 5 dispatched). Six findings, every one accepted, none refuted. Three of the six landed on one axis — whether a splice directory can survive and go unnoticed — and the other three were a swallowed error, a factory ignoring its own parameter, and a Markdown span that made shipped authoring guidance uncopyable.

**Judgment:** three rounds on one axis was a STAIRCASE, not a drip, and the difference is legible in the repairs. The AGENTS.md same-vector trigger asks whether successive rounds are widening a recognizer one corner at a time. These went the other way, and each repair strictly shrank the mechanism:

1. r1 — `deps.rm` failing was swallowed behind a bare `catch {}`, and the comment justifying it was itself wrong. Raised to exit 2 and contracted in spec §4.2 step 4.
2. r2 — a directory named for the process that created it is invisible to every later process, so an abrupt kill (where the `finally` never runs at all) left a survivor step 1 could never see. The pid AND the counter were DELETED for a fixed name.
3. r3 — `exists()` followed by `mkdir(recursive: true)` is a test-then-create window, so two invocations could both proceed into one directory. The `exists` dep was DELETED for a single non-recursive `mkdirExclusive`.

**r3's repair is categorically different from its two predecessors, and that is what closed the axis.** The first two were better checks; the third is an ATOMICITY PRIMITIVE. Non-recursive `mkdir` either creates or returns `EEXIST`, so there is no window left to find — the axis closes by construction rather than by care. It is the same move as replacing a nullable pair with a union so the invalid state is unrepresentable, which this arc had already made once (`ScannedMarker`, in the mutation repairs below). A round that ends an axis looks different from a round that advances one, and the tell is whether the repair removes the possibility or improves the vigilance.

**Mechanizable:** one, and it is ours. `nodeDeps(root, pid)` ignored its own `pid` and returned `process.pid` (r1 F2). It was invisible to all seven cases of the suite that depended on it, because the collision case LEARNED the directory from a first real run instead of predicting it. The durable form: **a dep that exists only so a test can construct a case is a dep that can be wrong in a way only that test sees.** The pid was introduced for exactly that reason and for no other, and r2 then deleted it outright — which is the general repair, not a coincidence. Where an injected seam exists solely to make a case constructible, prefer removing the need for the injection.

declined: the seam it names was DELETED in this same PR rather than guarded, so there is no follow-up work to schedule. Round 2 removed the pid dep outright when it removed the pid from the directory name, and `nodeDeps` is back to one argument. A mechanical check for "an injected dep whose only consumer is a test" would have to run against a codebase where this instance no longer exists, and the rule it would enforce is stated here and in the r2 commit body; a lint arm for it fails the process-facing mint bar, which wants a measured incident that a guard would have caught and finds instead an instance already closed by deletion.

Second instance of a related shape on the same arc: the splice counter was DEAD MECHANISM. `runCli` accepts exactly one document per invocation, so the counter could never reach 2, and it was carried for two review rounds as if it were load-bearing. A gate carrying machinery its own contract makes unreachable is worth a sweep at authoring time, not at round three.

**The second r3 finding: repair the instruction, preserve the observation.** r3's second finding was the document-side class sweep owed on r2's own repair and not done: four sites still argued from "the counter has already advanced", an argument that died with the counter. Three were corrected outright. The fourth — Task 5's embedded harness, which still seeds the old directory name and drives the deleted seam pair — was ANNOTATED rather than rewritten. That block is the record of the red the task actually observed at its own sequence position; editing it to match seams review moved two rounds later would falsify the record. The annotation states that it is no longer runnable, which seams changed under it, and where the landed suite is. The distinction generalizes: a stale INSTRUCTION is repaired, a stale OBSERVATION is annotated, and conflating them either misleads the next reader or destroys the evidence.

**Infra:** the mutation gate found what no review round did, and it found it first. `lib/specLint/fixtureContract.ts` was enrolled before the round-1 dispatch, per the enrolment-precedes-review rule. The first scored run reported 0.8194 with THIRTEEN unaccepted survivors; the final run reports 63/65 with an empty unaccepted-survivor set. Eleven were repaid and the direction was deletion in three of them — a second copy of the indent bound, a third copy of `parseDoc`'s, and a nullable pair replaced by a union — plus one dead `continue` whose removal was unfalsifiable by construction. Four defects the gate found that four review rounds did not, at roughly 93 seconds a run against roughly 25 minutes of dispatch per round.

Worth recording precisely, because the orchestrator's own acceptance condition had the locus wrong: **harness inadequacy does not appear as a harness-code mutant.** The runner overlays mutants on the enrolled SURFACE, and the tests are the detector, so a weak or tautological test shows up as a surviving SURFACE mutant — a test that cannot detect a change is exactly a test that fails to kill one. Thirteen survivors in the surface, eight repaid by adding cases, is the harness class being tested and coming back clean, in the surface's coordinates.

**Three harness defects were found by hand at implementation, before any dispatch.** All three were in the plan's own authored test block for the adapter task: a `spawn` fake returning an `ExecOutcome` where the landed seam returns a `SpawnResult` (so every real timeout and signal would have classified as "no exit status" with the suite green); no `pid` injection, which made the collision fence unreachable by any test; and a `runCli` case passing a dep the CLI does not have, omitting five it needs, and keying its marker to a different line than its report — a case no correct implementation could satisfy. A plan whose test blocks are executable should be executed; these surfaced only because the blocks were spliced and run rather than read.

**A scoped local check is not a local check.** Three CI failures on the first push were all this diff's and all reproduced locally the moment the scope was widened: a third `CliDeps` construction site under `docs/` that a `grep -E '^(tests|lib|scripts)/'` filter over `tsc` output had discarded, a Drive-call-bound walker false positive on `results.files.get(...)`, and the premise-contract declaration a new mutation enrolment owes. The typecheck one is the instructive one — the error was present locally and filtered out of view by the author's own command.

**Nothing else here was mechanizable.** No static check over the diff would have found any of the six findings; each rests on a property of a filesystem primitive, a report shape, or a Markdown renderer that only a probe reveals — and each was probed by the reviewer and independently re-probed here before repair. Every repair carries a regression case proved RED against the pre-repair source rather than argued to be red.
