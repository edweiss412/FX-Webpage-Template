# Review-round filing — `fix/screenshots-drift-cache` @ `b2ee5cb37240`

## diff — 4 rounds

**Examined:** R1 NEEDS-ATTENTION (2 findings), R2 NEEDS-ATTENTION (1), R3 NEEDS-ATTENTION (1),
R4 APPROVE (0). Every finding was correct and every one was a defect in the PIN, never in the
shipped workflow. The workflow edit itself drew zero findings across all four rounds; the
three real `workflow_dispatch` proofs stood unchallenged.

**Mechanizable:** Three of the four findings are one mechanizable shape — *an assertion that
recognizes a spelling instead of asking a closed question* — and the corpus shows it
ratcheting exactly as AGENTS.md predicts:

| Round | The assertion asked | What walked past |
|---|---|---|
| R1 F1 | "split the census on commas" | a joined literal `'app/**, components/**'` (23 boundaries) |
| R2 | "is the literal `actions/cache@v4` absent?" | `actions/cache@v4.2.0`, a real supported ref |
| R3 | "which steps have `path === CACHE_PATH`?" | six equivalent path spellings the action accepts |

Each repair invited the next escape because each stayed a recognizer. The terminal repair
stopped recognizing anything: assertion 1 now asks *which cache-family steps does this job
run* — exactly two, the sanctioned pair — and never consults the path at all. That version is
SMALLER than the two it replaced and cannot be escaped by a future ref or path syntax, which
is the property the earlier forms lacked.

The mechanizable lesson, stated so it transfers: **when a guard's subject is a value the
platform accepts in many equivalent spellings (an action ref, a path, a glob), do not assert
over the spelling. Find the finite set the guard can enumerate — here, the job's own steps —
and assert over that.** A spelling-based guard on such a value is a round generator; the round
count is set by how many representations the reviewer thinks of, not by the defect.

R1 F2 is a separate and more ordinary shape: a behavioral assertion with a compound fixture
(`both`) and a null fixture (`clean`) but no SINGLETON fixtures, so an `||` -> `&&` flip
survived. Mechanizable as a checklist item already in the repo's anti-tautology rule —
*for a predicate over N independent conditions, fixture each condition alone, not just their
conjunction and their absence.*

**Judgment:** None of the four required a product or design decision. The direction fence held
throughout: no round attempted to relitigate the input-hash key, the baselines exclusion, or
the split/always-save, all of which the brief pre-ratified with `file:line` citations. That is
the fence working — the reviewer spent every round on the guard's strength rather than on
settled scope, which is where review time is worth spending.

**Infra:** The reviewer's sandbox denied Vitest's temporary `ssr` directory with `EPERM` in
all four rounds, so it executed zero tests every time and correctly declined to treat those
runs as evidence; `api.github.com` was also unreachable, so the dispatch-proof run ids could
not be independently queried. Every finding was nonetheless produced by the reviewer's own
in-memory harnesses against the real files, and every one reproduced against the shipped tree
when the implementer ran it. Worth recording because a reviewer that cannot run the suite is
still able to find real guard defects — but it means the implementer's own mutant runs, not
the reviewer's, are the executable evidence in this arc's commits.
