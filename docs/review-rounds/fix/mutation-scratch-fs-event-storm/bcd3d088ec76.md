# Review-round economy — `fix/mutation-scratch-fs-event-storm`

## spec — 2 rounds

**Examined:** `docs/superpowers/specs/ci/2026-08-24-mutation-scratch-fs-event-storm-design.md`. This file records the arc's LAST TWO spec rounds, relabelled 1-2 to satisfy the contiguity rule; they are the arc's third and fourth. Verdicts BLOCKING (9 findings) then BLOCKING (3). The first two rounds are in the sibling `8bf8709914a3.jsonl`, both BLOCKING with 5 findings each. **Arc total: four spec rounds, 22 findings, every one accepted and none argued down.** The analysis below covers all four.

**Corpus note — the split, and why the numbering in this file is not the arc's.** `origin/main` was merged between the arc's rounds 2 and 3, and the corpus keys on `git merge-base`, so the four rounds landed 2+2 across two files and neither reaches the threshold of 4 alone. The rounds in THIS file were dispatched as 3 and 4 and are recorded as 1 and 2, following the relabel-plus-cross-reference shape ruled for the same collision on `feat/validation-prune-db-side-gate`, where it also surfaced as a merge blocker. A reader wanting the arc's real round numbers should read this note, not the `round` field. Live instance of `BL-REVIEW-ROUND-COUNT-RESETS-ON-REMERGE`; filed rather than left for the counter to miss.

**Mechanizable:** declined: neither shape below is filed as a row, and both reasons are the same screen. The figures arm's surviving practice is "measure directly and state no product", which is what the shipped §1.3 does; the guard for it would be a recognizer over prose asserting that a number is not a product of two other numbers, which is exactly the shape the round-economy rules say not to build. The package-script arm cannot be checked without knowing which criterion is meant to cover which entry point, so a lint would either pass vacuously or demand an AC per script. Both are process-facing and would need an incident beyond this arc under the mint bar; every instance either produced is repaired in-spec.

The two shapes, recorded for the next reader rather than scheduled:

- **Three of four rounds returned an arithmetic finding, and the fourth returned a fourth one.** R1: per-pass totals omitted the clean baseline run (`mutants`, not `mutants + 1`). R2: every figure multiplied STATIC `mkdtempSync` call sites, but a site executes once per test that reaches it — measured error up to 23x. R3: the extrapolation was unfixable as stated, because `runSuiteRecorded` returns on the first nonzero suite (`tests/mutation/source/runner.ts:221`) so a multi-suite surface does not run every suite per mutant. R4: a purge figure quoted a net census delta as the number removed. The mechanizable arm is not a better formula — it is **deriving figures from a command instead of composing them in prose**, which is what the surviving §1.3 does (measured per-run quantities only, no products). The R2 repair moved the tables into a script and R3 still found the underlying model wrong, so the lesson is sharper than "script it": a derived number inherits its model's defects, and the only figures that survived four rounds are the ones measured directly with no model at all.
- **An acceptance criterion that tests a layer below the production entry point.** R4 findings 1 and 2: AC-2/2b/2c all exercised the wrapper's Python path, so they passed whether or not the shipped `pnpm heavy:mutation` script delivered the flag — and R3 had already found that the first draft's `pnpm heavy --class mutation` could never deliver it. Mechanizable shape: **for any change whose contract is reached through a package script, one AC drives the package script.**

**Judgment:**

- **A repair whose acceptance criterion is satisfied by the defect it introduces.** R1's class-lock fix deadlocked (R2 finding 1), and its own AC-2b — "the nested process waits" — was satisfied BY the deadlock. No mechanism catches this; it needs a reviewer asking what else makes the assertion true. The repair direction was narrowing (refuse, never wait-while-holding), per the round-economy rule.
- **A premise asserted about a shared machine.** The retracted leak rate (R3 finding 5) was arithmetically fine and rested on "with no score run in progress", which was never verified and, on a box running eight arcs, was not this arc's to assert. Judgment, not mechanism: know which of your premises belong to someone else.
- **The cold-miss criterion** (R2 finding 3): an ON arm using distinct filenames reuses one root, satisfies every root-count premise, and never enters the invalidation branch, so a broken predicate ships green. Seeing it requires reasoning about which code path the test's own layout selects.

**Infra:** none. All four dispatches returned a verdict on the first attempt; no `no_verdict`, no reap, no wedge.

**What ended it:** R3's nine findings against R1/R2's five each — a rising rate — triggered the ratchet response. Five of those nine were about the cost tables rather than the repair, so the per-pass totals, the concentration percentage, the extrapolations and the observed-population reconciliation were **deleted rather than recomputed**, and R4 dropped to three findings, none of them on the repair. The document now claims strictly less than it did at R1 and every remaining claim is directly measured.

## plan — 4 rounds

**Examined:** `docs/superpowers/plans/ci/2026-08-24-mutation-scratch-fs-event-storm.md`, rounds 1-4, verdicts NEEDS-ATTENTION (8 findings), BLOCKING (6), BLOCKING (3), NEEDS-ATTENTION (1). Every finding accepted; none argued down. The declining count is the shape a converging stage should have, and round 2 is where the arc's scope changed rather than its wording.

**Mechanizable:** declined: the one shape worth a rule is already a rule. Round 4's finding was that the oracle round 3 rejected still stood in three other places — the marker's `why=`, the RED statement, and a fragment an earlier edit had grafted onto the repaired paragraph. That is the class-sweep rule in `AGENTS.md`, unapplied by me to my own repair, and a lint for "a document still asserts what a previous round retired" is a recognizer over prose of exactly the kind the round-economy block says not to build. The durable form is the habit: after repairing a finding, grep the document for the shape, not the sentence.

**Judgment:**

- **A criterion satisfied by the defect it was written to catch, and its mirror.** Round 2 found that the class-lock repair DEADLOCKED and that AC-2b ("the nested process waits") was satisfied BY the deadlock. Round 3 then found AC-5 could pass while every repaired suite leaked on failure, because a failure injected into a case that created no root leaves nothing behind. Together with the spec stage's guard-erases-its-own-subject-set and the AC-6 that could not pass on a correct change, this arc produced four faces of one family: a criterion whose truth value does not depend on the thing it names. The unified test adopted from it: **plant the defect AND plant the correct change; a criterion that fails to move on either is decoration.** No mechanism finds these; a reviewer asking "what else makes this assertion true" does.
- **Scope as a review outcome.** Round 2's finding that a five-case matrix still admitted four more wrong implementations, after round 1 had grown it from four cases, is what removed cache invalidation from this arc entirely. The finding was not repaired — it was taken as evidence that the axis does not close, and the work was split to `BL-MUTATION-SCANNER-CACHE-INVALIDATION` with those four implementations as its starting case set. Rounds 3 and 4 then returned 3 and 1.
- **A companion surface a task's own RED cannot see.** Round 3: `AGENTS.md`'s heavy-phase rule is content-pinned, and Task 2 edits it, but Task 2's `red=` watches `withHeavySlot.test.ts` and would have gone green with the repository guard red. Declaring the pin in the meta-test inventory is the fix; noticing it required reading what else asserts about a file being edited.

**Infra:** none. All four dispatches returned a verdict on the first attempt.

**What ended it:** the rescope. Rounds 1-2 averaged seven findings against a plan carrying a tier that retired an asserted contract; rounds 3-4 averaged two against a plan without it. The stage did not converge by better prose, it converged by shipping less.
