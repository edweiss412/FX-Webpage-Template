# Review-round economy — `fix/mutation-scratch-fs-event-storm` (whole diff)

## diff — 4 rounds

**Examined:** `git diff origin/main...HEAD`, rounds 1-4, verdicts BLOCKING (6 findings), BLOCKING (2), NEEDS-ATTENTION (1), APPROVE (0). Every finding accepted; none argued down. The spec and plan stages are recorded in the sibling `bcd3d088ec76.md`; the corpus splits by merge-base and `origin/main` was merged twice during this arc.

**Mechanizable:** declined: neither shape below is filed as a row. The plant-both check is a DISCIPLINE, not a lint — no static arm can decide whether an arbitrary assertion moves under a planted defect, and where the repo CAN decide it mechanically (a surface the source-mutation registry expresses) the harness already does exactly this, so a row would restate an existing mechanism for the cases it cannot reach. The flag-branch shape is real but narrow enough that a lint over it would be a recognizer looking for one argument-parsing idiom; it belongs in review attention, which is where it was caught. Both are process-facing and would need an incident beyond this arc under the mint bar, and every instance either produced is repaired in this PR.

The two shapes, recorded for the next reader rather than scheduled:

- **A discriminating assertion is proved by planting its defect, not by reading it — and this arc is the measured case for making that a gate rather than a habit.** `AC-2f` was written three times and **inspection endorsed all three**. Version 1 asserted the announcement order, which the planted slot-first defect passed, because the slot's own announcement comes after `acquire_loop` and both orderings print the same. Version 2 moved to one slot, which is worse: the class holder legitimately owns the only slot there, so the ordinary run cannot proceed under either implementation. CI caught that one as a 90 s timeout while it passed locally by winning a race. Version 3 runs at two slots and still passed the plant, because the ordinary run does complete under the defect — after the 60 s holder finishes. Only a DEADLINE separates them. The shipped form asserts elapsed time and the planted defect reports **61046 ms against a 20 s bound**, with the holder's sleep legible in the number. Filed as the standing check: **plant the defect AND plant the correct change; a criterion that fails to move on either is decoration.**
- **`--class` with no value returning "no class requested"** is the inverse-of-asked defect: the caller asked to be bounded and was silently admitted unbounded. Mechanizable shape: a flag whose ABSENCE and whose MALFORMED PRESENCE take the same branch is a defect wherever the flag requests a restriction.

**Judgment:**

- **A brief can state a bound the design cannot meet.** Round 2's first finding was against my own brief, not the code: I wrote "no state where something waits while holding", and class-first acquisition necessarily holds the class while taking the slot — as any ordered two-lock acquisition does. The reviewer was right to hold the diff to what the brief said. The repair was to state the real invariant (NO CYCLE, because ordinary runs never request the class) with its bounded consequence, and to drop a prohibition that was unsatisfiable by construction.
- **"Documented limit" is not a disposition reachable by writing a comment.** Round 3 rejected mine because the consequence bound admits a limit only when the outcome is conservative AND SURFACED, and a subject silently dropping out of a derived set is not surfaced: the remaining twelve keep every assertion green while the omitted file leaks. *A conservative outcome nobody is told about is a hole with a comment next to it.* The repair pins the derivation's OUTPUT — a tripwire on the derivation rather than a hand-maintained replacement for it — so a departure fails loudly and by name.
- **Four cases asserted a concurrency state they never established** (round 2). Premise arms are cheap and this suite already had the pattern; what it took was a reviewer asking what ELSE makes each assertion true.

**Infra:** none for the dispatches — all four returned a verdict on the first attempt. Worth recording as a review-capability limit rather than a defect: the reviewer sandbox denies temporary-directory creation, so **Vitest could not be executed in any round** and every finding here came from source and process topology. Two of the arc's defects were found by CI instead, and one of those (`AC-2f`'s one-slot timeout) was invisible locally.

**What ended it:** the finding count fell 6, 2, 1, 0 with no repair reopening an earlier axis. Rounds 2 and 3 each landed on the same underlying question — does this assertion establish the state it depends on — and answering it structurally (premise arms, then a pin on the derivation) closed the class rather than the instance.
