# Review rounds — fix/typo-v4-venue-shape @ 44b0d74b1107

## spec — 4 rounds

**Examined:** the four spec dispatches at this base (`44b0d74b1107.jsonl`, rounds 1-4, all `--stage spec`), their briefs, and the repair commit each produced. Findings by round: 3, 2, 2, 1. Every finding was admitted; none was refuted. None was a citation-shape or line-number complaint, because `spec:lint` was run to 0 hard before each dispatch — that took the whole mechanical class off the table before round 1.

**The distribution is the finding: 7 of the 8 were the same ~200 characters of operator copy.** Not the predicate, not the anchor scanner, not the three consumers of the routing key, not the corpus measurements. Those were probed once, agreed at round 1, and never reopened across four rounds. The copy was rewritten four times, and each rewrite made a NEW false claim while clearing every mechanical gate:

| draft | what it claimed | why it was false |
| --- | --- | --- |
| 1 | "the row still shows on the crew page" | probed: `contacts=[]` for all four aliases; `venue.contact_info` is a field `parseVenue` never writes |
| 2 | "correct the spelling in the sheet whenever it suits you" | directs a sheet edit, and `INFO_CODE_ACTIONABILITY`'s own comment makes "catalog copy directs a sheet edit" the criterion for `actionable`, which this code is not |
| 3 | "we read the row as intended" | probed: the row's value appears NOWHERE in the parse result |
| 4 | "misspelled label" | `TYPO_ALIASES` holds `"diagrams"`, and `resolveAliasFull` lowercases before testing, so correctly-spelled `Diagrams` emits the code too |

The banned-vocabulary regex and the length caps passed all four times. They lint vocabulary; none of these was a vocabulary defect. Nothing in the repo checked whether an operator-facing sentence was TRUE of the code it describes.

**Mechanizable:**

- declined: the four semantic copy classes are now checked in-arc rather than filed. `.probe/copy-check5` asserts, alongside the banned-vocabulary regex and the caps, that the copy makes no publication claim, no action invitation, no read-the-row claim and no misspelling claim. That is four substring sets and it would have collapsed rounds 1-4 into one. A ledger row is declined under the 2026-08-25 process-mint freeze: the finding is process-facing, its done condition is a property of the checker rather than a number a product arc or a human would notice moving, and it names no product-blocked incident. It lands in this arc's spec §5 as the authoring procedure, with a re-file trigger if a second arc authors card copy and hits a fifth family.
- declined: the general form, "does this operator string assert something the code does not do?", is not mechanizable and no row is filed for it. The four families that actually bit are, and they are the families a copy author reaches for.
- The one gap that was mechanizable AND load-bearing ships in this arc rather than filing anywhere: nothing enforced `CARD_SURFACED_LOG_ONLY` being a subset of `WARNING_CARD_COPY_CODES`, so a card-surfaced code could have its copy escape every copy gate while still rendering to Doug. Round 3 found it by probing a claim the spec itself made. It ships as AC-V11, a structural defense taken at first occurrence.

**Judgment:** three findings were genuine design questions no gate could have caught. Round 1's "the anchor witness is not a near-miss" required knowing that `Venu Notes` is recovered by the venue parser's fuzzy path while `Diagrams?` is not — a fact about which of two similar-looking labels the parser consumes, and the difference between a real assertion and a vacuous one. Round 2's dependent-claim sweep required knowing that a registry's SIZE is asserted in eleven places of prose. Round 4's `diagrams` finding required reading a comment that contradicts the set it annotates. All three are what a reviewer is for.

**Infra:** none. No dispatch was reaped, no `no_verdict`, no wrapper fault across four rounds. The reaper gated correctly throughout (`gated: N live codex tree(s) active; no orphans`), which is the repaired-hook behaviour working as documented.

**The procedural lesson, stated as an ordering rule rather than a regret.** Author operator copy LAST, from a payload probe, never from the spec's own account of what the code does. All four false drafts came from writing a sentence that described the intent and then looking for confirmation. The tell was available from round 1: the near-miss spec's §2.1 already said `venue.contact_info` is "a field `parseVenue` never writes", and this spec quoted that sentence while carrying copy that contradicted it.

**Self-inflicted costs, recorded because they were mine and not the reviewer's.** My own evidence was vacuous twice. `.probe/finding-repairs` read `r.venue`, a key the parse result does not have, printed `undefined`, and verified nothing while the spec cited it as proof. A fixture glob swept in a `README.md` and reported 18 fixtures where the harness has 17. Separately, a probe importing from the test tree turned the whole 30,000-test suite red locally, because `tests/components/admin/sheetIconLinkContainment.test.ts` walks the filesystem and `.probe/` is untracked but still on disk — the same hazard `.gitignore` documents for the mutation shard scratch. The probe-mini-review rule exists for exactly this class and I applied it late. Probes feeding this spec now assert the keys they read before reading them, and derive their own fixture list rather than importing one.
