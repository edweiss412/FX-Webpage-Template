# fix/bare-typeof-id-guards — base 60dece4d5722

## diff — 5 rounds

**Examined:** five diff rounds at this base, the only base of this branch — no rebase and no mid-arc merge of `origin/main`, so the arc's cross-base sum equals this file's count. The subject was a probe of all 55 id-like `typeof`-string guards under `lib/`, four sabotage-verified pin-test commits under `tests/auth/picker/`, and one `BACKLOG-archive.md` entry recording the disposition. **No production code changed in any round.** The ledger row this arc closes (`BL-BARE-TYPEOF-STRING-ID-GUARDS`) was filed `Reachability: INFERRED, NOT PROBED`; the probe refuted it, so the deliverable became the disposition document plus the coverage the probe found missing underneath the safe sites.

Rounds 1 through 4 returned 1, 2, 1 and 2 findings; round 5 was a past-cap VERIFY-ONLY round, authorized to ask one question — do round 4's repairs stand and do the two mechanically-closed classes hold at head — and it returned APPROVE with zero findings. **Every finding in all three rounds was an accuracy defect in that prose artifact.** Not one reached the guards, the pin tests, or a verdict — round 2 affirmatively confirmed the totals reconcile and the pins are non-tautological, and round 3 affirmatively confirmed the pin tests carry no admissible defect. The review's whole yield was: is the evidence document true about the tree?

**Mechanizable:** the citation class, closed as a derived cover rather than an enumeration. The entry carries 67 `file:line` citations and rounds 1-3 spent findings on six of them being off by one or two lines. The check extracts every backticked `path:line` (and `path:line-line`) from the entry's own text, resolves the path against a basename index (the entry cites basenames in prose where the full path appeared earlier in its row), and asserts the line is in range. Because it ranges over whatever the entry says, a citation added later is covered by default. It caught two of round 2's six defects before round 2 reported them.

Its limit, stated so it is not oversold: **in-range is not semantically correct.** A citation can point at a real line saying something other than what the prose claims, which is exactly what several findings were — `warningFingerprint` genuinely exists at the cited line and simply never consumes `roleToken`. That half stays reviewer judgment.

declined: LIM-FOREIGN-CITATION-DRIFT, whose shape statement already names this mechanization's exact limit — the check establishes that a cited line is IN RANGE and never what is AT it. The in-range half shipped here as a derived cover over the entry's own text; the semantic half is that slug's, and it is not mechanizable by the same means. No ledger row: under the 2026-08-25 process-mint freeze a process-facing row needs `invariant` or `product-blocked`, and neither holds — this arc was slowed, not blocked, and its done condition would be a property of the checker rather than a number outside the process.

Recorded for the index rather than acted on here: that slug's stated re-file trigger is "a second arc charged a review finding for a drifted foreign citation that produced no lint signal", and this is that second arc — six off-by-one locators across rounds 1-3 and a mechanism claim in round 4, none of which raised any signal. Whether the fired trigger earns a row is the ledger owner's call, not this filing's, and the two shapes are not identical: that slug is drift when a merge base moves, this arc's was wrong-on-authoring in a new document.

**Judgment:** the re-file trigger, and this is the finding worth carrying to other arcs. Every one of the three rounds produced exactly one instance of one class: **a trigger that is already true, so it fires on arrival.** Such a trigger is worse than no trigger — it pages a future arc at a site that cannot exhibit the defect, and a reader burned once stops trusting the other four.

The instructive part is the failure mode of the repair. Round 2's fix for rounds 1 and 2's instances introduced round 3's, and it did so because each repair was written to satisfy the reviewer's specific objection rather than checked against the tree. Only after round 3 did the arc stop patching instances and sweep all five triggers with one question — is this met TODAY? — which found **three more**, one of them a plain factual error the three rounds had all read past: residue 1 said "both current callers" where `buildShowReturnUrl` has seven call sites, five of them safe by an entirely different guarantee (route dynamic segments cannot be empty).

So a trigger names a **property**, never a function, and the sweep is the unit of repair, not the instance. The sweep is not a clean win either, and round 4 is why: its repair to residue 4 asserted that two empty-`objectId` stubs "share one cache entry today", which is false — `sourceKey` is `next/image`'s `src` identity that every loader ignores plus a per-instance reset comparison, not a shared cache. That claim was carried from a subagent's summary and never read at the consumer, which is the same defect as citing a line without opening it. Four rounds, and the last one still caught the arc asserting a mechanism it had not verified. Recorded as a limit rather than a mechanization because "is this condition already true?" is not decidable from the text — it took reading five different consumers.

**Infra:** none attributable to the wrapper; all four dispatches returned a verdict with no `no_verdict`, no reap, and no recovery. One local `tests/docs` run went 3-red and then passed twice on a byte-identical tree. The arc first blamed slot contention, then retracted that in favour of its own unrequested full local suite taking the shared Postgres concurrently with another arc's granted battery — and then corrected the retraction too, since docs guards touch no database: the suite supplied the LOAD, and the load tripped a latent timeout in `_metaReviewRoundEconomy`, which runs at ~29.6s against a 30s default on a busy machine (measured 9.66s idle on this branch). The rogue suite was killed leaves-first with no orphaned worker and no reparented heavy-slot holder, and the arc ran no local full suite afterwards, using CI's `unit-suite-db (1..8)` and `unit-suite-nodb (1..3)` legs as its readiness authority. That is arc discipline, not infrastructure.

**Why five, and what the fifth was for:** the finding rate never decayed across the four open rounds (1, 2, 1, 2), but the CLASS never moved off "the prose describes the tree imprecisely" and never once reached the code — no round produced a production defect, and rounds 2 and 3 affirmatively confirmed the verdicts and the pins. The bound was closed from round 1 and the probe domain was finite, so the cap was correct and round 5 was asked for before it was spent, scoped to repair-regression only. What was actually converging is visible in who found what: by round 3 the arc was finding the instances before the reviewer reported them, and the sweep that preceded round 4 closed both classes rather than a fourth instance. Round 5 confirmed that in one pass, zero findings.

The honest counterfactual: had the trigger sweep happened after round 1 instead of after round 3 — the class-sweep rule says to sweep on the FIRST finding, not the third — this arc plausibly closes in two rounds rather than five. Three of the five rounds were spent discovering that a class was a class.
