# Review rounds — `fix/fitwithinclip-stale-clip-subscription` @ `4cb585b3508a`

Row: `BL-FITWITHINCLIP-STALE-CLIP-SUBSCRIPTION`. Spec: `docs/superpowers/specs/admin/2026-08-27-fitwithinclip-clip-subscription.md`.

## spec — 4 rounds

**Examined:** four spec rounds at this base, which is also the arc total: no re-merge moved the merge base, so there is one `<baseSha12>` file and the cross-base sum equals this file's count. Ten reviewer findings across the four rounds (4, 3, 2, 1), every one accepted, none refuted. Two further defects were found by the arc's own pre-round-4 claim audit rather than by a reviewer. Subject was a design decision on a React hook's subscription path, roughly 230 lines of spec against a 226-line surface with one live consumer.

**Judgment:** eight of the ten reviewer findings are one vector, and naming it is the whole value of this filing: **a claim quantified over a domain it does not hold on.** Not a wrong mechanism, and never a wrong decision. In every instance the design was right and its statement was too strong.

**The vector closed, and the round-4 finding is the evidence.** Round 4 returned one finding on an axis none of the first three touched: `observe(target)` defaults to the CONTENT box while the cap is computed from two `getBoundingClientRect()` reads, which are border-box quantities, so padding toggled on an auto-height ancestor moves the cap and delivers nothing. Pre-existing since the hook was written, repaired in-branch because the fix is one options object at the exact line this arc was already rewriting. Findings ran 4, 3, 2, 1 and the last one was somewhere new: that is what convergence looks like, as distinct from a flat rate on one axis, which is what the corpus records for the arcs this cap exists to prevent.

The instructive part is what the repairs did to the round count. Rounds 1 and 2 were repaired by ENUMERATING: aliasing families, then a longer list of families with their costs. Round 2 found the round-1 list missing a member and miscounting two others. Round 3 found the round-2 replacement, a claim characterizing exactly when the rejected algorithm diverges, non-exhaustive in turn. Each repair was a bigger target than the thing it replaced, which is the ratchet `AGENTS.md` describes under repair direction, arriving here in a prose spec rather than in a recognizer.

Round 3's repair broke it by NARROWING, and round 4's silence on that axis is the check that it worked. The spec stopped characterizing the failures of an algorithm it does not use and argued the soundness of the one it ships instead: the observer's state is a set keyed by element, the reconcile is computed from that state, so the held set equals the desired set by construction and both acceptance properties follow with no case analysis at all. The enumeration became an explicitly non-exhaustive illustration. Whatever round 4 returns, that is the shape that should have been reached at round 1, and the tell was available then: correctness rested on a list being complete, and a list's completeness is exactly what the next reviewer is paid to break.

Second judgment note, smaller. Three of the nine findings were bound or limit statements that were too strong rather than false about behaviour: an unsatisfiable consequence bound (R1), a limit understating its own width and its closure (R2), and an invariant with an unstated coordinate-space precondition (R3). Writing the strongest defensible claim reads as rigour and is the opposite: a bound no design can meet fails every design forever, and it cost a round each time.

**Mechanizable:** one, measured inside this arc rather than imagined.

R1 finding 4b was that a documented limit claimed the live consumer passes `reapplyKey` to recover from an overflow change, while the consumer's own comment says it corrects the scale-95 entrance measurement. **`spec:lint` had already flagged that exact line, in the round-1 dispatch, as `ADVISORY CITATION_SYMBOL_ABSENT 116:236 ... identifiers: reapplyKey ... absent from components/admin/showpage/AttentionMenu.tsx`.** The arm was right and specific, naming the symbol and the file. It was read as locator noise and dispatched anyway, and it came back nine minutes later as a paid review finding.

So the mechanizable gap is not a missing arm, it is the arm's standing. `CITATION_SYMBOL_ABSENT` fires when a sentence names identifiers absent from the file it cites, which is the precise shape of "this citation claims more than its line says", and this arc has one confirmed true positive against it. The cheap change is a dispatch-time rule rather than a new detector: an advisory of that class is dispositioned in the brief before the dispatch, one line each, saying why it is noise. This arc's other advisories of the same class were genuinely locator granularity, so the rule costs a sentence per advisory and would have caught a finding at zero reviewer cost.

declined: not filed as a ledger row. The repair is a dispatch-time habit, disposition each `CITATION_SYMBOL_ABSENT` advisory in the brief before sending, which is process-facing work whose done condition is a property of the tooling rather than a number a product arc would notice moving. The 2026-08-25 process mint freeze routes exactly that to the owning surface's documented limits, and this filing is that record. It is also already actionable without a row: the rule costs one sentence per advisory and this arc has the true positive that motivates it. Re-file trigger, if the freeze is lifted or a product arc is blocked by it: a second measured instance of a `CITATION_SYMBOL_ABSENT` advisory that a dispatch dismissed and a reviewer then charged as a finding.

Nothing else here is mechanizable, and it is worth saying so plainly rather than padding. "This bound is unsatisfiable by any design", "this derivation is not exhaustive", and "these two sides are in different coordinate spaces" are semantic judgments about a design. No lint arm reaches them.

**Infra:** the reviewer could not run the deciding suite in rounds 1, 3 and 4. Its read-only sandbox denied Vitest's temporary client and SSR directory creation (`EPERM`), and it correctly declined to infer a test result from that failure, settling those rounds on file traces and platform specifications instead. Worth recording because the findings were sound anyway, so the sandbox limit cost nothing here, but a stage whose findings depend on observed test behaviour cannot be settled this way and should not be dispatched expecting it.

## plan — 4 rounds

**Examined:** four plan rounds at this base, which is also the arc total for this stage. Nine reviewer findings across the four rounds (3, 3, 2, 1), six of them P1, every one accepted and none refuted. Subject was a five-task TDD plan against a 226-line hook with one live consumer. Combined with the spec stage this arc spent eight review rounds and nineteen findings before its first line of implementation, which is worth stating plainly even though every finding was real: the rounds were not wasted, but the ratio is the number a reader should weigh.

**Judgment:** the plan stage had a different vector from the spec stage, and the difference is the useful part.

Round 4's single finding is the tidiest illustration of the whole arc and belongs first. Round 3's repair corrected the false termination claim in two places and STRANDED IT IN TWO OTHERS, which round 4 found. The repair had been keyed to the sentences round 3 quoted rather than to the claim, and the corpus rule for this is already written: re-run the consistency sweep across the WHOLE document after each round's repair, taking the cover from the claim's terms and not from the words the round happened to name. Grepping `thrash|time out|terminat` over the document takes seconds and would have caught both survivors. The plan now also states where such claims are allowed to live, so a fifth copy has nowhere to hide.

The spec's vector was claims quantified over a domain they did not hold on. The plan's was **claims about what a test artifact proves.** Three consecutive drafts told three different stories about what the Playwright suite established, and all three were wrong. Draft one said no unit stub could deliver an initial observation, so the browser suite carried AC-8. Round 1's own repair then introduced a stub that does deliver, and I left the contradicting sentence standing, which round 2 caught. Draft three said the browser suite proved termination because a loop would thrash and time out, and round 3 refuted it in one line: a per-frame loop rewrites the SAME cap, and the settle helper compares two geometry samples 80ms apart, so stable geometry and a spinning loop look identical to it.

What broke the sequence was not a better story. It was declining to tell one. The plan now says the suite is a regression gate on the live consumer's geometry and nothing more; task 5 cites the criterion it actually exercises; and AC-8's platform model is declared an unmeasured citation to the Resize Observer specification's broadcast step, with its exposure written out (a wrong count on a transition no shipped consumer takes, never a stale cap, and termination safe because the loop REQUIRES delivery). The pattern matches the spec stage's: three rounds of elaborating a claim, then one round of narrowing it, and the narrowing is what held.

Second note, smaller and sharper. Round 2's P1 was a declared gate that collected zero tests, because the command omitted `--config tests/e2e/standalone.config.ts` and `popover-clip-fit` is in that config's `testMatch` and nowhere else. The suite's own header states the required invocation. I read the command and did not run it, in a plan whose own rules say a declared gate is verified by running it. The class-level repair is in the plan as a derived cover: grep every declared command, run each, record what it did.

**Mechanizable:** one, and unlike the spec stage's it is a missing arm rather than an ignored advisory.

`spec:lint --exec-red` already derives a `vitest list` probe to settle whether a vitest-shaped `red=` can COLLECT the tests it claims to observe, precisely so a red that exits non-zero for a collection reason is named rather than believed. **No equivalent probe exists for a Playwright-shaped command**, and this arc has the incident: a declared gate that collected `0 tests in 0 files` while reading as a regression gate, caught by a human-equivalent reviewer at round 2 rather than at authoring time. The check is the same shape as the one already shipped, `playwright test --list --config <cfg> <file>` and a zero-collection verdict, and Playwright commands in this corpus almost always name a config because most e2e specs here are allow-listed by `testMatch`.

declined: not filed as a ledger row. This is process-facing work whose done condition is a property of the lint tooling rather than a number a product arc or a human would notice moving, which the 2026-08-25 process mint freeze routes to the owning surface's documented limits, and this filing is that record. Re-file trigger, if the freeze is lifted or a product arc is blocked: a second measured instance of a Playwright-shaped declared command that collected nothing and was caught by review rather than by lint.

**Infra:** the reviewer could not run Vitest in any of the four rounds, nor the heavy-wrapped Playwright commands, because its read-only sandbox denied temporary directories, the IPC socket, and the semaphore lock. It did successfully run direct `playwright test --list` under both configs in round 3, independently reproducing the 0-versus-34 numbers, and it ran both shell `red=` gates and found them red. So the sandbox permits direct collection probes and shell gates but not wrapped runs or Vitest, which is worth knowing when deciding what a plan-stage dispatch can settle: RED validity for a shell gate is checkable by the reviewer, and unit behaviour is not.
