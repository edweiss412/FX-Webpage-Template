# Review-round economy filing — fix/changes-feed-batch-flake @ 6e297799e6a0

## spec — 4 rounds

**Examined:** all eight findings across R1-R3 (R4 = APPROVE, 0). R1: recovery annotations invisible on green runs (list reporter prints none; artifact upload is failure-only); neither-locator timeout left as Playwright's bare message, missing the server signature the consequence bound requires; AC-1 claimed the recovery branch was "exercised structurally" when nothing executes it; the proposed `code` log field collided with the §12.4 telemetry-code slot that `tests/admin/readShowReviewSnapshot.test.ts` pins as absent. R2: the enriched-timeout branch had no test until the reviewer located a deterministic trigger (nonexistent slug → loader missing-show redirect → both locators starve); `pgrstDetails`/`pgrstHint` were specified but never asserted; the collector's read-location semantics were undefined against Playwright's dual serialization of runtime annotations. R3: the print seam — a correct collector whose rows the CLI never prints keeps every recovery invisible on exactly the runs that matter.

**Judgment:** every finding was the same species — a gap between an AC's prose promise and its executable instrument — and every repair was naming the instrument, its red condition, and its fixture semantics inside the AC itself. None required a design change; the root-cause analysis and repair shape survived all four rounds untouched. The one genuinely generative reviewer contribution was the dead-slug deterministic trigger for the timeout branch (R2 F1), which converted a documented limit into a tested path and grew the spec's floor from 6 to 8 cases.

**Mechanizable:** none — "AC names no test instrument" needs semantic judgment about what counts as an instrument; `spec:lint`'s citation and task-contract arms already cover the mechanical residue.

## plan — 5 rounds

**Examined:** fourteen findings across R1-R5, none surviving unrepaired. R1 (5): the enrolled Task 3 command was never run verbatim; the diagnostic regex omitted the boundary testid; three concrete escapes in the annotation-print fixture (flat suites hiding the recursion, an all-one-type fixture hiding the filter, no title assertion); count-bearing comments left unreconciled (nine specs, 77 executions); two bookkeeping errors (duplicate README row step, the surviving BACKLOG umbrella paragraph). R2 (2): an unresolved-import RED — invalid by construction — replaced with a guard-less stub whose absent slug guard is the named production line; the graduation lacked its `BACKLOG_GRADUATED` red instrument. R3 (3): the staged diagnostic red was optional rather than the default path; the graduation red and green ran different commands; the `Last reconciled:` line's verbatim exemption row would strand. R4 (2, BLOCKING): the graduation cluster sat before the five-green loop, violating the marker-lives-as-long-as-the-work invariant; six playwright-transitive commands ran outside the semaphore. R5 (4, BLOCKING, at cap): the enrolled marker string itself was the one unwrapped command; the two filed-by-this-arc BL ids were already dangling on the live branch (probed: the referential-integrity guard WAS red, and filing the entries in the repair commit turned the same command green); the whole-diff review predated the graduation commit (review must cover what merges — a delta review step now follows the final commit); and the final run's AC-5/AC-6 accounting had a rerun loophole and a stale-report window.

**Judgment:** rounds 1-3 were the same species as the spec stage — instrument gaps between step prose and executable observation — but rounds 4-5 were a different vector: closeout choreography (marker lifetime, review-covers-what-merges, semaphore discipline), where each repair moved a step and thereby created the next ordering defect. The R5-F2 probe was the round's real yield: the branch was live-red on its own dangling citations, a defect no amount of plan prose would have surfaced. Repairs to all four R5 findings were applied at the cap; the arc fenced rather than dispatching a sixth round, per its brief.

**Infra:** none — every dispatch returned a verdict; no reaper kills, no `no_verdict` results.
