## diff — 4 rounds

**Examined:** R1-R4, 7 findings (1, 2, 4, 0). Every finding was accepted and repaired; none was disputed. The arc is a docs-only demotion of `BL-SEVERITYLESS-WARNING-DROPPED-IN-PARSER-FILTERS`, and the rounds were spent almost entirely on whether the evidence for demoting was true, not on what to build.

**The rounds changed the answer, twice.** Round 1's probe reported a clean zero. Round 2 named a population it had never read (`sync_log`), and reading it found 198 severity-less elements, so the zero was wrong. Round 3 then showed the replacement argument was wrong too: it claimed a code arm rejects those elements, when both read sites short-circuit on severity BEFORE their code test, which the positive control demonstrates with a severity-less `FIELD_UNREADABLE` that is a member of both gating sets and is dropped today. What survived is narrower than either earlier claim: empirical absence over an `information_schema`-derived population set, plus a trigger that detects its own condition by code. That is worth recording as the shape of the cost. Three of four rounds attacked the evidence for a "nothing to do here" conclusion, and each one was right to.

**Mechanizable:**

- **A stated numeric table with no command that produces it** (R3, finding 2: the published SQL could not produce the published table, and a bare `group by` silently dropped an empty population instead of showing zero). Indexed as `LIM-NUMERIC-TABLE-PROVENANCE`, whose stated trigger is "a spec whose stated table cannot be reproduced from its own commands reaching a review round again". This arc is the FOURTH independent naming, so that trigger has fired, and the class is filed as `BL-SPECLINT-NUMERIC-TABLE-UNREPRODUCIBLE` under the process freeze's `recurrence` exception.
- **A measurement quantified over "every X" whose X-set is hand-listed rather than derived** (R1, R2 finding 1, R3 finding 1: the probe's population set missed `shows_pending_changes`, then `sync_log`, then `pending_ingestions`, one per round, each arriving as a fresh finding rather than as one class). declined: LIM-HAND-LISTED-POPULATION-SET, a documented limit at one arc, this one; the in-arc repair was to derive the column set from `information_schema` and write the re-derivation into the re-file procedure, and the class needs two more namings before a row is admissible under the freeze.
- **A declared check whose command cannot decide the condition its prose states** (R2 finding 2, then R3 finding 3 after the first repair: the re-file trigger required a severity-less element carrying a gating-set code, while the published query returned only a count, then only counts by population). declined: LIM-GATE-EVIDENCE-SHAPE-ONLY, whose shape is the same at altitude, a declared check reporting on something other than what it claims to decide; this arc's sub-form is a documented limit's re-file trigger rather than a plan's gate command, and it is recorded there rather than given a twin slug.

**Judgment:** the disposition itself, twice. The orchestrator ratified demotion on a clean zero, and again on the qualified zero after round 2 made the first ruling's premise false; round 3 then moved the orchestrator's own stated basis a second time. None of that mechanizes. So does the call that a repair moving counts on three pinned suites, with no reachable divergent element, is what the filing bar demotes rather than ships.

**Infra:** one self-inflicted tear. The absence-with-detection framing was applied to the working tree while round 4 was in flight, and `codex-guard` reads the live tree rather than the commit named to it, so R4's verdict attaches to `a5c61c68e` and not to the head that carries that paragraph. Disclosed rather than absorbed; the orchestrator ruled that verdicts attach to heads, accepted R4 for what it reviewed, and pre-authorized one scoped round over the cap covering only the delta. The rule that would have prevented it already exists: freeze the worktree for the duration of every dispatch.
