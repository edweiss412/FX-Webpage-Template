# fix/quick-wins-2-mech — review-round filing

Base `d2a31e4aa021`. Corpus: `d2a31e4aa021.jsonl`.

## diff — 6 rounds

**Examined:** every dispatch on this branch. Three tight-scope briefs per round (guards / resolver / sweep+docs), split from the start per the AGENTS.md large-diff rule. Round 1 returned 8 findings; round 2 returned 3, plus one dispatch killed by the wrapper's own timeouts (`no_verdict`, which cleared nothing); round 3 returned 6 + 3, with the RESOLVER scope closing at APPROVE and never reopening; round 4 returned 4; round 5 returned 3; round 6 returned 2 and BLOCKING. Twenty-four findings, every one probe-backed, none refuted. Every finding across all rounds arrived with a probe and every one was real — none was refuted.

**Mechanizable:** the dominant class was **a guard whose fixture or probe shares an assumption with the mechanism it checks**, and it is mechanizable in the sense that it has a stated repair shape: hold the fixture's constants apart from the scanner's, then assert the two AGREE. R1-1 shared roots/extensions/token spellings (four mutants each left all tests green); R2-1 shared the parser grammar; R2-2 shared a root-scope assumption; R2-3 shared a single-viewport assumption. My own sweep, run instead of waiting for a fourth instance, found the largest one: the fixture never exercised the LIVE walker at all, so a `git ls-files` returning nothing would have reported zero offenders and passed.

The second mechanizable class was **premise placement**: a premise that validated a separately-built stand-in rather than the call (R1-3), a row that could execute zero assertions (R1-2), and an object member that vanished silently instead of failing (R3-3). `docs/agents/writing-plans.md` already names all three shapes. I cited that rule in a file header and then wrote two of them anyway, which is the honest measurement: the rule was known and not applied.

**Judgment:** two mechanism deletions and one guard deletion, each taken at the point the pattern became nameable rather than after another round paid for it.

Round 3 is where the economics changed and the call was to STOP WIDENING. All six of its guard findings were "the recognizer does not recognize enough" — round 2 had taught the gap extractor about `min-[720px]:gap-0`, and round 3 handed it four more spellings (`gap-x-0`, `md:hover:gap-0`, a semantic-valued variant already in this corpus, and an intermediate collapse restored at the far endpoint). Widening a third time buys the next round's spelling, because "which Tailwind utilities win at this width" is an open question about a compiler.

So the recognizer was DELETED. The AST now answers only a closed question — which element, and what does its className literally say — and the whole class string goes to the real engine at every declared viewport. All four escaping spellings die against a mechanism that knows none of them exist. That is the round-economy rule's own prescription (prefer deleting or deriving over widening) applied at the third occurrence rather than the ninth, and it is the single highest-value decision on this branch.

The residual is fenced rather than chased: an ancestor that scopes `--spacing` at RUNTIME still diverges from the detached probe, and closing that needs the container mounted — the harness redesign the ratified scope excludes. Recorded as a documented limit with its reasoning, in the code.

Rounds 4-6 then ran the same play on the OTHER class. "A name is not an identity" recurred at uniqueness (R4-4), at name-plus-scope (R5-1) and at the identifier resolver (R6-1); round 5 replaced name lookup with use-site discovery, and round 6 showed that discovery needs a resolver which does not match TypeScript's own scoping. The prescribed repair was a checker-backed symbol graph — a static-analysis engine, inside a test, to pin nine constants, and a reimplementation of resolution the lint rule already performs.

**The guard was deleted instead (owner decision, 2026-08-10).** The nine constants are wrapped, `enforce-canonical-classes` traverses them, and `quality` is a required CI context — so drift fails CI with no guard at all. The behavioral evidence the guard proxied for is recorded in A3's commit. What is lost is a regression pin whose subject the rule already covers. Six rounds on a test whose product change was nine `cn(...)` wraps is the measurement that argument rests on.

**Infra:** three separate infrastructure faults cost real wall-clock and none of them was a review outcome.

1. A round-2 dispatch was killed by the wrapper's timeouts (`attempt_timeout` then `total_timeout`) and returned `no_verdict`. I diagnosed that correctly, then wrote "came back clean" into the closeout anyway — the round-3 docs review caught it. `no_verdict` clears nothing, and the failure mode is writing the opposite down.
2. A round-3 re-dispatch never ran at all: `ATTEMPT_MAX_SECS exceeds max 1380: 1800`, a usage error sitting in `dispatch.log` while I waited ~80 minutes on three empty output directories. Exit 2 is documented as a usage error; reading `dispatch.log` immediately after dispatch would have cost seconds.
3. Local full-suite runs showed 3-6 failures that all passed in isolation and passed in CI — sibling-worktree DB contention plus 40+ concurrent codex processes, the documented hazard. CI stayed the authority.
