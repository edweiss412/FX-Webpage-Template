## diff — 5 rounds

**Examined:** R1-R5 at this base, and 7 whole-diff rounds across the arc (R1-R2 at `53a1fc82fb36`, R1-R5 here after `origin/main` was folded in). Counted as distinct `(baseSha, round)` pairs, which is what crosses the threshold; the base moved mid-arc because #952 merged at 07:29 and turned this PR CONFLICTING with zero CI runs.

**Mechanizable:** one, parked as `LIM-WORKFLOW-PATHS-TEST-IMPORT-GAP`. R2's third finding was that `.github/workflows/step3-live-bundle.yml` RUNS the draft-restored-note cases while its `paths:` filter did not name the component they exercise, so a note-only edit would have got no Chromium coverage with the gate sitting green. That one is derivable: for each workflow carrying a `paths:` filter, every component transitively imported by the specs it runs should appear in that filter. It is not filed as a row -- it is process-facing, its done condition is a property of the tooling, and the 2026-08-25 freeze sends exactly that shape to documented limits instead.

declined: not filed as a row, and the slug is the disposition rather than a placeholder for one. It is process-facing, so the 2026-08-25 freeze applies, and it fails that freeze's admission test on its own terms -- the done condition would be a property of the import walker (does it resolve every edge), which an adversarial round can refute once per round and which therefore never finishes. It earns a row when it blocks a product arc: a component edit that reaches `main` with its workflow green and its cases unrun, with that arc named in the `**Incident:**`. The instance R2 found is repaired in this diff.

The earlier "none" was written before R2 and was true of R1's classes only.

The completed diff rounds declared **26 findings across 7 rounds** -- 3, 3, 3, 4, 4, 4, 5 (R1 BLOCKING and R2 NEEDS-ATTENTION at `53a1fc82fb36`; R1 through R5 all BLOCKING here). Read from the corpus, and this line has now been wrong TWICE in the same way: an early draft said four, counting only the ones I remembered repairing, and the version R3 reviewed said "9 findings, three per round" because I wrote it during the R2 repair and left out R2's own four -- while the **Examined:** line directly above it already counted that round. R3 raised it as a finding (P1) and it was independently re-derived here minutes earlier by sweeping my own artifacts for stale numbers. A tally is not a fact about the world, it is a fact about the corpus, so it is now taken by counting rows rather than by writing a sentence. None of the classes below is reachable by a rule that could be written today.

- Two findings were the SAME class-sweep gap at different depths: Decision 7 applied to the wizard twin without the repairs the decision made necessary on the published pill. A linter can see two files sharing a class recipe; it cannot see that a ruling on one implies a repair on the other, because the implication ran through a design decision made at 07:19 that day.
- One finding was a FALSE-GREEN oracle: `T-ALERT-CAP` stripped `.sr-only` and not `max-sm:sr-only`, so it read `99+ issues` from a detached clone while the phone rendered `99+`, and asserted the wording the ratified fence forbids. A grep for `.sr-only` near `cloneNode` would have found this one instance and would be noise everywhere else; the general form (does this oracle's extraction match what the page renders) is the thing, and it is not expressible as a pattern.
- Round 1 at the old base REFUTED a repair rather than finding a defect: an `AttentionMenu` adoption branch reasoned from `offsetParent` being null mid-entrance, which is false because `opacity-0 scale-95` keeps an element in layout. The brief had told the reviewer the repair was unobserved and that refuting it would be the round's most valuable outcome. That is a brief-authoring technique, not a rule.

- **R2 found the defect R1's own repair created**, and R3 then found the defect R2's repair created. One axis, three rounds: R1 gave warnings a hollow ring, which collided with monitoring's existing ring; R2 made it filled-and-ringed, whose ring measures 1.179:1 against its own fill. Each repair was locally correct and each opened the next round. What finally closed it was changing the CHANNEL rather than tuning it again -- geometry, where a square is a square in both modes and to a colour-blind reader, with no contrast token in the loop. No rule catches this; what catches it is noticing that the third round is on the second repair.
- **Three of R3's four findings were guards that accept what they exist to reject.** The `display: contents` gap is the sharpest: three widened text walkers treat a zero client rect as hidden, and an element with `display: contents` generates no box while its children render normally, so a one-class edit from `max-sm:sr-only` to `max-sm:contents` made the noun visible and every counts-only guard still passed. Proven by mutation in both directions rather than argued. The class is "the oracle's extraction does not match what the page renders", which is the same class as R1's `.sr-only` strip -- and it is still not expressible as a pattern.

- **R4 found the repair for R3's P0 unswept, which is this arc's signature failure a fourth time.** The invariant-8 gate's P0 gave the published pill a per-segment mark; the wizard twin did not get one, so its composite pill still identified the judgment segment by position alone. The same shape as R1's finding (Decision 7 applied to the twin without its consequences) and the same shape as the R2 walker sweep. The lesson that finally stuck is narrower than "sweep the class": the twin is not a copy, it is a SECOND INSTANCE with its own ground -- its segment ring needed `text-subtle` where the published one needed `status-positive`, because the wizard's composite plate is `warning-bg` and its judgment-only plate is `surface-sunken`. A sweep that copies the fix rather than re-deriving it on the new ground produces the next round's finding, which is exactly what the reverted ring repair did earlier.
- **Three of R4's four findings were guards that accept the defect they name**, and all three were mine, written in the previous round: `clip-path.includes("polygon")` accepts the four-point square this arc had just retired; `borderWidth` was READ and never asserted, so a ring with no stroke passed as a hollow circle; and the accessible-name proofs sampled nouns rather than requiring each one, so an alternation stayed green with either wizard noun deleted. Each was demonstrated with a mutant rather than argued, and each is now pinned by the mutation that broke it. The through-line for the whole arc: every oracle defect here was a predicate that was ADJACENT to the claim rather than equal to it.

- **R5 reviewed the STRUCTURAL repair and found five ways it was still a claim rather than a proof**, which is the most useful round of the arc because none of them was about the product. The mark builder was real; the guard around it was not yet. Its recognizer read quoted literals, so writing the mark as a template literal with an interpolation walked straight past it. Its contrast arm hard-coded one ink instead of reading the builder's output, collapsed the project's TWO dark blocks with last-wins, and ignored opacity entirely. It counted polygon points, so three COLLINEAR points passed as a triangle while painting nothing. And the plate parameter, the thing that made the ground explicit, was never checked against the ground the element actually paints on -- passing `sunken` for a mark on `warning-bg` type-checked, satisfied every structural assertion, and shipped a 2.793:1 ring.
- **The repair that mattered was changing what the guard reads.** It now identifies the plate from the RENDERED colour and does the contrast arithmetic on token tables, so a wrong argument is caught by what is on screen rather than believed. That also sidestepped a trap: toggling `data-theme` on these harnesses switches surrounding surfaces but not the pill's own plate, so a rendered dark-mode check asserts dark ink against a light ground -- a state no viewer ever sees. Every one of the five is now pinned by the mutant that broke it.

**Judgment:** The rounds were NOT spent on the same axis, which is why the count reached 7 rather than converging earlier. R1(old) refuted a mechanism, R2(old) corrected false prose inventories, R1(new) caught a design-decision footprint plus a false-green oracle, and each was a different class. The one thing that would have collapsed the count is upstream of review entirely: three of my own oracles failed at write time this arc (a clip test reading `scrollHeight` on an `overflow: visible` box, a mark test comparing `backgroundColor` that passed against its own target defect, and a P0 test whose two harness flags resolved to one state). All three were caught by asking what the assertion would report if the defect were present. Asking that at authoring time is cheaper than any round, and it is the habit this arc actually changed.

**Infra:** The base moved once mid-arc (#952 at 07:29), splitting the corpus. The split is visible here rather than papered over: rounds restart at 1 per base by design, and the arc-sum rule is what makes 2+2 oblige a filing that neither file would oblige alone. Separately, one CI cycle was lost to that merge because a CONFLICTING PR runs zero workflows while presenting two stale checks that read as a small green board — caught by comparing the check count against the 41 seen on the previous head, not by any signal the PR itself surfaced.
