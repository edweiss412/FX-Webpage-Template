# fix/shell-lexer-quoted-value-recall — base `4dfd784ed062`

## diff — 4 rounds

**Examined:** R1–R4, 14 declared findings (4 / 3 / 3 / 4), every round returning
BLOCKING. Subject: `tests/cross-cutting/psqlStartupFiles/scan.ts`, a shell recognizer
deciding whether a `psql` invocation suppresses its startup files. One deciding suite,
one enrolled mutation surface.

**Mechanizable:** `BL-MUTATION-SCORE-NONDETERMINISM` — two runs of the source-mutation
runner disagreed about one site (`relational-boundary:3578:35`) against byte-identical
declared inputs, four observations, three of four saying it survives. The row was
removed on a single stale-row report and had to be restored a round later. That is
mechanizable and it is filed with the reproduction.

A second mechanizable defect surfaced in round 4 and is filed here rather than as a
ledger row, because the fix is one line of an instrument this arc owns: **a
stale-number sweep reported "0 documents need attention" while three stale sites
stood.** The sweep checked a DERIVED value. The retired vector was 75 mutants / 27
rows and the shipped one is 74 / 26 — and `75 − 27` and `74 − 26` are both 48, so the
derived count was invariant across exactly the edit that changed its inputs. A
one-row-added-one-row-retired change is the compensating move that preserves a
difference. `declined: the general repair — sweep the inputs, never the difference —
is recorded in the registry comment at the defect site and in §6 of the design spec,
where the next author of that sweep will be standing; a ledger row asserting "make
sweeps check inputs" would be a process-facing mint with no incident beyond this one.`

**Judgment:** The dominant cost was not any single finding, it was the **axis**. Rounds
2, 3 and 4 each returned behavioural findings of one identical shape — an input bash
binds where the recognizer reports nothing — and each was one more grammar corner:
the `-` special parameter, then fd-0 redirection precedence, then `read`'s first-line
semantics, then literal newlines in an operand, a quoted `]` in an array subscript,
and `${…}` nested inside double quotes. The behavioural subset ran 2, 2, 3 across
those rounds and the declared totals ran 4, 3, 3, 4. **The rate never decayed.** Three
rounds of repair grew the recognizer and made it a larger target; the fourth was
declined. R4's three behavioural findings ship as documented limits 13–15 in the
design spec, each with its probe, the direction of the error stated plainly as a
NON-REPORT, its live-corpus count (0, 0 and 0), and a re-file trigger.

The brief is where this was decidable in advance, and it is where it went wrong. It
carried a consequence bound saying a conservative non-report plus a declared limit is
a documented limit, and a decision rule saying anything inside the six accepted
operators is a real defect in promised scope. The reviewer took the second, lawfully,
and aimed all three findings precisely at it — each finding argues in its own text
that it is *inside* the accept-set. Two clauses in one brief that partition the same
input space differently will be resolved by the reviewer in the wider direction every
time. AGENTS.md already names this ("a clause that omits the citation loses to
itself"); this arc is a second measured instance.

**Infra:** Every one of the four dispatches returned a verdict — no reap, no silent
death, no `no_verdict`. The wrapper's heartbeat and native-binary path held across
roughly 40 minutes of cumulative dispatch. Vitest could not collect inside the Codex
read-only sandbox in R2 and R4 (`EPERM` creating its temp SSR directory), so both
reviewers reproduced behaviour by importing the scanner directly and differentially
testing against `bash` — which produced sharper findings than a suite run would have,
and is worth copying deliberately rather than treating as a degraded mode.
