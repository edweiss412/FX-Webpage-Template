# Review-round economy — fix/mutation-shard-weight-seconds at d4324ba0a269

Diff-stage rounds. The spec and plan stages ran at an earlier merge base and are filed
separately at `300a9f937b8a.md`; rounds sum across bases, so this arc's total is 8 + 8.

## diff — 4 rounds

**Examined:** all 23 findings across four rounds and two surfaces, the repair each drove,
and which of them a mechanism could have caught before a reviewer did.

Findings per round: **7, 9, 5, 2** — 4+3, 3+6, 4+1, 1+1 across surfaces A and B. **All 23
were accepted; none was refuted.** The decay is real and is the first stage of this arc
where it happened: spec ran 4/7/6/4 and plan 12/8/7/8, both flat.

**What made the difference is worth naming, because it was not reviewer luck.** The diff
was split into two tight-scope dispatches from round 1 — 40 files and 8722 insertions is
the size at which whole-diff runs died silently on four consecutive PRs here — and each
brief carried the file list inline. Both surfaces then converged on the SAME underlying
class from opposite ends in round 3 (order assumed, never verified: argument order in the
report, run-id order in the fixtures), which is the signal that the scope was cut along a
seam the code actually has.

**Mechanizable:** three classes, two of which now have their mechanism in the tree.

1. **A plant anchor orphaned by the repair that moved the code.** This happened TWICE, both
   times to me, both times because a repair I had just made moved code out from under an
   anchor. The full sweep does catch it — ANCHOR-FAIL is a red run — but the sweep costs a
   vitest process per entry, so it runs at milestones and both orphans lived in that gap.
   `node scripts/mutation-weight-plant.mjs --anchors` resolves all 52 with no child
   processes at all, and CI runs it. **declined: the mechanism is in-branch and generalising
   it to other hand-authored plant lists is process-facing, which the 2026-08-25 mint
   freeze admits only under `Mint-exception: invariant` or when it blocks product work.
   Neither applies. Re-file trigger: a second arc loses a plant to a repair.**

2. **A guard with no premise on ITSELF.** With the plant corpus emptied, `--anchors` printed
   `0/0 resolve exactly once` and the sweep printed `caught 0, not caught 0`, both exiting
   zero — every ratio the script prints is satisfied by an empty numerator over an empty
   denominator, so deleting the corpus or any subset lowered BOTH sides and read as a pass.
   The backreference assertion I had written is what made `0/0` look convincing.
   `SELF_TEST_DEFECTS` already carried a nonzero premise and the real list did not, which is
   the more expensive of the two omissions. A floor now refuses it in both modes.
   **declined: `tests/_shared/premise.ts` already exists and this is that rule applied; what
   was missing was me applying it, not a mechanism to apply.**

3. **A fixture cited by an interpolated path cites nothing.** Building fixture paths from a
   loop index made three fixtures discoverable-by-accident rather than claimed, and
   `_metaOverlayConfigParity` refused them — correctly, and it is the only reason I noticed.
   **declined: the guard already exists and already caught it. Nothing to build.**

declined: all three, and none for lack of merit. The first is process-facing under a freeze
that admits process rows only when they block product work; the second and third already
have their mechanisms and were caught by them. Each carries its own re-file trigger above.

**Judgment:** the findings that mattered most could not have been mechanised, and two of
them were about the SHAPE of evidence rather than the behaviour of code.

- `driftReport` skipped an unusable observation before asking whether the surface was
  declared, so a surface that was both undeclared and unobservable appeared in NO category —
  the silent case, in the function that implements this design's own consequence bound.
- AC-7's only evidence was a score run, and a LOWERED score floor makes that run GREENER.
  The single most likely violation would have made the evidence look better. Seeing that
  requires asking which direction evidence fails in, not whether it passes.
- The immutability guard was branch-local and shipped permanently: it compares against
  `merge-base origin/main HEAD`, which is a property of an UNMERGED branch, and a hardcoded
  additions list turned a one-arc proof into a permanent registry freeze.
- `if: always()` on a consumer whose prerequisite lacked it. The condition was worthless
  without the download's, and the integrity test pinned only the consumer.
- Two runs omitting their modelled dump were both labelled with the checkout's HEAD and
  medianed as one sha, reintroducing the historical averaging this model exists to reject.
- Reversed run order passed BOTH reconciliations and inverted every cross-run block —
  plausible, wrong, silent.

**Infra:** two dispatches were refused before running, and the refusal was correct both
times: a round-1 `GUARD SURFACE:` line must carry its score, its zero-unaccepted-survivors
phrase and its operator set on ONE PHYSICAL LINE, and mine wrapped. No `no_verdict`, no
reaped runs, no retries across eight dispatches.

**What I would tell the next arc.** Split the diff at the first sign of size, and cut along
a seam the code has rather than by file count — the two halves finding one class from
opposite ends is what told me the cut was right. And when a reviewer hands you a surviving
mutant, plant it yourself before repairing: four of my repairs were wrong on the first
attempt in ways only the planted mutant showed, and two "fixed" cases still passed the
mutation they claimed to catch.
