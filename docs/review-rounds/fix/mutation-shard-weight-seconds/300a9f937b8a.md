# Review-round economy — `fix/mutation-shard-weight-seconds`

Base `300a9f937`. Rows in `300a9f937b8a.jsonl`; every count below is derived from that
file, not typed.

## spec — 4 rounds

**Examined:** the weight-model design at `docs/superpowers/specs/ci/2026-08-25-mutation-shard-weight-seconds-design.md`,
together with the instrument every figure in it comes from — `lib/mutationWeight/{records,weights}.ts`,
`scripts/mutation-shard-weight-report.ts`, `scripts/mutation-score-surfaces.ts`, the
suite `tests/mutationWeight/instrument.test.ts`, the plant harness
`scripts/mutation-weight-plant.mjs`, and the two registry rows this arc enrols.

Findings by round: **4, 7, 6, 4** — the first three BLOCKING, the fourth
NEEDS-ATTENTION. **Every finding in all four rounds was correct; not one was
refuted.** That is the most useful thing in this record. These
rounds were not a reviewer dripping instances — they were a document that kept being
wrong the same way, and the count fell only when the mechanism below was built.

**The dominant class is a single class across all three rounds: the artifact did not do
what the document said it did.**

| round | the claim | what was true |
| --- | --- | --- |
| 1 | reconciliation refuses a cross-tree comparison | it printed the comparison anyway |
| 1 | "one command prints every figure" | five figures had no generator |
| 2 | reconciliation is total over both surface sets | it intersected them, so an enrolment was invisible |
| 2 | the drift report names every surface | it printed eight rows and cited a fuller output that did not exist |
| 2 | the seed medians within one sha | no sha was carried, so it could not |
| 2 | `--emit-registry` refuses a partial table | it did not exist |
| 3 | the enrolled module's logic is decided by its suite | three helpers were never called by it |
| 3 | reconciliation proves one tree | the third input to the boot formula had no check |
| 3 | an author can read the first rate off a record | the layout and the arithmetic were both missing |

**Mechanizable — and the mechanism is in the diff.** `scripts/mutation-weight-plant.mjs`
turns "the suite asserts X" into "a defect in X makes the suite red". It went 15 → 26
planted defects across these rounds, and it is the reason round 4 could be entered with
the class closed rather than narrowed. Three findings were caught by it BEFORE the
reviewer reached them, and three defects it exposed were in the SUITE rather than in the
code — including a tie-break fixture that passed under the exact defect it named, and a
median fixture using two values, for which mean and median are always equal.

**The generalisable rule, and it is cheap:** a suite covering an instrument whose output
feeds a spec is not finished when it passes. It is finished when a planted defect in
every claim it makes causes it to fail. Authoring the plant list beside the first
assertion would have moved most of rounds 2 and 3 into round 1.

**Judgment — not mechanizable, and settled by measurement rather than argument.** The
four-way design fork; the choice of the BINDING LEG over the spread as the criterion,
which the data forced when the spread got worse while the longest leg got shorter; and
the decision to EXCLUDE a surface no seed run has measured rather than synthesise a rate
for it.

**Round 4 is where the shape changed**, and it is worth recording why rather than
just that. Its four findings were all small and all of one kind — an assertion with no
plant behind it, two medians that picked the upper-middle value for an even population,
an acceptance criterion left carrying a bound the design section had already retracted,
and a line of report text contradicting the code beneath it. None touched the design.
The verdict moved from BLOCKING to NEEDS-ATTENTION on the same class of finding,
which is what convergence looks like here: the class did not disappear, its instances
got cheap.

The median defect is the one to generalise from. Three private copies existed and two
were wrong in the same direction, while the third — inside `seedRates` — was right. A
reviewer found the two wrong ones; nothing in the suite could, because the fixture for
one of them had a single-surface population where every statistic collapses to the same
number. **The repair was not to fix two call sites but to delete them: one exported
`median`, three callers, one plant.**

**Infra:** none. No round was lost to a wedge, a usage wall, or a `no_verdict`.

### What the instrument corrected that review did not

Rebuilding it to be honest changed the document's own conclusions three times, which is
the argument for building the instrument before the prose rather than beside it:

- the median-over-time seed rule TIES on the binding leg (5157 s against 5174 s) and
  separates only on spread — the spec had claimed it collapses;
- the held-out margins are 965 / 1191 / 1160 s once the tautological arrival fallback is
  removed, not the 289 s first reported;
- documented limit L-6 is probed at min 0.50, median 0.99, max 4.60, with one surface of
  43 moving its ratio across four nightlies — it had been reasoning, not measurement.

## plan — 4 rounds

**Examined:** all 35 findings from plan rounds 1 to 4, the repair each drove, and which a
mechanism could have caught before a reviewer did.

BLOCKING every round, 12 / 8 / 7 / 8 findings, **every one accepted and none refuted**. The
rate did not decay, which matches the 2026-08-04 measurement that BLOCKING findings per
round are flat. A 100% acceptance rate is not a compliment to the reviewer; it is a verdict
on the drafts. The plan was under-built in ways visible from outside and not from inside.

**Mechanizable:** three classes, two of which now have their mechanism in the plan itself.

1. **A plant anchor that is not unique, or not a literal at all.** The harness refuses
   anything not occurring exactly once, so this is decidable without judgment, and it was
   the recurring vector — rounds 2, 3 and 4 all spent findings here. The repair is the
   Form A/B rule plus a uniqueness sweep run and pasted at plan time. It earned itself
   immediately: it caught an anchor of `scoreFloor: 0.9,` that occurs twelve times, and
   round 4 then found three more guessed anchors in a table written before the rule existed
   and not revisited when it landed. **A `spec:lint` arm is the obvious form** — for every
   plan table row declaring a literal anchor, assert it occurs exactly once in the file it
   names. That check is pure text and needs no judgment. Filed as
   `BL-SPECLINT-PLANT-ANCHOR-UNIQUENESS`; the scoped form landed in-branch.

2. **A count retyped into prose that the tree can already answer.** Round 1 found 28
   instrument cases claimed against 56 and three different plant totals in three sentences;
   round 2 found six authored markers claimed against five; round 4 found a GREEN requiring
   eleven plants against a table of ten. Each repair replaced the number with the COMMAND.
   This is the existing derive-don't-retype rule arriving late, three times.
   **declined: the mechanism already exists.** `docs/agents/writing-plans.md` and the
   derive-don't-retype rule both say this; nothing was missing but my applying them, and a
   second copy of a rule I ignored would not have been read either.

3. **A stale anchor left behind by the repair that moved the code.** Round 4's first
   finding is the sharpest thing in the corpus and it is entirely self-inflicted: the
   `legSeconds` repair at `75e63a2a2` restructured the loop, orphaning an existing plant
   whose anchor then matched zero times. So the "37 plants, 0 escaped" baseline this plan
   asserted was FALSE at the very commit it cited, and Task 1's GREEN — every pre-existing
   entry reports CAUGHT — was unreachable. The mechanism already exists and I did not run
   it: the harness reports ANCHOR-FAIL and exits non-zero. **Running the plant harness is a
   post-repair obligation whenever a repair touches a file the plant list targets**, in the
   same commit, and the number goes in from the run rather than from memory.
   **declined as tooling: the mechanism exists and was not run.** The harness already exits
   non-zero and names the anchor. The gap is a post-repair obligation, which is recorded in
   the fix-round regression-budget rule rather than in new code; the anchor-orphaning
   WARNING is carried by the filed row above.

**Judgment:** the findings that mattered most were not mechanizable.

- `reconcile` recomputing the partition by raw boots, so pricing the weight would make ~29
  of 43 surfaces read as moved and kill the AC-1 evidence path before it produced anything.
  Requires holding shipped code and planned change together.
- AC-7 resting on a command that would CONCEAL its violation, since a lowered score floor
  makes the score run greener. Requires asking which direction the evidence fails in.
- A plant caught by a pre-existing assertion proving that assertion rather than the new one
  (round 4, on the two workflow plants).
- `@ts-expect-error` declared under a Vitest-only command, where types are stripped and the
  directive is invisible. Requires knowing what each runner actually checks.
- Task 1 unable to reach its own GREEN because its plant anchored on code a later task
  creates. A sequencing property across task boundaries.

**Infra:** none. Four dispatches, four verdicts, no `no_verdict`, no reaped runs, no
retries, no usage interruptions. The wrapper behaved throughout.

**What I would tell the next arc.** Three of the 35 were found by me rather than the
reviewer, all while the tree was frozen waiting on a dispatch, all by RUNNING my own plan's
executable claims instead of re-reading them. That is the cheapest review available and it
costs only the wait that was already happening. Two of the three were the same
ENOENT-shaped mistake in different tasks, which is the lesson underneath the lesson: finding
one instance did not make me sweep for its peers, and the class-sweep rule applies to my own
repairs. Round 4's stale-anchor finding is the same shape a third time, from the other
direction — a repair that moved code and left a peer pointing at where the code used to be.
