# `fix/hidden-tab-ref-suppress` — review-round filing

One base, `c6908827344f`. No spec or plan stage: the change was classified bounded and approved in chat. The diff stage reached the cap and owes the section below.

## diff — 4 rounds

**Examined:** R1–R4 at this base, 3 findings, one P1 per round in R1–R3, every one with a probe that ran. R4 returned APPROVE with zero, on its own 400-case corpus sweep (a dead tab spliced at every position of every corpus workbook, three bare-literal shapes): 696 suppressions, 0 wrong. The three findings were the same finding three times. The rule removes a `#REF!` warning when the cell cannot reach the crew page, and each round refuted the proxy the code was using for "cannot reach the page": R1 the section kind (the AGENDA token-header table tracks as generic and renders), R2 the tab's content by containment ("Event #REF!" is a title, not an error cell), R3 the tab's content by equality (`parseGearTab` continues across a tab boundary and appends a dead tab's bare cells to a room field). The round that converged replaced the proxy with the property: serialize the parsed output, search it for the literal, remove nothing if it is there. The live show still clears its five warnings under that gate, and the reviewer's sweep is what says the gate holds across the corpus.

**Mechanizable:**

- **A corpus sweep as a committed test (R3).** The reviewer's 240-case probe, and its 400-case successor in R4, is a five-line loop over `fixtures/shows/exporter-xlsx/*.xlsx` with `withHiddenTabAfter` at each position, asserting that a removed warning never coincides with a rendered literal. It terminates, it is in-domain, and it would have caught R3 before dispatch. Not built: the repair made the property the code checks directly, so against the parsers that exist the sweep can only restate the oracle's construction (a string search over the same serialized output), and the interesting failure it would guard, a parser that renders a cell the serialization does not contain, is exactly the case the sweep cannot see either.

declined: LIM-INPUT-SHAPE-PROXY-FOR-OUTPUT-PROPERTY, three rounds each named one more input shape the proxy misjudged, and the class closed only when the code tested the output property instead; process-facing under the 2026-08-25 mint freeze, and the owning record is the oracle's own docstring in `lib/drive/waveCodeAnchors.ts` (`parsedOutputHoldsRefLiteral`), which states the limit that a rendered literal anywhere keeps every `#REF!` warning.

**Judgment:** R1 accepted as stated. R2 accepted as stated. R3 accepted at the class level rather than as the GEAR special case the probe suggested: pinning the one continuation parser that exists today leaves the next one open, and the output oracle sees all of them. The conservative side of that oracle (one rendered `#REF!` anywhere, even a crew member literally named `#REF!`, keeps every dead-tab warning) is a limit chosen over precision, because the warnings are byte-identical and nothing can say which one belongs to the rendered cell.

**Infra:** none; every dispatch returned a verdict. The reviewer's sandbox denied Vitest its temporary directory (`EPERM`) in all four rounds, so it never ran the suite; it typechecked and ran in-memory probes over the committed corpus each time, which is where every finding and the final sweep came from.

**Why four rounds, honestly.** The premise "no label, so nothing can bind" was stated in round 1's design and never tested against the parsers; it was three separate parsers' business, and each round found one. The brief's consequence bound was quantified over the right thing (a removed warning whose cell renders), which is why every finding was admissible and every repair was a narrowing; the miss was mine in choosing an input proxy for an output property and then defending it twice before replacing it.
