# Review rounds — fix/confirm-focus-restore (base e306dc9e387b)

## spec — 4 rounds

**Examined:** `docs/superpowers/specs/2026-08-31-confirm-focus-restore.md`, the
focus-restore contract for the two-tap destructive confirm controls, across four
Codex rounds returning BLOCKING 7, 7, 6, 7. The reviewer also re-verified, every
round, the ratified exclusions: the `ArchiveShowButton` refutation, the ShareHub
lifecycle-rescue citation, the deleted `ResetPickerEpochButton`, and the
three-control scope. Those held in all four rounds and were never the axis.

**Judgment:** the axis is not reviewer appetite and not an open input space. It
is that **each round's repair authored the next round's finding**, in my own
work, four times:

| round | finding | what my repair then created |
| --- | --- | --- |
| 1 | three controls specified as one shape | an assertion on the focus outcome |
| 2 | that assertion was a tautology (`toMatchObject` comparing a value to itself); the in-flight requirement was undefined | a definition: "focus the trigger at `confirm → resolving`" |
| 3 | no control RENDERS a trigger at that moment (`app/admin/show/[slug]/PickerResetControl.tsx:224` folds confirm and resolving into one branch) | the descope, plus a revoke root moved onto the heading |
| 4 | the revoke root named the heading in one field and the list in the other, so the case could not reach its own settled assertion; five further self-contradictions introduced in rounds 2-4 | — |

Three properties of that pattern are worth recording, because none is fixed by
reviewing harder:

1. **Two of the four rounds found assertions that could not fail.** Round 1's
   probe asserted a reading count; round 2's replacement compared a value to
   itself. Both were reported by me as repairs. No browser run can catch this
   class: a probe that cannot fail passes exactly like a probe that found
   nothing wrong. What caught it was a reviewer reading the assertion, and what
   now prevents it is `tests/e2e/helpers/confirmFocusProbe.decide.test.ts` —
   seven constructed cases, browser-free, with the self-referential mutant
   planted (4 of 7 red) and restored.
2. **The expensive vector was invented, not discovered.** The in-flight
   `confirm → resolving` requirement appears nowhere in the measurements; I
   wrote it while repairing round 2 and then defended it through round 3. The
   three-round prose cap in `docs/agents/writing-plans.md` prescribes descoping
   over a third patch, and that is what closed it (ratified, §2.5).
3. **Prose repairs to a multi-component contract do not converge by patching.**
   Rounds 2-4 each fixed the named instances and left the shape. What actually
   moved the finding count was restructuring per control (§2.4) and deriving the
   pair enumeration mechanically instead of by eye.

**Mechanizable:** declined: both shapes below are guard-fidelity gaps on this
arc's own probe helper, and the process mint freeze (AGENTS.md, 2026-08-25)
binds those to the owning surface's documented-limits record rather than the
open queue. Neither has recurred across independent arcs, so neither meets the
`recurrence` exception; both are recorded on the helper instead. Naming them
here because a declined entry that hides what was declined is worth nothing.

- **Pair-count parity.** Rounds 2 and 4 both found transition tables whose
  directional rows contradicted their unordered-pair claim, losing
  `confirm ↔ couldnt_confirm` entirely. `N*(N-1)/2` against the declared state
  list is arithmetic; I now compute it rather than count rows by eye.
- **Same-element root parity.** A `ConfirmControl` whose `root` Locator and
  `rootSelector` name different elements is statically detectable, and it is
  what broke the revoke case in round 4. Parked as a documented limit on the
  probe helper rather than filed: the process mint freeze binds a guard-fidelity
  gap to the owning surface's limits record, and one instance is a gap rather
  than a recurrence.

**Infra:** none. All four dispatches returned a verdict on the first attempt; no
reaper kill, no usage `no_verdict`, no retry.
