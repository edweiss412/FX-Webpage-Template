# Review-round economy — `fix/control-outline-surface-fills`, diff stage

Base `daa53759a953`, the arc's third. The base moved twice under sibling merges, so the arc's
rounds are split across three corpus files and this one holds the diff stage from the second
merge onward. Spec (8 rounds) and plan (4 rounds) are filed at `119895a7c756.md`; the first diff
round is at `77de29e811af.jsonl` under the intermediate base. **Rounds are per-file and contiguous
from 1**, matching `docs/review-rounds/test/psql-scan-mutation-enrolment/d6de92acad70.jsonl`, so
the round numbers here are not the review thread's — the thread ran five whole-diff rounds and
this file holds four of them.

## diff — 4 rounds

**Examined:** the 22/21/16 swap arity and both two-arm ternaries; the executable fence (51 → 28)
and the 28 survivors it protects; the regression pin's self-proof obligations — premise on the
same input the rows resolve from, cardinality against a literal, every row resolving before its
classes are asserted, the negative control's own premise; AC-2 across all five switch-track
render paths; the mutation enrolment and its score; the invariant-8 gate record and its marker
arithmetic; every ratio and count in DESIGN.md, both ledgers, `DEFERRED.md`, the spec and the
plan; and invariant 12's marker timing.

**Mechanizable:** eight of the ten findings across the four rounds were **one shape — a number or
a claim correct in one document and stale or wrong in another**, and the arc's own spec stage had
already named that shape as its dominant defect. R1's census said 28 where the entry's published
query returns 30; R2's spec kept the `≤1.13:1` universal that DESIGN.md had just been corrected
for; R3's DESIGN.md still said the predicate was "the ledger entry's to make" ~120 lines below the
ruling; R4 found `border-border` at 1.15:1 in four documents where the spec had 1.27:1 correct
throughout, plus a plan step saying 29 survivors where its own sibling step's fence says 28.

Two sub-classes are worth separating because they need different mechanisms. **Cross-document
numeric parity** — the same quantity stated in a spec, a design doc, two ledgers and a plan — is
tracked as `BL-SPECLINT-ENUMERATED-UNIVERSAL-PARITY`, filed by this arc at the spec stage for
exactly this arm; R4's five instances are added evidence for it. **A quantity taken from a
reviewer's prose rather than recomputed** is the narrower half and is the one that actually cost
the round: the 1.15 figure came from the invariant-8 critique's report and was propagated to four
documents without being recomputed, while the citation pass that ran over the same diff verified
that every `file:line` resolved and that the load-bearing ones matched their claimed content. A
ratio is a claim like any other; the pass checked citations and not arithmetic. The derived form
is the one this arc now states in its briefs: **every ratio in the diff is recomputable from
`app/globals.css`, so recompute rather than compare against prose.**

**Judgment:** two findings could not have come from any sweep.

R2's P0 — the archive/marker-removal commit was no longer the PR's last commit — is a process
contract that no test can see, because `tests/docs/_metaLedgerInProgress.test.ts` enforces the
SAFETY property (no in-flight entry reaches main, which never broke) and not the TIMING one that
invariant 12 also states. The repair was to reorder rather than argue: reopen the entry, commit
the other work, re-archive at the tip, with `git diff --stat <base> -- BACKLOG.md
BACKLOG-archive.md` empty as the proof that only the timing moved. That happened **three times**,
because each subsequent round required a correction, and each correction would otherwise have
landed past the ledger step. The cost is real and the alternative was worse: the guard cannot
distinguish "archived early and nothing re-added the marker" from "archived last", so a reviewer
reading the contract literally is right every time, and only the author can see that the content
is identical.

R1's P0 — both invariant-8 P1s were deferred to `BACKLOG.md` with no `DEFERRED.md` entry — is
judgment for the same reason. Invariant 8 names `DEFERRED.md` specifically, the two files serve
different purposes (an open deferral queue with un-defer triggers, versus a speculative backlog),
and nothing checks that a gate's P1 dispositions reached the right one. The repair added both
entries with their un-defer triggers and made §12's disposition column name the mechanism rather
than the queue row alone.

**Infra:** three, none of them the reviewer's or the implementation's.

The `pnpm mutation:guards` gate calls `runSurface` at COLLECTION time for every enrolled surface,
so `-t` cannot scope it and a surface appended last waits behind all the others. Queued under
`pnpm heavy` at 11:12 local, it was still waiting at 12:52 with both machine-wide slots held
continuously by other arcs' full-gate runs (1h47m and 2h47m at their peaks) and up to six priority
waiters. The route taken was a scratch runner importing the gate's own `runSurface` /
`evaluateGate` / `runControl` and asserting the same seven conditions in the same order — 24
mutants, 24 killed, score 1.0, empty survivor set, control mutant killed. That establishes the
per-surface claim; the aggregate belongs to CI, which is the right split anyway since two
unrelated surfaces are red on main.

The corpus is keyed by base sha, and two sibling merges moved this arc's base mid-review. The
round-5 dispatch wrote a lone `round: 2` into a fresh file and red the economy gate with "rounds
that are not a contiguous 1..N" — correct of the thread, wrong of the file. Renumbering per the
existing convention fixed it, and the split is recorded at the top of this filing because per-file
numbering is exactly what hides it.

Codex's sandbox denied vitest's temp-directory creation (`EPERM`), so no round could run the
suites; every round's verification came from direct scanner probes instead, which is why each
report carries `universe=362 unresolved=13 census=21 resolved=21 faint=21 strong=0` rather than a
test summary.
