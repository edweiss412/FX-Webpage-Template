# Review-round economy — `fix/server-action-origin-sweep` @ `119895a7c756`

## spec — 4 rounds

**Examined:** all four spec rounds (5 / 4 / 2 / 0 findings; BLOCKING for R1-R3, APPROVE at R4),
their briefs, and the repair commits `64e2a2be8`, `5fed54a9a`, `6857feb63`. The subject is a same-origin
gate applied to every destructive Server Action in the repo plus a structural walk that
re-derives the census, so the arc has two review surfaces with different economics: a census
(finite, probeable, settled once) and a recognizer (the part that generated the rounds).

**Mechanizable:** the census half was free, and it was free because it was derived rather
than written. It was re-verified independently in all three rounds — 56 units, 31 files, the
42/4/1/3/3/3 split, every §3.7 locator — and drew **zero** findings across the whole arc. The
one census-adjacent defect either side found was arithmetic in prose (`887` total-scanned
misreported as outside-defining, and a mock census whose denominator and named suites were
both wrong), and both were caught the same way: by re-running the probe instead of re-reading
the sentence. Two of the eleven findings are therefore the numeric sweep
(`docs/agents/spec-self-review.md:14`) applied to a number the author had not re-derived
since the sentence was written. That is mechanizable and was not mechanized.

**Judgment:** the other nine findings are one class with one lesson, and the lesson is about
repair DIRECTION rather than about the findings themselves.

- **R1** defeated a "the first statement contains a call to the gate helper" predicate with a
  polarity flip on the live `clearIdentityCore`, and showed a `descend: false` read-only check
  accepting 22 known-destructive units.
- **R2** defeated the accept-sets that replaced it by naming four expression positions they
  left unpinned — a second argument, a parameter default, and the returned expression of both
  refusal forms.
- **R3** defeated the body predicate itself: a module-local refusal helper edited to write a
  cookie satisfies every clause, because `scanBody` models write builders, `.rpc(` and
  `logAdminOutcome` and nothing else.

Rounds 1 and 2 were repaired by widening the recognizer, and each widening was a bigger
target for the next round — the exact ratchet `AGENTS.md`'s repair-direction rule describes,
arrived at in three rounds rather than fifteen only because the rule was applied at R3 instead
of later. The R3 repair is the narrowing one: the refusal position stopped being verified by
body analysis and started resolving **by name** against four designated exports of one module;
the exemption stopped being a predicate and became a **closed three-name set** cross-checked
against two independently maintained registries; and the delegator exemption was deleted
outright by gating its three wrappers, which cost three one-line edits and removed an entire
exemption kind, a registry file, and the verification code for both.

The generalizable finding: **a recognizer asked to decide a semantic property of
author-written code has no fixed point.** "Can this body mutate?" does not terminate, and each
round teaches the scanner one more verb. The move that terminates is to shrink what the
question ranges over — one reviewed module instead of every call site's helper, three pinned
names instead of any row someone appends — so the predicate becomes exact instead of
approximate. Two rounds were spent discovering that; a third would have been spent the same
way had the rule not been applied. R4 was dispatched with the closed criterion stated and no
fresh axis named, per the late-arc brief rule, and returned zero findings — which is what a
finite bound looks like when it is actually finite.

Worth recording against the reversal rule: Resolved scope #9 (exempt the three `.tsx`
wrappers) was accepted at draft time on an argument rather than a probe, and R3 refuted the
argument. It is now #9r and fenced in both directions. That is the shape
`docs/agents/adversarial-round-economy-2026-07-31.md` clause (d) predicts — a probe-less
acceptance charged back later — and the probe that would have settled it at draft time was
one body read of `clearIdentityAndSkipFormAction`.

**Infra:** none. Every dispatch returned a verdict on the first attempt — no
`no_verdict`, no reap, no retry, `lintArm: present` on all four.
## plan — 9 rounds

**Examined:** all nine plan rounds (10 / 4 / 2 / 1 / 1 / 2 / 2 / 3 / 2 findings), their briefs, and the
repair commits `7a6d7cd68`, `42ac62cec`, `2563ceae2`, `a4f986e69`, `922238740`, `0609c9599`,
`432f77ab0`, `0cc22eaa5`, and the round-9 repair this filing update lands with. One
corpus row is `plan/1/no_verdict`: that dispatch was killed by hand seconds in, because the brief
cited `tests/auth/_originGateExemptions.ts` and a "Task 9" that the spec's round-3 narrowing had
deleted. A brief defect, not an infra fault, and the cheapest finding in the arc — the citation pass
the orchestrator rule already requires (`AGENTS.md`, "Verify every citation in a review brief before
dispatch") catches it before the dispatch instead of after.

**Mechanizable:** two arms, and the second is the one this arc argues for.

Four findings are numbers written once and never re-derived — the missing 16→17 registry-array
reconciliation (R1 #8), a `scoreFloor` described as "the value every shipped row uses" against a
live 7/6/3 split (R2 #4), and the exempt-unit count stated as eight when the enumeration prints
nine (R2 #2, restated as R3 #2 because the first repair corrected the mechanism and left the
number). Each is the numeric sweep at `docs/agents/spec-self-review.md:14` applied to a figure the
author had not re-run since typing it, and each was settled by the reviewer in one probe. The repair
that held was to stop stating counts in prose and paste the derivation that produces them —
extended at R6 to a count the plan could not yet know, where the derivation itself, with its probed
baseline and its ceiling, is what the plan states.

The larger arm has no gate today. **Five findings across four rounds are one shape: the plan
asserted a pass/fail state the branch cannot produce.** R1 #1 — Task 2's GREEN was unreachable,
because the walk rejects the three `clearIdentity` actions' module-local refusal helper and they
were not on the ratchet list. R1 #4 — Task 11's declared RED ("the harness reports zero mutants
because no row exists") is a PASS: enrolment is opt-in (`tests/mutation/source/registry.ts:8-9`).
R2 #3 — one of Task 10's two asserted RED causes is vacuous over an empty array. R3 #1 — Task 11's
`red=` command could not collect its own declared `red-target`, because the guard file lives only in
a project that exists under `VITEST_INCLUDE_MUTATION_HARNESS=1`. R4 — Task 10 required the whole
suite **green** on a branch where `tests/docs/_metaInvariant8Closeout.test.ts` is red by design.
The `red-contract` lint arm already parses every marker and validates the `red-target` citation's
grammar; what it never does is EXECUTE the claim. An arm that ran `vitest list` on each `red=`
command and asserted the `red-target`'s file appears in the collected set would have caught R3 #1
outright, and the same posture — run the declared command, compare against the declared state — is
what the other four needed. Every one was found by the reviewer doing exactly that by hand.

R7 #1 is the same shape turned on the arc itself: Task 10's newly-stated "exactly one failing file"
was already false, because this arc's own plan stage had passed `ROUND_THRESHOLD` and owed the
filing you are reading. A claim about how many tests fail is a claim about the repository, and the
repository includes the arc's own paperwork.

**Judgment:** the genuinely non-mechanizable half is small and clustered.

Three findings are **what one registry row silently turns on**, and they took three consecutive
rounds because each round's sweep ranged over the wrong universe: R1 #5 (`EXPECTED_ENV_TOUCHING`),
R2 #1 (`EXPECTED_LEDGER_KINDS`), and then R3 #1, which the R2 repair's declared class sweep — "every
consumer of `GUARD_SURFACES`", three found — could not have seen. **A sweep over "who references
this symbol" is not a sweep over "what must hold for this command to prove anything."** The first is
a grep; the second includes the project gating that decides whether the file is collected at all,
and nothing about the identifier points at it.

Three more are test-mock reality (R1 #2, #3, #7): a "cross-site" case whose `next/headers` mock has
no `headers` export lands in the catch-allow and is ALLOWED, a baseline that asserts `{ ok: true }`
proves no spy is live, and a premise inside a `describe.each` callback is unreachable in the
degenerate case it guards. These are what a plan review is uniquely good at and no meta-test
produces.

R6 #1 is its own lesson about the cost of a narrowing. The spec's round-3 narrowing deleted body
analysis as the exemption CLASSIFIER but deliberately kept a `scanBody` tripwire over the three
exempt bodies (spec §3.5, AC-5); the plan carried the three set-membership assertions and dropped
the tripwire, because "we removed the body predicate" is easier to remember than "we removed it from
one of its two jobs." **When a spec narrows a mechanism rather than deleting it, the plan has to
carry the surviving half explicitly, or the narrowing reads as a deletion downstream.**

**The most transferable finding is the R7 #2 / R8 #2 / R9 #1 sequence, and it took three rounds to
find because the first two repairs were each locally right.** Closeout originally kept the ledger
archive as a post-review commit and argued an exception to `docs/agents/writing-plans.md:28` —
reviewed CODE equals merged CODE, no behavior, a shipped precedent. R7 refuted it: the rule says
*diff*, not *executable lines*. The repair moved the archive ahead of the review, which read as the
narrowing move — make the property literally true and delete the paragraph defending it. R8 showed
that ordering spent the OTHER rule: invariant 12 wants the marker off in the last commit and the
origin claim alive as long as the work, and archiving before the gates leaves the row unclaimed
through gates, review, and CI. The repair made the archive the last commit AUTHORED with the review
after it, which satisfies both rule texts. R9 then showed that ordering has no terminal path at all:
every codex-guard dispatch appends a corpus row committed with the arc, so the APPROVING round's own
row must be committed after the archive.

**The terminating move was to stop searching for an ordering.** The rule pair is unsatisfiable at
the boundary for a structural reason — a record of an event cannot precede the event, so the final
round's corpus row and any filing summarizing that round can never be inside the diff it reviewed.
The plan now states that residual as a documented limit with its impossibility argument, and closes
the final commit to exactly three permitted contents (the approving round's row, a threshold filing,
the invariant-12 removal), so that "what may ride after the review" is enumerable rather than
argued.

The generalizable form, and it is a correction to the lesson the spec arc recorded three rounds
earlier: **narrowing is the right direction, but "delete the exception" is not the same move as
"narrow the mechanism."** Deleting an exception by reordering assumes an ordering exists. Where two
rules genuinely cannot both hold, the narrowing move is to bound the residual — name exactly what
may differ between reviewed and merged, and prove why it cannot be zero — rather than to keep
permuting the sequence. Two rounds were spent permuting. The check that would have skipped them is
one question asked before the first repair: *what does the other rule buy, and can any ordering
deliver it?*

R9 #2 is the cheap sibling: the invariant-8 marker was directed into an unnumbered `## Closeout`
section when `AGENTS.md` specifies `## 12` for a flat plan, and three shipped plans
(`2026-08-02-copy-deadcode-sweep.md`, `2026-08-03-app-wide-font-binding.md`,
`2026-08-02-admin-popover-overlay-cluster.md`) all use `## 12` — one grep against precedent, never
run.

R8 #3 is the cheap sibling of the same edit and is charged to the author, not the reviewer: the
closeout renumbering left three cross-references pointing at the old step numbers, in Task 10, in
the mutation-guard acceptance section, and in its review-brief line. The class-sweep rule in
`AGENTS.md` covers exactly this — the sweep after a renumbering is over every reference to the thing
renumbered, and running it costs one grep. R8 #1 belongs with the derivation class above: the
three-array reconciliation table added at R6 stated its post-condition (`17 / 17 / 25`) against step
1 when step 1 produces `17 / 16 / 24` and that mismatch IS the RED, so a table written to fix one
count defect introduced another by attaching it to the wrong step.

**Infra:** none beyond the self-inflicted `no_verdict` above. Every other dispatch returned a
verdict on the first attempt — no reap, no retry, `lintArm: present` throughout.
