# Review-round economy — `fix/server-action-origin-sweep` @ `119895a7c756`

## spec — 4 rounds

**Examined:** all four spec rounds (5 / 4 / 2 / 0 findings; BLOCKING for R1-R3, APPROVE at R4),
their briefs, and the repair commits `64e2a2be8`, `5fed54a9a`, `6857feb63`. The subject is a same-origin
gate applied to every destructive Server Action in the repo plus a structural walk that
re-derives the census, so the arc has two review surfaces with different economics: a census
(finite, probeable, settled once) and a recognizer (the part that generated the rounds).

**Mechanizable:** the census half was free, and it was free because it was derived rather
than written. It was re-verified independently in all three rounds — 56 units, 31 files, the
42/4/1/3/3/3 split, every §3.7 locator — and drew **zero** findings across the whole arc. The
one census-adjacent defect either side found was arithmetic in prose (`887` total-scanned
misreported as outside-defining, and a mock census whose denominator and named suites were
both wrong), and both were caught the same way: by re-running the probe instead of re-reading
the sentence. Two of the eleven findings are therefore the numeric sweep
(`docs/agents/spec-self-review.md:14`) applied to a number the author had not re-derived
since the sentence was written. That is mechanizable and was not mechanized.

**Judgment:** the other nine findings are one class with one lesson, and the lesson is about
repair DIRECTION rather than about the findings themselves.

- **R1** defeated a "the first statement contains a call to the gate helper" predicate with a
  polarity flip on the live `clearIdentityCore`, and showed a `descend: false` read-only check
  accepting 22 known-destructive units.
- **R2** defeated the accept-sets that replaced it by naming four expression positions they
  left unpinned — a second argument, a parameter default, and the returned expression of both
  refusal forms.
- **R3** defeated the body predicate itself: a module-local refusal helper edited to write a
  cookie satisfies every clause, because `scanBody` models write builders, `.rpc(` and
  `logAdminOutcome` and nothing else.

Rounds 1 and 2 were repaired by widening the recognizer, and each widening was a bigger
target for the next round — the exact ratchet `AGENTS.md`'s repair-direction rule describes,
arrived at in three rounds rather than fifteen only because the rule was applied at R3 instead
of later. The R3 repair is the narrowing one: the refusal position stopped being verified by
body analysis and started resolving **by name** against four designated exports of one module;
the exemption stopped being a predicate and became a **closed three-name set** cross-checked
against two independently maintained registries; and the delegator exemption was deleted
outright by gating its three wrappers, which cost three one-line edits and removed an entire
exemption kind, a registry file, and the verification code for both.

The generalizable finding: **a recognizer asked to decide a semantic property of
author-written code has no fixed point.** "Can this body mutate?" does not terminate, and each
round teaches the scanner one more verb. The move that terminates is to shrink what the
question ranges over — one reviewed module instead of every call site's helper, three pinned
names instead of any row someone appends — so the predicate becomes exact instead of
approximate. Two rounds were spent discovering that; a third would have been spent the same
way had the rule not been applied. R4 was dispatched with the closed criterion stated and no
fresh axis named, per the late-arc brief rule, and returned zero findings — which is what a
finite bound looks like when it is actually finite.

Worth recording against the reversal rule: Resolved scope #9 (exempt the three `.tsx`
wrappers) was accepted at draft time on an argument rather than a probe, and R3 refuted the
argument. It is now #9r and fenced in both directions. That is the shape
`docs/agents/adversarial-round-economy-2026-07-31.md` clause (d) predicts — a probe-less
acceptance charged back later — and the probe that would have settled it at draft time was
one body read of `clearIdentityAndSkipFormAction`.

**Infra:** none. Every dispatch returned a verdict on the first attempt — no
`no_verdict`, no reap, no retry, `lintArm: present` on all four.
