# fix/rowactions-submenu-stale — review rounds at base d2800d997170

## spec — 2 rounds

**Examined:** `docs/superpowers/specs/admin/2026-08-28-rowactions-submenu-stale-on-row-menu-replace-design.md` and its plan, across two Codex dispatches (R1: 3 findings, NEEDS-ATTENTION; R2: 1 finding, NEEDS-ATTENTION). Subject was a placement-notification repair for `BL-ROWACTIONS-SUBMENU-STALE-ON-ROW-MENU-REPLACE`. Filed below threshold deliberately: the arc was refuted mid-flight and this is where that result survives.

**The finding that outranks all four review findings came from a mutant, not from review.** After both spec rounds and the implementation, removing the repair's one wiring line left the acceptance probe GREEN. The row is refuted: the submenu already tracks its anchor, because its own `ResizeObserver` fires on the anchor BUTTON — traced, targets `row-action-preview-<slug>` and its portal — since the button's SIZE changes when the confirm region reflows the row menu panel under it. The row's load-bearing premise, "the anchor only moved", is false for every trigger in the probe domain.

**Judgment (R1, 3 findings, all correct).** (1) The spec blamed the `ResizeObserver` for the row menu's move; React runs sibling layout effects in render order, so the row menu's own every-commit effect measures first and schedules the placement. (2) The notification's effect phase was unspecified, and `useEffect` versus `useLayoutEffect` decides whether a stale frame paints. (3) The `null`-to-first-placement transition would have notified, adding a third commit-driven measure to the open path and breaking the ratified floor of 2. None of the three was mechanizable — each needed React execution-order reasoning against this specific component.

**Judgment (R2, 1 finding, correct).** Task 5 could not prove AC-7 with a jsdom-only command, because jsdom flushes passive effects synchronously and so cannot distinguish the two effect phases. Correct, and moot once the arc was refuted.

**Mechanizable:** the defect that actually cost this arc was test-side and cheap to catch. Two shapes:

1. **The repair-removal mutant was run last instead of first.** The anti-tautology rule already mandates it; running it at Task 1 would have refuted the row before the spec, both review rounds, and the implementation. Nothing about it needed judgment — it is one edit and one re-run.
2. **A prefix locator silently selected the wrong element.** `[data-testid^="row-action-preview-"]` also matches the portal, the menu, every crew row and the empty hint; `.first()` resolving to the button was DOM-order luck. The probe then measured the MENU's top on a viewport-clamped, height-capped panel, where a top that does not move is CORRECT behaviour, and read that as the defect. A geometry assertion should name its element exactly, and should assert the invariant the placement core actually maintains — here the facing-edge gap, which held at exactly `GAP` throughout.

declined: both shapes are process-facing and the 2026-08-25 process mint freeze admits neither. There is no `product-blocked` incident — no product arc was stopped or slowed by either gap, this arc was the only one affected — and no `recurrence`, which would need the same shape hitting at least two independent arcs. Under the freeze these file here, as this surface's documented limits, rather than into the open queue. Re-file trigger for shape 1: a second arc burning rounds on a repair its own acceptance test cannot discriminate. For shape 2: a second geometry probe measuring the wrong element through a prefix locator.

**Infra:** none. Both dispatches returned clean verdicts through codex-guard; no reap, no `no_verdict`, no recovery.
