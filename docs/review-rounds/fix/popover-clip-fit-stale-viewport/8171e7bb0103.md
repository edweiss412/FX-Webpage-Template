# fix/popover-clip-fit-stale-viewport @ 8171e7bb0103

Arc outcome: the parked repair did NOT ship. A Stage 0 premise recount refuted the core premise, and the arc closed as subsumed with its design preserved. The deliverable is documentation: a subsumption record, the recovered design, and a contribution to an existing `LIM-` entry.

## diff — 4 rounds

**Examined:** 12 findings across four rounds on a documentation-only diff (5, 3, 0, 4). Round 3 returned APPROVE; the orchestrator then ruled the arc closed and specified a different PR shape, so the diff was restructured after that approval and round 4 reviewed the restructure. No finding was disputed and none was refuted; all twelve were admitted and repaired. The reviewer independently confirmed the premise counts, the current line references, the `2c5b718a4` changes, the byte-identity of the recovered extracts against `ee9f1fdb4`, the task marker's inertness under specLint, and that every cross-document citation resolves.

**Judgment:** Eleven of the twelve findings are one shape: **a claim stated more strongly than its evidence supports.** Not eleven mistakes in eleven places — one habit, re-committed every round in whatever prose that round happened to produce.

The progression is the useful part, because it explains why a class-sweep did not end it at round 1. Round 1 caught the conclusion itself ("the defect is gone" on evidence that could only reject a rate). I swept that at all five sites and repaired the record. Round 2 then caught the same shape one level down, in the sentences describing the probe ("could not provoke a stale read" when only `available` was ever sampled) and in a margin expressed as fractions that were wrong in the unsafe direction. Round 4 caught it one level down again — this time not in the record at all, which by then was careful, but in the derived prose written AROUND it: the `LIMITS.md` entry, the design doc's trigger, and the PR body each restated the record's hedges as absolutes. "Has a wide observed margin" became "cannot fail." "Did not reproduce across 26 runs on one host" became "does not reproduce."

**So the class does not live in a location, which is what a location sweep cannot fix.** It lives in the derivation step. Every repair produced new summary prose, and summarising is exactly where a hedge gets dropped, because the hedge is the part that does not compress. Sweeping every site in the record was correct and did not help, because the next round's finding was in text that did not exist when I swept.

That is `LIM-INDEX-RESTATEMENT-DRIFT`, and this is its **third naming arc** — which fires its stated re-file trigger. It does not mint a row: the 2026-08-25 process freeze admits a process row only under `invariant` or `product-blocked`, and no product arc was blocked here. The count is recorded in the index so the next arc meeting this shape finds it by one grep.

The twelfth finding was different and worth separating out: round 4 found that the design document's re-file trigger, as worded, was **already satisfied** by a read the same documents identified, while the design's status said it stayed parked. That is not an overstatement but a live contradiction between two documents in the same diff, and it was repaired by narrowing the trigger and naming the known site as a deliberate exclusion rather than an oversight.

**Mechanizable:** none. Distinguishing "cannot fail" from "has a wide observed margin" requires knowing what the evidence supports, which is the reviewer's judgement and not a text property. `LIM-INDEX-RESTATEMENT-DRIFT`'s owning record already declines an index-versus-artifact comparison lint by design, and the freeze's warning about the measuring tool becoming the subject applies with force to a lint whose job would be grading epistemic hedges in prose. The repair the class actually has is the one its entry states: narrow the claim, and cite rather than restate.

**Infra:** none. All four dispatches returned a verdict on the first attempt; no silent death, no `no_verdict`, no retry. The reviewer's sandbox blocked a Playwright run it attempted in round 1 (`EPERM` on the transform cache), which cost nothing, since the measurements under review were already committed with their procedure.
