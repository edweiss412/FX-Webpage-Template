## diff — 3 rounds

**Examined:** the whole diff of a four-row polish arc on the wizard's step-3 finalize
surface, at this base. Arc-summed across both bases the stage stands at **4 rounds, the cap**:
BLOCKING/6 at `47e9544e65dd`, then BLOCKING/3, BLOCKING/1 and BLOCKING/1 here. The old base's rounds
are in `47e9544e65dd.jsonl`; the merge base moved when merges 47, 48 and 49 were absorbed
in one step, which opened this file and restarted the numbering by design.

Every finding across all four rounds was accepted. None was disputed and none was
reversed by a later round. Any further round needs bl-orch's word, which is why this
filing lands with the cap rather than after it.

**Mechanizable:** the dominant class is *a prose claim asserting a measurable or
structural fact that nothing executable checks, and that the code or a measurement
contradicts*. Four of the old base's six, all three of round 1's here, and the single finding of
each of rounds 2 and 3, are that class, and every instance was prose the arc itself
wrote while repairing something else.

Two things about it are worth recording, because neither is obvious from the finding
list.

The first is that measurement, not argument, settled all of it, and the numbers were not
what the prose guessed. The footer at 375px in the real composition reads idle 48px,
batch 54.59px, CAS before the first phase event 48px, CAS with a phase 54.59px. The spec
said "flat at 54.6px across the whole ladder"; that ladder varies the COUNT inside one
phase, so it was a claim about count width wearing the clothes of a claim about the run.
The plan said the tracking block "never exceeds the reserved 48px, so the footer is the
same height running as idle"; it exceeds it at 54.59px and the footer is not. A
`display: none` box was described four separate times as an unrendered element. None of
these was reachable by reading the diff, which is why three review rounds each found more
of them.

The second is the failure mode of the repair. Round 2's finding and round 3's finding are
the SAME class as round 1's, still open because the sweep that was supposed to close it
was an enumeration: five files edited by hand, grepped by name. `DEFERRED-archive.md` was
in the diff and not in the list, so it kept the false claim through two more rounds. The
class-sweep rule already says the output must be a derived cover rather than a longer
list, and this arc violated it while repairing findings about unchecked claims. Re-swept
as `git diff --name-only origin/main...HEAD` piped through the claim shapes, the class
closes and the same sweep proves two surviving "footer flat at 54.6px" sentences TRUE,
because each is explicitly scoped to the count ladder.

The third repair attempt is the one that names the real defect. Round 2's sweep was a
list of five hand-picked files. Round 3's sweep fixed that — it derived the FILE set from
`git diff --name-only` — and still missed three instances, because it was still
enumerating PHRASINGS: it grepped `no longer rendered` and `not rendered` and never
matched `no element is rendered` or `no phase-label element at all`. A recognizer over an
open set of wordings does not terminate, which is the repair-direction rule in AGENTS.md
arriving the hard way, three rounds running.

What closed it was inverting the sweep: stop matching the CLAIM and match the SUBJECT.
Grepping the concept anchors — `empty:hidden`, `casPhaseLabel`, `cas-phase`, `phase
label`, `phase element` — yields a finite set of about forty lines across the diff, small
enough to read, and every false claim is in it by construction regardless of how it is
worded. The repair then narrowed rather than widened: the term `suppressed` is now DEFINED
once, at its first use, as the box and never the node, so nothing downstream has to
restate it and no future sweep has to re-derive it.

Only one instance was repaired with a guard rather than with accurate prose, and the
distinction is the useful part: the `ProgressPanel` docstring's "(no animation)" survived
because every assertion in its guard is about tokens or the shared bar, so the false
clause sat green. That claim is now pinned against the STYLESHEET — while an animation
reaches this panel's indeterminate bar, a blanket denial fails — with a premise asserting
the stylesheet really does animate it, so the case cannot pass vacuously. Probed both
ways: the old docstring reds it, the correction greens it, the plant reverts clean.
Everywhere else the honest repair was to narrow the claim, and one census header now says
outright which of its copies is machine-checked and which is kept in step by hand.

declined: no ledger row is filed for this class, and the decline is the arc's contract rather
than a judgement call — the arc brief states that this arc files no new `BL-`/`DEF-` row of any
facing, and AGENTS.md's 2026-08-25 process mint freeze independently bars a process-facing row
without `invariant` or `product-blocked`. This class needs neither: it was closed IN BRANCH by
the derivation sweep, which is the class-sweep default (every instance of one shape repaired in
the same PR), and the one instance that could recur silently now has an executable guard rather
than a note. What remains is a documented limit on the census header, which says outright that
its figures are kept in step by hand while the scanner's copy is machine-checked.

**Judgment:** the invariant-8 audit re-run is the round that paid for itself, and it is
not a review round at all. Run with the browser steps the first pass skipped, it found a
P1 three adversarial rounds had missed: the `animation` on `::-webkit-progress-bar` does
not run in Chromium or WebKit, so `background-position` is the permanent painted state,
and at `-40%` it parked the accent off-canvas. The CAS phase's only visual progress
affordance rendered as an empty track, and the reduced-motion user got MORE signal than
the default user. Every prior gate had read the stylesheet, where the rule is valid and
cascades correctly. Only pixels say otherwise, and the control that makes it a fact about
the pseudo-element rather than about headless — a `<div>` with identical keyframes, same
page, same run, four distinct frames against the bar's one — cost one probe.

**Infra:** round 3's reviewer reported that Vitest could not start because its read-only
sandbox denied temporary-directory creation, so its verdict rests on TypeScript plus
reading. Not a repo fault and not treated as one; the suites were run locally and in CI
against the same head.
