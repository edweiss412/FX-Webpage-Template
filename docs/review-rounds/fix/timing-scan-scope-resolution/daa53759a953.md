# fix/timing-scan-scope-resolution — review rounds (authoring segment)

Base `daa53759a953`. Spec + plan only; implementation is a separate session.

**The corpus JSONL holds ONE row and it is a `no_verdict`.** That row is honest and it is also the whole mechanical record, because the three rounds that actually happened did not go through `codex-guard` at all — OpenAI credits were exhausted (resets 2026-08-22) and the substitute reviewers are in-process subagents the wrapper never sees. `pnpm review:economy` will therefore report this arc as one failed dispatch; this file is what it cannot see.

### The spec side of the train

| round | reviewers | findings | verdicts |
| --- | --- | --- | --- |
| 1 | 4 (spec mechanism, spec discipline, plan execution, terse both) | 26 | BLOCKING · NEEDS-ATTENTION x2 · APPROVE(0) |
| 2 | 1 (verify repairs + fresh attack) | 6 | NEEDS-ATTENTION |
| 3 | 1 (verify round-2 repairs, frozen tree) | 5 | NEEDS-ATTENTION |

Rounds 1-3 reviewed the spec and the plan together, so the two stage sections below split one review train by SUBJECT rather than by dispatch.

**Examined —** the resolution design (`ts.Program` with `noResolve`+`noLib`, binding-identity suppression), its 15-probe evidence record, the plan's task/RED contract, and the handoff.

**Mechanization candidates** (each `declined:` here — this arc's mandate is a scanner repair, not new `spec:lint` arms; recorded so the next `spec:lint` arc has the evidence):

- **A key that can alias is not an identity.** Two of the three silent-drop findings across all rounds were the same shape one layer apart: the covered-declaration set keyed by `${file}:${line}` (two bindings on one line), and form 2d's site position taken from the property node while the resolver read the name node. Both would have suppressed a site today's mechanism reports. A guard's identity key wants a probe that asks "what two distinct things can share this key?" at design time — the same rule the round-economy memory already carries as "a key that can ALIAS is not a clock".
- **Numbers quoted from a probe RUN rather than from the committed transcript.** Every wall-clock figure in the first draft came from an interactive run; the record held a later re-run. `spec:lint`'s numeric arm cannot catch this — both are internally consistent. The mechanical form is to quote figures only from the committed file, which is what the repair did.
- **A markdown table that six appends walked off the end of.** Six probe rows lost their header and rendered as literal pipe text, including the row describing the design change. A doc-lint rule for "a table row not preceded by a header row" would have caught it; `spec:lint` has no such arm today.

**Judgment calls:**

- Whether the valuation axis (a reassigned `let` inventoried at its initializer) belonged in this arc. Filed as `BL-TIMING-SCAN-VALUATION-VS-REASSIGNMENT` with zero live instances rather than widened into — the recognizer-ratchet rule points that way, and the arc's own subject is resolution.
- Whether `noResolve`'s cost (a re-export hop through a module outside the universe now reports where it is suppressed) is acceptable. Taken as an accepted, stated cost against a 30× cheaper program, absent from the tree today, with `EXPLICIT_INCLUDES` as the escape hatch if it ever lands.

**Infrastructure:**

- `codex-guard`: three attempts, all `no_verdict` / `attempts_exhausted`, OpenAI usage limit to 2026-08-22. Batch-wide, not arc-specific.
- **Every one of the six substitute reviewers went idle without delivering, and every one had already written its report.** Recovered by scraping the last assistant message from `~/.claude/projects/<workspace>/<sessionId>/subagents/agent-a*.jsonl`. Degrading to "reviewers unavailable" — which this session had already written into the spec — would have discarded all 32 findings. The memory entry now leads with recover-before-degrade for ANY subagent, not just impeccable critique halves.
- Round 2's first finding was this session's own: three commits landed while it was reading. Round 3 ran against a frozen tree.

### Round 3, in one line

All six round-2 repairs LANDED and the freeze held (`a0368acfc` at start and finish). Its five fresh findings were an index that still pointed at the task its own repair had emptied (AC-9 mapped to Task 5), an EXTENDS list omitting Task 3's new export one line below the sentence warning about undeclared additions, an absence-only verification that deleting the paragraph would satisfy, a second cost budget with no baseline recorded to compare against, and a call count that was right for the post-#827 tree while the plan claimed it was measured at plan time on this one. None was a design defect; every one was a seam where a repair changed an instruction and left an index, a list, or a baseline pointing at the old shape. That is the class this arc would watch in round 4 if there were one.

### What the arc did NOT do

A fourth round. Round 3 reported the silent-drop shape closed after checking the covered-key producers against the landed scanner directly, and its five findings were bookkeeping seams rather than mechanism. The remaining risk is stated instead of reviewed away: no opposing model has read any of this, and the implementer's round-1 diff brief carries the cross-model gate for the design as well as the diff.

### The plan side of the train

**Examined —** the task/RED contract, the acceptance-criteria map, the mutant protocol, the meta-test inventory, the handoff, and the ordering constraint that Task 4's line-keyed `siteId` re-derivation imposes on every later edit to the scanner.

**Mechanization candidates** (each `declined:` here — this arc's mandate is a scanner repair, not new `spec:lint` arms; recorded so the next `spec:lint` arc has the evidence):

- **A repair that moves an instruction must sweep the indexes pointing at it.** Round 3's findings 3 and 4 were both this: AC-9's coverage row still routed to the task whose step now says "the header rewrite is NOT here", and the EXTENDS list omitted Task 3's new export one line below the sentence warning about undeclared additions. `spec:lint` has no arm for "a plan's AC map disagrees with the task bodies", and one would be cheap — the `ac=` markers already declare the mapping the table restates.
- **An absence assertion needs a positive counterpart, everywhere, not where it was first noticed.** Round 2 repaired the mutant protocol for two absence-asserting cases; round 3 found AC-9's verification with the same defect untouched. The sweep was per-instance rather than per-shape.
- **A budget without a recorded baseline is not a budget.** AC-10(b) (`mutation:guards` ≤ 25%) had no before-number in Task 0 and no trigger in Task 5, so an implementation could breach it and pass every step.

**Judgment calls:**

- The same-line-neighbour case is kept as a REGRESSION PIN rather than dropped: it is green today (probe P16) and red only against a design this arc rejected, so it proves nothing about the implementation but it does stop a future refactor from re-adopting a key that aliases. Counting it as a RED would have been the tautology; deleting it would have discarded the one assertion that pins the identity choice.
