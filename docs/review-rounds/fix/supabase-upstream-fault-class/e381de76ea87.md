# Review rounds — fix/supabase-upstream-fault-class @ e381de76ea87

**Why this file exists.** The arc absorbed `origin/main` at `e381de76e` mid-plan-stage, which moved the merge base, and the corpus is keyed by merge base. Rounds 1 and 2 of the plan stage are at `d04d6370985f`; the two here are rounds 3 and 4 of the arc. They were DISPATCHED as `--round 3` and `--round 4`, which was wrong: `--round` restarts at 1 after anything that moves the merge base, so a new base's first dispatch is round 1. The rows were renumbered to 1 and 2 to match the rule rather than leaving a non-contiguous start, which reads as rows lost or hand-edited — the one diagnosis that is not true here.

## plan — 2 rounds

**Examined:** `docs/superpowers/plans/2026-08-25-supabase-upstream-fault-class.md` against its spec, plus the already-committed observer, describer and harness. **Cross-base total: 4 plan rounds**, split by the mid-arc absorb of `origin/main` at `e381de76e`, which moved the merge base and so opened this file. This file holds the arc's LAST two plan rounds. Declared findings across the whole arc, both bases summed: 5, 6, 5, 2 — four rounds, of which rounds 1 and 2 sit at the previous base `d04d6370985f` and rounds 1 and 2 here are the third and fourth the arc actually ran. **Every finding in every round was accepted; none was refuted.**

**Mechanizable:** one shape, found three times, and it is the reason this stage cost four rounds rather than two.

Every round's largest finding was the same defect wearing different clothes: **a predicate that asserts SHAPE while the plan claims it asserts BEHAVIOUR.** Round 1 found AC-4's workflow assertions passing while `playwright.config.ts` lacked `stdout: "pipe"`, which leaves the whole capture chain decorative. Round 2 named four mutants that satisfied the repaired "pins the commands" predicates — `rm app-e2e.log` as the replay, a grep from another input plus a hardcoded `count=0`, an unconstrained dump body, a dump condition reversed to `== '0'`. Round 3 found two MORE that survived the equalities I had then written: a redirect to `wrong.log`, and an extract body that deleted the load-bearing `|| true` or appended an overwriting echo.

Three rounds, one axis, and each repair was itself too weak in the same direction. The arm is not a new tool — it is the four-mutant rule this project already has, applied at PLAN time rather than at implementation time. Every predicate the plan proposes gets its mutants written down beside it before dispatch: the emptied value, the appended suffix, the present-but-not-live form, and each discriminating parameter varied. That is exactly what round 2 and round 3 did on my behalf, one round each, and it is cheap to do first.

declined: no ledger row, by Eric's standing no-new-rows directive on this arc. The decline is correct on the merits as well: the rule exists and is written down in the writing-plans additions this arc was required to read. It was not applied to the plan's own predicates because I read it as a rule about TEST bodies rather than about any assertion the plan proposes, and the repair is to apply it where it already reaches, not to add a second copy of it.

**Judgment:** two.

The first was mine and round 3 called it P0 correctly: the plan read invariant 12's PURPOSE and relaxed its LETTER so repair commits could follow Task 6. An invariant is not a plan's to reinterpret from the inside, and the reading was convenient in the direction that saved work, which is the tell. The resolution costs two commits on a docs-only change and only when the final review is non-empty.

The second was the merge-base in Task 6's no-new-id arm: correct on a developer machine, unresolvable in the checkout where the test actually runs, because that project's workflow grafts the ancestry away with a depth-one fetch. A check that computes locally and cannot run in CI is worse than no check, because it reads as coverage.

**Infra:** unchanged from the spec stage. The reviewer's sandbox could not collect Vitest (`EPERM` creating its temporary SSR directory) in any round, and said so each time without inferring a green from it. Every predicate probe it ran — the four mutants, the depth-one ancestry check, the classifier counts — it ran directly rather than through the suite, which is why those findings are as concrete as they are.
