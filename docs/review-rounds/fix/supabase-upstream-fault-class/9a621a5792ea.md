# Review rounds — fix/supabase-upstream-fault-class @ 9a621a5792ea

**Why this file exists.** The arc absorbed `origin/main` three times while the diff stage was live, and the corpus is keyed by merge base, so the diff rounds are spread across three bases. This is the latest one holding diff rows. The stage's COUNTED rounds by base: `aea3423cfbd2` 2, `94de73ef6e53` 1, `9a621a5792ea` 1. This file also holds one row with NO verdict — a dispatch stopped because it carried the previous base's round number, which the base rule forbids — and a row without a verdict is recorded but never counted.

## diff — 1 round

**Examined:** the whole diff of `fix/supabase-upstream-fault-class` against `origin/main`, 26 files: the transport observer and its install on every server-side client factory, the shared bounded target describer, the walked coverage guard, the app-e2e capture chain in both halves, the mutation enrolment, and the graduation of both ledger rows. **Cross-base total: 4 counted diff rounds**, declared findings 1, 4, 2, 1. Verdicts NEEDS-ATTENTION, BLOCKING, BLOCKING, BLOCKING. **Every finding in every round was accepted; none was refuted.**

**Mechanizable:** one shape, and it is not a missing detector.

Three of the four rounds found the same thing in different clothes: **a guard of this arc's own making that reported OK while not covering what it claimed.** Round 2 found AC-5's structural proof promised in the plan and never written, so the production wiring was correct and nothing asserted it. Round 3 found the coverage guard deduplicating by FILE, so a construction added beside a sanctioned one inside `lib/supabase/server.ts` left the hit set unchanged, and the no-new-row guard reading only the BACKLOG pair while calling itself "anywhere". Round 4 found an EXEMPTION whose stated ground was false: `lib/dev/materialize/client.ts` excused as "not a request path" while two server actions construct it per request.

The arm is the four-mutant rule this corpus already carries, applied to the guard rather than to the code it guards. Every one of these was found by constructing the input the guard claims to reject and observing that it does not: a behaviour-preserving private describer, a second construction in an existing file, a `DEF-` row in a deferred ledger, a 502 through the exempted factory. All four are cheap, and all four were run only after a reviewer asked for them.

Round 4's is the one worth carrying furthest, because it is not a coverage gap at all. **An exemption is only as good as the sentence justifying it, and that sentence never executes.** The mutation score cannot help here either: 8/8 on `observeTransport.ts` measures whether that module's suites pin it, and says nothing about where the module is installed. An installation bypass the wiring guard explicitly accepted is invisible to both.

declined: no ledger row, by Eric's standing no-new-rows directive on this arc, and the decline is right on the merits. What is missing is not a check but a habit — write the mutant for the guard at the same time as the guard — and a habit is not a queue entry anyone can close. It is recorded in the plan's working rules instead.

**Judgment:** two, both mine, both P0.

Round 2 caught the plan reading invariant 12's PURPOSE and relaxing its LETTER so repair commits could follow Task 6. An invariant is not a plan's to reinterpret from inside, and the reading was convenient in the direction that saved work. The repair costs a revert and a re-application; this arc paid it three times as later rounds kept landing after the graduation, and deferring it to immediately before the push is the ordering that stops the churn without weakening the rule.

Round 2 also caught two measurements of mine that did not measure what they claimed: an AC-4 control that used a DIFFERENT spec, confounding injection with route and workload, and a pre-existing-failure isolation that reverted one file while leaving `stdout: "pipe"` active. Both were redone. The true control needed all THREE injector headers removed, not one — two cases override `FORCE_HEADERS` inline — and removing only the first still yielded 19 records.

**Infra:** the reviewer sandbox could not collect Vitest in any of the four rounds (`EPERM` creating its temporary SSR directory) and said so each time without inferring a green from it. Every finding above therefore came from direct probes rather than from the suite, which is why they are as specific as they are — and why the executable checks were the implementer's to run on this side.
