# Review rounds — fix/supabase-upstream-fault-class @ d04d6370985f

## spec — 4 rounds

**Examined:** the arc's spec, `docs/superpowers/specs/2026-08-25-supabase-upstream-fault-class.md`, together with the transport observer, the shared target describer and the plant-four harness under `lib/supabase/` and `tests/supabase/`. Declared findings by round: 9, 3, 4, 2. Verdicts BLOCKING, BLOCKING, NEEDS-ATTENTION, NEEDS-ATTENTION. **Every finding in every round was accepted; none was refuted, and no round repeated a prior round's vector.**

**Mechanizable:** two, and the same gap under both.

declined: no ledger row, by Eric's standing no-new-rows directive on this arc, and the decline is the correct disposition rather than a concession to it. The arm both findings want is not a new detector — a detector for "will this workflow edit break the coverage scanner" IS the coverage scanner, and `tests/ci/_metaE2eWorkflowCoverage.test.ts` already existed and would have gone red the moment the pipe landed. What was missing is an ORDERING: run the guard that reads a surface at design time, while the shape is still cheap to change. That is a working practice, not a check, and it is recorded as one in this arc's plan §7 and in `AGENTS.md`-adjacent session practice rather than as a queue entry nobody can close. The smaller item is already mechanical and needed no arm at all: the committed clustering probe had stated an exact extraction rule for this very corpus before the arc began, and reusing it instead of inventing one is free.

The capture step was designed from a written recipe (`BACKLOG.md:683-688`, `tee` plus `set -o pipefail` under `shell: bash`, which `x-audits.yml` uses at thirteen sites) without first running the guard that reads the surface being edited. That shape would have made all twenty app-e2e specs report as covered by no workflow: `tests/ci/_workflowCoverageScan.ts` refuses both any pipe and a `shell:` key on any step in the job. `tests/ci/_metaE2eWorkflowCoverage.test.ts` already existed and would have gone red the moment the pipe landed. The same gap produced the Playwright finding: `stdout` is forwarded only on an explicit `"pipe"`, readable in the config and the vendored plugin the whole time.

The arm is not a new detector. **A detector for "will this workflow edit break the coverage scanner" IS the coverage scanner.** It is an ordering rule: when a change edits a surface an existing guard reads, run that guard at DESIGN time, while the shape is still cheap to change. Both findings cost a round each and both would have cost nothing.

One further mechanizable item, smaller and already mechanical: rounds 1 and 3 both landed on the measurement's counting rule, and a committed probe (`docs/superpowers/specs/ci/probes/2026-08-24-admin-loader-502-clustering.md:164`) had stated an exact rule for the same corpus before this arc began. Reusing a ratified extraction rule instead of inventing one is free; inventing one cost two rounds and produced a table that omitted four of the seven consumers.

**Judgment:** three, none mechanizable, none regrettable.

The association-versus-causation line in §2.2 is the arc's central judgment and review forced it twice. The first draft treated the log's silence about other mechanisms as evidence, while the arc's own §9.2 records that the likeliest alternative path logs nothing — silence used as information exactly where it carried none. The corrected table then produced the fact that settles it better than any argument: an attempt carrying two 502 events that went green. A fault is not a failure.

The exclusion rule was the second. Defensible when written, wrong in fact: one excluded run was a third occurrence of the deciding case. The repair was to delete the rule rather than defend it, and the unselected population proved stronger than the selected one.

The disposition was the third, and it was escalated rather than decided — twice. Once when the evidence dissolved the repair choice the row posed, and again when the justification changed underneath a ratification already given. Both were re-scope calls belonging to the orchestrator, and the second escalation mattered more than the first: the outcome did not change, only the reason, and a ruling resting on a withdrawn reason is a ruling nobody can defend later.

**Infra:** the reviewer's sandbox could not execute Vitest (`EPERM` creating its temporary SSR directory) and could not reach `api.github.com`. It said so each round and declined to use either failure as evidence, which is correct behaviour, but it means every code verdict here came from source reading rather than execution. Round 3's sharpest finding — that the plant-four suite would pass a `clone()` regression — was reached by reading the assertions, and the mutant that confirmed it was run on this side. Worth knowing when reading these rounds: the reviewer's reach was static, and the arc's executable checks were the implementer's to run.

**Why the arc stopped at four and did not open a fifth.** Round 4 evaluated the spec against a closed criterion the brief stated, found criteria 1 to 4 and 6 met, and returned two gaps in criterion 5 — both about executable proof rather than about the design. Both were mechanically checkable, so the arc closed them with mutants rather than with another opinion: a clone-and-discard mutant and a rebuild-the-Request mutant each survived every plant-four assertion before the repair and each kills a case after it. Past the round cap a killed mutant is a better answer than a fifth reviewer, and the declared-findings trend across the four rounds is 9, 3, 4, 2, with no round repeating a prior round's vector and no round opening a new axis.
