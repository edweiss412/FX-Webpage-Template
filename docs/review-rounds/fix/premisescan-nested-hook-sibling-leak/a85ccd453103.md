# Review-round economy — `fix/premisescan-nested-hook-sibling-leak` @ `a85ccd453103`

Arc: `BL-PREMISESCAN-NESTED-HOOK-SIBLING-LEAK`. Subject is `tests/mutation/source/premiseScan.ts`, a
recognizer, plus the spec and plan describing a three-line narrowing of it.

## spec — 10 rounds

**Examined:** all seventeen findings of rounds 1-10 (1, 3, 5, 1, 1, 1, 1, 2, 2 and 0 — round 10
returned APPROVE), sorted by what each was a defect IN, and by whether a mechanism could have caught
it before a reviewer was paid to.

The composition is lopsided and worth recording plainly. **Zero findings touched the repair.** The
narrowing itself — three lines in `hookBodies`, with verdict-neutrality measured against the shipped
classifier before a line was written — was never contested in ten rounds. Every finding was about
the documents: an enumerated acceptance criterion, an inert task-marker region, a false `red-state`
claim, an over-stated census denominator, a stale lint count, an unswept inventory, an off-by-one
citation, and a TDD ordering violation.

That is not a complaint about the reviewer; each finding was correct and three were materially
important. The inert markers meant the plan's own contract was checking nothing. The false
authored-red meant a task claimed a discrimination its command could not make. The round-4 ordering
finding meant two tasks would have changed production before the test that reds without it — the
`export` authored ahead of its consumer can only ever be observed green — and round 5 found the
same class surviving in one more place after that repair: the matcher DEDUP still preceded the only
test able to exercise it, because the behavioural family provably cannot. But it does say where this
arc's review cost went, and most of it is mechanizable.

**Mechanizable:**

1. **The inert task-marker region is the expensive one, and it is already detectable from data the
   linter computes.** A plan carrying `<!-- task: … -->` markers and NO
   `<!-- tasks: depth=N red-contract -->` region parses to zero enrolled markers, and `spec:lint`
   then reports `0 hard` — a report about nothing, which its author reads as a pass. `taskTopology`
   (`lib/specLint/taskContract.ts`) already computes both the marker count and the region extent;
   nothing compares them. `taskMarkers > 0 && taskRegionLines === 0` is an advisory the arm could
   emit today, and it would have removed one full round here. Filed as
   `BL-SPECLINT-ORPHANED-TASK-MARKERS`.
2. **A document restating a tool's output goes stale by construction, and the second repair failed
   the same way as the first.** §7 quoted `0 hard, 1 advisory`; two rounds later the truth was
   `0 hard, 3 advisory` and round 3 spent a finding on it. The repair — declare the advisories by
   CLASS instead of by count — went stale too, and round 9 spent another finding on two classes the
   list had grown past. **An enumerated list is a count wearing a longer form.** The third repair
   states only the invariant: zero hard, nothing suppressed, and `codex-guard --lint-doc` attaches
   the LIVE report to every dispatch, so the reviewer reads the tool rather than the document's
   account of it. Generalizes to any document restating a tool's output, and it is a
   `docs/agents/spec-self-review.md` rule rather than a tool change.
3. **Production-before-red survived its own repair TWICE**, which is the argument for mechanizing it
   rather than trusting a re-read. Round 4's resequencing fixed two tasks and left a third standing;
   round 5 fixed that one by introducing reds that round 6 showed were invalid by the project's own
   RED-validity rule. Three dispatches, one axis, each repair adding surface — which is precisely the
   same-axis-recurrence shape AGENTS.md says to answer by NARROWING. The eventual repair does exactly
   that: **the two exports are deleted from the design entirely**, the derived covers read the
   scanner's source through the AST instead, and the production surface drops back to two edits with
   every red naming a scanner line. Both halves are statically checkable — a production edit ordered
   before the red it answers, and a `red=` whose failure is an unresolved import — and the
   red-contract arm already parses the marker and its owned steps.
4. **The root-file red-target constraint is ALREADY FILED**, as
   `BL-SPECLINT-RED-TARGET-CANNOT-NAME-A-REPO-ROOT-SURFACE` (filed 2026-08-18 by a different arc).
   Round 3's finding here is a second incident on that row rather than a new one: Task 6's
   `red-target` cannot name `BACKLOG.md`, so it names the guard predicate instead. Recorded here so
   the row accumulates evidence instead of the class being re-minted.


**Judgment:** the derived-versus-enumerated class (r1 finding 1, r3 finding 4) is real review value
and is not mechanizable. Deciding that a criterion's population must be READ from the matcher rather
than typed beside it is a design judgment, and it generalized twice in this arc — the sweep that
repaired AC-5 carried AC-6 in the same round, and round 3 still found the meta-test inventory
unswept, which is the same class one level up. Round 3's census-bounding finding is also judgment:
the harness was correct, its denominator was not what the prose claimed, and only a reader comparing
the two would see it.

**A second class, and it is ALREADY FILED.** Round 7 found the round-6 narrowing applied to the
sections it edited and not to the rest: the spec's §2 preamble still said AC-5 and AC-6 need extra
production edits, the plan's own surface section still listed the two exported name lists, and
Task 5 still assumed they existed. That is
`BL-SPEC-CLAIM-SWEEP-AFTER-REASONING-FINDING` (filed 2026-08-18 by `fix/control-outline-border-token`,
two incidents there), and this is its third. Recorded here so the row accumulates evidence rather
than the class being re-minted — and worth noting that the repair which triggered it was itself the
correct one, so the cost is entirely in the sweep, not the decision. The sweep that closed it was a
derivation: `grep -in export` over both documents, every hit dispositioned, leaving only negations.

Round 8 found the same class once more, in its cheapest form: two `Task N Step M` references in the
plan's acceptance index still pointed at the pre-resequencing numbering, and one task's
edit-surface sentence still said "no production edit except Step 3" while a later step installs and
reverts a mutant. **That half IS mechanizable and is the fourth entry below** — a plan's own step
map is derivable from its headings, so a reference naming a step its task does not have is a static
error. The sweep that closed it was that derivation rather than a re-read:

```
for each `## Task N:` collect its `- [ ] **Step M` numbers; assert every `Task N Step M`
reference in the document resolves against that map
```

which returned `unresolved references: none` after the repair and would have returned both before
it.

4. **A plan's internal `Task N Step M` references are checkable against its own headings.** Round 8
   spent a finding on two stale ones left by the round-4/5/6 resequencing. Ten lines of derivation
   settle it, and the red-contract arm already walks the task topology this needs.

**Judgment, added at round 9:** the embedded-fixture census miscounted its UNIT — 15 "literals" where
the truth is 11 literals carrying 16 `describe` sites. The conclusion did not move (the one leaking
literal is the same fixture), but a population figure whose unit is wrong is exactly the shape round 3
forced a bound on, one level down. Counting literals and sites separately is now explicit in both the
spec and the probe record.

**Infra:** none. All ten dispatches returned a verdict — no `no_verdict`, no reap, no wedge, and
the wrapper's native-binary path resolved on every attempt.

## diff — 15 rounds

**Examined:** every round of this stage, against the same question the spec section asks. Round
counts are read from the corpus rather than restated here — see the heading, which the gate checks.

**The findings that touched the repair are rounds 2, 4 and 6 — enumerated rather than tallied,
because a count in a growing document goes stale and this one did.** All three sit on the same axis:
the stop's argument handling.
Round 2 probed a hook in a nested registration's EAGER positions — a `describe.each` producer, a name
argument — and showed the stop pruning the registration WHOLE, which read those hooks as the nested
branch's and turned a touching sibling `environment-free`. A silent free, introduced by this arc,
caught by review rather than by the suite. That is what a whole-diff review is for, and it justifies
the stage on its own.

**Round 4 is the second on that axis and the reason the repair is now grammar-closed.** The body
test read the argument node's OWN kind, so a runtime-transparent wrapper — `(fn)`, `fn as T`,
`fn satisfies T`, `fn!`, a type assertion — made the argument something else and the walk descended
into the body, reintroducing the very leak the arc closes. Five spellings probed, all reproducing.
Per the same-axis recurrence rule the repair NARROWS rather than accumulating spellings:
`isSuiteBody` tests through TypeScript's outer-expression grammar, a closed set, so a sixth wrapper
spelling is covered by construction instead of by a sixth round.

**Round 5 is the THIRD on that axis, and it was DECLINED rather than repaired — which is the
finding about this arc's own review economy.** A comma expression whose right operand is the suite
callback is outside TypeScript's outer-expression grammar, so `isSuiteBody` reads it as eager and the
nested hooks reach the sibling. Handling it would have been the third grammar feature added to one
predicate in one afternoon, which is exactly the ratchet AGENTS.md's same-axis rule exists to stop.
It is filed as spec §4 limit 4 instead: population **zero across the enrolled suites**, outside the threat fence, and failing in the CONSERVATIVE direction (the sibling reads
touching, so a test is told to carry a premise it does not need — loud, never a silent free).

**The round was invited by my own brief, and that is the mechanizable half.** Round 4's repair closed
its accept-set through the outer-expression grammar, and round 5's brief said a wrapper "outside that
grammar" would be admissible — an admissibility clause that named the grammar but NOT the threat
fence or the probe domain. AGENTS.md warns about exactly this: *a clause that omits the citation
loses to itself.* The reviewer applied my words correctly; the words were wrong. **Every admissibility
clause must cite the fence AND the domain, and a brief that widens one clause past them buys the next
round itself.**

**Round 6 is the fourth on that axis and was REPAIRED, not declined — the distinction is the point.**
`isSuiteBody` omitted `ExpressionWithTypeArguments`, a member of TypeScript's own
`OuterExpressionKinds`. That is not one more grammar feature bolted on; it is the closure the code
ALREADY CLAIMED, failing to hold. Round 5's comma expression sits outside that grammar and was
declined; round 6's omission sits inside it and made the claim false, so completing it is the
narrowing finishing rather than the ratchet resuming. The reviewer swept the class and found no other
source-representable omission — `PartiallyEmittedExpression` is synthesised-only — so the accept-set
is now checkable against TypeScript's list rather than against whichever spellings were thought of.

**Round 7 split the two dispositions cleanly, which is what a healthy late round looks like.** Its
first finding — a suite registered with a NAMED factory loses its hooks entirely, reading free while
the hook spawns — was bracketed against `origin/main` BEFORE any repair, and the named-factory rows
are byte-identical on both trees. Pre-existing, a different mechanism (`hookBodies` collects
lexically; a module-scope factory's body is elsewhere), filed as
`BL-PREMISESCAN-NAMED-SUITE-FACTORY-HOOKS-LOST` with the differential and recorded as spec §4 limit 5.
Its second finding was mine and actionable: the PLAN was never swept after rounds 2, 4 and 6 — its
acceptance index stopped at AC-7 and its only `isSuiteBody` mention was an undefined call — so an
implementer following the canonical artifacts could not reproduce the shipped repair under TDD.

**Round 8 made it the FIFTH, and its instance is the most mechanical of the set:** inserting two
limits before §5 created a SECOND `## §4 Documented limits` heading, so "§4 limit 1" named two
different things and this record's own fenced citations pointed at neither uniquely. Also a stale
"four separate fixture classes" where six were enumerated. Both repaired by derivation — the
sections merged and renumbered 1-5, the count phrase deleted rather than corrected (a count that
moves is not worth restating, which is r1's lesson), and every `spec §4 limit N` citation across
this record, the ledger and the plan checked to resolve against the definitions.

**That is the arc's document-sweep class again — its occurrences are enumerated below rather than
tallied here, for exactly the reason above** (spec r7, spec r8, diff r3, diff r7, diff r8, diff r11, diff r12 F2),
and the pattern is now specific enough to name: when a review round changes the DESIGN, the spec gets
swept because the finding points at it, and the PLAN gets forgotten because no finding did. The
mechanizable form is a plan/spec acceptance-criteria parity check — every `AC-n` defined in the spec
must appear in the plan's index and be discharged by some task — which is a set comparison over two
documents the lint already parses. Filed against
`BL-SPEC-CLAIM-SWEEP-AFTER-REASONING-FINDING` as its fourth incident rather than minted anew.

**Round 9 found the arc's own criterion over-claiming, which is the most useful thing a late round
can do.** AC-8 stated the eager-position rule universally, while every fixture supplied an enclosing
`describe`. At FILE scope the top-level seed collects only direct hook statements, so the same hook
in an eager position reaches no sibling — bracketed against `origin/main` and IDENTICAL on both
trees, with that round's own sweep putting the population at 84 constructed cases. Pre-existing, so
AC-8 is BOUNDED to a `describe` parent and the file-scope case is spec §4 limit 6, filed as
`BL-PREMISESCAN-FILE-SUITE-EAGER-HOOKS-LOST`.

**Judgment, and it is worth stating because it cuts against this arc's own instincts:** an acceptance
criterion whose fixtures all share a structural feature is claiming less than its words say, and
nothing mechanical catches that. No lint arm reads "every AC-8 fixture has an outer `describe` while
AC-8's sentence does not require one". That is a reader comparing a criterion's SCOPE against its
witnesses, which is exactly the work a whole-diff review exists for and the third time in this stage
it paid for itself.

**Round 15 reviewed the GRADUATION as a working-tree diff**, which is the plan's Task 6 Step 5 and
exists because invariant 12 forbids anything following the graduation commit — so without it the
PR's last commit ships unreviewed. It earned its dispatch: the record claimed "four gaps filed" and
named three ledger rows. The fourth, the comma-expression body, deliberately has NO row because its
worst case is conservative and its population is zero, which the 2026-08-04 filing bar demotes to a
documented limit. That asymmetry was in the author's head and not on the page, and a graduation
record is precisely where such a thing must be written down.

**Round 14 returned APPROVE with zero findings**, on a tree whose scanner had been unchanged since
the round-12 revert. The stage converged where the code stopped moving, not where the reviewer tired:
rounds 12, 13 and 14 found no production-code defect between them, and round 13 independently
reproduced round 12's bracket.

**Round 13 found the two remaining restated numbers in this very document, which is the class
closing over its own filing.** A census cardinal survived in the spec and in this record after the
spec had already promised not to restate it, and two cumulative tallies here had gone stale — "two
findings touched the repair" when round 6 made it three, and a "FIFTH" occurrence beside an
enumeration of seven. Both are repaired the only way that terminates: the numbers are DELETED and the
things are enumerated. **A tally inside a document that keeps growing is a defect with a delay
fuse**, and this filing had been arguing that for six rounds while carrying three of them.

**Round 12 REVERTED round 10, and that reversal is the most important entry in this filing.**
Completing `MODIFIERS` was justified on its own terms — the gap was live in an enrolled suite and the
completion was verdict-neutral — but it made a shape reachable that had not been: `registrarRoot`
peels callee CALLS and PROPERTIES in separate loops, so a conditional chain resolves by neither.
Bracketed on both trees, `origin/main` returns `[]` for `test.skipIf(...).each(...)` — silently
incomplete — while the completed set returns a fabricated `environment-free` row. **Silently
incomplete became silently WRONG, one round after the widening.** Population of chain forms across
the whole `tests/` tree: ZERO.

That is the same-axis recurrence rule's prediction arriving on schedule — *each widening is a bigger
target for the next round* — and the response is the one AGENTS.md prescribes: **the kill.** The
completion is reverted, the scanner diff returns to the two edits the arc is actually about, and BOTH
halves (the uncensused `.skipIf` test AND the peel loop) ship together on
`BL-PREMISESCAN-REGISTRAR-ACCEPT-SETS-HAND-MAINTAINED`, because completing the sets without fixing
the loop trades an omission for a wrong verdict and fixing the loop without completing the sets
leaves the enrolled test uncensused.

**The cost of learning this the slow way is legible here:** round 10 spent a repair, a gate cycle and
a re-key; round 12 spent another round finding what the repair opened; the revert spent a third. Had
the sixth round on this axis been read as the recurrence signal it was, the kill would have come
three rounds earlier. **The rule's value is entirely in applying it EARLY, and this arc did not.**

**Round 10 split on the probe domain, and that is the rule doing its job.** Both findings were
hand-maintained accept-sets drifting from Vitest: `MODIFIERS` missing `shuffle`/`skipIf`/`runIf`, and
`HOOK_REGISTRARS` missing `aroundAll`/`aroundEach`. Both pre-existing, both identical on
`origin/main`. They were dispositioned OPPOSITELY because the domain says so:

- **`MODIFIERS` was REPAIRED.** `test.skipIf(...)` is live in an ENROLLED suite
  (`tests/cross-cutting/psqlStartupFileSuppression.test.ts:1653`) and that test was absent from the
  scanner's census entirely — a silent gap inside the declared probe domain. Verdict-neutral
  (`_metaPremiseContract` 10 passed either way), so the completion is cheap and mandatory.
- **`HOOK_REGISTRARS` was NOT.** Zero `aroundAll`/`aroundEach` call sites across all 62 enrolled
  suites, and adding them changes which tests are classified corpus-wide — an AC-1 movement outside
  this arc's mandate. Spec §4 limit 7.

**The derived cover paid for itself here, measurably.** Adding three strings to `MODIFIERS` generated
`describe.shuffle`, `describe.skipIf` and `describe.runIf` cases in the AC-5 family with no test
edit. That is the property AC-5 was built for, and it is the argument for deriving accept-sets rather
than enumerating them — which is why the class is filed as
`BL-PREMISESCAN-REGISTRAR-ACCEPT-SETS-HAND-MAINTAINED` with the derivation as its repair. Completing
a hand-maintained list BY HAND is what this arc did twice (round 6, round 10) and it does not
terminate: the next Vitest release adds the next member.

**Round 11 is the document-sweep class for the SIXTH time, and its instance is the most damning
argument in this filing for mechanizing it.** §7 claimed `spec:lint` reports 0 hard; a citation added
at round 10 pointed into an installed dependency, which is untracked, so the claim was false. **The
repair then broke the same rule a second time** — the sentence written to explain "never cite a
dependency path" contained a backticked dependency path and re-failed the lint. Two consecutive
authorial attempts, one of them by an author actively concentrating on that exact rule.

Nothing about that is a knowledge gap, which is what makes it mechanical work: a lint arm already
computes it, and §7's contract is now stated as a claim to RE-RUN rather than to trust.

**The other two were the arc's recurring shape one level out.** Round 1: a quoted `describe`-site
census that the arc's own fixtures moved. Round 3: the spec, plan and probe record still describing
the design round 2 had killed — and since AGENTS.md rule 7 makes the spec canonical, an implementer
following it would have restored the silent free. Both are the same class the spec stage saw at
rounds 7 and 8: **a repair fixes the site the finding names and leaves the document unswept.**

**Mechanizable:**

1. **A code block in a spec or plan that quotes production can be diffed against it.** Round 3's
   finding is mechanical in principle: §2's fenced `ts` block and plan Task 2 Step 3's copyable code
   both purport to show `hookBodies`' stop, and both disagreed with
   `tests/mutation/source/premiseScan.ts:1834` after round 2. A lint arm that anchors a fenced block
   to a `file:symbol` and compares them would have caught it before dispatch. Not filed as a new row:
   this is the third incident of `BL-SPEC-CLAIM-SWEEP-AFTER-REASONING-FINDING`, and it belongs there
   rather than in a fourth row minted for the same class.

**Judgment:** round 2 is not mechanizable and is the stage's whole value. No lint arm derives
"the arguments of a nested registration evaluate in the parent's scope" — that is knowledge of
Vitest's collection semantics, applied to a three-line control-flow change, by a reader who went
looking for what the change would newly miss.

**Infra:** round 4's first dispatch was KILLED mid-run by the harness and wrote `status:no_verdict`
with the reviewer's probes still in flight. Per AGENTS.md that is an infrastructure fault rather than
a review outcome, and it is NOT a counted round; it was re-dispatched to a fresh out-dir and the
verdict here is that second dispatch. Three background tasks were killed the same way during this
arc, which is why the long-running measurements moved to Monitor.

The arc's own gate cost is recorded on `BL-MUTATION-SITEID-LINE-KEYED-CHURN` rather than
here: two re-keys of one unmoved expression, each costing a full gate cycle, both bought by edits in
a different function. Nothing in the round corpus is a `no_verdict`.

