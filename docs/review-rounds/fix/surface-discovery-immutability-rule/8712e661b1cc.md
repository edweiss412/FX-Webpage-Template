# Round economy — fix/surface-discovery-immutability-rule (base 8712e661b1cc)

## diff — 0 rounds

**Examined:** the arc's whole diff history, not this branch alone. Diff review ran twice on
`fix/surface-discovery-unnameable-forms` (base `59a9ef25a97b`, then `bb440b9d1519`, split A/B each
time) and the repairs from its third round are what this branch carries. The convergence gate
blocked the next dispatch as round 6 on the same artifact. Its ruling was taken rather than
overridden: options 1 and 2 from its own list — ship the change, and record the residual as
documented limits.

**Judgment:** the finding axis was a WRITE DETECTOR, and each round bought exactly one more write
form. Round 1 found the module-name resolver ignored ordinary writes; round 2 added member writes,
compound assignment and `var` redeclaration; round 3 added destructuring assignment, `for..of`
targets, `Object.assign`, `Reflect.set`, `Object.defineProperty` and array `splice` — plus the other
half of the same defect, a refusal fired on `doIt.displayName = "..."`, which is correct code. That
is the shape AGENTS.md names: successive rounds widening a recognizer one input family at a time,
with the class-level repair pointing at NARROWING.

So the sixth round would have reviewed a question this branch no longer asks. What ships asks
IMMUTABILITY: a `const` binding cannot be rebound by any form in that enumeration, so the
enumeration collapses and the code SHRINKS — the member-chain ascent is deleted, the wrapper-kind
list is replaced by a downward walk from the three assignment constructs, and a nine-kind
declaration list is replaced by reading the NAME SLOT positionally.

**Judgment (continued):** the claims are settled by machinery rather than by another opinion.
`tests/log/mutationSurface/enumerate.ts` scores **248/249** (one survivor, carrying an `equivalent`
row) with zero unaccepted survivors; `totality.ts` is **20/20** with an empty ledger; the live
corpus walk is unchanged at **98 units and 0 gaps**, which is the false-advisory check the stricter
rule is most exposed to. The rewrite first scored 234/264 with 30 survivors; 29 were repaid with
cases rather than accepted, and simplifying the two kind lists removed the remaining sites outright.

**Documented limits, stated rather than left implicit:**

- A `let`- or `var`-bound module action is refused even when nothing writes it. The remediation is
  one keyword and the refusal names the export.
- A destructuring holder resolves one hop deep and only when nothing else in the file references it.
  A second reference — a member write, an `Object.assign`, an export, a call — refuses.
- `shadowsName` fails toward REFUSAL: any gap in it over-refuses and is surfaced by name, never
  silently resolved.

**Mechanizable:** none — the residual is a documented limit, not a mechanizable arm.

**Infra:** two dispatches on this branch produced no verdict for infrastructure reasons, neither of
them reviewer silence. The first hit the wrapper's 1200s attempt timeout and its retry inherited too
little of the total budget (`total_timeout` at 300s). A re-dispatch at a larger budget was refused
outright because `ATTEMPT_MAX_SECS` is capped at 1380s — the flag exits 2 rather than clamping, so
the out-dir stayed empty and looked, for half an hour, exactly like a long-running review. Both rows
are in the sibling `.jsonl`.
