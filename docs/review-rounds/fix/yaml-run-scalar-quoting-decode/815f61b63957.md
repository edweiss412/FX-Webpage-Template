# fix/yaml-run-scalar-quoting-decode — review rounds at merge-base 815f61b63957

The second closeout merge moved the base here, so numbering restarts at 1. The
rounds at the previous base are in `bcd3d088ec76.md`; the two are not
consolidated, and this file's round 1 is the arc's fifth gates round overall.

## diff — 17 rounds

**Examined:** the gates scope only. `core` closed at APPROVE with zero findings
in round 1 at the first base and was never re-dispatched, because re-reviewing a
closed scope manufactures a round. The per-round verdict and finding counts are NOT restated here: they are in the
sibling JSONL, `pnpm review:economy` reads them, and every hand-copy of them in
this file has drifted at least once.
Which rounds were BLOCKING is corpus-derived too, and is not listed here for the
same reason the totals are not: r16 turned "four BLOCKING rounds" into five while
this sentence sat unchanged, which is the drift in miniature.

Every one was real. None was refuted, and none relitigated a ratified decision.

**Judgment:** the previous filing at the earlier base concluded that the product
change was settled and what remained was documentation drift. **That conclusion
did not survive this base.** Several findings here are EXECUTABLE defects in
shipped gates, and the severity signal was a poor guide to which rounds carried
them: r2 and r3 were NEEDS-ATTENTION and carried executable defects all the same.
The count is deliberately absent — it moved twice while earlier drafts asserted
it. The instances are enumerated below and the corpus holds the arithmetic:

    r2  AC-8 seam check keyed imports by NAME alone, so a changed module source,
        a deleted or renamed default import, and a type-only conversion all
        false-passed
    r3  AC-8 still missed import ATTRIBUTES and the PHASE modifier one round
        after being widened for the above
    r4  AC-6 census accepted YAML PARSE ERRORS silently — `parseDocument` does
        not throw, it returns a document carrying `errors`, so a malformed
        workflow printed `parse failures: 0` and PASSED
    r13 the scanner fabricates a site for two unterminated spellings, `>(` and
        `\$(`, while `bash -n` exits 2 on both. Unlike the three above it is NOT
        repaired here: it is pre-existing, sits in the shell lexer rather than
        the YAML decode path, and surfaced at round 13. Carried as documented
        limit 9 and `BL-SHELL-UNTERMINATED-PROCESS-SUBSTITUTION-FABRICATES`

An earlier draft of this section listed four by including the census input-class
and registry cross-reference defects. Those were found at the PREVIOUS base and
belong to `bcd3d088ec76.md`; counting them here double-counted them across two
filings. Corrected, and worth recording as its own small lesson: a filing keyed
to a base must count only that base's rows.

The r4 finding is the one that matters most and it is worth stating without
softening. The arc exists because a quoted `run:` scalar was read wrongly in both
forbidden directions — one spelling fabricated a site, the other went silent. The
census is the gate that certifies the corpus contains none of that spelling, and
it was itself failing in the silent direction on unparsed input. The same failure
shape as the defect under repair, one layer up, in the machinery asserting the
repair is safe. The reviewer's probe was one ordinary edit to a live workflow.

**Some findings were PROCESS defects against invariant 12, and they came as a pair.**
r5 found that this branch had implemented one of
`BL-REVIEW-ROUND-REPORT-TEST-TIMEOUT-GROWTH`'s proposed changes while that row
sat `OPEN` with no branch claim. r6 then found that claiming it locally was not
enough: the branch had not been pushed, so the claim was invisible to
`ledger:claims` in every other session, which is the entire function of a claim.

Both have the same root and it is a skipped step rather than a judgement call. I
never ran `pnpm ledger:claims --check`, and never grepped the ledger for the test
I was about to edit — a row already existed, filed three times, carrying three
prior measurements of the exact failure I was independently measuring a fourth
time. Then, holding to a single-push discipline adopted for a different reason
(a push cancels queued path-filtered CI runs), I let that override an invariant
that says explicitly to push the claim immediately. A local discipline outranked
a stated invariant, which is the wrong precedence.

**A second pattern is worth recording, because it changed how the repairs were
made.** AC-8 was found false-passing in two consecutive rounds. The first repair
WIDENED it — module specifier, type-only flag, default imports — and the next
round found two more features it still could not see, import attributes and phase
modifiers. That is a recognizer growing one grammar feature per round, which
AGENTS.md names as the wrong direction under same-axis recurrence. The second
repair NARROWED instead: blank the named-import list and compare what remains,
so nothing enumerates the features and there is no list left to be incomplete.
The distinction between the two repairs is the most transferable thing in this
file.

The remainder were DOCUMENTATION: restatements drifting from the artifacts
computing them. The split into executable, process and documentation is the only
judgement here; the total is computed, and the documentation figure is whatever the
total leaves after the other two. None of the three is written down, because writing
one down is how each of them went stale before.

That line has now been wrong twice in the direction it exists to warn about. It said
fourteen and summed to twenty while the corpus already held twenty-two, and the
repair to sixteen went stale the moment round 15's own row landed. The lesson is not
"restate more carefully" - it is that a hand-classified split cannot be restated at
all. Only the SPLIT is judgment here; the total is computed, and the documentation
figure is now derived as total minus executable minus process rather than written
down beside them.

**Most of those were about THIS FILE**, and that is the finding worth keeping.
A filing whose subject is the round count changes every round, so each round
manufactures the next round's drift. Naming which rounds, and how many, would
be one more number that goes stale the moment the next row lands.

Those are counted in corpus units, one per reviewer finding. Counted as individual
wrong STATEMENTS the
figure is larger: as of r10 the archived outputs enumerated eleven, or ten once r6's
duplicated round-count claim is collapsed. That enumeration is the reviewer's and
is adopted rather than re-derived, because it is a judgement a script cannot
make — a bullet count over the same archives gives fourteen, since bullets about
other subjects are indistinguishable from bullets about this file without reading
them. Earlier drafts said "six instances, five distinct claims", true when only r6
and r7 existed and never updated as later rounds added more — which is why the
figure above is stated AS OF a named round rather than as a running total. A
dated fact cannot go stale; a live one goes stale every round, and this file
has proved that repeatedly.

Round 8's finding was that unit mix and round 9's was the arithmetic inside the
corrected version. This file then went on being the subject of later rounds too,
which is the point rather than a number worth pinning.

The conclusion it supports needs stating carefully, because a sweeping version of
it is false. It is NOT true that every hand-written count in this file was wrong:
"this file's round 1 is the arc's fifth gates round overall" and core's "APPROVE
with zero findings" were correct when written and never moved. What is true is
narrower and still damning — every count that TRACKS A GROWING CORPUS was wrong
at least once, while the claim sweep, which derives the same kind of number from
`DECLARED.length` and `MATCHER_CANNOT_REACH.size`, was correct in every round. A hand-written number is safe exactly when the thing it counts cannot
change; these did, every round, by construction.

What is written here, and what is not. There WAS a regenerator that recomputed
the round roster, the totals, the BLOCKING list and the Infra counts from the
corpus rows and asserted each substitution matched exactly once. It is retired,
because a generator that keeps a hand-written number correct is still a
hand-written number: it has to be RE-RUN, and the round that forgets is the round
that files the drift. Rounds 10 through 15 are that argument.

What replaced it is subtraction. The counts are simply not written down, so there
is nothing to regenerate and nothing to go stale. The one number left is the
heading, and `_metaReviewRoundEconomy` checks it against the corpus on every run
without anyone remembering to invoke it.

What remains judgement rather than derivation is listed below, stated because an
earlier version of this paragraph implied a script settled it. The list is
deliberately not counted: this very sentence said "two" against three bullets in
the commit that removed the other counts, which is the point being made.

- **The executable/process/documentation split.** `findingCount` records how many
  findings a round raised, never what kind. So the KINDS are judgement. The counts
  that once accompanied them are gone rather than recomputed.
- **Which rounds were about this file.** The corpus carries no subject field, so
  this can only ever be hand-stated — which is exactly why it is now stated
  qualitatively rather than as a roster that goes stale each round.
- **The Vitest `EPERM` result in Infra.** That comes from reading the reviewers'
  messages, not from any corpus field.

One of its derivations was outright WRONG until review caught it, and the lesson
outlives the script: it counted every JSONL row as a round. A row is not a round —
the sibling corpus files five rows for four rounds, because r1 dispatched core and
gates together. It happened to coincide at this base and would have misreported the
sibling. Anything counting rounds must count distinct `round` values on
verdict-bearing `diff` rows, the way the economy gate does.

The instance count above is adopted from the reviewer and dated, for the reason
given there. The generator's one genuinely good habit is worth recording even
though it is retired: it REFUSED rather than guessed, raising on a finding count
its number-word map could not name instead of writing a wrong word. A tool that
cannot express an answer should stop, not approximate. What replaced it needs no
such habit, because it writes no counts at all.

**Mechanizable:** the arc's own evidence points at one mechanism and away from
another.

declined: a lint for stale `path:NNN` citations. It catches a shape but not the
instances that actually occurred — round 4 at the previous base was an IN-RANGE
citation pointing at the wrong line, which no existence check sees. Declined once
already at the earlier base and re-declined here on the same reasoning, now with
a second supporting instance: the `322-328` range was in range of its file
throughout and simply named the wrong bullet.

declined: enumerating what a guard must inspect. Two rounds of AC-8 are the
argument against it. Every enumeration is complete until the next feature, and
each widening is a larger surface for the following round. The repair that held
was subtraction — derive what matters by removing the part allowed to vary. That
is a review habit rather than a tool, and a ledger row would not make anyone
adopt it.

The mechanism that DID work is already visible in this arc and needs no filing:
the claim sweep derives its counts from `DECLARED.length` and
`MATCHER_CANNOT_REACH.size` and was correct in every round, while every
hand-written restatement of those same numbers drifted. The dispatch brief's
scope partition now works the same way — computed at dispatch, not written.

**The sharper form, and the reason this section is now shorter than it was.** A
round-economy filing records the rounds, so a filing at a base that is STILL
RECEIVING ROUNDS changes every round, and each change manufactures the next
round's finding. This file was cited by round after round; its two siblings
were cited by none, and both verify clean against their own corpora. The
difference is not care taken — it is that their bases closed. Rounds cannot fix
a document that rounds are what perturb.

So the counts came OUT rather than being restated more carefully. What a gate
enforces stays: the heading count, which `_metaReviewRoundEconomy` checks against
the corpus. Everything else that could be derived was cut, and the numbers left
standing are historical — statements about what an earlier draft said, which are
fixed by the past rather than by the corpus. This was done once as a class, not
one number per round, because the one-per-round form is what produced r10, r11,
r12, r14 and r15.

**Infra:** no round was lost. Every dispatch returned a verdict.
Every one reported that vitest could not start inside the review sandbox (`EPERM`
on its temporary directory), which is a limit of the reviewer's environment, not
a finding — worth noting because it means no round here executed the suite, and
the suite results in this arc come from local runs.
