# Round-economy filing — fix/modal-wait-skeleton-tolerant @ b24e3ac5f2d8

## plan — 4 rounds

**Examined:** rounds 1-3 returned 5 / 4 / 1 findings, all accepted and repaired; round 4 APPROVEd with 0. Round 1 was red-contract structure (a Task 4 red command on a transient file that could never go green on the removal branch, deferred same-commit disposition re-scores, a Task 2 red whose named target the observed failure did not exercise, a FakePage cardinality case with no selector engine behind it, three red-mechanism wordings contradicting the plan's own probes). Round 2 was probe-backed assertion strength, two findings landing on round-1 repairs (an includes-filter watchdog assertion the initial race satisfies with no watchdog at all — plus the missing converse case; the jsdom cardinality case red-ing by empty NodeList rather than the claimed TypeError; a second red arm on Task 3 the plan had explicitly denied; missing same-commit `EXPECTED_LEDGER_KINDS` reconciliation on accepted-survivor branches). Round 3 caught the origin-(a) seam omission: the adopted deeplink URL literal stays an `a-route-literal` candidate, so `callsHelper`, the `a/member-helper-call` count, and `a/exempt-declared` all had to move — Task 2's GREEN as written left the meta suite red.

**Mechanizable:** one shape. The round-3 omission is a cross-origin coverage sweep made checkable: when a plan adds a helper entry-point name, grep the census for EVERY predicate holding the existing names (`HELPER_CALL`, `callsHelper`) and require the plan to disposition each hit site — a derivation, not an enumeration, since the sweep command (`rg -n 'openShowReviewModal(?:At)?|awaitReviewModalOrRecover' tests/ci/modalWaitHelper/`) regenerates the list.

declined: recorded for the lessons corpus as a plan-authoring step — one census is too narrow a target for a `lib/specLint` arm on current evidence, and the sweep command above regenerates the site list without a lint.

**Judgment:** rounds 1-2 were the red-contract and anti-tautology rules `docs/agents/writing-plans.md` already states, applied by a reviewer running spliced probes — non-compliance, not rule gaps, consistent with that corpus's standing observation. The watchdog includes-vs-equality finding is the guard-premise class on a unit assertion: the discriminating condition (only the watchdog's waitFor carries the bare selector) had to be stated and used, not assumed.

## spec — 2 rounds

**Examined:** round 1 returned one probed P2 (the adopted-site arithmetic summing a fixed three-id list, so the new U-frame member rule could be silently omitted — repaired to a derived sum over all origin-(f) member rules); round 2 APPROVEd with 0 after re-probing the repair.

**Judgment:** the finding is the derived-cover rule from AGENTS.md's class-sweep bullet applied to a meta-test aggregation; the repair (derive, don't widen the list) is the recorded disposition and is carried into the plan's do-not-relitigate set. No mechanizable residue distinct from the plan section's sweep note.
