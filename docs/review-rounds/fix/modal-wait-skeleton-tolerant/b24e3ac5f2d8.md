# Round-economy filing — fix/modal-wait-skeleton-tolerant @ b24e3ac5f2d8

## plan — 4 rounds

**Examined:** rounds 1-3 returned 5 / 4 / 1 findings, all accepted and repaired; round 4 APPROVEd with 0. Round 1 was red-contract structure (a Task 4 red command on a transient file that could never go green on the removal branch, deferred same-commit disposition re-scores, a Task 2 red whose named target the observed failure did not exercise, a FakePage cardinality case with no selector engine behind it, three red-mechanism wordings contradicting the plan's own probes). Round 2 was probe-backed assertion strength, two findings landing on round-1 repairs (an includes-filter watchdog assertion the initial race satisfies with no watchdog at all — plus the missing converse case; the jsdom cardinality case red-ing by empty NodeList rather than the claimed TypeError; a second red arm on Task 3 the plan had explicitly denied; missing same-commit `EXPECTED_LEDGER_KINDS` reconciliation on accepted-survivor branches). Round 3 caught the origin-(a) seam omission: the adopted deeplink URL literal stays an `a-route-literal` candidate, so `callsHelper`, the `a/member-helper-call` count, and `a/exempt-declared` all had to move — Task 2's GREEN as written left the meta suite red.

**Mechanizable:** one shape. The round-3 omission is a cross-origin coverage sweep made checkable: when a plan adds a helper entry-point name, grep the census for EVERY predicate holding the existing names (`HELPER_CALL`, `callsHelper`) and require the plan to disposition each hit site — a derivation, not an enumeration, since the sweep command (`rg -n 'openShowReviewModal(?:At)?|awaitReviewModalOrRecover' tests/ci/modalWaitHelper/`) regenerates the list.

declined: recorded for the lessons corpus as a plan-authoring step — one census is too narrow a target for a `lib/specLint` arm on current evidence, and the sweep command above regenerates the site list without a lint.

**Judgment:** rounds 1-2 were the red-contract and anti-tautology rules `docs/agents/writing-plans.md` already states, applied by a reviewer running spliced probes — non-compliance, not rule gaps, consistent with that corpus's standing observation. The watchdog includes-vs-equality finding is the guard-premise class on a unit assertion: the discriminating condition (only the watchdog's waitFor carries the bare selector) had to be stated and used, not assumed.

## spec — 2 rounds

**Examined:** round 1 returned one probed P2 (the adopted-site arithmetic summing a fixed three-id list, so the new U-frame member rule could be silently omitted — repaired to a derived sum over all origin-(f) member rules); round 2 APPROVEd with 0 after re-probing the repair.

**Judgment:** the finding is the derived-cover rule from AGENTS.md's class-sweep bullet applied to a meta-test aggregation; the repair (derive, don't widen the list) is the recorded disposition and is carried into the plan's do-not-relitigate set. No mechanizable residue distinct from the plan section's sweep note.

## diff — 4 rounds

**Examined:** rounds 1-3 returned 1 / 1 / 2 findings, every one accepted and repaired; round 4 was dispatched at the spec §6 cap with the closed criterion restated and no fresh axis. Round 1 was the only runtime defect: the frame classification sampled loaded and skeleton in separate `isVisible()` calls, so the documented streaming overlap could land between them — the loaded-priority tie-break was defeated, "not loaded, not skeleton" was inferred as a boundary and clicked a Retry no page was showing, and the post-recovery arm reported skeleton unconditionally. Round 2 caught the second-order cost of that repair: the helper's non-enrolment disposition still quoted a 17/27 probe measured against the pre-repair source (35 sites at head), so a live decision rested on numbers describing code that no longer existed. Round 3 was two durable-citation errors — the dynamic-import shadow cited as documented limit 4 (the realtime fresh-mount premise) rather than limit 2, in five places including the registry disposition whose whole argument is that limit; and a §4.6-4 rationale claiming two registry rows sit in different tests when they share a scope.

**Mechanizable:** one shape, and it is the round-2 finding: a probe-backed disposition comment can go stale silently, because nothing ties the numbers to the source they measured. A registry disposition that cites a score could carry the source digest it was measured against, and a meta-test could recompute that digest and fail when the file has moved beneath a quoted measurement — the same derived-cover discipline the census arithmetic already uses.

declined: not built here. This arc holds exactly one such comment, which is far too narrow a base to design the digest format on, and the honest first step is a survey of how many disposition comments across `tests/mutation/source/registry.ts` quote numbers at all. Filed as an observation for the lessons corpus rather than a lint nobody has evidence for.

**Judgment:** rounds 1 and 3 are the two halves of one authoring lesson and neither is a rule gap. Round 1's TOCTOU is the guard-premise class applied to a live DOM: the discriminating condition (which selector is up at which sample) had to be OBSERVED at each branch rather than inferred from the absence of another, and the repair is stated as a property — classification is total — rather than three patches. Round 3 is the citation rule in `docs/agents/spec-self-review.md` applied to a document's own cross-references: the limits were renumbered when a new one was inserted mid-list, and every reference to the old numbering silently pointed at an unrelated limitation. Both were caught by a reviewer reading the artifact rather than by any check the repo already runs.

**Infra:** the reviewer's sandbox is read-only, so focused Vitest execution was blocked before collection (`EPERM` on its temp transform directory) in rounds 2, 3 and 4. Every verification claim in those rounds therefore rests on read-only AST enumeration and on the numbers this arc measured and quoted; the round-2 finding is a direct consequence — the reviewer could enumerate the mutation population statically and compare it against the quoted one, which is exactly the check that caught the stale probe.
