# Review-round economy — `fix/auth-picker-hardening` @ `33c70ba1fdf2`

Arc: SPEC + PLAN only (no implementation). Closes `BL-SERVER-ACTION-ORIGIN-GATE` (logout-CSRF via missing-`Origin`) and `BL-IDENTITY-CLEAR-FAILURE-IS-SILENT` on the crew picker's identity-clear Server Actions.

## spec — 6 rounds

Trend: R1 BLOCKING/7 → R2 NEEDS/3 → R3 NEEDS/2 → R4 NEEDS/2 → R5 NEEDS/1 → R6 APPROVE/0. Strictly monotonic decay, every round a distinct refinement (no same-vector drip). Two `no_verdict` infra rows also landed in R3 (see Infra).

**Examined:** the Fetch-Metadata (`sec-fetch-site`) origin gate and its `NEXT_PUBLIC_SITE_ORIGIN` fallback against the filed missing-`Origin` bypass (reproduced from Next 16 framework source, §2.1); the §3.3 `{sec-fetch-site} × {origin}` truth table; the three CSRF-reachable exported actions (`clearIdentity`, `clearIdentityAndSkip`, `clearIdentityCore`); the producer-scanner interaction for the returned-vs-logged rejection code (`PICKER_INVALID_INPUT` returned, `PICKER_ORIGIN_REJECTED` via `log.warn`); the in-menu failure-state machine (local `useState`+`useTransition`, reset-on-open) across its full close/pending/reopen lifecycle, empirically probed 4/4; the `role="alert"` sibling-of-`role="menu"` DOM contract; the WAI-ARIA pending-item `aria-disabled` vs native-`disabled` keyboard contract; the `warning-*` alert token existence; the §12.4 catalog three-lockstep; the Google-session menu-switch loopback limit.

**Mechanizable:** three of the eight distinct findings were self-consistency/citation defects a mechanical sweep would have caught pre-dispatch:
- **R3-F2** — the alert used `text-danger`/`border-danger`, which do not exist in `app/globals.css` (only `--color-danger-bg`). A "referenced Tailwind token exists" check over spec code blocks catches this class.
- **R4-F2** — probe count stated 3/3 in files-touched vs 4/4 in §2.3. The self-consistency numeric-literal sweep (global spec rule) catches it.
- **R5-F1** — residual native-`disabled` assertion in §6C case (iii) left by the R4 fix, contradicting the same paragraph's `aria-disabled` contract. A grep for the changed term across the whole doc on the R4 fix would have closed it in R4; this is the exact "sweep to a derivation, fix all instances in the same round" rule, missed once. Cost: one full round (R5).
  Pre-emption for future arcs: after any spec edit that changes a term-of-art (a DOM attribute, a token name, a count), grep the whole doc for the old term before re-dispatch.

**Judgment:** the round-multiplying findings were genuine UI-state-machine and security-model reasoning, not mechanizable:
- **R4-F1** — native `disabled` on the pending submit removes it from focus and breaks the menu's roving-tabindex `.focus()` contract; the fix (aria-disabled + focusable + in-handler re-entry guard) required knowing the WAI-ARIA menu pattern and this component's specific focus model. High-value; correctly caught.
- **R3-F1** — the Closed→Open-pending transition (reopen before the clear settles) is reachable and was undocumented; needed a per-pair transition-inventory walk plus a new empirical probe case to settle.
- **R2-F1** — failure copy claimed a retry that does not exist on the close-while-pending path; a copy-vs-behavior accuracy call.
- **R1** front-loaded the genuine design gaps (success-model split by identity source, emit-proof pin, no-mutation-on-reject anti-tautology) alongside three author errors (orphan returned code, alert placement inside `role=menu`, wrapper-name break of the role-chip contract). All distinct, all fixed.

**Infra:** R3 produced two `no_verdict` rows before its verdict landed — codex-guard dispatches interrupted (usage-limit SIGTERM of the backgrounded child, the documented silent-death / reaper class). Not reviewer outcomes; not counted toward the six. Handled per contract: re-dispatched on the fresh window, no blind retry burn.
