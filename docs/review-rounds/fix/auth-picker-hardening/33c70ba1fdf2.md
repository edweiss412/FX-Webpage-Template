# Review-round economy — `fix/auth-picker-hardening` @ `33c70ba1fdf2`

Arc: SPEC + PLAN only (no implementation). Closes `BL-SERVER-ACTION-ORIGIN-GATE` (logout-CSRF via missing-`Origin`) and `BL-IDENTITY-CLEAR-FAILURE-IS-SILENT` on the crew picker's identity-clear Server Actions.

## spec — 6 rounds

Trend: R1 BLOCKING/7 → R2 NEEDS/3 → R3 NEEDS/2 → R4 NEEDS/2 → R5 NEEDS/1 → R6 APPROVE/0. Strictly monotonic decay, every round a distinct refinement (no same-vector drip). Two `no_verdict` infra rows also landed in R3 (see Infra).

**Examined:** the Fetch-Metadata (`sec-fetch-site`) origin gate and its `NEXT_PUBLIC_SITE_ORIGIN` fallback against the filed missing-`Origin` bypass (reproduced from Next 16 framework source, §2.1); the §3.3 `{sec-fetch-site} × {origin}` truth table; the three CSRF-reachable exported actions (`clearIdentity`, `clearIdentityAndSkip`, `clearIdentityCore`); the producer-scanner interaction for the returned-vs-logged rejection code (`PICKER_INVALID_INPUT` returned, `PICKER_ORIGIN_REJECTED` via `log.warn`); the in-menu failure-state machine (local `useState`+`useTransition`, reset-on-open) across its full close/pending/reopen lifecycle, empirically probed 4/4; the `role="alert"` sibling-of-`role="menu"` DOM contract; the WAI-ARIA pending-item `aria-disabled` vs native-`disabled` keyboard contract; the `warning-*` alert token existence; the §12.4 catalog three-lockstep; the Google-session menu-switch loopback limit.

**Mechanizable:** three of the eight distinct findings were self-consistency/citation defects a mechanical sweep would have caught pre-dispatch:
- **R3-F2** — the alert used `text-danger`/`border-danger`, which do not exist in `app/globals.css` (only `--color-danger-bg`). A "referenced Tailwind token exists" check over spec code blocks catches this class.
- **R4-F2** — probe count stated 3/3 in files-touched vs 4/4 in §2.3. The self-consistency numeric-literal sweep (global spec rule) catches it.
- **R5-F1** — residual native-`disabled` assertion in §6C case (iii) left by the R4 fix, contradicting the same paragraph's `aria-disabled` contract. A grep for the changed term across the whole doc on the R4 fix would have closed it in R4; this is the exact "sweep to a derivation, fix all instances in the same round" rule, missed once. Cost: one full round (R5).
  Pre-emption for future arcs: after any spec edit that changes a term-of-art (a DOM attribute, a token name, a count), grep the whole doc for the old term before re-dispatch.

**Judgment:** the round-multiplying findings were genuine UI-state-machine and security-model reasoning, not mechanizable:
- **R4-F1** — native `disabled` on the pending submit removes it from focus and breaks the menu's roving-tabindex `.focus()` contract; the fix (aria-disabled + focusable + in-handler re-entry guard) required knowing the WAI-ARIA menu pattern and this component's specific focus model. High-value; correctly caught.
- **R3-F1** — the Closed→Open-pending transition (reopen before the clear settles) is reachable and was undocumented; needed a per-pair transition-inventory walk plus a new empirical probe case to settle.
- **R2-F1** — failure copy claimed a retry that does not exist on the close-while-pending path; a copy-vs-behavior accuracy call.
- **R1** front-loaded the genuine design gaps (success-model split by identity source, emit-proof pin, no-mutation-on-reject anti-tautology) alongside three author errors (orphan returned code, alert placement inside `role=menu`, wrapper-name break of the role-chip contract). All distinct, all fixed.

**Infra:** R3 produced two `no_verdict` rows before its verdict landed — codex-guard dispatches interrupted (usage-limit SIGTERM of the backgrounded child, the documented silent-death / reaper class). Not reviewer outcomes; not counted toward the six. Handled per contract: re-dispatched on the fresh window, no blind retry burn.

## plan — 6 rounds

Trend: R1 BLOCKING/10 -> R2 BLOCKING/8 -> R3 NEEDS/5 -> R4 NEEDS/1 -> R5 BLOCKING/1 -> R6 NEEDS/2. Not strictly monotonic: after the front-loaded surface (R1-R2), the tail (R4-R6) was single-digit concrete surviving mutants on ONE axis, test-adequacy of the security-gate suite and the failure-alert suite. No security bypass survived any round; the reviewer confirmed the gate covers all three exported actions and pins Fetch-Metadata precedence.

**Examined:** the three-endpoint same-origin gate and its per-endpoint no-mutation + forensic-emit proof; the §3.3 precedence truth table (both directions); the §12.4 catalog three-lockstep; the in-menu failure state machine (local useState+useTransition, reset-on-open) across close/pending/reopen; the alert's DOM contract (direct next sibling of role=menu, popover last child); the WAI-ARIA pending-item aria-disabled keyboard contract (four commands); the invariant-8 closeout marker disposition; the invariant-12 ledger archive timing vs review-covers-what-merges; task-to-AC traceability.

**Mechanizable:** the round-multiplying class was test-contract precision a stricter pre-dispatch pass would have caught before review saw it:
- Strict-tsconfig typecheck of every pasted test/impl snippet would have caught R2-F3 (crewFacing string|null indexed with .length), R3-F1 (zero-arg mock indexed at [0]), and R4-F1 (widened return type in the React 19 form-action slot) BEFORE dispatch. Pre-emption: typecheck a scratch copy of every plan's pasted snippets before the first plan-review dispatch.
- RED/GREEN command identity (R3-F4) and beforeEach mock-reset completeness (R2-F4) are structural checks over the plan's own task steps.
- The em-dash-in-code-fence hard-fails (recurring) are already caught by spec:lint; running it to zero-hard before EACH dispatch removes them from the review surface.
- Transition-inventory completeness (R1-F7 -> R2-F7 -> R3-F5, three rounds of "one more attribute ternary") is the enumerate-don't-sample lesson: the fix that finally closed it was a stated grep over AvatarMenu.tsx for `? `, `&&`, and every `aria-*={`/`tabIndex={`, not a longer hand list.
- **The anti-tautology tail (R5-F1, R6-F1, R6-F2) is mechanizable by mutation testing, not by inspection.** Each was a mutant the suite claimed to kill but did not: R5-F1 an empty-cookie fixture that could not express the guard-order mutation; R6-F1 a code-only emit assertion that let a gate-only-core mutant pass (the wrappers delegate to core); R6-F2 a single-failure-code fixture that let a code-narrowing mutant pass. The closed operator set the suite now kills: guard {absent, order-swapped, per-wrapper-deleted-leaving-core}; alert {empty, suffix, placement, ok-flip, code-narrowed}. Pre-emption: for a security-gate or failure-branch suite, enumerate the closed mutant set in the plan and assert each is killed, rather than relying on the reviewer to construct them one per round.

**Judgment:** genuine design reasoning, not mechanizable -- the invariant-12 vs review-covers-what-merges reconciliation (R2-F1: marker before review, ledger-status-only archive as the sole invariant-mandated post-review commit); the invariant-8 HONEST-CEILING deferral for a spec+plan-only arc (R1-F8, an unmerged-branch red by design); removing the manufactured-RED "Task 5" (R2-F2); the keyboard act-timing (R3-F2, focus must commit before the roving handler reads the index); the placement contract precision (R3-F3); and the single-chokepoint delegation analysis behind R6-F1 (clearIdentity delegates to core; clearIdentityAndSkip short-circuits on !result.ok before signOut).

**R6 disposition (orchestrator triage, plan-review cap):** R6's two findings were concrete-mutant-backed anti-tautology holes, both fixed in-branch (per-endpoint emit `action` assertion; a PICKER_INVALID_INPUT failure fixture), closing the enumerated mutant sets above. Per the late-arc cap, R7 was NOT dispatched: verification is downstream and executable -- implementation TDD must include tests that kill exactly those mutants, and whole-diff review re-examines the surface. The orchestrator accepted the fixes and authorized proceeding to plan-approved. Rationale recorded here per corpus rules.

**Infra:** none at the plan stage (the two R3 no_verdict reaped-dispatch rows are at the spec stage above).
