# Review-round filing — fix/mutation-score-nondeterminism @ c80f844278bd

## spec — 5 rounds

Findings by round: **11, 9, 7, 4, 1.** Verdicts NEEDS-ATTENTION throughout; approved on orchestrator ruling after round 5 returned only a residue of an already-opened class. Refutations: 0 / 0 / 3 / 0 / 0 (round 5 additionally CLEARED one of the two contract changes it was scoped to).

**Examined:** the attribution design and its probe record — the per-mutant evidence schema, the gate notice channel, the durable record sink and its retention, the determinism harness contract, the acceptance table (17 rows when the spec stage closed; AC-18 was added at plan r3, taking it to 18), and four measurement probes with their pre-registrations, controls and instruments.

**Mechanizable:**

- **A bare `:NN` citation** (`` `runner.ts:112`, constant at `:51` ``) drew `CITATION_MALFORMED` three times across the arc, twice reintroduced by my own repairs. `spec:lint` catches it at zero cost; the arc's cost was one repair cycle each time it slipped past a local run.
- **`TASK_AC_UNRESOLVED`** caught two `ac=` ids present in markers and absent from prose on the first plan lint — a class no reviewer had to spend a round on.
- **A wrong directory in a meta-test citation** (`tests/mutation/source/_metaSourceShardIntegrity.test.ts` for `tests/mutation/_metaSourceShardIntegrity.test.ts`) was caught by `CITATION_FILE_MISSING`, not by reading.
- **The class that dominated rounds 1-4 is the reason the count started at 11:** an acceptance criterion satisfied by a strictly weaker implementation. Five of round 1's findings and three of round 2's were that shape.

  **declined: a document linter cannot evaluate whether an unwritten implementation satisfies a stated proof** — deciding it requires constructing the weaker implementation and running it, which is the killer-audit procedure a human performs, not a static check over prose. The structural repair taken instead is a fourth AC-table column naming the killing implementation per row, which moves the omission from review time to AUTHORING time: a row whose fourth column is empty is visibly unfinished. No `BL-` row is filed, because the process-facing mint bar wants an incident plus a mechanism, and the mechanism here would be the audit itself rather than an arm.

**Judgment:**

- **Three findings turned on statistics and evidence handling rather than on the design**, and each was correct: an unstated independence assumption behind a rate exclusion (six serial runs in one process cannot establish it, and no sample size repairs it); a duration distribution that mixed baseline children into a mutant population; and an arithmetic bound computed for a different statistic than the one stated.
- **The best finding of the stage spans two files and could not be found in either alone:** the record sink overwrote a surface's record on every re-run, while `BACKLOG.md:108` instructs operators to RE-RUN before acting on a stale-row report. Nothing about the sink looks wrong alone; nothing about "re-run before acting" looks wrong alone. It survived three rounds.
- **Rounds are not interchangeable.** Round 4 produced zero refutations and no new classes — every finding a residue of a class already opened — which is what justified a fifth confirming round rather than a fence.

**Infra:**

- Zero `no_verdict` dispatches. Five dispatches, five verdicts, no reaps.
- The reviewer-states-its-sha convention was adopted at round 2 and every subsequent round named the tree it read, which made contamination a one-line check rather than a three-signal inference.

### What the count does NOT indicate

The artifact SHRANK in claim strength across the stage: an "eliminated branch" became a rate bound and then an OPEN branch with a stated structural reason; a population-wide timeout inference became a single-surface measurement; a headline ratio was retracted and re-measured in the opposite direction. A high finding count on a narrowing artifact is convergence, and the monotone decay plus the disappearance of new classes is what distinguishes it from a recognizer ratchet.

## plan — 4 rounds

Findings by round: **10, 8, 5, 3.** Verdicts NEEDS-ATTENTION throughout; approved at the cap because every round-4 finding was a residue of an already-opened class. Refutations: 4 / 4 / 3 / 2.

**Examined:** nine tasks over a declared red-contract region — the per-child record, the injectable ceiling, the gate notice channel and its console half, the durable sink, the determinism core and its adapter, the workflow upload, and two marker-less measurement tasks — against the spec's acceptance table and the live production lines each red names.

**Mechanizable:**

- **`spec:lint` caught four defects in the first draft** that no reviewer had to spend a round on: a meta-test cited under the wrong directory (`tests/mutation/source/` for `tests/mutation/`), two `ac=` ids present in markers and absent from prose, and placeholder paths cited as tracked files.
- **A derived cross-reference sweep** — every `Task N` mention extracted and checked against the actual task headings — found the second stale reference the first hand sweep missed, and proved there were no others. Cheap, and it is the form the hand sweep should have taken the first time.
- **declined: the dominant class here is not mechanizable by a document linter.** Six of round 2's eight findings were a task body restating an approved AC more WEAKLY than the spec's row. Detecting that requires deciding whether one prose paraphrase is weaker than another, which is a judgement. The structural repair was to delete the paraphrase: the plan no longer restates AC content at all, so the act that produced every instance no longer occurs. No `BL-` row is filed, because the repair removed the surface rather than leaving a gap for an arm to guard.

**Judgment:**

- **Three invalid reds, each failing differently**, and two of them would have gone green when the TEST changed rather than when the implementation landed: an assertion whose stated discriminator (`kind`) is identical under both the shipped and the repaired code; an import of a module that does not exist, which is a resolution failure rather than a value assertion; and a red pre-satisfied by an earlier task with no implementation that could legitimately turn it green.
- **A pin that was already green.** The shipped `runAllSuites` short-circuits, so AC-5's spawn-count assertion is true before any implementation lands — a non-regression pin masquerading as a TDD red, repaired by authoring its red against a named mutant in the production surface.
- **The plan asserted the negation of the arc's own central finding.** Task 6b demanded field-for-field equality including `durationMs`, the exact quantity this arc measured swinging 19.8 s to 39.1 s on byte-identical inputs. Establishing that a quantity is unstable does not propagate to the places you casually depend on its stability.
- **Two findings were gaps in the APPROVED AC table rather than defects in the plan**, and the spec was amended rather than the plan papering over them — a plan cannot fix a gap in an approved AC, it can only hide it, and the hiding survives review because the plan lints clean against the AC it cites.

**Infra:** zero `no_verdict` dispatches across the stage; every round named the tree it read.

### The shape of the count

The artifact SHRANK: a nine-task plan that restated acceptance criteria became a nine-task plan that cites them, three invalid reds became two valid ones and a measurement, and two tasks split rather than grew. Monotone decay with the class count falling — one dominant class in round 2, none new by round 4 — is the same convergence signature the spec stage showed.
