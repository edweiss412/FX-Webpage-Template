# Review-round filing — fix/mutation-score-nondeterminism @ c80f844278bd

## spec — 5 rounds

Findings by round: **11, 9, 7, 4, 1.** Verdicts NEEDS-ATTENTION throughout; approved on orchestrator ruling after round 5 returned only a residue of an already-opened class. Refutations: 0 / 0 / 3 / 0 / 0 (round 5 additionally CLEARED one of the two contract changes it was scoped to).

**Examined:** the attribution design and its probe record — the per-mutant evidence schema, the gate notice channel, the durable record sink and its retention, the determinism harness contract, the 17-row acceptance table, and four measurement probes with their pre-registrations, controls and instruments.

**Mechanizable:**

- **A bare `:NN` citation** (`` `runner.ts:112`, constant at `:51` ``) drew `CITATION_MALFORMED` three times across the arc, twice reintroduced by my own repairs. `spec:lint` catches it at zero cost; the arc's cost was one repair cycle each time it slipped past a local run.
- **`TASK_AC_UNRESOLVED`** caught two `ac=` ids present in markers and absent from prose on the first plan lint — a class no reviewer had to spend a round on.
- **A wrong directory in a meta-test citation** (`tests/mutation/source/_metaSourceShardIntegrity.test.ts` for `tests/mutation/_metaSourceShardIntegrity.test.ts`) was caught by `CITATION_FILE_MISSING`, not by reading.
- **The class that dominated rounds 1-4 is the reason the count started at 11:** an acceptance criterion satisfied by a strictly weaker implementation. Five of round 1's findings and three of round 2's were that shape.

  **declined: a document linter cannot evaluate whether an unwritten implementation satisfies a stated proof** — deciding it requires constructing the weaker implementation and running it, which is the killer-audit procedure a human performs, not a static check over prose. The structural repair taken instead is a fourth AC-table column naming the killing implementation per row, which moves the omission from review time to AUTHORING time: a row whose fourth column is empty is visibly unfinished. No `BL-` row is filed, because the process-facing mint bar wants an incident plus a mechanism, and the mechanism here would be the audit itself rather than an arm.

**Judgment:**

- **Three findings turned on statistics and evidence handling rather than on the design**, and each was correct: an unstated independence assumption behind a rate exclusion (six serial runs in one process cannot establish it, and no sample size repairs it); a duration distribution that mixed baseline children into a mutant population; and an arithmetic bound computed for a different statistic than the one stated.
- **The best finding of the stage spans two files and could not be found in either alone:** the record sink overwrote a surface's record on every re-run, while `BACKLOG.md:108` instructs operators to RE-RUN before acting on a stale-row report. Nothing about the sink looks wrong alone; nothing about "re-run before acting" looks wrong alone. It survived three rounds.
- **Rounds are not interchangeable.** Round 4 produced zero refutations and no new classes — every finding a residue of a class already opened — which is what justified a fifth confirming round rather than a fence.

**Infra:**

- Zero `no_verdict` dispatches. Five dispatches, five verdicts, no reaps.
- The reviewer-states-its-sha convention was adopted at round 2 and every subsequent round named the tree it read, which made contamination a one-line check rather than a three-signal inference.

### What the count does NOT indicate

The artifact SHRANK in claim strength across the stage: an "eliminated branch" became a rate bound and then an OPEN branch with a stated structural reason; a population-wide timeout inference became a single-surface measurement; a headline ratio was retracted and re-measured in the opposite direction. A high finding count on a narrowing artifact is convergence, and the monotone decay plus the disappearance of new classes is what distinguishes it from a recognizer ratchet.
