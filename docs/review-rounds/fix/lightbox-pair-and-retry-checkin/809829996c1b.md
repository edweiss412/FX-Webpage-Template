## diff — 2 rounds

**Examined:** THREE diff rounds across the arc, ten findings, 4 / 3 / 3, every round BLOCKING.
Two bases: `caf96a60ab40` holds the first, and this file holds the second and third — the arc
re-absorbed `origin/main` mid-stage, which moves the merge base and opens a new file, so the rounds
in THIS file are numbered 1 and 2 and the heading declares that count. The arc total of three is the
number that matters and it is stated here, per the corpus rule that a heading counts its own base
while `Examined` names the cross-base total.

**Stage outcome: SHIPPED AT THREE.** The orchestrator ruled on 2026-09-01 that the deciding number is
the product-defect decay below, not the raw finding rate, and that a fourth round's expected yield is
more findings for a fixer that was already ruled out. This filing was offered voluntarily and
accepted in place of that round, on the ground that recording the signature serves better than
running against it.
Filed VOLUNTARILY below the four-round threshold, because the shape is legible now and waiting for
the gate to compel it would only make the record later, not better.

**Two rates, and they point opposite ways.** The raw finding count did not decay: 4, 3, 3. The
PRODUCT-defect count did: one, one, zero. Round 1 found abandonment keyed on Embla's `select` event
rather than on inactivity itself, so a prop reorder that leaves `activeIndex` alone left a slide's
retry armed and invisible. Round 2 found a stranded timer handle that breaks nothing at the moment it
happens and silently suppresses the NEXT retry's watchdog forever. Round 3 found no product defect at
all. Both product defects were temporal — a state that is wrong only later — and neither is the kind
of thing a static guard finds, which is what the two rounds bought.

**Mechanizable:** seven of the ten findings were A GUARD THAT COULD NOT FAIL, and they arrive in
three shapes worth naming separately, because they fail for different reasons:

- **A recognizer that sees one spelling of a mechanism.** The no-cancellation guard matched `.src =`
  with two quote forms, and missed the empty template literal, `removeAttribute`,
  `setAttribute(…, "")`, and `toggleAttribute("src")` with and without its second argument. Five
  spellings of one act, surfaced one round at a time across two rounds.
- **A scanner whose discovery predicate is narrower than its claim.** `phaseWriters` discovered only
  `setRetryPhase((prev) => …)`, so a direct `setRetryPhase(new Map())` was not a writer that failed
  the shape check — it was not a writer at all, and the population count was unmoved by adding one to
  each component. A scanner asserting a population must first assert its own reach.
- **An assertion whose subject an unrelated actor can satisfy.** Two instances, and the second is the
  more instructive. `cleared.length > before` on unmount is satisfied by the announce log's own TTL
  timer, so both cleanup bodies could be deleted with every suite green. And after that was repaired
  to assert handle IDENTITY, round 3 showed the repaired case still could not see a missing
  `clearTimeout`, because the case retried at the SAME fake-clock instant the retired timer was
  scheduled at: the stale callback fired exactly when the replacement's would have. One second of
  offset is the whole difference between an assertion and a decoration.

declined: no ledger row is filed for any of this. The arc's standing constraint is that it files no
`BL-`/`DEF-` entry of any facing, and the process mint freeze would refuse these anyway — every one
is a property of the tooling rather than a number a product arc or a human would notice moving,
which is the freeze's admission test. They are recorded here, in the owning surface's own record,
which is where the freeze says a process finding goes.

**The mechanical answer to that class already exists in this repo and this arc did not use it:** the
source-mutation gate (`tests/mutation/source/registry.ts`). These guard surfaces are not enrolled.
Enrolment was NOT taken mid-arc, and that is a ruling rather than an omission (orchestrator,
2026-09-01): enrolment precedes review and never joins it under pressure, because a registry row
added at round three is a scope change made by the party the gate constrains. **The next arc touching
these surfaces enrols first and cites this filing.** What that buys, measured elsewhere in this repo:
a surface enrolled after four review rounds scored 41/64 with 23 survivors on its first run, on code
that had already cleared three adversarial rounds. Absence is what review is worst at, and a score is
what finds it.

**Judgment:** two findings were not mechanizable and were worth their rounds outright — the two
product defects above. A third belongs here rather than under Mechanizable: round 3 found that the
round-2 prose sweep had repaired the nine CITED sites instead of deriving the class, leaving fourteen
more. That is a discipline failure, not a tooling gap; the rule against it is already written down in
this repo and I broke it anyway, one round after citing it.

**One correction to this file's own data, recorded rather than made silently.** The two rows here
were dispatched as rounds 2 and 3, carrying the numbering forward across a merge-base change. That
is the trap AGENTS.md documents: `round` is declared WITHIN one base file, so the first dispatch at a
new base is round 1, and carrying the old number forward lands as a contiguity gap that reads as
"rows were lost or hand-edited" — the one diagnosis that is not true. They are renumbered 1 and 2,
which is what they were: the first and second dispatches at this base. The arc's cross-base total is
unchanged at three distinct (baseSha, round) pairs.

**Infra:** two dispatches were spent on my own process errors and produced no rows. I started two
concurrent reviews by accident and then edited components while both were reading the live tree,
which makes a verdict unfalsifiable; both were killed and one re-run on a frozen tree. Separately,
the line-keyed census keys were re-derived correctly and then invalidated twice by later commits in
the same arc, once reaching CI red — deriving is not sufficient, deriving LAST is, and that ordering
now lives in the repair script's header rather than in anyone's memory.
