# fix/promote-identity-validation — round-economy filing (base a7393880ae6d)

## spec — 6 rounds

**Examined:** rounds 1-6, 18 findings. R1 (6, BLOCKING): the promised mismatch telemetry path did not exist (all three callers discard fulfilled outcomes), a nonexistent list-query seam, undefined delta fields on two other mismatch branches, an inaccurate retry consequence, a dropped malformed-variants guard, a wrong data-classification rationale. R2 (4, BLOCKING): the seam repair was type-incoherent against the composed LockablePromoteTx path and its blast radius incomplete, set-equality realdb oracle discarded the multiplicity AC-3 needs, retry mechanics still wrong against watermark/fresh-prefix/GC probes, a matrix row contradicting §4.3. R3 (2, BLOCKING): the shared-interface widening would break five other implementers (pivoted to LockablePromoteTx-scoped), one malformed locator. R4 (2, BLOCKING): basename-only comparison fails to bind the manifest to the promoted revision (stale-revision snapshotPath passes and 410s at serve — fixed with a mispathed delta class and path binding), one stale matrix row. R5 (2, BLOCKING): row provenance lost in the names query (a malformed bare snapshotPath indistinguishable from a variant key — discriminated row kinds shipped), and the mispathed branch outside the deltas/telemetry contract. R6: APPROVE, 0 findings.

**Mechanizable:**
- "a spec claims an emit/telemetry path exists without probing the callers" (R1 F1) — the invariant-10 mutation-surface meta-test already walks emit sites; a spec-side arm could require any "existing emit" claim to cite the emitting line, which the citation pass then verifies. Covered by the existing live-code-citation rule; the failure was non-compliance, not a rule gap. No new row.
- "interface widening specs must enumerate implementers from the compiler, not memory" (R2 F1 → R3 F1 → R4 F2 chain) — `tsc`-derived implementer lists for any widened type, pasted at plan time. Fits the writing-plans registry-reconciliation authored-AND-run posture; no new row.

**Judgment:** the exact-set policy (user-ratified), the LockablePromoteTx scoping, and the mispathed-delta design were settled calls.

**Infra:** none — every dispatch returned a verdict.
