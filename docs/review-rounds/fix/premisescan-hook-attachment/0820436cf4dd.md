# Round economy — `fix/premisescan-hook-attachment` at `0820436cf4dd`

Base moved from `64c40a68e228` when `origin/main` advanced mid-arc; the spec and plan rounds are
filed against the earlier base and are not re-counted here.

## diff — 5 rounds

**Examined:** five whole-diff rounds, BLOCKING every time, with 2 / 3 / 1 / 2 / 4 declared findings.
Twelve findings, all accepted, none refuted, one accepted IN PART. The subject is a mutation-guard
surface (`premiseScan`) that reports two Vitest registration shapes it cannot follow.

The finding count does NOT decay, and the shape of that is the whole lesson. Rounds 1 and 2 found
design gaps. Rounds 3, 4 and 5 each found a defect **introduced by the previous round's repair**:

| round | finding | introduced by |
| --- | --- | --- |
| 3 | a `deferred` flag certified falsely | r1's repair |
| 4 | producer A's `ts.isFunctionLike` stop certified falsely | r1's repair, unswept sibling |
| 5 | descent into a nested inline body wrongly attributed | r4's repair |

**Judgment:** three of five rounds were spent on ONE question a static scanner cannot answer — does
this function ever get invoked — and each repair answered it a different wrong way before the design
settled on declining to answer it at all. Every suppression is a trade: accept one failure direction
to avoid another. Wrong attribution is noisy and PERMITTED by the bound; false certification is
silent and FORBIDDEN. Intuition prefers quiet output, so the unsound direction is the one that feels
like an improvement, and it was chosen twice.

Two structural causes, both chargeable, neither mechanizable:

1. **A sweep that spares a site must justify everything that site DOES, not the aspect that raised
   the question.** At r3 the suppression in producer B was deleted and the twin in producer A was
   spared, because the exemption was checked against the `.each` datum case that raised it. The
   exemption was scoped to a CASE while the code was scoped to a CONSTRUCT. r4 cost one full round.
2. **A suppression's comment is a checkable claim.** The r3 flag carried "an unfollowable
   registration that DOES invoke the enclosing function is itself reported" — false, since nothing
   syntactic connects a body to a call site. An unexplained suppression invites scrutiny; one with a
   confident false rationale deflects it, which is why it survived two rounds.

**Mechanizable:** two.

declined: both landed in this arc, so there is nothing left to schedule — filing a `BL-` for work
that shipped in the same PR would put a closed item in the open queue.

- **The differential belongs in the admissibility clause, not in triage.** At r4 the reviewer sent
  ten probed shapes under one mechanism; seven were pre-existing (`environment-free` on `origin/main`
  AND at HEAD) and only two were caused here. Reconstructing that afterwards is a round already spent
  arguing backwards. Clause 2 of the r5 brief REQUIRES a bracket against `origin/main` before a
  finding counts, and r5 arrived fully pre-bracketed on its first outing. The general test: does the
  reviewer have the means, and would the implementer otherwise do this to every finding it sends.
- **Prose counts rot; derive them.** The §5.2 cell population moved 16 → 17 → 18 → 19 across the
  arc, and each move stranded prose. `cell-check.mts` now computes and prints its own split, and
  `claims-check.mts` check D holds every spec site that states a count to that output. It named
  seven stale sites in its author's next commit and nine more a round later.

**Infra:** none. No `no_verdict`, no reaped dispatch, no wrapper fault across the five rounds.

### Implementation note — what would have collapsed this

Nothing in the brief. The rounds were bought by repair QUALITY, not review scope: the closed
criterion, probe domain and threat fence were stated from round 1 and no round litigated them. The
cost centre was three consecutive repairs shipped without the both-directions proof that would have
caught them, and the proof is cheap — r5's repair was verified against eleven shapes and two controls
on both trees in a single probe run, which is the check the earlier three skipped.
