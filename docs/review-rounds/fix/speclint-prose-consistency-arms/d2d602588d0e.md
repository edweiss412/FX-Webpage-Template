# Review-round economy filing — fix/speclint-prose-consistency-arms @ d2d602588d0e

## spec — 4 rounds

**Examined:** all 10 findings across R1–R4 (2/5/2/1), every one probe-backed by the reviewer against the live corpus or the committed instrument; zero refuted, zero relitigated after disposition.

**Mechanizable:** the dominant class (7 of 10 findings — R2 F2, R2 F4, R3 F1, R3 F2, R4, plus halves of R1) is CONTRACT/INSTRUMENT DIVERGENCE: the spec's normative accept-set wording and the committed calibration instrument disagreeing about a population the record claims to measure. Each instance was found by the reviewer re-implementing the contract's words and diffing against the instrument's output. The terminating form is a derivation, not more prose care: the implementing arc's layer-3 record is produced by the SHIPPED module (contract and measurement are one artifact), which closes the class structurally — the divergence can only exist while the instrument is a hand-written approximation of prose. A candidate future arm: a spec:lint-adjacent check that a spec's stated calibration figures match a named committed record file (same shape as SCRIPT_CONSTANT_PARITY, doc↔record instead of doc↔script).

declined: the class dies with the draft-time instrument at implementation (the layer-3 record is produced by the shipped module itself), so there is no persistent surface for an arm to guard; noted here for the next detector spec to copy.

**Judgment:** two calls stand: (a) the heading exclusion and the digit-only/value-bound accept-set were resolved by NARROWING each time (decline to fire, document the limit), never by widening the recognizer — the speclint-prose-count-parity 20-round lexer trajectory did not recur; (b) R4 stopped the train at the cap per the brief's drip rule (R3→R4 were consecutive same-axis rounds) — disposition escalated to the orchestrator rather than a unilateral R5.

**Infra:** none — all four dispatches returned parsable verdicts on attempt 1.
