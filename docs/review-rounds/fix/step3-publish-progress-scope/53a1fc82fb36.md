# Review-round economy — fix/step3-publish-progress-scope @ 53a1fc82fb36

The spec (4 rounds) and plan (4 rounds) stages are filed at the previous base,
`6bfb58e4f66b.md`. This base opened when `origin/main` was folded mid-arc, so the diff stage's
rounds are numbered from 1 here by design rather than continuing that file.

## diff — 4 rounds

**Examined:** R1 BLOCKING/9, R2 BLOCKING/7, R3 BLOCKING/3, R4 BLOCKING/5, all on the whole diff.
Cross-base total for the arc is 12 counted rounds across three stages. Finding counts fell 9, 7, 3
and then rose to 5 — the rise is the interesting number and most of this filing is about it.

Every one of the 24 findings was verified against the tree by hand before being accepted, because
all four reviewers were execution-blocked: each reported that Vite could not create its temporary
directories under the read-only sandbox, so none of them ran a test. Two findings were refuted with
mechanism rather than repaired, and both refutations are recorded in the plan so no later round
re-derives them.

**Judgment:** the rounds split cleanly into two populations, and only one of them was worth paying
for.

R1 and R2 found real defects in the shipped change. R1's blocker was the arc's own repair being
half-done: the restage persists the refreshed parse BEFORE dirtiness is decided, and three
post-restage early returns skipped the rebind, so a renamed sheet was reported to the operator under
its OLD name against storage that already held the new one. Review named one branch; the sweep found
three, which is why the bind now sits ahead of every branch instead of being repeated inside them.
R2 found that the test fake projected a column the query had not selected, so the guards sold as
protecting that column could not have noticed it leaving. Both are the kind of finding a whole-diff
round exists for.

R3 and R4 are a different population: seven of their eight findings were about the arc's own
transition-audit guard, not about the product change. That guard was authored in this arc, and each
round found one more corner of Tailwind's class grammar it did not model — R2 inline longhands, R3
variant prefixes, R4 commas inside arbitrary values. Each repair widened the recognizer and made a
bigger target for the next round. This is the ratchet AGENTS.md's repair-direction rule names
exactly, and it was three rounds before the rule was applied.

R4 was answered by NARROWING instead: the class check stopped modelling Tailwind syntax and now
tests for the tokens `transition` and `animate` as substrings, which fails CLOSED on syntax nobody
has thought of yet where the pattern failed OPEN. The same move fixed the duplicate-mount check,
whose identity had been keyed on node text that legitimately changes mid-transition; identity is now
structural. Both were proved by planting the exact mutants that escaped the widened versions. The
documented limit is written beside the assertion. Had the narrowing been reached at R3, R4's two
blocking findings would not have existed.

The other lesson is not about recognizers. R3's P0 was a repair made in response to an invariant-8
critique finding, without checking whether the question was already settled — and it was: the plan
records the compact count's bare form as deliberate, and the spec's dimensional proof depends on it.
The repair therefore broke invariant 7 against two ratified documents. In the SAME round two other
findings were correctly refused by checking them against the code. The discipline was in hand and
was not turned on the arc's own repair. That is now a marker rule: a finding is a claim, not a
ruling, and every repair checks the plan and spec for a prior settlement first.

**Mechanizable:** declined: the one mechanizable gap here is the recognizer ratchet, and a ledger row for it is inadmissible under the 2026-08-25 process-mint freeze — it is process-facing, its done condition is a property of a guard rather than a number a product arc would notice moving, and it carries no product-blocked incident. It is recorded as a documented limit beside the assertion it constrains, which is where the freeze sends it.

The recognizer ratchet is the one thing here a tool could have caught, and the
tool is a rule rather than a lint. Three rounds of "the guard does not pin what it claims" on one
guard, each closed by adding a grammar case, is the documented trigger for inverting the default;
the trigger fired at R2 and was acted on at R4. A cheap check exists and is now written into the
audit itself: when a guard's repair is "accept one more syntactic form", ask whether a
fails-closed token test would make the whole grammar question moot. Recorded as a habit, not a
backlog row, and deliberately not filed as a process-facing ledger entry — the 2026-08-25 freeze
admits none, and this is a documented limit on a guard this arc owns.

The four deferrals this arc filed are product-facing and carry their exceptions inline; none is a
process row.
