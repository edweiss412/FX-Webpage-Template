# Review-round filing — `fix/control-outline-border-token`, post-merge base `39f57661c873`

**This is a CONTINUATION, not a new arc, and the split is an artifact of merging `origin/main` mid-flight.**

`<baseSha12>` is what gives an arc its identity (corpus README), and it is `git merge-base origin/main HEAD`. This branch merged `origin/main` after arc C landed as #849, because the ledger archive seam was hot and the merge de-risked it. That moved the merge-base from `2ddbf038bdf4` to `39f57661c873`, so dispatches made afterwards write here instead.

**The arc's full round history is in the sibling filing at `2ddbf038bdf4.md`** — 5 spec rounds and 5 plan rounds, 51 findings, all confirmed by probe and repaired. Read it first; this file records only what happened after the base moved.

## plan — 1 round

**This base identity has seen ONE plan round; it is the ARC's sixth.** The heading counts rounds under  because that is what the corpus keys on. The five that preceded it are in the sibling filing.

**Examined:** three dispatch rows, all `--stage plan`. Two are `no_verdict` and are NOT reviewer outcomes: the first was the account's Codex quota exhausted, the second was `attempt_timeout` at 200.09s from a deliberately short `--attempt-max-secs` used as a cheap credits probe. The third returned **BLOCKING, 6 findings**, all real and all repaired.

**On the round number.** The wrapper records the `--round` value the caller passes; it is an author-supplied label rather than a measurement. These were dispatched as `--round 6`, correct for the arc but wrong for this base identity, under which they are round 1. Corrected to 1 so the stage reads as a contiguous run, with the arc-level count recorded here in prose where it cannot be mistaken for a measurement.

**Judgment:** the six findings split three ways, and the split is the useful part. Three were fallout from the Option A ledger-sequencing rewrite and the R5 AC-channel repairs — the same under-verified-restructure pattern the sibling filing already names. Two were stale remnants inside corrections this arc had already made once. **One was caused by shipping Task 1**: adding assertions to `tests/styles/_metaControlOutlineFill.test.ts` shifted the ShareHub pin from `:156` to `:286`, so the plan's Task 7 would have filed a stale line citation as its ratified-scope evidence. That one generalises — **a plan that cites line numbers inside a file its own tasks edit is citing a moving target** — and the repair was to cite the pin by test NAME instead.

The orchestrator's sequencing caution is what kept the cost at zero: Task 2's commit was held until this round landed, precisely because the AC proof channels and the Task 7 ledger sequencing were the sections most likely to move. Three of the six landed there.

**Mechanizable:** one.

- **The shell checks in Task 7 all FAILED OPEN — filed as `BL-VERIFICATION-BLOCK-FAILS-OPEN-ON-UNREADABLE-INPUT`.**, and the reviewer proved it rather than asserting it: a missing ledger file makes `grep` error while `wc -l` still prints `0` at exit 0, and an invalid git object printed `fatal: invalid object name` then `0`, also exit 0 — so "prints 0" and "could not look" were indistinguishable. Every check now emits an explicit PASS/FAIL, exits 1 on failure, and validates its reads before taking counts; the repair was verified in BOTH directions, with a constructed duplicate id making the check print FAIL and exit 1. The general rule is cheap and worth carrying: **a verification block whose success and whose inability-to-look print the same thing is not a verification block**, and the way to find out is to run it against a constructed failure.

**Infra:** two `no_verdict` rows, neither a reviewer outcome and neither a wrapper fault — one a genuine account-quota outage, one a self-inflicted timeout. Recorded because AGENTS.md warns a `no_verdict` must never be read as "the reviewer found nothing"; this arc adds the corollary that it must not be read as "the outage is still on" either. `signal`, `killedReason`, `failureShape` and `durationSecs` distinguish them, and reading those before attributing cause is the standard.
