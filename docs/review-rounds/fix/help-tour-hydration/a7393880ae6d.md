# fix/help-tour-hydration — round-economy filing (base a7393880ae6d)

This arc rebased mid-review (main moved under it after PR #773 merged), so its spec rounds split across two base files: R1–R2 under `4cc9251c0312.jsonl`, R3 onward here, renumbered to 1..N per the wizard-step-connector precedent (rows on a new base restart at 1). Consolidation is forbidden by the corpus design; total spec rounds across both files is the sum.

## spec — 6 rounds

**Examined:** rounds 1-3 on this base (conversation rounds R3-R5; R1–R2 on the prior base brought 6 + 3 findings — oracle/governance inventory, five-green bar semantics, fallback coherence). R3: 3 precision findings (diff description vs recipe, group topology, visual-delta rationale). R4: 1 finding (byte-identity vs whitespace collapse — acceptance-property contradiction). R5: 1 BLOCKING finding (the compact recipe is not Prettier-stable on the Onboarding-wizard card; the lint-staged hook + `format:check` would either resurrect the hydration bug or hold CI red). Round 4 here (conversation R6) returned a second BLOCKING prettier probe: the compact form is unstable on ALL three cards, so the recipe went uniform expression-children — the R5 mechanizable candidate (formatter-idempotence pre-check) is confirmed twice over. Round 5 here (conversation R7) returned 4 propagation/precision findings on the uniform recipe (superseded prose left in place, topology cardinality, span-vs-p rationale, a stale workflow synopsis) — the self-consistency-sweep class, all repaired in one commit. Round 6 (conversation R8): APPROVE, 0 findings.

**Mechanizable:**
- "a fix to MDX/JSX formatting is proposed without checking the repo formatter preserves it" (R5) — a spec:lint or pre-dispatch arm that runs `prettier --check`'s idempotence over any file a spec's recipe rewrites, using the repo config, before the recipe is ratified. Candidate extension of the red-executability arm already filed as `BL-SPECLINT-RED-EXECUTABILITY-ARM` (a recipe whose end state a hook rewrites is the same class as a red= that is already green: the asserted state does not survive the pipeline).
- "numeric cardinality drifts between body sections" (R2 on the prior base, R3 group-topology here) — already filed as `BL-SPECLINT-PROSE-COUNT-PARITY`; no new row.

**Judgment:** R4's whitespace-equality semantics and R5's per-card recipe selection were probe-settled design calls, not mechanizable lint targets.

**Infra:** none — every dispatch returned a verdict.

## plan — 9 rounds

**Examined:** rounds 1-4, 16 findings, converging on executable-gate precision. R1 (7, BLOCKING): a vacuous five-green jq (no cardinality), two invalid red markers, an unenforced Task 4 red, a graduation/marker ordering conflict, a window that review repairs could invalidate, stale anchors. R2 (2, BLOCKING): Task 3's red grepped its own marker-comment text (fixed by line-anchoring — found by the reviewer running the exact command). R3 (5, BLOCKING): a false readiness claim (networkidle only), AC-1 unbound on the fallback path, post-gate UI repairs bypassing invariant 8, the §12 findings record reduced to aggregates, a fallback-falsified archive entry. R4 (2, BLOCKING): the SSR-DOM hydration gate proved nothing (replaced with an interaction round-trip); the acceptance command was not sha-bound. R5 (1, BLOCKING): same-sha five runs are impossible (rerun adds attempts) — settled as consecutive-window + newest-on-head. R6 (2, BLOCKING): run-link evidence step, fallback ninth-member rewrite. R7 (2, BLOCKING): diff-vs-index idempotence ordering; missing test-auth env on the fallback production run. R8 (1, BLOCKING): a premature mid-task commit (staging snapshot instead). R9: APPROVE, 0 findings.

**Mechanizable:**
- "a gate command's discriminating power is asserted in prose but not carried by the executable" (R1 F1, R4 F2 — cardinality and sha-binding both lived in prose first) — exactly the `BL-SPECLINT-RED-EXECUTABILITY-ARM` gate-command mutant-probe class; the constructed-failure-input probes this plan now records per command are the manual form. No new row.
- "a red= that matches its own marker text" (R2) — a one-line static check for the red-executability arm: the red command must not match the plan's own task-marker lines. Filed as a note on that BL row rather than a new one.

**Judgment:** the interaction-round-trip hydration-gate design and the rerun-vs-empty-commit window mechanics were probe-settled design calls.

**Infra:** none — every dispatch returned a verdict.

## diff — 2 rounds

**Examined:** the whole-diff rounds that ran against this base, before the branch merged `origin/main` and the corpus moved to `876cbd06c156`. R1 (2 findings, BLOCKING): both invariant-8 P1 dispositions were filed in `BACKLOG.md` where the invariant names `DEFERRED.md`, and the `impeccable-gate:` marker read `audit=RAN` while the same section's provenance banner six lines below read "audit — DEGRADED: single-context". R2 (1 finding, NEEDS-ATTENTION): the ledger census was computed against a base `origin/main` had moved out from under — main had already promoted `right-now-transitions`, so the promotion yields 23, not 24. That finding is what forced the merge, which is what split this arc's diff rounds across two base files. Two more rounds ran on the new base; see `876cbd06c156.md`.

**Mechanizable:**

- "a ledger count is restated rather than read" (R2, and R3 on the next base found four more instances of the same shape) — the parent entry already carries the rule in prose ("count the allowlist, not arithmetic") and every site that broke it broke it the same way. A check that a census figure adjacent to a named allowlist matches that allowlist's live count is the mechanical form. Related to `BL-SPECLINT-PROSE-COUNT-PARITY`, which covers intra-document parity; this is document-vs-code parity, a wider claim, so it is noted here rather than folded into that row without a probe.
- "an invariant names a specific ledger file and the disposition lands in a different one" (R1 F1) — statically checkable: a filled `impeccable-gate:` marker with `p0`/`p1` greater than zero and `dispositions=recorded` implies at least one `DEFERRED.md` entry citing the plan. Cheap, and it would have caught this at authoring time.
- "a machine-readable marker contradicts the prose it summarizes" (R1 F2) — the marker grammar already provides `RAN-DEGRADED`; a check that `audit=RAN` cannot coexist with a DEGRADED banner in the same section is a two-line assertion in the existing closeout guard.

**Judgment:** none this stage — both rounds were mechanical-contradiction findings, not design calls.

**Infra:** none — every dispatch returned a verdict.
