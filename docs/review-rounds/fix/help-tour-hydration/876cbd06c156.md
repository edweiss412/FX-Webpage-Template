# fix/help-tour-hydration — round-economy filing (base 876cbd06c156)

The arc merged `origin/main` mid-review (diff R2 found the ledger census computed against a stale base), which moved `git merge-base origin/main HEAD` and started this file. Rows here restart at 1..N per the corpus design; consolidation across base files is forbidden. Total diff rounds across both files is 9 — two under `a7393880ae6d.jsonl` and seven here.

## diff — 7 rounds

**Examined:** round 1 here (conversation R3, 1 finding, NEEDS-ATTENTION): the census repair that prompted the merge fixed the live ledger and left the same stale `25 → 24` arithmetic in four other places — the spec's §5.5 instruction, the plan's Task 4 step 1, the plan's own Task 4 observable check (`grep -q "24 app-dependent"`, which would have gone GREEN on the wrong number), and the archived entry's promotion paragraph. Round 2 (conversation R4): APPROVE, 0 findings.

**Mechanizable:**

- "a restated count goes stale, and the repair fixes the instance rather than the class" — this is one shape found twice, one instance at a time, across the base boundary (R2 on the prior base, R1 here). The reviewer's own sweep on the second pass enumerated all four remaining sites at once, which is what closed it. The durable form is the document-vs-code census check noted in `a7393880ae6d.md`; the cheap form already available is that a plan's observable check must not restate a number the plan elsewhere instructs the implementer to derive.
- "a plan's own green check can pass on a wrong value" (R1) — the check `grep -q "24 app-dependent"` is a self-consistent tautology: it asserts the number the plan wrote, not the number the allowlist holds. A red-executability arm that rejects a green check whose expected value is a literal restated from the same document would catch this class at plan time. Filed against `BL-SPECLINT-RED-EXECUTABILITY-ARM` as an extension rather than a new row.

**Rounds 3-4, after an APPROVE that the diff then outran.** Round 2 returned APPROVE with 0 findings; real CI then failed on two guards no local meta-suite in this arc's plan covers — an MDX-wide backslash ban (`sheetIconLinkContainment`) tripped by Prettier's escaped-apostrophe spelling, and a copy-contract test slicing the tour source from a literal `Review queues</h3>` marker the heading conversion moved. Fixing both touched the UI surface, so the invariant-8 gate was rerun and the diff was re-submitted rather than merged on a stale verdict. R3 (1 finding): the marker repair anchored on the card's `aria-label` and sliced to the first `</p>`, widening the guarded passage to the eyebrow, duration, and heading — the phrase could then move out of the body with the assertion still green. Repaired with a second anchor (the body `<p>`'s className) and mutation-probed both ways. R4 (1 finding): the graduated archive entry still described a subtle-token measurement as a controlled pre-fix render, when §12 had already recorded that measurement as contaminated — a probe of :3000 that reached a sibling worktree, because `playwright.config.ts` sets `reuseExistingServer: !CI`. Corrected in the archive; the same round's sweep found no other stale attribution.

**Mechanizable (rounds 3-4):**

- "a test reads another file as SOURCE TEXT through a literal marker, and a formatting-level change moves it" — this class produced BOTH CI failures. The marker is not the assertion, so nothing today ties it to anything stable. The mechanical form is a lint on source-slicing test helpers: a marker into a tracked file must match a byte-pinned attribute (className, aria-label, data-testid), never rendered text. Both repairs here converged on exactly that rule by hand.
- "a passage-scoped assertion silently widens when its anchor is replaced" — the guard against it is the mutation probe R3 asked for, and it is cheap: move the required phrase to a sibling element inside the same window and require RED. Worth a helper, since `between`-style slicing appears throughout `tests/help`.
- "a local e2e probe measures a sibling worktree" — `reuseExistingServer: !CI` plus a hardcoded port. `tests/e2e/help-pages.spec.ts` now reads `E2E_PORT`; a check that no spec hardcodes the baseline port would close the class, and the config header already documents the hazard the specs were free to ignore.

**Rounds 5-6 — one assertion, three consecutive widenings, each real.** The repair for round 3's finding introduced round 5's, and that repair introduced round 6's, on the SAME assertion (`tests/help/heldChangesCopy.test.ts` test 9). R5: anchoring on the card's `aria-label` and slicing to the first `</p>` widened the guarded passage to the eyebrow, duration, and heading, so the phrase could move out of the body with the test green. R6: narrowing to the body `<p>`'s className fixed the width but not the END — the slice ran to end-of-file, and every later card carries a body `<p>` with the same className, so deleting this card's body and planting the phrase in a sibling card stayed green. Bounded by the card's own closing `</a>`, both mutants now go red. Neither round was reviewer imagination: each shipped a mutant that survived the previous repair.

**Mechanizable (rounds 5-6), and this is the arc's most reusable output:** a test that slices another file as SOURCE TEXT must pin BOTH ends to something the content contract guarantees — an attribute the acceptance criteria hold byte-identical, never rendered text, and never an unbounded run to end-of-file. Three rounds were spent discovering that by hand on one assertion. The mechanical form is a lint over source-slicing helpers in `tests/**` (`between`-style calls into a tracked file): flag a marker that is not an attribute literal, and flag a start-anchor with no matching end-anchor inside the same element. `tests/help` alone carries several `between` call sites, so the class has population. Filed here rather than built in this arc, which is a UI fix plus CI wiring.

**Round 7: APPROVE, 0 findings.** Its probes re-derived the shipping evidence independently — Prettier stability, one legitimate `_components.p` with zero nested, rendered-copy and 66-attribute equality, 15 mobile-safari cases, 17 governance registrations, and an `UNSEEN` census of 23.

**The count-parity mechanism, recorded so the next arc does not spend a round on it.** The wrapper appends a corpus row at DISPATCH time, so any filing committed before a verdict necessarily declares one fewer round than the corpus holds while that round is in flight. Round 6 was spent on exactly that gap. It is not fixable from inside a round; it resolves when a round returns APPROVE and the filing is written to include it with nothing dispatched afterward, which is what closed this arc. Treat it as a property of committing the corpus with the branch, not as a defect.

**Judgment:** none across the seven rounds. Rounds 1-2 were count-parity findings with a machine-checkable oracle; rounds 3-4 were anchor-stability and evidence-attribution findings; rounds 5-6 were passage-scope findings; round 7 approved. Every one was settled by a probe or a surviving mutant rather than by argument, and none touched the shipping page's rendered output — which has drawn zero findings since the first diff round on the prior base.

**Infra:** none — every dispatch returned a verdict.
