# fix/observe-error-telemetry — review-round record at 44b0d74b1107

The spec stage's record is at the previous base, `26b99c4c0ad8.md`; a mid-arc rebase onto `origin/main` moved the merge base, so the plan rounds opened a new file and restarted at 1 by design.

## plan — 4 rounds

**Examined:** every finding raised at this base. Round 1 returned 6, round 2 returned 5, round 3 returned 5, round 4 returned 1 — seventeen in total, every one admissible, none refuted. The decay is the shape worth recording: 6, 5, 5, then one mechanical ordering defect that the reviewer explicitly noted had no peer instance.

**Mechanizable:** two classes, and both are the plan-stage rendering of the class the spec stage kept hitting — a claim resting on something the author maintains rather than on something the code computes.

| round | the plan claimed | what actually held |
| --- | --- | --- |
| R1, R2 | a task's RED fails because a module is missing | a test-file edit greens it; the reds are now on `lib/` and `components/` lines |
| R3 F1 | AC-5 and AC-6 hold because two named registry rows get added | a named list goes stale on the next file; both are now subset checks over sets the walker computes |
| R3 F2 | two file lists compared after the same filter prove acquisition | a shared `.ts`-only predicate satisfies it while every `.tsx` loader vanishes; membership is now `ts.getScriptKindFromFileName` |
| R2 F3 | one witnessed propagation exemption proves the resolver works | a resolver handling the imported callee and failing the local one passes; both categories must be witnessed |

The second class is **a claim about what a suite observes, made without reading the suite**. Three instances: `_metaLedgerMintBar` does not "prove no ledger row was filed" (it constrains rows that exist); `_metaLedgerInProgress` does not pin commit atomicity (it observes an archive holding an in-flight entry and an in-progress entry naming a dead branch); and `_invariant8Closeout` treats *any* non-matching `impeccable-gate:` line as malformed while also requiring a marker from any unit that names the two command phrases — a double bind that reds the plan either way until the declaration and the marker are moved into the closeout file together.

The mechanizable arm for both: **before a plan asserts that a suite verifies a task, open the suite and quote the assertion.** Two of the three instances were repaired by declining the claim rather than by changing the plan's work, which is the cheaper outcome and would have been free at draft time.

declined: this arc files no `BL-`/`DEF-` row of any facing (Eric's directive, 2026-08-25), and the process mint freeze of 2026-08-25 would refuse both arms on their own terms — each done condition is a property of plan-authoring discipline, not a number a product arc or a human would notice moving. Recorded here, in the corpus the next plan author reads.

**Judgment:** the disposition of Task 6's absent observer. No suite watches whether the documented limits reach their headers, and building one would mean parsing spec §9 and checking each named file — new guard surface for a documentation task, which the mint freeze exists to refuse. Stating the weakness was the call; a reviewer could reasonably have wanted the guard instead.

**Infra:** the reviewer sandbox could not run Vitest in any round at this base — Vite attempts an `os.tmpdir()` write and gets `EPERM`. That is distinct from the `estree-walker` export-map failure recorded at the previous base, which was our brief's command form and was fixed. This one is the sandbox itself, and the reviewer worked around it by executing the parsers directly, which is why findings 3 and 4 of round 3 are quotes from the suites rather than test output. A dispatch that needs a suite run cannot get one here; briefs should ask for direct execution of the module under test instead.

**Infra, second instance, and this one cost the arc a silent stall.** `herdr agent send` writes literal text into the target pane and does **not** press Enter. Every escalation this arc sent that way sat unsubmitted in the orchestrator's prompt buffer — a read-back at 01:44 showed three of them queued as `[Pasted text #172][#173][#174]`, including the 00:36 spec-cap escalation the orchestrator reported never receiving. The command's own help says it: *"agent send writes literal text; use pane run when you want command text plus Enter."* `herdr pane run <pane> <text>` is the submitting form, and a read-back of the target pane is the only confirmation that a message arrived rather than merely being typed.

The failure is silent in the worst direction: `herdr agent send` returns `{"type":"ok"}` on a message nobody will ever see. An arc that escalates and waits on `blockedOn` will wait forever. Confirm every escalation by pane read-back, which the orchestrator had already asked for after the first miss.

One more edge worth knowing: the text argument goes through the shell, so backticks in a message are command substitution. A word wrapped in backticks is silently deleted from what arrives — this filing's own escalation lost the word `any` that way.
