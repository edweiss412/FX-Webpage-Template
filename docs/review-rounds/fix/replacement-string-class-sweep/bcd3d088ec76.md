## plan — 6 rounds

**Examined:** all 6 plan rounds (3 / 4 / 4 / 2 / 1 / 0 declared findings) on `fix/replacement-string-class-sweep`, rows under
`docs/review-rounds/fix/replacement-string-class-sweep/bcd3d088ec76.jsonl`. The spec stage's seven
rounds and its filing live under the previous merge-base, `8bf8709914a3`, because `main` was
merged between stages; the two files are one arc.

**Mechanizable:** one shape, already owned, plus one that is genuinely new.

- **A plan's own reconciliation section can be wrong about the plan.** Round 1 found AC-1b and
  AC-4b declared and owned by no task while the plan asserted in prose that every criterion was
  claimed; round 2 found Task 8's file table totalling 39 against its own declared 34. Both were
  PROSE claiming a property of the document that a three-line script decides. Both are now
  COMMANDS embedded in the plan — the AC reconciliation prints the unclaimed set, and Task 8's
  table is generated from `count-conservative.mts --list`. `spec:lint` already owns the
  neighbouring arm (`TASK_AC_UNRESOLVED` catches an id no task claims); what it cannot see is the
  opposite direction, a declared criterion no marker mentions, because that is a property of the
  pair rather than of either. Filed against the existing
  `BL-CODEX-GUARD-SPECLINT-PREDISPATCH-GATE`, which already owns "lint the plan before spending a
  reviewer on it" — this is one more assertion for that gate, not a new gate.

- **`red-state=authored` admits a collection failure, and should not.** Round 3 found Tasks 1 and
  2 declaring "the module does not exist, so the suite cannot collect" as their red. The
  RED-validity rule already rejects this in prose — an unresolved import goes green when the test
  file changes, not when the implementation lands — but nothing checks it, and the marker grammar
  accepts it happily. The mechanical form is narrow: under `--exec-red`, a `red-state=authored`
  marker whose `red-target` names a file that does not yet exist is asserting a collection
  failure by construction, and can be reported as such. *declined for this arc, filed as the
  observation:* the arm belongs to the spec-lint owner, and this plan's repair (stub the module,
  cite the stub's return) is the pattern the arm would enforce.

**Rounds 5 and 6, added after this filing was first written at the threshold.** Round 5 was one
finding — a third population figure, 3670, surviving as a live claim where the two real numbers
are 3586 (the scanner's) and 3672 (the derivations'). Round 6 APPROVED with zero.

That makes rounds 4 and 5 both about the same two figures, once conflated and once stale, which is
a duplication signal rather than two mistakes: the plan restates populations the spec and probe
record already own. The narrowing — state them as a pointer plus a command, one owner — was
written and DECLINED, because the plan was already APPROVED by then and applying it would mean the
diff that merges is not the diff the final round examined. It is recorded here instead, as the
thing to do first if this plan is ever revised.

**Judgment:** the four earlier rounds divide cleanly, and only one division was expensive.

Rounds 1 and 2 were BOOKKEEPING — criteria without owners, a table that disagreed with its own
total, an enrollment fan-out I had not looked up. Cheap to fix, and every one was a fact about the
document that a command can settle, which is why both are filed above rather than here.

Round 3 was DESIGN, and it is the one worth the rounds. Three of its four findings were the same
mistake in three costumes: **I kept declaring reds that could not be observed.** A missing import
(Tasks 1, 2), an assertion that is green on arrival (Task 4), and a sequencing that required a
measurement to exist before the run that produces it (Task 9). Each time I had written a plausible
sentence about what would fail, and each time nothing would have. The rule that catches all three
is the one the repo already states and I did not apply: **name the production line whose defect
makes the red, then check that line exists and is defective.** A red-target that resolves is not a
red-target that explains.

The fourth was a claim about cost rather than correctness — Task 9 spending "one heavy slot" while
containing two unwrapped heavy-shaped commands. Worth recording because the reasoning that
produced it is the tempting one: I judged the parity check by its measured cost (a key comparison,
no mutants) instead of by its invocation shape. `AGENTS.md` classifies by shape precisely so that
argument never has to be had, and the honest number is four wrapped invocations.

**No round in this stage was spent on the product.** The judge, the accept-set and the repair
classification were settled in the spec stage and no plan round reopened them, which is the
division of labour the two stages are for.
