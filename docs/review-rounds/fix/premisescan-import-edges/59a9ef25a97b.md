## diff — 6 rounds

**Examined:** R1–R6 on base `59a9ef25a97b`, subject `tests/mutation/source/premiseScan.ts` (an AST recognizer) plus its spec, plan and registry. Declared findings 8, 8, 5, 2, 2, 0. Verdicts BLOCKING x5 then APPROVE.

**Mechanizable:** two classes, both of which cost rounds and both of which a check could have closed before dispatch.

1. **"A caller rebuilt a discriminated-union value by hand and dropped a field."** This was ONE defect found THREE times across three rounds, at three different returns: R1 #2 caught the star loop's two returns, R3 #1 caught the E2 extent merge. Each repair fixed the instance in front of it; none derived the cover. The mechanical form is a lint or structural test over `ExportResolution` constructions — a site that has a `via`/`res` of the same type in scope must SPREAD it (`{ ...via, … }`) rather than re-literal `{ kind: …, … }`. The round-4 brief carries the derived enumeration (11 sites, one had a `via`); that enumeration is what should have existed at round 1, and it is a grep, not a judgement. Filed as `BL-EXPORTRESOLUTION-SPREAD-NOT-RELITERAL`.

2. **`accepted` siteIds are LINE-KEYED, so every diff that moves lines invalidates the ledger.** `premiseScan`'s two equivalences were re-keyed FOUR times in one day (601→721→604→603 and 1752→2061→1864→1872), each round costing a ~8-minute gate cycle to discover and another to verify — call it 90 minutes of wall clock spent on bookkeeping that carried no information. The gate already names both sides (`unaccepted-survivor` lists the new keys, `stale-ledger-row` the old) and the two sets are complementary, so the re-key is fully derivable; it is just derived by a human reading a failed run instead of by the tool. Keying on the mutated EXPRESSION plus a disambiguator, or emitting a `--rekey` patch, removes the class. Filed as `BL-MUTATION-SITEID-LINE-KEYED-CHURN`, which also carries the third shape below.

   A third shape appeared at the end and belongs with these: **removing dead code CHANGED the mutation surface.** Deleting the orphaned `forward` guard forced a rewrite of the condition containing it, and the natural rewrite turned a truthy numeric check into `carried.length > 0` — an operator where there had been none, so the gate reported a brand-new survivor whose `>=` variant is arguably equivalent. The repair was to delete the site rather than bless it, but nothing warned that a deletion had widened the surface; the gate is the only thing that noticed, one cycle later.

**Judgment:** the round-2 scope decision, which is the arc's real content and could not have been mechanized.

Round 2's eight findings split 0 / 6 / 2: **zero** on the export-resolution core the arc exists to build, **six** on two heuristics added during round-1 REPAIR, two elsewhere. Findings 4, 5 and 7 were each one more execution spelling — class static initializers, enum member initializers, namespace bodies, an inline object method, `functionExpr.call(...)`, an inline class constructor, a hook wrapped in `void`. That is the same-axis recurrence AGENTS.md names, and the axis was opened by the repair rather than by the deliverable.

The user was asked and chose ship-and-fence: both heuristics withdrawn to §4 limits 16 and 17 carrying round 2's own probe output, and round 2's finding 6 fenced in both directions as limit 18. **The fence held** — round 3 returned zero execution-spelling findings and the rate fell 8 → 5, with only one of those five a behavioural defect in shipped code.

The transferable lesson is the diagnostic, not the outcome: **when a round's findings cluster on something the PREVIOUS round's repair introduced rather than on the deliverable, the split itself is the signal.** Counting findings per round would have shown 8 → 8 and read as flat; counting them per AXIS showed the deliverable was already clean at round 2.

**Infra:** the reviewer sandbox could not run the project's own verification commands in rounds 2, 3 and 4 — `pnpm vitest` and `pnpm mutation:guards` both failed with `EPERM` creating Vite's temporary `ssr` directory. The reviewer worked around it by invoking `classifyTests` directly through `/dev/stdin`, which is why its probes are shaped that way. This did not produce wrong findings — spot-checks of R2 #1/#3 and R3 #1 reproduced independently here — but it means every behavioural claim cost the reviewer a bespoke harness, and one round-2 finding (#4, class static initializers) did NOT reproduce against my fixture, which a runnable suite would have settled in one command rather than in prose.

**The rate is the strongest evidence here, and it is worth stating as a number.** Findings per round: **8, 8, 5, 2, 2, 0** — and the composition moved even faster than the count. Round 2 was 6/8 on heuristics the round-1 REPAIR had introduced; round 3 was 1/5 a behavioural defect in shipped code; round 4 was **0/2 code — both findings are stale prose in the spec and plan.** Two of round 4's sites were ones the round-3 sweep had touched and left inconsistent, which is the same class-sweep failure as Mechanizable item 1 wearing prose instead of types: the round-3 pass amended the sites the reviewer NAMED rather than deriving the set. Round 4's pass derived it (`grep -niE 'verdict.neutral|EXPECTED_ENV_TOUCHING|zero movement|headline constraint|COMMENTS ONLY|equivalent: 3'` across both documents, then dispositioned every hit as amended, historical-and-labelled, or already correct).

**A documentation amendment is a class-sweep target exactly like a code repair.** That is the cheapest lesson in this filing and the one most likely to recur: AC-1 was asserted in nine places across two documents, the amendment touched four of them in round 3 and five more in round 4, and nothing mechanical distinguishes "the spec says X" from "the code does X" when X has changed.

**Round 5 is the counter-example to this filing's own optimism, and it belongs here.** The rate flattened at 2 rather than reaching zero, and one of the two was a genuine BLOCKING defect in shipped code: the memberless-namespace report fired before asking whether the specifier was in-repo, so `import * as ts from "typescript"` classified `unclassifiable` — against ratified L-2, and against §4 limit 3's own words about that exact import. The near-domain sweep measured 70 tests across 6 files, every one reported for that reason alone.

Two things make it worth recording rather than filing away as one more bug. **First, it was pre-existing in the arc and my round-2 repair DUPLICATED it** — the own-body path mirrors the traversal's namespace branch, and mirroring copied the omission along with the rule. A mirrored rule is a class of two by construction, and neither the implementer nor four review rounds treated it as one. **Second, it went unfound for five rounds because every namespace fixture in the suite used an in-repo specifier**; the bare case was never exercised, so no amount of re-reading the rule would have surfaced it. What surfaced it was a corpus sweep — the reviewer running the shipped classifier over 2343 near-domain files and counting.

That is the same lesson as Mechanizable item 1 from the other direction: **the cover has to be derived from the input space, not from the rule's statement.** Enumerating the rule's call sites finds mirrored omissions; enumerating the domain finds untested inputs. This arc needed both, and got each one round late.

**Round 6 closed it at zero.** The arc converged on the round after the one that flattened, which is the outcome the round-5 note above declined to predict — and the reason it converged is worth separating from luck. Round 5's defect was found by a corpus sweep, and round 6's brief carried the fix plus a stated, refutable claim about its cover; there was nothing left for a sweep to find because the sweep had already been run against the shipped classifier rather than against the diff.

Final shape: **six diff rounds, 8/8/5/2/2/0 findings**, one ratified scope decision, three documented limits carrying the reviewer's own probe output, one invariant breach recorded rather than papered over, and two mechanizable classes filed with measurements instead of absorbed as review habit.
