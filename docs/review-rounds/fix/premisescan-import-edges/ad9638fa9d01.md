# Review-round economy — `fix/premisescan-import-edges` @ `ad9638fa9d01`

Continuation of `daa53759a953.md`. PR #827 merged as `ad9638fa9` on 2026-08-16 at 22:07 CDT and this branch was rebased onto it, which moved `git merge-base` and started this corpus file. Round numbering restarts here under the gate's contiguity rule: the plan stage's rounds 1-8 and the spec stage's rounds 1-6 are in the sibling file, and this file's plan rounds 1-7 are the arc's plan rounds 9-15.

## plan — 7 rounds

Declared findings by round: **3 / 2 / 3 / 1 / 2 / 3 / 3** — the arc's rounds 9-15.

**Examined:** each round's Codex verdict and its repair commit (`e0fb43d51`, `c71f1a644`, `4ad9130ed`, and this one), every probe run to triage a finding, and the three oracles those rounds produced. Every finding across the seven rounds reproduced; none was refuted.

**Round 7 is where the seed rule and the two structural defenses each proved one notch narrower than their statement, and all three failures are the SAME shape.** The seed for module-load references covered the static `import "./side"` form only, so a bare `await import(specifier)` in a top-level statement — one hoist away from a real near-domain site — stayed silently free. The merge step's commands ran but did not ASSERT: `gh pr view` exits 0 printing `"state":"OPEN"` and `git rev-list --left-right --count` exits 0 printing `27 111`, both confirmed against constructed input. And the anchor check added in round 5 exempted any citation sharing the anchor's line NUMBER, so a wrong FILE at the same line passed. Each was a guard whose premise was narrower than the claim above it, which is this repo's `BL-GUARD-PREMISE-REACHABILITY` shape appearing three times in one round. The repairs widened each to its stated claim and probed the discrimination: the anchor check now also fails a `why=` that cites NOTHING, which is what made it vacuous for six of the seven markers.

**Round 6 is the first round whose findings the oracles could not have produced, and all three are about the plan's DESIGN rather than its bookkeeping.** One was a genuine data-flow gap — `resolveExport` follows forwards internally, so a barrel it loads is invisible to `reaches`, and a side-effect import on an intermediate barrel would vanish while the terminal extent stayed pure. One was invariant 12 read literally: the marker-removal commit must BE the last commit, which two successive drafts had each broken in opposite directions. One was that Task 7's merge step was prose where the pipeline needs commands. That is the shape of a stage nearing its end: the mechanical layers are closed, and what remains needs a reader.

**Round 5 is the cost of repairing a class by hand, measured.** Round 4's rationale sweep re-pointed five `red-target=` anchors and round 5 found two it had missed — the same axis, one round later, which is the trigger the class-sweep rule names. The repair is therefore mechanical rather than a third sweep: `planCensus` now checks every marker (the anchored file and line must exist; and if the `why=` cites a `path:line` of its own, that citation must BE the anchor). Both round-5 instances fail it by name, and the check was proven by re-introducing one.

**One defect class produced eight of the nine findings, and the repair kept landing one layer short of it.** The class is *the plan asserting something about its own test blocks that nobody executed*. Rounds 8 and 9 miscounted the blocks' cases; round 9 found a case listed RED that was already GREEN (`E4: a NAMED default class declaration resolves` — today's local-declaration lookup finds the class's own name, so it passed and discriminated nothing); round 10 found the count oracle validating the duplicate DECLARATION rather than the prose; round 11 found a duplicate count two paragraphs outside the scanned window. Each repair was correct and each left the next layer exposed, which is the shape worth recording: **when the artifact under review is executable, every layer of prose about it is a claim the artifact can settle, and any layer left unexecuted is where the next round goes.**

The terminal repair was to stop describing and start running. `planRun` splices every test-shaped block into a real suite in front of the shipped harness, executes it against the merged scanner, and diffs the observed failing set against a declared `plan-redset` block: **102 executions, 67 red, 35 green**, by name. `CLAIM` lines pin each block's split size, and the run rejects any count RESTATED in a forecast region, so a number lives in exactly one gated place. Round 11's only non-oracle finding is the evidence it works — F1 was a genuine design gap (a side-effect import inside a reached helper stayed silently `environment-free`), found by probing the merged scanner rather than by re-reading, and the fixture written for it was measured red rather than forecast red.

**Mechanizable:**

1. **A plan whose test blocks are executable should EXECUTE them, from round 1.** Filed under `BL-PLANLINT-ASSERTIONLESS-EXPECT`, whose scope is already "a plan's executable content is checked the way executable content is checked". The three oracles here cost roughly two hours and closed eight findings; the first of them (`planTypecheck`) arrived at plan round 5 and the last (`planRun`) at round 9. Arriving at round 1 would have closed the same eight before they were written.
2. **A count restated in prose is a second copy that drifts.** Two rounds went to it. The narrowing repair — prose states WHICH, a gated line states HOW MANY, and the oracle rejects a restatement — generalizes to any plan that declares counts, and is cheaper than a prose parser, which is the widening direction the same-axis rule forbids.
3. **A pinned base that is a live branch will move.** Already filed as `BL-SPEC-PIN-VS-BRANCH-HEAD` from the spec stage; this stage paid for it twice more (round 9's F3, and eleven `CITATION_LINE_OUT_OF_RANGE` lint failures that resolved themselves the moment the branch merged and the citations were re-anchored to tracked lines).

**Round 4's single finding is the class the oracles cannot reach, and it is worth naming precisely.** Every count and every status in this plan is now measured, so what round 4 found was RATIONALE drift: four places where the prose explaining WHY a case is red contradicted the measurement it sits beside — a membership rule with a counterexample among its own foils (a renamed default of a PURE target measures free either way), a `why=` naming a forward stub that Task 2 says plainly does not exist, a Step attributing a behaviour to Task 1 that Task 1's own deferral table assigns to Task 3, and a foil list swallowing a case the redset records as red. Each was repaired, and the same sweep re-pointed five `red-target=` anchors that still carried pre-merge line numbers. **An oracle that checks WHAT is red cannot check WHY**, and the honest close of this stage is that the why-text is human-verified prose, reviewed rather than gated.

**Judgment:** whether a transitive side-effect import must report or may stay a documented limit (it must report — the spec's bound is stated over resolved in-repo references, and the worst case here is silence, not a conservative demote); and how to satisfy invariant 12 and "the review covers what merges" simultaneously in Task 7, resolved by giving the last commit its own scoped review rather than granting either rule an exception.

**Infra:** none this stage.
