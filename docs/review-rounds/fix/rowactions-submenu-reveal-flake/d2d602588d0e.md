# Round-economy filing — fix/rowactions-submenu-reveal-flake @ d2d602588d0e

## spec — 7 rounds

**Examined:** all seven spec rounds (findings 5 → 3 → 2 → 1 → 1 → 2 → 0, APPROVE at R7; every finding accepted, none refuted). R6's two findings were stale residue of R5's own repair (the fix-round-regression class): a §8 bullet contradicting the reversal and an uncorrected family count — both purely textual. R5 justified its past-cap dispatch: it surfaced a genuine design defect (the scroll-restore's own events self-sustaining a per-frame re-measure loop through the capture listener), forcing a probe-backed REVERSAL of a round-1 rejected alternative — the self-origin scroll filter — now fenced in both directions in §1. R1 was dominated by cross-checking design claims against live call-site semantics (the cap-application restore-leak, probe-description accuracy, a stale sibling count). R2–R4 were a single sustained vector: whether the §5.4 behavioral pins actually discriminate the migration-mutant families they claim to close — each round's repair was accepted and the next round found the residual instance (cap-state blindness to coordinates in R3; the unclipped early-return bypass in R4). The reviewer's live-core probes (coordinate deltas, the 322→372 stale-fit number) were load-bearing in every one of those rounds; no probe-less finding was accepted.

**Mechanizable:** two candidates. (1) R3 F2's shape — a runtime contract introduced in a design section with no named test case in the test-design section — is a cross-section presence check a spec-lint arm could catch. (2) R1 F5's shape — a sibling-file count restated as prose and stale against the workflow file — is the `NUMERIC_NOUN_MISMATCH` family extended to file-list cardinality vs a cited workflow line.

declined: both are single observed instances; (1) needs a contract-sentence recognizer over free prose, the recognizer-growth shape AGENTS.md's repair-direction rule warns against, and the mandated self-review passes already own that check; (2) needs a YAML file-list parser in `lib/specLint/numerics.ts` for one defect caught in round 1 at minimum cost. Refile with a `BL-` id if either shape recurs on another arc.

**Judgment:** the R2–R4 pin-discrimination vector is not mechanizable — each finding required constructing a mutant against a live code path and observing which assertions move. The generalizable lesson is authoring-side, not lint-side: when a spec claims a regression pin discriminates a mutant family, derive the pin from the mutant (state the mutant first, then the assertion that moves under it) rather than from the happy path; three of the four rounds here were that derivation being done by the reviewer instead of the author.
