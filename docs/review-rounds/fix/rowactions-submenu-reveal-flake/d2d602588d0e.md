# Round-economy filing — fix/rowactions-submenu-reveal-flake @ d2d602588d0e

## spec — 4 rounds

**Examined:** all four spec rounds (findings 5 → 3 → 2 → 1, strictly decaying; every finding accepted, none refuted). R1 was dominated by cross-checking design claims against live call-site semantics (the cap-application restore-leak, probe-description accuracy, a stale sibling count). R2–R4 were a single sustained vector: whether the §5.4 behavioral pins actually discriminate the migration-mutant families they claim to close — each round's repair was accepted and the next round found the residual instance (cap-state blindness to coordinates in R3; the unclipped early-return bypass in R4). The reviewer's live-core probes (coordinate deltas, the 322→372 stale-fit number) were load-bearing in every one of those rounds; no probe-less finding was accepted.

**Mechanizable:** (1) R3 F2's shape — a runtime contract introduced in a design section with no named test case in the test-design section — is a cross-section presence check a spec-lint arm could catch (grep design-section `throw`/contract sentences against the test-section case list). declined: one observed instance in the corpus; a contract-sentence recognizer over free prose is the recognizer-growth shape AGENTS.md's repair-direction rule warns against, and the existing self-review pass (run the mandated passes) already owns this check — refile with a `BL-` id only if the shape recurs on another arc. (2) R1 F5's shape (a sibling-file count restated as prose and stale against the workflow file) is the existing `NUMERIC_NOUN_MISMATCH` family extended to file-list cardinality vs a cited workflow line. declined: single instance, caught in round 1 at minimum cost; extending the numerics arm to resolve workflow-file list cardinality needs a YAML file-list parser for one observed defect — refile against `lib/specLint/numerics.ts` if it recurs.

**Judgment:** the R2–R4 pin-discrimination vector is not mechanizable — each finding required constructing a mutant against a live code path and observing which assertions move. The generalizable lesson is authoring-side, not lint-side: when a spec claims a regression pin discriminates a mutant family, derive the pin from the mutant (state the mutant first, then the assertion that moves under it) rather than from the happy path; three of the four rounds here were that derivation being done by the reviewer instead of the author.
