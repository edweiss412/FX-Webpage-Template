# Round-economy filing — fix/modal-wait-candidate-contract @ 59a9ef25a97b

## plan — 4 rounds

**Examined:** rounds 1-3 returned 6 / 8 / 3 findings, all accepted; round 4 APPROVEd with 0. Round 1 was structural compliance (loader-error red masking, same-commit mutation-ledger contract, missing expectedLedgerKinds reconciliation, undemonstrated gate red, graduation not red-first, missing review/merge tail). Round 2 was probe-backed execution defects, several in the round-1 repairs themselves (vacuous origin-(d) fixtures against a spec-only fixture root, a gates command that collects nothing outside the mutation project, a missing EXPECTED_ENV_TOUCHING row, an incomplete GuardSurface row, a `| sum` checksum masquerading as a count, a Task 4/5 last-commit ordering contradiction, a wrong claimed red message, missing string-presence mutants). Round 3 caught that the round-2 "complete enrolment row" repair had been AUTHORED but never APPLIED — the repair script appended the substitution to a list it never looped, and nothing verified the new text landed — plus an empty-commit branch and unwrapped heavy commands.

**Mechanizable:** two shapes. (1) A repair-verification step: after any scripted document repair, grep the repaired file for a distinctive span of the NEW text and fail loud on zero hits — the round-3 regression existed because the repair commit was trusted on its exit code, not its effect; this is a session-procedure rule, recorded here for the lessons corpus rather than a repo lint. (2) A plan-lint arm over executable command spans in `docs/superpowers/plans/**`: flag `--project mutation` or `test:fast` invocations not wrapped in `pnpm heavy` (the AGENTS.md semaphore rule made checkable text-side); candidate extension to `lib/specLint/**`, unfiled pending a corpus count per the first-scheduled-step precedent of the planlint ledger row (`BL-PLANLINT-RECONCILIATION-AND-MARKER-CITATIONS`).

**Judgment:** the round-2 findings on fixture vacuity and command-collection are the guard-premise-reachability class applied to a PLAN's claims — they need a reviewer running probes, not a lint; the round-1 findings were compliance with rules `docs/agents/writing-plans.md` already states, confirming that corpus's own observation that early rounds are dominated by non-compliance rather than rule gaps.

## spec — 3 rounds

**Examined:** round 1 (within-scope placement limit overclaim) and round 2 (comment-trivia statement inheritance refuting the "no enclosing statement" premise) each returned one probed P1 repaired into the spec as a documented-limit re-scope and a mechanism change (strip-before-match); round 3 APPROVEd with 0 after independently re-probing both repairs.
