# Review-round filing — `fix/screenshots-drift-residue` at `9a621a5792ea`

## diff — 4 rounds

**Examined:** the whole merge candidate, 22 files and roughly +1300/-130, closing three ledger rows. Four rounds at this base, findings 3, 4, 2, 1. Every one was taken and repaired; none was argued down. The arc's spec stage ran four rounds at `d04d6370985f` and its plan stage four at `e381de76ea87`, each filed there; the merge base moved twice when this branch absorbed `origin/main`, which is why the three stages sit in three files.

**Judgment: three of the four rounds found the SAME defect, and it was not in the code.** Round 1 found three ordinary implementation gaps. Rounds 2, 3 and 4 each found that the PREVIOUS round's repair had fixed the instances the finding named and missed other copies of the same claim.

The corpus of that one defect:

- Round 2: the canonical spec still called four waivers "CLI read-path windows" after round 1 corrected the source comments; and four more unqualified total-coverage claims survived a repair that had already qualified four others.
- Round 3: two more coverage claims in an earlier plan, and two more copies of a re-file trigger that had just been narrowed in three places.
- Round 4: two test-case names still calling every registry entry a "flag" after the registry had been renamed away from that word.

**What actually fixed it was changing the METHOD, not trying harder.** Through round 3 each repair was authored by reading the finding and editing what it pointed at. The repair that held was one exhaustive `rg` per claim SHAPE across the whole tracked tree, applied before writing anything — and round 4 proves even that is only as good as the pattern, since a Shape-C sweep for `Shape-4 residue|flag-shaped residue|two hand-marked flags` missed `registered flag` and `file and a flag`.

Round 4's repair is therefore the first that removes the class rather than its instances: the registry key's third segment was NAMED `flag` while one of its seven values is an exported component, so every claim written about it inherited a false universal. Renaming the concept to `symbol` makes the wrong sentence hard to write again. That is the difference between a sweep and a derivation, and it took four rounds to reach.

**A note on cost, since it is the reason this filing exists.** Nine of the ten findings across rounds 2 to 4 were prose. None changed shipped behaviour. They were admissible and correct — a guard whose documentation claims total coverage while its scanner silently drops a live branch is exactly the "silently wrong" the consequence bound forbids — but the ratio is the signal: this diff's one behavioural change (`Date.now()` to `await nowDate()`) cleared review in round 1 and was never revisited.

**Mechanizable:** one.

- **A claim of totality about a scanner is checkable against that scanner's own accept-set.** Every finding in shapes A and C was a sentence saying "every" or "never" about a population the scanner's arms do not fully cover. A lint arm over the guard's own tree could flag an unqualified totality claim in a file that also declares a documented limit, which is where all seven live instances sat.

declined: this arc files no `BL-`/`DEF-` row of any facing, by Eric's directive of 2026-08-25. The candidate is a property of a lint tool rather than of anything a crew member or Doug can observe, so it is recorded here and in that tool's documented limits, which is where the process-mint freeze sends a process-facing finding whose done condition is a property of the guard.

**Infra:** none at this base. Two local full-suite runs showed failures in `.db.test.ts` and reseed round-trip suites that pass in isolation and in their own groups; this machine's `TEST_DATABASE_URL` is non-loopback and a sibling arc was reseeding the shared local Supabase in the same window. Nothing in the diff can reach the seed path.
