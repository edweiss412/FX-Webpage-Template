# Review-round filing — `fix/screenshots-drift-residue` at `d04d6370985f`

## spec — 4 rounds

**Examined:** `docs/superpowers/specs/ci/2026-08-25-drift-residue-design.md`, a three-decision spec closing `BL-SERVER-TIME-GUARD-EXCLUDES-LIB`, `BL-RENDER-FAULT-TERNARY-RESIDUE-ASYMMETRY` and `BL-SCREENSHOTS-DRIFT-SINGLE-FAILURE-UNEXPLAINED`. Four counted rounds at this base (a fifth dispatch, numbered 1, returned `no_verdict` on the wrapper's attempt cap and is not a review round). Findings 5, 4, 1, 1. Every one was taken and repaired; none was argued down.

**Judgment:** the findings that mattered were all one shape, and it is not a shape a linter reaches. Each was a CLAIM the spec was not entitled to make.

- Three declared causes that overlapped on live nodes, so a wrong relabel passed all three predicates and "0 false causes" meant nothing.
- A cause named `unreached` derived from a predicate that did not establish unreachedness, which would have labelled a reached `IfStatement` candidate unreached.
- A re-file trigger set at 3 that the computable set tripped on the day it was written, because the 3 discounted four sites by a rule the assertion never applied.
- A conclusion that eight passing records refuted the runner axis, when not one of them reproduces the failure they were supposed to explain.
- A trigger naming an event that cannot occur: one evidence record holding two hashes for one identity, which the shipped verifier rejects.
- A decline that left four normative coverage sentences false in the design that owns the contract.

The unifying error is asserting more than the evidence carries, and the unifying repair is narrowing the claim rather than widening any recognizer. That is the arc's own directed repair direction, and it held on every finding: nothing was added to `classifyExpression`, no `whenFalse` arm was built, no perceptual comparator was written, and one cause was DELETED from the taxonomy because no AST predicate decides it.

**Mechanizable:** two, and both are narrow.

- **A spec's own verification command should not match the spec.** AC-6 spelled the stale `path:line` token that its `rg` command greps for, so the command could never come back clean. A lint arm could flag an acceptance criterion whose literal text matches its own declared command.
- **A cross-document section citation should resolve to a section.** `spec:lint` validates that a cited `path:line` exists; it makes no claim about a `§N.M` reference into another document. Round 4's finding was exactly that: `§8.5` resolves, and it is the wrong limit. The target's list is additionally labelled 1, 2, 3, 5, 4 in source order, so this is not purely an authoring slip.

declined: this arc files no `BL-`/`DEF-` row of any facing, by Eric's directive of 2026-08-25, and both candidates are properties of `spec:lint` itself rather than of anything a crew member or Doug can observe. They are recorded in that tool's documented limits, which is where the process-mint freeze sends a process-facing finding whose done condition is a property of the guard.

**Infra:** round 1 produced no verdict. Attempt 1 was SIGTERM'd at the wrapper's 1200s attempt cap while the reviewer was still probing, attempt 2 died 304s later on the 1500s total cap. Not the codex reaper — it logged `gated: 2 live codex tree(s) active; no orphans` at the time. The first retry then failed on `ATTEMPT_MAX_SECS exceeds max 1380`, a usage error that exits before dispatch, and cost fourteen minutes because the wrapper's log was not read immediately after launch. Two lessons, both cheap: read `wrapper.log` in the same turn as the dispatch, and when a review is genuinely long, inline the probe outputs so the reviewer verifies rather than re-derives. Round 2 carried the probe outputs and finished in 1071s, inside the same cap that killed round 1.
