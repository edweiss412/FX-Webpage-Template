# Review-round filing — `fix/screenshots-drift-residue` at `e381de76ea87`

## plan — 4 rounds

**Examined:** `docs/superpowers/plans/2026-08-25-drift-residue/plan.md`, the five-task plan executing the arc's three-decision spec. Four rounds at this base, findings 6, 4, 1, 0, closing APPROVE. The arc's spec stage ran four further rounds at the previous base `d04d6370985f` and is filed there; this branch's merge base moved when it absorbed `origin/main` at `e381de76e`, which is why the two stages sit in different files.

**Judgment:** every finding was about an ORACLE, never about the design the plan executes. Not one round disputed depth 1, the decline, the two-cause taxonomy or the screenshots reading; all eleven were some version of "this assertion does not prove what it says it proves."

Three shapes account for all of them, and they compound:

- **An oracle with no durable base.** A waiver count derived from a three-dot diff range gives 12 on the PR and 0 after merge.
- **An oracle green in both directions of the change it forbids.** The sharpest instance came with a live mutant: asserting "no `unknown` candidate originates from a ternary" holds before AND after someone teaches `classifyExpression` to recognise the guard, because the site becomes an ACCEPTED candidate rather than an unknown one.
- **An oracle that pins a different thing than it claims.** A control for "no `ConditionalExpression` begins here" that actually pinned `classifyExpression === null`, which belongs to the other cause; a branch-ordered derivation omits the untested conjunct and passes both controls.

The expensive lesson is the third round, and it is a lesson about REPAIRS rather than about plans. Round 2 said a text match was too weak to bind a registry key; the repair swung to requiring a declaration at that line; round 3 blocked because a ternary entry's line is where the flag is USED, so the two criteria could never both hold. An over-correction is its own defect class, and the fix was not a third rule but the recognition that the two causes genuinely put their coordinate at different kinds of node. Splitting the contract per cause dissolved the contradiction, and each half got stronger than the text match it replaced by asserting UNIQUENESS in the file rather than existence.

What generalises: after repairing a criterion, ask whether it can still be satisfied ALONGSIDE its siblings. That check was then run pre-emptively on the one other pair with the same exposure — the extended specifier resolver against the assertion that the render-fault scanner does not move — by running the scanner against a copy of itself carrying the extension. Identical, 35 candidates both ways. Round 4 confirmed all seven coordinates unique and satisfiable and returned zero findings.

**Mechanizable:** one.

- **An acceptance criterion whose siblings it contradicts is statically detectable in the narrow case.** Where two criteria on one task assert incompatible node kinds at the same coordinate — "a `ConditionalExpression` begins here" against "a declaration is here" — a plan linter could reject the pair without executing anything.

declined: this arc files no `BL-`/`DEF-` row of any facing, by Eric's directive of 2026-08-25, and the candidate is a property of `spec:lint` rather than of anything a crew member or Doug can observe. It is recorded in that tool's documented limits, which is where the process-mint freeze sends a process-facing finding whose done condition is a property of the guard.

**Infra:** none at this base. The dispatches ran inside the wrapper's caps once the briefs carried their probe outputs inline, which is the practice the spec stage's filing arrived at.
