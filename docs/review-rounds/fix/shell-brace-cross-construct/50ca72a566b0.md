# Review-round economy — `fix/shell-brace-cross-construct` at base `50ca72a566b0`

## spec — 5 rounds

**Examined:** every finding across the spec stage, classified by what it was AGAINST — the
DELIVERABLE (the design in §1 and §3, and the acceptance criteria that decide which implementations
the spec ratifies) or the arc's own INSTRUMENTS (the six probes it added to measure them). Rounds:
r1 BLOCKING/3, r2 NEEDS-ATTENTION/2, r3 NEEDS-ATTENTION/4, r4 BLOCKING/1, r5 APPROVE/0 — ten
findings, every one accepted as real, ZERO refuted across the whole stage. The count is read from
`50ca72a566b0.jsonl`, not from a running tally.

**The stage CONVERGED rather than closing on a disposition.** Round 4 was terminal under the cap and
returned BLOCKING; the orchestrator authorised one SCOPED round 5 (`CONVERGENCE_ACK`) because round
4's repair was a design restructuring — one guard per lexical context, three recognizers — landed
after the terminal verdict, and a criterion that had escaped three recognizers one spelling at a time
was the last thing that should self-certify. Round 5 was bounded to that decision and the two rows
that pin it, and returned APPROVE with zero findings. Recording the shape because the alternative on
offer was a named diff-stage kill, which would have covered the code and not the design decision, and
the design is where the class lived.

| round | verdict | findings | against the DELIVERABLE | against the arc's INSTRUMENTS |
|---|---|---|---|---|
| 1 | BLOCKING | 3 | **2** — `$$` missing from opener precedence; §3.2 contradicted the Q3 requirement | 1 — the limit baseline was the working tree, so it self-compares the moment the repair lands |
| 2 | NEEDS-ATTENTION | 2 | **1** — a repair-created report on bash-rejected input, which forced the bound to be scoped to inputs bash ACCEPTS | 1 — `VACUOUS` printed an honest label and still exited 0 |
| 3 | NEEDS-ATTENTION | 4 | **1** — the §3.2 site-path invariant was false | 3 — the bash-rejected class never verified rejection; the movement was not recorded exactly; every population denominator was stale |
| 4 | BLOCKING | 1 | **1** — `$$` precedence was missing from a THIRD recognizer: a `${…}` operand is re-lexed by `lexShellWords`, whose own `$`-branches read the second `$` as an opener | 0 |
| 5 | APPROVE | 0 | — | — |

**Split by subject: four findings against the arc's own INSTRUMENTS, three against the DELIVERABLE,
three against the prose.** The instruments drew the most, and they failed in the SAME class as their
subject. This arc's whole subject is that presence cannot discriminate a
boundary defect — a mis-delimited span still leaves something reporting, so only attribution sees it.
That exact class then appeared three times inside the measuring apparatus: a movement recorded as a
count pair let both `$$` under-repairs pass the entire probe; a candidate-side check over counts and
aggregate flags could not see hit text, offsets, tokens or suppression; and a count-based reading of
the eight consumer routes reported all eight identical when one of them moves. **The instrument
inherits every class the subject has**, and the third instance was found inside the verification
rather than inside the thing verified, which is the version worth carrying forward.

**The second pattern is a rule that needed one more recognizer every round it was examined.** `$$`
precedence entered at round 1 in the delimiter walk, round 3 found `attachedTargetEnd` still without
it, and round 4 — the terminal round — found `lexShellWords` re-lexing `${…}` operands through a
third set of `$`-branches. Each round's input was one ordinary edit from the previous round's. The
repair direction changed at round 4 in response: instead of a fourth patch, one guard at the head of
each lexical context, ahead of every `$`-branch, because the rule is about the FIRST `$` and a
per-branch fix has to be repeated by whoever adds the next branch. That is how it went missing twice.

**What round 4 says about acceptance sets, stated because it is uncomfortable.** Its two new rows
turned the weaker-walk table's accept-set column from a row of passes into a row of failures: `w4`,
`w6`, `w7` and the `#` widening had all scored a clean `20/20` and now score `20/22`, because every
partial repair inherits a fabrication the merge-base already has. Four candidate implementations were
clean on that population purely because nobody had thought of the input. An acceptance set measures
the author's imagination until a reviewer extends it.

**Mechanizable:** yes, and it is NOT a new arm — the arm exists, fired, and was filtered out by the
operator's own verification command.

- **`spec:lint`'s `NUMERIC_NOUN_MISMATCH` named the round-3 stale-denominator finding two rounds
  before a reviewer did.** Re-measured against the r2 head (`83fae7f89`): the report carries
  `ADVISORY NUMERIC_NOUN_MISMATCH 200:35 "accept-set" appears with 2 distinct numbers`, pointing at
  the exact `16/17` versus `21/21` pair the finding later charged a round for. Every round of this
  arc ran `pnpm spec:lint <doc> 2>&1 | grep -E "FAIL|summary:"`, which discards the entire ADVISORY
  tier — so the gate did its job and the operator's filter hid the answer. **The generalisation:** a
  verification command that greps its instrument down to the failure tier throws away the tier the
  instrument exists to provide, and it does so invisibly, because what is left still looks like a
  clean report.

  **Honest about the arm's signal-to-noise, because "read the advisories" is only useful advice if
  the advisories are readable.** Re-run on the CURRENT spec, the numeric arm emits nine advisories
  and none is live drift: most are round-and-finding references (`round 1 finding 3`, `§1.2 row 2`)
  colliding with a noun-number heuristic, and one is a historical figure the prose explicitly
  narrates as superseded. So the arm is noisy AND it was right — at the r2 head the same arm named
  the real drift among the same kind of noise. The reading rule that follows is not "treat every
  advisory as a defect" but "read them, and reconcile each against the executable declaration",
  which for this spec is one command: `shapes.mts` prints `ROWS: 29 total = 20 + 5 + 4` and every
  denominator in the document must match it. Verified at filing time: they do.

declined: no new mechanism is warranted, because the mechanism already exists and reported
correctly. `spec:lint`'s numeric arm named this drift two rounds before a reviewer did; the defect is
in how the gate was READ — a verification command that filtered its own instrument down to the
failure tier — which is a habit rather than a missing arm. Filing a row for it would ask a future
session to build a second detector for a class the first one already detects. Recorded here instead
so the next arc greps the numeric arm by name rather than filtering to FAIL, and reconciles each
advisory against the executable declaration.

**Judgment:** four calls, each recorded in the spec so a later reader does not re-derive them.

1. **The ledger row's own remedy was declined on measurement.** The row states a construct-aware
   prototype times out at 400s against the shipped 13s, and names memoisation or a tokeniser as the
   way through. Neither is needed: the delegating walk scans the live corpus in the same time as the
   shipped one, with the digest unmoved. Both branches are declined with re-file triggers rather than
   argued away, and the row's own 400s measurement is left standing as a fact about a prototype whose
   source is not in the tree.
2. **Decline-and-surface was refused on a population count, not on cost.** The corpus holds six live
   mixed nestings, none of them a crossing, and an advisory keyed on lexical mixing would fire on all
   six — trading a silent miss with zero live population for a loud wrong answer with six.
3. **The consequence bound was SCOPED rather than defended.** Round 2 found a bash-rejected input
   where the repair reports and the merge-base is silent. The disposition turned on a measurement one
   character away: delete the stray token and the identical input parses, runs psql, and the
   merge-base is still silent — that silence was the defect coincidentally hiding an executing call,
   so no repair preserves it without preserving the defect. The bound now ranges over inputs bash
   accepts, with a third population recording the rejected ones' movement in both directions.
4. **A widening that improves bash fidelity is still refused.** The `#`-comment rule makes the walk
   agree with bash where today it does not, and it is planted as a DEFECT in the probe's own
   self-test, because it is the parser growth the design fences. A fence that only rejects mistakes
   is easy; this one rejects an improvement.

**Round 5's confirmation is worth more than its verdict**, because the reviewer re-derived the kill
attribution instead of accepting it: with neither guard both new rows fabricate; the bare-only guard
fixes `P4` and not `P5`; the double-quote-only guard fixes `P5` and not `P4`; both together silence
both, with `P3` still reporting its one real non-nested site. That is the two rows proven to
discriminate INDEPENDENTLY, on the axis the design claims for them, by someone who did not write
them. It also independently re-ran the recognizer inventory and found no fourth — confirming
`SUBSTITUTION_OPENER` is a downstream advisory predicate and `acceptedExpansionOperand` receives an
already-delimited span, so neither performs delimiter recognition.

**Infra:** the reviewer's sandbox was read-only in rounds 3, 4 and 5, which denied `shapes.mts` its
`node_modules/.cache` baseline write and `syntax-error-class.mts` its `mkdtemp`. Both aborted before
evaluating any row, and the reviewer correctly declined to count that as an artifact finding. Round 4's
brief names the condition explicitly so the next reader does not diagnose it as a broken probe. In
round 5 tsx's temporary IPC socket was denied as well, and the reviewer fell back to no-write Bash
and direct scanner probes and said so. No dispatch was reaped and no round returned `no_verdict`.

## plan — 5 rounds

**Examined:** every plan-stage row of `50ca72a566b0.jsonl` with `status: "verdict"`, plus the
reviewer message in each round's `--out` directory. The table below is DERIVED from that corpus by
`scripts/`-free one-liner at filing time, not from a running tally — the spec stage's own filing says
the same thing, and this arc has now been bitten twice by numbers typed from memory.


| round | verdict | findings |
| --- | --- | --- |
| 1 | BLOCKING | 9 |
| 2 | BLOCKING | 4 |
| 3 | BLOCKING | 3 |
| 4 | BLOCKING | 1 |
| 5 | APPROVE | 0 |

**Seventeen findings, every one accepted as real and ZERO refuted** — the same clean sheet the spec
stage recorded, and the same warning sign: a stage where nothing is refutable is a stage where the
author is not being careful enough before dispatching. The finding rate DID decay, 9 → 4 → 3 → 1 → 0,
which the spec stage's did not.

**The stage reached its cap at BLOCKING and converged on a SCOPED terminal round** — the same shape
the spec stage used, and the second time this arc has needed it. Round 4's single finding was the
second half of round 3's finding 1: round 3 repaired the global constraint and the acceptance table
to say AC-7 is the FULL mutation gate, and left Task 6's own marker and body still calling the scoped
iteration run AC-7. The repair was a five-line completion of a decision already ratified in round 3,
not a new axis, and the reviewer stated explicitly that its own sweep of AC-1 through AC-8 found no
peer mismatch. bl-orch granted a scoped round 5 (`CONVERGENCE_ACK`) bounded to exactly that repair;
it returned APPROVE with zero findings, confirming that Task 6 now executes AC-7's exact command and
that the marker's cycle still holds.

**Both stages of this arc closed the same way, and that is the pattern worth naming.** Spec: four
rounds to a terminal BLOCKING, then a bounded fifth. Plan: the same. In both cases the cap round's
finding was a completion of a decision the previous round had already ratified rather than a new
axis — the signature of a stage that is DONE and a repair that reached two of its three sites. The
cheaper move, both times, would have been to sweep the ratified decision to every site in the round
that ratified it.

**Mechanizable:** declined: no ledger row. The dominant class was CLOSED IN-ARC rather than deferred — the unproduced-claim findings are all fixed, and `weaker-walks.mts` is the arm they wanted. The one residual candidate, a marker-property check, needs a dataflow analysis over prose that no current arm carries, and under the ledger filing bar (AGENTS.md, 2026-08-04) an entry filed now would be unprobed; the probe that would settle it is a frequency count of the defect shape across the plans corpus, and that probe, not an entry, is the first step if anyone picks it up. Detail below, one class wearing two hats.

**A CLAIM THE PLAN MADE THAT NO COMMAND PRODUCED** accounts for 4 of round 1's 9 and 1 of round 3's 3.
Round 1: the weaker-walk kill table contradicting its own scores, a hand-built fixture list wrong in
BOTH directions at once (it omitted the two rows that independently kill a missing `lexShellWords`
guard and included a row the probe classifies as bash-rejected), five mutant proofs where one exists,
and a meta-test inventory naming work no task performed. Round 3: a slowdown ratio asserted without
the recipe that produces it — filed against the round-1 repair, by the author who had just written
the rule.

All five closed the same way, by turning the claim into a command. That transformation was available
at authoring time and nobody reached for it: the weaker walks already existed in an untracked build
cache, so the table was ONE SCRIPT away from being reproducible for four rounds. `weaker-walks.mts`
is that script; the fixture population now comes from an `rg` over the probe's own row statuses.
**A table nobody can re-run cannot be caught being wrong**, which is exactly how the contamination
survived: every walk had been built from whatever the prototype was on the day, so the ones predating
spec round 4 carried TWO deviations rather than one, and every score in the table was depressed and
every attribution wrong.

**Mechanizable, and worth filing as a candidate arm:** a marker needs THREE properties — the command
fails today, the green half runs the SAME command, and THIS task creates the failing case.
`spec:lint`'s red-contract arm checks the first (`--exec-red` runs live-declared markers) and the
citation arms check the target resolves. **Nothing checks the second or the third, and each cost a
round on a different task** — round 1 finding 1 was property two on Task 5, round 2 finding 1 was
property three on Task 5 after that repair, and round 1 finding 2 was property three on Task 6. An
arm that flagged a `red-state=live` marker whose failing condition is removed by an EARLIER task in
the same plan would have caught round 2 finding 1 statically. Filed as a candidate, not a commitment:
the analysis needs task order plus a model of what each task changes, which is more than the current
arms carry.

**Why the marker-property arm is declined rather than filed.** The measured cost is three rounds on
ONE arc, and the cheaper mitigation already shipped: the three-property rule is now stated in the plan
in transferable terms, which is what the next author actually reads. Under the process-facing mint
bar it would also add to a queue that is already majority process-facing.

**Judgment:** three things a static arm would not have found.

The ORDERING finding (round 2, finding 3) — a mutation score taken before seven later comment edits,
so AC-7 did not describe the final bytes. It needs a model of which artifacts each task perturbs and
which measurements depend on them. A reviewer reading task order finds it cheaply.

The TASK 5 DISPOSITION (round 2 finding 1, round 3 finding 2) is judgment of the useful kind, and the
right call was NOT to manufacture a red. Probing showed no in-scope mutant can red either of its
gates: all nine named mutants leave the live-corpus digest at `8ebe8b08…` because the live population
of the crossing is zero, and a plausible span-quadratic slowdown lands at 1.21× against a 1.5× bound.
The close was a declared measurement task outside the red-contract region with the evidence attached.
**A round spent establishing that a gate CANNOT be red is a round well spent** — what it bought was
not writing a fabricated slowdown mutant that would have proved the gate's arithmetic and nothing
about this repair.

The ACCEPTANCE-CHANNEL class (round 1 finding 6, round 2 finding 2, round 3 finding 1) recurred three
times in three different shapes: a criterion missing outright (`AC-3b`), a criterion naming a command
no task ran (AC-5), and a criterion naming a command that is not the one the task executes (AC-7,
where `pnpm mutation:guards` hard-codes four shards and matches no `shardTmp` file). Three rounds on
one axis. The mechanizable half is thin — checking that an `ac=` id appears in the acceptance table is
already linted — but checking that the NAMED COMMAND is the one the task body runs is a prose-to-prose
correspondence no arm reads today.

**Infra:** none. No round was lost to a wedge, a reap, or a `no_verdict`; every dispatch returned a
verdict on its first attempt. The one infra-adjacent cost was self-inflicted and cheap: `spec:lint`
caught four hard citation failures in round 3's own repair text, which is the arm working.

**What the sweep rule cost, stated plainly — this is the transferable part.** Two of the three later
rounds opened with a PEER of a finding the previous round had already closed. Round 2's finding 1 was
the broken-cycle class on Task 5 after round 1 fixed it on Task 6; round 3's finding 2 was the
unproduced-claim class on the round-2 repair itself. Both times the sweep ran against the INSTANCE's
wording rather than the finding's SHAPE: "red and green are the same command" instead of "the marker
describes a cycle this task performs", and "the table is produced" instead of "every number is". Two
rounds, chargeable to the sweep and not to the reviewer. The rule already says to sweep the shape;
what this arc adds is that **the shape has to be restated in more general terms than the finding
that surfaced it, before the sweep runs** — the reviewer's wording is the instance, not the class.
