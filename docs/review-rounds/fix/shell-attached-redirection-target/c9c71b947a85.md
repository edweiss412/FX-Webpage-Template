# Review-round economy — `fix/shell-attached-redirection-target` at base `c9c71b947a85`

The plan and spec stages were filed against the previous base and live in the sibling
`e5d1d723d69c.md`. This record covers the DIFF stage only.

## diff — 4 rounds

**Examined:** the five code files (`tests/cross-cutting/psqlStartupFiles/scan.ts`, the deciding
suite, `tests/mutation/source/registry.ts`, `expectedLedgerKinds.ts`, and the ledger closeout
script) and the documentation set (spec, plan, handover, probe scripts, the two ledger files and
this corpus), dispatched as TWO tight-scope briefs per round rather than one whole-diff review,
because the diff is 23 files and 4213 insertions. Rounds: code r1 BLOCKING/2, docs r1 BLOCKING/8,
docs r2 NEEDS-ATTENTION/4, code r2 BLOCKING/2, docs r3 NEEDS-ATTENTION/6, code r3 BLOCKING/1,
docs r4 NEEDS-ATTENTION/3. Zero findings refuted across every round; one disposed by filing
rather than repair. **Round 3 of the code scope found a REGRESSION this arc introduced that four
earlier rounds and a 1.00 mutation score all missed** - `$((...))` arithmetic read as a `$()`
command substitution, producing a resolved site for a command bash never runs. That is the
jurisdiction line drawn in one example: the score settles whether the suite pins what is WRITTEN,
and only domain probes settle whether what is written covers the domain.

**Mechanizable:** yes, filed as `BL-MUTATION-REGISTRY-KEYS-STALE-AFTER-ANY-SOURCE-EDIT`, and it is the arc's most transferable output. **Any edit to a line-keyed
source file invalidates its mutation-registry keys, INCLUDING a comment or prose edit.** That cost
two rounds in different disguises: code r2 found 28 of 30 rows stale after a behavioural fix, and
the identical failure recurred through the comment-only route while repairing docs r3, where all 30
went stale. Both times the weak link was re-deriving the post-fix check set from memory instead of
from the blast radius of the edit. A gate that runs `pnpm mutation:sites` unconditionally after any
touch to a registered surface file would have caught both, and neither needs judgment to detect.

**Judgment:** the disposition of the pre-existing cross-construct brace-matcher defect. The
reviewer reported it as wrong attribution; reproduction found a silent miss underneath it, and the
same inputs run against the base on the detached path proved it pre-existing rather than introduced.
Filing it rested on a measurement that could not be delegated to a rule: a construct-aware prototype
fixes all four shapes and then times out over the live corpus at 400s against the shipped walk's
13s, so its digest-stability is currently unprovable. That is `BL-SHELL-BRACE-MATCHER-CROSS-CONSTRUCT-BLIND`.

The other judgment call was narrowing rather than widening. Docs r3 found the arc's central claim
stated at seventeen sites as a universal ("every undelimitable attached target is REPORTED") while
the code fires only on a span carrying a substitution opener. The narrowing was deliberate and had
already prevented nine false advisories on live JS template literals; the repair was to bring the
prose down to the code and record the quiet case as a documented limit in the owning surface's own
limits block, not to widen the recognizer to match the prose.

**Infra:** two dispatch-boundary costs, both cheap to avoid once known. The round-1 code dispatch
was refused (exit 2, no `result.json`) because the `GUARD SURFACE:` line carried a `(1.00)`
parenthetical between the ratio and the survivor clause, which the wrapper's separator class does
not admit; the refusal echoes the line truncated at an em-dash, which points at the wrong culprit.
Reading the regex out of `scripts/codex-guard.mjs` and testing both spellings against it settled
it in one attempt. Separately, GitHub REST was quota-exhausted for part of the window, so
`gh run list` returned empty; an empty read there is a dead read and says nothing about CI, and the
`Stop`-hook alarm with empty run/summary/logs is unsupported rather than a failure to investigate.
