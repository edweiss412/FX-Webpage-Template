# Review-round economy — `fix/shell-attached-redirection-target` @ `e5d1d723d69c`

## spec — 4 rounds

**Examined:** every finding across the spec stage, classified by what it was AGAINST — the
DELIVERABLE (the design in §3/§3.1, and the acceptance gate in §2.2/§4/§5 that decides which
implementations the spec ratifies) or the SCAFFOLDING this arc itself added (probe record, census
mechanics, digest, pin inventory).

| round | verdict | findings | against DELIVERABLE | against scaffolding |
|---|---|---|---|---|
| 1 | BLOCKING | 6 | **4** — case F unreachable by the design; the unlexable channel named nothing; a brace inside a quoted target silent; an escaped backtick closed a span early | 2 — stale pin inventory, probes that could not fail |
| 2 | BLOCKING | 3 | **0** | 3 — census keyed on the regex under repair, digest blind to the fields §5 forbids moving, stale probe record |
| 3 | BLOCKING | 3 | **0** | 3 — census blind to multiline targets, digest collapsing `undefined` into `null`, one stale census number |
| 4 | BLOCKING | 4 | **4** — the declared probe domain named a surface production cannot read; the acceptance set was same-line-only; no case crossed the file-descriptor-prefix axis; the attribution predicate was existential | 0 |

**The headline is that 0 → 0 was not convergence.** After two consecutive zero-deliverable rounds
the stage was dispositioned closed at three, on the reasoning that the remaining subject was the
arc's own instruments and TDD re-runs instruments. That ruling was reversed and a terminal fourth
round dispatched, and it returned FOUR deliverable findings — the highest deliverable count of the
whole stage. Each one lets a WEAKER implementation pass the spec's declared gate: a same-line-only
delimiter satisfies AC-1, AC-5 and AC-8 while staying silent on a continuation bash executes; an
implementation that gives up when a file-descriptor digit precedes the operator passes everything;
and a repair that adds a correct attribution record while leaving the wrong one satisfies case I.

**What the reversal turned on, and it is the transferable part.** Implementation TDD does not
re-verify this class. The recurring defect across all four rounds is *a control blind along an axis
its author never varied*, and an executable red INHERITS its author's unvaried axes — the red runs
green and proves the same nothing the prose did. Round 3 is the direct evidence: its multiline
finding was repaired in the census and NOT in the acceptance set, so the identical defect sat one
layer along for a full round until fresh eyes crossed the axis again. Only a reviewer who did not
write the controls varies a dimension the author never considered.

**Stage outcome: DISPOSITIONED, not CONVERGED.** There is no APPROVE row. The last verdict is round
4, BLOCKING, four findings, all accepted and repaired; those repairs are unreviewed by any
reviewer. Round 4 was declared TERMINAL in its own brief, so the stage closes on its verdict either
way, and the disposition of a terminal round's findings is the orchestrator's. Recorded here and in
the handover so a later reader never mistakes it for convergence.

**Mechanizable:** none outstanding. Every instrument defect from rounds 2, 3 and 4 ships with a
committed executable cover rather than a filing — `digest-sensitivity.mts` proves the digest
discriminates across seven perturbations including `null`→`undefined` and `null`→ABSENT;
`corpus-family3.mts` carries thirteen controls, three multiline and one file-descriptor-prefixed,
and ASSERTS its zero; `probe-attached.mts` carries fifteen cases and fails in both directions;
`oracle.mts` proves all twelve snippets execute. What is NOT mechanizable is the axis-crossing
itself: no gate can enumerate the dimensions its author did not think of.

**Judgment:** four calls were judgment rather than mechanism, each recorded in the spec so a later
reader does not re-derive them. (1) The acceptance set GREW from six spellings to eleven, the
ratchet direction — taken because each addition is one ordinary edit inside the declared PROBE
DOMAIN and closes a named gap. **Provenance, stated precisely rather than rounded up:** G, H, J and
K each witness a specific review finding; **case I was found by the author** while repairing round
2's stale probe record, not raised by a reviewer. Saying "every addition witnesses a review
finding" overstated it, and the trail review caught that, and because plan §2b shows a corpus of A–F alone
certifies the wrong implementation. (2) The round-1 escaped-backtick fixture was replaced rather
than repaired after the oracle measured it at `executions=0`. (3) Round 4's package-script finding
was repaired by NARROWING the declared domain to what production reads, not by widening the
scanner to open `.json` — the direction AGENTS.md prescribes under same-axis recurrence, and the
scanner has recorded that surface as invisible since 2026-08-03. A census population and a probe
domain are different sets, and conflating them is what produced the finding. (4) The stage closed
on a disposition rather than an APPROVE.

**Infra:** the `codex_models_manager` cache-TTL error (`missing field base_instructions`) appeared
on every dispatch and cost nothing — the wrapper's retry rung absorbed it. Recorded so a later
reader does not diagnose it as a dispatch failure. A reviewer under a read-only sandbox could not
create the IPC socket `pnpm exec` needs and ran the probes as `node --import tsx`; same results, and
the oracle rerun was denied its temp directory for the same reason at rounds 3 and 4, which the
reviewer correctly declined to count as an artifact finding.

---

## plan — 4 rounds

**Examined:** every plan-stage finding, classified by DEFECT SHAPE rather than by which task it
landed in, because the shape is what recurred and the task is not.

| round | verdict | findings | blind-axis shape | other |
|---|---|---|---|---|
| 1 | BLOCKING | 7 | 4 | 3 — a gate cited without its gating flag, an unmapped AC, an accepted-then-reversed invariant claim |
| 2 | NEEDS-ATTENTION | 5 | 3 | 2 — a third count conflict, an incomplete documentation sweep |
| 3 | BLOCKING | 5 | 5 | 0 |
| 4 (terminal) | BLOCKING | 2 | 2 | 0 |

**Eighteen of nineteen findings share one shape: a control blind along an axis its author never
varied.** No implementation exists and `scan.ts` is unchanged from base, so every finding landed on
the plan's own test design. The rate held flat for three rounds and then halved: 7, 5, 5, 2. Zero were refuted.

**The rate held flat for three rounds because repairs generated the next round's findings.** Two of round 3's
five were defects in controls added at round 2 to close other axes: the multi-target control put
`true` in the first target and `psql` in the last, so an implementation keeping only the LAST
target's bodies passed it; and J pinned a one-line displacement, which `line = operatorLine + 1`
satisfies by construction. Round 2's own finding 4 was the same story one level up — the
sibling-body repair from round 1 closed bodies-per-target and left targets-per-chunk open. **A
repair is the next round's most likely defect, and this stage is the measurement.**

**Mechanizable:** none. Two of the three non-shape findings had a mechanical flavour — a command
cited as a gate that cannot fail (`baseline-corpus.mts` named as AC-5's gate without `--expect`;
bare, it exits 0 on any digest), and an AC present only in the AC map (AC-5b mapped but never
executed). **Both were found and closed inside this arc by sweeping the first instance, not by a
guard**, and the sweep turned up a second instance of each that review had missed:
`probe-attached.mts` cited bare everywhere, and AC-7 in the map alone. Filing two process-facing
lint arms for defects already repaired would mint queue mass against the minority class while the
DOMINANT class — eighteen of nineteen findings — stays unmechanizable by construction, since no gate
can enumerate the dimensions its author did not think of. The transferable output is the sweep
procedure recorded in plan §2a-bis, not a lint arm.

**What changed at round 4, and it is the transferable result.** Rounds 1-3 repaired each blind axis
with a FIXTURE, and each fixture left the next positional heuristic alive: anchor to the operator's
line, then `operatorLine + 1`, then the target's FINAL line, then the scanner's current line at EOF.
Round 4 found the last two. The repair is no longer a fixture but a DERIVED RULE — every coordinate
assertion chooses inputs so the asserted line differs from every other candidate line in that
fixture (operator line, target start, target end, EOF), which kills the whole family including
heuristics nobody has named. That is the difference between the rate halving and the rate holding,
and it took four rounds to stop reaching for another fixture.

**Judgment:** five calls. (1) Axes were closed by DERIVATION rather than by more fixtures wherever
an external authority existed — the operator axis iterates the shipped `REDIRECTION_OPERATORS`
array, the unlexable openers derive from §3.1's accept-set table, and the population axes assert
COUNT EQUALITY rather than exhibiting a witness. The acceptance set had already ratcheted 6 → 9 →
11, and an equality covers an added case where another fixture would not. (2) W11 ships with NO
killer in the acceptance set, stated rather than hidden. (3) Round 1 finding 7 was accepted and the
step reversed, then reversed BACK: a batch-wide ratification supersedes the AGENTS.md wording it
cited, and a sibling arc had refuted the identical finding. The round-economy rules charge a forced
change later undone to the ACCEPTANCE, and the real defect was that the plan never cited the
ratification — an uncited inheritance is indistinguishable from an unratified choice. Step 5 now
fences it in both directions. (4) Round 4 was declared TERMINAL at the threshold rather than run open-ended. (5) Its two findings
were repaired as a DERIVATION rather than as two more fixtures, which is what rounds 1-3 should have
done and is the reason this filing exists.

**Infra:** round 1 spent a whole extra Codex attempt because the reviewer declared `FINDINGS: 3`
inside a ```text fence; the wrapper elides fenced blocks, recorded `no_marker`, and retried. The
retry declared 7 — and this session read `attempt-1.last-message.txt` instead of `result.json` and
acted on the wrong count for a full repair cycle. Every brief since carries an instruction to
declare findings outside a fence; the durable fix is to read `result.json`, and only once it exists.
Round 3 was KILLED mid-flight when its subject became false under it (the Step 5 reversal); its row
records `no_verdict` and correctly does not count. Separately, **`pnpm typecheck` was RED on this
branch from the spec stage and survived twelve commits and three adversarial rounds** — reviewers
run sandboxed and read-only, so they do not run the gates. It was found by running one.
