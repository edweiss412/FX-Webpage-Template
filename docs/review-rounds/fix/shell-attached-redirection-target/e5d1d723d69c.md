# Review-round economy — `fix/shell-attached-redirection-target` @ `e5d1d723d69c`

## spec — 3 rounds

**Examined:** every finding across the spec stage, classified by what it was AGAINST — the
DELIVERABLE (the design in §3/§3.1) or the SCAFFOLDING this arc itself added (probes, census,
digest, probe record, pin inventory). Filed at three rounds rather than at the four-round
threshold, because the stage CLOSED here on an orchestrator disposition and the split is the
reason it closed.

| round | verdict | findings | against DELIVERABLE | against scaffolding |
|---|---|---|---|---|
| 1 | BLOCKING | 6 | **4** — case F unreachable by the design; the unlexable channel named nothing; a brace inside a quoted target silent; an escaped backtick closed a span early | 2 — stale pin inventory, probes that could not fail |
| 2 | BLOCKING | 3 | **0** | 3 — census keyed on the regex under repair, digest blind to the fields §5 forbids moving, stale probe record |
| 3 | BLOCKING | 3 | **0** | 3 — census blind to multiline targets, digest collapsing `undefined` into `null`, one stale census number |

**The deliverable rate went 4 → 0 → 0.** The design stopped moving after round 1 and every finding
since was against an instrument this arc introduced. Two consecutive zero-deliverable rounds is the
review-changed-subject signal held twice, which is what closed the stage.

**Stage outcome: DISPOSITIONED, not CONVERGED.** There is no APPROVE row. The last verdict is round
3, BLOCKING, and the repairs made after it are unreviewed by any reviewer. That is an orchestrator
ruling, recorded here and in the handover so a later reader never mistakes it for convergence.
What cleared it instead of a fourth round: the remaining subject is the arc's own instruments, and
every one of them becomes an EXECUTABLE gate at implementation — each census control, the digest
and each probe is proven red-in-both-directions by a task rather than read as prose. A fourth round
would have spent a dispatch reading about instruments that TDD runs.

**Mechanizable:** none. Both round-2 and both round-3 instrument defects ship with committed
executable covers rather than filings — `digest-sensitivity.mts` proves the digest discriminates
across seven perturbations including `null`→`undefined` and `null`→ABSENT, and `corpus-family3.mts`
carries twelve controls including three multiline ones and ASSERTS its zero. Nothing is left to
schedule.

**Judgment:** three calls were judgment rather than mechanism, each recorded in the spec so a later
reader does not re-derive them. (1) The acceptance set GREW from six spellings to nine, the ratchet
direction — taken because G, H and I are witnesses to review findings drawn from one ordinary edit
inside the declared PROBE DOMAIN, and because plan §2b shows a corpus of A–F alone certifies the
wrong implementation. (2) The round-1 escaped-backtick fixture was replaced rather than repaired
after the oracle measured it at `executions=0`. (3) The stage closed on a disposition rather than an
APPROVE, per the split above.

**Infra:** the `codex_models_manager` cache-TTL error (`missing field base_instructions`) appeared
on every dispatch and cost nothing — the wrapper's retry rung absorbed it. Recorded so a later
reader does not diagnose it as a dispatch failure. A reviewer under a read-only sandbox could not
create the IPC socket `pnpm exec` needs and ran the probes as `node --import tsx`; same results, and
round 3's oracle rerun was denied its temp directory for the same reason, which the reviewer
correctly declined to count as an artifact finding.
