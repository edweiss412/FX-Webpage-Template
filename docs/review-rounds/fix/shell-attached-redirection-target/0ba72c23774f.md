# Review-round economy — `fix/shell-attached-redirection-target` at base `0ba72c23774f`

**This base is a continuation, not a new arc.** The branch merged `origin/main` mid-review, which
moves `git merge-base` and therefore the corpus file the wrapper writes to. The diff stage's first
five rounds are recorded at the previous base in `c9c71b947a85.md`, and the spec and plan stages at
`e5d1d723d69c.md`. Read all three together.

The two rounds here were dispatched carrying numbers 5 and 6 from the previous base's sequence and
are renumbered 1 and 2, which is what they are at THIS base; each row keeps its original number in
`_roundAtPreviousBase` so the dispatch history stays traceable. Nine diff rounds have run across the
two bases.

## diff — 2 rounds

**Examined:** the same two tight scopes as at the previous base — the five code files and the
documentation set — re-reviewed after the base settled. code r1-here NEEDS-ATTENTION/1, docs
r1-here NEEDS-ATTENTION/2. **No BLOCKING verdict at this base**, the first time in the arc that both
scopes came back below BLOCKING together.

**Judgment:** the code finding is REFUTED by measurement and the refutation is the interesting part.
It reports that quoted workflow `run:` scalars misbehave — single-quoted silently unsignaled,
double-quoted producing a fabricated `PsqlSite` for a command bash never runs. Both were verified
against `scan.ts` at the merge-base: **byte-identical at base and HEAD**, so both are PRE-EXISTING
on the YAML decode path rather than introduced here. On the third spelling, a plain scalar, this arc
strictly IMPROVES matters: base is silent where HEAD emits the advisory. A finding whose two failing
directions predate the diff and whose third direction the diff repairs is not a finding against the
diff.

**Mechanizable:** none new. The two documentation findings were a stale plan cardinality and an
in-branch operator count, and the second produced a rule rather than a repair: two honest
enumerations of the same branch disagreed because the count depends on where the branch is cut, with
an ordinary `$()` arm sitting immediately after it. A number whose value depends on an arbitrary
boundary is not a measurement. The row now states the predicate that survives the boundary choice —
which operators REACH the branch — plus each operator's file-wide cost, which is enumerated over the
whole file and has no boundary to choose.

**Infra:** the merge itself is the entry. It was taken on the DIRTY limb with an EMPTY closure
intersection — main's 23 files touched none of the arc's 8 stamped inputs — which is the measured
proof that closure debt and merge debt are independent limbs and both must be checked. Its
side effect is this file: a mid-review merge splits the arc's corpus across two bases, and the
economy gate reads each file as its own contiguous `1..N` sequence.
