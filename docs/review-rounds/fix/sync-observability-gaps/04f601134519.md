# Review-round economy — `fix/sync-observability-gaps` @ `04f601134519`

Spec: `docs/superpowers/specs/observability/2026-08-14-sync-observability-gaps-design.md`

## spec — 9 rounds

Findings 8, 5, 2, 4, 3, 3, 2, 1, 0 — APPROVE at round 9 (round 3 also has one
interrupted-dispatch row — the orchestrator's own foreground timeout SIGTERM'd the
wrapper, not a Codex fault — which counts as no round). Verdicts: BLOCKING,
NEEDS-ATTENTION, BLOCKING, BLOCKING, NEEDS-ATTENTION ×4, APPROVE; repairs landed
per round in commits `f980a769b`, `970cfdba7`, `b1948da37`, `0f61bed5f`,
`eafe8fd21`, `06b663f4b`+`ce94ccc49`, `2161bb063`, `7b8932b60`. After R8 the
orchestrator ordered the drip stopped: findings triaged against the admissibility
contract, every twice-raised vector fenced in the round-9 brief with citations, and
the two dominant classes closed by derivation (the census as a pasted run-sweep;
the thrown-runner surfaces as a closure table over §3's surfaces). Round 9 then
converged at zero under the fences.

**Examined:** the spec against the live sync pipeline — `runManualStageForFirstSeen`,
`runManualSyncForShow` (locked + `_unlocked`), `processOneFile_unlocked` /
`prepareProcessOneFile` / `handleFetchFailure_unlocked` in `runScheduledCronSync.ts`,
`applyStaged.ts`'s tail and post-commit region, the pending-ingestion retry route,
both live staged-apply routes, and the existing test corpus around them.

**Mechanizable:** the dominant round-driver (R1 F1, R4 F3 — two rounds on one
function) was "a terminal branch with no emission", found branch-at-a-time by
reading. The spec's own repair is the mechanization: the §5 input-partition matrix
over `handleFetchFailure_unlocked` (code class × show × existing-pending) makes the
class executable, and a future arm fails the matrix instead of waiting for a
reviewer. The second mechanizable class is the existing-test impact census (R3 F2,
R4 F4): "which tests reach this production default without injecting the seam" is a
two-grep cross-check (`applyStaged\(` vs `logSync\s*:`) the plan now runs and pastes
rather than asserting from memory.

**Judgment:** the emission-topology findings (R2 F1 terminal-value-vs-input,
R3 F1 which wrapper owns the catch, R4 F2 seam-type double-emission) needed the
reviewer to hold the tx-boundary model of three modules at once — genuine
cross-file design review, not a checkable pattern. The R1 F2/F6 catch-semantics
family (dedupe, substitution) is the same: the defect was in sequencing the spec
prescribed, not in any claim a grep could refute.

**Infra:** one dispatch lost to the orchestrator running codex-guard in a
foreground Bash call with a 10-minute cap — the timeout SIGTERM'd the wrapper
mid-review (`failureReason: "interrupted"`). Dispatch backgrounded thereafter;
no wrapper defect.

## plan — 5 rounds

Findings 12, 4, 1, 2, 0 — APPROVE at round 5. The plan inherited a settled spec,
so every finding was an EXECUTABILITY defect, none a design one: an injection
point that no-ops (the null-prior quality-regression seam), TS1016 on
required-after-optional at all three widened seams, a `never`-check default that
does not terminate control flow, a live case that 409s on an unseeded
`app_settings.watched_folder_id`, a unit assertion on a field `SyncLogEntry`
does not carry, stale counterparts left by a repair, and commit steps without
staging.

**Examined:** the plan's nine (then eight) tasks against the live signatures,
unions, route seams, test corpus, and DB conventions it names.

**Mechanizable:** two shapes. (1) Compiler-checkable snippet defects (TS1016,
non-terminating `never` default, the dropped required property in a cast) are
exactly what the writing-plans "typecheck pasted snippets" pass exists for —
the pass was run against claims, not against the literal snippet text; running
`tsc` on extracted fenced blocks would have caught all three pre-dispatch.
(2) Cross-reference staleness after a repair (Files section vs Step text
naming different homes for one test) is a same-document grep the self-review
sweep should include: every filename mentioned twice must resolve to one
disposition.

**Judgment:** the TDD-ordering findings — a GREEN that depends on a later
task's production change, a close-out with no observable red — needed the
reviewer to simulate execution order across tasks, which no static check does.

**Infra:** none; all five dispatches clean.
