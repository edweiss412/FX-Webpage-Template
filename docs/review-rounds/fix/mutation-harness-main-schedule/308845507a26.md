## plan — 4 rounds

**Examined:** the single plan document for this arc, `docs/superpowers/plans/ci/2026-09-01-mutation-harness-main-schedule.md`, across four dispatches at one base (`308845507a26`); no base moved during the stage, so the cross-base total is also 4. Verdicts BLOCKING/6, BLOCKING/5, BLOCKING/4, BLOCKING/1 — the finding count decayed 6, 5, 4, 1 and round 4 verified every round-3 repair against the artifacts and the live tree. Every finding of every round was accepted; none was disputed or refuted.

**Mechanizable:** **retyped numbers, and it was the whole of round 3.** All four of that round's findings were one defect wearing four hats: a figure produced by a probe, transcribed into prose, and then stale or simply wrong. A cost that subtracted a PART from a MAKESPAN and so compared nothing (176 s where makespan-against-makespan is 157 s); two rates divided from child totals already rounded to whole seconds, costing a millisecond per boot; a median taken as the fifth of eight values rather than the mean of the middle pair; and, in rounds 1 and 2, six further stale numbers left behind by my own repair of the round before, two of them inside the machine-readable task marker. Round 2's finding 1 and round 3's findings 1-3 are the same class, and the class did not close by being repaired twice.

It closed when it stopped being prose. `derive-numbers.ts` now emits every load-bearing figure from the artifacts and the live registry — per-leg elapsed and child seconds, the sorted overheads and their median computed the way `lib/mutationWeight/weights.ts:16` computes one, the corpus child total, both split parts' mutant counts, boots, unrounded milliseconds and rounded rates, and the makespan, percentage, elapsed and headroom for both candidate splits at N=8 through 12. It carries zero retyped measurements: the REJECTED split's numbers are derived from its operator sets rather than copied forward, and doing so independently reproduced the 2333.122 s that round 3 corrected me to. `SHARD_BUDGET_SECONDS` is imported rather than typed. From here the plan and every brief cite the command and paste its output, and a figure appearing in prose but not in that output is a figure nothing derives.

declined: already mechanized in this branch — the emitter and its immutable measurement anchor ship in this arc, and they ARE the disposition, so no row remains to schedule. The process mint freeze independently forbids this arc filing a row of any facing.

**Judgment:** two calls that no mechanism would have made, and both changed the shipping design.

The first was the split itself. Round 1 and 2 both cleared the operator-balanced split on its arithmetic; what rejected it was running the plan's own class-sweep instruction, which showed that 2 of 8 rationale banners straddled it and that the two were a chain — the second banner's text refers back to the first. Splitting there would have left accepted rows under an antecedent no longer in front of them. The family-preserving split costs 157 s of makespan and keeps every rationale intact. No reviewer asked for that trade; the sweep produced it.

The second was declining to repair the `source-shards` failure in this arc. The tempting reading was that it is downstream of the budget breach — a starved child scored as a dead overlay. The assertion reads the child's exit code, and a timeout returns non-zero while a signal kill throws, so neither shape produces the observed `expected +0 not to be +0`. The refutation is recorded in the plan so the next reader does not re-derive it, and the defect is left to its own arc rather than bundled into the one clearing sixteen nights of red.

**Infra:** none. Four dispatches, four verdicts, no `no_verdict`, no reaper interference, no wrapper faults.

**Round 4's single finding, and the sweep it prompted:** the drift history was counted as five incidents in one place and six in another, because the still-open instance was counted among the already-re-blessed ones and then again as the new work. Five incidents, four re-blessed, this arc completing the fifth. Sweeping the same over-claim one level down found a second: the plan said all five "sit in `BACKLOG-archive.md`", which is true of three — the 08-16 waldorf instance carries no row and is attributed from the run log and the fixture diff. The review named four count phrases; the sweep repaired five statements.

**What the rounds did not touch, which is the number worth carrying forward:** zero findings across four rounds against the actual repair — the recalibration, the split's completeness, the shard count, or the `sourcePath` consumer sweep. Round 2 and round 3 each said so in as many words. The rounds were spent on the document describing the change, not on the change.
