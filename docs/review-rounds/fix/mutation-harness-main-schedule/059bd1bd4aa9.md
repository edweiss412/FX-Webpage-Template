## diff — 2 rounds

**Examined:** the whole arc diff, reviewed in two scoped dispatches per round because a 24-file diff is past this repo's threshold for a whole-diff dispatch. Two rounds at this base (`059bd1bd4aa9`, arc rounds 3 and 4); the cross-base total for the stage is **4** — rounds 1 and 2 sat at `c87b2001f4aa` before `origin/main` was absorbed, and the arc-sum rule counts distinct `(baseSha, round)` pairs. Verdicts by arc round: R1 scope A BLOCKING/2 + scope B BLOCKING/3; R2 A BLOCKING/1 + B BLOCKING/3; R3 A **APPROVE/0** + B BLOCKING/1; R4 scope B alone BLOCKING/1, scope A having approved on a file list the R3 repair did not touch. Every finding of every round was accepted; none was disputed or refuted.

**The cap, and what replaced round 5.** Round 4 landed the stage on its cap with a live BLOCKING finding. bl-orch's ruling (2026-09-01 06:05 CDT): repair authorized, **round 5 refused, an oracle replaces it**, per the finalize and diagtile precedents. The reasoning is specific to this finding rather than general impatience — the finding arrived already mutant-proved, so its closure is mechanical and a fifth prose round would re-derive what a mutant kill demonstrates. The conditions attached: the binding must be a reconciliation the validator refuses on, all seven demonstrated mutants must become permanent deciding-suite cases killed at the shipping head, and this filing must say so on the record. It does. The standing order for the class: any later check of the same shape comes to bl-orch before a repair.

**Mechanizable:** **the measurement anchor's completeness, and it took four rounds because each repair closed a hole instead of the class.**

The shape repeated exactly. Round 1: an absent operator cost was priced at zero, so deleting a measured operator changed every figure silently. Round 2: the repair covered absence and finiteness, and a sweep around it found five more — a deleted leg, a deleted `surfaceMs` row, a deleted `rates` row, a deleted prior-run leg, and a cost of zero or below, all exiting 0 with a rendered number. Round 3: the repair covered those, and four more arrived — both run-date removals, equal dates, an absent `priorRun.surfaces`, and a single wrong digit in an operator cost that produced figures which were plausible and wrong. Round 4: a single wrong digit in a RATE, which no check on that table could see at all.

Three things closed it, and only the third ends the sequence.

The first is a real invariant rather than another field check, and the round-3 reviewer named it while reporting the finding: the operators PARTITION the surface's measured time, so their per-operator costs must sum to what the per-surface table independently recorded (4,413,402 ms). Positivity accepts any plausible number and therefore could not tell 1269906 from 269906; a partition cannot. That is the same shape as the legs-versus-surfaces cross-check the round-2 repair had already introduced — two tables, one quantity, measured separately — and noticing it was available twice, having used it once, is the honest description of why round 3 happened.

The second is the structural backstop. Every check names the field it knows about, so each closes its own hole and none closes the class; three consecutive rounds each found one more field whose corruption reached the output as `NaN`, `Infinity` or `undefined`. A backstop downstream of the validator asserts the property those nine findings share — that every derived scalar the report rests on is a real number — so a field nobody has thought of yet fails there even when no per-field check names it.

Round 4 proved the backstop insufficient, and doing so identified what the first three repairs had actually been missing. `observedPerBoot` prices the entire partition, and a wrong rate is a finite positive number that reaches the output as a finite positive number: the backstop cannot see it, and neither can any check confined to the rate table. Deleting one digit from `connectionCensus`, 6842 to 684, passed every condition the anchor had and reported N=10 elapsed at 2694.859 s, inside the 3600 s budget, while the same assignment priced at the measured rate puts leg 3 at 4348.508 s. The reviewer swept the field and found all seven instances (`connectionCensus` to 642, 684, 682 or 842; `interactiveScanCore` to 374, 384 or 387), every one reporting the same 2694.859 s.

The third repair is a reconciliation, and it needed a number the anchor did not carry. The rate table and the millisecond table are two measurements of one run, related by that run's boot count, so the anchor now records `bootsAtRun` — `bootsOf` for every surface evaluated on the tree at `runHeadSha` (47e9544e6, the commit run 33404224554 ran on) — and `observedPerBoot` must equal `round(surfaceMs / bootsAtRun)` exactly. All sixty surfaces do, which is itself the check that the added field is a measurement rather than a convenience.

Two decisions inside that repair are worth the record, because both had a cheaper wrong answer sitting next to them. Recovering the boot count as `surfaceMs / observedPerBoot` would have been circular AND very nearly vacuous: for almost any mutated rate v there is an integer b with `round(ms / b) === v`, so a self-consistency check would have passed all seven mutants while looking like a reconciliation. And the count is the RUN's rather than today's `bootsOf` on purpose — the boot count moves with the source, `controlOutlineScan` has 66 boots at 47e9544e6 and 64 today, so an exact check against the live count would have refused the true anchor over a legitimate drift the arc had already documented.

**The oracle, with its numbers.** The gate had lived inside a CLI-shaped script nothing could import, which is the mechanical reason three rounds of it were proved by hand and a fourth hole survived all three. It now lives in `scripts/probes/2026-09-01-mutation-shard-figures-anchor.ts` with a deciding suite at `tests/mutation/figuresAnchorReconciliation.test.ts`. The cases are derived rather than listed: every single-digit deletion of every number on all three reconciled tables, **703 mutants, zero survivors**. The seven the reviewer demonstrated are asserted to be members of that generated set, so narrowing the sweep fails rather than quietly covering less. Non-vacuity was measured, not assumed: disabling the reconciliation comparison alone leaves **367 of the 703 alive**, all seven demonstrated among them, so the new check is what kills them and the pre-existing cross-checks account for the rest. Every figure the probe emits is byte-identical to the pre-repair head `35e311bbf`.

declined: already mechanized in this branch. The validator, the partition invariant, the backstop and the reconciliation all ship here, and they ARE the disposition. The process mint freeze independently forbids this arc filing a row of any facing.

**Judgment:** one call, and it went against the tool.

Round 2's scope-B finding 3 was the sixth licensing condition — a complete, internally consistent alarm set from an OLDER commit passing every check the tool had. The brief had stated the licensing set as CLOSED at five conditions and said a sixth was admissible only with a probe showing silent corruption while all five held. The reviewer supplied exactly that: it partitioned the base commit's ledger rows through the live shard assignment, and provenance, both cardinality checks and the whole reconciliation passed while the ledger rolled backwards in silence.

Accepting it widened a set this arc had deliberately closed, which is the move a late-round brief is supposed to resist. It was accepted because the brief's own rule licensed it and the probe met the rule exactly — the closed form was doing its job, not failing. The repair binds a run to the tree it describes (`headSha`, refused on mismatch) and makes a deliberate crossing a named `--allow-head` argument rather than an inference. The alternative, treating "which run" as sufficient provenance for "which tree", is what shipped five previous re-blesses. Round 4's condition seven arrived under the same rule and was accepted the same way, which is the closed form working twice rather than failing twice.

**Infra:** no `no_verdict`, no reaper interference, no wrapper faults across eight dispatches.

One process note worth carrying, because it cost a blocked dispatch twice. The convergence gate counts rounds by GLOBBING OUT-DIRS, and this arc runs two scoped dispatches per round, so four out-dirs read as "round 5" and six as "round 7". The corpus rows are the truth — distinct `(baseSha, round)` pairs — and both dispatches were released with `CONVERGENCE_ACK=1` and the corpus count stated in a comment at the dispatch site. The gate is right that this arc is at its cap; it reached that conclusion by the wrong arithmetic.

**What the rounds did not touch:** across all four rounds, zero findings against the repair itself — the recalibration, the split's completeness, the shard count, or the consumer sweep. The stage was spent on the tooling that describes and licenses the change: the figures probe's anchor, and the re-bless tool's notion of provenance.
