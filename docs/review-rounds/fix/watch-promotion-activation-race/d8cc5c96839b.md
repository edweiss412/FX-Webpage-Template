# Review-round economy filing — fix/watch-promotion-activation-race

## spec — 4 rounds

**Examined:** R1 (3 findings: downstream-consumer enumeration incomplete; probe S2/S4 synchronization tautological; DB completeness matrix absent), R2 (2: leftover pre-repair paragraph contradicting the dedicated-branch design; abort's positive side effects unpinned in tests), R3 (3: reconcile outcome not named normatively; entry-point inventory undercounted finalize-cas's two subscribe sites; test-port contract not executable as written), R4 (0, APPROVE).

**Mechanizable:** R1-F3's class — a DB-touching spec missing the mandated tier×layer matrix — is a heading-presence check the same shape as spec-lint's existing `SECTION_MISSING_DIMENSIONAL_INVARIANTS`: trigger on citation paths under `supabase/` OR SQL fences naming `update`/`insert` on `public.*`, require a "completeness matrix" heading. Not built here (one observed instance; file if it recurs).

**Judgment:** the R1-F1 / R3-F1 / R3-F2 shapes (consumer enumeration completeness, normative value naming, inventory counts against code) are exactly what cross-model review is for — each was settled by reading the live union/callers, not by pattern. R2-F1 (contradictory leftover after a repair) is the known repair-tidy-up defect class from `docs/agents/writing-plans.md` (fix-round regression budget); the self-consistency sweep ran but missed prose contradiction across sections — the sweep catches numerics reliably, prose contradictions only when the same tokens recur.

**Infra:** none — all four dispatches returned clean verdicts (no reaps, no `no_verdict`).

## plan — 7 rounds

**Examined:** R1 (6, BLOCKING: adapter-test compat, fake seam shape, RED-first structure, S4 seam, alert restore, close-out order), R2 (7: fake rows not RED, inexecutable mutant, app_events restore, unscoped grep, missed watch.ts comment, unstaged commits, PR-number ordering), R3 (4: settings isolation for pre-existing cases + vacuous OR-premise, missing classifier seam and emit bindings, retry suite unwired, S2 mutant expectation), R4 (4: filter not matching the failing tests, delete-row deviation, lock-order comment unauthored, finalize-cas unmapped), R5 (3: wrong union widened, underivable fake default, untyped mock without import), R6 (1: fake rows outside the RED command), R7 (0, APPROVE).

**Judgment:** the dominant vector was test-step executability against the live test files (R2 F1, R3 F1-F3, R4 F1, R5 F2-F3, R6 F1 — 8 of 25 findings). The same-vector rule fired after R5: a comprehensive audit of every test-referencing step against the live files (override-site enumeration, sink seams, mock typing, filter matching) landed before R6, and R6/R7 dropped to 1 then 0. The audit should have been the PRE-DRAFT pass: the writing-plans pre-draft verification rule already mandates it, and running it at draft time would plausibly have cut four rounds.

**Mechanizable:** none new — spec:lint already catches the citation/staging shapes that appeared; the executability class needs reading live files, which is the judgment work above.

**Infra:** one dispatch interrupted by the caller's own 10-minute Bash timeout (recorded `failureReason: interrupted`, not counted as a round); re-dispatched backgrounded. No reaps, no `no_verdict` from Codex itself.
