# Review-round economy — `fix/control-outline-cover` @ `b30413cf5e51`

## spec — 5 rounds

**Examined:** all five spec rounds (3 / 2 / 1 / 2 / 0 findings; BLOCKING, NEEDS-ATTENTION, BLOCKING,
BLOCKING, APPROVE at R5), their briefs, and the repair commits `24471717d`, `8c4e67598`, `5a0d4c827`,
`27f8d5a70`. This branch has one merge base, so the cross-base total is the same 5. R5 ran past the
four-round cap with a brief that stated the closed criterion only and named no fresh axis, which is the
cap-compliant form; no re-scope was taken, and both of R4's findings were admissible under the standing
bound and fence.

**Judgment:** three of the eight findings could only have come from reading the corpus against the
mechanism, and each was worth its round. All three are the same shape at three depths: a claim that
some input is safely handled, refuted by looking at what actually renders.

- **R1's P0 is the round that earned the arc.** The spec declined to follow a component invocation and
  filed that as a documented limit, naming the one switch-track instance it knew about. The reviewer
  enumerated the class (six weak-painted component definitions reached through seven calls) and found
  the one that is not chrome: `EventRow`'s `role="button"` wraps nothing but `<CronRunSummaryCard>`,
  whose root paints the weak token, so the "limit" was hiding a live control-outline defect. The repair
  also produced the acceptance check the ledger row itself had set, `count("switch-track")` 3 to 5
  rather than 3 to 4. Nothing mechanical separates a documented limit from a silent hole; a reader has
  to look at what the component renders into.
- **R3's P0 is the same shape one level down.** `EventFilters`' input paints through
  `className ?? "<weak default>"` and both call sites supply their own, so the weak string the
  transcript named is dead. Repairing exactly what the red named would have deleted the only readable
  weak token, cleared `isResidue`, and left both rendered controls at 1.27:1 behind a green guard.
- **R4's P0 refuted R3's own repair.** That repair merged the caller's className into a strong base and
  claimed the residual was conservative, because an unreadable prop leaves the element `unresolved`.
  False: `isResidue` is `weakSides(...).length > 0` and `weakSides` reads only readable tokens, so an
  element with no readable weak token is not residue at all. With `cn` merging nothing, a caller
  passing `!border-border` overrides the base with the guard seeing nothing. The repair stopped
  merging and removed the prop.

That last one is the pattern AGENTS.md names as a review defect chargeable to a probe-less acceptance:
R3's repair was accepted on an argument about `unresolved` that one read of `isResidue` refutes. The
cheap round here was R4's P0, and it was cheap only because R4 probed rather than argued. The lesson is
narrow and worth carrying: **a claim that a guard "stays conservative" is a claim about the guard's
code, and it is settled by reading that code, not by reasoning about the scanner's output.**

**Mechanizable:** declined: four of the eight were one class already named in this corpus, and the
repair that closed it is a format change inside this arc rather than schedulable work. Eric's directive
of 2026-08-25 for the ready-queue arcs forbids a new row of any facing, so the residual gap is recorded
on the surface that owns it rather than filed.

Four of the eight were that one class.

- **R2's P1 and P2 and R1's P2 are prose cardinalities about a live array.** I wrote nine accepted
  mutation rows; the reviewer wrote eleven; the registry has fourteen across twelve distinct source
  lines. Two independent hand counts of one array disagreed with each other and with the array. R1's P2
  and R2's P2 are the same defect in its other form: counts that were right when written and were
  stranded when a later round's repair widened the population. `NUMERIC_NOUN_MISMATCH`
  (`lib/specLint/numerics.ts`) exists for this and caught none of them, because it compares numbers to
  nouns INSIDE the document and cannot reach a number that describes a file outside it.
- **The repair that worked is a format change, not a new arm.** The spec no longer carries a hand count
  of that array; it carries the extractor and its output, so the number is derived at authoring time
  and a reader can re-run it. Applying that form to the remaining counts caught a fifth defect neither
  review round found: a "nine" that counted distinct source LINES where the sentence claimed ROWS,
  which changed the consequence from one shifted accepted row to four.
- **R1's P1 and R4's P2 are the same family against a different artifact:** a prose claim about live
  code (`the pill has no colour transition`; `all 59 would need census rows`) that one grep or one run
  of the live predicate refutes. Same repair: cite the line that carries `transition-colors`, and quote
  the measured 9-clear / 50-unclassified split rather than asserting the total.

**No row is filed for any of this.** Eric's directive of 2026-08-25 for the ready-queue arcs is stronger
than the AGENTS.md process mint freeze, so the gaps are recorded on the surfaces that own them: the
dead-alternative shape and its class sweep are
`docs/superpowers/specs/2026-08-26-control-outline-cover-widening-design.md` §6.4, and the corrected
statement of what the residue guard is silent about is L7 in §17, each with a re-file trigger.
