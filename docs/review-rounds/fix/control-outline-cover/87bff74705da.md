# Review-round economy — `fix/control-outline-cover` @ `87bff74705da`

## diff — 1 round

**Examined:** the one diff round in THIS base file (0 findings, APPROVE), and the three at
`26b99c4c0ad8` before the base moved (5 / 2 / 2 findings; NEEDS-ATTENTION at each). Cross-base total
for the stage: **4**. Also examined: their briefs, the repair commits `abe439c90`, `ee2e2b9bf`,
`9d969d4a2`, `6f699409f`, `faad7b3f9`, and the two mutation records the arc scored against. The base
moved three times mid-arc (#904/#899/#905/#906 into `26b99c4c0ad8`, then #908 into `87bff74705da`), so
`--round` restarted at 1 here by construction; the arc-sum rule is why this filing exists at four.

**Mechanizable:** declined: both are already mechanized and neither needs anything built. Two of the
nine findings were mechanizable in principle, and in both cases the guard that catches them already
existed and already covered the case — what failed was the sequence, not the tooling. Filing a row
would name work nobody has to do.

- **Round 1's dark specs.** Both e2e specs this arc added ran in no workflow and carried no allowlist
  row, so they would have passed forever by never executing. `tests/ci/_metaE2eWorkflowCoverage.test.ts`
  already checks exactly this and already covered the case; nothing needed building, and the round was
  spent because nobody ran the suite between writing the specs and dispatching. That is a sequencing
  defect, not a missing guard: the full suite is the cheap gate and it caught all five of round 1's
  siblings the moment it ran.
- **Round 3's stale rows.** Seven accepted mutation rows had drifted because round 2's own commit
  shifted every site below it. `pnpm mutation:sites` exists for precisely that and reports the
  remapping. Same shape as above: the tool was there, the step was skipped. Both are now part of the
  arc's pre-dispatch sequence rather than habits.

**Judgment:** the other seven needed a reader, and three of them changed the shape of the deliverable.

- **The dark-spec finding paid for itself three times.** Once as filed, then twice more as its
  consequences surfaced: the resync dimension case was asserting on an element that never renders on
  the route it visited (`ReSyncButton` mounts only in the per-show strip), and the dimensions spec never
  established the dashboard state it measures in, so on a seeded database `/admin` renders the wizard.
  Both had been invisible for exactly the same reason — nothing ran them. A finding whose repair
  uncovers two more real defects is the cheapest round in the arc.
- **The resolver rounds are one axis across rounds 2 and 3, and the repair direction is the lesson.**
  Round 2 named five shapes a component resolver could not follow — aliases, mismatched defaults,
  anonymous defaults, barrel re-exports, lexical shadowing — and the tempting repair was to teach it
  all five. That is parser growth, and this repo's same-axis rule says a wider recognizer is a bigger
  target for the next round. The repair instead made the scanner REPORT what it cannot name, and a
  guard turned the report into a closed class. Round 3 then found the one shape a sink cannot see:
  shadowing returns a WRONG declaration, not no declaration, so nothing fired. The second repair
  narrowed again rather than widening — count the declarations, follow one, decline two or more — and
  the axis closed. Two rounds on one surface, and the arc never grew a resolver.
- **Round 2's ground finding is the one worth copying.** The closed row-actions arm was measured on
  `bg-bg` while production renders it on the shows table's `bg-surface`. Both grounds clear 3:1
  (3.210/3.998 against 3.352/3.763), so no number moved and nothing was broken — the finding was that
  the harness was measuring a page production never draws. The repair matched the ground rather than
  adjusting a threshold, which is the only repair that would have survived the next token change.

**Infra:** the Codex account hit its usage limit mid-arc (recovered by purchase, ~2h of the arc's wall
clock unavailable for review dispatches) and GitHub Actions had a declared major outage the same day.
Neither cost a round: the arc had non-review work to drain in both windows, which is the whole reason
the "drain the answer-independent work first" rule is worth following.

**A round the arc spent on itself.** Re-scoring after round 3's repair found four survivors in the
repair's own new code, two of them mutants that moved the LINE a report names while every assertion
still passed. The fixtures asserted that a report mentioning the tag existed; they now pin the exact
`file:line <tag>`. Nothing external caught this and nothing would have: a guard's own new code is only
as pinned as the first score after it lands, and the score is the thing that finds it.
