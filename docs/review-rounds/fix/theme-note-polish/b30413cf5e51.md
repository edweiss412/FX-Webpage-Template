## diff — 3 rounds

**Examined:** the whole-diff removal of the theme persist-failure note — `components/layout/ThemeToggle.tsx`, `components/auth/AvatarMenu.tsx`, `components/layout/useAppliedTheme.ts`, the two `text-right` class-sweep peers (`app/admin/settings/admins/RevokeRowButton.tsx`, `components/admin/settings/DeveloperToggleButton.tsx`), their unit suites, the rewritten `tests/e2e/theme-persistence-note.spec.ts`, the §2.2 retirement amendment, and the CI registries the removal disturbed. Three counted rounds at base `b30413cf5e51`, plus one dispatch that died `no_verdict`/`interrupted` and is recorded but not counted.

**Mechanizable:** declined: no ledger row. This arc files no `BL-`/`DEF-` entry of any facing under the owner's 2026-08-25 directive, and the mechanization below is process-facing besides, which the 2026-08-25 process freeze excludes from the open queue on its own. It is recorded here as a documented limit with the diagnosis intact, so the next arc that hits this shape has the analysis without a row having been minted for it.

Nine findings across three rounds were, with two exceptions, ONE defect: **an assertion pinned the identifier or location a change happened to touch, rather than the property the rule requires.** It recurred at a deeper level each round, and each of my repairs was itself the next round's finding.

| Round | The assertion | What walked through it |
| --- | --- | --- |
| 1 | OS-change test expected `dark` after an event that also said `dark` | deleting either handler write |
| 2 | AvatarMenu absence keyed on two `data-testid` values | the same note under a different testid |
| 3 | AvatarMenu absence keyed on live-region set + direct children | the same note nested inside `avatar-menu-items` |
| 2 | alignment guard excluded `text-right`, `text-center` | `text-end` |
| 3 | alignment guard excluded the six unprefixed utilities | `max-sm:text-right` |
| 1 | retirement stated as a hand-written list of ACs and limits | AC-2 and limits 1, 3, 4, 6 |
| 3 | retirement banners applied to two named files in a directory | the third file, `closeout.md` |

The mechanizable read: **a guard written as a list of members is a guard that fails open on the member nobody named.** Each closing repair replaced enumeration with a derivation — a full text census rather than a structural location, a variant-stripping matcher rather than a token list, a directory walk rather than named files, a `cover` over §3 and §4 rather than an AC list. That is a rule an author can apply without a reviewer, and applying it three rounds earlier would have cost this arc two rounds and roughly forty minutes of dispatch.

A narrower mechanization also exists and is cheap: a meta-test flagging absence assertions keyed on a string literal in files whose diff DELETES that literal would have caught round 2 and round 3's first findings without a round. Whether that is worth building is the orchestrator's call under the 2026-08-25 process freeze; this arc files no row for it.

**Judgment:** the product decision itself (whether the note should exist at all) and the disposition of the two `text-right` peers found by the class sweep both needed a human. Neither is mechanizable and neither was a review finding — the ruling arrived from the owner mid-arc, and the peer scope came from the orchestrator.

**Infra:** one round-2 dispatch returned `status: no_verdict`, `failureReason: interrupted` with no verdict text after roughly seven minutes. The wrapper recorded the row because the corpus counts dispatches that reached an exit path; it does not advance the round economy, which counts distinct `round` values on rows whose `status` is `verdict`. Re-dispatched under the same stage and round to a fresh out dir. Separately, rounds 2 and 3 both reported that Vitest could not start in the read-only review sandbox (`EPERM` creating its temporary client/SSR directories, 0 tests run). Each declared it honestly rather than counting it as a finding, but it means the runtime-suite half of those two reviews rested on this arc's own local runs rather than on independent execution — worth knowing when weighing how much the APPROVE, when it comes, actually covers.
