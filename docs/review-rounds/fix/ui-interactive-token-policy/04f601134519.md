# Round-economy filing — fix/ui-interactive-token-policy @ base 04f601134519

## spec — 4 rounds

**Examined:** rounds 1–4 of `docs/superpowers/specs/2026-08-14-ui-interactive-token-policy-design.md`
(verdicts BLOCKING/BLOCKING/BLOCKING/BLOCKING, findings 3/4/2/2). The dominant class was
census-scope truth: the round-0 probe inherited the ledger entry's restricted tag set, and three
consecutive rounds each widened the reachable-className derivation one step (identifier
resolution + `<Link>`/role/onClick tags in R1; prop-flow truth in R2; same-file helper calls and
allowlist-component call sites in R3), each time re-deriving the census (34 → 53 → 55). The
second class was guard-claim honesty: the height-vs-width claim split and the rule-8 defeater
grammar (important/variant/`max-h-*`/arbitrary-property/recipe-scoped forms) both landed as
repairs rather than being stated at draft time.

**Mechanizable:** a reusable in-scope-interactive-element scanner. Every census round re-ran a
bespoke probe; the spec now mandates ONE shared core module
(`tests/styles/interactiveScanCore.ts`) for both guards, which is the durable form. Had a
committed scanner existed at filing time (BL-TAP-TARGET-STRUCTURAL-GUARD's own §2.6 pass was
ad hoc and never landed as a module), rounds 1 and 3 would have been a re-run, not a rebuild.
The defeater-grammar class (R2 F2) is the mutation-registry's territory once the guard exists —
enrolment precedes guard review per the spec's R9, which is the mechanized successor to these
prose rounds.

**Judgment:** the Family D (state-pair dim) carve-out was a genuine design fork surfaced by the
census widening; it was escalated to the user mid-round-1-repair (one follow-up ask) rather than
decided unilaterally, and the reviewer honored the ratification in R2–R4 (its F3 challenged the
recorded cue FACTS, not the disposition — the facts were wrong and were corrected). That split —
user owns the fork, review owns the facts — is what kept the vector from relitigating.
