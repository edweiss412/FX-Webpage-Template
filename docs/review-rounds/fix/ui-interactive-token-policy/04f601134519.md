# Round-economy filing — fix/ui-interactive-token-policy @ base 04f601134519

## spec — 5 rounds

**Examined:** rounds 1–5 of `docs/superpowers/specs/2026-08-14-ui-interactive-token-policy-design.md`
(verdicts BLOCKING×4 then APPROVE at R5; findings 3/4/2/2/0). The dominant class was
census-scope truth: the round-0 probe inherited the ledger entry's restricted tag set, and three
consecutive rounds each widened the reachable-className derivation one step (identifier
resolution + `<Link>`/role/onClick tags in R1; prop-flow truth in R2; same-file helper calls and
allowlist-component call sites in R3), each time re-deriving the census (34 → 53 → 55). The
second class was guard-claim honesty: the height-vs-width claim split and the rule-8 defeater
grammar (important/variant/`max-h-*`/arbitrary-property/recipe-scoped forms) both landed as
repairs rather than being stated at draft time.

**Mechanizable:** a reusable in-scope-interactive-element scanner. Every census round re-ran a
bespoke probe; the spec now mandates ONE shared core module
(`tests/styles/interactiveScanCore.ts`) for both guards, which is the durable form. Had a
committed scanner existed at filing time (BL-TAP-TARGET-STRUCTURAL-GUARD's own §2.6 pass was
ad hoc and never landed as a module), rounds 1 and 3 would have been a re-run, not a rebuild.
The defeater-grammar class (R2 F2) is the mutation-registry's territory once the guard exists —
enrolment precedes guard review per the spec's R9, which is the mechanized successor to these
prose rounds.

**Judgment:** the Family D (state-pair dim) carve-out was a genuine design fork surfaced by the
census widening; it was escalated to the user mid-round-1-repair (one follow-up ask) rather than
decided unilaterally, and the reviewer honored the ratification in R2–R4 (its F3 challenged the
recorded cue FACTS, not the disposition — the facts were wrong and were corrected). That split —
user owns the fork, review owns the facts — is what kept the vector from relitigating.

## plan — 9 rounds

**Examined:** rounds 1–9 of `docs/superpowers/plans/2026-08-14-ui-interactive-token-policy.md`
(verdicts BLOCKING×8 then APPROVE at R9; findings 10/9/2/4/3/1/2/1/0). Rounds 5–8 were
execution-mechanics precision on two tails: the Task 8 closeout protocol (patch production
from the working tree with exact commands, bytes-exact diff-of-diffs identity, the
marker-vs-suite red distinction, the corpus JSONL row declared in the two-part last commit,
immediate push of a graduation revert) and one live snippet defect (bare indexOf anchors
resolving to a comment and the @theme alias block, repaired with line-anchored regexes and
probed against the live CSS before committing). Two dominant classes. First, execution-contract
precision: RED validity (an opt-in registry cannot red on absence; a census suite cannot red on
an empty census whose file does not exist), commit-ordering (guard-RED merged into its GREEN
task; the graduation/marker commit vs review-covers-what-merges vs invariant-12's
last-commit letter, settled by a reviewed patch file applied verbatim as the final commit with
a revert-and-redo restoration loop). Second, resolver-model soundness: the flat string model
lost conditionality (R1), the branch-group model lost ancestry (R2), and the fix converged on
a path-set model whose quantifier IS spec rules 1/3/4; the executable-fixture demand (R3) is
what made first-wins and flattening scanners falsifiable.

**Mechanizable:** red= executability is partially mechanized already (`spec:lint` task markers)
but the "red for the STATED reason" half is judgment; the reviewed-patch-as-last-commit shape
is a candidate for a reusable closeout recipe since every ledger-closing arc faces the same
three-way tension. The heavy-wrapper placement rule for Docker captures (host-side wrap, probed
against the slot dir mount) belongs in the AGENTS.md heavy-phase section's member list next
time that file is edited.

**Judgment:** the path-set resolver redesign and the graduation-patch mechanism were design
decisions made in repair, each directly answering a reviewer counterexample; neither was
relitigated once landed with executable cases.
