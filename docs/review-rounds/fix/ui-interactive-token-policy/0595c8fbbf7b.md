# Round-economy filing — fix/ui-interactive-token-policy @ base 0595c8fbbf7b

This base exists because a merge with `origin/main` moved the merge-base mid-review. The review
history is continuous with `1e503d714b6e.md`, which carries the first nine rounds and the
substantive analysis; these rounds are its tail, and only the product half ran.

## diff — 4 rounds

**Examined:** four product-half dispatches after the merge; the guard half APPROVED at round 6
under the previous base and was frozen, with `tests/styles/**` and `tests/mutation/**` declared
out of scope in every brief here. Verdicts: BLOCKING(2) → BLOCKING(3) → BLOCKING(3) →
**APPROVE(0)**. Findings: the graduation patch stopped applying and had to be regenerated (twice —
the first regeneration inserted at a blank line that fell INSIDE an existing archive entry); the
six-control topology sentence; the dev-panel entry's quoted markup; two counts; and one P0.

**The P0 is the finding worth keeping.** AC-6 and R9 require BOTH scan modules enrolled in the
source-mutation registry. The branch shipped two of three and explained why in the closeout, and
the reviewer's objection was not about the reasoning — it was that a technical rationale in a
closeout is not a spec amendment, and invariant 7 says the spec wins. That is correct, and the
repair is `AMENDMENT 1` in the spec: dated, with the measurement (the third module was enrolled,
generated ZERO mutants, and tripped the harness's own `no-mutants` condition), and with what is
NOT amended stated beside it. The distinction the round enforced — a reason is evidence, an
amendment is authority — is the durable lesson.

**Mechanizable:** the graduation artifact. It is a committed unified diff against two ledger
files that every merge with `main` can invalidate, and it did: `git apply --check` failed after
the merge, the regeneration had to be redone at a complete-entry boundary, and its blank context
lines trip `git diff --check` by construction. A patch file is the wrong shape for a ledger
move that always happens at a known moment — the durable form is generating it at graduation
time from the entries themselves, which is what the regeneration procedure now does by hand.
Filed as an observation here rather than as a ledger entry because the arc it would change is
the graduation convention itself, not this branch.

**Judgment:** none of these four rounds required one. Every finding was settled by reading the
tree.

**Infra:** the merge-base moved mid-review, so the round-corpus started a new file and the
dispatch that ran as round 10 became round 1 here. Its row carries a note saying so, because a
corpus that silently renumbers is worse than one that explains itself.
