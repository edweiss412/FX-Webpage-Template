# Round-economy filing — fix/ui-interactive-token-policy @ base 1e503d714b6e

## diff — 4 rounds

**Examined:** rounds 1–4 of the implementing diff, dispatched as TWO tight-scope reviews per
round (guard + mutation harness; product + design + ledger) rather than one whole-diff pass,
per the AGENTS.md split-review default for a 96-file change. Verdicts: guard half
BLOCKING(7) → BLOCKING(2) → NEEDS-ATTENTION(2) → round 4; product half BLOCKING(2) →
NEEDS-ATTENTION(3) → NEEDS-ATTENTION(1) → round 4. Every finding across the first three
rounds was real; none was refuted, and none was a re-raise of a settled item.

Two classes account for all of them.

**Class one — the guard cleared things it had not proven.** Round 1's F2/F3 were four shapes
that passed the height floor without establishing it: a `max-h-*` ceiling read as a floor, a
`hover:`/`sm:` variant read as an at-rest guarantee, an `[&:hover]` self-variant read as a
descendant's box, and a horizontal `before:-inset-x-*` bleed read as proof of height — the
last being the shape two live sites actually wore. F4 added three resolver claims of the same
kind: a `let` read from its initializer, a helper's fall-through path dropped, and the
floor-component allowlist matched on tag SPELLING. Each was probed against the shipped scanner
before repair, and each repair narrowed what clears.

**Class two — a claim that was true of the pattern and false of the site.** Three
`parent-label-target` census rows (the label existed; it carried no floor), one Family C
exemption (the "Clear filters" action is not a chip), two `state-dim` cue pins (one pinned the
colour delta Family D requires a cue BESIDES; the other covered one branch of two), a
`full-bleed` reason naming a fixed-height bar that does not exist, and a co-visible proof
naming a render path that renders the quiet variant. None of these was visible from the
diff's own text — every one required reading the markup the row described.

**Mechanizable:** two things, and both landed.

1. **The mutation gate did the work three prose rounds could not.** Enrolment (spec R9)
   scored the surface at 0.7053 with 61 survivors on a suite that had already passed three
   impeccable rounds and cross-model spec and plan reviews; a second round on the repaired
   code scored 0.871 with 36 more. Across both, 67 survivors were repaid with fixtures, 11
   ledgered with arguments, and ONE function deleted outright after the gate proved rule 8
   made it unreachable. The survivors were exactly the boundaries a hand-written suite does
   not think to stand on: all three resolver bounds, the `p-3` threshold, the 4px scale, the
   axis map, and the assumed row height — which needed a fixture landing the sum on 43,
   because whole 4px steps cannot see a 20-vs-21 change.
2. **Counts in prose drift from the data they describe.** The census tally was corrected in
   §4.3 and left standing in fourteen other places, including the graduation patch that would
   have archived it; the section headers then disagreed with the rows a round later. Both are
   now DERIVED: `_metaTapTargetFloor` parses the census header and compares it to the
   registry, per category and in total, with comment markers and line wraps normalised so a
   prettier rewrap cannot silence it. The number lives in one place and the guard reads it.

**Judgment:** three findings were dispositions rather than defects, and each went to the
ledger with the reason it could not be settled here — the tinted-plate outline ratios (a
design decision under the standing R5 frame that this was an upgrade, not a compliance
repair), the three under-floor checkbox rows (repairing them trades the checkbox's accessible
name against its target size), and the one Family S site with no visible fold cue. The
opposite call was made for the Family C row, because there the ratified policy already
prescribed the answer: a control in no carve-out family swaps. Correcting a false membership
claim is not reopening the family set, and the spec carries the correction beside its original
tally so the record reads as one.

**Infra:** the harness itself had a hole this arc found by hitting it. `statement-removal` of a
loop-advance statement produces a mutant that never TERMINATES, and `runSuite` passed no
timeout — one mutant ran 1h48m while the run scored nothing, with four wedged children from
OTHER arcs alive on the same machine at that moment. The repair took three rounds to get
right, and the last two were the interesting ones: a timeout alone leaves the descendants
alive, and a descendant walk cannot find them because `spawnSync` returns only after killing
the root, by which point they have been reparented. The shipped form launches the child
through `perl -e 'setpgrp; exec @ARGV'` so the group outlives its leader. Recorded as limit
L-9 in the harness spec.
