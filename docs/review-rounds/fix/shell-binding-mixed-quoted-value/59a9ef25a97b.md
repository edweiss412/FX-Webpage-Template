## plan — 7 rounds

**Examined:** all four plan-stage dispatches (r1 NEEDS-ATTENTION/5, r2 NEEDS-ATTENTION/3,
r3 NEEDS-ATTENTION/3, r4 NEEDS-ATTENTION/1 — an undeclared pre-existing zero in the
path-separator family, closed by the basename clause — r5 NEEDS-ATTENTION/1 — the same family's
multiword siblings, where re-lexing the dequoted value read literal quote data as syntax; closed
STRUCTURALLY by implementing the word-split consumer grammar exactly (plain IFS split) instead
of approximating it through the shell re-lex — r6 NEEDS-ATTENTION/1, the same axis a third time
(wrapper-prefixed quoted-directory value) — r7 the confirmation dispatch after the orchestrator
disposition). Finding classes by round: r1 — red-marker
validity for declaration-only tasks, `pnpm heavy <arg>` execvp shape, mutant-fixture digit-bound
discrimination, registry coordinate arithmetic, premise-helper compliance; r2 — cross-document
consistency after a disposition (probe-record reading vs spec), cleanup-assertion vs conditional
file set, expansion-operand recall class; r3 — a fidelity fix colliding with a committed
KNOWN-miss pin, unterminated-input decode contract, dual consumer grammars (eval newline vs IFS
word-split) for multiword values. No class repeated across rounds; each round's findings were
repaired with class sweeps in the same commit.

**Mechanizable:** declined: a `pnpm heavy` argument-shape check — the wrapper execvp's its
first argument, so `pnpm heavy <pkg-script-name>` is always wrong unless the name is a real
executable, and a lint over plan/docs command lines would have caught r1-F2 at author time.
Declined because its checkable domain is "every command line in every doc", which needs a
shell-command recognizer over free prose — the open-grammar growth this repo's own
repair-direction rule refuses, and the shape that took a peer arc to a 1207-line detector with
a flat finding rate. AGENTS.md already carries the wrap-the-outermost-command rule in prose,
and the review round that caught it is the intended catcher.

The second candidate is FILED as BL-PLANLINT-DECLARED-LIMIT-PIN-COLLISION: a "fidelity fix vs
committed KNOWN-miss pin" collision sweep, so a plan that changes lexer or recognizer behavior
mechanically greps the deciding suite for `KNOWN miss`/declared-limit pins over the affected
surface before review (r3-F1's class). Unlike the declined one it ranges over a CLOSED domain —
the mutation registry already names each enrolled surface's `suitePaths`, so the pins to check
are enumerable from disk.

**Judgment:** the dual-consumer-grammar semantics (r3-F3) and the digit-boundary mutant
discrimination (r1-F3) are genuine review judgment - both needed a reviewer running bash and
the mutant decode by hand; no static rule proposed. The r4/r5/r6 single-finding train on the
quoted-value path/prefix axis was dispositioned by the orchestrator (option b, 2026-08-17):
the split reading's claim is NARROWED to argv[0]-psql command lines and the wrapper-prefixed
quoted-directory value is a declared, pinned documented limit fenced in both directions -
the narrowing repair the repair-direction rule prescribes for a dripping recognizer axis, in
place of wrapper-vocabulary growth. r7 is confirmation-only.
