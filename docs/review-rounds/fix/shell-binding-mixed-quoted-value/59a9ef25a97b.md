## plan — 6 rounds

**Examined:** all four plan-stage dispatches (r1 NEEDS-ATTENTION/5, r2 NEEDS-ATTENTION/3,
r3 NEEDS-ATTENTION/3, r4 NEEDS-ATTENTION/1 — an undeclared pre-existing zero in the
path-separator family, closed by the basename clause — r5 NEEDS-ATTENTION/1 — the same family's
multiword siblings, where re-lexing the dequoted value read literal quote data as syntax; closed
STRUCTURALLY by implementing the word-split consumer grammar exactly (plain IFS split) instead
of approximating it through the shell re-lex — r6 the convergence dispatch). Finding classes by round: r1 — red-marker
validity for declaration-only tasks, `pnpm heavy <arg>` execvp shape, mutant-fixture digit-bound
discrimination, registry coordinate arithmetic, premise-helper compliance; r2 — cross-document
consistency after a disposition (probe-record reading vs spec), cleanup-assertion vs conditional
file set, expansion-operand recall class; r3 — a fidelity fix colliding with a committed
KNOWN-miss pin, unterminated-input decode contract, dual consumer grammars (eval newline vs IFS
word-split) for multiword values. No class repeated across rounds; each round's findings were
repaired with class sweeps in the same commit.

**Mechanizable:** (a) a `pnpm heavy` argument-shape check — the wrapper execvp's its first
argument, so `pnpm heavy <pkg-script-name>` is always wrong unless the name is a real
executable; a lint over plan/docs command lines would have caught r1-F2 at author time.
(b) a "fidelity fix vs committed KNOWN-miss pin" collision sweep: any plan that changes lexer
or recognizer behavior should mechanically grep the deciding suite for `KNOWN miss`/declared-
limit pins over the affected surface before review (r3-F1's class).

**Judgment:** the dual-consumer-grammar semantics (r3-F3) and the digit-boundary mutant
discrimination (r1-F3) are genuine review judgment - both needed a reviewer running bash and
the mutant decode by hand; no static rule proposed.
