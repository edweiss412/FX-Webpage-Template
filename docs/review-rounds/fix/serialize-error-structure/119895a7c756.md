# fix/serialize-error-structure — review-round filing

Arc identity: branch `fix/serialize-error-structure`, merge-base `119895a7c756`. Corpus: `119895a7c756.jsonl`.

## spec — 4 rounds

**Examined:** R1–R4, 10 declared findings (R1 4, R2 3, R3 3, R4 0 → APPROVE). One recorded `no_verdict` row at R3 (dispatch produced no verdict and was re-dispatched at the same round number); it is not counted.

**Mechanizable:** none.

The tempting candidate is the repeated shape across R2 and R3 — the spec asserting the runtime shape of an external error family without a probe (R2: Supabase Auth/Storage errors are `Error` subclasses and `AggregateError.errors` is an own NON-enumerable property; R3: which fields a Storage error actually carries). A linter cannot settle it: the claim is about a third-party runtime's object graph, and the only oracle is executing against the installed package. That is exactly what AGENTS.md's finding-admissibility contract clause (a) already requires ("claims about current behavior are settled by probe"), and the repair each round was to run the probe and quote it into the spec. The gap was authoring discipline against an existing rule, not a missing mechanism, so filing a `BL-` row here would be filing the rule twice.

The one genuinely new mechanism this arc produced is on the DIFF side and shipped in-branch rather than as a backlog row: `tests/log/noDoubleSerializedLogError.test.ts` grew the pre-flatten predicate (`String(...)` / `JSON.stringify(...)` in an `error:` initializer), which is the static enforcement of a property the spec previously stated only in prose.

**Judgment:** R1's key-redaction finding (an email-bearing object KEY leaks once structure survives serialization) was a genuine design gap the spec had not considered — it became Task 2, a second chokepoint repair in `sanitizeContext`, not a serializer change. R1 also set the dual test posture (literal constant pins PLUS `BOUND`/`BOUND+1` behavior fixtures) and required the comment sweep be DERIVED (`rg -ln` output committed in the plan) rather than enumerated. R3's null-prototype accumulator finding (an own `__proto__` key from `JSON.parse` is swallowed by the inherited setter on a plain `{}`) changed both the helper and `sanitizeContext`.

**Infra:** one R3 dispatch returned `no_verdict`; re-dispatched at the same round and returned a verdict with 3 findings. No reap signature was investigated — the row is recorded for completeness.
