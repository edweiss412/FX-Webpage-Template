# Review-round economy — fix/avatar-menu-switch-pending-watchdog @ 4cb585b3508a

## plan — 4 rounds

**Examined:** the four plan-stage dispatches recorded in `4cb585b3508a.jsonl`, their briefs at `.codex-reviews/plan-r{1,2,3,4}-brief.md`, and the four repair commits between them. Fourteen findings across rounds 1 to 3 (3, 6, 5), every one of them repaired in the round that received it. Round 4 is the cap round.

**Judgment:** the round count is not a story about a hard problem. The product change is a watchdog on one menu row, roughly forty lines, and its design has been stable since round 1's repair: a phase this component owns rather than React's entangled `useTransition` flag. What burned three more rounds was a single recurring defect class, and it is worth naming precisely because it does not look like a defect while you are writing it.

Every finding in rounds 1 to 3 was a claim the plan made about a mechanism it did not own, believed because the mechanism usually behaves that way. `useTransition`'s pending flag is per-attempt (it is not; it is entangled across concurrent transitions from one hook). A rejected transition action leaves pending set (it does not; it surfaces as a render error). Batched updates make two states mutually exclusive (they do, but say nothing about an already-queued timer callback). A string `digest` identifies Next control flow (it does not; ordinary server failures carry opaque ones). "Closed" is one state (it is not; the announcer is outside the popover and always mounted). In every case the CODE was defensible and the SENTENCE was wrong, which is why each survived self-review: self-review re-reads the sentence and agrees with it.

Two rounds also asserted a guard was load-bearing on the strength of a probe that never exercised it. Round 3 caught it, and running the mutant settled it the other way: the callback's attempt ordinal was defensive code with no failing case behind it, so it was deleted along with the criterion that claimed to prove it. That is the cheapest outcome available at round 3 and it should have been available at round 2 — the mutant costs a minute.

**Mechanizable:** one shape, and it is small. For every "cannot" or "impossible" a plan writes, require the LINE that refuses it rather than the mechanism that usually would; for every "proved", require the mutant that reds. Both are checkable by reading, and both would have caught the whole class: round 1 F1 (a claim about React's flag with no line behind it), round 2 F1 (batching with no line behind it), round 3 F1 (an ordinal with no mutant behind it), round 3 F3 (a state model with no node behind it). The existing anti-tautology rule asks for a premise on the case's own inputs, which is the same instinct one level down; this is that instinct applied to the PROSE, where these findings actually lived.

declined: no ledger row. The process mint freeze (AGENTS.md, 2026-08-25) admits a process-facing row only under `invariant` or `product-blocked`, and this is neither. It is also not mechanizable in the sense the field usually means: a guard cannot tell a claim with a line behind it from one without, because supplying the line is the human's half. The durable home is this filing plus the plan's own round sections, which state the check in the form both a reviewer and an author can apply.

**Infra:** none. Four dispatches, four verdicts, no reaper deaths and no `no_verdict`.
