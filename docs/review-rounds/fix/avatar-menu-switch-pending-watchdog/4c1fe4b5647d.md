
## diff — 4 rounds

**Examined:** the four diff dispatches at base `4c1fe4b5647d` recorded in `4c1fe4b5647d.jsonl`, plus the three at the two earlier bases (`4cb585b3508a.jsonl`, `5fccaaac7804.jsonl`) that the arc-sum rule counts with them. Seven diff rounds, sixteen findings, every one repaired in the round that raised it. Two `origin/main` absorptions moved the merge base mid-arc on the orchestrator's instruction, which is why the rounds sit in three files.

**Judgment:** the product change never moved. A watchdog on one menu row, roughly forty lines, stable since the plan stage's first repair. All sixteen diff findings were in two classes, and both are worth stating because the repair that finally worked was the same shape in each.

The first class was a document restating something executable. The plan carried the submit path, then the transition inventory, then the transition audit; each copy went stale, each was found by a reviewer reading the code, and each was repaired by DELETING the copy rather than correcting it. The third one was the expensive one: the pasted audit was the pre-repair version, and an implementer following it would have restored two defects earlier rounds had fixed. One clean lesson: a plan may hold the contract and the reasons, and must not hold a second copy of anything that runs.

The second class was a guard claiming more than it checked. The transition audit was tightened four times, and every tightening came from a live escaping mutant somebody ran, never from an argument: a regex census that saw 7 of 12 ternaries and counted a comment; a many-to-many mapping that let a deleted supersession guard stay green because its row matched a textually identical twin; `||` invisible, so a widened busy definition disabled the row in Open-error with the census byte-identical; and `&&` counted only with a JSX right operand, so a narrowed one enabled the row in Open-pending the same way. The first three were repaired by widening a predicate. The fourth was repaired by deleting the predicate: every logical operator counts now, because each earlier version was an attempt to guess which operators "really" mean a branch, and that guess has a corner every time.

**Mechanizable:** the one shape worth a rule is the one the arc kept re-learning at its own expense. A mechanical rename is not a sweep. Round 1 at this base renamed `onSwitchSubmit` to `beginSwitch` inside comments without rechecking the claim wrapped around the name, so three comments went on asserting that an alert paints while the phase is still pending and swallows an immediate retry. Probed false: the settle writes phase-idle and the error in one update, the row is already enabled when the alert appears, and an immediate retry works. That false premise was the entire justification for a `settled()` helper and its two call sites, all three of which are gone.

declined: no ledger row. The process mint freeze (AGENTS.md, 2026-08-25) admits a process-facing row only under `invariant` or `product-blocked`, and this is neither. The durable homes are this filing and the guard file's own header, which now records each escape and the mutant that demonstrated it.

**Infra:** none for the dispatches; seven rounds, seven verdicts, no reaper deaths and no `no_verdict`. One machine-level note that is not a finding: the readiness suite's eight failures were 30-second timeouts in two exhaustive parser typo-generators under concurrent load, untouched by this diff, and both files pass 107/107 when re-run on a quieter box.
