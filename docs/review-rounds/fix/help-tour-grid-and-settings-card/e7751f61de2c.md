# Review-round economy — `fix/help-tour-grid-and-settings-card` @ `e7751f61de2c`

## spec — 5 rounds

**Examined:** all five spec rounds (2 / 2 / 4 / 1 / 2 findings, NEEDS-ATTENTION throughout), their
briefs, and the repair commits `a014ac4f3`, `98f8257d6`, `bdad0c682`, `3cb4b6518`, `ef7b49c4a`,
`f2a195b7f`. R5 was a single round authorized past the cap by the orchestrator; it found the
cap-binding intervals were the two-column SWITCH thresholds mislabelled, and that the criteria did
not assert the column sequences at all — a permanently single-column page passed AC-1, AC-1a, AC-1b
and AC-1c. That second one is why the plan carries a violation inventory.

**Judgment:** one vector produced a finding in every round, and it was the same mistake each time —
a claim about layout computed from a model of the container that was missing a term the real page
has. R1 missed that `.help-prose` caps the column. R2 missed that `auto-fit` is continuous, so a
five-viewport matrix reported a worst case of 31.2ch when the true worst was 27.6ch at a switch no
sample touched. R3 missed two terms at once: the shell drops 240px of sidebar plus a 24px gap into
`main` at `md`, making the container NON-MONOTONIC in viewport width, and the errors `nav` is not
bled so its own 704.4px cap made a two-column state unreachable at every viewport — a permanently
collapsed grid that AC-1b and AC-1c would both have passed, because one column neither wraps nor
overflows.

Each round's repair was arithmetic corrected by more arithmetic, and each corrected model was
missing a different term. The findings did not decay through three rounds: 2, 2, 4.

R4 fell to one, and that one was the same vector again — the pre-`md` window, 752 to 767, where
`main` is 720 and the cap binds by 15.6px. Every earlier draft had said the cap binds "only at
1024px and above", read off a table sampling 390 / 768 / 900 / 1024 / 1280 that steps straight over
that window. The §2.3 sweep measures at 4px and had the data; the §2.2 table it fed did not carry
the row, so a claim derived from the table stayed wrong while the measurement beneath it was right.
The lesson is narrower than "measure instead of derive", which R3 already learned: a measured sweep
does not protect the prose that summarises it, and the summary is what reviewers and implementers
read.

That round also found the one place where a criterion contradicted the design it was accepting.
AC-1a said no viewport's measure may regress, and at 752 the baseline is a single 65.8ch column
against the proposed 30.8ch two-column state. The decrease IS the change — a scannable grid instead
of one wide column of cards — so as written the criterion would have failed a correct
implementation. It now says nothing crosses the 28ch floor, which is what it was for.

What closed it was not a better model. R3's repair replaced derivation with measurement — the
proposed CSS injected into the real page and swept 320 to 1440 at 4px, reading computed column
counts rather than predicting them. That is the same lesson `test/modal-wait-helper-adoption`
recorded at its own R4 ("the re-analysis produced a prose table, and a prose table over a live tree
is the format that had already failed three times"), reached here one round later and by the same
route.

The cheapest round to have avoided was R1's second finding. The four pre-dispatch mutants that
`docs/agents/writing-plans.md` mandates for presence guards include exactly the case R1 found — the
expected content present but not live, which for the completeness guard is `/help/admin/settings`
named in prose with no card. Those mutants are specified as a PLAN-stage obligation. Run at spec
time they would have cost minutes and removed a round.

**Mechanizable:** two classes, both declined below.

*Intra-document section references.* R3's fourth finding was a `§2.3` cited before that section
existed, plus two sections numbered `§1.1` since the first draft, which made every ratification
citation ambiguous — including the ones in this arc's own review briefs. `spec:lint` validates that
a cited `path:line` exists and makes no claim about section references.

declined: LIM-CROSS-DOC-SECTION-RESOLVE, whose parked shape is the cross-document form of exactly
this defect; this arc names the intra-document variant, where a spec cites its own section numbers.
The process mint freeze admits a process-facing row only under invariant or product-blocked and
this is neither — the defect is loud the moment anyone follows the reference, and it cost one P2
finding inside a round that was already running for other reasons.

*Layout claims derived rather than measured.* The dominant vector, and not a lint arm: no
recognizer can know that a model of a container omits a term the real page has. The rule that
already covers it is the mandatory empirical spike in `docs/agents/spec-self-review.md`, whose
trigger list names component lifecycle, close/navigation races, optimistic state and undocumented
framework behaviour, and does not name responsive layout. Four rounds say it should.

declined: the repair is one sentence added to a rule that already exists, which is a rule-text
change rather than a mechanized gate, and the process mint freeze does not admit a row for it.
Recorded here so a later arc naming this shape can cite two namings rather than one.

**Infra:** Codex usage credits were exhausted mid-arc, between the round-1 repair and the round-2
dispatch. All dispatches were held on the orchestrator's order rather than waived or substituted,
and the arc drained its reviewer-independent work in the meantime (the plan skeleton, its mandatory
pre-draft verification, and the CI-wiring analysis all landed in that window). No round was lost
and no `no_verdict` row was produced.

## plan — 5 rounds

**Examined:** all five plan rounds (6 / 5 / 1 / 1 / 2 findings; BLOCKING through R3,
NEEDS-ATTENTION at R4 and R5), their briefs, and the repair commits `4a192bf49`, `f779e0c22`,
`451a12674`, `f552b94f5`, `e652b0e8e`, `e2dadc898`, `9edb568d0`. R5 was a single round authorized
past the cap; both its findings were in text added as R3's and R4's own repairs, which is what
decided the orchestrator to converge rather than run a sixth.

**Judgment:** one vector again, and it is the same one the spec stage had, displaced by a level.
The spec's version was "a layout claim computed from a model of the container that was missing a
term". The plan's is "an assertion that holds for a reason other than the one it names", in two
shapes.

The first shape is a red-green cycle that greens for the wrong reason, and it appeared three times
in round 1 alone: a task whose green came from retargeting a guard rather than from the
implementation landing; two tasks ordered so that neither ever observed the defect; and a task
whose red was a missing file against production that was already correct. None of these would have
failed if implemented — they would have passed while proving nothing, which is the expensive kind.

The second shape is a violation inventory whose entries are wrong about their own violations. Round
2 found three: a 16rem stage named at the one matrix viewport it passes, an AC-1a mutation that was
a no-op because `help-bleed` is already unprefixed, and a claim that today's grid is single-column
at 1016 when `md` is active there. Re-running every row against the committed sweep afterwards found
a fourth the review had not flagged, AC-2 breaching only at 1280 and not at 1024. Four of eleven
rows were wrong, in the document whose entire purpose is to prove each criterion can fail.

The lesson is narrower than "check your numbers". The inventory was written in the same sitting as
the criteria it validates, from the same mental model, so it inherited that model's errors and
could not detect them. What broke the loop was computing every row from a data file produced by a
different process — the same move that ended the spec stage's arithmetic vector one stage earlier.
The second occurrence is the interesting one: knowing the lesson did not transfer it, because the
plan looked like a different kind of document.

Two review findings were caught by machinery rather than by reading, and both were free. `spec:lint
--exec-red` proved a `red-state=live` marker exits 0 today, and `RED_TEST_NAME_FILTER` caught a `-t`
filter that would have reported green from birth. Both landed before dispatch. The rounds were
spent on what no arm checks.

**Mechanizable:** one, declined.

*A declared failure that is never executed.* The red contract makes a task's RED checkable —
`--exec-red` runs `red-state=live` commands and reports any that exit 0, and it caught one here
before dispatch. Nothing checks that a VIOLATION row actually violates. Four of eleven rows were
wrong and survived a review round because the inventory is prose: a `16rem` stage named at the one
viewport it passes, a mutation that was a no-op, a false account of today's column count, and a
stage that lands inside the bound it claims to breach.

declined: LIM-AUTHORED-RED, whose parked shape is this one — "a declared `red=` that cannot be red
for its stated reason, and nothing executes it to find out", with a new killing case green against
clean production named as one of its two remaining forms, which is exactly this arc's plan-R1 task 7.
This arc extends the naming outside the marker grammar: a violation-inventory row is a declared
failure with no execution behind it, so it fails the same way and for the same reason. Not coined as
a twin slug, per the corpus README's instruction to cite an existing slug covering the same shape.
The process mint freeze admits a process-facing row only under `invariant` or `product-blocked`, and
this is neither — this arc is product-facing but was never blocked by the gap, only slowed inside
its own review budget, and a round burned on a review surface is what the freeze says does not
qualify.

**A note on the last two rounds, because they are the same mistake made twice by the repair
process rather than by the draft.** R3 and R4 each returned one finding, and both were in text I
had ADDED as a repair. R3's was a command missing the runner-process auth the setup asserts; R4's
was a claim that `_metaE2eWorkflowCoverage` PROVES the project wiring, when its scanner cannot see
project-only invocations at all and every sibling help spec carries an allowlist row saying so. The
second is the sharper: the sweep that produced it found the right FILE and drew the opposite
conclusion from it, reading "fails by default for new dark specs" as validation without reading the
allowlist twelve lines below. A sweep that stops at locating the surface has done half the work.

**Infra:** none in this stage. The Codex credit exhaustion that interrupted the spec stage was
resolved before the plan stage opened.
