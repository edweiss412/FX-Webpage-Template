# Review-round economy — `fix/help-tour-grid-and-settings-card` @ `066c034c9a3e`

## diff — 4 rounds

**Examined:** all four diff rounds at this base (4 / 4 / 1 / 1 findings), their briefs, the
seventeen staged violations in
`docs/superpowers/plans/2026-08-29-help-tour-grid-and-settings-card-violations.md`, and the repair
commits `db7168c9a`, `19d2093b5`, `5021ccaa7`, `a80725068`, `3874014fe`, `fd5ffd9a7`, `a65d9bd2e`,
`21e7fe659`, `dcd51dbde`, `311971ac4`, `70e5c7eb0`. Diff rounds live only at this base; the spec and
plan stages ran five each at `e7751f61de2c` and are filed there. Cross-base total for this branch:
14 rounds.

**Judgment:** every diff finding was one shape — an assertion that holds for a reason other than the
one it names — and the four sub-shapes are worth separating, because they need different repairs.
Assertions ABSENT (the bleed, §4's shared height, the 1440 cap). Assertions NARROWER than the claim
they carry (AC-1c reading one track, the focus row asserting "no focus-scoped utility" while
claiming "nothing transitions", the measure pin matching any integer `ch`). Populations UNPINNED (a
card-body list that was a filter with only "at least one" asserted; no bridge between the
completeness guard's population and the layout guard's). And samples that CANNOT BIND (`22rem`,
where every sampled viewport resolved to the same 720px container; the span card's body, sampled
only where `p-5` padding kept it under the measure).

The rate went 4, 4, 1, 1, and the decay is explained by something I would rather not have found.
Producing a class breakdown at the orchestrator's request showed that THREE of round 2's four
findings were consequences of my own incomplete round-1 sweeps, not new axes: §4's shared-height
half sat in the very row whose width half I had just asserted, and the focus row's spec drift was in
§5, which my round-1 spec sweep had not covered. So the flat rate was never evidence of a bottomless
defect surface; it was evidence that a sweep run to 80% reads exactly like a sweep run to
completion. Running both sweeps to exhaustion before round 3 found four more residuals unprompted
(§4 row 4's unsampled 1440, §4 row 7's body arm, §3.5's floor provenance contradicting its own
ratification, §3.4's stale count), and round 3 returned one finding instead of four.

**Mechanizable:** every staged violation in this arc's inventory REMOVES something — the cap, the
bleed, the `grid` class, the `min()`, the card, `max-w-6xl`. None of them CHANGES a value. A
removal-only perturbation family cannot catch a value that is merely wrong, and that is exactly how
round 3's finding survived seventeen staged violations: AC-2's row tested a deleted cap, so `70ch`
to `69ch` passed it while moving every capped child on every help page by 10px. The gap is not that
an instrument went untested — every row here IS an instrument test, which is what separates this
from LIM-SWEEP-POSITIVE-CONTROL — it is that the perturbations were uniform in direction. The
mechanical form: any criterion naming a VALUE owes two violations, one removing the carrier and one
altering the value.

declined: LIM-REMOVAL-ONLY-VIOLATION-SET, a new slug, because the shape is distinct from the two
adjacent parked classes. LIM-SWEEP-POSITIVE-CONTROL is an instrument never shown to find a known
member; here every row IS such a demonstration, and the defect is that they all perturb the same
way. The process mint freeze admits a process-facing row only under invariant or product-blocked,
and this is neither, so it parks with the slug and recurrence becomes one grep.

A second class, and the same arc found three instances of it: an assertion that passes for a reason
unrelated to the behaviour in its own name. AC-1c compared a grid element to its container, which a
block child satisfies at every width. The span card's body-vs-measure assertion sampled only
viewports where the card's padding kept the body under the measure. The measure pin accepted a
family where the criterion names one value. All three were caught by staging the violation, never by
review or by reading the code — including one written three commits after the finding that taught me
the class.

declined: LIM-NONDISCRIMINATING-FIXTURE, citing the existing slug rather than coining a twin; its
parked shape is precisely this, and its stated repair — give the assertion a contest it can lose — is
what all three repairs did. This arc is its fourth naming.

**Infra:** none. All four rounds returned a verdict on the first attempt, no reaped runs, no
`no_verdict`.
