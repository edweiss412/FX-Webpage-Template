# fix/mutation-harness-truth — review-round filing

## plan — 2 rounds

**Examined:** three plan-stage dispatches across two merge bases — round 1 at `34d8d0a121fe`
(BLOCKING, 8 findings) and rounds 1 and 2 at `8710aefc7a47` (BLOCKING/5 and BLOCKING/4). The
arc-sum rule counts distinct `(baseSha, round)` pairs across the branch directory, so the arc's
total is three where this file's own count is two, and the threshold is reached on the sum. Subject: `docs/superpowers/plans/ci/2026-09-01-mutation-harness-truth.md`, a plan for four
repairs to the source-mutation harness.

**Mechanizable:** declined: the 2026-08-25 process mint freeze binds a process-facing finding to
the owning surface's documented-limits record rather than the open queue, and this one is
process-facing with no product arc blocked by it. The class is recorded below and in the plan's own
"Red-command coverage" section, which is the owning surface; its re-file trigger is a
product-facing arc blocked by the gap.

Seventeen of the findings were one class, and it is mechanizable — a task's
`red=` claiming a verdict its command cannot observe. Round 1 raised it on Tasks 3, 6, 7 and 8;
round 2 on Tasks 5, 6 and 7, two of them on round 1's own repairs; round 3 on Tasks 2, 5 and 5
again, one of them on the coverage table round 2 had introduced as the fix. Each round's probe was
the same shape: take the task's command, take the defect its `why=` names, and check whether the
command can see that defect. That is a script, not a reading.

The plan now carries the derivation (a two-column walk of every task's `**Files:**` list against
the suite names in its own `red=`) AND an executable obligation, because round 3 showed the
derivation alone is still a prediction: every task demonstrates its red by planting the named
defect and recording that the same command fails for that reason. The obligation is the part that
would have caught all seventeen.

`spec:lint`'s `RED_TARGET_INVALID` already checks that a cited line is in range and, by its own
documented limit, never what is AT that line. This class is the next arm out from that one: not
"does the citation resolve" but "can the command see the thing the citation names". A repo-level
arm would take each `<!-- task: -->` marker, extract `red=` and `**Files:**`, and report any edited
file no suite in the command reads. That is not filed as a row — the 2026-08-25 process mint freeze
binds it to this record, and its re-file trigger is a product-facing arc blocked by the gap.

**Judgment:** the rest was genuine and not mechanizable. Round 1's finding that the E1 rate
exclusion rested on an unsettled premise was correct and could not have been scripted: the
mechanism I had asserted — that `workflow_dispatch` carries `refs/remotes/origin/*` refs a
`schedule` checkout lacks, so `fileOids` executes and the suite costs more — fails its own
arithmetic, because `fileOids` executing makes the mutant THROW and a thrown mutant bails at the
first case, which is cheaper. The claim predicted the dispatch run would be faster and it measured
2.2x slower. Round 3's arithmetic finding (two constructed cases demanding a failure at 3595 and at
3600, both compliant under the module's documented strict `>`) is the same character: a number
wrong in prose, catchable only by someone doing the addition.

**Infra:** none. Every dispatch returned a verdict; no reaper kill, no usage `no_verdict`, no
recovered rollout. The convergence gate blocked one dispatch for a missing acceptance-posture
phrase, which was the gate working — the bound was stated in a form its matcher does not accept,
and the fix was one clause.
