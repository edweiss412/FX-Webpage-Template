# fix/mutation-gate-fidelity — review rounds at base 75b8f7a3ec76

## spec — 4 rounds

**Examined:** R1–R4 at this base, the arc's only base. 13 findings declared, decaying 7 / 3 / 2 / 1. Every finding was correct, and every figure any of them asserted reproduced independently when re-derived here. No finding was refuted.

**Mechanizable:**

A spec that names TWO different proof mechanisms for one acceptance criterion. R4's single finding was exactly this: §3.1 and AC-1 had converged on an in-process injected registrar, while the §8 meta-test inventory still named the child-Vitest fixture that AC-1 says was dropped. Not inventory drift, because it leaves an implementer free to build the mechanism that does NOT close the escape three rounds were spent finding. A `spec:lint` arm could compare the mechanisms named in an AC's proof column against those named in the meta-test inventory and report a disagreement.

declined: this arc files no `BL-`/`DEF-` row of any facing (owner directive, 2026-08-25), so the gap is recorded here, on the surface that owns it, rather than in the queue.

The superseding-edit sweep exists and was not run. R4's finding is precisely the class `pnpm spec:lint --repair <rev> --superseded/--claim-about` was built to catch, and the miss was the author not invoking it after superseding the child-Vitest design.

declined: there is nothing to mechanize, because the mechanism already ships. The durable correction is procedural and belongs with the author: run the repair sweep after every edit that supersedes a named design, not only after one that changes a number.

**Judgment:**

- **R5's direction was reversed against the brief, on a measurement.** The arc brief proposed lowering `MUTANT_TIMEOUT_MS` from 180 s to 30 s on a bimodal distribution measured on `connectionCensus` alone. Pooling the run's 50 uploaded records (6961 non-timeout children) refuted the premise: 215 children sit inside the supposedly empty 10 s–180 s gulf, `psqlStartupScan` has a median of 31,054 ms, and 51 of the 128 children above 30 s exited ZERO. A 30 s ceiling would convert 51 genuine survivors into false kills. The brief's own measurement was sound for its surface and wrong for the pool. Escalated to the orchestrator rather than actioned silently.

- **The disposition of `BL-MUTATION-HARNESS-MAIN-RED` was an orchestrator call, not a review outcome.** R1 finding 7 correctly established that the row's required `workflow_dispatch`-on-`main` proof cannot exist before this PR's last commit. Whether to archive anyway was ratified by the orchestrator: archive, with the outstanding nightly named in the entry and the orchestrator owning the observation, on the grounds that a row whose only remaining step is watching a cron is the stale-queue shape. Both the ruling and the rejected alternative are recorded in the spec's §6.1 so neither is re-derived.

- **Three rounds went to one vector before the repair became structural.** R1, R2 and R3 each found that the proposed cover for the fault-isolation repair could not observe what it claimed to prove, and each repair was prose arguing the escape was unreachable. The structural-defense rule says to ship the mechanism at the first nameable recurrence; it was shipped at the third. The cost is two rounds. Recorded in the spec's §7.1.

**Infra:** the sandbox blocked the `pnpm tsx` verification commands the briefs supplied, with IPC/temp-directory `EPERM`, in R3 and R4 both; the reviewer substituted `node --import tsx` and reached the same results. The briefs should carry the `node --import tsx` form directly, since the `pnpm` wrapper is not runnable in the environment the reviewer actually has.

## plan — 4 rounds

**Examined:** R1–R4 at this base, the arc's only base. 10 findings declared, decaying 5 / 4 / 1 / 0, APPROVE at R4. Every finding was correct; none was refuted. Class sweeps on those findings turned up four further instances the rounds had not named, which are repaired in the same commits.

**Mechanizable:**

A declared `red=` that cannot be red for the stated reason. R1's first finding was a killing case whose command is green before AND after, because a case that kills a mutant passes against clean production the moment it is written. `spec:lint`'s red-contract arm checks that a `red-state=live` command currently exits non-zero, and that a `red-state=authored` marker names a production line — neither catches an `authored` marker whose new case will pass immediately on the current tree. A mutation-aware arm could notice that a task's `**Files:**` touch only a deciding suite of an enrolled surface, and require the marker's command to be a scored one.

declined: this arc files no `BL-`/`DEF-` row of any facing (owner directive, 2026-08-25), so the gap is recorded here, on the surface that owns it, rather than in the queue.

A hand-written list where a derivation exists. R3's only finding was a fourth enrolled surface missing from the scoring duty, found because the duty was enumerated. The registry already contains the answer — which rows name an edited file as `sourcePath` or `suitePath` — and the plan now carries that derivation with its output.

declined: the derivation is a ten-line script over an existing registry, and it lives in this plan where it is used. Promoting it to shipped tooling is a real option, but it would be a process-facing row, which the freeze declines by default and this arc's directive declines outright.

**Judgment:**

**Four rounds across spec and plan went to ONE vector before the repair became structural.** Spec R1, R2 and R3 and plan R2 all found the same shape: the proposed cover could not reach the path it claimed to prove. Each earlier repair argued the escape was unreachable; the fourth removed the second code path entirely, so the injected registrar and the production default are the same code after a `??`. The structural-defense rule says to ship the mechanism at the first nameable recurrence. Shipping it at the fourth cost three rounds, and it is the single largest identifiable cost in this arc.

**Two findings were about evidence that could not exist when it was demanded**, and both were mine to avoid. R1 finding 4 required a merge sha in a commit written before the merge; R2 finding 4 required a scheduled-run count from a run that happens after it. The shared shape is an acceptance criterion assigned to the arc that only the orchestrator can observe. The plan now separates what this arc can prove pre-merge from what its owner observes after, and says so in both documents.

**The lock booking moved twice, and both moves came from review rather than from measurement.** It began as three surfaces and one run, became three surfaces with `connectionCensus` run twice (R2: red and green must be the same command), and ended at four surfaces (R3: an edited file was a deciding suite of a surface nobody had listed). Booking scarce fleet capacity from a hand-written list is the same defect as scoring from one.

**Infra:** the sandbox blocked the `pnpm tsx` verification commands supplied in the spec-stage briefs, with IPC/temp-directory `EPERM`, in spec R3 and R4. The plan-stage briefs were written in the `node --import tsx` form from R1 onward and the reviewer reported no such blocks; that form is now the fleet convention.
