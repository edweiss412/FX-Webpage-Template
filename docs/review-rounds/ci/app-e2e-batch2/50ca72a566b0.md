# Round-economy filing — `ci/app-e2e-batch2` @ `50ca72a566b0`

Arc: batch 2 of `BL-E2E-APP-DEPENDENT-SPECS-CI-DARK`, the implementation session. This base is the merge-base after two ledger-only merges of `origin/main`; the rounds taken before them are filed at `0ba72c23774f.md`, and the rounds here are renumbered from 1 per the corpus's contiguity rule (`_roundAtPreviousBase` = 3, so this base's round *n* is the stage's round *n* + 3). The sibling `.jsonl` is the count; this preamble keeps no copy of it.

## diff — 9 rounds

**Examined:** scope B only (the sibling `.jsonl` holds how many, and the heading above derives from it) — the workflow, the executed-count oracle, the coverage registry, the allowlist and the docs — at heads `c7d8b943`, `1cdd02f7`, `c5a7a6da6`, `eb46d6eca`, `1e792370a` and `acc232a89`. Scope A had already converged (APPROVE, 0 findings, round 3 at the previous base) and its movement since is strictly subtractive: two members removed by the ratified AC-4 procedure, no scope-A file otherwise edited, so removal cannot introduce a defect in what remains. Scope B could not transfer that way, because its movement was not subtractive — the drops rewrote the run step, the `REQUIRED` table and the `governs` arrays, and AC-3's bar was re-scoped by orchestrator ruling — so a reviewer of record saw both.

R1 here (the transfer round): two findings, both accepted, both prose against a correct tree. Four narratives still described the pre-drop state, including a merge-corrupted phrase in the ledger; and `BL-ADMIN-LOADER-CI-TRANSIENT` claimed three runs while linking six and claimed a local CI-posture reproduction for every occurrence while supplying three. That row is what the amended AC-3 exception reads its signature from, so an overclaimed row is a live gate reading prose it cannot verify.

R2 here (the closing round): one finding, accepted — three MORE stale count claims the first repair pass missed, because that pass fixed the sentences it remembered instead of deriving the set.

R3 here: BLOCKING, two findings, both accepted. AC-3 had not been validly re-earned — a comment-only edit to `scripts/check-app-e2e-executed.mjs` landed between measurement triggers 3 and 4, and the restart predicate is path-based, so that five-run set spanned two trees; and the loader row's occurrence count had been copied into six narratives, all stale against its links, which was repaired by making every copy a derivation.

R4 here: BLOCKING, three findings, all accepted. The close-out still certified the superseded set; three more count claims were wrong (the plan's occurrence reference, the loader row's "three PRs" where its links span two, and the empty-state row's "five `toHaveScreenshot`" where the file has four and four baselines are committed); and `BL-MUTATION-SHARD-BUDGET-AGGREGATE-OVER` claimed run evidence it did not carry, now linking the last five failed `mutation-harness` runs on `main` and labelling the routed shard figures as routed. Section 12 no longer certifies AC-3 at all until a set from one tree exists.

R5 here (head `c5a7a6da6`): one finding, accepted. The fourth AC-4 drop had not reached every scoped narrative. Spec section 4 and plan section 12 still described three drops and eleven members; the oracle comment assigned all four drops to `BL-ADMIN-LOADER-CI-TRANSIENT` where the allowlist records `published-show-attention`'s decisive second red as the separate gateway-502 family; and the plan's occurrence figure sat beside a row linking more runs than it named.

R6 here (head `eb46d6eca`): one finding, accepted, and it was five more contradictory counts. Repaired at `1e792370a` by turning each into a pointer rather than a corrected number: the allowlist rows are the authority for which spec left, the run step for which members ride, the loader row for how many occurrences there are, and the list itself for how many superseded sets exist.

R7 here (head `1e792370a`): one finding, accepted in four parts and refuted in one. The refutation is recorded at `acc232a89` with the reading that produces it. The oracle comment's "the authority for all three" enumerates three facts in one sentence, which spec left, on which runs, to which class, and not three specs, so `rg -c 'LEFT BATCH 2 UNDER AC-4'` printing 4 counts restored rows and not that phrase's referent. It also stands because editing that file would have retired a five-green set to change a word.

R8 here (head `acc232a89`): one finding, accepted. `app-e2e.yml`'s header enumerated the shipped members and then quoted "Seven were green on a run; seven took a named test-only repair first", figures that describe the fourteen candidates rather than the ten above them. Repaired at `3203faa98` by deleting the copy instead of correcting it, the same shape as the oracle comment's repair. The cost was paid rather than dodged: the header is a non-docs path, so the earned set was retired as superseded set 4 and the five-green count restarted.

R9 here (head `5b479ff4c`): one finding, accepted, and it was this filing. The R5 to R8 write-up landed with a summary sentence claiming every round at this base returned exactly one declared finding, which the sibling `.jsonl` contradicts at rounds 1 to 4. A bare count beside its own named authority, written into the very document whose `Mechanizable` line names that class, one commit after the rest of the file was pointer-ised. The reviewer's changed-count sweep found no other in-domain contradiction and reproduced the wiring clean: the workflow, `REQUIRED` and every `governs` set resolving to the same 19 specs, their floor sum 158, no workflow member left allowlisted, the loader row's links unique, and four AC-4 markers standing.

After the transfer round, every finding at this base was prose. The sibling `.jsonl` holds each round's declared count and this line keeps no copy of it, which is the same repair the rest of this filing took and which this sentence had to take twice. Each reviewer ran the executable probes and reported the wiring reproducing clean at the head it read: the workflow and oracle resolving to the same spec set and the same live identity total, the batch-2 membership, the census arithmetic, and the governance and env-pair checks. No round after R1 found a defect in what this arc ships.

**Mechanizable:** `BL-CLOSEOUT-COUNT-PROSE-DRIFT`. It is what this base's rounds are made of, and the sibling `.jsonl` says how many that is: a count stated in prose beside a tree that moved, found again each round in whichever narrative the last repair pass did not reach. The repair pattern that works is the one the oracle's header comment now uses — state no count, name the table that holds it — and the general form is a check that greps every numeral in an arc's close-out against a command that produces it, which is what `pnpm intraleg:claims` already does for one arc's campaign figures.

**Judgment:** none new at this base. The two judgment calls this arc needed (the loader-class classifier correction, and the AC-3 re-scope that followed it) are the orchestrator's and are recorded in the previous base's filing and in the plan's close-out.

**Infra:** both reviewers reported the same sandbox limit — vitest could not collect because its temporary SSR directory was write-denied — and both said so rather than implying a suite pass; their Playwright `--list`, census and identity probes ran and are quoted in the rounds.
