# Round-economy filing — `ci/app-e2e-batch2` @ `0ba72c23774f`

Arc: batch 2 of `BL-E2E-APP-DEPENDENT-SPECS-CI-DARK`, the spec+plan session (implementation is a separate pane). Spec: `docs/superpowers/specs/ci/2026-08-21-app-e2e-batch2-design.md`.

## spec — 4 rounds

**Examined:** the spec and its probe record on each round, at the head the reviewer named (`0038d5b28`, `c15ac178f`, `0f75cc6d9`, then round 4's head). Every round carried one finding, each on a different axis, and each reviewer reproduced the census, the Playwright resolution and the identity counts by command before filing.

R1: the class-C reclassification sweep stopped at five rows because it matched the invocation FORM (project-only run steps) where the class is "already run by a path-filtered workflow through an invocation the scanner cannot see"; `screenshots-drift.yml`'s `pnpm screenshot:help` hop was the invisible member. Accepted; repaired for the whole class (seven). R2: the closeout census said total 51 where deleting fourteen member rows makes it 37; the ledger row's own text warns that deletions move the total and reclassifications do not, and the spec's newest editor still did it. Accepted; every bucket is now derived and printed. R3: R7's pre-hydration click loss read as an app defect rather than test staleness. Refuted by orchestrator ruling on the ratified precedent (`docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:309` repaired this exact class test-side; `docs/agents/writing-plans.md:31` mandates the gate), the window recorded as a spec section-9 documented limit with run 3's trace verbatim and two re-file triggers. R4: the section-2 census pipeline printed an unlabeled `3` for the custom-reason rows (their values sit on continuation lines) while the draft quoted a labelled line the command never produced: a quoted output not taken from the instrument. Accepted; the command now classifies by value token, is pasted from the run with a positive control, and the plan's closeout assertion uses the same classifier. The fourth counted round; this filing is owed at it. Stage closed DISPOSITIONED, not CONVERGED, by orchestrator ruling at the cap: the residue is a fact a command settles, so a fifth reader would verify what the command already does.

**Mechanizable:** none. The R2 and R4 classes (a hand-restated census; a quoted output the printed command does not produce) are closed by the repair itself: the spec's closeout numbers are produced by the two commands printed beside them, pasted from a run with a positive control, and the plan's Task 11 asserts every bucket through the same classifier, so a future restatement or mis-quotation fails by command. The R1 class (a sweep matching a form instead of a class) has no cheaper mechanical form than the one the spec now carries, the evidence commands listed per row.

**Judgment:** R3. Whether a pre-hydration click loss on a server-rendered control is a harness-readiness concern (the repo's ratified stance) or a product defect is a classification the orchestrator owns under the arc's app-defect fence; it cost one round and one disposition call, and the spec records the outcome as a documented limit with a re-file trigger, so a later arc reads the ruling instead of re-deriving it.

**Infra:** the r2 and r3 reviewers could not start vitest in the review sandbox (temporary-directory write denied); both said so and reviewed statically, which is recorded here and in the corpus rows rather than laundered into an executable APPROVE.

## plan — 4 rounds

**Examined:** the plan at the head each reviewer named (`e6cbc9630`, `3a32a44eb`, `b4be54e15`, then round 4's head), with the spec and the probe record as the subject's contract. Every round reproduced the census by command; no round could replay Playwright `--list` in the review sandbox and each said so.

R1 (four findings, all accepted): Task 12's CI gate had two `gh api` calls and no predicate; Task 12 dispatched one diff round with no convergence branch; Task 4 re-derived the badge text where spec R4 requires the +1 and -1 deltas asserted; three commands carried a literal `…` where an executable line belonged (two named, the third found by the sweep). R2 (four, all accepted): the R1 Task 4 repair left the two initial literal assertions standing, so its own GREEN could not pass and the rendered transition went unproved; the AC-4 fallback stopped at Task 8 while Tasks 9 and 11 assumed fourteen; Task 10 inherited the batch-1 loop's hardcoded `--branch ci/app-e2e-batch1`; Task 10's ceiling selector printed nothing and exited zero on an absent job. R3 (two, both accepted): Task 4's badge oracle named the `wizard_session_id` filter on one table where production filters both, and prescribed no call-boundary contract (the spec's own R4 sentence carried the same one-table wording and was corrected as a citation fix); Task 12's five-green restart set was an enumeration that omitted `playwright.config.ts`, replaced by a `git diff --name-only` derivation. R4, the cap (one, accepted): Task 2 repaired one of the two assertions on the retired `/show/<slug>` path in a single case, so its GREEN was unreachable; the repair now names both sites and pastes the `rg` sweep that bounds them, and the same sweep was run for every other repair task's retired literal (one site each).

**Mechanizable:** the round-4 shape, a repair task naming one site of a literal that recurs in the same file, has the cheapest mechanical form of any here: every repair bullet carries an `rg -n -F <literal> <file>` line with its expected site count, which Task 2 now does and the other repair tasks were checked against by hand. The R2 shapes have a cheap mechanical form too: a plan step whose measurement command can print nothing and exit zero (the ceiling selector, and Task 12's required-set read the sweep caught) is detectable by a lint that flags a `--jq` selector or `grep -o` in a measurement line without an `-s`/length/`error(` guard beside it; and an inherited `--branch <literal>` in a plan is a copy-chain literal `spec:lint` could compare against the plan's own branch.

declined: none of the three is filed as a row. Each would be its own process-facing row needing an incident beyond this arc (the mint bar wants the incident before the row), and every instance this arc produced is repaired in-plan.

**Judgment:** none. Every finding was a repair defect or a measurement-shape defect inside the stated fence; no round re-opened a design decision, and no disposition needed the orchestrator.

**Infra:** the review sandbox denied Playwright's transform-cache writes on every round, so `--list` was never replayed by a reviewer; each relied on the probe record's dated resolution, which held because no code, workflow, script or Playwright config changed between the probe head and any reviewed head.
