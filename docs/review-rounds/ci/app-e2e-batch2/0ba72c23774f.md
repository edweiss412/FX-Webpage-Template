# Round-economy filing — `ci/app-e2e-batch2` @ `0ba72c23774f`

Arc: batch 2 of `BL-E2E-APP-DEPENDENT-SPECS-CI-DARK`, the spec+plan session (implementation is a separate pane). Spec: `docs/superpowers/specs/ci/2026-08-21-app-e2e-batch2-design.md`.

## spec — 4 rounds

**Examined:** the spec and its probe record on each round, at the head the reviewer named (`0038d5b28`, `c15ac178f`, `0f75cc6d9`, then round 4's head). Every round carried one finding, each on a different axis, and each reviewer reproduced the census, the Playwright resolution and the identity counts by command before filing.

R1: the class-C reclassification sweep stopped at five rows because it matched the invocation FORM (project-only run steps) where the class is "already run by a path-filtered workflow through an invocation the scanner cannot see"; `screenshots-drift.yml`'s `pnpm screenshot:help` hop was the invisible member. Accepted; repaired for the whole class (seven). R2: the closeout census said total 51 where deleting fourteen member rows makes it 37; the ledger row's own text warns that deletions move the total and reclassifications do not, and the spec's newest editor still did it. Accepted; every bucket is now derived and printed. R3: R7's pre-hydration click loss read as an app defect rather than test staleness. Refuted by orchestrator ruling on the ratified precedent (`docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:309` repaired this exact class test-side; `docs/agents/writing-plans.md:31` mandates the gate), the window recorded as a spec section-9 documented limit with run 3's trace verbatim and two re-file triggers. R4: the cap round, dispatched with the ruling fenced and its falsifier (the click still lost with the gate present) kept admissible; its row is the fourth counted round this filing is owed for.

**Mechanizable:** none. The R2 class (a hand-restated census) is already closed by the repair itself: the spec's closeout numbers are produced by the two commands printed beside them and the plan's Task 11 asserts every bucket by command, so a future restatement cannot pass silently. The R1 class (a sweep matching a form instead of a class) has no cheaper mechanical form than the one the spec now carries, the evidence commands listed per row.

**Judgment:** R3. Whether a pre-hydration click loss on a server-rendered control is a harness-readiness concern (the repo's ratified stance) or a product defect is a classification the orchestrator owns under the arc's app-defect fence; it cost one round and one disposition call, and the spec records the outcome as a documented limit with a re-file trigger, so a later arc reads the ruling instead of re-deriving it.

**Infra:** the r2 and r3 reviewers could not start vitest in the review sandbox (temporary-directory write denied); both said so and reviewed statically, which is recorded here and in the corpus rows rather than laundered into an executable APPROVE.
