## spec — 4 rounds

**Examined:** the arc B review-infra spec train (R1 BLOCKING 8, R2 BLOCKING 9, R3 BLOCKING 6, R4 APPROVE 0). Dominated by guard-contract precision: baseline identity/growth semantics, waiver scoping, probe fidelity (nested fences, truncated output, iterating declared-vs-imported), grammar-coverage split, and AC/§4 lockstep drift after repairs.

**Mechanizable:** (a) the AC/limits-ledger drift class (R2 F5/F6/F9, R3 F4/F6) is exactly the self-consistency sweep the spec-self-review mandates per round — the misses were non-compliance, not rule gaps; running the numeric+cross-ref sweep mechanically over the DIFF of each repair commit would have caught all five. (b) Probe scripts feeding a spec's calibration table deserve their own mini-review (the declared-vs-imported iteration bug and the truncation both shipped in a probe the spec then quoted as fact).

**Judgment:** the baseline growth-semantics chain (R1 F1 → R2 F1 → R3 F1) was a genuine design negotiation ending at an honest strength statement — adversary-shaped, three rounds well spent.
