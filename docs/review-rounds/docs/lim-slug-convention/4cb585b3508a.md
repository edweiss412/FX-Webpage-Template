# docs/lim-slug-convention — review-round filing

## diff — 4 rounds

**Examined:** R1–R4, 6 findings. R1: the AGENTS.md pointer conflated a slug's arc count with the freeze's admission-test number, and LIM-AUTHORED-RED overstated `lib/specLint/redContract.ts` coverage. R2: the narrowed coverage claim was still broader than the implementation, and the index claimed completeness while parked classes probed from merge-base filings were missing. R3: an owning record miscounted a cited note's line count. R4: one section fused two of its source filing's instances and reinstated a consumer count the filing itself had corrected away.

**Mechanizable:**

- Five of the six findings are one class, restatement drift: index or pointer prose restating a cited artifact (an implementation file, a filing, a note) and misstating it.

declined: LIM-INDEX-RESTATEMENT-DRIFT, an index-vs-artifact comparison lint is exactly the gate surface the convention's own fence rules out (the README states nothing validates the index, deliberately, per the freeze's measuring-tool warning), and the repair direction that converged across rounds was narrowing the claims, not widening a checker.

**Judgment:** R1's admission-test conflation was a wording call between two ratified texts, repaired by keeping the done-condition number external in both. R2's completeness finding was repaired in both directions at once: the totality claim was dropped and the enumerated classes were indexed.

**Infra:** none.
