## spec — 4 rounds

**Examined:** the arc C quick-wins spec train (R1 NEEDS-ATTENTION 1 LOW, R2 BLOCKING 1, R3 BLOCKING 2, R4 APPROVE 0). R2's blocker was the arc's one real design catch: the ratified retain-symmetry mechanics would have upserted a frozen hold snapshot over live rows — found by the reviewer's in-memory mutant probe, repaired by redesign to a live-row retain.

**Mechanizable:** R3 F1 (vacuous staleness oracle — live and held fixtures sharing a default) is exactly the guard-premise class `tests/_shared/premise.ts` exists for; a plan-time rule "every new oracle asserts its own divergence premise" is already codified in writing-plans — the miss was not running that check against the DRAFTED test design before dispatch. Checkable at authoring time by grepping drafted assertions for a paired premise.

**Judgment:** R2's stale-overwrite catch required tracing the retain through the upsert contract — cross-file consequence analysis, adversary-shaped. The redesign it forced (live-row retain + filed mi11 sibling) is the round economy working as intended.

## plan — 4 rounds

**Examined:** the arc C plan train (R1 BLOCKING 6, R2 BLOCKING 4, R3 BLOCKING 4, R4 NEEDS-ATTENTION 1 — repaired, converged at cap). Findings were execution-fidelity: harness inheritance mechanics (inline blocks vs helpers, port hardcodes, hydration helpers), gate-check robustness, final-diff review ordering.

**Mechanizable:** two shapes recur across this batch's plan reviews and belong in the writing-plans checklist as named checks: (a) "review covers what merges" — the final-diff ordering defect appeared independently in two arcs and was only caught by review; (b) "the handoff's gate check must read anchored state from origin, not bounded log output". Both are one-line lint rules over HANDOFF/plan text.

**Judgment:** the E2E_PORT relocation vs hardcoded BASE_URL catch (R3 F1) required cross-referencing the config's env plumbing against the spec file's constants — adversary-shaped, and exactly the silent-wrong-server class the mutant protocol exists to prevent.
