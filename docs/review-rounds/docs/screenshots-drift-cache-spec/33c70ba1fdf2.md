# Review-round filing — docs/screenshots-drift-cache-spec

## spec — 5 rounds

**Examined:** the spec rounds to date on `docs/superpowers/specs/ci/2026-08-15-screenshots-drift-cache-refresh-design.md` (R1 BLOCKING/5, R2 BLOCKING/4, R3 NEEDS-ATTENTION/3, R4 NEEDS-ATTENTION/1, R5 APPROVE/0). R1 was the load-bearing round: it refuted the scope brief's recommended default (always-save on the prefix-fallback key) with the entry's own same-sha evidence — Next reused stale compilation for changed content, so an always-save would PROPAGATE the poisoned cache rather than converge it — and the arc reversed to the entry's direction 1 (exact input-hash key, no fallback). R2-R4 then hardened that design against GitHub Actions semantics the drafts had asserted from intuition: save-time key re-evaluation over capture-mutated baselines (R2 F1), content-only `hashFiles` (R2 F2), fail-fast shell hiding untracked names (R3 F2), the default `success()` condition skipping the drift check after a capture death (R4 F1).

**Mechanizable:** none beyond what already exists — every R2-R4 finding was an external-semantics claim (actions/cache save behavior, `hashFiles` implementation, workflow shell/status-check semantics) that no repo-local linter can check; the effective countermeasure is the already-standing rule to cite the platform documentation or a reproducible run for every external-API claim before drafting (`feedback_verify_review_findings_against_external_api_spec`), applied at authoring time rather than review time.

**Judgment:** the R1 direction reversal is the round that justified the review's existence: a spec ratifying the brief's recommended default survived self-review and would have shipped a repair that entrenches the incident's poisoned-cache state. The reviewer's probe-first posture (its F1/F2 both carried probe output) matched the round-economy contract, and the arc's own H2 derivation from the md5 evidence confirmed rather than argued.

## plan — 5 rounds

**Examined:** the plan rounds on `docs/superpowers/plans/2026-08-15-screenshots-drift-cache/plan.md` (R1 NEEDS-ATTENTION/5, R2 NEEDS-ATTENTION/3, R3 NEEDS-ATTENTION/2, R4 NEEDS-ATTENTION/1, R5 APPROVE/0). R1's five were plan-mechanics classics — a pin assertion lost to an aborted edit script, a bounded-log gate, an exit-inverted terminal grep, a commit-less task, a read-order gap; R2-R4 each narrowed one seam (four-mutant treatment + parsed-YAML extraction; evidence-task framing for the dispatch proofs plus a refuted invariant-12 misread, recorded in the plan; the comment-checking assertion's parse-vs-raw split).

**Mechanizable:** (1) the lost-edit shape (an aborted multi-replace script discarding its successful replaces) recurred across this batch — the durable fix is the asserted-replace discipline plus writing each repair as its own scripted transaction; (2) `! grep -q` as the canonical zero-match gate form belongs in the writing-plans gate examples — the `grep -c` inversion has now cost two arcs a round each.

**Judgment:** R3 F2's invariant-12 claim was REFUTED rather than repaired (the graduating-entry clause binds the strip to the archive commit); the refutation lives in the plan text so no later reviewer re-derives it — the modal-header close-out §11 pattern.
