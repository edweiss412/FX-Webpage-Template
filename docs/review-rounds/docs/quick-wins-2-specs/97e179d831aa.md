# Review-round economy — `docs/quick-wins-2-specs` (quick-wins-2 authoring, base `97e179d831aa`)

## spec — 4 rounds

**Examined:** all mech-spec dispatches in `97e179d831aa.jsonl` (R1 BLOCKING 6, R2 NEEDS-ATTENTION 2, R3 NEEDS-ATTENTION 2, R4 APPROVE 0), plus the sibling UI-spec train (R1 BLOCKING 9, R2 NEEDS-ATTENTION 2, R3 APPROVE 0) recorded in the same corpus file.

**Judgment:** the round count was driven by genuinely probe-refuted design premises, not drip-feed — R1 alone killed two authoring theories with live probes (the `-runtime`-indirection lint-gap theory: 29 of 35 such tokens ARE canonicalized; the impossible internal-padding footer geometry), and R2/R3 each surfaced a real mechanism the prior repair could not have known (Tailwind breakpoint tokens resolve DIFFERENT tokens through their would-be canonical utilities; libpq `?service=` + default-service-file steering that survives hostname parsing and `PG*` scrubbing). Every finding arrived with the reviewer's own probe output and every repair was verified rather than re-derived in the following round. Findings-per-round decayed 15 → 4 → 2 → 0.

**Mechanizable:** one shape recurred twice and is worth automating — "sweep table built from truncated command output" (the R1 F3 `head -10` census error). A spec-lint advisory that flags a disposition table whose stated command's live hit count differs from the table's row count would have caught it at authoring time. Filed as a candidate only; not opened as a ledger row from this filing.

**Infra:** none — all seven dispatches returned verdicts on attempt 1 with no reaper interference (native binary + heartbeat path).

## plan — 4 rounds

**Examined:** all four plan dispatches (R1 BLOCKING 14, R2 BLOCKING 6, R3 NEEDS-ATTENTION 2, R4 APPROVE 0) in `97e179d831aa.jsonl`.

**Judgment:** R1's fourteen were dominated by genuinely non-discriminating REDs (archive greps satisfied by prose cross-references, a `test -f` that proves nothing, a grep for a phrase that wraps mid-line) and two real contract gaps (no branch-A closeout task; the invariant-8 marker grammar). R2/R3 were the residue of those repairs (an impossible step-3 color oracle the reviewer refuted from production source; a second advisory-lock holder the topology table missed). Every finding carried the reviewer's own probe; findings decayed 14 → 6 → 2 → 0.

**Mechanizable:** "RED command validated against the live tree at plan time" — a spec-lint arm that RUNS each task's `red=` command and reports any that already exits 0 would have caught four of R1's fourteen and both halves of R2 F1 mechanically. Candidate only; not opened as a ledger row from this filing.

**Infra:** none — verdicts on attempt 1 across all four dispatches.
