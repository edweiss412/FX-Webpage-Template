# Review-round convergence filing — docs/help-refanchor-a11y-spec @ fafa354accf0

## spec — 6 rounds

**Examined:** the five spec rounds on the RefAnchor a11y spec (R1: 2 findings — untested fallback branch, missing file:line anchors; R2: 1 — vacuous console-error timer-cleanup oracle, refuted by the reviewer's own runtime probe; R3: 2 — region parentage unasserted, loose skip-path ordering/containment oracles; R4: 2 — settlement timing not discriminating without deferred promises, skip-link visibility/tap-floor prose-only; R5: 1 — an overclaimed precedent citation; R6: APPROVE, 0 findings). Every finding was a TEST-SHAPE finding against §3's acceptance obligations — none touched the three repairs' design, the §1.1 fences, or a code claim; the design converged at R1 and the rounds priced the oracle set.

**Mechanizable:** a §3-style "test obligations" section is a recognizer for passing counterfeits, and four of the seven findings name a counterfeit a static rule could screen at authoring time: (a) any spec test asserting on a promise-driven UI state without a deferred-promise case (R4 F1); (b) any "assert no console error" oracle — React ≥18 emits nothing on set-state-after-unmount, so the shape is vacuous by construction (R2 F1); (c) a containment assertion (`contains`) where the acceptance criterion says adjacency or first-descendant (R3 F2); (d) an ordering assertion anchored to a mid-list element instead of index 0 (R3 F2). These are spec-lint candidate rules over test-obligation prose, not code guards; filing them as speclint arms is only worth it if a second arc burns rounds on the same shapes — none filed now.

**Judgment:** the R1 citation-format finding (add `file:line` everywhere) traded against the project's own "line numbers rot" rule; resolved by carrying both (locator + the drafting-time caveat), which later rounds accepted. The R5 brief's past-cap fence ("a new test-strengthening axis is a re-scope decision, not a round") is the closed-criterion form this corpus' own README prescribes; it was stated only at R5 — stating it at R3 would likely have saved a round.

## plan — 5 rounds

**Examined:** five plan rounds (R1: 5 findings — graduation-after-merge ordering, six omitted spec obligations, premise on an adjacent catalog subset, stale red reason, bounded-log handoff gate; R2: 5 — gate-half rerun staleness, stranded-announcement transition, aria-label-only counterfeit, fail-open status greps, cron-id ordering; R3: 3 — timer-rule contradiction, review-not-on-merging-diff, invariant-12 wording; R4: 1 — merge over dirty ledger edits; R5: APPROVE, 0). All findings were sequencing/executability findings against the plan's own steps — none re-opened the approved spec's design.

**Mechanizable:** two shapes recur across arcs and could lint: (a) a plan step that edits a file the SAME task later merges from origin (dirty-tree merge abort — R4's finding is a static order check over "edit X" preceding "merge origin/main" within one task); (b) a handoff gate grep matching a bare token rather than a field shape (R2 F4 — the gate-grep-precision lint shape iii already names this; a speclint arm over `git show origin/main:` + `grep -q "<word>"` without an anchor would have caught it at authoring).

**Judgment:** R1's BLOCKING on graduation ordering traded against review-covers-what-merges; settled by invariant 12's own graduation clause (marker off in the archive commit, repairs after are lawful), which R3/R4 then refined to merge-first-on-clean-tree. The right order was discoverable from AGENTS.md at draft time; the rounds priced my not deriving it up front.
