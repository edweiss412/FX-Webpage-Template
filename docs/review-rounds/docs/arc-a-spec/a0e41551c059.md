## spec — 5 rounds

**Examined:** the arc A a11y/privacy spec train (R1 BLOCKING 9, R2 BLOCKING 4, R3 BLOCKING 2, R4 NEEDS-ATTENTION 1 MEDIUM, R5 NEEDS-ATTENTION 1 LOW; every finding probe-confirmed and repaired in-branch; converged after the confirmation round ended on a single repaired LOW).

**Mechanizable:** the R1 class was dominated by pre-draft-citation gaps a deeper first-pass probe would have caught (the raw-fallback flight path, the viewerContext fallback semantics, the report-status gate anatomy) — the three-agent citation pass read the named sites but not every RENDER PATH of the named component; a per-component render-path enumeration step (every expression that can emit the protected data class, not just the cited lines) in the pre-draft pass would have cut R1 from 9 to roughly 3.

**Judgment:** R2-R5 were genuine design refinements (collapse-mid-flight semantics, post-suppression visibility uniformity, the assigned-names content rule) — the class of finding that needs an adversary, not a checklist; the declining-severity train is the intended shape.

## plan — 4 rounds

**Examined:** the arc A plan train (R1 BLOCKING 8, R2 BLOCKING 6, R3 BLOCKING 5, R4 NEEDS-ATTENTION 2 — repaired, converged at cap on a declining train). Dominated by execution fidelity: meta-suite count reconciliation, TDD ordering (behavioural REDs before repairs, tautology in a provider-fixture RED), graduation provenance mechanics, e2e fixture reachability, and reconciliation-sweep completeness.

**Mechanizable:** (a) the RED-is-tautological class (R2 F2, R3 F1) is checkable at authoring: for every planned RED, ask "which production line makes this fail?" — a RED whose failure derives from a test-local fixture is invalid by construction; (b) count-reconciliation across a registry-bearing meta-suite (R2 F1) is a mechanical diff of the registry arrays against the tasks' removals — a plan-lint rule shape.

**Judgment:** the e2e fixture-reachability catch (R2 F4: no seeded show could render the share-link row) and the graduation provenance contract (R3 F4) required reading the harness and guard implementations against the plan's claims — adversary work, both real execution blockers caught before an implementer burned a session on them.
