## spec — 5 rounds

**Examined:** the arc A a11y/privacy spec train (R1 BLOCKING 9, R2 BLOCKING 4, R3 BLOCKING 2, R4 NEEDS-ATTENTION 1 MEDIUM, R5 NEEDS-ATTENTION 1 LOW; every finding probe-confirmed and repaired in-branch; converged after the confirmation round ended on a single repaired LOW).

**Mechanizable:** the R1 class was dominated by pre-draft-citation gaps a deeper first-pass probe would have caught (the raw-fallback flight path, the viewerContext fallback semantics, the report-status gate anatomy) — the three-agent citation pass read the named sites but not every RENDER PATH of the named component; a per-component render-path enumeration step (every expression that can emit the protected data class, not just the cited lines) in the pre-draft pass would have cut R1 from 9 to roughly 3.

**Judgment:** R2-R5 were genuine design refinements (collapse-mid-flight semantics, post-suppression visibility uniformity, the assigned-names content rule) — the class of finding that needs an adversary, not a checklist; the declining-severity train is the intended shape.
