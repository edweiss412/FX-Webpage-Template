# Round-economy filing — docs/m-wave-2-spec

## spec — 6 rounds

**Examined:** all six verdict rounds on `docs/superpowers/specs/2026-08-09-m-wave-2-design.md` (findings 7 → 4 → 4 → 2 → 2 → 0, converged APPROVE at R6; every finding probe-backed; zero relitigated vectors — each round's findings were on surfaces the prior repair had not touched). One additional dispatch aborted with no verdict (background-task SIGTERM at 643s, the harness's 600s cap on backgrounded Bash; re-dispatched detached via nohup, which is the durable pattern).

**Mechanizable:** two shapes recur across waves and could move into `pnpm spec:lint`: (a) "writer census completeness" — a spec naming a DB column's writers can be checked against `rg` over the column name for unlisted writers (r1 F1's backfill script, r3 F1's admin adapter were both this shape); (b) "guard population circularity" — a spec whose acceptance test derives from the same hand-authored sweep it validates (r3 F4) is textually detectable when the spec lacks a committed-scanner clause. Neither is filed as a ledger row yet; both are candidates for the next spec-lint pass.

**Judgment:** the wide-pool wave-spec format (16 work items, one document) concentrates review value in rounds 1-2 (cross-item consistency, per-entry contract vs entry body) but keeps producing per-item findings through round 4 because each item is its own surface — a per-unit spec split would trade 4 wave rounds for ~6 unit rounds and lose the cross-item checks. The wave format stays, priced at ~4 rounds.

**Infra:** the first dispatch of the arc died to the 600s background-task cap (recorded above); detached `nohup … & disown` dispatch is the reliable form on this machine and is what `docs/agents/codex-silent-death-2026-07-24.md` §8's heartbeat cannot cover (the kill came from the harness task runner, not the codex reaper).

## plan — 6 rounds

**Examined:** all six verdict rounds on `docs/superpowers/plans/2026-08-09-m-wave-2/plan.md` (findings 12 → 9 → 4 → 2 → 1 → 0, converged APPROVE at R6; every finding probe-backed). Dominant classes: census integrity (lock-holder enumeration completed over three rounds — lib, then app routes, then the last two in-route holders; z census settled only when the deriving instrument, not a grep, was declared the census of record) and RED validity (four tasks re-ordered to production-behavior REDs).

**Mechanizable:** (a) lock-holder census completeness is a one-command check (`rg pg_advisory app lib scripts`) a plan-lint rule could enforce against any plan containing an advisory-lock table; (b) "raw grep presented as census" is detectable when a plan states a count next to a command whose output includes comments — same derived-cover rule the class-sweep bullet already prescribes.

**Judgment:** plan rounds for a six-unit wave priced at ~6; the census/RED classes were front-loaded 1-2 and the tail was pure count-integrity. The wave-plan format held.

**Infra:** all six dispatches ran detached (nohup) after the r1 SIGTERM lesson; zero infra faults this stage.
