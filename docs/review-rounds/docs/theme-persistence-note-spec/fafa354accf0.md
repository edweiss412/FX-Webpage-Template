# docs/theme-persistence-note-spec @ fafa354accf0

## spec — 4 rounds

**Examined:** `docs/superpowers/specs/2026-08-15-theme-persistence-note-design.md` — the `persistFailed` hook state's survival across the mount sync, the full consumer census for the note's anchoring, and the geometry of the anchored bubble at the narrowest live viewport, against the live `components/layout/` + `components/auth/` tree. Four findings across four rounds (2 + 1 + 1 + 0), each confirmed against the live tree, none refuted.

**Judgment:** One genuinely underivable surface drove rounds 2-3: placement geometry. The R1 batch caught the mount-sync state wipe (the effect's object literal drops `persistFailed` — repaired with the functional update) and the incomplete consumer census (the AdminNav 320px action cluster and the help header were missing from a two-consumer draft). R2 showed the in-flow note displaces the very control it annotates inside the 320px cluster — repaired by taking the note out of flow (anchored bubble). R3 measured the bubble clipping at the help header's right edge (224px of bubble against roughly 161px of available viewport — repaired with the derived max-width plus break-words and a real-browser containment AC). R4 returned zero and approved. Each round's repair was a probe-measured geometry fact, not a relitigated opinion; the census sweep in R1 is the class-sweep rule doing its job on a spec.

**Mechanizable:** Partially. The consumer-census class ("a spec names N consumers; the live tree has M > N") is greppable per component (`rg '<ThemeToggle'`) and the round-1 fix encoded exactly that derivation into the spec's §1.1 census item; future specs touching a shared component should run the usage grep before drafting (the live-code-citation rule already says this — the miss was applying it to consumers, not just APIs). The viewport-clipping class needs a rendered measurement and stays judgment plus the AC-10b real-browser containment proof the spec now mandates.
