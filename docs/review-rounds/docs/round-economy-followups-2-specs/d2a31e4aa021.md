# Round-economy filing — docs/round-economy-followups-2-specs @ d2a31e4aa021

## spec — 10 rounds

**Examined:** R1 (NEEDS-ATTENTION, 3), R2 (NEEDS-ATTENTION, 2), R3 (NEEDS-ATTENTION, 3; the
first R3 dispatch was interrupted by the harness background-task SIGTERM, recorded
`no_verdict`, uncounted; re-dispatched detached), R4 (NEEDS-ATTENTION, 1), R5
(NEEDS-ATTENTION, 2), R6 (NEEDS-ATTENTION, 1), R7 (NEEDS-ATTENTION, 1: the spec's own
file-scope claims excluded the corpus record the branch must add), R8 (NEEDS-ATTENTION, 1:
the P3 arm omitted its accept-set escapes), R9 (NEEDS-ATTENTION, 1: an R8 repair quotation
nested straight apostrophes, falsifying AC-3), R10 (APPROVE, 0). Subject: a docs-only
promotion spec whose every claim is about merged filings and live tree anchors.

Rounds 4-6 were one vector: the P1 red-marker taxonomy. R4 showed the drafted rule rejects
ordinary TDD (a red= targeting a test its task creates); the R4 repair's exists-vs-created
split then failed R5 on the add-a-case shape and on a mislabeled never-red example the merged
plan itself contradicts. The R5 repair applied the same-vector rule's comprehensive form —
the rule was re-derived from the invariant (red-then-green ON THE SAME COMMAND) so shapes
classify by whether the cycle can complete — and the R6 finding was pure label residue in two
reachability summaries. Three rounds to learn what one derivation-first draft would have
avoided: state the contract, then let categories fall out, rather than enumerate categories.

**Judgment:** every finding was probe-backed and none was reversed. R1 killed three factual
defects (an "exported" attribute the declaration lacks, a filing count off by one against its
own enumerated list, a false enrolment history). R3's three were second-order — each opened
by an R1/R2 repair: the truthful-history rewrite introduced a registry-expressibility claim
a deeper probe (no exports, terminal `process.exit`, no importing suite) refuted; the honest
AC-3 rewrite carried a quotation taxonomy ten advisories narrower than the tool's output.
R4's single finding was the sharpest and the most valuable: P1 as drafted would have REJECTED
ordinary TDD, probed against the classname source arc's own plan where that exact shape is
valid. A reviewer probing the rule against the very arc it cites is cross-model review
earning its round; a lint arm built from the unrepaired wording would have blocked every
new-test plan in the repo.

**Mechanizable:** none new — the shapes this train surfaced are the ones the spec itself
promotes (P3's executable-single-source for the count defect; probe-before-claim for the
attribute and history defects). The arc files no additional row from its own review train.

**Infra:** one dispatch interrupted by the caller's harness background-task SIGTERM
(`failureReason: interrupted`, row recorded `no_verdict`, uncounted); the detached
`nohup … & disown` re-dispatch completed cleanly. Same box behavior and same remedy as the
task-enrollment-multi-region and step3-a11y arcs. Two review-watch background tasks (not
dispatches) were also SIGTERMed mid-wait; Monitor-based watchers and the cron nudge covered
every wake thereafter.
