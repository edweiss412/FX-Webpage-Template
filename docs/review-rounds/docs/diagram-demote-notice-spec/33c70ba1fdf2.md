# docs/diagram-demote-notice-spec @ 33c70ba1fdf2

## spec — 5 rounds

**Examined:** `docs/superpowers/specs/crew/2026-08-15-diagram-demote-notice-design.md` — the transient demote chip's state machine (set gate, four clear conditions, the `openNonce` re-open seam) against the live `components/diagrams/GalleryLightbox.tsx` / `Gallery.tsx` retention behavior probed by `tests/components/diagrams/gallery.failedItem.test.tsx` and `galleryLightbox.zoomGate.test.tsx`. Eight findings across five rounds (the fifth returned zero — the APPROVE round), every one confirmed against a live probe, none refuted.

**Judgment:** The round driver was one genuinely hard surface, not drip: the lightbox's AnimatePresence retention semantics (frozen exit props, canceled exits that never fire `onExitComplete`, the same instance surviving into the next dialog session). Round 1 found the class (state surviving a canceled exit); rounds 2-4 each showed the previous repair's seam sat one step too late in that lifecycle — an `open`-prop effect that cannot observe a close (R2), a close-initiator clear that an exit-window demote can repopulate (R3), and an `openNonce` EFFECT that leaves a commit-to-effect window a pending image error can land in (R4). Convergence came from moving the mechanism earlier each time until it ran during render, where no DOM error event can precede it. The R1 batch (chip-over-placeholder, static figure anchor, tautological lifetime oracle) was ordinary spec-quality residue, cleared in one round.

**Mechanizable:** Partially. The recurring shape — "a cleanup keyed on unmount/effect timing, on a surface whose instances outlive their close" — is exactly the retention contract `gallery.failedItem.test.tsx` already pins; the reviewer found every instance by re-reading those probes. A future spec touching lightbox-session state should cite the retention suite's probes as design INPUTS up front (the spec-side empirical-spike rule), which is what rounds 2-4 amounted to retroactively. No new lint or meta-test arm falls out of four rounds on one component's lifecycle.

## plan — 4 rounds

**Examined:** `docs/superpowers/plans/2026-08-15-diagram-demote-notice/plan.md` + `HANDOFF.md` — task decomposition, RED-cycle validity per task, the transition-audit task's existence and executability, and the close-out ordering, against the live `components/diagrams/` tree and the retention suites. Eight findings across four rounds (5 + 1 + 1 + 1), each confirmed against the live tree, none refuted.

**Judgment:** Rounds 2-4 were one hard structural question, not drip: WHERE the mandated transition-audit content can live once the implementation task precedes it. R2 showed a post-implementation audit task's behavioral cases pass on arrival (false RED); R3 showed the replacement manifest-style structural RED is the prohibited test-controlled-value shape; R4 caught the fold's residue (a spec §2.3 row omitted from the folded table while its test already existed). Convergence came from eliminating the separate task — the audit content folded into the implementation task's body and commit, where the compound cases genuinely red — and mirroring the spec table one-for-one. The R1 batch (harness realism for session cases, timer oracles, single-commit §5.5 lockstep, close-out ordering, claims protocol) was ordinary plan-quality residue, cleared in one round.

**Mechanizable:** Partially. The recurring shape — "a follow-on task whose tests cannot fail because the preceding task already shipped the behavior" — is checkable at plan-lint level in principle (a task whose RED claim names a production line an EARLIER task's GREEN already created), but the check needs cross-task data flow the current spec:lint marker grammar does not carry; not worth a lint arm off one arc. The table-parity residue (plan audit table mirrors spec §2.3 row-for-row) is the same derived-cover rule the class-sweep discipline already states — enumerate from the spec, not from memory.
