# docs/diagram-demote-notice-spec @ 33c70ba1fdf2

## spec — 4 rounds

**Examined:** `docs/superpowers/specs/crew/2026-08-15-diagram-demote-notice-design.md` — the transient demote chip's state machine (set gate, four clear conditions, the `openNonce` re-open seam) against the live `components/diagrams/GalleryLightbox.tsx` / `Gallery.tsx` retention behavior probed by `tests/components/diagrams/gallery.failedItem.test.tsx` and `galleryLightbox.zoomGate.test.tsx`. Seven findings across four rounds, every one confirmed against a live probe, none refuted.

**Judgment:** The round driver was one genuinely hard surface, not drip: the lightbox's AnimatePresence retention semantics (frozen exit props, canceled exits that never fire `onExitComplete`, the same instance surviving into the next dialog session). Round 1 found the class (state surviving a canceled exit); rounds 2-4 each showed the previous repair's seam sat one step too late in that lifecycle — an `open`-prop effect that cannot observe a close (R2), a close-initiator clear that an exit-window demote can repopulate (R3), and an `openNonce` EFFECT that leaves a commit-to-effect window a pending image error can land in (R4). Convergence came from moving the mechanism earlier each time until it ran during render, where no DOM error event can precede it. The R1 batch (chip-over-placeholder, static figure anchor, tautological lifetime oracle) was ordinary spec-quality residue, cleared in one round.

**Mechanizable:** Partially. The recurring shape — "a cleanup keyed on unmount/effect timing, on a surface whose instances outlive their close" — is exactly the retention contract `gallery.failedItem.test.tsx` already pins; the reviewer found every instance by re-reading those probes. A future spec touching lightbox-session state should cite the retention suite's probes as design INPUTS up front (the spec-side empirical-spike rule), which is what rounds 2-4 amounted to retroactively. No new lint or meta-test arm falls out of four rounds on one component's lifecycle.
