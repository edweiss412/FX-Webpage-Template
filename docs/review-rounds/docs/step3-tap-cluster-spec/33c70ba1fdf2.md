# Round-economy filing — docs/step3-tap-cluster-spec

## spec — 5 rounds

**Examined:** all five rounds (r1 NEEDS-ATTENTION/6, r2 NEEDS-ATTENTION/2, r3 NEEDS-ATTENTION/2, r4 NEEDS-ATTENTION/1, r5 APPROVE/0) on `docs/superpowers/specs/2026-08-15-step3-tap-cluster.md`. Eleven findings total: ten repaired in-branch, one refuted by probe.

**Mechanizable:** two shapes. (1) **Testid/locator provenance across component boundaries** — r1-1 was a FALSE stale-locator claim built from a two-file grep (`-review-section-*` is rendered by `components/admin/review/ShowReviewSurface.tsx:1057`, not by the sections file the reviewer scanned), and r1-2/r2-1 were true findings of the same axis in the other direction (a spec-promised testid that did not exist; a provenance sentence testid-less nodes could not satisfy). A spec:lint arm that resolves every `data-testid` literal a spec names against a repo-wide component grep would settle the axis mechanically in both directions — killing the false positive and the true gaps in one pass. (2) **Single-source numeric constants** — r3-2 and r4-1 were both "a derived constant is duplicated across clauses and its claimed single source does not hold" (the 34px/9.5px thresholds), the exact `BL-SPECLINT-PROSE-COUNT-PARITY` shape already filed; its mechanization would have ended the arc at round 3.

**Judgment:** the render-path coverage finding (r1-3: three `SheetTitleLink` sites, one seeded), the nullable-matrix truth finding (r1-4: the unconditional `mt-1.5` contradicting "tops the cell"), and the transition-inventory completeness pair (r2-2, r3-1: the new focus-visible pseudo-state and the per-site state deltas) were genuine document-truth findings a checker cannot reach — each demanded the spec say what the DOM actually does in a state nobody had enumerated, and each produced a structural repair (a second seeded variant, a documented limit with a by-construction argument, a per-site delta table) that closed its class rather than its instance.

**Infra:** none — five dispatches, five verdicts on attempt 1, no wrapper faults, no silent deaths.
