## spec — 4 rounds

**Examined:** the control-outline forward-guard spec train (R1 BLOCKING 6, R2 NEEDS-ATTENTION 2, R3 NEEDS-ATTENTION 1, R4 NEEDS-ATTENTION 1); every finding probe-confirmed before it was argued, zero refuted, R1 to R3 each repaired as a class in-branch (the repair commit swept every count, bar, limit and test name the finding touched); R4 landed on the same axis as R2 and R3 (a Tailwind spelling the weak-token grammar did not classify: the important marker, the opacity modifier, then the directional `border-t-border*` family) and its disposition went to the orchestrator as a re-scope decision rather than a fourth grammar rule.

**Mechanizable:** (a) the count class (R1 F1: the acceptance floor stated in three places with three numbers) is a spec-lint shape already half-built: a floor that is "stated once and counted once" can be a single definition every later count references by name, and the lint's numeric-noun inventory would have flagged "five source mutations" beside "seven cases" beside "six" on the day it drifted; (b) the grammar class (R2 F1 the important marker, R3 F1 the opacity modifier) is the narrowing rule applied at the second round instead of the fifteenth: a recognizer that exact-matches a token invites one suffix per round, and the derivation that closes it is a stem test that consults no suffix plus an executable premise pin (AC-15) on the only fact the stem rests on; a brief-time check ("does any accept-set in this spec match a token exactly where Tailwind allows a modifier?") would have pre-empted R3; and R4 shows the derivation that actually terminates: the reviewer's instrument in all three grammar rounds was the Tailwind compiler, so the predicate that cannot be out-spelled is the compiler itself (a compile-time oracle over the candidate tokens, restricted to `border*-color` declarations on the element's own selector), probed feasible with `compile()` from `tailwindcss` v4 on the branch on 2026-08-21.

**Judgment:** R1's bars (a reason bar that cannot decide trackness, an accept-set for dividers, a ledger citation that must name the file) and R2's per-occurrence evaluation were genuine design refinements of the census's own failure direction, the class that needs an adversary reading the bars against the corpus; R1 F5 (which browser harness reaches which residue element) settled Outcome A by measurement rather than argument and is the finding the whole three-outcome §1 needed.
