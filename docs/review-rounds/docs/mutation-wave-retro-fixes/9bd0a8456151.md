# Round-economy filing — docs/mutation-wave-retro-fixes

## plan — 5 rounds

**Examined:** rounds 4-8 (retro cross-model pass over the merged wave plan; r4 found 7 probe-backed defects incl. a 46-hole segmentation-model gap codex measured by replaying the discriminator against the ledger; r5-r7 each caught a residual of the r4 repair batch — a two-cell opener case, wrong vocabulary keys, an un-collected test filename, a missing import, one stale note; r8 APPROVE 0).

**Mechanizable:** two shapes recur and could be gated. (1) Plan-snippet import completeness — the extract-and-typecheck pass run before each dispatch caught type errors but NOT a snippet calling a helper it never imports when the helper stub happened to be ambient; extracting each snippet into an ISOLATED compilation unit would have caught r6's missing import at authoring time. (2) Stale-note contradiction after partial amendment — r5 amended 04's segmentation note but not 03's identical twin, and r7 caught the half-swept class; the existing class-sweep discipline covers this, the miss was execution, not a missing rule.

**Judgment:** the r4 findings (segmentation unit, canonical routing keys, exhaustive-vs-sampled swap proof) were genuine design-level catches available only to a reviewer that replays code against the live corpus — no static gate substitutes for that.
