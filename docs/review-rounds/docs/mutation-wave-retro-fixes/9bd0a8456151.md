# Round-economy filing — docs/mutation-wave-retro-fixes

## plan — 5 rounds

**Examined:** rounds 1-5 of this arc (dispatched as wave-continuation rounds 4-8; renumbered to the per-arc 1..N contiguity the corpus grammar requires). Round 1 found 7 probe-backed defects incl. a 46-hole segmentation-model gap codex measured by replaying the discriminator against the ledger; rounds 2-4 each caught a residual of the round-1 repair batch — a two-cell opener case, wrong vocabulary keys, an un-collected test filename, a missing import, one stale note; round 5 APPROVE 0).

**Mechanizable:** two shapes recur and could be gated. (1) Plan-snippet import completeness — the extract-and-typecheck pass run before each dispatch caught type errors but NOT a snippet calling a helper it never imports when the helper stub happened to be ambient; extracting each snippet into an ISOLATED compilation unit would have caught the round-3 missing import at authoring time. (2) Stale-note contradiction after partial amendment — one repair amended 04's segmentation note but not 03's identical twin, and the next round caught the half-swept class; the existing class-sweep discipline covers this, the miss was execution, not a missing rule.

**Judgment:** the r4 findings (segmentation unit, canonical routing keys, exhaustive-vs-sampled swap proof) were genuine design-level catches available only to a reviewer that replays code against the live corpus — no static gate substitutes for that.
