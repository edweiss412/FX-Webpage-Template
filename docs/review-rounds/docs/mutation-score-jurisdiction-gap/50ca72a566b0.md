# Review-round economy — `docs/mutation-score-jurisdiction-gap` @ `50ca72a566b0`

Spec: `docs/superpowers/specs/ci/2026-08-22-mutation-score-jurisdiction-gap.md`

This arc is spec + plan only; a fresh pane implements. The spec stage crossed the threshold; the plan stage is recorded here if it does.

## spec — 4 rounds

**Examined:** the spec's three-way decision (§1), the disclosure arm and its trigger (§2.2), the admissibility text (§2.3), the documented limit (§2.4), the graduation (§2.5), the committed census probe and its output, and the live gate `scripts/codex-guard.mjs` with its fixtures. Verdicts: R1 BLOCKING/1, R2 BLOCKING/1, R3 BLOCKING/2, R4 APPROVE/0. Six findings across four rounds; every one accepted and repaired, none refuted. A first round-3 dispatch was killed by the author two minutes in, after the orchestrator instructed a family sweep ahead of it, and is recorded as a round-3 `no_verdict` row; it is not a counted round.

The shape worth recording: rounds 1 and 2 were the SAME class in one grammar, arm precedence in the GUARD SURFACE line recognizer. Round 1 found the trigger blind to a form the corpus writes (24 heading-form declarations, 10 of 24 declaring round-1 briefs dark to the shipped gate). Round 2 found the rescue arm outranking the disclosure (a score-shaped line dispatching on a cannot-express tail). The per-instance repair after round 1 left the family open, and the orchestrator's instruction to sweep it before round 3 is what closed it: one matrix over every combination, derived from the shipped control flow, with "a score-shaped line is never rescued" as its single invariant. Round 3 then found nothing in that family and moved to two citation-level defects.

**Mechanizable:** declined: every candidate below was either mechanized in this arc (the corpus replay, spec AC-9), already covered by an instrument that fired and went unread (spec:lint's score-noun advisory), or cost one blocked command with an existing override (the hook's directory-count cap); no row is filed, and the three are recorded so the next arc inherits the reading rather than the re-derivation.

- "the gate's trigger never reads a declaration form the live corpus uses" (R1) — mechanized IN THIS ARC rather than filed: the 24 corpus heading lines become replay fixtures in `tests/codexGuard/guardSurfaceGate.test.ts` (spec AC-9), so the gate's accept-set is asserted against the corpus shape it serves, not against constructed lines. Shipped here; no row owed.
- "a prose number about a mutation score is not the number the shipped `score()` returns" (R3 finding 1) — the instrument already exists and fired: `pnpm spec:lint` reported `NUMERIC_NOUN_MISMATCH "score" appears with 2 distinct numbers` as an ADVISORY on the very first lint of the draft, and the author dismissed it as noise. The class is not un-mechanized; the advisory was unread. No new arm; the repair is the reading discipline, recorded here so the next author treats a score-noun advisory as a pointer, not noise.
- "the convergence hook's round cap counts out-directories, so a killed dispatch (no_verdict, uncounted by the corpus) consumes a cap slot" (R4 dispatch) — the fourth counted round needed `CONVERGENCE_ACK=1` because the killed round-3 dispatch left a fourth directory. The override exists, the cost was one blocked command and one line of reasoning in the marker, and a process-facing row needs a measured cost event larger than that (mint bar); noted for the hook's owner as a documented limit, not filed.

**Judgment:** R3 finding 2, the provenance of outcome (c): the row's own close condition names only a region mechanism or a ratified widening, and the documented-limit close entered by the orchestrator's wave-6 re-scope of 2026-08-22. Accepted: the spec now states the re-scope before the decision and carries it into the archive entry, keeping the original condition verbatim and superseded, because a close whose authority is a ruling rather than the row's text must say so first or a future reader calls it ultra vires. R2's family sweep was an orchestrator ruling as well (sweep before round 3, not after), and it is the reason round 3 found nothing in the grammar.

**Infra:** the killed round-3 dispatch (author-initiated, SIGTERM to the codex child, then to the retrying wrapper; `failureReason: interrupted`, row emitted), and the cap-override on round 4 described under Mechanizable.

## plan — 4 rounds

**Examined:** the plan's task contract (three markers, two regions), the grammar-copy enumeration (§1), the Task 1 test and wrapper snippets, the Task 4 graduation, and the closeout ordering (§5). Verdicts: R1 BLOCKING/5, R2 BLOCKING/4, R3 BLOCKING/6 (five blocking, one needs-attention), R4 BLOCKING/1. Sixteen findings across four rounds, every one accepted and repaired, none refuted; the stage did not reach APPROVE within the cap and closed on the orchestrator's disposition (below).

The shape: the plan's defects were almost all CLAIMS ABOUT ITS OWN COMMANDS that the commands did not bear out. A `-t` name filter that matched five of seven new titles; a grep gate already green on two unrelated regexes; a hand-counted census (113 lines across 41 files) where the command returns 119 across 45; a snippet that started mid-statement and did not compile; a diff count that double-counted the fixture and omitted the branch's own eight paths, then a pre-graduation scope that subtracted a path already present; a zero-call assertion that could not observe a dispatch because the fake needs a scenario to record one; a suite credited with checking fields it deliberately does not scan. Each was settled by running the thing the sentence described, and the repairs replaced the sentence with the command's output. The ordering question (graduation before or after the whole-diff review) took two rounds because rounds 1 and 2 each stated one half of a two-sided constraint; the reconciliation (last commit, scoped review of exactly that commit, fresh CI read on the pushed final head, redo rather than append on a closeout defect) is the form to reuse.

**Mechanizable:** declined: the three candidates below were either already covered by an advisory that fired and went unread, mechanized in this arc as a derived cover, or are prose-parsing of the shape the repair-direction rule forbids; no row is filed, and each is recorded so the next plan author inherits the reading.

- "a `red=` name filter that does not cover the task's own titles" (R1) — the instrument already exists and fired: `pnpm spec:lint` reported `RED_TEST_NAME_FILTER` as an advisory on the draft and the author kept the filter. Same class as the spec stage's unread score-noun advisory. Repaired by dropping the filter (the red and the green are the same full-file command). No new arm; an advisory that names the defect and is read is the whole mechanism.
- "a zero-call assertion that passes vacuously when the run has no scenario" (R3) — mechanized IN THIS ARC as a derived cover: `readCalls` in the codex-guard test harness throws on a scenario-less run (Task 1), so the class cannot return with the next case; the ten pre-existing cases with the shape are repaired in the same commit. Shipped here; no row owed.
- "a count in prose that the command beside it does not produce" (R1 census, R2 and R3 diff scope) — the standing rule already on record (derive every reported number in the same command that reports it); `spec:lint`'s `NUMERIC_NOUN_MISMATCH` advisory cannot see a count whose producing command is in a different sentence. A recognizer pairing numbers with commands is prose parsing of exactly the shape the repair-direction rule forbids; the repair is the authoring form (paste the command's output, never a retyped number), recorded here.

**Judgment:** the ordering thread, rounds 1, 2, 3 and 4: round 1 required the reviewed head to be the merged head, round 2 required the marker-strip commit to be the PR's last commit (the literal text of invariant 12 and of spec §2.5 as approved), round 3 required a push and a redo rule, and round 4 measured that the literal reading cannot hold on this repo at all, because every review dispatch appends a tracked corpus row after any last commit. The orchestrator had already ruled this on 2026-08-18 (arc F): the property is that the marker never reaches main, the mechanism is the last commit, and the whole ledger change lands as ONE commit BEFORE whole-diff review. The plan's round-1 form was that ordering; round 2 moved it onto the literal reading, and the author accepted rather than citing the ruling, which cost rounds 3 and 4. The repair restores the ruled ordering, quotes the ruling in the plan, amends spec §2.5's wording to it, and verifies absent-at-HEAD before readiness. Also under judgment: R3's `**Status:** CLOSED` citation-level mismatch with the approved spec (invariant 7, accepted as written); the one-token spec §2.5 wording amendment ("merge date" to "closeout date") disclosed in the plan and the round-4 brief rather than re-opening the spec stage.

**Infra:** none; every plan dispatch returned a verdict on the first attempt.

**Disposition at cap (orchestrator ruling, 2026-08-22, recorded so it is visible in the record rather than inferred):** round 4's finding was ACCEPTED and repaired by restoring an already-ratified ordering and quoting its authority, and the orchestrator ruled it a pre-authorised disposition with NO fifth round: the scoped-extra-round precedent covers NEW design decisions landed after a verdict, and this repair is the opposite, so the only reviewable question is conformance to the ruling, which the orchestrator verifies at merge (marker absent at HEAD, mechanical). The plan stage therefore closed at the cap without an APPROVE row; the authority for implementation is this disposition plus the four committed verdict rows. The lesson the orchestrator named: round 2's push onto the literal reading was the round's real defect, and citing the ruling over accepting the literal reading is what would have saved rounds 3 and 4.
