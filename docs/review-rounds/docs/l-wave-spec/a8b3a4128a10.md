# Round-economy filing — docs/l-wave-spec @ a8b3a4128a10

## spec — 4 rounds

**Examined:** R1 (NEEDS-ATTENTION, 4): the claim set omitted the 15 stamp/classification targets; raw-character censuses counted comments as copy; the probe decision rule was not total; template proofs were per-file rather than per-shape. R2 (BLOCKING, 3): a stale "14 markers" count survived the R1 claim-set widening; the report-link semantics overstated what a navigational link delivers; "executable string literal" was defined by predicate name rather than node kinds, silently excluding template fragments. R3 (BLOCKING, 1): the em-dash accept-set stopped at components/app/notify while `lib/**` held 19 live copy sites. R4: APPROVE with one filing-rationale correction (invariant-10 misattribution on GET routes).

**Mechanizable:** two shapes. (a) Stale-count drift after a repair round — the exact class the spec-self-review numeric sweep exists for; the miss was not re-running it across §3 after editing §0, so the mechanization exists and the failure was skipping a pass, not a tooling gap. (b) The comment-vs-copy census error and the accept-set surface gaps (R1 F2, R2 F3, R3 F1) are all mechanized by the em-dash guard itself once W-EMDASH ships — the guard IS the census, which is why the wave builds it; no additional tooling filed.

**Judgment:** the claim-set question (R1 F1 — does a stamp edit constitute declared work under invariant 12?) and the report-link semantic level (R2 F2 — what the user's ratified L→S resize covers versus the memo's fuller mechanism) were genuine design calls a reviewer could only settle by argument against AGENTS.md and the decisions brief; neither is a recognizer gap.
