# perf/admin-diagram-next-image — review-round economy

## plan — 2 rounds

**Examined:** 4 plan rounds across three bases (`5fccaaac7804` r1, `fb4642740622` r1, `66c9857f56a5` r1 and r2), 23 findings, every one accepted and repaired, no verdict yet. Two of the three base changes were `origin/main` absorbs taken at round boundaries, so the per-file counter restarted twice while the arc's total did not. The deliverable is a ~120-line component change behind a shipped loader; the plan is what has been reviewed, and the plan is what kept moving.

**Mechanizable:** Three classes, all parked, none minting a row — this arc files no ledger entry of any facing, and the process mint freeze parks a process-facing finding to the owning documented-limits record with a slug.

declined: LIM-PLAYWRIGHT-RED-TESTMATCH, an existing parked class this arc is the third to name. Round 1's first finding: `playwright test <file>` without `--config=tests/e2e/standalone.config.ts` matches no project in the default config and exits 0 having collected nothing — the slug's own second-arc widening, "any DECLARED playwright command, not only a `red=` marker", covers it exactly. Measured both sides at plan time the way that arc did: `Total: 35 tests in 1 file` with the flag, nothing without it. Third distinct arc, so the slug's re-file trigger has fired; disposition is the orchestrator's, and the freeze's admission test still applies.

declined: LIM-ADVISORY-UNDISPOSITIONED, the second measured instance, which is that slug's stated re-file trigger. Round 3's finding 7 charged three stale `file:line` citations into files this branch does not own (`BACKLOG.md`, `docs/agents/writing-plans.md`), drifted by two `origin/main` absorbs. The lint had already said so: probed on this base by restoring the stale `BACKLOG.md:68`, `spec:lint` raises the `CITATION_SYMBOL_ABSENT` family naming the file and the absent identifiers, and this arc dispatched three rounds carrying 20 to 26 undispositioned advisories without saying which were noise. The repair the slug names — one line per advisory in the brief — is cheaper than the round it cost, and this arc's Task 6 adds the procedural half (re-read every foreign line-form citation after the final absorb) because `RED_TARGET_INVALID` and the citation arms check that a line is IN RANGE and never what is AT it.

declined: LIM-GATE-EVIDENCE-SHAPE-ONLY, coined here; no existing slug covers a gate that accepts the shape of its evidence. Two consecutive rounds on one Task-5 close-out gate: round 3 found it passing with both evidence sections reading "Pending" under real headings, and round 4 found the repaired version passing on the findings table's HEADER row and with no positive requirement at all on the pre-code section. Both are the fail-open shape the mutant-red rule already prescribes for declared gate commands — probe each against a CONSTRUCTED failing input — and both would have been caught by that rule as written, had it been applied to the gate at authoring time instead of after a reviewer constructed the input. What is mechanizable is narrow and worth naming: a declared gate whose predicate is a `grep` for a heading, a table pipe, or the ABSENCE of a placeholder word accepts an empty section, and the check is decidable from the gate command alone.

**Judgment:** The rest is not mechanizable and should not be. Three findings were disproofs of an ARGUMENT the plan was making, not of a fact it stated: that the mandatory layout task could not have an honest red (one grep disproved it — the live anchor is `className="block"`); that the `sizes` oracle could observe a tier in a harness whose loader is width-independent; and that a two-tier fixture could exhibit a three-tier ladder's transitions. A reader who accepts the plan's framing cannot find these; a reviewer who re-derives the claim can. That is what the rounds bought.

The `sizes` axis alone took three rounds (r1 f2, r2 f1, r3 f1) and each repair was correct at the level it was aimed. The lesson is in the escalation, not the arithmetic: rounds 1 and 2 corrected the NUMBER, round 3 found the INSTRUMENT could not read it. A stated derivation invites correction of its terms; only asking "what would observe this?" moves it. The plan now answers with the browser's own `img.currentSrc`, and the model it validates ships as exported code rather than as a paragraph.

**Infra:** One round was lost to an inference, not to the artifact. The round-1 dispatch completed at 10:58 with a verdict on disk, but the orchestrator checked `/tmp/wz-plan-r1-out` — a path this arc's own marker text named — found it absent, and wrote "the dispatch died with the usage wall" into the arc's marker as a stateFact. Absence at a named path was ambiguous between "died" and "wrote elsewhere"; the artifact was in `.codex-out/plan-r1-20260827-104644/`, with its corpus row already committed. The arc caught it against disk in minutes. The durable form: a marker is where an arc goes for what it cannot re-derive, so an inferred cause written there outranks the arc's own evidence until something contradicts it. Facts on disk beat a fact in the marker, and the marker should say which it is.
