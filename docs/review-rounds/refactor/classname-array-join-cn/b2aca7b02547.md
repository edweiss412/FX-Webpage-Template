# Review-round economy — `refactor/classname-array-join-cn` @ `b2aca7b02547`

Arc: `BL-CLASSNAME-ARRAY-JOIN-MIGRATION`. Spec
`docs/superpowers/specs/2026-08-07-classname-array-join-cn.md`, plan
`docs/superpowers/plans/2026-08-07-classname-array-join-cn.md`.

This base was created by C4's **final rebase**, which conflicted. The arc's first eighteen dispatches
are in the sibling files (`61281c23e8ce.md` — spec 7 rounds, plan 13; `9bd0a8456151.md` — diff 15
rounds, all three surfaces APPROVED). The rounds below are the DELTA rounds that approval voided,
numbered 1–5 for this base.

## diff — 5 rounds

| Round | Verdict | Findings | Subject |
| --- | --- | --- | --- |
| R1 | NEEDS-ATTENTION | 2 | stale `36` / `seven` in the parity script, one of them a live success message |
| R2 | NEEDS-ATTENTION | 1 | the same class in spec §2.2/§4.1 prose |
| R3 | NEEDS-ATTENTION | 1 | the same class, eight declarations in the plan's P1 |
| R4 | NEEDS-ATTENTION | 1 | the same class, spec §7.1's live-tree calibration + the parity header |
| R5 | **APPROVE** | 0 | — |

**Examined:** all 5 findings across the first 4 rounds; R5 approved with none. **Every one is the same defect**: a cardinality
re-derived in the executable declarations and left stale in a prose declaration that is still
load-bearing.

The rebase conflicted because `fix/step3-a11y-cluster` restructured the wizard step pill into a 44px
tap-target anchor plus an inner painted span. The resolution applied spec §5's transform to
UPSTREAM's structure, and `scripts/audit-cn-operand-kinds.mjs` then STOPPED on the untouched result
naming all four movements at once — site count 4→5 in that file, an unknown identifier `tapTarget`,
total 36→37, unfiltered 30→31. **That is the guard doing exactly the job P1 step 3 was written for,
firing on a real upstream change eighteen dispatches after it was authored.** Every round below
CONFIRMED the resolution itself: upstream markup, comments, link semantics, test ids and ARIA
attributes preserved; all five sites on `cn` in order; the new site byte-identical
(`beforeBytes: 228, afterBytes: 228`); `tapTarget` a sole `const` non-empty literal.

**Mechanizable** — one candidate, and it is sharp precisely because the same class survived four
rounds of review and then a CI cycle:

**A re-derived cardinality needs a single source, or a lint that ties prose to it.** The arc declares
its inventory in five places: two scripts (executable), spec §2.2 and §4.1 (canonical), and the
plan's P1 (operative instructions). Re-deriving after the rebase, I fixed the executable ones first —
which is the right order, because they gate — and then chased the prose declarations
instance-by-instance across three rounds, missing some every time. The reviewer's fourth round found
the last of them in spec §7.1's *"Calibrated against the live tree"* paragraph, which is
load-bearing recognizer calibration and reads as present tense.

Two shapes would have closed it in one pass, and neither is exotic:

1. **Name the inventory once.** Put the counts in one place the other four reference by name rather
   than by literal, exactly as `AGENTS.md`'s self-consistency sweep prescribes ("put shared values
   behind one named definition that later sections reference"). The scripts already do this
   (`EXPECTED_SITE_TOTAL`); the documents do not.
2. **Lint the documents against the scripts.** `spec:lint` already parses these artifacts. A rule
   asserting that any `\b3[5-9]\b sites?` in the spec or plan either matches the scripts' declared
   total or carries an explicit "at spec/plan time" qualifier would have flagged all five findings
   locally, in one run, before the first dispatch.

Filed rather than built here: this is a guard surface of its own, and building it inside a
lint-coverage migration is the scope blowout class-sweep exception (c) exists to prevent. It belongs
with the mutation-gate candidate filed in the sibling `9bd0a8456151.md` — both are the same
observation, that a proof's *declarations* want machine checking rather than review attention.

**Judgment** — one call, and I got it wrong twice before getting it right:

Which stale figures are HISTORICAL and which are LOAD-BEARING is a judgment, not a rule. The §2.5
probe transcript is a dated record of what was measured and must not be "corrected"; Task 3's counts
are an execution record of what that task actually did; §2.2's inventory total and §7.1's live-tree
calibration are present-tense claims about the current base and must be re-derived. My first two
passes drew that line by pattern-matching on numbers, which kept catching the wrong set. The fix was
to enumerate every occurrence of `36|38|29|30|seven` across all seven artifacts and classify each one
individually — after which exactly five were load-bearing, and a residual grep confirms none remain.
The lesson is the arc's own class-sweep rule applied to prose: **enumerate the class, then classify
each instance; do not grep for the ones you remember.**

**Infra:** none in these five rounds. All five dispatches returned verdicts on attempt 1.

**One self-inflicted CI round, recorded because it is the same defect one level up.** The filing
above was written at four rounds and committed; the approving fifth then landed, its corpus row went
in with the process-record commit, and `_metaReviewRoundEconomy` failed on the graduation head with
*"stage diff declares 4 rounds; the corpus counts 5"*. A filing that hard-codes its own round count
is exactly the stale-cardinality shape this stage spent four rounds on — mechanized this time, which
is why it cost one CI cycle instead of a review round, and which is the best available argument for
the Mechanizable candidate above. Recovery followed C4 step 7: revert the WHOLE graduation commit
first (restoring the live entry and its marker), then fix, then re-author graduation as the new last
commit. (The
earlier stage's infra events — a usage limit, a synckit timeout at load average 57, two starvation
timeouts at load average 201, and a wrapper `ATTEMPT_MAX_SECS` ceiling — are recorded in
`9bd0a8456151.md`.)
