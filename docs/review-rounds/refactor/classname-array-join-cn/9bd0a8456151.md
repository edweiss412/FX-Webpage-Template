# Review-round economy — `refactor/classname-array-join-cn` @ `9bd0a8456151`

Arc: `BL-CLASSNAME-ARRAY-JOIN-MIGRATION`. Spec
`docs/superpowers/specs/2026-08-07-classname-array-join-cn.md`, plan
`docs/superpowers/plans/2026-08-07-classname-array-join-cn.md`.

The spec (7 rounds) and plan (13 rounds) filings live in the sibling `61281c23e8ce.md`, keyed to
the pre-rebase merge-base. This file covers the `diff` stage only, which began after the P1 rebase
moved the base twice.

## diff — 14 rounds

Dispatched as THREE SCOPED reviews per round rather than whole-diff, per the split-review
discipline: `helper` (the `cn` helper + the two one-shot arc scripts), `components` (the 18 migrated
files + the DayCard render test), `guards` (the zero-tolerance guard, the token-identity test, the
dimension harness, the CI wiring). Round verdicts and declared finding counts, from the corpus:

| Round | helper | components | guards | total |
| --- | --- | --- | --- | --- |
| R1 | BLOCKING (8) | NEEDS-ATTENTION (1) | BLOCKING (4) | 13 |
| R2 | BLOCKING (4) | **APPROVE (0)** | NEEDS-ATTENTION (2) | 6 |
| R3 | BLOCKING (7) | _not re-dispatched_ | NEEDS-ATTENTION (5) | 12 |
| R4 | BLOCKING (4) | _not re-dispatched_ | NEEDS-ATTENTION (2) | 6 |
| R5 | NEEDS-ATTENTION (3) | _not re-dispatched_ | BLOCKING (2) | 5 |
| R6 | BLOCKING (4) | _not re-dispatched_ | NEEDS-ATTENTION (1) | 5 |
| R7 | **APPROVE (0)** | _not re-dispatched_ | NEEDS-ATTENTION (1) | 1 |
| R8 | _not re-dispatched_ | _not re-dispatched_ | NEEDS-ATTENTION (1) | 1 |
| R9 | _not re-dispatched_ | _not re-dispatched_ | NEEDS-ATTENTION (2) | 2 |
| R10 | _not re-dispatched_ | _not re-dispatched_ | NEEDS-ATTENTION (2) | 2 |
| R11 | _not re-dispatched_ | _not re-dispatched_ | NEEDS-ATTENTION (1) | 1 |
| R12 | _not re-dispatched_ | _not re-dispatched_ | NEEDS-ATTENTION (2) | 2 |
| R13 | _not re-dispatched_ | _not re-dispatched_ | NEEDS-ATTENTION (1) | 1 |
| R14 | _not re-dispatched_ | _not re-dispatched_ | BLOCKING (1) | 1 |

`components` converged at R2 and `helper` at R7 — the migrated product code was never the problem,
and the helper surface took six rounds of proof-hardening before it was.

**The ratified-limits section is what converged `helper`.** R7's brief added a RATIFIED LIMITS block
naming the three vectors already closed structurally and stating that another instance of one is the
enumeration this arc has decided not to run. `helper` returned APPROVE with zero findings on the
same code that had drawn 4 the round before. A closable bound stated in the brief did in one round
what six rounds of repair had not.

R8 is the shape a converging round should have: ONE finding, in the BOUNDED wrapper class (the
callee itself can be wrapped — `(arr.join)(" ")`, `arr.join!(" ")`), closed in both recognizers and
pinned by four new fixture shapes. A finite set of syntactic wrappers terminates; that is exactly
why it was never a ratified limit.

R9's two are the same kind — bounded parsing questions with exact answers, both on the token test
that is C1's ONLY discriminating proof, and both nastier than they look because C1's rect keys are
the accepted zero-width tripwire, so neither had a second net beneath it. A line-anchored CSS regex
saw only the first of two declarations sharing a line (`--spacing-confirm-box: 60px;
--spacing-confirm-box: 64px;` reported `["60px"]` while the utility resolves to 64px), and textual
presence anywhere in a `className` initializer let `false ? "max-w-confirm-box" : "max-w-16"` pass.
Fixed by splitting declarations on semicolons, and by requiring the utility to appear in a
top-level, UNCONDITIONALLY-APPLIED string-literal argument — "always rendered" is decidable
syntactically, which is why this closes rather than ratchets.

R10 stayed on that same file with two more, and both had exact answers: the element lookup took the
FIRST match, so a dead earlier element with the same `data-testid` could keep the canonical utility
while the rendered one changed (now: exactly one source element may carry the id); and every call's
literal arguments were treated as applied, so a helper that discards an argument —
`pickSecond("max-w-confirm-box", "max-w-16")` — passed (now: only the sanctioned `cn` callee counts,
since `cn` is the one callee whose semantics this arc actually pins). R13's single finding is the cleanest illustration in the whole corpus of why
this filing's Mechanizable candidate is the right one: R12 taught the peel loop to unwrap
intermediate callees, and there were TWO COPIES of that loop twenty lines apart — the direct one
and the one inside the const-resolution hop. Only the first was fixed, so `const t = [...].filter!(Boolean)`
routed through a const stayed silent while the identical direct form was caught. A mutation gate
would have flagged the surviving mutant immediately; a human reviewer needed a fourteenth dispatch.
The repair was to delete the duplication — one `peelArrayChain` function, called from both places.

Six of the last eight findings
have been on this single file, which is the predictable consequence of it being C1's ONLY
discriminating proof — the zero-width connector tripwire gives it no second net, so every gap in it
is a P1. **All 58 findings landed on the two PROOF surfaces**, which is the fact this filing is
about.

**Examined:** all 58, classified by whether the finding named a way the proof could pass while the
migration changed rendered output (a REAL false pass) or a way the proof was merely imprecise.

Fifty were real false passes. That number is the story, and it is not a story about
enumeration: the arc's whole claim is "36 sites were rewritten and nothing rendered differently,"
and every one of those 50 was a concrete input where the proof said yes and the output moved. A
sample, each probed by the reviewer with the escaping input:

- The baseline oracle was cast, not validated: `NaN > 0.5` is false, so three correctly-named EMPTY
  objects accepted any geometry at all.
- `--base` was required but never verified, so the stale pre-rebase anchor — the exact value the
  script's own header warns about — passed silently.
- Operand normalization collapsed whitespace INSIDE string literals, making `"a  b"` → `"a b"`
  compare equal in the proof built to catch precisely that.
- The base separator was never compared: `cn` emits one ASCII space, so a base joined with `"\t"`
  had identical form, identical operands, different output.
- Identifier operands were compared by SPELLING, so `const base = "class-a"` → `"class-b"` passed.
- A declared C1–C6 delta could rewrite a conditional's DISCRIMINATOR, selecting a different branch.

**Mechanizable** — one candidate, and it is the reason this stage took fourteen rounds rather than one:

**An equivalence proof needs a mutation gate, not a review loop.** Every round followed the same
shape: the reviewer constructed an input, ran it through the shipped script, and showed the script
reporting OK while the rendered string changed. That is mutation testing performed by hand, one
mutant per round, at roughly 25 minutes of dispatch per mutant. This repo already has the
machinery — `tests/mutation/source/registry.ts`, `pnpm mutation:guards`, spec
`docs/superpowers/specs/ci/2026-08-04-source-mutation-guard-gate.md` — and AGENTS.md already says
that for an enrolled surface the convergence criterion becomes "mutation score plus an empty
unaccepted-survivor set," both machine-computed. **These two scripts were not enrolled, and that is
the defect.** Enrolling them would have moved 50 findings from fourteen review rounds into one ~90s
local run, and would have replaced "name an input the guard misses" — which does not terminate —
with a closed operator set over a finite program, which does.

The self-test grew 10 → 27 rejection paths plus 6 reconcile premises across these rounds, which is
the same information the mutation gate would have produced, obtained the expensive way.

**A second, sharper instance of the same lesson closed at R5.** `isInClassNamePosition` — the
predicate deciding whether a join feeds a `className` — was extended in three consecutive rounds:
R3 added variable bindings, R4 added alias chains, R5 arrived with late assignment, function
declarations, parameter defaults and destructuring. That is unbounded by construction, because
className-ness is a DATAFLOW property and a lexical walk can always be routed around by one more
hop. Rather than patch a fourth time, the predicate was DELETED and the exemption key replaced with
one that is closed: `<file> :: <enclosing function> :: <operand signature>`. It ranges over a finite
program instead of an open set of routing shapes, and escaping it now requires reproducing the data
join's operand signature inside the same named function — at which point the "escape" is code that
renders `leg.date` as a CSS class. **Three rounds on one vector is exactly the trigger this repo's
own rule names; the fix was to change the question, not to widen the answer.**

**A THIRD instance arrived at R6, and it is where the widening stops.** The receiver-resolution
rule — "what does this join's receiver reduce to?" — was extended at R5 (one const hop) and R6
arrived asking for factory calls, member arrays, conditional arrays and `Array.from(...)`. Same
shape: each resolution rule invites one more indirection, because "is this an array of class
strings" is a dataflow/type question a lexical guard cannot decide. Resolution was extended once
more where it is BOUNDED (up to three same-file sole-`const` hops, re-peeling the array-method
chain after each, which covers alias and filtered-in-a-const), and the remainder is now a STATED
LIMIT in the guard's own docblock rather than a silent gap. The limit is defensible on a fact that
does not move: the deleted census scanner anchored on the literal text `className={[`, so it missed
every one of those shapes AND the ones this scanner now catches. The replacement is strictly
stronger than what it replaces, which is the actual contract (spec §1.1 R4), and `pnpm lint` is the
primary mechanism for anything routed through `cn(...)`.

Filed rather than built here: enrolling a one-shot script surface is a registry change with its own
before/after numbers, and building it inside a lint-coverage migration is the scope blowout
class-sweep exception (c) exists to prevent.

**Judgment** — three calls where a rule would not have helped:

- **The threat-model fence held, and then I chose to cross it.** R3 raised a locally-shadowed
  `Boolean` and a locally-shadowed `cn`. Both are pathological rather than ordinary authoring, and
  my brief fenced deliberate obfuscation out. But the walked set is 18 declared files, so proving
  their absence cost one loop and removed the argument permanently. Fencing was defensible;
  closing was cheaper than defending. That is a judgment about the cost of an argument, not about
  the threat model.
- **The inert-connector disposition.** C1's `max-w-[60px]` is 0-width at every viewport for a
  structural reason, so its rect comparison is `0 == 0`. Keeping both baseline keys as a
  regression tripwire that ASSERTS the 0-width state — rather than deleting them or pretending
  they discriminate — is the honest option, and R2 and R4 both examined it and agreed. R2 then
  found the real gap underneath it: the token proof was not bound to the production site, so
  `max-w-16` would have passed everything. The disposition was right and incomplete.
- **`x`/`y` recorded but not compared.** A locally-captured sub-pixel position verified on a Linux
  CI runner is the byte-comparison lesson in miniature; width/height carry the whole claim for a
  `max-w` and a `min-h`. Two rounds examined it and neither disputed it.

**Infra:** two events, one costly.

The first three dispatches of R1 all returned `no_verdict` with `failureShape: no_o_file` after 3
attempts each. This was NOT the documented silent-death/reaper pattern — the reaper log was clean
and every attempt failed identically and immediately with
`ERROR: You've hit your usage limit ... try again at Aug 11th, 2026 6:21 PM`. A second Codex account
at `~/.codex-ship-prefetch` turned out to have a REVOKED refresh token, so the run escalated once
and set `blockedOn`. The limit then cleared on its own well before the quoted reset, and a
`--stage task` probe confirmed it in ~10s. **Lesson: probe the account before believing a quoted
reset time**, and record `no_verdict` as an infrastructure fault rather than a review outcome —
which the corpus does, so those rows correctly do not count as rounds.

The third: R11's first two dispatches died on the wrapper's time budget
(`attempt_timeout`, then `total_timeout`) at a measured load average of **201** — this machine was
running roughly two dozen concurrent Codex processes across sibling arcs. The retry with raised
limits then failed as a USAGE error (`ATTEMPT_MAX_SECS exceeds max 1380`) and produced no dispatch
at all, which cost another cycle. Both are worth recording: a `no_verdict` under heavy load is a
starvation fault, and the wrapper's ceiling is 1380s per attempt, not arbitrary.

The second: at a measured load average of 57 (several arcs ship concurrently on this machine) the
eslint coverage pair failed with `Atomics.wait() failed: timed-out` inside the plugin's synckit
worker. A scheduler failure that says nothing about the rule. Raising `SYNCKIT_TIMEOUT` took the
test from a reproducible 50s timeout to 7.8s green.

**What I would do differently, concretely:** enroll `scripts/verify-cn-operand-parity.mjs` and
`scripts/audit-cn-operand-kinds.mjs` in the source-mutation registry BEFORE the first diff dispatch.
The brief already carried a consequence bound and a threat-model fence, and they were not enough —
because for an equivalence proof the closable criterion is a mutation score, and stating one in
prose does not compute it.
