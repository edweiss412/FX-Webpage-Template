# chore/round-economy-enforcement-pair — filing

Arc: the round-economy enforcement pair (spec `docs/superpowers/specs/ci/2026-08-15-round-economy-enforcement-pair.md`). Written when the spec stage crossed the threshold mid-arc; diff-stage rounds reconcile at close-out.

## spec — 13 rounds

**Examined:** R1–R13, 19 declared findings; R13 APPROVE/0. R2 was an infrastructure round — the round-2 brief's own stale commit pin, not an artifact defect. The dominant class (R1, R3, R4 on Half A; R5–R12 on Half B) was one shape both times: a line-level recognizer weaker than the contract it enforces. Half A closed by NARROWING to one canonical anchored score grammar plus a hygiene-not-gate-truth fence (§1.1.7/§5.8); Half B closed by DERIVATION — the whole Mechanizable analysis moved onto the markdown AST, after which each remaining round was a node-semantics gap (visibility, boundary, representation decoding, strikethrough, nesting) repaired by node type rather than pattern.

**Mechanizable:** declined: every rule-shaped finding in this train was folded into the spec's own contract — the arc IS the mechanization its rounds kept demanding, and the reviewer-probe shapes are mandated §6 fixtures rather than schedulable follow-ups.

**Judgment:** the stale-none limit (§5.7 — live idiom probe against forcing one-line none explanations); the below-floor fence (§5.8 — dispatch hygiene versus registry truth); row-vs-decline dispositions for the §4 backfill.

**Infra:** R2 (stale commit pin in the brief, chargeable to the orchestrator's brief authoring, repaired by pinning the post-repair commit).

## plan — 5 rounds

**Examined:** R1–R5, 15 declared findings; R5 APPROVE/0. R1's ten were executable-quality defects (a RED dying on module resolution, missing fake-codex scenario wiring, registry co-edits, an unattainable gate criterion, shipping order); R2–R4 each closed one residual (discriminator fixtures, inventory completeness, terminal-row and invariant-12 ordering).

**Mechanizable:** none new — the recurring class (a red= that cannot fail for its stated reason) is already filed as `BL-SPECLINT-RED-EXECUTABILITY-ARM`, and the registry co-edit class is closed structurally by the registries' own fail-by-default key-equality assertions.

**Judgment:** the terminal-commit confinement rule (which mechanics may land after the approving round — resolved as exactly the CI-gated set); the scoped-versus-whole mutation-gate criterion.

**Infra:** none. All five dispatches returned verdicts on attempt 1.
