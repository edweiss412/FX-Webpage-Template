# Review-round filings — chore/guard-completeness-wave @ 04f601134519

## spec — 6 rounds

**Examined:** R1 (BLOCKING/4): detached-method bypass of the execution-site accept-set (probe: `const {unsafe}=target` returns `{ok:true}` from the live analyzer); false "prList ENOBUFS is loud" claim (probe: fault lands in the `status !== 0` branch and returns `[]`); missing mutation-registry enrolment for two enrollable exported modules; a 9-file discovery count compressing away the 7-real+2-exempt partition. R2 (BLOCKING/3): the `pr-universe-unavailable` degraded marker was not promoted to exit 2 (`ledger-check.ts` promotes only `fetch-failed`); status-zero malformed-gh classes (empty stdout, invalid JSON, non-array, malformed row) all silently `[]`; a stale out-of-scope bullet contradicting the R1 repair. R3 (BLOCKING/1): row shape omitted boolean `isCrossRepository` — the `=== true` coercion reads a missing flag as a base-repository PR. R4 (BLOCKING/3): ledger-closeout class — A/B/D had no terminal archive disposition and no `BACKLOG_GRADUATED` rows; a fabricated `pnpm ledger:*` acceptance command; non-string `headRefName` missing from the row matrix. R5 (BLOCKING/1): §7.1's blanket "markers off in the PR's last commit" contradicted invariant 12's graduating-entry rule for entry C's early archive. R6: APPROVE/0.

**Mechanizable:** (a) The R2-F3/R5 shape — a repair round stale-ifying a statement elsewhere in the same document — is the mandated post-repair self-consistency sweep; both escapes were forward references ("out of scope" bullet, marker-timing summary) that a term-grep of the repaired text cannot see. A spec:lint rule cross-checking out-of-scope bullets and closeout summaries against sections mandating the same change would catch both. (b) The R3/R4 pair is one shape — consumed-field validation vs test-matrix mismatch — and its terminating form is a derivation: the matrix derived from the consumed-field set (one missing + one wrong-type case per field), not a longer enumerated list. (c) The R4 closeout class suggests a lint arm: a spec that dispositions `BL-` ids owes a graduation/closeout section naming each id's terminal state.

**Judgment:** R1 F1 (detached method) and R3 F1 (field coercion) were genuine design gaps that only probing review finds; both forced real design additions (destructive-string anchoring + containment rules; full-field validation). Every finding across all six rounds arrived with an executable probe, no finding was relitigated, and each round was a distinct class — the burn was breadth of a four-entry compound spec (two guard redesigns, a demotion, and an oracle build), not a recognizer ratchet. Count trajectory 4→3→1→3→1→0.

## plan — 6 rounds

**Examined:** R1 (BLOCKING/10): seam suite absent from `ledgerGit` suitePaths; owner-shape matrix escapes (string/number/boolean/array/empty-object all coerced to null); sabotage case proved nothing about census wiring; two unresolved-import REDs (invalid by construction); missing `EXPECTED_LEDGER_KINDS` rows and non-finite floors; fabricated `pnpm ledger:*` command; missing unenumerated-acquisition fixture; misidentified gh-fault test; stale 78/78 score. R2 (BLOCKING/5): two non-compiling snippets (module-scoped `git()` vs local seam binding; `runFiringSmoke`/`PgCronMode` shape); Task 9 implementation-before-test; `currentBranch`'s no-spawn CI branch unpremised; conditional enrolment red interval; Task 12 commit-contract conflict. R3 (BLOCKING/4): gate reducer omits zero-count kinds so `"accepted-gap": 0` can never match; Task 11's red vacuous for its AC (task unenrolled with rationale); fixture (s) regrouped to Rule 1/2 after its current reason is a deleted rule; eight commit steps without staging. R4 (BLOCKING/2): contingency-path staging; `liveCase` inside a skipped describe inflates the query floor at collection (Vitest executes skipped-suite factories) while plain `test` bypasses the local-unreachable skip — closed by registration-time gating. R5 (BLOCKING/3): six tasks ran different commands at red and green (marker contract is one unchanged command); fileOids canned stdout not the ls-tree shape the parser splits; stale insertion line anchor. R6: APPROVE/0.

**Examined (plan, addendum):** every finding in all six rounds arrived with an executable probe; none was relitigated after repair; five recurring watchpoints were closed by derivation and fenced (staging union, gate-reducer shape, RED validity, sabotage registration, red/green parity).

**Mechanizable:** (a) red= executability — a plan-lint arm running each declared red= at plan time and diffing the red-step and green-step command lines against the marker would have caught R1 F7/F10, R2 F2, R3 F2, and all of R5 F1 mechanically (`BL-SPECLINT-RED-EXECUTABILITY-ARM` already filed covers most of this; the red/green same-command diff is a new sub-check worth folding in). (b) The liveCase collection-time floor (R4 F2) is a suite-machinery fact worth a header note in `_liveCaseCounter.ts` so the next author does not rediscover it by review round.

**Judgment:** ten enrolled tasks across four sub-arcs with two mutation enrolments and repo-specific gate machinery is the widest plan this corpus has carried through the round-economy contract; finding count decayed 10→5→4→2→3→0 with zero relitigation, and the two genuinely design-shaped catches (owner-shape coercion matrix, liveCase floor inflation) both forced real contract additions. The R5 rise (2→3) was the parity class landing late — the mechanizable arm above is the structural close.
