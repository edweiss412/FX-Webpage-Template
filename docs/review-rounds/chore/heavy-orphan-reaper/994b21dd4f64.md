# Review-round economy — `chore/heavy-orphan-reaper` @ `994b21dd4f64`

## spec — 4 rounds

**Examined:** all four spec rounds (5 / 6 / 3 / 2 findings; BLOCKING, BLOCKING, BLOCKING,
NEEDS-ATTENTION), their briefs, and the repair commits `a44ce9cf8`, `53b4cf4d4`, `d4bbdef2b`,
`0426547d7`, `c01a4d1b5`.

**Mechanizable:** **eight of the sixteen findings are one class — a normative claim restated in
prose contradicting the table it summarizes** — and it recurred in every single round (R1 F1/F4,
R2 F2/F5, R3 F1/F2, R4 F1/F2). Nothing about the subject made it hard; the document simply stated
each behavior twice and the copies drifted at every repair.

- The repair that finally closed it is structural and cost one commit: give every §4.4 condition an
  ID (C1-C8, R1-R5, K1-K5) and make §6.2 and every criterion in §8 CITE the ID instead of
  restating the behavior. Round 4 then found the last two instances in the ONE place the
  structural pass had not swept — §4.4's own preamble, the text introducing the tables.
- **The mechanizable lesson is to assign the IDs in the first draft.** A spec whose normative
  content lives in tables should never paraphrase a row; three rounds were spent rediscovering
  that each restatement drifts, and the fourth was spent discovering that the sweep must include
  the table's own preamble.
- **A derived sweep is what actually closed it, not a longer reading.** The R4 repair ran a regex
  over every non-fenced, non-table line asserting a behavior verb, classified all 19 hits, and
  repaired the 6 that were paraphrases — leaving the other 13 enumerated in the commit rather than
  asserted clean. Reading for it had already failed three times.

**Second mechanizable class, 1 finding:** R3's BLOCKING F1 was a claim contradicted by this
document's OWN quoted probe. The identity check required `etime` unchanged, while §1.3's P4 probe
— in the same file — shows one pid's `etime` advancing `00:27 → 00:51`. The self-consistency sweep
mandated at `docs/agents/spec-self-review.md:18` compares claims against other claims; extending it
to "claims against the probe outputs this document itself quotes" would have caught it at draft
time for the cost of one pass.

**Judgment:** the irreducible findings were genuine design gaps and they front-loaded exactly as
the retrospective predicts — all three are in round 1 or 2, and each changed the deliverable rather
than its wording.

- **R1 F2** — the design did not bound what it claimed to bound. Its only automatic trigger was a
  `Stop` hook, and a session that CRASHES runs no `Stop` hook of its own. The repair added a
  pre-admission trigger in `package.json`'s `heavy` script and restated the bound honestly as
  "ceiling plus the interval to the next trigger" (L-2).
- **R1 F3** — the `classify` signature could not express the reporting §4.4 promised, in seven
  enumerated ways. The repair rewrote §5 around a parsed/unparsable row union and a slot survey
  carrying its own problems.
- **R2 F1** — the worker recognizer matched on containment, so `tail -f …/forks.js` was reapable.
  Refuted by probe rather than argument: over 679 live rows the containment rule matched 14, one of
  which was a `zsh -c` wrapper that merely mentions the path.

**Infra:** the reviewer's sandbox denied `ps` (`zsh:1: operation not permitted: ps`) and had no
network, so round 1 could not verify the six nightly durations §4.3 derives its ceiling from.
Supplying the raw `gh run list` output inline in the round-2 brief closed that permanently, and it
is worth doing from round 1 on any spec whose numbers come from a network-bound command. All four
dispatches returned a verdict on the first attempt: no `no_verdict`, no reap, no retry.
