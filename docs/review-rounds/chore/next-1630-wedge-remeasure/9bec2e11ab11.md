# Round-economy filing — chore/next-1630-wedge-remeasure

## spec — 4 rounds

**Examined:** all four rounds (r1 BLOCKING/6, r2 BLOCKING/4, r3 BLOCKING/3, r4 APPROVE/0) on `docs/superpowers/specs/2026-08-09-next-1630-wedge-remeasure-design.md`.

**Mechanizable:** two shapes. (1) Claimed-vs-actual CI trigger coverage — r1-4, r2-4 both caught the spec asserting workflow trigger behavior that a 20-line YAML walk over `.github/workflows/*.yml` (pull_request present/absent, paths vs paths-ignore, dependency-file membership) settles mechanically; a spec:lint pass that cross-checks any "auto-runs on this diff" claim against parsed workflow triggers would have killed both rounds. (2) Quoted-copy consistency between a spec's disposition wording and its own counting rule (r3-3's missing denominator) is a numeric-sweep variant already half-mechanized by NUMERIC_NOUN_MISMATCH; extending it to compare quantities ACROSS quoted disposition templates would have caught it.

**Judgment:** the decision-rule completeness findings (r2-1 dispositions, r3-1 drift-branch terminals) were genuine design gaps a checker cannot find — they converged because each round demanded the next undefined execution branch, which is the review working as intended. The r1 graduation-registry miss was a repo-convention knowledge gap (the guard exists precisely to make the archive fail without the row; review caught it earlier than CI would have, but CI WOULD have caught it).

## plan — 7 rounds

**Examined:** all seven rounds (r1 BLOCKING/10, r2 BLOCKING/5, r3 BLOCKING/7, r4 BLOCKING/4, r5 BLOCKING/2, r6 BLOCKING/1, r7 APPROVE/0). Monotone convergence after r3's probe reset; every round's repairs verified clean by the next round.

**Mechanizable:** one dominant shape across r2/r3: hand-authored shell in a markdown plan reimplementing tool internals (Playwright trace anatomy, reporter output, gh run selection) from memory instead of from a probe. r3's two CRITICALs (wrong trace member, wrong reporter marks) fell to a 90-second local Playwright probe that should have run BEFORE the first classifier draft — the writing-plans empirical-spike rule already mandates this; the failure was non-application, not a rule gap. Candidate mechanization: spec:lint flagging fenced bash in plans that greps/unzips artifacts of a tool (playwright, gh) without a nearby "probed" annotation citing a transcript.

**Judgment:** the ordering findings (r1-2 head-binding, r1-6 marker ordering) and the torn-pair/TS-race findings (r2-4, r3-6) were genuine concurrency reasoning the reviewer earned; each produced a structural repair (capture_run, TS-before-dispatch, executable head detector) that closed its class rather than the instance. Rounds 4-7 were a shrinking tail on one shell block (merge guard control flow: fall-through, restart targets, ungated pull) — the shape is mechanizable in principle (shellcheck-style control-flow lint over fenced bash in plans), but each was a real defect a fresh implementer would have executed.

**Infra:** none — no wrapper faults, no silent deaths; every dispatch returned a verdict on attempt 1.
