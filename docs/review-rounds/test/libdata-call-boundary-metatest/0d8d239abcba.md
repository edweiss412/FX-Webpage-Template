# `test/libdata-call-boundary-metatest` — base `0d8d239abcba`

Second base for this arc; the spec and plan rounds ran against `b2a9122935c8` and are filed there. This base begins after `origin/main` was merged into the branch mid-implementation.

## diff — 3 rounds

**Examined:** three counted rounds (R2, R3, R4) across two scopes, scanner-logic and registry-fidelity, declaring 20 findings; R1 is not counted because its dispatch returned no verdict, and neither is R3's registry-fidelity dispatch. R2: BLOCKING 5 / NEEDS-ATTENTION 3. R3: BLOCKING 3. R4: BLOCKING 5 / BLOCKING 1. Every finding was probed by the reviewer against the shipped guard, and every accepted one was repaired with a planted self-test plus a mutant proving the repair load-bearing.

**Mechanizable:** The dominant class — 14 of the 20 findings — was one shape wearing different clothes: **a text pattern used to answer a question about program structure**. R1's recovered probe found five literal spellings the character class dropped; R2 found two more (an escaped `${`, a backslash line continuation); R3 found three (non-adjacent generics, `?.`); R4 found four more (`sb./* c */from`, `?.<T>`, `!`, `!<A,B>`) plus the same shape in the *other* two recognizers — a block comment whose inner line began with the waiver marker, and export-shaped text inside a template literal.

Each round's repair widened the pattern, and each next round found the next spelling. Round after round is exactly what the round-economy rule predicts for a recognizer whose convergence criterion is enumeration: *"does this text look like a call?"* ranges over an open set and does not terminate. The repair that actually converged was the one the repair-economy rule prescribes — **derive, do not widen**. Sites, waiver comments, and export names now come from `typescript`'s parse and lexer, which this repo already depends on. Twelve call spellings became planted positives at once; two documented limits (parenthesised and nested-angle generic segments) were retired outright, because they had never been real limits, only artifacts of bounding `<...>` in a regex.

The measurable cost of learning this the slow way: every counted round on this base, and roughly 250 lines of pattern that the parse replaced. The signal was available at R2 — the same function, twice — and the structural-defense-calibration rule already says to ship the structural fix at first recurrence rather than waiting for the third. That rule was not applied here until R4.

**Judgment:** Two findings were genuine design calls rather than mechanizable defects, and both concerned `coveredBy` rows. R2 and R4 each found a cited suite that exercised only ONE arm of the boundary (`revokeAdminEmail`, then `listAdminEmails` — whose mock could not throw at all, so the thrown arm was structurally unreachable). Both were repaired, but the class was deliberately NOT mechanized: recognizing "this suite reaches both arms" textually is the same recognizer ratchet the diff rounds just paid for, and a weak one. It ships as an authoring bar in spec §3.4 under limit 7's division — the machine proves the citation is live, humans review whether it is strong — with all four rows audited and the audit recorded.

One finding was REFUTED and recorded rather than repaired: R2's broader claim that a `coveredBy` row can pass without executable coverage is spec §6.5 verbatim, a ratified limit whose worst case is a hollow citation, conservative and visible to any reader following it. Only its comment sub-case was actionable and was closed.

**Infra:** Two of eight dispatches returned no verdict, both machine-level rather than review outcomes. R1's whole-diff dispatch was killed at its attempt timeout with the audit complete — its stderr held the probe output that became the R1 finding, recovered by reading the stream rather than re-dispatching. R3's registry-fidelity dispatch ran a single 7166s attempt with Codex's command runner stalling on `/bin/pwd`; the machine's load average was 172 at the time, from concurrent runs in other worktrees. Concurrent dispatch was the aggravating factor: two guard reviews plus a full vitest suite saturated the box, and the same review completed cleanly in ~20 minutes once serialized. Later rounds ran one dispatch at a time with `--attempt-max-secs 1380 --total-max-secs 4200`.

A smaller one worth recording because it cost a silent round: `nohup … & disown` with two background dispatches in a single shell disowns only the most recent job, so the first dispatch takes the SIGHUP and dies leaving an empty out-dir and an empty log. Dispatch one per shell invocation.
