## spec — 4 rounds

**Examined:** rounds 1-4 of the lib/data call-boundary meta-test spec (`docs/superpowers/specs/ci/2026-08-09-libdata-call-boundary-metatest-design.md`). R1: authored-count self-reconciliation, extension-filter gap, missing behavioral citation. R2: coveredBy literal-vs-mock mismatch, backtick template invisibility, vacuous empty discharges, uncoupled copied pins. R3: invented export names (upsertAdminEmail, runAdminEmailWrite), non-exact via containment, waiver substring recognition. R4: generic-type-argument invisibility.

**Mechanizable:** the R3 invented-symbol class — both wrong names were extrapolations never grepped against the live file. The existing live-code-citation pass covers it; the failure was non-compliance at draft time, not a rule gap. A cheap mechanization exists: `pnpm spec:lint` already resolves cited paths; extending it to resolve backticked IDENTIFIERS against the cited file's export set would have caught both (filed as an observation, not a commitment — the citation pass run properly is sufficient).

**Judgment:** the scanner-widening train (R2 backtick, R4 generics) is the classic recognizer-target ratchet the round-economy doc predicts; it converged because each widening carried a planted positive/negative pair and the documented-limits fence (§6.1) absorbed the open tail (substitution templates, parenthesized generics) instead of chasing it. The R1/R2 discharge-vacuity findings (counts, empty arrays, copied pins) were genuine design-strength gaps — the derive-don't-author repair each time.

**Infra:** none — all four rounds dispatched, returned verdicts, and parsed cleanly via codex-guard.
