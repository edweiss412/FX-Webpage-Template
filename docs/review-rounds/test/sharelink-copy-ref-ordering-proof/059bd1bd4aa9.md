# `test/sharelink-copy-ref-ordering-proof` — review-round filing

One base, `059bd1bd4aa9`. Plan stage converged at 3 rounds and owes nothing; the diff stage reached the cap and owes the section below.

## diff — 4 rounds

**Examined:** R1–R4 at this base, 8 findings, all four rounds against the same nine-file diff. R4 returned APPROVE with zero. Every single finding across all four rounds was PROSE: a sentence in a plan, a test-file header, a report-doc caption, a ledger entry or the PR body that claimed something the code did not do. Zero findings touched behaviour, and the executable evidence never moved after round 1 — the harness was green on the shipped component and red under the `A39` mutant from the first dispatch to the last, with the same seven mutants killed and the same single-row `A39` attribution each time.

**Mechanizable:**

- **"prose names a symbol the file does not define" (R2).** One instance, `ReleaseProbe` for `CommitProbe`. `spec:lint` already has the adjacent arm — `CITATION_SYMBOL_ABSENT` fired correctly on `A39` while `A39` did not yet exist — but it only checks identifiers on a line that also carries a citation. The derivation that found this one is three lines of script: extract every backticked identifier from the arc's ADDED lines, subtract the symbols the described file defines. It is cheap enough to be an arm and it terminates, because both sides are finite.
- **"citations verified before an insertion, never after" (R1).** Thirteen citations went stale at once because this arc's own `A39` block added 25 lines to the file every one of them pointed into. `spec:lint` resolves citations but deliberately does not check what is AT the cited line — that limit is already recorded in `docs/agents/writing-plans.md` under `RED_TARGET_INVALID`, and this arc is another instance of exactly the shape it predicts. The re-derivation that fixed it reads each cited line and matches it to the symbol its sentence names, which no current arm does.
- **"a hand-maintained count in prose" (R1, R2, R3).** The same sentence carried a wrong number three rounds running: one code block, then three, actually eighteen fence lines. Not repaired by getting the number right a fourth time. The sentence now states the property and carries the grep that derives the count, which is the standing rule about storing self-invalidating values as commands rather than as text.

declined: this arc files no ledger row of any facing under any exception clause, by standing instruction, and all three candidates are process-facing under the 2026-08-25 mint freeze. Their done conditions are properties of the tooling (a lint arm pins what it claims, a citation resolves to the symbol it names), which is precisely the shape the freeze's admission test excludes, so they are recorded here as the documented limits they are rather than minted as rows. The one nearest to admissible is the extract-and-subtract symbol check, and even that is not blocked on anything: whoever wants it can read the derivation above and build it.

**Judgment:** R3's Playwright finding is the one no arm settles. The DEFERRED row this arc closes said in its own words that Playwright cannot schedule inside the commit-to-passive window, and I restated that as established fact in four places. What is actually established is narrower and is better evidence for the arc: `T-FLASH-COPY-RACE` releases the promise only after `await rotate(page)` returns (`tests/e2e/share-link-flash.spec.ts:466-469`), so the seven browser passes show no SHIPPED browser row rescues the credit, and say nothing about what some other Playwright test might do. Deciding that an inherited claim needs downgrading rather than repeating is a reading of the evidence, not a lint. R2's settlement-timing wording is the same kind: `settle()` runs inside the layout phase and the CONTINUATION lands after it, a distinction that changes no behaviour and every sentence describing it.

**Infra:** the reviewer's sandbox denied Vitest's temporary-directory creation with `EPERM` in R1, R2, R3 and R4, so it never once executed the suite directly. Its behavioural verification came from retained artifacts in three rounds and from an in-memory probe of the component source in the fourth. That is worth recording rather than shrugging at: four rounds of review ran against a subject the reviewer could not run, and the one thing that made those rounds sound was that the arc had already committed the run outputs the reviewer could read.

**Why four rounds, honestly.** The findings did not decay — 2, 4, 2, 0 — and the reason is visible in the classes above: rounds 1 and 2 each repaired the instances a round named instead of the class it was an instance of, and round 3 paid for that twice. The round that converged was the one where the sweep produced a derivation (extract-and-subtract for symbols, re-read-every-cited-line for citations, delete-the-count for the census) rather than a longer list of fixed sites.
