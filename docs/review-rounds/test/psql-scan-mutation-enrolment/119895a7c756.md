# Review-round filing — test/psql-scan-mutation-enrolment @ 119895a7c756

## plan — 4 rounds

**Examined:** R1 BLOCKING (7: zsh parse failure on unquoted site ids in every checker command; seven task markers without a same-command red→green cycle; two uncovered survivor-drift paths; a false 8+7+8+8 partition claim; wrong boundary attribution for both batch-C terse-command sites; a materially wrong export/consumer map for batch D; the full gate never re-run against the final committed floor). R2 BLOCKING (2: the replacement batch-red mechanism relied on vitest `-t` exiting non-zero on zero matches, which live vitest treats as skip/exit 0; Task 1's red derived from the helper script not existing — the anti-tautology rule's named banned shape). R3 NEEDS-ATTENTION (2: the zero-kill contingency removed a marker inside a checked region, which `spec:lint`'s task contract rejects — probed as TASK_MARKER_MISSING at all four batches; the accepted-gap path omitted the spec's same-PR `BL-` filing mechanics). R4 APPROVE, 0 findings.

**Mechanizable:** a red= command that invokes `vitest ... -t '<pattern>'` can never satisfy the observed-red contract (no-match is skip, exit 0) — a static `spec:lint` check over red-contract regions could reject the shape outright, the same way `RED_CONJUNCTION` flags `&&`. Likewise a red= whose command embeds an unquoted `<`/`>` token (a mutation site id) fails in zsh before it can express any verdict; a shell-parse dry-run (`zsh -nc`) of red-state=live commands would catch both this and the quoting class at authoring time. Both are filed together as `BL-SPECLINT-RED-COMMAND-SHAPE`.

**Judgment:** the durable difficulty was fitting the red→green task contract to work whose deliverable is a JUDGMENT (a disposition that may legitimately end "no test changes, ledger row instead"). Three designs failed in sequence — describe-block match counts, helper-absence, marker removal — before the stable shape landed: anchor each batch's red on a DESIGNATED expected-kill site through the single-mutant checker (a real coverage defect of the production surface), with written contingencies for re-anchoring (site blessed equivalent) and whole-task deletion (zero-kill batch). That shape is reusable for any future survivor-disposition arc; the contingency prose belongs in the plan template, not re-derived per arc.
