# Round-economy filing — test/resurrect-mobile-safari-e2e @ 9bd0a8456151

## spec — 4 rounds

**Examined:** all four rounds of `docs/superpowers/specs/ci/2026-08-09-resurrect-mobile-safari-e2e-design.md`
(R1: 5 findings, BLOCKING; R2: 3, BLOCKING; R3: 3, NEEDS-ATTENTION; R4: 1, NEEDS-ATTENTION) —
12 findings total, every one probe-confirmed against the live tree, none reversed later.

**Mechanizable:** two shapes worth extracting.
(1) *Allowlist-semantics lookup before proposing registry edits* — R1-F1 (wired specs must
RECLASSIFY, not remove, their coverage rows because paths-ignore workflows are never "covered")
was knowable from the scanner's own test case ("rejects a pull_request paths-IGNORE filter too")
and the four existing precedent rows; a drafting-time grep of the registry's classification
constants against the target workflow's trigger would have caught it pre-dispatch. Same lesson as
the live-code-citation pass, but pointed at REGISTRY SEMANTICS rather than symbols.
(2) *Skip-form-complete census* — R2-F1's sixth skipped block escaped a `test.describe.skip(`
token grep because it is line-wrapped (`test.describe␤.skip(`). Any census of skip/fixme forms
must match the wrapped form; the spec now bakes that pattern into its own verification step.

**Judgment:** the R1-F3/R4-F1 pair is one design thread, not drip — R1 established desktop
executions of crew-page are counted no-ops; R4 found the repair's enrollment mandate
(`expectWired`) collides with the file's now-vestigial project gates. Each round's finding was
only visible after the prior repair existed. Four rounds for a spec that rewires three guard
registries at once is within the economy bar; convergence came from probes (early-return site
counts, regex collision tables, readyState/bodyPresent parse-timing fixtures), not prose
iteration.
