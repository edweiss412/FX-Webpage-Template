# feat/theme-persistence-note — review-round economy filing

Base `40b62d397ea4`. Corpus: `40b62d397ea4.jsonl`, plus `9e949297f51d.jsonl` — the arc's fourth
round ran after a conflict-resolution merge moved the merge-base, so it is round 1 AT THAT BASE and
the counter sees 3 here. Filed anyway: the ARC ran four diff rounds, and a filing that appears only
when a single file's counter trips would miss exactly this split-base case. (The round-4 row in this
file is the dispatch killed mid-run once the PR turned out to be `CONFLICTING`; it carries
`status: no_verdict` and is not counted.)

## diff — 3 rounds

R1 BLOCKING (3) · R2 BLOCKING (2) · R3 BLOCKING (3) at this base, then APPROVE (0) at
`9e949297f51d` after the merge — four rounds of review across two bases.

**Examined:** the whole branch diff each round — the hook state and its mount-sync preservation, both
controls' rendered notes, the new Playwright spec and its five enrollment surfaces, the merge with
main's switch-person alert, the impeccable gate record, the contrast pin, and the ledger archive.

**Every finding but one landed on the PAPER TRAIL, not the behavior.** Eight findings across three
rounds: two on invariant 12 commit ordering, three on invariant 7 (the canonical spec not describing
what shipped), one on invariant 8 (a P1 dispositioned without the `DEFERRED.md` entry the invariant
names), one on invariant 8's re-run requirement after a UI repair, and one on a stale workflow
census. The shipped product code drew ZERO findings in all four rounds. The one behavioral defect on
this arc — a live region mounted inside the popover's conditional render, which arrives with its
message and is never announced — was caught by neither the reviewer nor the impeccable pair, but by
the repo's own class guard (`tests/components/_metaLiveRegionMounting.test.ts`,
`BL-ANNOUNCE-REGION-UNMOUNT-CLASS`) on the first full-suite run.

**Mechanizable:** three of the eight are machine-checkable and two already have a machine that
checks them, just not one this arc consulted early enough.

- **Commit ordering (R2 F1, R3 F1)** is a pure function of `git log` and the ledger grep: "the
  archive is the last authored commit AND no `IN PROGRESS` marker survives". It was raised twice
  because the first repair was a reorder, and then more repairs landed on top — the ordering is not
  a state you reach, it is an invariant you re-establish after every repair. A pre-push hook that
  re-checks it costs nothing; today it takes a review round to notice.
- **Spec/code divergence (R3 F2)** is the same shape as the guard that DID fire: the repo pins
  live-region mounting mechanically, but nothing pins that a spec's §2.2 still describes the shape
  the component renders. That one is genuinely hard to mechanize in general, so the cheap version is
  a habit rather than a test: when a repair changes the ARCHITECTURE (not the styling), the spec
  edit belongs in the same commit as the code, and both reviews of this arc's repairs would have
  been unnecessary.
- **The workflow census (R2 F2)** is a `--list` invocation. `163 tests in 10 files` is derived, not
  asserted, so the comment can only go stale silently — a test that re-derives it would have failed
  in the same commit that added the spec.

**Judgment:** R3 F3 — that the impeccable pair certified a UI shape one commit before the final UI
shape — is a genuine gate-integrity finding no counter would produce, and re-running the pair on the
delta immediately paid for itself: the scoped audit found duplicate accessible-name exposure (the
visible paragraph and the announcer both holding the same string, read twice in browse mode) and an
inert `suppressHydrationWarning`, both repaired. The lesson generalizes past this arc: a repair that
lands after the gate is an ungated repair, and the smaller it looks the more likely it is to be
waved through.

**Infra:** every round reported that Vitest could not START in the reviewer's sandbox (denied
temporary-directory creation), so no round of this arc executed a test — all four verdicts were read
from source plus targeted probes. The briefs stated the implementer's own run results from round 2
onward, which is what let the reviewer reason about behavior at all. Separately, the round-4 dispatch
had to be killed and re-run because the PR was `CONFLICTING` against a moved `origin/main`: that
state fires ZERO pull_request CI, and the tell is an empty non-Vercel check list rather than an
Actions outage.
