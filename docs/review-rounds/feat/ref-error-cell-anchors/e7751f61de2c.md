# feat/ref-error-cell-anchors — review-round record at e7751f61de2c

One base for the whole arc so far (`e7751f61d`, no merge of `origin/main`). The spec stage closed in three counted rounds (BLOCKING/5, NEEDS-ATTENTION/2, APPROVE/0) and owes nothing here. The plan stage reached the cap at R4 and closed at R5.

## plan — 5 rounds

**Examined:** R1–R5, 27 findings: 12, 9, 5, 1, 0. Every one admissible under the brief's consequence bound, none refuted, none reversed. R5 was dispatched past the cap on bl-orch's authorization (13:44 CDT, wave-common rule 16), with the record above already filed, and returned APPROVE.

**Mechanizable:** four classes, each parked with its slug.

Placeholder snippet bodies (R3, four of its five findings): a fenced `ts`/`tsx` function or `it` body written as `{ /* what it would do */ }` compiles, passes vacuously, and reads to a reviewer as an assertion that does not exist. A `spec:lint` arm over fenced code (a block whose only statement is a comment, or a non-void function with no `return`) is decidable from the document alone.

- declined: LIM-PLACEHOLDER-SNIPPET-BODY, a plan-lint arm is process-facing under the 2026-08-25 freeze and no product arc was blocked by it; parked with the default three-arc re-file trigger.

Snippet identifiers the target file does not import (R2 F1, swept over every RED step): the snippet typecheck the writing-plans rule prescribes was run on the pure snippets in a scratch harness, which cannot see the file the snippet lands in, so `join`, `premise` and `readFileSync` resolved there and not in `tests/drive/synthesizeBlocks.test.ts`.

- declined: LIM-SNIPPET-IMPORTS-UNRESOLVED, same freeze; the repair that held was an imports sentence at every RED step, which is authoring discipline rather than a lint arm.

A `red=` whose only red was the absence of the file the task itself writes (R1 F9, the old Task 6).

- declined: LIM-AUTHORED-RED, parked by six arcs before this one; this is the seventh naming, and the class's narrowed re-file trigger names an observable this arc does not have.

Lint advisories waved through (R2 F9): `spec:lint` flagged the exact stale `leadingColumnNormalize.ts` citations as `CITATION_SYMBOL_UNMATCHED` before R1 was dispatched, and the brief's "each cited line verified by hand" was not true of those two.

- declined: LIM-ADVISORY-UNDISPOSITIONED, an existing slug; the advisory was right and the disposition step was skipped.

**Judgment:** the one design finding and three ordering calls.

- R1 F3 asked for one unreachable workbook variant to be replaced. The probe that answered it (`docs/superpowers/specs/probes/2026-08-29-leading-column-reachability.*`, seven shapes) showed the whole code is unreachable from this exporter, the same class as `ROW_CELLS_FUSED`, so the spec was amended (§1, §2.1, §5, §8) rather than the plan patched. Wider than filed, cheaper than the round it would otherwise have cost.
- R1 F4 (the grain pin's untested upgrade half) was repaired by deriving two halves over `CELL_ANCHORED_CODES` with a partition premise, rather than by adding the one missing case; the spec's T5 wording was narrowed to match ("every member whose branch can yield a cell").
- R2 F6–F8 were one ordering defect in three places (harness dispatch, live check and evidence commit all before the push that makes them measurable); Task 6 was re-sequenced once, with the 2026-08-18 ruling quoted so the "review covers what merges" rule and the wrapper's corpus-row writes do not contradict each other.
- R4 F1: "DB-free half" named `pnpm test:fast`, a full-suite runner, and a `tests/admin` directory glob that holds DB suites. Repaired by naming the `parallel` project and the four client-free serial files explicitly, which is what the term should have meant from the first draft.

**Infra:** plan R1's first dispatch died `interrupted` at 09:51 (the harness killed its background task; re-dispatched detached via `subprocess.Popen(start_new_session=True)`). The Codex usage limit then held the plan stage from 11:28 to 12:28 CDT; the seven `--stage task` canary rows in the JSONL are the probes that found the resume, and count toward nothing.

## diff — 4 rounds

**Examined:** R1–R4, 3 findings: 0, 1, 1, 1. Cross-base total is the same 4 — this branch
has one base, `e7751f61d`, and never re-merged main. **Every finding landed on the
CLOSEOUT'S EVIDENCE; none touched production code, which R1 approved and which has not
moved since.** That split is the whole story of this stage: the implementation converged in
one round, and the account of it took three more.

**Mechanizable:** two of the three. Both are instances of classes this corpus has already
parked, so they are cited rather than coined as twins, and both are parked again under the
2026-08-25 process freeze: neither blocked a product arc, and a lint arm over evidence
tooling is process-facing by construction.

An extractor quoting a count with nothing to check the count against (R2). The drift
extractor was a block parse that also swallowed vitest's source excerpts, so §4 published
826 records and a three-operator list, and asserted on that basis something false about
which shapes had drifted. The instrument was never shown finding a known total.

- declined: LIM-SWEEP-POSITIVE-CONTROL, an existing slug whose shape is exactly this — a
  sweep's "exactly N" quoted without the instrument being demonstrated against a known
  member. The repair that held is worth copying and is cheap: vitest prints its own array
  length in the assertion it fails on (`expected [ …(74) ] to deeply equal []`), so the
  extractor asserts its per-shard set size EQUALS that declared length. An external oracle
  the instrument cannot talk itself past; it would have caught this defect with no reviewer
  in the loop. Trigger unchanged and not fired: no product arc was blocked.

A comparison that cannot fail (R3). The committed differential resolved an untracked
extractor and had no premises, so a missing file or a failed download would have produced
two EMPTY sets and reported them `SAME` — inside the evidence for an acceptance criterion.

- declined: LIM-NONDISCRIMINATING-FIXTURE, whose shape is an assertion that passes for a
  reason unrelated to the property, and whose stated general repair is precisely what was
  applied here: a negative control and a premise. Five per-shard premises now abort
  non-zero, and the negative control — two nonexistent run ids, expecting exit 1 rather
  than agreement between two empty sets — is the part that proves the abort fires. Trigger
  unchanged and not fired: this arc authored the fixture it stalled on, which the trigger
  excludes.

**Judgment:** R4's finding is not mechanizable and should not be. It caught a sentence in
§6 asserting that later documentation commits are not executed by the `parallel` project,
which is false because that project includes `tests/docs/**`. No linter is going to
adjudicate whether a claim about a test project is true; reading it against
`vitest.projects.ts` is exactly review's class. Notably it appeared in a bullet written
while sweeping for overstatements — which is the honest lesson: the sweep that removes two
overstatements can introduce a third, and only a reader catches that.

**Infra:** none this stage. All four dispatches returned a verdict on the first attempt,
the native binary resolved on each, and no run was reaped.
