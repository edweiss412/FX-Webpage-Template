# Review-round filing — feat/admin-ui-surfaces @ 50a803371712

## spec — 4 rounds

**Examined:** the 18 findings across rounds 1–3 (8 / 5 / 5; round 4 APPROVE with 0) on `docs/superpowers/specs/step3-onboarding/2026-08-15-step3-crew-preview-and-opslog-disposition-design.md`, against the round-economy question: which of these were mechanizable pre-dispatch, which were genuine judgment, which were infra.

**Mechanizable:** the dominant class (R1-2/3, R1-5 in part, R2-5, R3-4 — roughly a third of all findings) was "the adapter/spec claim omits a transform or gate the live projection applies." A projection-parity checklist derived mechanically — enumerate every statement in `readShowDataForViewer` between the reads and the return, then require the spec's census to cover each — would have caught the hotel filter, the restriction normalization pair, the runOfShow gating, the financial nulling, and the LEAD-or-FINANCIALS gate before round 1. That sweep now exists in the spec itself (§2.2 governing rule + census); for future adapter-shaped specs the pre-draft pass should run it against the SOURCE function first. A second mechanizable class (R2-1, R2-3, R2-4): claims about a component's behavior citable to a line the spec never read (StaleFooter escalation, fileId-only agenda rendering, parameter-default-fires-on-undefined) — all reachable by the existing "read every render path of every component the spec names" rule (`docs/agents/spec-self-review.md` render-path enumeration), which was run for `CrewShell` but not for the SECONDARY components the degradation table named. Lesson applied: render-path enumeration extends to every component a degradation/suppression claim names, not only the primary subject.

**Judgment:** R1-1/R2-2 (where the decode-trust boundary really sits, and that a descendant RSC throw escapes a page try/catch), R1-4/R1-5 (which child affordances misbehave with a synthetic show id), R3-1 (jsdom cannot prove segment routing) were genuine design findings — each changed the design (runtime-grain normalization contract, segment `error.tsx`, explicit-null cardReport widening, fault-hook boundary arm) rather than a citation.

**Infra:** none — all four dispatches returned verdicts on attempt 1 with the lint arm present.

## plan — 6 rounds

**Examined:** the 24 findings across rounds 1–5 (11 / 5 / 4 / 3 / 1; round 6 APPROVE with 0) on `docs/superpowers/plans/step3-onboarding/2026-08-15-step3-crew-preview.md`, against the round-economy question: which were mechanizable pre-dispatch, which were genuine judgment, which were infra. Round 5 hit the arc's cap with one finding open; its repair (leaf sweep recurses every array index) was applied post-cap, the disposition escalated to the user per the arc brief rather than dispatching a sixth round unilaterally, and the user authorized exactly one confirmation round fenced to that repair — round 6 returned APPROVE/0.

**Mechanizable:** the dominant multiplier — five findings across four consecutive rounds (R2-1, R3-1, R4-1, R5-1, plus R2-2's oracle sibling) — was ONE axis: the malformation sweep's completeness mechanism repeatedly enumerated less than it claimed (hand list → per-family list → index-0-only recursion), each round widening the enumeration one step. The class repair that should have landed at round 2 is the one that landed at round 5: a full recursion plus an executable non-vacuousness premise (sentinel paths asserted present), i.e. a derivation, not a longer list — exactly the AGENTS.md class-sweep rule ("sweep to a derivation"; a sweep verified by enumeration re-opens the moment someone adds a site). Second mechanizable class: registry fan-out (R4-2 `PROTECTED_ROUTES`) — a pre-draft "walk every disk-walked meta-test a new `app/admin` page can trip" pass would have caught it before round 1; the fan-out memory index existed and was not consulted for the ROUTE-audit registry specifically. Third: tautology-shaped test gaps (R2-3 FINANCIALS-only, R3-2 vacuous M/D oracle, R4-3 mocked-only not_found) — all instances of the global anti-tautology rule, catchable by asking "which implementation deletion still passes every named case" per task before dispatch.

**Judgment:** R1's structural findings (own module for `lookupStagedRow` so the route test can mock it; CrewShell module-mock boundary; tests-before-helper ordering; marker-removal sequencing R3-4/R4-style) genuinely shaped the plan's task topology. R2-5/R3-3's ordering contradictions were real single-line defects a fresh reader catches — cheap rounds, but not mechanizable without a plan-wide step-consistency checker that does not exist.

**Infra:** none — all five dispatches returned verdicts on attempt 1 with the lint arm present. (One TaskOutput retrieval error on the round-4 poll was harness-side, not a dispatch fault.)
