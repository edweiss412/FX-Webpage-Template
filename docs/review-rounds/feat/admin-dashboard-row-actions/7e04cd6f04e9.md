## spec — 4 rounds

**Examined:** rounds 1-4 of the dashboard row-actions spec. R1: five shipped-contract mismatches (slug arg, shrink_held two-phase flow, non-catalog sentinels, typed-result-vs-409, DESIGN.md destructive recipe). R2: seven — the server/client render-function boundary, tier-2 held Accept, the CrewRowActions-not-AppHealthPopover a11y precedent, sentinel AC placement, residual 409 prose, openHref modal target, extraction-vs-fence contradiction. R3: four — a silently no-opped AC-5 repair, missing keyboard ACs, preview-as reachability staleness, a pseudo-path cite. R4: one — the overflow-hidden clipping/portal contract.

**Judgment:** the R1/R2 density reflects a spec drafted from an investigator summary against a deeply-shipped surface — every finding was the reviewer reading the shipped components closer than the summary did. The R3 no-op-replace finding is process, not design: a string-replace repair that silently missed is indistinguishable from an unrepaired finding, and the fix is verifying the patch landed (grep after edit), which this session now does.

**Mechanizable:** the silent no-op replace class — a repair commit whose claimed edits should be grep-verified against the committed file before the next dispatch. Cheap to self-enforce; no new tooling filed.

**Infra:** the first R4 dispatch died in the harness background-task sweep (four concurrent dispatches killed at 21:2x); re-dispatched detached and completed.

**Disposition (post-R4):** ship-and-fence. R4's single finding is repaired with an explicit portal/collision contract plus a real-browser geometry AC; further prose rounds would re-verify contracts the implementation's Playwright assertions prove executably. Plan-stage review re-reads the spec whole.

## plan — 4 rounds

**Examined:** R1: eight (geometry ordering, e2e wiring, extraction fences, a11y assertion gaps, canonical closeout). R2: fourteen (server-boundary composition already fixed in spec; flip ownership, destructive-recipe registry, screenshot dispositions, reviewed-SHA ordering, four-mutant rows, portal primitive resolution). R3: six (primitive already flips; audit distribution; document-vs-table scroll; status roles; per-task mechanical sweep; popover path filters). R4: five (one REFUTED with the repo-ratified result-object citation; close-after-refresh ordering distribution; spec AC-7 scroll sweep; Task-3 AC annotation; alert-role anchors).

**Judgment:** the volume reflects a UI plan graded against every writing-plans clause on a deeply-shipped surface; task structure stabilized at R2 and every R3/R4 finding was consistency or citation grain, with one R4 finding refuted against the ratified cross-arc style ruling. The scroll-semantics train (R2 F7 → R3 F3 → R4 F3) is the classic follow-the-correction sweep: each fix now sweeps spec AND plan in the same commit.

**Mechanizable:** amendment sweeps — when a repair changes a contract word (scroll target, role name), grep BOTH documents for the old word in the same commit. Compliance rule, no tooling.

**Infra:** all rounds detached after the 21:2x sweep.

**Disposition (post-R4):** ship-and-fence. R4's surviving findings are all repaired; the one refutation is recorded with its citation; the implementation's own whole-diff review, impeccable dual-gate, and geometry e2e re-verify everything a fifth prose round would re-read.
