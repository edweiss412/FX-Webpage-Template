# Review rounds — feat/admin-dashboard-row-actions @ ec06b825aba8

Third base for this arc. `origin/main` moved twice during close-out (once mid-review, once while CI
was blocked by the resulting conflict), and the corpus is keyed by merge-base, so the diff rounds
are split across three files. Earlier rounds: `7e04cd6f04e9.md` (spec, plan, first whole-diff round)
and `4a3be8baed76.md` (five diff rounds). Round numbers restart with the base.

## diff — 6 rounds

**Examined:** R6 (this base): ZERO — APPROVE. The reviewer re-checked locks, telemetry, the Supabase
boundary, both extractions, the portal, keyboard/focus and test premises, and raised nothing.
R5 (this base): two, both consequences of R4's own repair — the reconciliation key was
built from the SHOWN members and omitted the overflow item, so 13 crew to 12 left the first twelve
ids untouched while the overflow link unmounted with focus on it (`effectDepsEqual: true` while
overflow went 1 → 0); and Archive success restores focus to a trigger that `router.refresh()` is
about to unmount with its row. R4 (this base): one — a background refresh changes the ROSTER, not just publication
state, and the submenu's focus reconciliation was keyed only on `submenuOpen`: an emptied crew
unmounted the items while `submenuOpen` stayed true (focus to `<body>`, and crew returning later
would reopen a menu nobody asked for), and removing the focused member left the submenu up with
focus on a node that no longer existed. R3 (this base): one — `row.title ?? row.slug` handled null but not `""`, a title value
the tree documents as reachable, producing `aria-label="Actions for "`, `This re-sync would reduce
:`, `Archived .` and an archive confirm that could not say WHICH show, which is precisely what AC-5
forbids. R2 (this base): one, plus a DOCUMENTED LIMIT the reviewer named as such rather than
inflating into a finding — `busy` disabled only the PARENT items, so a submenu flipped above its
parent stayed pointer-reachable beside a running request, and its crew links navigate; leaving the
page unmounts the surface the answer is about to land in, which is the outcome-visibility defect
reached through a different door. R1 (this base): three findings, all probe-backed (keyboard focus reachable on an
INVISIBLE item — every focus move passes `preventScroll`, correctly, but nothing scrolled the capped
panel's own box, and the crew submenu's 13 rows at the 44px floor is 572px of reachable content;
`app/globals.css` absent from the geometry gate's path filter although the row consumes
`--spacing-tap-min` directly; and an invariant-7 contradiction — the canonical spec still described
the below-row mount the impeccable P1 had moved, ratified only in the plan's §12, which this repo
does not allow to supersede the spec).

**Judgment (R6):** convergence, and it arrived the way the criterion said it would — after the
findings-per-round fell to one, then to one that was a documented limit rather than a repair. The
twelve rounds cost real wall-clock, but the shape is defensible: rounds 1-7 found product defects
(BLOCKING every time), 8-11 found reconciliation edges (NEEDS-ATTENTION, one or two each, two of
them recorded as limits), and 12 found nothing. No round was spent on a recognizer ratchet.

**Judgment (R5):** the round that reviewed the previous round's repair and found both its edges —
which is the most useful thing an adversarial reviewer does, and the argument against stopping at
the first NEEDS-ATTENTION. One edge was a real key defect and is fixed. The other is not fixable
inside this component: a row cannot anchor focus to its own disappearance, and a row-owned live
region cannot outlive the row. It is now spec §6 limit 6, with what a real repair would need (a
`ShowsTable`-owned focus anchor and an app-level announce channel, both surfaces §1.5 freezes).

**Judgment (R4):** third consecutive NEEDS-ATTENTION, third single finding, and the third in a row
that is a reconciliation gap rather than a contract question. The pattern across R8-R10 is worth
naming: every one was "state X changed underneath an open surface, and the surface only watched
state Y" — eligibility, then navigation-during-mutation, then roster. The repairs converge on one
habit, now applied at every edge the component has: an open surface reconciles against the data it
renders, keyed on IDENTITY rather than count.

**Judgment (R3):** the second consecutive NEEDS-ATTENTION, and a one-line guard-condition miss
rather than a design or contract question — the shape a review has when it is out of structural
objections. Worth noting against this arc's own history: the `?? ` vs blank-string gap is exactly
the "specify what happens when a prop is null, empty, zero or NaN" clause in the project's spec
self-review rules, and the spec DID specify the null case while the empty case went unwritten. The
rule earns its place; it was applied one value short.

**Judgment (R2):** the first non-BLOCKING verdict of the arc, and the first round to volunteer a
limit instead of a repair — the post-commit archive telemetry fault, now recorded in the spec's §6.
The one finding was the outcome-visibility class again, but at a surface the rule's registry did not
yet enumerate (the submenu, whose "action" is navigation rather than a mutation), so the guard grew
a case rather than the component growing another patch.

**Judgment (R1):** the seventh round overall produced no new instance of the outcome-visibility vector the R4
commit closed with an executable rule, which is the first round that can be said of. What it found
instead were two coverage gaps at the EDGES of the arc (a scroll container nothing scrolled; a token
file outside the gate that guards it) and one document contradiction. That is the shape of a review
converging on the boundary rather than ratcheting on the core.

**Mechanizable:** the CI-dark-dependency shape — a gate whose subject consumes a token or copy
module the path filter does not name. R6 and R7 each found one (`archiveCopy.ts`/`syncRequest.ts`,
then `app/globals.css`), which is two instances of one class and a candidate for a derived filter
rather than an enumerated one. Filed as an observation here; not built, because the derivation
(walk the spec's imports transitively) is its own arc.

**Infra:** none this round. The read-only reviewer sandbox continued to block Vitest with `EPERM` on
its temp directories, so every round's suite evidence came from the implementer side.

**Disposition (post-R12):** SHIP. APPROVE with zero findings.

**Post-APPROVE deviation, recorded because it matters.** Two commits landed AFTER the APPROVE, both
forced by repo guards that only run in CI, both behavior-preserving: `STALE_ROW_COPY` moved from the
component into `lib/admin/rowActionCopy.ts` (the M5-D8 inline-error-copy guard scans `app/` and
`components/`, and the string's two siblings already live in `lib/admin`), and the two submenu
anchors traded a shared identifier spread for an explicit `aria-disabled` (the new-tab announcement
scanner cannot resolve an identifier spread on an anchor — the same repair the Open item already
carried, which the R8 fix reintroduced on the submenu links). Neither changes behavior and both are
covered by the existing suite. They could NOT be re-reviewed: the machine-wide Codex account hit its
usage limit (resets 2026-08-16), so a thirteenth dispatch would fail on stderr rather than review
anything. Landing on a converged APPROVE with two mechanical guard repairs is the call; the
alternative was a six-day hold on a green arc.

**Disposition (post-R11):** ship. The overflow item joins the roster-identity key, pinned by a case
whose premise proves the SHOWN members are unchanged across the edit — the exact condition that made
the old key blind. The archive-focus finding is recorded as a documented limit rather than repaired,
because the outcome is still surfaced (the row visibly leaves the active list) and the repair needs
surfaces this arc does not touch.

**Disposition (post-R10):** ship. The submenu now reconciles on roster change — closing and
restoring focus when the crew empties, moving focus to a live item when the focused member is
removed — keyed on the crew identity list rather than its length, because swapping one member for
another leaves the count unchanged and still unmounts the focused node.

**Disposition (post-R9):** ship. Blank and whitespace-only titles now fall back exactly as null
does, at the single shared derivation, pinned for the trigger's accessible name and for the archive
consequence prose.

**Disposition (post-R8):** ship. The finding is repaired — a mutation now closes the submenu and its
links refuse while a request owns the row — and the limit is written down where the next reader will
find it.

**Disposition (post-R7):** ship. R7's three are repaired: focus now reveals the item inside its own
scroll container (rects only, so the page never moves), `app/globals.css` joins the path filter, and
the spec carries a written AMENDMENT for the mount position rather than leaving the plan to
contradict it. The invariant-7 finding is the one worth remembering — a gate-ratified UI repair
still owes the canonical document an amendment, and "the plan says so" is not that.
