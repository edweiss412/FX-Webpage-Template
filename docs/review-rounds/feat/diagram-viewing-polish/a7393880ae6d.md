# feat/diagram-viewing-polish — round-economy filing (base a7393880ae6d)

## spec — 7 rounds

**Examined:** rounds 1-6, 21 findings. R1 (3): a missed live input path (wheel zoom), a CDP-on-WebKit impossibility in the verification plan, an unproved announce region. R2 (3): a contradictory intent threshold (1.01 vs any-scale), stale three-path cardinality, a rendered-log oracle that a pre-populated region defeats. R3 (4, BLOCKING): the gallery-root region is outside the aria-modal dialog (DESIGN.md's own exclusion rule), a failed lightbox trigger restores focus to body via a detached node (probe-confirmed), the desktop network gate had no live CI invocation, two citation inaccuracies. R4 (3, BLOCKING): the 220 ms AnimatePresence exit window as an unmodeled routing state, no executable oracle for the modal-local region, and executed-count floors that credit bare project-gated early returns as coverage. R5 (5, BLOCKING): per-slide isolation unverified, retarget not closed over successive failures, the exit-window focus half unproved, the route-into-exiting-dialog design refuted by a Framer-Motion frozen-props probe (replaced with a mutable-ref bridge + buffer/flush on onExitComplete), unpinned region labels. R6 (3, BLOCKING): the already-wired mobile case still enforced the superseded active-uses-original contract, session persistence untested (isolation alone passes a reset-on-selection boolean), and the exit-buffer oracle needed its three-phase form (empty before, both untouched while exiting, one addition after flush). R7: APPROVE, 0 findings.

**Mechanizable:**
- "a spec places a live region without checking the aria-modal exclusion boundary" (R1 F3 → R3 F1 → R4 F1 chain) — a DESIGN.md-derived checklist item for any spec adding announcements to a surface that also owns a modal: name the channel per modal state, including exit windows. Belongs in `docs/agents/spec-self-review.md`'s render-path enumeration bullet; no new BL row (the rule exists, the sweep did not reach transition windows).
- "executed-count floors credit assertion-free project-gated returns" (R4 F3) — already documented at `tests/cross-cutting/picker-flow-e2e-ci-wiring.test.ts:623`; the mechanization is a per-case×project oracle shape, which this arc now specifies and which future wiring arcs can copy.

**Judgment:** the zoom-gate direction itself (user-ratified), the 1.01 threshold choice, and the two-region topology were design calls; not lint targets.

**Infra:** none — every dispatch returned a verdict.

## plan — 6 rounds

**Examined:** rounds 1-6, 19 findings. R1 (7, BLOCKING): superseded assertions in the existing suite unhandled, a non-implementable oracle spec, a paraphrased-not-verbatim inventory with no site enumeration, a missing stale-onError guard, an unpinned shared focus-helper API, invariant-12 ordering vs review repairs, no red transcript. R2 (4, BLOCKING): a wrong regression-suite path, summary-cells labeled verbatim, review-not-covering-graduation, a stale consumer comment. R3 (3, BLOCKING): a P0/P1 disposition loophole, re-gates leaving stale §12 records, the new checker outside the guard-enrollment rule. R4 (2, BLOCKING): the R3 repairs had silently no-oped on string drift (re-applied with assertions). R5 (3): isolated-server red flag, subset-enrollment loophole, mobile-only prose sweep. R6: APPROVE, 0 findings.

**Mechanizable:** the "review covers what merges + graduation lands last" staging pattern (R1 F6 → R2 F3 chain, also hit on the tap arc) is now written identically in three plans — a candidate writing-plans bullet; files with the existing final-diff-ordering lint shape, no new row.

**Judgment:** the buffer/flush + ref-bridge design carried from the spec; plan rounds were execution-contract precision.

**Infra:** none.
