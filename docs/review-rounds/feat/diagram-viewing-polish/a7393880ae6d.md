# feat/diagram-viewing-polish — round-economy filing (base a7393880ae6d)

## spec — 4 rounds

**Examined:** rounds 1-4, 13 findings. R1 (3): a missed live input path (wheel zoom), a CDP-on-WebKit impossibility in the verification plan, an unproved announce region. R2 (3): a contradictory intent threshold (1.01 vs any-scale), stale three-path cardinality, a rendered-log oracle that a pre-populated region defeats. R3 (4, BLOCKING): the gallery-root region is outside the aria-modal dialog (DESIGN.md's own exclusion rule), a failed lightbox trigger restores focus to body via a detached node (probe-confirmed), the desktop network gate had no live CI invocation, two citation inaccuracies. R4 (3, BLOCKING): the 220 ms AnimatePresence exit window as an unmodeled routing state, no executable oracle for the modal-local region, and executed-count floors that credit bare project-gated early returns as coverage.

**Mechanizable:**
- "a spec places a live region without checking the aria-modal exclusion boundary" (R1 F3 → R3 F1 → R4 F1 chain) — a DESIGN.md-derived checklist item for any spec adding announcements to a surface that also owns a modal: name the channel per modal state, including exit windows. Belongs in `docs/agents/spec-self-review.md`'s render-path enumeration bullet; no new BL row (the rule exists, the sweep did not reach transition windows).
- "executed-count floors credit assertion-free project-gated returns" (R4 F3) — already documented at `tests/cross-cutting/picker-flow-e2e-ci-wiring.test.ts:623`; the mechanization is a per-case×project oracle shape, which this arc now specifies and which future wiring arcs can copy.

**Judgment:** the zoom-gate direction itself (user-ratified), the 1.01 threshold choice, and the two-region topology were design calls; not lint targets.

**Infra:** none — every dispatch returned a verdict.
