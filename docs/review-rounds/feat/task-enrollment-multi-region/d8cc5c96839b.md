# Round-economy filing — feat/task-enrollment-multi-region

## spec — 4 rounds

**Examined:** all four rounds of `docs/superpowers/specs/2026-08-09-task-enrollment-multi-region-design.md` (R1: 5 findings, R2: 2, R3: 1, R4: 0 APPROVE), plus the two `no_verdict` R4 rows (infra interruptions, not reviewer outcomes).

**Judgment:** the round-driver was corpus/origin fidelity, not design soundness — R1's largest finding was a worked example asserted against a plan whose heading census had not been re-probed (range headings), and R1/R2's supersession-map findings were incomplete enumeration of a ratified origin spec. The repair that converged was sweep-to-derivation: the map was rederived from a grep whose command is now inlined in spec §3, and the §6 test-migration rule was restated as a derivation (every fixture holding more than one `OPEN` line) rather than a list — R3's sole finding was exactly a fixture the hand-list missed, and the derivation caught it in-sweep. Lesson already covered by the AGENTS.md class-sweep rule; applied here at repair time rather than filing a new rule.

**Infra:** two consecutive R4 dispatches died to the box's background-task SIGTERM (~2 min in; reaper log shows `gated`, so not the codex reaper — the harness background task was stopped). Remedy per the existing post-merge-dispatch memory: detached `nohup … & disown` launch with cron-nudge polling for `result.json`; the detached run completed and returned the APPROVE. Wrapper behaved correctly throughout (`no_verdict`/`interrupted` recorded, fresh out dirs per attempt).
