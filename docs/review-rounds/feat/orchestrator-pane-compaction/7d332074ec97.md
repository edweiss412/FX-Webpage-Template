# feat/orchestrator-pane-compaction — review-round filing

Base `7d332074ec97`. The arc also holds `77de29e811af` (spec 8, plan 6) and
`ad9638fa9d01` (one diff round); numbering restarts per base, so the diff rounds
recorded here are 1..4 and the arc's running total is higher.

## diff — 4 rounds

**Examined:** every finding across the four rounds, against what would have
caught it earlier. Findings by round across the whole arc: 9 (3 P0), 5 (4 P0), 4 (2 P0), 4 (3 P0), 4 (2 P0).
All accepted and repaired; none argued away, and no `accepted-gap` row was
opened to absorb one.

**Mechanizable:** (`BL-SEND-AUTH-SINGLE-READ-LINT`, `BL-ENROLLED-SUITE-PLACEMENT-METATEST`)

- **One class produced a P0 in four consecutive rounds: a decision assembled
  from several reads taken at different instants.** r1 re-read the marker but
  authorized against a nonce captured earlier. r2 re-observed the pane but
  compared only the verdict. r3 compared everything but ran against the original
  roster. r4 fixed all of that and still read the marker twice, once inside
  revalidation and once for the nonce, so a takeover that changed `sessionId`
  between them preserved the nonce and passed rule 5 on the stale copy.
  Each repair addressed the input the finding named, which is why there were
  four of them. The closing repair is structural: one snapshot per
  authorization, with the classification and the nonce derived from the same
  read. **Mechanizable as `BL-SEND-AUTH-SINGLE-READ-LINT`: inside a send-authorization
  path, a surface read may occur at most once per pass.** Nothing in the suite could express
  "these two values came from different instants", which is why four rounds of
  green tests coexisted with the defect.
- **A second class, silent exclusion, produced findings in three rounds at four
  different readers:** an unparsable `gh` payload treated as checks, a corrupt
  marker indistinguishable from an absent one, a verdict row with an unparsable
  timestamp excluded but never named, and a corpus line skipped on a parse
  failure. Each reader independently chose "drop what I cannot read" over
  "report that I could not read it". **Mechanizable, declined: every reader that can discard an
  input must route the discard through the accept-set. declined: the four
  readers are now individually pinned by tests and there is no fifth reader in
  this surface, so a rule would range over one closed set already covered.**
- **Test placement is invisible to a passing suite.** Eight round-2 survivors
  existed because the new assertions lived in `adapter.test.ts`, which is not in
  this surface's `suitePaths`. They ran, passed, and bought no score.
  Mechanizable as `BL-ENROLLED-SUITE-PLACEMENT-METATEST`: every test file naming
  an enrolled surface's exports is either in its `suitePaths` or explicitly exempt.

**Judgment:**

- **Two of my own repairs introduced defects the next round caught**, and both
  are worth recording because they are the cost of repairing under round
  pressure. The round-4 gauge fix added a `lastIndex` reset that was dead code;
  the mutation gate flagged it. The round-4 authorization repair encoded roster
  disappearance as a stale report with a null nonce, which made the "left the
  roster" branch unreachable and produced a refusal that NAMED THE WRONG
  CONDITION -- an operator reading it would re-checkpoint a pane that no longer
  exists. A repair that silences a finding while adding a new failure mode is
  not a repair, and neither was caught by the suite that grew alongside it.

- The mutation gate found a defect I introduced *while repairing a review
  finding* (`GAUGE.lastIndex = 0`, dead code from the round-4 gauge fix). It was
  removed rather than ledgered as equivalent: a line that provably cannot affect
  behaviour is dead code, and an `equivalent` row would have preserved it
  permanently with a rationale for why it does nothing.
- Two `newestVerdictTie` counter mutants are ledgered equivalent on the strength
  of a 3616-sequence differential run, not on how the lines read. The same
  standard rejected the gauge reset.
- One of my own killing tests pinned a spec violation (first-wins on a corpus
  tie, where §3.5 requires `UNDETERMINED`). A passing test asserting the wrong
  contract is worse than no test, because it reads as coverage.

**Infra:**

- `-t <surfaceId>` does not scope this gate: `runSurface` executes at module
  scope during collection, so a name filter prunes reporting after every surface
  has already run. Scoping requires filtering the surface list before
  `registerSurfaceCases`.
- Corpus round numbers are per `(branch, baseSha)`. Passing the arc's running
  total forced two hand renumbers before the convention was applied.
