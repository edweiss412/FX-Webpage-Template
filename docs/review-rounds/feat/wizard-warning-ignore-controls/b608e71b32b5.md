# Review-round filings — feat/wizard-warning-ignore-controls @ b608e71b32b5

## spec — 4 rounds

**Examined:** 4 spec rounds (cross-base total 4; single base), 15 declared findings (3 + 3 + 5 + 4), zero APPROVE. R1 surfaced an incomplete chrome-consumer inventory, a wrong jump-anchor selector claim, and a nonexistent parallel read wave. R2 surfaced the design-changing fact: first-seen wizard rows have no `shows` row until finalize, which forked the backend by row linkage and pulled a `pending_syncs` column, a staged server action, and a dual-path finalize carry into scope. R3 held the fork but found five specification gaps (carrier chain, shadow-payload hop, taxonomy disjointness, one stale sentence, undefined result union). R4 verified the R3 design intact and returned four instance-level repairs (staged report sheet identity, two residual taxonomy sentences, an enumerated lock-holder list wrong in four details plus a mis-named registry template, one stale "pair").

**Judgment:** the round burn was dominated by two judgment classes. (1) Design-fact discovery: R2's no-show-row finding was a genuine unknown that reshaped scope — not recoverable by any lint, only by a reviewer probing the finalize path. (2) Enumerated forms decaying: R1's five-row count table, R3's caller list, and R4's residual "object without warnings" sentences are all the same shape — an enumeration hand-maintained in prose losing to the codebase. The R4 repair converts the worst of these (lock holders) to a derivation the plan re-runs, per the class-sweep "sweep to a derivation" rule.

**Mechanizable:** none new. The comma-citation and bare-`:N` citation forms that cost pre-dispatch lint cycles are already hard failures in `spec:lint` (CITATION_MALFORMED caught all of them before any round burned on it); the prose-count-parity arm that would catch stale enumerated counts is already filed (`BL-SPECLINT-PROSE-COUNT-PARITY`) and this arc adds no new evidence class beyond what that row records.

## plan — 4 rounds

**Examined:** 4 plan rounds (cross-base total with the spec stage: 9), 19 declared findings (10 + 5 + 3 + 1), zero APPROVE inside the cap. R1 was breadth (harness shapes, wiring coverage, invalid REDs, guard-walk premise, unobservable assertions, undispositioned rows); R2 was five residuals of those repairs; R3 surfaced a NEW class — commit-atomicity against fail-by-default meta-tests, plus two invalid RED shapes — repaired with a plan-wide sweep; R4 cleared everything except ONE instance of the R3 class (Task 6 retained the guard-red shape Task 13 shed) and explicitly confirmed the rest of the sweep complete.

**Judgment:** two judgment classes dominated. (1) Test-harness fidelity: the plan repeatedly named harnesses by plausible shape rather than reading them (injected-deps vs module mocks; proveAdminOutcomeBehavior's Response assertion; the fetchStep3 wiring suite) — each caught only by the reviewer reading the live suite. (2) RED-validity for guard-shaped tests: the honest "no chronological red" declaration had to be discovered over two rounds; once stated for Task 13 it was endorsed verbatim, and the R4 residual was the form not yet propagated to Task 6.

**Mechanizable:** none new. The `red-state=`/`red-target=` machinery (`spec:lint --exec-red`) already expresses the live-vs-authored red distinction this arc kept tripping on; this plan did not enroll (opt-in), and enrolment would have caught the R3/R4 RED-shape findings statically. That is an adoption gap on an existing arm, not a new arm — no row minted (process mint freeze).
