# Round-economy filing — feat/help-report-surface

## spec — 6 rounds

**Examined:** R1 raised 4 (oppositeSurface mischaracterized as sessionStorage hygiene when it is the error-copy audience fallback; a universal "every helpHref" claim false for exactly one catalog row; a hash-capture test that a mount-only reader would pass; a ringOffset token that cannot match the Callout's actual background). R2 raised 2 P1s (persisted-attempt identity vs live autocapture drift; `help`+non-null `show_id` bypass). R3 raised 2 P1s (hashless-sentinel scope collision with a literal `#no-code` fragment; `showTitle`/`showSlug` smuggling show-scoped copy past the null-`show_id` gate). R4 raised 1 P2 (spec recorded the unknown-surface route path as fail-closed when the live route has an admin-identity fallback). R5 raised 1 P1 (no test pinned the keyed remount for a mid-open hashchange — every prior case exercised close-then-reopen). R6: APPROVE, 0 findings. Every finding was probe-backed and repaired in the following commit; none was re-raised. Decay: 4, 2, 2, 1, 1, 0.

**Mechanizable:** a `spec:lint` arm that flags universal quantifiers ("every", "all", "always") over greppable code populations when the spec does not carry the count — R1's helpHref claim and R4's fail-closed claim were both quantified assertions a counting probe refutes in seconds.

**Judgment:** the R2/R3 attempt-identity train (persistence scope vs live context) is a genuine design-space discovery loop, not a compliance miss — hash-scoped surfaceId, then its sentinel collision, were each only visible after the previous repair existed. Two rounds for that class is close to floor.

**Infra:** none — all dispatches returned verdicts; no reaper kills, no rollout scrapes.

## plan — 6 rounds

**Examined:** R1 raised 8 (premise API named wrong; guard task ordered after the flips it guards, voiding its RED; a collection-failure RED; missing help auth-error rows; two non-discriminating doc probes; missing graduation-registry enrollment; a marker-strip/corpus-row sequencing contradiction; four-mutant protocol enrolled on one task of five). R2 raised 6 (corpus-row emission makes "last commit" operationally false — settled by merged precedent PR #750/#751; a dropped commit step; a variant mutant escaping name+ring assertions; a RED cause claiming type errors from a type-stripping runner; a suffix mutant passing a contains-assertion; missing registry-count reconciliation). R3 raised 2 coverage gaps (ring-wiring prop untested; §13.2.1 and M11-I-D-1 edits gate-less). R4 raised 1 (the §13.2.1 probe collided with §13.1's own helpCode text). R5 raised 2 (probe-numbering drift after R4's repair; the audience-fallback case claimed as a live RED when today's crew arm coincidentally renders the expected copy). R6: APPROVE, 0. Decay: 8, 6, 2, 1, 2, 0; no repair re-raised.

**Mechanizable:** a plan-lint arm asserting every task with a Step-N GREEN has a commit step (R2 F2 was a mechanical omission a heading-shape check catches); and a probe-collision check that greps each doc-probe pattern against the OTHER edits' required text before dispatch (R4 F1 and R5 F1 were both probe-anchoring collisions).

**Judgment:** the R2 sequencing finding was genuinely operational — the wrapper's post-verdict corpus emission vs invariant 12's "last commit" wording could only be settled by reading merged history, and the settled form (trailing docs/review-rounds-only commit) is now stated in the plan for reuse.

**Infra:** none — all dispatches returned verdicts; two long rounds (~15 min) but no reaper kills.

## diff — 2 rounds

**Examined:** Two counted rounds against this base (three dispatched — R2 returned no verdict to the wrapper and does not count); the arc ran five, because merging `origin/main` mid-arc moved the merge-base and the last two rounds landed in the sibling `bdcd336f3f9c` corpus. All five are described here so the arc reads in one place. R1 raised 1 BLOCKING — the invariant-8 dual-gate deferrals were filed in `BACKLOG.md`, while invariant 8 and the plan's §12 step both name `DEFERRED.md`; the probe was an `rg` over `**/DEFERRED.md` returning nothing. R2's reviewer returned `FINDINGS: 0 / VERDICT: APPROVE`, but the wrapper recorded `no_verdict` (`killedReason: attempt_timeout`, `error: "unkillable child"`) so the row is honest about what the wrapper observed, and the round is uncounted. R3 raised 1 P1: a submission that outlives its modal ran its terminal branch from a detached component and cleared the sessionStorage scope, rotating the idempotency key out from under whatever mounted in its place — a second GitHub issue for one report, with the first success never shown. R4 raised 1 P1 on the same surface one await deeper: the R3 guard sat between `await fetch` and `await response.json()`, so an unmount during the body read resumed past it. R5: APPROVE, 0. Decay: 1, (uncounted), 1, 1, 0.

**Mechanizable:** an ESLint arm for the R3/R4 class — in an async function that writes component state or shared storage after `await`, require a liveness check after EVERY await, not just the first. Both findings were the same defect at different suspension points, and the second was invisible to a test suite that only held the first promise. The `react-hooks` plugin has no such rule today; a bespoke one keyed on "await followed by a call to a known-shared writer" would have caught both before dispatch.

**Judgment:** R1 is a contract-placement question no scanner settles — two ledgers exist, both are legitimate homes for deferred work, and only invariant 8's own wording decides which one an impeccable-gate P1 belongs in. Worth the round; the repair also removed a duplicate-ledger habit.

**Infra:** two events, both wrapper-side, neither a Codex fault. R2's verdict text was complete and well-formed but the wrapper timed out at its 1380s per-attempt cap and recorded `no_verdict`; subsequent rounds ran with `--attempt-max-secs 1380 --total-max-secs 4200 --max-attempts 3` and returned cleanly. Separately, merging `origin/main` mid-arc moved the merge-base, which opened a second corpus file; the rows dispatched as `--round 4/5` are numbered 1/2 there, because rounds are numbered within a `(branch, baseSha12)` corpus and the CLI numbering had followed the arc instead. Worth mechanizing: the wrapper could derive `--round` from the corpus file it is about to append to, rather than trusting the caller.
