# feat/speclint-ac-unclaimed-arm — 44b0d74b1107

## plan — 4 rounds

**Examined:** 4 plan rounds at this base, the arc's only base, against one
document (`docs/superpowers/plans/ci/2026-08-26-speclint-ac-unclaimed-arm.md`).
Declared findings 10, 5, 7 and 2 — decaying, and the last round's two were the narrowest of the arc. All accepted; none disputed; no round
spent arguing. The spec stage is not counted here — it ran to its own cap on the
parent arc (`docs/review-rounds/feat/speclint-dispatch-gates/b30413cf5e51.md`) and
this arc opened no spec round.

**Mechanizable:** four shapes, three of which `spec:lint` can already see.

1. **A `-t` name filter in a red-contract region should be HARD, not advisory.**
   Round 1 spent three of its ten findings on this one shape (Tasks 1, 2 and 3),
   and `spec:lint` had already found it: `RED_TEST_NAME_FILTER` fired on all three
   markers, at ADVISORY. The pre-dispatch lint gate refuses on HARD findings only,
   so it dispatched a plan whose reds could report green from the moment they were
   written. Inside a region declared `red-contract`, a name filter is not a style
   preference — it is a red that cannot be observed. The advisory already carries
   the reasoning; only the severity is wrong.

2. **A `red=` naming exactly one corpus-equality file cannot discriminate.** Four
   findings across rounds 2 and 3 (Task 5 in R2, Tasks 4 and 5 in R3) were the same
   defect: an equality between a live set and a committed record is satisfied by
   both sides being empty, and by any regression that empties both. The pattern is
   recognisable without understanding the test — a `red=` whose file list has
   length one and whose target is a corpus walk. It wants the suite that pins the
   producer on the same command.

3. **A plan that quotes a command's output should have that command re-run.**
   Round 1's last finding was that the plan claimed 106 enrolled plans where the
   quoted command returns 108. `docs/agents/writing-plans.md` already requires
   sweeps to be authored AND RUN; a plan that prints a command beside a number is
   the same obligation with a checkable shape, and `--exec-red` already proves the
   machinery for running a plan's own commands exists.

4. **A criterion a plan declares and no task claims is this arc's own arm.** Round
   3 found that the plan promised to archive its ledger row and no task performed
   it, and round 1 found AC-9 declared with no executable owner. Both are
   `TASK_AC_UNCLAIMED` in prose form, against a plan that had not yet shipped the
   check. The arm this arc builds would have caught two of its own findings, which
   is the strongest argument for it in the corpus.

declined: all four are process-facing, and the process mint freeze of 2026-08-25
admits a process row only under `invariant` or `product-blocked`. None of these
qualifies — each one's done condition is a property of the tooling (a severity, a
`red=` shape, a re-run, a plan-prose check), which is exactly the shape the freeze
names as unable to finish. They are recorded here as the owning surfaces'
documented limits instead. Shape 4 is the strongest and needs no row at all: the
arm this arc ships is itself the mechanisation, and it would have caught two of
this arc's own findings. Shape 1 is one severity constant away and belongs to
whoever next opens `lib/specLint/redContract.ts` with a product-facing reason to
be there.

**Judgment:** one distinction did the real work and does not mechanise.

**A guard that verifies its evidence EXISTS is not a guard that verifies its
CLAIM.** Round 2's sharpest finding was that the residue's admissibility check
asserted a quoted line could be found at the line it named. That proves the
quotation is real. It says nothing about the row's actual claim — that the plan's
prose settles nothing — so an `unsettled` row for a criterion the plan assigns to
Task 10 could quote that very assignment verbatim and pass. The repair was to give
each row kind an executable PREDICATE pointing at the claim (no owner word beside
the id anywhere in the plan; or an owner the grammar demonstrably rejects), with
the quotation demoted to reader evidence. The same shape recurred in round 3 as
the walk premise: counting documents proves the walk was not empty, which is not
the claim "every enrolled plan was walked". A reviewer finds these by asking what
the assertion would still pass under; no lint rule asks that question.

Round 4 found both of its two by the same question, applied to the repairs rounds
2 and 3 had just landed: an `owner-inexpressible` row whose owner copies the
sentence period (`Task 10.`) satisfies the line-membership check and fails the
grammar for the wrong reason, so a settled criterion would be exempted as
inexpressible; and "every near-miss form still REPORTS" is a description rather
than a case list, so an implementation tolerating `(discharged by Task 10).`
passes every specifically named case. Both repairs NARROW — normalise the owner
before testing expressibility, and enumerate the near misses — which is the
pattern for the whole arc: every repair deleted something (a name filter, a
receipt, one side of a contradiction) or enumerated something, and none grew a
recognizer. That is why the rate decayed here rather than running flat the way the
round-economy retrospective's long arcs did.

## diff — 4 rounds

**Examined:** 4 diff rounds at THIS base against the whole diff, declared findings 5, 4, 3 and 1. Four reaches `ROUND_THRESHOLD`, so this section is obliged rather than permitted. All accepted; none disputed. The stage opened after the plan stage closed at its own cap, and the score was re-run rather than carried once a repair touched a declared input. The absorb of `8be90aba7` moves the base after this stage closed, so the final round examined this branch's contribution rather than the merged result; `git merge-tree` reported the absorb clean beforehand and none of the twenty absorbed commits touches a declared score input.

**Mechanizable:** accepted-row siteIds are LINE-KEYED, and that is a registry design property rather than an arc's mistake.

`tests/mutation/source/registry.ts` keys each accepted survivor by `operator:line:col:text`. Any formatter that moves lines invalidates every row below the edit, and `score()` in `tests/mutation/source/ledger.ts:79-91` excludes ledgered equivalents from the DENOMINATOR — so one mis-keyed row costs twice, appearing as an unaccepted survivor AND inflating the denominator. The failure is silent until a score run reports both `stale-ledger-row` and `unaccepted-survivor` for what is one underlying drift.

Measured on this arc: **37 re-keys across three score runs** (22 after the first insertions, 15 more after prettier reformatted within the 139-518 band), with two runs failing at 0.7906 and 0.9116 before the cause was separated from genuine coverage gaps. Independently on `fix/typo-v4-venue-shape` the same night: `f353ea4fb`, "re-key fieldNearMiss's two accepted rows after prettier moved their sites". Two arcs, one wire, hours apart.

declined: the class repair is not filed as a row. It re-keys every enrolled surface's accepted list, which is a redesign of a shared surface this arc does not otherwise touch — class-sweep exception (c) — and the 2026-08-25 process mint freeze admits a process-facing row only under `invariant` or `product-blocked`, neither of which this is: no product arc was blocked, and the cost fell entirely on two process arcs' own score runs. It lands here and in a note at the head of `tests/mutation/source/registry.ts` instead, with the re-file trigger being a third arc hitting it or a product arc blocked by it.

**The class repair is content-keyed siteIds** — `operator + from-text + to-text`, which is exactly how both arcs re-keyed by hand once the report named the drift. A content key survives reformatting and identifies the same mutant; the line number is then a hint for a reader rather than the identity. Not attempted here: it changes the registry's key format across every enrolled surface, which is a redesign of a shared surface this arc does not otherwise touch, and the process mint freeze puts it in a documented-limits record rather than the open queue. `tests/mutation/source/registry.ts` carries a two-line note at its head so the next arc verifies after its commit hooks instead of rediscovering this.

**Mechanizable, and the more useful of the two: three of the four diff rounds landed on ONE class, and my own round-1 repair is why.**

The class is residue-row field binding — a `tests/specLint/acUnclaimedResidue.ts` row carries `quote`, `nearMiss` and `owner`, and the corpus predicate at `tests/specLint/acUnclaimedCorpus.test.ts` must bind each to the line it cites or the row can lie. Round 1 found it on `nearMiss` and `quote` (`line.includes("")` is true for every line, so a blanked field passes). I repaired those two BY NAME. Round 3 found the id was unbound on the quoted owner line. Round 4 found `owner: ""` still passes, on the identical `includes("")` mechanism round 1 had already named.

Three rounds, one defect, one field at a time — the exact drip-feed the class-sweep rule in AGENTS.md forbids, committed by the arc that cites that rule. The rule also names the repair that would have stopped it after round 1: **sweep to a derivation, not a longer list.** A predicate that iterates the row's string fields and requires each to be non-empty and bound to its cited line closes the class; a predicate that names `quote` and `nearMiss` re-opens the moment someone adds a field, which is what happened. The enumerated repair is not a weaker version of the derived one, it is the thing that guarantees the next round.

Cost: two diff rounds. The general lesson is already written down, so what this arc adds is the measurement — the enumerated form failing on the very next round, twice, on a three-field record small enough that enumerating it felt complete.

**Judgment:** the diff stage's findings changed character between rounds. Round 1's five were about the arm and its records; round 2's four were about MY GUARDS being narrower than the claims attached to them — a predicate that verified evidence exists rather than that its claim holds, parity compared by count rather than identity, an exported helper outside the scored set. That is the ratchet signature the round-economy retrospective names, and it is worth recording that the repairs were narrowings and enumerations rather than recognizer growth: a documented limit where spec §4.3's own example cannot work, an id-free reason in the author-facing docs, and cases moved INTO the deciding suites rather than new machinery added around them.
