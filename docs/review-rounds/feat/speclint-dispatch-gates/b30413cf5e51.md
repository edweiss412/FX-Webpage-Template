## spec — 4 rounds

**Examined:** every spec finding across rounds 1-4 on `b30413cf5e51` — 6, 5, 4 and 4 findings, all BLOCKING — plus the two probe records and the per-id disposition classification the rounds forced into existence.

**Mechanizable:** none. Every round's findings were about THIS design's content rather than a shape a checker could have caught in advance — with one exception that is already mechanized and did fire: the arc's own `pnpm spec:lint` caught the spec at 12 hard on first draft, including four empty-path `CITATION_MALFORMED` citations of exactly the form the two incidents this arc closes were about. That gate ran before every dispatch and no reviewer round was spent on it, which is the whole thesis of the lint arm being tested on its own spec. `declined:` this arc files no ledger row of any facing (Eric's directive 2026-08-25), so a mechanizable gap here goes to the owning surface's documented-limits record, not to the queue.

**Judgment:** three of the four rounds landed on ONE axis — what counts as a declared acceptance criterion in a plan's body — and each found a genuinely new lexical class rather than a repeat. R1 refuted the id grammar (hyphenated segments, a 200-character window). R2 refuted its replacement (sentence-bounded collection is no help when the line is one sentence; no token-end boundary). R3 refuted the narrowing that answered R2 (dropping secondary ids removed four foreign ids and three real criteria, silently) and found the boundary still admitted `AC-1..AC-7` plus a fence-unaware walk.

That is the same-axis recurrence signature, and the round that mattered was the one where the repair direction changed. Rounds 1-3 each answered a lexical defect with a lexical rule, which is precisely the parser-growth the repo's own repair-direction rule forbids and which had a fresh corner available every time. R4's design does not refine the grammar at all: it declines to classify any declaring line carrying more than one id, records every such line by name, and takes its cut from a COUNT rather than a pattern. R4 says it does not yet, and its two structural findings are why: the spec still carried a pre-ambiguity row saying a disposition on a two-id line disposes both (mutually exclusive with declining that line), and "recorded by name" was a promise with nothing executable behind it, so a multi-id line added after the snapshot would have been declined in silence. Both are repaired, and the ambiguous set now gets the residue's own treatment: an exact-equality assertion, fail-closed. The shape of the decision is still the transferable lesson — a recognizer that has been refuted three times on one axis should stop being refined and start declining, and the declining boundary has to be structural or it is just the next pattern.

Two second-order costs worth recording. The measurement instrument was itself defective in three of the four rounds, and a spec that cites its own probe inherits every defect that probe has; the probe deserved the same review the spec got, earlier than it got it. And a committed report can go stale against the corpus it describes within one commit — R3 caught the report predating this arc's own plan, which is how the plan's unclaimed `AC-9` went unreported by its own author's instrument.

**Infra:** the round-1 dispatch showed live why a reviewer cannot execute a probe: Codex runs `sandbox: read-only` and `tsx` failed with `Error: listen EPERM ... tsx-501/33336.pipe`. Briefs from round 2 on cite the COMMITTED `.report.txt` and offer the script only as provenance. This is the same fact the spec states as its own motivation — a reviewer is blind to a gate-red by construction — arriving in the review of that very spec.
