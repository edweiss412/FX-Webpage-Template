# feat/diagram-failure-retry — review-round record at 6bfb58e4f66b

Filed because it is **owed**. The diff stage reached `ROUND_THRESHOLD` by the arc-sum rule
rather than at any single base: four counted rounds across three merge bases, three of them
here and one at `c6eafc19458f`. Each base move was a fold of `origin/main` mid-arc, which
opens a new corpus file and restarts `--round` at 1 by design, so no single file's counter
ever showed four.

## diff — 3 rounds

**Examined:** every finding across all four counted diff rounds of this arc — three at this
base and one at `c6eafc19458f`. Round 1 at the previous base returned 3, round 1 here
returned 6, round 2 returned 2, round 3 returned 1. Twelve findings. None was refuted; one
sub-claim did not reproduce and is recorded as such rather than repaired.

**Mechanizable:** one, and it is the finding that recurred most expensively — my own tests
passing while not exercising the defect. Five separate instances this arc, each caught by
something other than the assertion:

- a `tabIndex >= 0` filter that excluded the very element under test;
- nine call sites passing `rev=` where the prop is `snapshotRevisionId`, so the component
  received no revision at all (caught by typecheck);
- four focus guards that focused a control on the item that SURVIVES and removed a different
  one, so the focused node never left the document (caught by the reviewer);
- a modal-close test measuring mid-exit-animation state;
- a hook test passing an options-object argument positionally, so the override under test was
  never set — after which I concluded three times that the environment could not reproduce
  the defect. Typecheck found it in one line.

The mechanizable form is narrow and worth stating: **a test asserting that focus is not on
`<body>` must also assert that the previously focused node actually left the document.**
Without that premise the assertion passes on any test that removed the wrong element, which
is precisely how four guards shipped green. The same shape generalises to any
absence-assertion — assert the precondition that makes absence meaningful.

declined: process-facing under the 2026-08-25 mint freeze — the done condition is a property
of the test suite, not a number a crew member or Doug would notice moving, so it files to
`LIM-NONDISCRIMINATING-FIXTURE` rather than minting a row. That slug is exactly this shape
("an assertion whose extraction is narrower than the property in its own test name, so it
passes for a reason unrelated to the behaviour"), and this arc is enrolled there with its five
instances. Re-file trigger is the slug's own: a product-facing arc measurably blocked by it.

**Judgment:** three rounds were spent repairing the same three classes at successively wider
sets of call sites. Round 1 named two instances of each; I repaired those two and the next
round found the same classes at untouched sites. The rule against that already exists in
AGENTS.md, and I did not apply it until the second recurrence made it unmissable. The cost is
legible: had round 1's repairs been class-level, rounds 2 and 3 would have had materially less
to find. Round 3's single finding is the same story in documentation — I amended one site per
claim and left 54 mentions of dead mechanisms standing across the spec, plan and two test
headers.

**Infra:** none of the four rounds died. One dispatch was refused before spending a round
(`--stage plan requires at least one --lint-doc`), which is the wrapper failing closed and is
not counted here. Round 3's reviewer could not run a focused Vitest rerun because its
read-only sandbox denied temp-directory creation with `EPERM`; it declined to count that as a
finding, correctly.
