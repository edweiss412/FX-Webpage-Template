# feat/wifi-password-legibility — round-economy filing (base a7393880ae6d)

## spec — 17 rounds

**Examined:** rounds 1-5, 26 findings. R1 (6): architecture holes — client-boundary undecided, wrong announce role, wildcard geometry, missing focus treatment, no byte oracle, dark CI home. R2 (7): the dd flex-col wrapper, AC/invariant height contradiction, wrong ring offset, unproved rendered region, a status-swap regression of R1's own repair, two citation-precision misses. R3 (5): protruding negative-margin geometry, wrong height arithmetic against live tokens, unguarded focus and timer contracts, missing graduation registry rows. R4 (5): a wrong SSID testid, the `last:pb-0` containment break with a reachable fixture, a browser home whose seed cannot render the subject, an impossible same-row-twice oracle on the production route, one blank-line citation. R5 (3): the render-constant claim refuted (router.refresh under a live island — both precedent stale-value guards restored), the standalone config allowlists filenames so the harness home needed explicit registration, a content-box-vs-border-box oracle impossibility. R6-R9 (7): standalone registration detail, adapted-not-verbatim precedent claims, tabular inventory, ring-paint clearance, the write-ordering race + live-entry harness rule. R10-R12 (10): stable row keying + ownership ledger design (three iterations, ending in layout-effect registration), corrective-entry truthfulness, action-color token, the created shared timing constant, font-wait enrollment, an executed ownership spike. R13-R16 (9): spike executed then twice strengthened then claim-corrected to ledger-correctness-from-commit with the environment ordering asserted; ONE value-only resolution rule; the sentinel fail-soft mechanism corrected to the VenueSection raw fallback. R17: APPROVE, 0 findings. The copy-control async design consumed rounds 9-16 — every convergence step was a probe fold or a claim downgrade to what was measured, the modal-close-exit-anim lesson replayed in miniature.

**Mechanizable:**
- "spec cites a line that is blank or off-by-one from the claimed symbol" (R2 F6, R3's :95, R4 F5) — the spec:lint citation arm already flags unmatched symbols as advisories; promoting CITATION_SYMBOL_UNMATCHED to hard for NEW specs would have converted three review findings into pre-dispatch lint failures. Candidate `BL-` row for the spec:lint arc already in flight (`BL-SPECLINT-RED-EXECUTABILITY-ARM` family).
- "a real-browser assertion is specced against a route whose seed cannot render the subject" (R4 F3) — same class as the tap arc's render-premise findings; the writing-plans layout-dimensions rule could require a seed-renders-subject probe line per browser assertion. Files with the guard-premise rule (`docs/agents/writing-plans.md`), no new row.

**Judgment:** R1's client-island architecture, R3's margin-geometry trade, and R4's two-home test topology were design calls settled by probes; not lint targets.

**Infra:** none — every dispatch returned a verdict.

## plan — 4 rounds

**Examined:** rounds 1-4, 17 findings (R1 BLOCKING 9, R2 NEEDS-ATTENTION 4, R3 BLOCKING 4, R4 APPROVE 0). R1 (9): Tasks 2-3 declared reds Task 1 had already made unobservable, fixture fallout assigned to the wrong task, per-task commits and green-before-commit missing, five settled dimensional contracts without executable coverage, no e2e harness-readiness contract, the timing-inventory trigger declared but not operationalized, stale crew-e2e workflow comments, the header missing Task 4's red transcript. R2 (4): Task 3's red not observed by its own marked command, a targeted run that can never satisfy the zero-argument baseline checker, the seed edit without its `pnpm db:seed` materialization, no failing case pinning testId keying. R3 (4): the graduation commit had no red phase, and three registry/sweep additions (timing sweep, font-wait CALLERS row, BACKLOG_GRADUATED rows) lacked plan-time mechanical reconciliation transcripts. R4: APPROVE, 0 findings.

**Mechanizable:** "a plan declares a registry/sweep addition without recording the current registry state and the exact rows added at plan time" — R3's three registry findings share it, and the writing-plans authored-AND-run sweep rule already names the fix; the miss was non-compliance, not a rule gap. The red-axis chain (R1→R3: every task whose subject lands in an earlier task needs an independent red axis — seed absence, registration, registry-rows-first) is a candidate bullet for `docs/agents/writing-plans.md`'s red-validity guidance.

**Judgment:** R2's full-config-run requirement (the zero-argument checker compares against the COMPLETE baseline, so a scoped run can never observe the intended red) was the round's design call; the rows-first graduation red in R3 is its extension to a docs-only commit.

**Infra:** none — all four dispatches returned verdicts.
