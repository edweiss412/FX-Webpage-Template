# Review rounds — feat/forced-colors-pass, base 8b33a4f0c890

## diff — 1 round

**Examined:** FOUR whole-diff rounds across the arc, one of them at this base. The
heading declares this file's count because `count_mismatch` is per base; the arc total
is what the filing duty is measured against, and the two differ here for a reason
worth stating.

Rounds 1-3 ran at base `8710aefc7a47` — r1 BLOCKING/7, r2 BLOCKING/4, r3
NEEDS-ATTENTION/7 — and their rows are in that file. A sibling PR then merged, this
branch absorbed `origin/main`, and the merge-base moved to `8b33a4f0c890`, which opens
this file and restarts numbering at 1 by design. The round recorded here is NEEDS-ATTENTION/6 and
is the arc's fourth. Counting distinct `(baseSha, round)` pairs across every base of
this branch directory gives four, which is the threshold.

That file also holds a fifth diff row, `status: "no_verdict"`: a round-4 dispatch went
out at the old base and was cancelled seconds later when the sequencing was revised to
put the union merge first. It is kept because the corpus records dispatches that
reached an exit path, and it is not a counted round. The spec and plan stages converged
at an earlier base again and are filed separately at `059bd1bd4aa9.md`.

**The through-line, and it is the same one the plan-stage filing found.** Not one
finding in any of the four rounds was about FEASIBILITY. Every one was accuracy: a
count that outlived its table, a claim that outlived the thing it described, a guard
whose premise had gone false. The diff was executable from round 1 and got more
accurate each round without getting more possible. That is the same shape recorded at
the plan stage, which is why it is worth writing down twice — it is a property of how
this arc was reviewed, not a coincidence of one document.

**Mechanizable:** three classes, one closed in-branch and two recorded.

1. **A count restated in prose beside the rows it counts.** The census headings said
   `(14)` above rows that declared twelve `repaired`, and `13`/`4` above sections whose
   dispositions had drifted. Round 3 read two misfiled rows; deriving per-section
   dispositions found six. The repair is NARROWING rather than renumbering: the
   sections are homogeneous now and the headings state no count at all, because a
   number written beside its own source is a second source that goes stale the moment
   one row moves. CLOSED in-branch — `tests/styles/_metaForcedColors.test.ts` pins that
   every row sits under a heading its disposition agrees with AND that no heading
   restates a count, both arms verified by planting the defect.

2. **A `file:line` citation that survives the line moving.** Adding one attribute with
   an eight-line comment, then relocating that comment, moved four separate line-keyed
   rows: `controlOutlineScan` 119 → 127 → 135, the forced-colors census 113 → 121,
   `DESIGN.md` 106 → 122, and this arc's plan 105 → 121. Three were caught only because
   something else failed first; the fourth was caught by the reviewer.

   **`DESIGN.md` is the uncomfortable one: it was CORRECT before this arc.** Line 106
   held the switch track's recipe on `origin/main`, and this branch made that citation
   wrong. So this is not a sweep finding pre-existing rot — it is an arc breaking a
   living reference and needing three passes to land it, because the first repair
   pointed at the `<span>` (121) while every sibling path in that sentence cites the
   line holding the `border-accent-edge bg-accent` recipe (122). The census legitimately
   uses 121, since that is the element Arm 1 reports. Two conventions, one file, and
   nothing marks which a given citation follows.

   A guard is conceivable — walk every `path:NN` citation and assert the cited line
   still holds a non-comment token — but note it would NOT have caught the 121/122
   error, since both lines are code. It is RECORDED rather than filed, per the process
   mint freeze. Its re-file trigger is a second arc paying this cost.

3. **A rule copied into a second document, checked by count rather than by content.**
   AC-9 required `DESIGN.md` to carry spec §3.1's rules. Round 3 replaced a "verbatim"
   claim nothing delivered with a derived rule COUNT, and the union round then found
   two clauses missing anyway: `border-style: none` as an off-state option in rule 2,
   and rule 3's instruction to give one state a surviving carrier or record why not.
   A count check and four hand-picked `toContain` fragments both pass while a clause
   is dropped, because neither reads the clause.

   CLOSED in-branch, and the shape of the fix is the point. The verifier now compares
   the DECLARATION spans of each rule — a backticked `property: value` — which is the
   actionable half, the part an author types. Derived by SHAPE, not by an exemption
   list: `DESIGN.md` legitimately says "a gradient" where the spec says "a gradient
   `background-image`", and legitimately does not cite `DESIGN.md` inside itself, and
   both are excluded by the predicate rather than by being named. An accept-list would
   have had to enumerate them and would fail open on the next one.

3. **A task description that outlives the design it describes.** Task 6 still
   specified `route`/`setup`/`locator`/`toggle` per row with a navigate-seed-toggle
   case, three rounds after the shipped design became `locator`/`toggle`/`bound` plus
   one live case. `spec:lint` checks a marker's shape and its `ac=` coverage; nothing
   checks a task's PROSE against what the task shipped, and nothing can, because the
   prose is where the intent lives. Not mechanizable. It belongs under Judgment and is
   listed here only because the first two made it look mechanical.

declined: this arc files no ledger row of any facing (arc batch directive,
2026-08-31), and two of the three classes above shipped as MECHANISM inside it rather
than as a queued row — the census heading guard and the AC-9 declaration-span check are
both committed, and both were verified by planting the defect they exist to catch. The
third, a walker over `path:NN` citations, is declined rather than queued for a reason
the arc measured rather than assumed: it would not have caught the error that motivates
it. `DESIGN.md` cited line 121 where the recipe sits at 122, and both lines are code, so
"the cited line still holds a non-comment token" passes on the wrong one. A guard that
fails on the case that prompted it is worse than the documented limit, because it reads
as coverage. Re-file trigger: a second arc paying this cost, with a citation form a
walker could actually discriminate.

**Judgment:** which citations must track the code, and which are a record of the past.
The line-key sweep turned up 43 live (non-fixture) references to the moved switch
track. THREE are at the current line and were re-derived by this arc: `DESIGN.md`, the
design system an author reads today; this arc's plan; and its census. The other FORTY
are stale, and every one of them was deliberately left alone — they sit in other arcs'
shipped plans and specs, and ten of those are captured probe transcripts, literally
recorded terminal output. Rewriting any of them would falsify a record rather than
repair a reference.

That ratio is the argument against mechanizing this. A guard that "fixed" all 43 would
corrupt 40 records to correct 3, and no mechanical rule distinguishes a citation that
must track the code from one that describes what the code WAS. The distinction is
authorial intent, which is why it stays in this section.

Worth recording that this filing's first draft said "eight", from a grep run while
writing rather than a derivation. Checking it before the filing landed is what turned
up 43 — the same defect class the Mechanizable section above is about, committed
inside the document that describes it.

The same judgement decided AC-9. It said `DESIGN.md` carries spec §3.1 "verbatim", and
neither the deliverable nor its assertion met that: the design system carries the rules
in an author's voice, and the test checked four hand-picked fragments. Byte-identity was
available — the sibling share-link contract does exactly that, and its filing recommends
it — and was still the wrong answer here, because the two texts address different
readers. The criterion now states the contract that was wanted, and the RULE COUNT is
derived from the spec so a rule added to one and not the other fails.

**The count I got wrong, recorded because the filing is about exactly this.** The
union merge commit's message states that 11 lines leave main's state. It is 12, across
ten files. The number came from `git diff origin/main -- .` run against the working
tree BEFORE the merge commit existed; the instrument that answers the question is
`git diff --numstat <merge>^2 <merge>`, which reads the committed merge against its
second parent. The reviewer derived 12, inspected all twelve, and agreed every one is
intentional — so the audit's conclusion holds and only its number was wrong. The
commit message is pushed and not rewritten; this is the correction of record.

**Infra:** one, and it did not cost a round. Round 3's reviewer reported that its
read-only sandbox refused the temporary directory and socket creation Vitest and
`spec:lint` need (`EPERM`), so it could not execute the suites and said so plainly
rather than inferring a passing claim from the failure. Every dispatch returned a
verdict; no reaper kill, no usage `no_verdict`, no re-dispatch.
