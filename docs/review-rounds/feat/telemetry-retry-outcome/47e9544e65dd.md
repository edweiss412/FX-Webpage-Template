# feat/telemetry-retry-outcome — review rounds at base 47e9544e65dd

Row `TELEMETRY-RETRY-OUTCOME-ANNOUNCEMENT-1`. Spec `docs/superpowers/specs/2026-08-31-telemetry-retry-outcome-announcement.md`, plan `docs/superpowers/plans/2026-08-31-telemetry-retry-outcome.md`.

Spec stage: 1 round, APPROVE, 0 findings. No filing owed.

## plan — 4 rounds

**Examined:** whether each planned test case can be satisfied by a one-edit mutant of the component it is meant to pin. Four rounds, six findings, all one class and all in the same fifteen lines of Task 2. Round 1 (4): epoch zero rejected by a truthiness guard on either side of the comparison; `!Number.isNaN` substituting for `Number.isFinite` and so admitting ±Infinity; an ordering comparator substituting for `!==`, which a monotonic-only fixture cannot see; the parity suffix satisfied by an ordinary space rather than U+00A0. Round 2 (1): the outcome setter leaving `seq` unchanged, invisible because intent and outcome copy alternate so adjacent pairs differ anyway. Round 3 (1): the round-2 repair itself, which asserted that the two outcomes in a tap-outcome-tap-outcome sequence differ, when the stated implementation puts both at even `seq` and renders them identically. Round 4: APPROVE, 0.

**Judgment:** rounds 2 and 3 are the interesting pair, and they are one story. Round 2 asked for a red on a state write; the repair I wrote to produce that red asserted a pair that cannot differ, which round 3 then refuted with the arithmetic. What settled it was neither another assertion nor a wider mutant list: it was noticing that the settlement's increment changes no reachable rendering, because two consecutive announcements can share their text only when both are intents (an outcome clears the baseline, so a second outcome needs an intervening tap, and the two strings are pinned apart), and the tap-side increment already separates that pair. So the honest disposition is an equivalent mutant carrying its reachability argument, not a red the suite cannot produce. The test that replaced the false assertion walks a sequence and compares each rendered announcement to the one before it, which is the property the spec states; it reads what rendered instead of predicting parity, so it cannot go stale the way the arithmetic claim did.

**Mechanizable:** declined: LIM-NONDISCRIMINATING-FIXTURE, the round-1 class is that slug's boundary arm — a planned case whose inputs do not span the operand's own type domain, so it passes for a reason unrelated to the behaviour. The sharper statement, for whoever meets it next: a guard over a numeric prop needs a case at zero (against a truthiness test), one per non-finite value (against an `isNaN` test), and one in each direction (against an ordering comparator), and all four findings of round 1 were one of those three missing. A lint that read a plan's stated guards and demanded a case per operand substitute would have collapsed the round into the draft; the process mint freeze binds it to this record rather than a row, and nothing here blocked a product arc.

**Infra:** none. Five dispatches, five verdicts, no `no_verdict` and no reaper shape.

## diff — 4 rounds

**Examined:** whether the shipped diff announces only true outcomes, whether each test can fail for its stated reason, and whether the three canonical documents describe the code that shipped. Four rounds, seven findings. Round 1 (3): the intent copy was only ever asserted against its own constant, so rewording it to a settled claim stayed green; the documents still bound the parity design the impeccable gate had deleted; and the closeout claimed a misattribution fix the code does not implement. Round 2 (1): the sweep round 1 asked for had missed spec §3.7, which also promised a cadence the code does not have. Round 3 (2): §7.1 still described the deleted text-swap tree, and §10's test inventory named a suite the diff never touched, claimed an unchanged suite was updated, and implied coverage from a suite that mocks the component to `null`. Round 4 (1): the plan's Task 1 still asserted that a cache planted at the head of `nowDate` leaves the frozen-header cases green, which the arc's own commit had already measured as false.

**Judgment:** six of the seven findings were about DOCUMENTS, not code, and every one of them was created by a mid-arc mechanism change. The impeccable gate replaced the announcement channel after the spec and plan were written and reviewed to APPROVE, and every later round found another place where the documents still described the version that lost. The interesting part is the shape of the tail: round 1 named the class and asked for a sweep, round 2 found the sweep incomplete, round 3 found two more, round 4 found one in a section nobody had looked at since the channel changed. A sweep run by grepping the words the repair used ("parity", "seq", the code point) cannot see a sentence that describes the same mechanism without naming it, which is what §7.1 ("the only mutated DOM is the region's text") and §10 (a file list written from intent) both were.

The one code-shaped finding is worth separating out. Round 1's first, the self-referential intent copy, is the same defect the arc had already fixed once for the outcome string in Task 3 and did not sweep to its sibling. That is the class-sweep rule failing on a two-element class in the same file, and it cost a round.

**Mechanizable:** declined: LIM-NONDISCRIMINATING-FIXTURE for the copy half, the same slug the plan stage parked; an assertion derived from the constant it is meant to pin is that shape exactly, and the repair is the literal beside it. The documentation half is a different and sharper thing, and this arc is the argument for naming it: **a mechanism swap late in an arc invalidates every prose description of the old mechanism, and no tool in this repo checks prose against code.** `spec:lint` verifies that citations RESOLVE, never that what they claim is still true, which is the documented limit its own arm records. Parking it here rather than minting a row, per the process mint freeze: the done condition would be a property of a checker rather than a number a product arc would notice moving, which is precisely the admission test the freeze applies. What a future arc can do without any tooling is cheaper than a checker anyway, and this filing is where it is written down: when a gate changes a mechanism mid-arc, re-read the spec and plan END TO END against the shipped code, rather than grepping for the words the change used.

**Infra:** none in the dispatches themselves; eight verdicts from eight dispatches, no `no_verdict` and no reaper shape. Worth recording that the reviewer's sandbox could not run vitest in any diff round (`EPERM` on its temp directory), so every round's test evidence was the implementer's, quoted in the brief and verifiable from the corpus rather than re-run by the reviewer.

### Post-cap, within the diff stage: the derivation self-sweep ordered instead of a fifth round

The diff stage was accepted at its cap rather than given a round 5, on the reading that the axis was a prose census and buying a review round to hunt prose is the anti-pattern. In its place, one sweep, derived rather than enumerated: name what the OLD announce channel was MADE of, then grep every identifier in that set across the spec, the plan, the closeout, the component and the three test files.

The derived set, and it is the point of the exercise, because a grep keyed to the words a repair happened to use would have missed most of these: `attempts`, `setAttempts`, `seq`, `setAnnouncement`, `announcement.text`, `parity`, `modulo`, `00A0`, `NBSP`, `non-breaking`, `role="status"`, `text swap`, `toggle`.

Thirty-four mentions across seven files. Thirty-two are historical and correct: they say what was deleted and why, which is exactly what a reader arriving at this arc later needs. Two were stale in the sense that matters, both in the plan, and both asserting present tense about a mechanism that no longer exists: the closure set still named the settlement's `seq + 1` as an equivalent survivor "recorded in Task 3", which contradicts Task 3's own amendment two sections below it, and the adjacency bullet still explained round 3's finding in terms of where outcomes land on a counter the swap deleted. Both now carry the tense the fact deserves.

The general lesson, which is the parked class from the diff section stated as a procedure: after a mid-arc mechanism change, the sweep that finds the residue is over the OLD mechanism's vocabulary, not over the new one's, and it distinguishes historical prose from stale prose rather than deleting both.
