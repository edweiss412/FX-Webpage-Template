# feat/telemetry-retry-outcome — review rounds at base 47e9544e65dd

Row `TELEMETRY-RETRY-OUTCOME-ANNOUNCEMENT-1`. Spec `docs/superpowers/specs/2026-08-31-telemetry-retry-outcome-announcement.md`, plan `docs/superpowers/plans/2026-08-31-telemetry-retry-outcome.md`.

Spec stage: 1 round, APPROVE, 0 findings. No filing owed.

## plan — 4 rounds

**Examined:** whether each planned test case can be satisfied by a one-edit mutant of the component it is meant to pin. Four rounds, six findings, all one class and all in the same fifteen lines of Task 2. Round 1 (4): epoch zero rejected by a truthiness guard on either side of the comparison; `!Number.isNaN` substituting for `Number.isFinite` and so admitting ±Infinity; an ordering comparator substituting for `!==`, which a monotonic-only fixture cannot see; the parity suffix satisfied by an ordinary space rather than U+00A0. Round 2 (1): the outcome setter leaving `seq` unchanged, invisible because intent and outcome copy alternate so adjacent pairs differ anyway. Round 3 (1): the round-2 repair itself, which asserted that the two outcomes in a tap-outcome-tap-outcome sequence differ, when the stated implementation puts both at even `seq` and renders them identically. Round 4: APPROVE, 0.

**Judgment:** rounds 2 and 3 are the interesting pair, and they are one story. Round 2 asked for a red on a state write; the repair I wrote to produce that red asserted a pair that cannot differ, which round 3 then refuted with the arithmetic. What settled it was neither another assertion nor a wider mutant list: it was noticing that the settlement's increment changes no reachable rendering, because two consecutive announcements can share their text only when both are intents (an outcome clears the baseline, so a second outcome needs an intervening tap, and the two strings are pinned apart), and the tap-side increment already separates that pair. So the honest disposition is an equivalent mutant carrying its reachability argument, not a red the suite cannot produce. The test that replaced the false assertion walks a sequence and compares each rendered announcement to the one before it, which is the property the spec states; it reads what rendered instead of predicting parity, so it cannot go stale the way the arithmetic claim did.

**Mechanizable:** declined: LIM-NONDISCRIMINATING-FIXTURE, the round-1 class is that slug's boundary arm — a planned case whose inputs do not span the operand's own type domain, so it passes for a reason unrelated to the behaviour. The sharper statement, for whoever meets it next: a guard over a numeric prop needs a case at zero (against a truthiness test), one per non-finite value (against an `isNaN` test), and one in each direction (against an ordering comparator), and all four findings of round 1 were one of those three missing. A lint that read a plan's stated guards and demanded a case per operand substitute would have collapsed the round into the draft; the process mint freeze binds it to this record rather than a row, and nothing here blocked a product arc.

**Infra:** none. Five dispatches, five verdicts, no `no_verdict` and no reaper shape.
