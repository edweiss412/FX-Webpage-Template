# Review-round economy — `feat/switch-person-google-signout` @ `4ee843c23351`

Spec: `docs/superpowers/specs/2026-08-25-switch-person-google-signout-design.md`

## spec — 4 rounds

**Examined:** R1–R4, 3 findings (1, 1, 1, 0; APPROVE at R4). A small product spec: route the menu's "Switch person" through the sign-out helper the guest path already has, archive a second row on an orchestrator ruling. Every finding was about the spec's own testing section or its ordering, none about the ratified behavior.

R1 was a real design defect: with the entry cleared first, the core's `revalidatePath` re-rendered while the session was still live, and bootstrap re-minted the identity, hiding the failure. Repair reversed the order on this path. R2 and R3 were one class: a fault injection in §6 test 6 that an earlier stage could absorb (R2: a `cookies()` rejection reaches the sweep before the core) or that never reached its target at all (R3: the signing-key env var is read through a module-level cache). R3's repair was a probe, not more prose: the §6 block was appended to the live suite against a scratch implementation, run green, and four mutants were killed; R4 approved with nothing left.

**Mechanizable:** none. The R2/R3 class (an injector whose reach is not pinned by the assertions) is a spec-authoring discipline, now stated as a rule in §6 item 6; under the 2026-08-25 process mint freeze it files there rather than as a row.

**Judgment:** R1 ordering (accepted, repaired); R2 and R3 test-oracle reach (accepted, repaired by probe).

**Infra:** none. All four dispatches returned a verdict.
