# feat/nearmiss-surface, base b30413cf5e51

## plan — 4 rounds

**Examined:** R1-R4 on `docs/superpowers/plans/2026-08-26-nearmiss-candidate-render.md`, an 8-task TDD plan for a two-surface UI render plus a nine-site copy lockstep. R1 8 findings, R2 6, R3 3, R4 1, all NEEDS-ATTENTION, all 18 repaired. A fifth dispatch ran concurrently with R3 against the same head and returned 4 more, 3 of them new; those were folded into the R4 repair. The spec stage of this arc closed separately at 3 rounds and owes no section here.

**Judgment:** The rounds were worth running, and the reason is visible in the shape rather than the count: each round attacked a DIFFERENT property of the same artifact, and the finding rate fell 8, 6, 3, 1 as each property was closed.

R1 asked "can these reds fail at all?" It found a task that edited the implementation first and ran its command afterwards as a confirmation, a precondition placed after the code it must precede, a citation into a template FIXTURE mistaken for the executable rule, a stated defect the guard documents itself as unable to catch, a red satisfiable by deleting the row entirely, a Playwright `-g` applying across the combined selection, an e2e injection point asserted without being traced to the page the browser loads, and a missing mandatory inventory.

R2 asked the harder question, "can these reds pass ONLY on the right implementation?", and found six wrong-but-minimal implementations going green.

R3 turned that question on R2's own repairs and found the sharpest defect of the arc: the `42` case R2 forced does not connect the guard to either component, because all ten committed candidates are already trimmed, so a local untrimmed guard passes every declared case. Only a PADDED input separates the guard's rule from the nearest wrong rule. R3 also found one red that could not pass on the CORRECT implementation, which is the same defect wearing the other face.

R4 found one hole, in the same red R1 and R3 had already narrowed twice: its `###`-only terminator ran away because the archive's newest entries are `##`, so the extracted block ran 990 lines and borrowed a neighbour's `Resolution:`.

Two observations worth carrying forward. First, one shell predicate absorbed three rounds and a concurrent reviewer, each finding a different way for it to lie, which is an argument for writing the accept/reject table BEFORE the red rather than after the round that demands it; the final version is pinned across seven states. Second, the concurrent reviewer disagreed with the sequential one about which defects the same artifact had, and the union was strictly larger than either. That was an accident, and it is not a recommendation to double every dispatch, but it is evidence that a single reviewer's finding set is a sample rather than a census.

**Mechanizable:** ONE gap, named and deliberately not filed as a row. A lint arm over any declared task region could execute each `red=` at the merge-base and report the ones exiting ZERO, which is R1's first and seventh findings mechanically. `spec:lint --exec-red` already runs collection probes for `red-contract` regions, so the machinery is adjacent rather than new. Named and not minted, per the 2026-08-25 process mint freeze: its done condition is a property of the lint arm, not a number a product arc or a human would notice moving.

declined: process-facing under the 2026-08-25 mint freeze, whose done condition is a property of the guard rather than a number a product arc would notice moving. Re-file trigger is a product-facing arc measurably blocked by the gap.

R2's and R3's classes have no mechanizable form and should not acquire one. "Does this red pass only on the right implementation" is a judgment about intent, and a guard asserting it would be a recognizer over an open space, which is the shape the convergence criterion exists to refuse.

**Infra:** One dispatch was launched with a stray `&` INSIDE a `run_in_background` command. It did NOT kill the child, which is what I first concluded from an absent `result.json` eleven minutes before the run finished. What it cost was the completion notification: the run looked dead, the round was re-dispatched, and two reviews of the same head ran concurrently. The corpus therefore holds two `plan r3` rows; the threshold counts distinct round values on verdict rows, so the duplicate collapses and the stage count is 4, not 5. Reading an absence at one instant as a terminal state is the error worth remembering, not the ampersand.
