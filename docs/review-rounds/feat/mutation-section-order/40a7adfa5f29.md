## spec — 5 rounds

**Examined:** rounds 3–7 of the near-miss detector spec review (rounds 1–2 were codex no_verdict infra casualties; substitute independent-Claude rounds ran in parallel and are recorded separately, never counted as cross-model). r3 BLOCKING 8, r4 BLOCKING 7, r5 BLOCKING 3, r6 BLOCKING 1, r7 APPROVE 0 (converged). Finding classes: stale/incorrect live-code citations (parseEvent vs parseEventDetails, ParseWarning location, GuardSurface shape), calibration-composition falsehoods that exposed a real semantics defect (write-site vs resolution-site consumption), TDD red-cycle validity per task, committed-probe runnability.

**Mechanizable:** (a) cited-symbol existence — spec:lint verifies cited FILES exist but not that named exports/symbols exist at the cited path; a symbol-grep pass would have caught `parseEvent`, the `broken` GuardSurface property, and the warnings.ts ParseWarning claim pre-dispatch (three findings across two rounds). (b) committed-probe runnability — a CI or pre-dispatch step executing every `probes/*.ts` entrypoint would have caught the stale relative imports (r4-F5) the day they were committed.

**Judgment:** the r4 composition recount (6 DETAILS-family empty-value rows misdescribed as Timestamp echoes) was genuine reviewer value no static check reaches — it forced the resolution-site semantics amendment and removed a user-facing false-positive class. Rounds spent there were bought, not burned.

**Infra:** rounds 1–2 were lost to a budget misfit, not reviewer content: gpt-5.6-sol reasons silently past codex-guard's default 1200s attempt budget and the kills produced 0-byte transcripts misread as a machine wedge. The working recipe (--attempt-max-secs 1380 --stall-secs 1370 --max-attempts 1 --total-max-secs 1450) is recorded in the kickoff brief and AGENTS-adjacent docs for this arc.

## plan — 5 rounds

**Examined:** plan review rounds 1–5 (r1 BLOCKING 8, r2 BLOCKING 1, r3 NEEDS-ATTENTION 1, r4 BLOCKING 1, r5 APPROVE 0 - converged). r1 carried the genuine design findings (opener-keyed ledger occurrence identity, transport regex omission, RED-contract validity, guard witnesses, copy coverage, whole-diff gate, plan-time sweeps); r2–r4 were a single-thread convergence on Task 1's transport witness (marker-suite reach, then per-path pinning, then the constructed doc's header shape).

**Mechanizable:** constructed-fixture satisfiability — every constructed markdown snippet a plan embeds should be executed through its named parser at plan-authoring time (a three-line tsx probe); the r4 header-shape miss and the r6 spec-stage FOO BAR miss are the same class and both would have been caught pre-dispatch.

**Judgment:** r1's occurrence-identity finding (byte-identical rows in two blocks defeating a label+value ledger key) required corpus-wide duplicate probing no static check reaches — bought rounds.

**Infra:** none this stage; all four dispatches returned verdicts under the 1380s budget recipe.
