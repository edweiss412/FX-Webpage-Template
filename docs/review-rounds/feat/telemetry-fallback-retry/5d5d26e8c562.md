# Review rounds — feat/telemetry-fallback-retry (base 5d5d26e8c562)

The plan stage's four rounds are at the previous base, `66c9857f56a5.md`, with its own filing. This branch merged `origin/main` before the whole-diff review so the review would examine the diff that actually merges; that moved the merge base, opened this file, and restarted the round counter at 1 by design.

## diff — 4 rounds

**Examined:** four whole-diff rounds at this base, 1 + 2 + 1 + 0 = 4 findings, converging APPROVE with zero findings at round 4. None was disputed; each was reproduced locally before repair. Cross-base total for the arc is 8 rounds over both stages.

**Judgment:** three of the four findings landed on ONE file, `tests/components/telemetry/telemetryRetryButtonSites.test.ts`, and the shape of that sequence matters more than any single finding.

That file is a census claiming every call site of a component is canonical. Round 1 found the claim was not total: a site written with reordered props, an expression value, a spread, an extra prop, or a non-self-closing tag matched the tag but not the pattern, and was silently excluded while the count still read three. Round 2 found the aliased form, `const Retry = TelemetryRetryButton`, escaping the totality bridge that repaired round 1. Round 3 found a substring test where `bg-warning-bgg` satisfied `toContain("bg-warning-bg")`.

Rounds 1 and 2 are one input family per round against a recognizer, which is the ratchet this repo's repair-direction rule exists to stop. The reviewer proposed a TSX AST walk with import-binding and ancestor association at round 1, and again implicitly at round 2. Declining it twice was the right call and the reason is worth writing down: an AST walk is a bigger surface than the thing it replaces, so it is a bigger target for round 3, and the arc had already shown the reviewer would find one input family per round for as long as there were families to find. What shipped instead is narrower and total over a stated claim: a token pattern, plus a bridge asserting that the count of tag mentions in ANY form equals the count of canonical sites, so a real site cannot escape unnoticed even though the pattern cannot read it. The aliased form is recorded as a documented limit in that file's header, with its probe transcript and a re-file trigger.

Round 3 was different in kind and it was mine: round 2 made the layout assertion token-exact and I left the identical substring shape in the plate assertion one line below. Two instances of one shape, repaired one round apart. That is the drip the class-sweep rule exists to prevent, and it was this arc's second instance of it, after the container class reached two of three fallback sites. Both class-name assertions now route through one `classTokens` function so a third cannot reintroduce substring semantics by being written the old way.

**Mechanizable:** none.

declined: the recurring defect here is a substring test standing in for a structural one, and it is already named in the repo's own guidance rather than being a new mechanism anybody could add. A guard that detects "this assertion should have been token-exact" would have to distinguish a class-name token list from prose, where substring semantics are correct, and this arc contains both kinds two lines apart. The process mint freeze admits a process row only under `invariant`, `product-blocked`, or `recurrence`; none applies, since this cost the arc one review round, blocked no product arc, and the recurrence exception requires hits across two independent arcs while both of these were inside this one.

**Infra:** two non-findings worth separating from the four above, because both cost real wall clock and neither is a defect in the diff.

`unit-suite-db (1)` failed once on `failed to set up container networking` after three `supabase start` attempts, having run zero tests, while legs 5, 6 and 8 passed on other runners with the same image. Runner-local and transient; it did not recur on the next head, so no rerun was needed.

The `Vercel` context reads FAILURE with `Deployment rate limited — retry in 24 hours`. It is not one of the twelve required checks. Recorded with its description rather than as "the known environmental red", because the fleet's standing guidance that Vercel could be ignored went stale mid-arc: the limit had cleared by 18:20 and was exhausted again by 19:08. A dismissal rule with no timestamp is indistinguishable from a live diagnosis, which is the same failure the derived counts in prose have.
