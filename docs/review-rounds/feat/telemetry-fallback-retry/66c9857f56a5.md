# Review rounds — feat/telemetry-fallback-retry

## plan — 4 rounds

**Examined:** four plan rounds at this base, 5 + 1 + 1 + 3 = 10 findings, none disputed and none reversed. Rounds 1 and 4 carried multiple findings; rounds 2 and 3 carried one each. No stage other than `plan` has run at this base. Cross-base total is the same 4: this branch has one base.

**Mechanizable:** two of the ten, and both are already mechanical elsewhere in the repo, which is the uncomfortable part.

declined: both are process-facing, and the process mint freeze admits a process row only under `invariant`, `product-blocked`, or `recurrence` (the third added 2026-08-27 by `3d5b2c8be`, after this arc's rounds ran). Neither applies. The first defends no plan-wide invariant and blocked no product arc; it cost this arc one review round and was closed inside it by a habit rather than a guard. The second is a `spec:lint` arm whose done condition is a property of the linter, which is exactly the shape the freeze's admission test excludes: it names no number a product arc or a human would notice moving. `recurrence` does not admit either, and the rule excludes the first one by name: it requires hits across at least two INDEPENDENT arcs, because "one arc hitting its own limit twice is that arc failing to route around a hazard it already knows about". The lost-write shape has hit twice and both hits are inside THIS arc, which is exactly that case. The TDD-ordering shape has hit once. Both are recorded here as the owning surface's documented limits, and the re-file trigger for each is now sharper than a product arc blocked: it is a SECOND INDEPENDENT arc hitting the same shape, named with its corpus row.

1. **Round 2's finding was a lost edit, not a judgement.** Round 1's citation repair landed at one of four sites because a python repair script asserted its match counts, one assertion later in the same script failed, the process aborted before its single write, and I read a LATER script's surviving edits as evidence the earlier one had landed. Nothing in the loop was wrong except that the write never happened. The general shape is "a batch edit that fails partway leaves no trace, and the next read looks like success". The mechanism that closes it costs one line and is now the arc's habit: after every scripted edit, grep each intended string and print the count. Every repair commit from `39586dd2e` onward carries that verification in the same tool call as the edit. Not filed as a row: the freeze binds it, and the repair is a habit rather than a guard.

2. **Round 4's second finding is a TDD-ordering shape a static check could see.** Task 1b's red came from two peer suites lacking a `next/navigation` mock, and its green came from adding one, so the observed transition proved harness repair while the production change rode along invisibly. `spec:lint`'s `red-contract` arm already checks that a `red-state=authored` task names a `red-target=`; what it cannot see is that the GREEN step edits the same test file the RED failure comes from. That is expressible — "the file whose absence causes the red is edited by the green step" — and it is exactly the class the RED-validity rule in `docs/agents/writing-plans.md` describes in prose.

**Judgment:** the other eight, and the three that mattered were all the same kind: a claim I could have settled by reading, stated instead from a plausible mental model.

- Round 1 finding 4 is the one worth remembering. The plan asserted that no guard would catch `border-text-faint` on a tinted plate, citing `_metaControlOutlineFill.test.ts`'s own threat-fence comment, which says exactly that about ITSELF. A different guard written a week later, `tests/styles/tintedPlateOutline.test.ts`, derives the cover from the ring-offset token and would have caught it. Reading one guard's fence and concluding the class is uncovered is the error; the fence describes that guard, not the repo.
- Rounds 3 and 4 are one axis probed twice: jsdom swallows navigation. `location.reload()` and `history.go(0)` both log "Not implemented" and return, so both are invisible to every behavioural assertion. Round 3's repair was an enumerated API list, and round 4 escaped it in one probe. **The repair that closed the axis was a narrowing, not the next item on the list**: ban the `window`, `location`, `history` and `document` namespaces from a seventy-line component outright. There is no fifth namespace a navigation can come from, so the check is total and cannot be extended by naming another API. This is the repair-direction rule in `AGENTS.md` applied at the moment it was about to be violated, and it is the single most useful thing in this arc's review history.

**Infra:** none. Codex was reachable for all four rounds; one dispatch exited before reaching Codex on a `spec:lint` gate (a `red-target=` that became invalid the instant the plan was committed and therefore tracked) and left no corpus row, which is the wrapper's documented behaviour for a usage error rather than a fault.

**Round 5 is not opened on my own authority.** The cap is four. Round 4's three findings are repaired at `HEAD`, and one of the three (the namespace ban) replaced a mechanism rather than extending it, so the axis it closed is closed rather than narrowed. Whether a fifth round verifies that, or whether the plan is accepted and implementation starts, is the orchestrator's call and it has been escalated with the round count and the axis.
