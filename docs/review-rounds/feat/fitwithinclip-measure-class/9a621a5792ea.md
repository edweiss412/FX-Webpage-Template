# Review rounds — `feat/fitwithinclip-measure-class` @ `9a621a5792ea`

## diff — 4 rounds

**Examined:** **4 counted `diff` rounds in THIS file** (`9a621a5792ea.jsonl`), declaring 3, 1, 3 and 1 findings — eight in total. **Cross-base arc total for the diff stage: also 4** — every diff dispatch happened at this base, and the three sibling files (`449f29faba03`, `e381de76ea87`, `aea3423cfbd2`) hold only `spec` and `plan` rows, so counting distinct `(baseSha, round)` pairs across the branch directory gives the same 4. Verdicts: BLOCKING, then NEEDS-ATTENTION three times. The stage closed at the orchestrator's four-round cap with the last finding repaired, not on an APPROVE.

**The number that matters is not the round count. It is that the shipped hook drew ZERO findings in four rounds.** One production file changes, `components/admin/useFitWithinClip.ts`, 107 insertions and 90 deletions. Round 4 says it outright: "The shipped hook itself drew no finding." All eight findings landed on the plan, the spec, a test, a test comment, or the guard I wrote to check the documents. (An earlier summary I sent the orchestrator said the product "drew a finding in round 1 and never again" — that was sloppy. Round 1's three were a process deviation, an e2e test defect, and a coverage claim. None was in the hook.)

**Mechanizable:** one, and declining it is the substantive judgment of this stage.

declined: the process-facing mint freeze ratified 2026-08-25 forbids a queue row, and the done condition here is a property of a lint rather than a number a product arc would notice moving.

The gap is nameable exactly: nothing checks whether a document's claim about which case pins which behaviour is TRUE. Round 1 caught the plan crediting `(h5)` and `(h7)`, cases the suite does not declare. Round 2 caught seven places where a mutant-to-case mapping contradicted its own measurement. Round 3 caught four transition rows citing a case that performs a different sequence, and a "both modes" claim when the suite has exactly one `<StrictMode>` case. Round 4 caught the paragraph in which I had described the round-2 repair, still claiming more than was true.

One shape of it IS mechanizable and now is: the committed case-id parity probe was widened from the spec to the plan and immediately found a second false site that the table repair had left behind. The rest is not, because "does `(h18)` perform the `unchanged / stays absent` row" is a question about what a test body DOES, and answering it mechanically means parsing test semantics.

**I declined to build that, and that decision is the stage's real content.** Round 3's finding 1 was precisely that my guard claimed a repository-wide property while checking only the spec. The obvious repair was to widen it until it could see every mutant claim in every file. That is the recognizer this repo's own round economy warns about: each round adds one construct it must classify, and a wider recognizer is a bigger target for the next round. I narrowed the claim to what the guard verifies, recorded the rest as documented limits in spec §3.1a, and round 4 then charged the one narrowing I had missed. If the judgment is wrong, the evidence will be a future arc re-finding this class in these two documents.

**Judgment:** the other seven, and they earned their rounds even though none touched the product.

Round 1's P0 was a real invariant violation: Task 2's production change was subsumed by Task 1, so its RED was never observed separately. The plan recorded it, which is how the reviewer found it, and the orchestrator accepted it as a recorded deviation because the guarantee the invariant exists for — that a red was observed for the behaviour — holds by another route: case `(h)` ships as a pin and mutant M2 turns it red.

**Round 1's finding 2 is the one I would not have found alone, and it is the argument for adversarial review in a single line.** I had "repaired" the painted-frame race by waiting for `__clipFrames.length > 0`. The reviewer probed the predicate instead of reading it:

```text
{"waitCondition":true,"firstPresent":-1,"presentCount":0}
```

Absent rows recorded during hydration already satisfied it, so the wait guaranteed no PRESENT frame and my repair had widened the race rather than closing it. A green run showed the race had not occurred once, not that it could not occur. The predicate now requires a present row, and round 4 confirmed it closed.

Round 1's finding 3 was the same shape one level down. Case `(h3)` asserted an `applyCount` delta after unmount, and a leaked listener never moves that count — the handler fires, a frame is booked, and `apply()` returns at its null-node guard. The case could not see the defect it appeared to guard. `(h23)` and `(h24)` now assert on the SCHEDULER, and mutants M25 and M26 prove they bite, M26 reddening exactly one case. Before that repair M26 escaped the suite entirely.

Two mutants were measured to kill nothing and are recorded as accepted gaps rather than quietly assigned a case: M9, because jsdom tears down synchronously so no `transitionend` can arrive between `coalescer.cancel()` and the listener's removal, and M3. Recording an unkillable mutant honestly costs one line; assigning it a case that does not catch it is how a suite comes to claim coverage it lacks, which is the class this stage spent three rounds on.

**Infra:** none, with one thing worth recording. Four dispatches, four verdicts, no `no_verdict`, no reap, no retry. The reviewer could not execute the suite in its read-only sandbox on any round (`EPERM` creating jsdom's temporary directory) and said so plainly every time rather than reporting zero executed tests as a passing result. That is the correct behaviour and the reason its findings stayed source-derived and checkable.
