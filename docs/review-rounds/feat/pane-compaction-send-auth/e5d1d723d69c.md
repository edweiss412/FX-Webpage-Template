# feat/pane-compaction-send-auth — review-round filing

Base `e5d1d723d69c`. Spec-stage rounds for the send-authorization design
(`docs/superpowers/specs/2026-08-21-pane-compaction-send-authorization.md`).

## spec — 4 rounds

**Examined:** every finding across the four rounds (3/2/3/1, all verdict rows, none
refuted, none argued away), against what would have caught it earlier.

**Judgment:**

- **The dominant class, three rounds running, was an overclaimed pricing sentence — each
  repair's own prose claimed one mechanism further than the mechanism reached.** r1 claimed
  read-once implies one instant (cross-member skew missed); r2's repair claimed the address
  line priced the residual (same-recipient decay missed); r3's repair claimed marker
  deference priced every decay class (it closes `blockedOn` only — the one signal a
  recipient can read). Each repair was correct as a mechanism and wrong as a sentence about
  its own reach. The r4 close was subtractive: state per decay class whether a mechanism
  closes it or a bounded consequence prices it. The transferable lesson is the batch's
  rule 267 shape — a mechanism's description is where the error relocates after the
  mechanism is right — and the honest form is a per-class table, never a quantifier.
- **Two findings were the repair-carries-the-next-defect pattern** (r3F1 was a defect in
  r2F1's repair; r4F1 in r3F1's). Both were caught because each round's brief fenced the
  restatement and invited missed-site findings — the 253b fence shape did its job.
- **The reviewer's probes were uniformly in-domain and all eight findings were accepted** —
  zero refutations, zero relitigation across four rounds under do-not-relitigate fences
  that grew one disposition per round. Finding rate decayed 3/2/3/1 with the artifact's
  claims shrinking each round (echo deleted; atomicity withdrawn; impossibility withdrawn;
  quantifier withdrawn), which is the convergence direction, not the ratchet.

**Mechanizable:** declined: the overclaim class is prose about mechanism reach, and the
per-decay-class table that closed it is now pinned by the spec's own §10 prose meta-test
extension (AC-15); a lint over quantified pricing sentences would be a recognizer over open
English — the class the narrowing rule forbids growing.

## plan — 4 rounds

**Examined:** all seventeen findings across the four rounds (5/5/5/2 — r1
NEEDS-ATTENTION, r2 BLOCKING, r3 and r4 NEEDS-ATTENTION; sixteen accepted, one refuted),
against what would have caught each earlier.

**Judgment:**

- **The dominant class was the incomplete sweep — a repair or account verified by
  enumeration instead of derivation.** r3 F3 found the restoration table missing the
  `it.each` revalidates pair; the repair swept by TITLE-diff, and r4 F1 then found the one
  baseline case the fence commit MODIFIED in place — same title, changed body — which only
  a body-diff can see. Likewise r2 F4's AC-command sweep fixed four cells, r3 F5 caught a
  fifth, and r4 F2 caught a command that runs but cannot observe its criterion (the
  executable pin lives in a suite the cell did not name). The transferable lesson is the
  AGENTS.md sweep-to-a-derivation rule made concrete: the derivations that would have
  one-shotted these are the body-diff against the deleted baseline and a
  per-cell "which existing test asserts this criterion, and is its file in the command"
  check — both cheaper than the rounds they cost.
- **One finding was refuted (r2 F1, early-closeout ordering):** the reviewer read invariant
  12's "PR's last commit" sentence without its graduating-entry clause; refuted with the
  clause plus the ratified fleet ruling, and fenced in Task 7's body so it cannot
  relitigate. The refutation is recorded in the plan itself, which is the triage-record
  pattern this corpus prefers.
- **The remaining findings were genuine plan-shape gaps front-loaded then decaying**
  (proof-gap on the structural red, fail-open kill template, TDD-claim/task-structure
  mismatch, unrun closeout sweeps), with r4's two findings both narrow P1/P2 on
  previously-swept surfaces and no P0 after r2. Finding rate 5/5/5/2 with the last round's
  severity floor dropping is the convergence direction.

**Mechanizable:** `BL-PLANLINT-AC-COMMAND-OBSERVABILITY` — the "AC command cannot observe
its criterion" class (r2 F4, r3 F5, r4 F2 are the incident record; the row names the
lint-arm shape). The body-diff baseline sweep is a one-line procedure, not a lint — that
half stays judgment.
