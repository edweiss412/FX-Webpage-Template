# Review rounds — feat/review-modal-strip-dock @ 75b8f7a3ec76

## diff — 1 rounds

**Examined:** 4 diff rounds across three bases for this arc — `366dcc804e50` round 1, `b30413cf5e51` rounds 1 and 2, and this file's round 1 — which is what makes the filing owed under the arc-sum rule. Eight dispatches in total, because every round ran as two tight per-surface scopes. The base moved twice mid-arc, absorbing #901 and then #902 with #903, so the per-file counter restarted each time and no single base ever reached four on its own. Declared findings by round: 5, 10, 9, 5. Round 4 split cleanly by surface — production code APPROVE with 0 findings, tests NEEDS-ATTENTION with 5.

**Judgment:** The finding rate never decayed, and the surface was not what kept growing. Rounds 2 and 3 were dominated by one defect shape I kept repairing at the named instance instead of the class. Round 2's own record says it plainly: I fixed the named instance and not the class, three separate ways, in the very commit where I was repairing review findings. Round 3 then found that round 2's zero-area-host repair had replaced a hidden overlay with a silently mispositioned one, which is strictly worse under this arc's own consequence bound. The class-sweep default exists for exactly this and I did not apply it until round 3 forced it.

The cheapest lesson here is about ordering, not effort. Round 4's five findings are the same class as round 3's, at five sites round 3 did not name. Had the round-3 repair been a sweep for the shape rather than a fix for the instances, round 4 would have had nothing to find, and its production half already returned APPROVE with zero findings. The tests, not the code, set this arc's round count from round 2 onward.

**Mechanizable:** Every finding in rounds 3 and 4 that mattered has one form: an assertion whose extraction is narrower than the property in its own test name, so it passes for a reason unrelated to the behaviour. Round 3 proved three by mutant (a transition audit reading only the first `cn()` argument; a listener check comparing aggregate counts that stayed green with a leaked listener; a census counting comments as definitions). Round 4 found five more (height compared against a static CSS cap rather than available room; `spaceAbove > spaceBelow` standing in for "top fits"; a clamp measured on a title that fits unclamped; a 12-cell sweep that never proved which load rendered; containment checked on one edge of one branch). The general repair is the same in all eight: give the assertion a contest it can lose — a negative control, a premise on the fixture, or a bound derived from the same quantity the implementation uses. A structural guard that fails any case whose fixture cannot discriminate would have caught all of them before dispatch, and is the concrete mechanizable gap this arc leaves behind.

declined: process-facing under the 2026-08-25 mint freeze. The gap is a property of a guard, not of shipped behaviour, and its done condition ("every fixture can discriminate") ranges over an open space of test shapes, so it is refutable once per round forever — precisely the admission test the freeze added. No product-facing arc was blocked by it; this arc absorbed the cost inside its own rounds. Recorded here as the owning surface's documented limit, with the re-file trigger being a product-facing arc that stalls on a non-discriminating fixture it did not author.

**Infra:** Machine saturation, not review, set this arc's wall clock. A single full-tree gate ran over an hour against roughly eight concurrent arcs on the semaphore, one test file alone taking 743 seconds. Two consecutive full runs each reported failures that were entirely environmental: every failure passed alone, and the failing SET turned over completely between runs, which is the cheap signature to check before treating any of it as real. No finding in any round came from a suite failure.
