## spec — 4 rounds

**Examined:** R1: the accept-set corrupted the Consultants exporter-family value (probe-demonstrated) and the parser-synthesized GS name defeated the zero-room case — both structural, both from the initial probe reading only one fixture family. R2: mirror-aware corpus accounting, the 8/10 room-count correction, and the rooms fetch-failure gate. R3: residual accounting (4/4/2), the FactRow testId reachability gap, and round residue. R4: APPROVE, FINDINGS: 0.

**Judgment:** the R1 corruption finding is the arc's most valuable — a probe against the SECOND fixture family (exporter-xlsx) that the initial corpus sweep missed entirely; the two-families memory existed and was not applied at probe-brief time. Vocabulary widenings were all probe-backed; the raw-fallback consequence bound held every round.

**Mechanizable:** probe briefs over fixtures MUST enumerate both fixture families explicitly (the families are documented; the probe prompt named only raw/). Cheap brief-authoring rule; no tooling filed.

**Infra:** the first R1 dispatch died in the 21:2x harness background-task sweep; re-dispatched detached and all subsequent rounds ran detached without incident.

## plan — 4 rounds

**Examined:** R1: the transition-audit task folded the spec's inventory by reference rather than reproducing it, and the conditional-branch enumeration named only JSX ternaries while `VenueSection` pushes its fact rows imperatively — both repaired by inlining the inventory verbatim and enumerating the `factRows.push` conditions. R2: the inlined inventory had lost the verbatim arrow from the spec's row (a one-character fidelity regression introduced by R1's own repair). R3: the synthesized-`General Session` suppression case was specified as a hand-authored `{ kind, name }` literal, which passes vacuously if the parser's fallback literal is ever renamed — repaired to run `parseSheet` on one of the five raw fixtures that actually produce the synthesized name. R4: APPROVE, FINDINGS: 0.

**Judgment:** R3 is the round that earned its cost — a hand-authored fixture for a suppression case is the classic vacuous guard, and it would have passed forever against a renamed fallback. R1 and R2 are the same defect class in sequence (inventory fidelity), and R2 exists only because R1's repair introduced it; that is the fix-round regression budget rule paying for itself, one round later than it should have.

**Mechanizable:** none. The verbatim-reproduction rule is already written (`docs/agents/writing-plans.md`, transition-audit bullet: "Body must include the spec's exact inventory table"); R1 and R2 were failures to apply a rule that exists, not gaps in it. A diff-based checker over "quoted from spec §" blocks was considered and rejected — the plan text is prose-embedded, so the anchor set is not derivable without a marker convention that would itself need enforcing.

**Infra:** all four plan rounds ran detached through `codex-guard` with no wedges and no `no_verdict` results.
