# feat/crew-field-enrichment — review rounds at merge base `196334d5ef61`

One of several segments for this arc: each merge with `origin/main` moves the merge base, and the corpus is keyed by it, so every segment numbers its rounds from 1 and the count below describes THIS segment rather than the arc.

## diff — 4 rounds

**Examined:** R1: the transition audit scanned for motion inside a SLICE of one file, so motion on the Facilities wrapper — or inside `FactRows`, which renders every row the audit covers — was invisible, and the DOM assertions ignored class names entirely. R2: the suite protected the accept-set's behavior but never its CONTENT, so adding `WLAN` or `PIN` to the matcher left all 37 tests green; and the graduation meta-test's provenance slice terminated only on `\n## `, so two adjacent same-branch graduations satisfied each other's provenance. R3: the recorded impeccable gate covered an earlier commit, and `VenueSection` had changed materially since — a P0 against invariant 8. R4: the branch was stale again with `origin/main` having touched `VenueSection` itself; the successor ledger row misdiagnosed WHY date-only flight legs render raw; the rendered motion audit exercised two states out of a six-state declared inventory; the §6 limits list was inserted out of order so CommonMark renumbered it against every reference; and one sentinel case never reached the gate it named.

**Judgment:** every finding in this segment is against a GUARD rather than the product, and that is the segment's whole character — the splitter and the component were probed clean each round. Three of them are one shape: a guard that reads a proxy for the thing it claims to check. A source SLICE proxies for the rendered tree; a behavioral case proxies for the accept-set's content; two sampled states proxy for a declared inventory. Each passed while the property it named was false, and each repair replaced the proxy with the thing itself — assert the rendered card, pin the constants, sweep the declared inventory.

R3 is the most consequential and the least technical: the gate had genuinely run, on genuinely reviewed code, and then the code moved. Nothing was skipped; a record simply stopped describing what it covered. That is worth naming because the failure mode is invisible from inside the run — the §12 section reads exactly as complete after the drift as before it, and only a diff between the gated commit and HEAD can tell them apart.

R4's ledger finding is the counterweight to the guard theme: the entry was honest about a real residual but wrong about its cause, asserting the parser could not structure those legs when it structures them fine and the renderer has nothing to display. A wrong diagnosis in a backlog row is worse than a vague one, because it aims whoever picks it up at the wrong subsystem.

**Mechanizable:** one, and it is already this arc's practice rather than new tooling: a guard whose subject is rendered output asserts against the rendered output. The three proxy failures here would each have been caught by asking "what would I have to break for this to fail?" before writing the assertion — which is the mutant discipline the arc already runs, applied to guards rather than only to product code. Every repair in this segment was mutant-verified against the reviewer's own probe before being committed, and that is the habit worth carrying forward, not a new gate.

**Infra:** the reviewer sandbox could not initialize Vitest in any round (`EPERM` on its temporary directories), so every finding arrived probed through in-memory transpilation instead — which is why the guard findings are demonstrated with escaping mutants rather than argued. `origin/main` moved under the branch twice more during the segment, each time requiring a merge and, in R4's case, revalidation of the UI gate because upstream had touched the same component.
