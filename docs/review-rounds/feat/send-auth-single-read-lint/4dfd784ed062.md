# Review rounds — feat/send-auth-single-read-lint @ 4dfd784ed062

## diff — 4 rounds

**Examined:** all four diff rounds on this base (`4dfd784ed062`), their briefs, and every
finding's probe outcome — 5, 5, 8, 4. The subject is a
detector: a structural lint over an enrolled surface, enrolled in the source-mutation registry
before its first diff round.

**Mechanizable:** `BL-SENDAUTH-ARM-CLASSIFIER-UNIFICATION` — ONE axis produced the arc's only three real diff defects — r1 F3 (discovery
reached module-level functions only), r2 F2 (the callee was inspected without unwrapping
parentheses), r3 F6 (the walk suppressed on containment in a function discovery had declined).
Every one is the same sentence: AN ARM DECLINED TO CLASSIFY AND THE SCANNER SAID NOTHING. Rounds
1 and 2 each repaired the named instance and left the shape, so the shape came back. The r3
repair is the mechanizable form — the classification is computed once and both arms consume it,
so suppression can only rest on a classification that actually happened, and a form the
recognizer cannot classify REPORTS instead of vanishing. That change also turned up a second,
unfiled instance inside an already-pinned fixture (`destructured-param-sink`), which is the test
of whether a repair is structural: it finds instances nobody reported.

The mutation score cannot express this class and should not be read as covering it. No declared
operator generates "the recognizer declines a node shape" — the surface scored 1.0000 with an
empty survivor set while all three defects were live. That is exactly the gap the killer audit
exists to fill, and it is why the convergence criterion for this surface is stated as score PLUS
audit rather than score alone.

**Judgment:** sorting round 3's eight findings was not mechanizable and should not be. One probed
silent, two probed as conservative reports (refuted), and five are shapes outside the threat
fence or whose worst case is a different report rather than silence. That sorting is a reading of
the bound and the fence against a probe outcome, one finding at a time. The cost of NOT doing it
would have been five more recognizer arms — precisely the parser growth the same-axis rule
forbids.

**Infra:** two rounds' findings (r2 F4, r2 F5) were STALE CLAIMS in the arc's own artifacts, not
defects in the code under review: a fixture count and a mutation score that described an earlier
tree, and a header comment describing a limit the code had stopped having. Both cost a full round.
The count went stale three times (62, 75, 78) and every time by ADDITION alone, which is the tell
that the defect is the FORM of the claim. Now stated against the tree the gate scans rather than
as a bare number. A score restated after any commit that touches a score input is the same
discipline; this arc retired and re-measured its score twice for that reason.

**What the fourth round settled, and it is the reason this filing is not a formality.** Two of
its four findings probed SILENT and both are the SAME AXIS AGAIN, one arm over: a raw READ
through a property receiver (`this.ch.panes()`) and a raw HANDOFF of a name-shadowing parameter.
The r3 repair made the sink walk and discovery share one classification; the read arm and the
handoff arm still take a bare identifier and a name. So the axis is not "a missing node kind" —
it is that the scanner has FOUR arms each deciding independently what a receiver and a raw
binding are, and every round has found the next arm that had not been narrowed yet. Rounds 1-4
found 1, 5, 1 and 2 real defects; the rate is flat, not decaying, which is the signature the
round-economy spec names.

**The repair direction is therefore not another arm-by-arm fix.** Either every arm consumes one
shared receiver rule and one shared raw-binding predicate — with decline meaning REPORT, which is
what makes it a narrowing — or the remaining arms' forms are DECLARED as documented limits by
name, each with its probe, its corpus count, the direction of the miss, and a re-file trigger.
That choice is a re-scope, and it belongs to the orchestrator rather than to a review brief.
