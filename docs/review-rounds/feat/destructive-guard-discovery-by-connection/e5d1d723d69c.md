# Review-round economy — `feat/destructive-guard-discovery-by-connection` @ `e5d1d723d69c`

Spec: `docs/superpowers/specs/ci/2026-08-21-destructive-guard-discovery-by-connection-design.md`

## spec — 4 rounds

Findings 3, 3, 4, 2; every one accepted, zero refuted; no APPROVE. The stage closed at the cap
DISPOSITIONED rather than converged, on the orchestrator's pre-staged ruling (spec §3.10): nine of
the twelve findings were one vector — an input the census neither classified nor reported — and
the answer to a fourth round on it was the totality arm (§2.10, AC-C13), not a fifth enumeration.
The two round-4 repairs and §2.10 landed after the last verdict and were reviewed by nobody at this
stage; the plan stage reads the repaired spec.

**Examined:** the design document end to end each round, the ledger row, the filing spec §2.5, the
shipped destructive guard's three files, `_localDbUrlScan.ts` / `_localDbUrl.ts`, and the three
committed probes with their outputs (re-run by the reviewer every round and byte-identical).

**Mechanizable:** two classes, both declined with their reasons below.

declined: the rg-counts class is recorded as the spec's §3.9 instrument lesson rather than minted, because its only measured cost is this arc's own rounds and the process-facing mint bar needs an incident beyond the filing arc; the two-walks class is closed by a derived cover that ships IN this arc (one specifier extractor plus a structural assertion), so there is nothing left to schedule.

- **Counts produced by `rg` over a language with comments, templates and nesting.** Four of the
  twelve findings (R1 F3, R3 F2, R3 F3, and the helper over-count inside R1 F3) were numbers that a
  regex got wrong and an AST census got right: a type-only default import counted as a value, a
  `/* @vite-ignore */` hiding a non-literal specifier, an option sub-key attributed to the outer
  object, `.spec.ts` consumers labelled helpers. The repair was to make the three committed
  AST probes the only source of every number in the spec. The general arm — "a number in a spec
  about TypeScript structure must come from a committed AST script, not a grep" — is a process-facing
  lint over spec prose and has no measured cost event beyond this arc's rounds, which the mint bar
  does not accept as an incident on its own. Recorded as §3.9's instrument lesson in the
  spec, where the next author of a census meets it, rather than minted as a row.
- **Two walks that know different specifier positions.** R1 F2 (side-effect import), R3 F1
  (vitest loader as an edge) and R4 F1 (vitest loader as an acquisition) are one mechanism found
  three times: the edge walk and the driver walk each enumerated positions separately. Closed in
  the design by ONE extractor (`moduleSpecifiersIn`) that both walks consume, and in the plan by a
  structural assertion that no other specifier extraction exists in the module. That is
  sweep-to-a-derivation rather than a lint, and it ships in this arc. No row, because the
  cover lands in the deliverable.

**Judgment:** R2 F2 (whether a dispositioned helper site propagates to consumers as satisfied or
as a fresh obligation) and the cap disposition itself — choosing the totality arm over a fifth
round — were design calls no checker would have made.

**Infra:** none. All four dispatches returned a verdict; no reaps, no kills.

### plan — 2 verdict rounds, 1 no_verdict (not at threshold; recorded for the disposition)

Round 1 BLOCKING 5, round 2 BLOCKING 8 — all thirteen accepted, zero refuted: the draft predated
spec rounds 2-4 and was patched rather than re-derived (r1), then its own repairs owed their
both-directions proofs (r2). Round 3 `no_verdict`: Codex weekly usage limit, reset 2026-08-25
17:47. Closed DISPOSITIONED on the orchestrator's ruling; the implementation's whole-diff review is
the residue's reader and is wall-gated. Not a filing (threshold is four counted rounds); this
paragraph exists so the corpus and the plan's §4b say the same thing.
