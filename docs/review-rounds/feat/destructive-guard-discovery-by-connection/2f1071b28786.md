# Review rounds at base `2f1071b28786`

The fourth base file this arc has opened. The corpus is keyed by
`git merge-base origin/main HEAD`, so each mid-arc merge starts a new one; the rows are deliberately
not consolidated, and numbering is PER BASE. The thread reads:

| base | holds |
| --- | --- |
| `e5d1d723d69c` | spec rounds 1-4, plan rounds 1-2 (filing in `e5d1d723d69c.md`) |
| `c9c71b947a85` | whole-diff round 1, both scopes |
| `0ba72c23774f` | whole-diff rounds 2 and 3 (filing in `0ba72c23774f.md`) |
| `2f1071b28786` | this file: whole-diff round 4, the CAP round for scope B |

## diff — 1 round

Lifetime round 4, recorded here as round 1 of this base, with the dispatched value preserved on the
row as `_roundAtPreviousBase`.

**Examined:** scope B only — the live gate, the dispositions, the enrolment and the documents. Scope
A had returned APPROVE with zero findings at lifetime round 2 and was explicitly fenced out of this
dispatch, so a clean module was not re-reviewed to keep a live one company.

**Verdict: APPROVE, zero findings**, which closes the stage at its cap.

**Judgment:** the reviewer independently reproduced the previous round's own regression probe —
appending destructive SQL to `tests/help/render.test.ts`, the file whose pre-existing dispositioned
edge report had made it invisible — and observed the gate emit `channel` for it. That is the repair
checked from outside rather than accepted on the implementer's word, and it is the reason this
APPROVE means something: the same probe that demonstrated the defect now demonstrates its absence.

**Infra:** the named Vitest commands again could not collect in the reviewer's read-only sandbox
(`EPERM` on Vitest's temporary `ssr` directory), so the census was exercised through direct imports
instead. This is the fourth consecutive dispatch to pay that tax. The reconstruction has matched the
live gate every time, so it has not yet cost a wrong verdict — but a reviewer that cannot run the
repo's own verification commands is verifying a different thing than the gate does, and the day those
two diverge, nothing in this loop would catch it.
