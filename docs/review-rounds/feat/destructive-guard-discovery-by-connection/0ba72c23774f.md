# Review rounds at base `0ba72c23774f`

### Predecessor bases, because this arc's thread is split across three files

The corpus is keyed by `git merge-base origin/main HEAD`, so merging main mid-arc opens a new
file. That is the convention, not an accident, and the rows are deliberately not consolidated.
This arc's thread reads in this order:

| base | file | what it holds |
| --- | --- | --- |
| `e5d1d723d69c` | `e5d1d723d69c.jsonl` (+ `.md`) | spec rounds 1-4, plan rounds 1-2 |
| `c9c71b947a85` | `c9c71b947a85.jsonl` | whole-diff round 1, both scopes |
| `0ba72c23774f` | this file | whole-diff round 2, both scopes |

**Numbering is PER BASE.** The two rows here were dispatched as `--round 2` in the arc's own
lifetime numbering and are recorded as round 1 of this base, with the dispatched value preserved
on each row as `_roundAtPreviousBase`. Carrying lifetime numbering into a fresh file leaves a
non-contiguous singleton — a round 2 with no round 1 — which the economy gate reads as a gap in a
thread rather than the first round at a new base. Both readings of that trap were met on the same
day by two arcs from opposite sides, this one and `feat/shell-attached-redirection-target`.

## diff — 2 rounds

Two rounds at this base, rounds 2 and 3 in the arc's lifetime numbering, recorded here as rounds 1
and 2 per the convention above. Round 1 of this base returned APPROVE on SCOPE A (which is closed and
was fenced out of the next brief) and BLOCKING with three on SCOPE B; round 2 returned BLOCKING with
three more on SCOPE B alone.

**Examined:** the whole branch against `origin/main`, split into two scoped dispatches — SCOPE A
(the census module and its deciding suite) and SCOPE B (the live gate, the dispositions, the
enrolment, the documents). Scope A returned APPROVE with zero findings. Scope B returned BLOCKING
with three.

**Round 2 of this base, in brief.** Every finding landed on the round-1 repairs, which is the
post-repair pattern this arc has now met three times. The channel arm's join target was narrowed
twice: round 1 moved it off the walked population (which cannot fail), and round 2 moved it off
"any report at all" — because an unrelated dispositioned `unresolved-import` on a file accounted for
that whole file and silently swallowed a new production connection added beside it. The
discriminating probe is `tests/help/render.test.ts` precisely BECAUSE it already carries such a
report. The remaining findings were the canonical contract still describing the rejected version:
the spec's §2.7 and AC-C8, the plan's channel definition, a §5 export list naming two functions the
module never exported, and a §5.1 score-input list written from memory that omitted four of fifteen
members.

**Mechanizable:** declined: no detector, and the reason is the same one this arc has applied to
every recognizer question. The defect is real and general — a guard whose discriminating set equals
its population cannot fail, and `population` here was every walked file while `discovered` was drawn
from the same array. But catching it mechanically means identifying, in an arbitrary guard, which
binding is "the population" and which is "the set under test". That is a recognizer over an open
grammar, and it would grow one shape per round exactly like the surfaces the convergence doctrine
declines to build. The closable form already exists and is what shipped: `premiseHolds` states the
discrimination as an executable premise on the assertion that needs it (`accounted.size <
population.size`, measured 185 against 2565). The rule is "a subset assertion states its properness
as a premise", enforced by review and by the premise helper being right there, not by a scanner.

The stale document numbers in findings 2 and 3 are the re-key class already banked under
`BL-GUARD-PREMISE-REACHABILITY`'s neighbourhood: a fenced block labelled eight while holding seven,
an archive census predating a merge, a probe heading naming one class while printing two. Each is a
hand-maintained copy of a derivable fact, and the standing repair is a derived cover — the probe
output is now REGENERATED rather than hand-edited, which is that repair applied to the one of the
three that had a generator.

**Judgment:** Scope B finding 1's second half. Deciding that a test reaching a connection only
through PRODUCTION code should REPORT rather than widen the walk into `lib/` is a scope call the
spec's §1.3 item 8 fence settles, and no mechanism picks that side for you.

**Infra:** the reviewer could not start Vitest or the `tsx` CLI in its read-only sandbox (`EPERM`
creating SSR and IPC temp files) and reconstructed the census through a no-write path instead. Its
reconstruction matched the live gate exactly, so the findings stand on measurement — but a review
environment that cannot run the repo's own verification commands is a standing tax on every
dispatch, paid again here.
