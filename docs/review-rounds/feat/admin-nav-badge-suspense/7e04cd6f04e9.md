## spec — 4 rounds

**Examined:** rounds 1-4 of the admin nav badge streaming spec. R1 killed the Suspense-leaf design outright (resolved counts drive trigger branch, parent-link aria-label, zeroNow, panel refetch; pre-mount navigation stale-commit; bell's pinned degraded contract) — a full redesign, not a patch. R2 caught the post-zero demotion contract. R3 caught the demote-vs-discard contradiction and promise-identity supersession. R4 caught the always-commit prop path letting a late seed overwrite a newer refetch (virgin-state rule shipped in response).

**Judgment:** every round's finding was a genuine state-machine gap against the PINNED hook contracts — the reviewer read the hooks deeper than the initial investigator pass, and each repair simplified the design (final form: hooks own pending state; seed commits only into virgin state; supersession by promise identity; per-hook staleness posture inherited, never respecified). None of the four rounds was drip; the train converged because the invariant became one sentence.

**Mechanizable:** none — these were design-reading findings against client-state contracts, the class no lint reaches. The AC-5 interleaving tests are the durable mechanization: every round's interleaving is a named test case.

**Infra:** the first R4 dispatch was killed by a harness background-task sweep (all four concurrent dispatches died at 21:2x; reaper log shows it gated, not killed — the kill was the task layer). Re-dispatched detached (nohup) and completed. no_verdict results from the killed runs were discarded as infra faults per AGENTS.md.

**Disposition (post-R4):** ship-and-fence. The virgin-state rule is a one-sentence invariant with every interleaving a named AC-5 test; a fifth prose round would verify a mechanical rule the implementation's tests prove executably. Plan-stage review re-reads the spec whole.

## plan — 4 rounds

**Examined:** R1: seven (spike resolution proof, fallback contradiction, wrong frozen-suite paths, missing CLS, missing transition audit, closeout canon + marker grammar). R2: seven (audit RED validity — the existing transitionAudit suite; honest fallback shrink; DB-state CLS mechanism; seed result shapes; verbatim inventory). R3: three (CLS run wiring, honest readiness gate, byte-verbatim compound rows). R4: one (loader-rejection RED), repaired in the same commit as this filing.

**Judgment:** R1-R2 carried real structural findings (the pre-existing transitionAudit suite and the fallback's true shape); R3-R4 narrowed to wiring and verbatim-ness. Declining trail 7-7-3-1 with the last a single executable-coverage gap.

**Mechanizable:** none new — the wrong-suite-path class is the live-code-citation pass applied to test paths; compliance, not tooling.

**Infra:** all rounds ran detached after the 21:2x sweep.

**Disposition (post-R4):** ship-and-fence. The R4 repair adds the rejection REDs + a wrapper-deletion mutant; the implementation's whole-diff review and gates re-verify the full surface. A fifth prose round would re-read a plan whose task structure has been stable since R2.

## diff — 4 rounds

**Examined:** R1: two (a non-virgin `infra_error` seed demoting to a fetch instead of applying its own failure posture, so a hung fallback showed a count the server had already disowned — accepted; and a supersession/in-flight claim — refuted, both probed orderings being conservative rather than stale, recorded in spec §5.6). R2: three (a synchronous initial count populating state without CLAIMING the hook, so a same-render seed overwrote it — probed `paints=[5,1]` on both hooks; four post-amendment prose sites still describing the drop §3.2.1 had removed; the shipped ledger entry sitting OPEN still requesting the rejected `<Suspense>` mechanism). R3: two (the named AC-5 navigate-then-seed row asserting only frames, which a genuine drop reproduces identically — it now asserts the demoted fetch; and an archive entry claiming "verbatim" over a normalized meta line). R4: one (the passive-effect window between a newer promise's commit and the older subscription's cleanup) — refuted by equivalence to the §5.6 limit, with the attempted fix reverted and recorded.

**Judgment:** the trail is 2-3-2-1 and the KIND of finding decays cleanly — R1/R2 were behavior (a real stale-badge path and a real overwrite), R3 was test-discrimination, R4 was a timing mechanism whose outcome is a limit the spec had already ratified on its own merits. Two findings across the arc were refuted rather than repaired, and both refutations are written into the spec so the next reader gets the reasoning instead of re-deriving it. The most useful round was R2, because its F1 was reachable by any caller passing both props even though the layout passes only one — the class of bug that survives precisely because the shipping call site does not exercise it.

**Mechanizable:** one, and it landed: `tests/cross-cutting/_metaStripCommentsSingleSource.test.ts` caught the source scan rolling its own comment stripper — the same two-regex idiom that had already swallowed 10 KB of the layout earlier in this branch, silently making every assertion in that file pass for the wrong reason. That guard is the reason the defect cost one commit instead of one review round. The rest were design-reading findings against client-state contracts; the AC-5 interleaving suite remains the durable mechanization, now including the StrictMode-replay and dual-input rows.

**Infra:** the first R1 dispatch died with the child mid-read because `nohup … & disown` inside a tool call does not survive the call's shell; re-dispatched under the harness background runner and every later round ran clean. Separately, three of the reviewer's rounds could not execute vitest at all (`EPERM` on its temporary client/SSR directories under the read-only sandbox), so every behavioral claim it made was reasoned from source and re-verified locally before disposition — which is how R4's window turned out to be unconstructible in the harness it would have to be guarded in.

**Disposition (post-R4):** ship. The convergence criterion the briefs stated — the interleaving suite as mechanical closure, plus a consequence bound that files conservative-and-recoverable outcomes as documented limits — is met: R4's only finding resolves to an already-ratified limit rather than an unhandled state, and its speculative repair was reverted for being unguardable. Two real behavior defects (R1 F2, R2 F1) were found, fixed, and pinned by RED-first cases that fail against the prior code.
