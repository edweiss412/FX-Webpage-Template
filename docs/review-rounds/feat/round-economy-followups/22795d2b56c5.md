# feat/round-economy-followups @ 22795d2b56c5

The arc that promotes the corpus's own filing lessons into `docs/agents/`, obliged by the
gate those filings feed. Spec subject: nine-then-ten checklist promotions (W1) plus the
boundary-advisory repair (W2).

## spec — 5 rounds

**Examined:** `docs/superpowers/specs/ci/2026-08-07-round-economy-followups.md` — the
P1–P10 promotions table against its six source filings, and the W2 advisory design
(exclusion rule, timestamp accept-set, comparison semantics) against
`scripts/review-economy.ts`, `lib/reviewRounds/row.ts`, and `lib/reviewRounds/mergedArcs.ts`.
Sixteen findings across rounds 1–4, every one confirmed and repaired; round 5 returned
APPROVE with zero findings and independently re-probed both round-4 repairs.

**Mechanizable:** three shapes, and the first is this arc eating its own cooking.

1. **The comparison-site class converged only by derivation — the exact lesson this
   spec's P9 promotes.** Lexical-vs-chronological timestamp comparison surfaced at one
   site per round (R2: earliest selection; R3: the two merge-side comparisons; R4: the
   latest-merge selection — a fourth site the R3 sweep's "three sites" enumeration
   missed). The class closed when §3.1 stopped enumerating sites and adopted a
   structural rule: one parse-and-compare helper, no direct string comparison of
   timestamps in the advisory block, each existing site pinned by an offset-divergent
   fixture. A sweep verified by enumeration re-opened the moment the reviewer found the
   next site, on the arc whose deliverable says precisely that.
2. **Accept-set holes do not converge by enumeration either; a constructive net does.**
   R3 found two inputs inside the declared accept-set that `Date.parse` mishandles
   (out-of-range offsets to NaN, sub-millisecond digits silently discarded). The repair
   that held was not two more regex fixes — it was requiring finite `Date.parse` as a
   condition of placeability, making "placeable implies comparable" true by
   construction, with the regex bounds as the structural first line.
3. **Unprobed prose claims are findings waiting to be filed.** R3 F3 refuted the spec's
   own TZ-flip example (values that never flipped); R1 F3 caught an invented "at least
   twice" evidence claim. Every post-R3 numeric or behavioral claim was probed before
   commit (probe tables in the commit messages), and R5's independent probes found zero
   drift. The probe-before-argue rule already mandates this; the misses were
   non-compliance at drafting time, caught at one round each.

**Judgment:** the promotions-table fidelity findings (R1's filing-drift and
redundancy-classification calls, R2's stale-wording catch) are filing-vs-rule
integration judgments no recognizer settles — each needed the reviewer to read the
source filing and the target rule together. Continuing past round 4 was right: both R4
findings were real and in the losing direction (a silently suppressed advisory; an
operator-facing line that printed a falsehood in a reachable case), so the consequence
bound admitted them.

**Infra:** one dispatch (the first round-4 attempt) was killed by the harness ~3.5
minutes in — background task stopped externally; the reaper log shows it took nothing
(`gated: 2 live codex tree(s) active; no orphans`), the wrapper wrote a clean
`no_verdict` result and its corpus row, and a single retry completed with a verdict.
The `no_verdict` row stays in the corpus as the honest record; it is uncounted by
design.

## plan — 4 rounds

**Examined:** `docs/superpowers/plans/2026-08-07-round-economy-followups.md` against the
approved spec — task/AC ownership, fixture fidelity, RED honesty, and the Task-3 mutant
closure. Ten findings across rounds 1–3 (5 + 2 + 3), all confirmed; round 4 returned
APPROVE with zero findings after independently executing the RED shapes
(`1:F 3b:F 5:F 6:F 7i:F 7ii:F 8:F 13:F wording:F` against current behavior).

**Mechanizable:** (a) Round 1's five findings were ONE class — plan sections drafted
against earlier spec revisions and incompletely resynced. The numeric/self-consistency
sweep exists but is single-document; the miss was the CROSS-document boundary. A
mechanical diff of every AC id, §-ref, count, and quoted fixture value a plan cites
against the spec it implements would have caught all five before dispatch. (b) Rounds
2–3's surviving mutants were found by the reviewer EXECUTING mutants against the planned
case set — the l-wave-push lever again: run the mutants yourself before dispatch, on the
plan's paper design, not only post-green. (c) The arc's first explicit DECLINE: four
accept-set boundary-drift mutants were fenced (spec §5.5) rather than pinned, because
each is conservative-and-signaled or benign-widening — the consequence bound used to
STOP a recognizer ratchet instead of feeding it. Round 4 probed the fence and confirmed
it holds.

**Judgment:** whether to pin or decline class 3 was the round-economy call this corpus
exists to make; the decline held because the bound had been stated in every brief since
round 1. Continuing through round 3 was right — both repaired classes were in the losing
direction (a silently suppressed advisory; a silently missing signal note).

**Infra:** none. All four plan dispatches returned declared verdicts on the first
attempt.
