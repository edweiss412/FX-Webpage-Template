# Review rounds at base `c9c71b947a85` — `feat/speclint-red-reason-verification`

The spec and plan stages ran at the previous base and are filed in
`e5d1d723d69c.md`. This base carries the diff stage only, which began once the
Codex wall lifted.

## diff — 7 rounds

**Examined:** all 7 diff rounds (R1 APPROVE 0 + NEEDS-ATTENTION 2, R2 APPROVE 0 + NEEDS-ATTENTION 1, R3 NEEDS-ATTENTION 2, R4 NEEDS-ATTENTION 1, R5 NEEDS-ATTENTION 1, R6 NEEDS-ATTENTION 1, R7 NEEDS-ATTENTION 1), 9 rows over 9 declared findings. Counts derived from this directory's `.jsonl` by `scripts` in the arc's scratchpad rather than retyped; the two APPROVE rows are scope A, which stopped being re-dispatched at round 3 once its four files were byte-identical to the head it had already approved.

**Judgment:** the findings did not decay — 2, 1, 2, 1 — and it would be easy to file that as the recognizer treadmill the round-economy spec warns about. It is not, and the distinction is worth recording because it changes who pays.

Every finding was a distinct, real defect. None was refuted, none was a re-litigation of a ratified decision, and none was an admissibility failure. The reviewer never widened the scope: the bound, fence and probe domain were stated in round 1 and never moved, and each late brief stated the closed criterion only.

What actually generated rounds 3 and 4 was the implementer. Round 1's [P1] was genuine — two authored fixtures could not discriminate the shipped arm from a narrower one, because both also drew `RED_TARGET_MISSING` under containment assertions. The repair added `red-target=` to both. **That repair silently weakened a different test**: the v1 negative case's twin was now three fields away instead of one, so its silence was attributable to a missing `why=` rather than to the v1 exit it exists to pin. Round 3 found exactly that. Round 4 then found that the round-2 repair to §5.4 had re-asserted a v1 mechanism this arc had already disproven — symbol-naming the citation fixed the line number and left the false claim standing.

Round 5 then found a THIRD instance of one vector: a fenced movement table claiming HEAD while
carrying a number the scope-A repair had moved.

At that point the repair direction INVERTED rather than continuing: both movement tables were
DELETED instead of annotated, on the pattern PR #829 measured, and the derived cover of raw line
references fell from 32 candidates to 12. Round 6 then found the REPLACEMENT PROSE wrong on three
counts of its own: a row total (twelve, actually ten), an ordering repeated from the text it
replaced (`VITEST_SHAPE` at 580 sits above both union members, so "above every other line this
plan cites" was false), and a positional reference to a table that is not in that direction.

That is the lesson of this arc in one round: a replacement that RESTATES is still a restatement.
Deleting the tables was correct and the prose written to explain the deletion re-created the same
class one level up. The round-6 repair removed the count, the ordering and the direction word
rather than correcting them, and swept two further live counts that duplicated a list or a table
beside them, while leaving alone the two measurements explicitly bound to a moment. That is the same shape the AGENTS.md convergence section attributes to reviewers ("two repairs introduced the next round's defect") appearing on the implementer's side, and the honest charge is: rounds 3 and 4 are the cost of repairing narrowly without re-reading what the repair's neighbours claimed.

The one structural defence that did work: every repair from round 2 on was made by NARROWING. Stale prose citations lost their numbers rather than gaining new ones, so those sites cannot go stale again and §3's manual-re-read list is one entry shorter than it was. No recognizer grew; no parser was widened; the shipped `lib/specLint/redContract.ts` diff is unchanged since round 1 apart from one doc comment.

**Mechanizable:** two of the six findings were invisible to oracles that already exist, and both were invisible BY CONSTRUCTION rather than by oversight.

1. `probe:citations` derives its population as path-and-line citations into `redContract.ts`. A bare prose "line 742" carries no path, so the oracle cannot see it. This limit was already declared in the plan's §3 and in this PR's own residue note, and round 2 then found a live instance of exactly the declared blind spot. The mechanizable repair is not to teach the oracle prose parsing — that is recognizer growth on a doc scanner, which this repo has measured as the losing move. It is what was actually done: delete the numbers so the site leaves the manual list.
2. The plan's own `spec:lint` was failing (`CITATION_MALFORMED`) and nothing ran it. The plan declares a pre-dispatch lint obligation in prose, and prose obligations are the ones that get skipped — the two oracles it also declares ran at every gate, several times each, because they are commands someone types. **This is the one genuinely mechanizable gap the arc surfaced**, and it is filed as `BL-SPECLINT-SELFLINT-NOT-IN-PREDISPATCH-GATE` rather than fixed here, because adding a gate mid-arc is exactly the scope creep the convergence rules forbid.

Round 5's recurrence promoted the first item from a declined candidate to a filed one:
`BL-SPECLINT-DOC-BARE-LINE-NUMBERS-UNCOVERED` records the prohibition form (a raw line number may
appear only in the derived table or in a block bound to a named commit), which is a location test
rather than a grammar. The oracle-teaching form stays `declined: teaching the citation oracle to parse bare prose is recognizer growth on a doc scanner, which this repo has measured as the losing move; the repair taken instead deletes the numbers so those sites leave the manual list entirely.`

**Infra:** every diff round ran in a sandbox that denied Vitest's temporary SSR directory (`EPERM`), so no round executed the suites; each said so plainly rather than reporting silence as a pass. Findings were established by direct CLI probes and by the two oracles, which do run there. This is the same sandbox limit the spec and plan rounds hit at the previous base, recorded there too. The reviewer correctly treated "the suite could not run" as absence of evidence rather than evidence of absence, which is the behaviour that makes the limit tolerable rather than disqualifying.

**Named residue (ship-and-fence, bl-orch ruling 2026-08-21).** The diff stage is closed at this
base by disposition rather than by an APPROVE on scope B, and the argument is a measured
repair-defect chain rather than a judgement about reviewer quality. Scope A APPROVED at round 2 and
its four files have been byte-identical at every head since, verified by blob hash each time.
`lib/specLint/redContract.ts` is unchanged since round 1 apart from one doc comment. Every finding
from round 2 onward was prose in the spec or the plan, and none was refuted, re-litigated, or
outside the declared fence.

What closed the stage is that the rounds stopped measuring the change and started measuring the
previous round's repair. Four links, each one a repair that introduced or left the next round's
finding:

1. **Round 1's fixture repair broke a different test.** Adding `red-target=` to the positive twin
   closed the discrimination finding and left the v1 negative case three fields from its twin
   instead of one, so its silence became attributable to a missing `why=` rather than to the v1
   exit it exists to pin. Found at round 3.
2. **Round 2's citation repair re-asserted a disproven mechanism.** Symbol-naming the §5.4 citation
   fixed the line number and left standing the claim that the sixteen v1 corpus markers exit at
   that guard. They do not: they are unowned and drop above it, which this arc had already
   established and §1.1 already said. Found at round 4, and a second site of the same claim at
   round 5.
3. **Round 5's deletion was restated by the prose explaining it.** Deleting the two line-movement
   tables was the correct inversion and the paragraph describing the deletion carried a wrong row
   count, an ordering copied unchecked from the text it replaced, and a direction word pointing at
   a table in the other direction. Found at round 6.

4. **Round 4's byte-identical repair carried a claim into a place it was false.** Making the v1
   fixture's marker byte-identical to its twin closed round 3's finding and copied the twin's `why=`
   verbatim, so the negative fixture asserted that the arm reports unverified capability, which is
   the thing that fixture exists to disprove. Found at round 7. It is the round-6 ordering defect in
   a different costume: a text carried forward without re-checking whether it was still true where
   it landed.

The pattern is the one PR #829 measured, and the repair direction that works is removal: round 5
took the derived cover of raw line references in the two documents from 32 candidates to 12, and
round 6's repair deleted rather than corrected. The residue fenced here is any further prose
finding of the same kind. Two mechanical forms are filed rather than argued:
`BL-SPECLINT-DOC-BARE-LINE-NUMBERS-UNCOVERED` (a prohibition, not a recognizer) and
`BL-SPECLINT-SELFLINT-NOT-IN-PREDISPATCH-GATE`.

What is NOT fenced, stated so the fence cannot be read wider than it is: anything touching
`lib/specLint/redContract.ts`, the suites, or the fixtures. That half carries an APPROVE, a
239/239 mutation score re-measured on three separate trees, and CI green over the twelve required
contexts.
