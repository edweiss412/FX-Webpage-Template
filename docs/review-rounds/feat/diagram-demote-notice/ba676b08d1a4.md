# feat/diagram-demote-notice — review-round economy filing

Base `ba676b08d1a4`. Corpus: `ba676b08d1a4.jsonl`.

## diff — 8 rounds

R1 NEEDS-ATTENTION (2) · R2 NEEDS-ATTENTION (2) · R3 NEEDS-ATTENTION (2) · R4 NEEDS-ATTENTION (2) ·
R5 NEEDS-ATTENTION (4) · R6 NEEDS-ATTENTION (2) · R7 NEEDS-ATTENTION (1) · R8 NEEDS-ATTENTION (1).

**Examined:** the whole branch diff each round — the chip state and its four clear conditions, the
`openNonce` seam and the close gate, the chip markup and its token family, the `relative` added to
the slide figure, the DESIGN.md §5.5 timing row, the two test suites, the spec and plan prose, the
closeout, and the ledger archive.

**Every total below is DERIVED from `ba676b08d1a4.jsonl`, not retyped.** Summing `findingCount` over
the eight `status: "verdict"` rows gives 16 findings across 8 rounds (2, 2, 2, 2, 4, 2, 1, 1). Three
successive rounds found this filing's own arithmetic stale, which is the funniest possible instance
of what it documents and the reason the number is now stated as the sum of a file rather than as a
tally someone keeps in their head. Where the prose groups findings into classes below, the grouping
is illustrative and deliberately carries no total.

**Two rounds of the eight found something the code did wrong; the rest were the record disagreeing
with itself.** The code findings: R2's test-validity pair (an exit-window case that never exercised
its gate, and a re-entry case that could not discriminate render-time from effect-timed), and R5's
behavior gap (clear condition 4 wired on the ACTIVE slide branch only, so a demoted slide swiped away
kept its notice and timer when its clamped tier failed). Everything else was prose trailing the tree:
a spec clause describing a replaced mechanism, case counts, a claim about a removed assertion, a
missing record of two impeccable re-runs, and this filing's own sums.

**R5's lesson is the one worth carrying: an assertion that cannot fail is not coverage.** Three of
its four findings were of that shape, and the settling move is mechanical — mutate the mechanism and
watch the case. Four probes ran; three mutants died, and the fourth (deleting the unmount-cleanup
effect) SURVIVED and is recorded as an accepted gap in the closeout rather than hidden behind an
oracle that cannot discriminate. Had those probes run when each clear condition was written, R5's
first three findings would not have existed.

**Mechanizable, by class rather than by count.** Four classes below; the first three each had a
machine available from round one, and the fourth is the mutation probe described above.

- **The test-validity pair (R2) is the class the anti-tautology rule names, and it is not mechanizable
  in general — but its instances were.** The exit-window case reused a helper that had already
  demoted the slide, so its error landed in the placeholder branch and the gate under test was never
  asked anything; deleting the gate would have left the case green. The repair is a mutation probe,
  which is mechanical and cheap: `closedAtNonce !== openNonce` replaced with `true` now reds the case
  (1 failed / 40 passed). Any guard whose claim is "this condition suppresses something" can be
  settled that way in about a minute, and this arc did not do it until a reviewer asked twice.
- **The repeat (R3 F1) was an edit that reported success and wrote nothing.** The Python edit script
  aborted on a later assertion after building the new text, so the file was never written while the
  commit message said it was fixed. Mechanizable outright: an edit script that asserts anchors should
  write incrementally or verify the write, and the cheap habit is to re-grep the anchor after the
  edit rather than trust the exit status of the tool that made it.
- **The count claims (R4 F2, and R6 F1 when R5 added a case) are derivable.** "15 tests" was written
  by hand and was wrong by one; then "16" went stale the moment a seventeenth case landed. The true
  numbers come from `vitest -t "<describe>"`, which prints them, and the closeout now names that
  command beside the figure. A hand-typed count is a claim with no mechanism behind it, which is why
  it drifted twice across four rounds.

**Judgment — the prose findings were not noise, and the pattern in them is the lesson.** Three
successive rounds each found the retired mechanism (`closingRef`, then `lastNonce`, then both again)
surviving in a different file: the spec's Set clause, then its clear-condition 3, then the plan's
GREEN line, the HANDOFF's binding-shapes line, and the closeout's tables. Each round I repaired the
sites the reviewer named. That is the drip-feed the class-sweep rule exists to prevent, and the
correct move was available from R1: `rg` the retired names across the arc's own documents and repair
every hit in one commit, then verify by grep that the only survivors are explicitly historical. The
final sweep did exactly that and took a few minutes.

**Infra:** the reviewer's sandbox could not start Vitest in any round (denied temporary-directory
creation), so every verdict was read from source plus targeted probes; the implementer's own run
results were stated in the brief from R2 onward, which is what let the reviewer reason about
behavior. Separately, a mid-arc reorder is a real hazard for this workflow: the archive commit has to
be re-established as HEAD after every repair, and each reorder rewrites the SHAs the previous round's
findings cited.
