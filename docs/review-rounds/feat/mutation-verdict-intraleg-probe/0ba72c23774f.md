## diff — 2 rounds

**Examined:** R1-R2 at this base, which are rounds 4 and 5 of the arc. Merging
`origin/main` mid-arc moved the merge-base from `c9c71b947a85` to this one, so
the arc's five diff rounds are split 3 + 2 across two corpus files. Neither file
shows the split from inside it; the sibling filing at `c9c71b947a85.md` carries
the note in the other direction, and `pnpm review:economy` sums them correctly
because it reads the corpus rather than either file's prose.

**Mechanizable:** BL-DERIVED-NUMBERS-IN-DOCS-ROT. Both rounds landed one class:
a figure computed correctly and then compared against nothing. The in-branch
repair is `pnpm intraleg:claims`, which recomputes every figure this arc's
documents assert about the campaign artifact. Its own history is the argument
for the ledger row. First run: six findings, two of them its own scoping bugs.
Later: its artifact path was hardcoded to the round-3 campaign, so it compared
the round-4 documents against the previous run and flagged the CORRECT new
figures as stale. Round 5 then found that it built a `checks` array and bypassed
it with hardcoded regexes, leaving four of nine derived truths — the three arm
eligible counts and the flip count — computed and never read, while it printed
CLAIM AUDIT CLEAN. Three generations of one defect inside the instrument built
to catch that defect. The repair that closes it is not the four checks but the
self-check underneath them: a derived truth with no consumer now exits 2.

**Judgment:** Forty-six findings across five diff rounds, zero refuted, and the
per-round rate is flat — 10, 9, 9, 9, 9. That flatness is the whole finding.
Product-side the diff was settled after the result was verified; every round
since has been the instrument failing to pin itself, and each repair added a
surface that became the next round's target. Round 5's own verdict is the clean
demonstration: it opened by independently reconciling the artifact (12/6/2
eligible, zero flips, common stamp, distinct process identities) and then raised
nine findings, none of which touched the measurement.

So the terminal move was a ruling, not a better brief. bl-orch adopted
ship-and-fence: docs corrected to claim exactly what is proven, four bounded
repairs taken, the rest fenced as documented limits 10-14 with re-file triggers.
The condition attached to it is the part worth copying — classify each repair
against the plan's campaign-voiding rule BEFORE editing, rather than discovering
afterwards that the fix invalidated the measurement. Three repairs touched audit
tooling and a meta-test and were provably non-voiding: the deciding suite's
input was fingerprinted before and after at 3604 files, 76 psql sites, identical
sha `05343e0830a3504d` (measured at base `0ba72c23774f`; #873 later changed `scan.ts`, so the
absolute figures differ on the merged tree while the before/after equality they
establish does not). The fourth (`--out` required) touches the driver that
produced the artifact, so it moved to the fence with the next campaign as its
trigger, where the guard and the run arrive together.

**Infra:** Four campaigns where one would have done. Three were voided by my own
subsequent repairs, because I ran the measurement while findings were still
open. The ordering rule this arc paid for: land every fix from every open round
first, then run once on the committed post-formatter tree.
