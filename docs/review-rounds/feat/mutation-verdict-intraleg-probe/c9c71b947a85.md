# Review-round economy — `feat/mutation-verdict-intraleg-probe` at `c9c71b947a85`

## diff — 3 rounds

**Examined:** rounds 1-3 returned 10, 9 and 9 findings, every one accepted and
none refuted. That is the fact this filing has to explain, because the usual
reason a diff reaches four rounds is a reviewer grinding a recognizer wider one
constructed input at a time, and that is not what happened here. Three findings
across rounds 2 and 3 were defects in repairs landed in the round before, and
three more were numbers I hand-carried in prose while deriving the numbers
beside them. The finding rate has not decayed, but the CLASS has moved: round 1
was mostly the instrument's core logic, round 3 was mostly its evidence channel
and its own documentation.

**Mechanizable:** most of it, and some already is — filed as
`BL-DERIVED-NUMBERS-IN-DOCS-ROT` for the first item below; the other two are
`declined: both are single-site repairs already landed in this arc's diff, and a
structural test over either would have one member, which is a fixture wearing a
guard's clothes.`

- **Numbers stated about an artifact.** Every campaign figure that a renderer
  derives from `campaign.json` has been right since it was mechanized; every
  figure a person carried between documents went stale at least once. Three
  round-3 findings are that population. The rule that falls out is narrow and
  checkable: a number a document asserts about a suite or an artifact is
  produced by a command at write time, or it is on the rot list.
- **Rendered-but-unenforced fields.** `plannedPerArm` was displayed and never
  checked. That is the flag-lifecycle table's zombie row in a new place — a
  field with storage, a write path and a render path, and no enforcement path.
  A structural test could enumerate declared-count fields and require each to
  reach a comparison.
- **Differential guards with unproven starting state.** The attestation compared
  two snapshots while the driver seeded its own output directory into both. Any
  guard of the form "compare before and after" needs a separate assertion that
  the before is clean; the diff is blind to whatever preceded both ends.

**Judgment:** the rest, and it is the part worth the rounds. Whether
`RunRecord.passed` means "this trial was valid" or "the gate passed" is a
contract question no scanner settles, and getting it wrong put 20 records into
the durable channel asserting passes nobody measured. Same for whether a pair of
load halves agreeing with each other counts as evidence when they disagree with
the campaign anchor. Both are claims-wider-than-evidence, which is this arc's own
subject, and both were found by a reader who understood what the instrument is
FOR rather than by any property a test could have expressed in advance.

**Infra:** two rounds were spent re-establishing evidence rather than finding
defects. The round-1 repairs voided the first campaign and the round-3 repairs
voided the second, because a production edit after a campaign invalidates it —
so the arc paid for three campaigns to ship one. The sequencing rule that avoids
the third is stated plainly for the next arc: land every fix from every open
round FIRST, then run the campaign once, on the committed post-formatter tree.
Running it early feels like progress and buys an artifact that the next repair
throws away.
