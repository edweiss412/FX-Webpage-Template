# Round-economy filing — feat/mutation-verdict-intraleg-probe @ e5d1d723d69c

## spec — 5 rounds

**Examined:** the intra-leg process-boundary probe design (`docs/superpowers/specs/ci/2026-08-21-intraleg-process-boundary-probe-design.md`) — its trial-evidence model (receipts, pids, per-step binding), campaign arms (process boundary, ordering, load pair), pre-registered graduation branches, and the AC table's weaker-implementation columns. Counts derived from this file's sibling `.jsonl` at filing time: rounds 1-5, verdicts BLOCKING then NEEDS-ATTENTION x4, declared findings 5/3/4/2/1, fifteen total, all fifteen ACCEPTED and repaired in-arc, zero refuted. Round 5 was a bl-orch-authorized scoped confirmation of the round-4 repairs (overlapped with plan drafting, so its wall-clock was free); it confirmed one repair and found one real residual, which is the confirmation round paying for itself.

**Judgment:** the dominant round-multiplier was the repair-carries-the-next-round's-defect chain, three links deep: r2's findings were against r1's repairs (planned-text receipts admit a missing write), r3's against r2's (the sha check cannot kill the planned-text mechanism; per-trial stamp stability is not cross-trial identity), r4's against r3's (the eligibility machinery divides by zero at N=0 and adjudicates cross-stamp load pairs), and r5's single finding against r4's (the zero-eligible refusal covered the NUMBER while the GRADUATION text still certified a bound that did not exist). Every finding was probe-backed and admissible under the brief's own contract; none was scaffolding — the artifact under review IS the instrument design, so the finding rate (5/3/4/2/1, decaying) reads as convergence on a shrinking defect set rather than a recognizer ratchet. The reviewer's probes were consistently drawn from the declared domain (live `psqlStartupScan` sites, registry digests), which is the admissibility contract working as written.

**Mechanizable:** declined: every mechanizable surfaced by these rounds lands in-arc as an AC fixture (the zero-eligible refusal, the cross-stamp pair refusal, the rotation refusal, the prefix-bearing behavioral kill) — the killer-audit at implementation derives its obligation list from the AC table's fourth column, so there is nothing left to schedule as a separate row.

## plan — 4 rounds

**Examined:** the implementation plan (`docs/superpowers/plans/2026-08-21-intraleg-process-boundary-probe.md`) — its two-region task structure, the task↔AC proof-case mapping, red validity, and the measurement/closeout sequencing. Counts derived from the sibling `.jsonl` at filing time: plan rounds 1-4, all BLOCKING, declared findings 7/3/5/3, eighteen total — seventeen ACCEPTED and repaired in-arc, one REFUTED with inline documentation (the early-ledger-closeout ordering, which is the ratified operative rule; the repair was carrying the ratification, trade-off, incident and residual in the task body so no future reviewer re-derives it — the lessons corpus's known tracked-file/ruling divergence, and the plan had left it undocumented, which is the predicted cost).

**Judgment:** the same repair-carries-the-next-defect chain as the spec stage, at the plan grain: r2 pressed r1's repairs (the in-process isolation pair missed the cross-process env channel; the pid provenance path was unnamed production code), r3 pressed r2's (a parent-passed nonce echo; two elided-writer variants conflated into a contradiction), r4 pressed r3's (a constant position field; deep-equal standing in for byte-identity; the cwd nonce channel). Every accepted finding was probe-backed, most against live shipped code (`records.ts:56`, `spawnBounded.ts:110`/`:124`, `_metaPremiseContract.test.ts:373`); the one refutation was itself predicted by the corpus. Finding rate 7/3/5/3 without clean decay — consistent with the reviewer walking the fixture-strength frontier of a large AC surface rather than with a ratchet, since zero findings proposed mechanism growth and every repair was a fixture or a derivation rule.

**Mechanizable:** declined: as with the spec stage, every mechanizable lands in-arc as a named fixture or derivation rule (position-derived property, byte-identity comparison, spawn-parameter-derived channel sweep, mid-trial-edit stamp-timing kill); the killer-audit task derives the full obligation list from the spec table, so nothing remains to schedule as a row.
