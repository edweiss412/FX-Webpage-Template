# Round-economy filing — feat/mutation-verdict-intraleg-probe @ e5d1d723d69c

## spec — 4 rounds

**Examined:** the intra-leg process-boundary probe design (`docs/superpowers/specs/ci/2026-08-21-intraleg-process-boundary-probe-design.md`) — its trial-evidence model (receipts, pids, per-step binding), campaign arms (process boundary, ordering, load pair), pre-registered graduation branches, and the AC table's weaker-implementation columns. Counts derived from this file's sibling `.jsonl` at filing time: rounds 1-4, verdicts BLOCKING/NEEDS-ATTENTION x3, declared findings 5/3/4/2, fourteen total, all fourteen ACCEPTED and repaired in-arc, zero refuted.

**Judgment:** the dominant round-multiplier was the repair-carries-the-next-round's-defect chain, three links deep: r2's findings were against r1's repairs (planned-text receipts admit a missing write), r3's against r2's (the sha check cannot kill the planned-text mechanism; per-trial stamp stability is not cross-trial identity), r4's against r3's (the eligibility machinery divides by zero at N=0 and adjudicates cross-stamp load pairs). Every finding was probe-backed and admissible under the brief's own contract; none was scaffolding — the artifact under review IS the instrument design, so the finding rate (5/3/4/2, decaying) reads as convergence on a shrinking defect set rather than a recognizer ratchet. The reviewer's probes were consistently drawn from the declared domain (live `psqlStartupScan` sites, registry digests), which is the admissibility contract working as written.

**Mechanizable:** declined: every mechanizable surfaced by these rounds lands in-arc as an AC fixture (the zero-eligible refusal, the cross-stamp pair refusal, the rotation refusal, the prefix-bearing behavioral kill) — the killer-audit at implementation derives its obligation list from the AC table's fourth column, so there is nothing left to schedule as a separate row.
