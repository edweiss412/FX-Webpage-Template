# feat/speclint-expect-n-exit-status — round filings

## plan — 4 rounds

**Examined:** all four plan rounds at this base (the arc's only base; cross-base total 4). Nine findings across rounds 1–3, zero at round 4. Shape: r1 was test-design rigor (an unreachable rule-2 fixture promise, incomplete rule isolation, missing string-presence mutant quartets, an adapter RED that injected past the surface under test, and a fifth export drifting from the approved spec's §6 table); r2 was contract-reading depth (unresolved-import REDs against the RED-validity rule, a spec-required stderr tail dropped from an interface record, a 1-vs-3 declaration count against the premise-contract equality); r3 was one optional-field coverage gap (`Finding.detail` omitted silently by the renderer). Every finding was probe-backed by the reviewer and none was disputed; every repair landed in the next commit with the class swept in the same round.

**Judgment:** seven of nine. Fixture-isolation design, adapter seam placement, spec-interface fidelity, and optional-payload coverage are readings of the plan against the spec and the writing-plans rules — review's class, repaired at normal cost (one round each, no recurrence: no vector survived two rounds).

**Mechanizable:** two candidates, both declined.

- declined: plan-time reconciliation of a registry edit's declaration count against the premise-contract per-suite equality — the equality already gates at implementation time (`tests/mutation/_metaPremiseContract.test.ts:535`), the gap was only that the PLAN under-stated the count (one round's cost), and the 2026-08-25 process mint freeze parks tooling-on-tooling arms without a product-blocked incident.
- declined: export-table parity between a plan's interface list and its approved spec's §6 table — it needs spec-table extraction that does not exist, its done condition is a property of the tooling (the freeze's admission test), and the defect cost one fresh-eyes round.

**Infra:** none. All seven dispatches (3 spec + 4 plan) returned clean verdicts on attempt 1; no reaps, no `no_verdict`, no recovery paths.
