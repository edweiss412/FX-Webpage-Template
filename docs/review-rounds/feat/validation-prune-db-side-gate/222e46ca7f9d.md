# Review-round economy — `feat/validation-prune-db-side-gate` @ `222e46ca7f9d`

**This file is the second half of one arc.** The merge-base moved from `50ca72a566b0` to
`222e46ca7f9d` when `origin/main` was merged mid-diff-stage, and `<baseSha12>` is the corpus's arc
identity, so the diff stage's rounds are split across two files. The spec stage (8 rounds), the plan
stage (5 rounds) and the FIRST diff round are recorded next to `50ca72a566b0.jsonl`, with the spec and
plan filings there.

Per the per-base convention already on disk at
`docs/review-rounds/feat/destructive-guard-discovery-by-connection/`, the three dispatches in this
file are labelled 1..3 at this base rather than carrying lifetime numbering, and the first row records
`_roundAtPreviousBase: 1` so the two halves can be read as one sequence. **The stage ran FOUR rounds in
total** — the section below describes all four — and the heading counts the three recorded here,
because that is what this file's corpus holds. The arc-tier undercount that results is the known
status-quo defect `BL-REVIEW-ROUND-COUNT-RESETS-ON-REMERGE` exists to fix; this arc did not work around
it.

## diff — 3 rounds

**Examined:** every finding across all four whole-diff rounds and the mutant each one carried — r1 at base `50ca72a566b0`, r2 to r4 here as rounds 1 to 3. R1
NEEDS-ATTENTION (1), R2 NEEDS-ATTENTION (1), R3 NEEDS-ATTENTION (1), R4 APPROVE (0).

**All three findings landed on the same thirty lines**, and none of them touched the shipped SQL. Every
one was against AC-9's STATIC pin — the assertion that the shipped function bodies are the intended
ones — and the pattern across them is the filing.

- **R1: the pin was circular.** It derived its expected body by reading the migration it was testing, so
  channel-dependent logic added to that migration moved both sides of the comparison. The companion
  check, that each prune's `prosrc` contains the `perform` before any `delete`, was a substring ORDER
  oracle a conditional wrapper satisfies. Both instances shown with mutants that kept every psql-driven
  refusal green while PostgREST would have gone on deleting.
- **R2: the repair for R1 was itself defeated.** R1's repair added a denylist rejecting
  `request.jwt.claims` and `current_setting`. R2 got past it with `session_user = 'postgres'` — psql
  connects as `postgres`, PostgREST as `authenticator`, and `security definer` changes neither.
- **R3: the pin's normaliser erased the structure it needed.** Collapsing all whitespace destroys
  newlines, and `--` runs to end of line, so pulling the whole `if` block onto a trailing comment line
  comments the refusal out and still normalises to a byte-identical string.

**Mechanizable:** one candidate, declined.

- **A denylist over channel-discriminating SQL constructs**, which is what R1's repair actually shipped
  and what R2 defeated.

declined: the class is open — `request.jwt.claims`, `current_setting`, `session_user`, `current_user`,
`inet_client_addr()`, `application_name`, `pg_backend_pid()` — and a denylist grown one construct per
review round is the recognizer arms race AGENTS.md's round-economy rule names, whose prescribed answer
is to narrow. It was removed at R2 rather than extended, and spec §4.8 fences that decision in both
directions so neither side is relitigated. No ledger row is filed, because filing one would commit
somebody to building the recognizer this entry declines.

**Judgment:** the axis is one choice made in the spec, and it is worth naming.

- **AC-9 proves channel-independence STATICALLY because it cannot prove it behaviourally.** Spec §4.7
  settled that: a PostgREST `rpc` request cannot be enclosed in a transaction the probe controls, so a
  REST limb would COMMIT whatever a broken gate let through — the one exemption the arc's rollback rule
  forbids. Choosing a static proof was correct. But a static proof over a program's TEXT is a
  recognizer problem, and it drew three rounds while every other part of the diff drew none.
- **The behavioural cases caught all three mutants; the static pin caught none of them until repaired.**
  That is the measurement worth keeping. R3's mutant genuinely neuters the gate, and the refusal cases
  went red on it immediately. The static pin exists to cover the ONE channel those cases cannot reach,
  and it is the weakest assertion in the arc for exactly the reason it exists: it is the only claim here
  that cannot be settled by running something.
- **The rule that generalises**, and it is narrower than "prefer behavioural tests": when a criterion
  must be proven statically because its channel cannot be executed, expect its cost to land in review
  rather than in CI, and budget rounds for it accordingly. Three of this arc's four diff rounds went to
  the one assertion that could not be run. Nothing was wrong with the choice; what was wrong was
  treating that assertion as though it were as cheap to get right as the ones a database answers.

**Infra:** none. All four dispatches returned a verdict on the first attempt. The reviewer's sandbox
could not run Vitest (temporary-directory `EPERM`) or reach a database, and said so each round rather
than reporting those as findings — so every finding here is a static reading of the diff, and each
was reproduced by the author against a real database before being repaired.

**The stage converged, and where it converged is the honest close.** R4 returned APPROVE with zero
findings against the same bound, the same probe domain and the same fence the three previous rounds
used — the criterion did not move to reach the verdict. Across spec, plan and diff this arc ran
seventeen rounds and thirty-five findings, and the shipped SQL is the same five-statement migration
that was dry-run against a real database before round 1.
