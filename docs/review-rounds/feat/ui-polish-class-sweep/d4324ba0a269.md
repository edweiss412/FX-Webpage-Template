## diff — 1 round

**Examined:** 4 diff rounds across the arc's TWO bases, which is what reaches
ROUND_THRESHOLD under the 2026-08-22 arc-sum rule: rounds 1, 2 and 3 on
58b40e0548d5, then one more here on d4324ba0a269 after absorbing #891 moved the
merge base. This file's own count is 1. Declared findings: R1 4, R2 3, R3 1,
and 2 on the round in this file.

**Mechanizable:** declined: both gaps below are process-facing, and the 2026-08-25 process mint freeze keeps a process-facing finding out of the open queue — it files to the owning surface's documented-limits record instead. The repair direction is recorded in the arc's design doc (L0, corrected) and the red-tree rule is now a standing orchestrator rule, so neither needs a row to be actionable. Two distinct things, and only the second is a tooling gap.

The finding that kept coming back was one predicate, three times: rounds 1, 2 and 3 each answered the alert-pill outline test by naming one more recognised family (standalone physical widths, then side colours and logical sides, then the important marker). **That is an accept-list over an open grammar, and an accept-list fails CLOSED on every spelling nobody has thought of yet, which is why the same axis kept returning; the repair that closed it was to NORMALIZE and add a derived cover so the rules got FEWER rather than more, which is the AGENTS.md "repair direction under same-axis recurrence" rule made concrete.** The mechanizable lesson is the direction itself: on a recognizer, the third round on one axis is the signal to invert, not to extend.

The genuine gap: **all three rounds reviewed a tree whose full suite was red**, and nobody knew. The suite named five failing files:

- an e2e spec that called `compileEntryCss`, which enrols it in `_metaFontFidelityWiring`'s walked population, while binding `test` from `@playwright/test` — so the font oracle never attached and the spec had been running without it;
- a stale `standalone-baseline.json`, which is the registry fan-out a new e2e spec always owes;
- `help-prose-layer`, which read a FIXED 1600-character window over a 3145-character `:root` block and failed as "`--color-info-bg-runtime` must be defined" when the token was present and the parser had stopped early;
- `_metaStripCommentsSingleSource`, naming five files that hand-rolled comment handling against a mandatory shared module (see the correction above);
- `_metaRenderFaultMarking`, whose `file:line` residue key this branch's own +5-line commit had shifted.

Two were defects the diff introduced, two were guards the diff broke by joining their walked populations, and one was a latent brittle parser that an ordinary token addition tipped over. **None was reachable by reading a diff**, which is what a reviewer does. The predicate's false PASS survived three rounds for the same reason: nothing in the loop was executing the thing under review. Round 4 was dispatched only against a green frozen tree, and the standing rule is now that no review is dispatched on a tree whose full suite is not green.

The prose-versus-code shape recurred a fifth time in this arc, in an ad-hoc verification one-liner rather than a shipped scanner: a check for "is the dropped ledger row archived" counted PROSE cross-references to the row id as if they were rows. It is recorded at L0 with its re-file trigger; the shipped-scanner count is unchanged at four.

**Judgment:** The rounds that were genuinely about design ended at R1. R2 and
beyond were repair-verification: each found a defect in a REPAIR, not in the
product. The one finding neither round produced was SR-4, found by re-running
the class sweep on my own repair — a shared constant with five call sites, two
of them in a file that was not in the diff that changed its rendering. That is
the argument for sweeping the repair, not just the finding.

**Infra:** None. No dispatch was reaped, no result came back `no_verdict`, and
the round numbering stayed contiguous because the merge base never moved.
