# Plan — app-e2e batch 2

**Spec:** `docs/superpowers/specs/ci/2026-08-21-app-e2e-batch2-design.md` (the normative source; every task body quotes the spec's own requirement text rather than paraphrasing it)
**Probe record:** `docs/superpowers/specs/ci/probes/2026-08-21-app-e2e-batch2-membership-probe.md`
**Row:** `BL-E2E-APP-DEPENDENT-SPECS-CI-DARK` (`BACKLOG.md:687`)
**Branch:** `ci/app-e2e-batch2` (base `0ba72c237` = `origin/main` at Stage 0)

`impeccable-gate: N/A — no UI surface` (workflow YAML, test files, a scripts data table, docs; nothing under `app/` or `components/`).

---

## What this plan builds, in one paragraph

Seven test-only repairs (spec section 6, R1 to R7) make seven more `UNSEEN` specs green on the same command that observed them red; one post-fix run over all fourteen members measures the executed-count floors; one wiring commit names the fourteen in `app-e2e.yml`'s single run step, adds `DATABASE_URL` to that step, adds the fourteen `REQUIRED` rows and the `governs` extensions, deletes fourteen allowlist rows and rewrites seven class-C rows; then the five-green loop, the ceiling measurement, the early ledger closeout, whole-diff review, merge. No app code moves.

## Acceptance criteria this plan discharges

Quoted from spec section 8; each names its discharging task.

- AC-1 (wiring, structural) — Task 9.
- AC-2 (repairs, red then green on the same command) — Tasks 1 to 7, proven green together by Task 8.
- AC-3 (five consecutive green `pull_request` runs at `--retries=0`) — Task 10.
- AC-4 (fallback: a member that cannot clear AC-3 leaves, row restored with the run ids) — Task 10's procedure.
- AC-5 (oracle floors equal live resolution; the oracle reds on a doctored report) — Task 9.
- AC-6 (real CI green, sha-keyed, both vocabularies) — Task 12.
- AC-7 (ceiling measured, formula applied, recorded) — Task 10.
- AC-8 (census restated by command, `UNSEEN = 23 - members - 7` = 2, custom-reason 10, total `51 - members` = 37) — Task 11.
- AC-9 (ledger closeout lands before whole-diff review; in-progress meta-test green) — Task 11, ordering in Task 12.

## Global constraints

- **Invariant 11:** every edit in `/Users/ericweiss/FX-worktrees/app-e2e-batch2`; the main checkout is read-only.
- **Every Playwright run is a heavy phase** (AGENTS.md): wrapped at the outermost entry with `pnpm heavy`, one slot per run, never `--ui`/`--debug`. The `red=` commands below are therefore written with the wrap INCLUDED; they are replayed byte-identical.
- **Both DSNs pinned to the local stack on every local run** (spec 5.2; probe record 4.3). On this machine `.env.local`'s `TEST_DATABASE_URL` points at the remote validation pooler, so an unpinned run sends every postgres.js route elsewhere and reads as red. The env prefix `TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1` is part of every command below; a red observed without it is not evidence.
- **Shared local database.** `pnpm db:seed` before each batch run; foreign `drive-*`/`slug-*` rows from other suites are expected (probe record 4.2) and are why R3 and R4 key their fixtures to the seed or to the live count. Never delete rows outside this arc's namespaces.
- **The tree is frozen while any run or review reads it** (runs 1 to 3 were taken on a clean tree; every future measurement states `git status --porcelain | wc -l`).
- **`spec:lint --exec-red` on this plan is itself a heavy phase**: every `red-state=live` marker below is a Playwright command. Run it as `pnpm heavy pnpm spec:lint --exec-red docs/superpowers/plans/2026-08-21-app-e2e-batch2.md`, or rely on the probe record's observed reds (runs 1 to 3) and the per-task RED steps, which replay each command once. The collection-probe arm declines non-vitest commands (`lib/specLint/redContract.ts:664`, "not-vitest-shaped"), so no `RED_SUITE_UNCOLLECTED` verdict is available for them; the RED step's quoted failure line is the verdict.
- **Commit per task**, conventional style: `test(e2e): …` for repairs, `infra: …` for workflow/oracle/registry, `docs(…)` for ledger and docs. `--no-verify` only where the worktree setup already prescribes it; every committed file is `prettier --check` clean.
- **No app code** (`app/`, `components/`, `lib/`). A repair that turns out to need one stops the task and files a row (spec 1.1).

## e2e harness-readiness checklist (writing-plans rule)

- (a) Server boot: the port 3000 baseline webServer from `playwright.config.ts` (line 263: `pnpm build && pnpm start` under `CI`, `pnpm dev` otherwise), port 3000, `BASELINE_SERVER_ONLY=1` so the port 3001 to port 3004 servers never boot (`playwright.config.ts:424`). In CI the job env block of `app-e2e.yml` supplies Supabase and auth pairs; Task 9 adds `DATABASE_URL` at step level.
- (b) Readiness gate before the first assertion: every member signs in through `signInAs`, which POSTs `/api/test-auth/set-session`; modal specs gate on the loaded frame plus initial focus (R7's `awaitModalHydrated`), never `networkidle` alone.
- (c) Detach safety: no new sampler or `locator.evaluate` loop is introduced; R5 replaces a `toBeVisible` with `toHaveText`, which auto-waits on an element that stays mounted in both states.

## Meta-test inventory

- EXTENDS `tests/ci/_metaE2eWorkflowCoverage.test.ts`: fourteen `LOCAL_ONLY_ALLOWLIST` deletions, seven row rewrites (class C), nothing else; its scan discovers `app-e2e.yml` from disk.
- EXTENDS `tests/ci/_workflowCoverageScan.ts` `ENV_KEY_ALLOWLIST`: `governs` arrays gain the members on every pair the run step is in scope of (the hygiene suite's fresh derivation is the authority, `tests/ci/_metaE2eWorkflowCoverage.test.ts:1953`), including the `DATABASE_URL` value row the step gains.
- EXTENDS `scripts/check-app-e2e-executed.mjs` `REQUIRED` (data only) and, through it, `tests/cross-cutting/app-e2e-ci-wiring.test.ts`, which already pins key-set parity and live floors (line 203) and needs no change.
- CREATES none. The one shared helper this plan adds (`tests/e2e/helpers/awaitModalHydrated.ts`, Task 7) is a test helper, not a guard.
- Governing but untouched: `tests/docs/_metaLedgerInProgress.test.ts`, `tests/docs/_metaInvariant8Closeout.test.ts`, `tests/docs/_metaReviewRoundEconomy.test.ts`.
- Mutation enrolment: none (spec section 0; workflow YAML and data tables are not registry-expressible, the step3 precedent).

## Advisory-lock topology

Not applicable: no task touches `pg_advisory*` or any locked mutation path. Test fixtures write through the existing service-role helpers.

---

## Task 0: pin the environment (measurement, no red)

Runs once before Task 1 and again at Task 8; its output is pasted into the PR body.

```bash
cd /Users/ericweiss/FX-worktrees/app-e2e-batch2
git status --porcelain | wc -l                     # expect 0
lsof -nP -iTCP:3000 -sTCP:LISTEN | wc -l           # expect 0 before a run
supabase status 2>&1 | grep -c 'is running'         # expect 1
pnpm db:seed > /dev/null; echo "seed rc=$?"         # expect 0
```

If port 3000 is held, stop: a run against a sibling worktree's server measures the wrong tree.

<!-- tasks: depth=2 red-contract -->

## Task 1: `no-raw-codes` stops expecting a route the root redirect retired (R1)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/no-raw-codes.spec.ts --project=mobile-safari --retries=0` red-state=live why=`tests/e2e/no-raw-codes.spec.ts:96 asserts routePaths contains "/" while app/page.tsx does not exist and next.config.ts:66 redirects "/" — the case fails with "Expected value: "/" / Received array: ["/admin", …]" (probe record 4.1 row 9)` ac=AC-2 -->

**Files:** modify `tests/e2e/no-raw-codes.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 1 failed, 9 passed, the failure at line 96 with the quoted message.
- [ ] **Repair.** Replace line 96 `expect(routePaths).toContain("/");` with `expect(routePaths).not.toContain("/");` and a one-line comment: `/` is a next.config.ts redirect to /auth/sign-in?next=/admin (next.config.ts:66), asserted by root-landing.spec.ts; it has no page.tsx, so the static-route walk must not list it. Leave `/admin` and `not /admin/dev` expectations as they are.
- [ ] **GREEN.** Same command. Expected: 10 passed.
- [ ] **Commit:** `test(e2e): no-raw-codes stops expecting the retired "/" route`.

## Task 2: `sign-in-page` asserts the validator's rejection of a slug-only `next` (R2)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/sign-in-page.spec.ts --project=mobile-safari --project=desktop-chromium --retries=0` red-state=live why=`tests/e2e/sign-in-page.spec.ts:155 expects url.pathname to be /show/<slug> while lib/auth/validateNextParam.ts:16 rejects slug-only /show paths and app/auth/sign-in/page.tsx:128-148 sends a non-admin to /me — the case fails on both projects with "Expected: "/show/signin-…" / Received: "/me"" (probe record 4.1 rows 12, 26)` ac=AC-2 -->

**Files:** modify `tests/e2e/sign-in-page.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 2 failed (one per project), 22 passed, both at line 155.
- [ ] **Repair.** In the case at line 116: retitle to `already signed in (non-admin crew) + slug-only next=/show/<slug> is rejected → 302/redirect to /me`; change BOTH executable assertions on the retired path, line 155 (`expect(url.pathname).toBe("/me");`) and line 165 in the follow-the-redirect sanity block (`expect(new URL(page.url()).pathname).toBe("/me");`, its comment now saying the destination is `/me`, which renders no sign-in CTA, so the `toHaveCount(0)` at line 166 keeps its meaning), and rewrite the case's leading comment to state the contract: the validator rejects slug-only `/show/<slug>` (`lib/auth/validateNextParam.ts:16`; the crew route is `/show/<slug>/<token>`, line 25), and the sign-in page then routes a confirmed non-admin to `/me` (`app/auth/sign-in/page.tsx:128`). Keep the seeded show and crew row (the case's purpose, "the sign-in page bounces out of the way", is unchanged). Sweep, pasted at this head: `rg -n -F '/show/${slug}' tests/e2e/sign-in-page.spec.ts` prints six lines, two executable assertions (line 155, line 165; both change) and four request inputs (line 83, line 148, line 162, line 219; none changes, the slug-only `next` IS the input under test). Plan round 4 caught the repair naming line 155 alone, after which execution reaches line 165 and the stated GREEN is unreachable. Do not add a tokenized-`next` case here (spec section 9 names it as a strengthening outside this batch).
- [ ] **GREEN.** Same command. Expected: 24 passed.
- [ ] **Commit:** `test(e2e): sign-in redirect case asserts the picker-pivot contract for a slug-only next`.

## Task 3: seed-keyed show selection in `admin-route-boundaries` and `admin-parse-panel` (R3 + its class peer)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/admin-route-boundaries.spec.ts --project=desktop-chromium --retries=0` red-state=live why=`tests/e2e/admin-route-boundaries.spec.ts:57-67 selects the first published non-archived show with no namespace and, on the shared local database, receives a foreign slug-* show with zero crew; the helper throws "no crew member for show slug-… (run pnpm db:seed). error=no row" (probe record 4.1 row 14). On a database holding only the seed the same command is green, which is why the red must be observed on THIS machine's stack and why the repair keys the selection instead of cleaning the neighbour's rows` ac=AC-2 -->

**Files:** modify `tests/e2e/admin-route-boundaries.spec.ts`, `tests/e2e/admin-parse-panel.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 1 failed at line 132 with the quoted helper error, 4 passed. If it is green, the foreign rows have gone; record that and skip to the repair, whose discriminator is then the GREEN step's unchanged count plus the helper's new by-name failure under a constructed seedless state (insert nothing; point the prefix at a nonexistent namespace once, observe the throw, restore).
- [ ] **Repair (R3).** In `lookupPublishedShowWithCrew` (line 55-line 74): add `.like("drive_file_id", "seed-fixture:%")` (the prefix `supabase/seed.ts:22` manages) to the shows select, and keep the crew lookup's by-name throw.
- [ ] **Repair (class peer).** In `tests/e2e/admin-parse-panel.spec.ts` `lookupSeed` (line 52-line 62): add the same `.like("drive_file_id", "seed-fixture:%")` to the shows select. Same defect, different file, same commit (AGENTS.md disposition rule).
- [ ] **Sweep, re-run at this head and pasted:** `rg -n -A6 'from\("shows"\)' tests/e2e/admin-route-boundaries.spec.ts tests/e2e/admin-parse-panel.spec.ts tests/e2e/staged-preview.spec.ts tests/e2e/admin-settings-admins-refresh.spec.ts tests/e2e/dev-capture.spec.ts tests/e2e/developer-tier.spec.ts tests/e2e/needs-attention-page.spec.ts tests/e2e/no-raw-codes.spec.ts tests/e2e/published-show-attention.spec.ts tests/e2e/roles-settings-layout.spec.ts tests/e2e/sign-in-page.spec.ts tests/e2e/source-link-dimensional.spec.ts tests/e2e/telemetry-layout.spec.ts tests/e2e/warning-panel-polish.spec.ts tests/e2e/helpers/*.ts | rg -n 'eq\("published"' ` — every hit must now sit under a statement block that also carries `seed-fixture:` or an own-id scope; the spec-time sweep found exactly these two, and a third hit at this head is a new instance to repair here, not to file.
- [ ] **GREEN.** The marker command: 5 passed. Then `pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/admin-parse-panel.spec.ts --project=mobile-safari --project=desktop-chromium --retries=0`: 10 passed (it was already green on run 2; this proves the peer repair changed nothing it should not).
- [ ] **Commit:** `test(e2e): key the route-boundaries and parse-panel show lookups to the seed corpus`.

## Task 4: `needs-attention-page` derives the badge text from the live counts (R4)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/needs-attention-page.spec.ts --project=mobile-safari --project=desktop-chromium --retries=0` red-state=live why=`tests/e2e/needs-attention-page.spec.ts:189, :203 and :235 assert the badge literals "3" and "2" while the badge renders the two exact head-counts over the whole table (lib/admin/loadNeedsAttention.ts, components/admin/nav/AdminNav.tsx:230 caps at "9+"); with foreign pending rows present the three cases fail per project with "Expected: "3" / Received: "9+"" (run 1) or "8"/"4" (run 2), probe record 4.1 rows 6-8 and 5 (b)` ac=AC-2 -->

**Files:** modify `tests/e2e/needs-attention-page.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 6 failed (three per project) at line 189, line 203, line 235; 6 passed.
- [ ] **Repair.** Add two helpers to the spec: `pendingBadgeCount()`, which through the existing `admin` client runs a head count with `.is("wizard_session_id", null)` on BOTH tables, `pending_ingestions` (`select("id", { count: "exact", head: true })`, the production query at `lib/admin/loadNeedsAttention.ts:87`) and `pending_syncs` (`select("staged_id", { count: "exact", head: true })`, line 139; that table keys on `staged_id`), and returns the numeric sum. Each call destructures `{ count, error }` and throws by name on a returned `error`, on a thrown error, and on a non-numeric `count`, the production contract at `lib/admin/loadNeedsAttention.ts:80` rendered as a throw because a test oracle has no caller to hand a typed result to; the helper carries `// not-subject-to-meta: test-local oracle in a spec file, not a lib helper; every error path throws and fails the case by name` (the `tests/e2e/crew-page.spec.ts:58` shape). Plan round 3 caught the filter named on one table only; a fixture with a non-null wizard session would then have made this oracle disagree with the badge; and `fmtBadge(n)`, the nav's own rule (`n > 9 ? "9+" : String(n)`, `components/admin/nav/AdminNav.tsx:230`). Every one of the four literal assertions (line 189, line 194, line 203, line 235) is replaced; none stays. Flow 3: after `seedSyncRows(3)`, `await expect(badge).toHaveText(fmtBadge(await pendingBadgeCount()))`; after growing by 9, the same. Flow 4: after `seedSyncRows(2)` and before `goto`, `const before = await pendingBadgeCount();`; the line-203 assertion becomes `await expect(badge).toHaveText(fmtBadge(before))` (the rendered BEFORE state, proved); then the server-side insert, `const after = await pendingBadgeCount(); expect(after).toBe(before + 1);`, the soft-nav click, and `await expect(badge).toHaveText(fmtBadge(after), { timeout: 10_000 })`. Flow 5 is the same shape: `before` captured after the two seeds and before `goto`, line 235 becomes `toHaveText(fmtBadge(before))`, Discard, `expect(after).toBe(before - 1)`, then `toHaveText(fmtBadge(after))`. The before-assertion plus the after-assertion is the rendered transition the literals used to prove; the delta is asserted on the counts, which is what spec R4 requires, and stays exact past the cap (12 to 13 renders "9+" on both sides; the count assertion still discriminates). Anti-tautology: the counts come from the database through the admin client and the rendered text from the badge; a Discard that removed two rows fails `toBe(before - 1)` by number, a badge that stopped counting fails `toHaveText` by text, and a badge that updated early fails the before-assertion. A foreign row landing between `before` and the render is a local shared-database artifact, visible as a before-assertion failure with the count in the message; CI's database is private to the job. Keep the namespace pre-clean and cleanup exactly as they are.
- [ ] **GREEN.** Same command. Expected: 12 passed, with foreign rows still present (assert with `psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -Atc "select count(*) from pending_syncs where drive_file_id like 'drive-%'"` printed beside the run; a zero there means the repair was not exercised against the condition it closes, which is the premise rule: state it).
- [ ] **Commit:** `test(e2e): needs-attention badge cases derive their expected text from the live counts`.

## Task 5: `roles-settings-layout` waits for the confirmation text, not a box (R5)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/roles-settings-layout.spec.ts --project=desktop-chromium --retries=0` red-state=live why=`tests/e2e/roles-settings-layout.spec.ts:278 awaits toBeVisible on role-mapping-saved-confirm, which app/admin/settings/roles/RoleMappingRow.tsx:165-174 renders sr-only until updateRoleTokenMapping returns ok (:118); the 1 px box satisfies toBeVisible while the save's server action is still in flight (run 3 trace: status -1), so :279 measures "Expected: <= 1 / Received: 697" (probe record 4.1 row 25, 6)` ac=AC-2 -->

**Files:** modify `tests/e2e/roles-settings-layout.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 1 failed at line 279 with the quoted message, 4 passed. (Run 2's `beforeAll` 502 is the local gateway class; if it recurs, re-run once and record both.)
- [ ] **Repair.** At line 278, replace `await expect(saved).toBeVisible();` with `await expect(saved).toHaveText(COPY.EDIT_SAVED_CONFIRM);` (the constant the component renders at `app/admin/settings/roles/RoleMappingRow.tsx:174`), then measure. A slow action now waits; a failed action fails by name ("expected … to have text") instead of measuring the placeholder.
- [ ] **GREEN.** Same command. Expected: 5 passed.
- [ ] **Commit:** `test(e2e): roles saved-confirm measurement waits for the confirmation text`.

## Task 6: `admin-settings-admins-refresh` opens the disclosure before adding (R6)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/admin-settings-admins-refresh.spec.ts --project=desktop-chromium --retries=0` red-state=live why=`tests/e2e/admin-settings-admins-refresh.spec.ts:139-140 fills and clicks inside the add form while components/admin/settings/AddAdminDisclosure.tsx:8 keeps that form behind the admin-add-admin-trigger button (panel admin-settings-add-admin at :71); the click retries "admin-settings-admins-card intercepts pointer events" until "Test timeout of 60000ms exceeded" (probe record 4.1 row 15, 6; run 3 trace)` ac=AC-2 -->

**Files:** modify `tests/e2e/admin-settings-admins-refresh.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 1 timedOut (the only case), 60 s.
- [ ] **Repair.** Before the `addForm` fill at line 139: `await page.getByTestId("admin-add-admin-trigger").click();` then `await expect(page.getByTestId("admin-allowlist-add-form")).toBeVisible();` (the panel is `admin-settings-add-admin`, `components/admin/settings/AddAdminDisclosure.tsx:71`; the trigger carries `aria-expanded`, line 9, so an `toHaveAttribute("aria-expanded", "true")` on the trigger is the stricter wait and is preferred). Keep the revoke half untouched.
- [ ] **GREEN.** Same command. Expected: 1 passed, well under 60 s (record the duration).
- [ ] **Commit:** `test(e2e): admins-refresh opens the Add-admin disclosure before filling the form`.

## Task 7: `warning-panel-polish` gates the Ignore click on hydration (R7)

<!-- task: red=`pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/warning-panel-polish.spec.ts --project=desktop-chromium --retries=0` red-state=live why=`tests/e2e/warning-panel-polish.spec.ts:275-276 clicks the first dq-ignore-* button straight after openShowReviewModal (tests/e2e/helpers/openShowReviewModal.ts, mount-only) and asserts the announcer text; run 3's trace shows the click completing in 179 ms with NO data-quality/ignore request, so components/admin/DataQualityWarningControls.tsx's onClick={run} was not yet attached — the case fails "Expected: "Warning ignored." / Received: """ and the serial describe skips :340 (probe record 4.1 row 27, 6)` ac=AC-2 -->

**Files:** create `tests/e2e/helpers/awaitModalHydrated.ts`; modify `tests/e2e/warning-panel-polish.spec.ts`, `tests/e2e/dev-capture.spec.ts`.

- [ ] **RED.** Run the marker command. Expected: 1 failed at line 276 with the quoted message, 1 skipped (line 340), 2 passed.
- [ ] **Helper.** Move `awaitModalHydrated` from `tests/e2e/dev-capture.spec.ts:50` to `tests/e2e/dev-capture.spec.ts:62` into `tests/e2e/helpers/awaitModalHydrated.ts` (export, same body: loaded frame visible, `published-show-review-modal` count 1, initial focus polled to `published-show-review-close`); import it in `dev-capture.spec.ts` in place of the local copy (two copies drift; one is enough).
- [ ] **Repair.** In `warning-panel-polish.spec.ts` `openModal` (line 97-line 100), after `openShowReviewModal(...)`, `await awaitModalHydrated(page);`. Nothing else in the spec changes. The gate is the RATIFIED repair pattern for this class (`docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:309`), not a workaround; spec section 9 records the framework window itself as a documented limit with its re-file trigger, and spec review round 3 plus the orchestrator ruling settled the classification, so it is not re-opened here.
- [ ] **GREEN.** Same command. Expected: 4 passed, including the reveal case at line 340, which runs for the first time on this branch; if THAT case is red, its failure is new evidence and is dispositioned (test-only → repaired here as R8; app → the spec leaves the batch under AC-4 with a row), never hidden by a skip. Then `pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/dev-capture.spec.ts --project=desktop-chromium --retries=0`: 4 passed (the helper move changed nothing).
- [ ] **Commit:** `test(e2e): warning-panel gates the Ignore click on modal hydration; hydration helper shared`.

<!-- tasks: end -->

## Task 8: the post-fix run — the measurement membership derives from (measurement, no red)

One heavy-wrapped invocation over every member, the exact command `app-e2e.yml` will carry minus the `--reporter` destination:

```bash
cd /Users/ericweiss/FX-worktrees/app-e2e-batch2 && pnpm db:seed && \
pnpm heavy env TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres BASELINE_SERVER_ONLY=1 PLAYWRIGHT_JSON_OUTPUT_NAME=test-results/app-e2e-batch2-postfix.json \
  pnpm exec playwright test tests/e2e/sample.spec.ts tests/e2e/root-landing.spec.ts tests/e2e/admin-layout.spec.ts tests/e2e/admin-phase2-surfaces.spec.ts tests/e2e/notify-toggles.spec.ts tests/e2e/me-page.spec.ts tests/e2e/report-modal.spec.ts tests/e2e/help-pages.spec.ts tests/e2e/admin-changes-feed-layout.spec.ts tests/e2e/developer-tier.spec.ts tests/e2e/source-link-dimensional.spec.ts tests/e2e/staged-preview.spec.ts tests/e2e/telemetry-layout.spec.ts tests/e2e/admin-parse-panel.spec.ts tests/e2e/dev-capture.spec.ts tests/e2e/published-show-attention.spec.ts tests/e2e/no-raw-codes.spec.ts tests/e2e/sign-in-page.spec.ts tests/e2e/admin-route-boundaries.spec.ts tests/e2e/needs-attention-page.spec.ts tests/e2e/roles-settings-layout.spec.ts tests/e2e/admin-settings-admins-refresh.spec.ts tests/e2e/warning-panel-polish.spec.ts \
  --project=mobile-safari --project=desktop-chromium --retries=0 --reporter=list,json > /tmp/app-e2e-batch2-postfix.log 2>&1; echo "rc=$?"
```

(The nine batch-1 specs are included so the run is the job's exact content and the wall clock is the job's local proxy.) Expected: rc 0. Then derive the per-spec executed counts from the report through the oracle's own walk, never by arithmetic:

```bash
node -e '
const r=JSON.parse(require("fs").readFileSync("test-results/app-e2e-batch2-postfix.json","utf8"));
const m=new Map();const walk=s=>{for(const x of s??[]){for(const sp of x.specs??[]){const b=sp.file.split("/").pop();for(const t of sp.tests??[]){const ok=(t.results??[]).length===1&&t.results[0].status==="passed";if(!ok)continue;if(!m.has(b))m.set(b,new Set());m.get(b).add(`${sp.file}:${sp.line}:${sp.title}|${t.projectId}`);}}walk(x.suites);}};walk(r.suites);
for(const [b,s] of [...m].sort())console.log(b,s.size);'
```

Expected, from the `--list` resolution in the probe record section 2 (the report must agree; a disagreement is a runtime skip and stops the task): `developer-tier` 7, `source-link-dimensional` 5, `staged-preview` 8, `telemetry-layout` 3, `admin-parse-panel` 10, `dev-capture` 4, `published-show-attention` 6, `no-raw-codes` 10, `sign-in-page` 24, `admin-route-boundaries` 5, `needs-attention-page` 12, `roles-settings-layout` 5, `admin-settings-admins-refresh` 1, `warning-panel-polish` 4; the batch-1 nine unchanged from `REQUIRED`. Record the wall clock from the log's last line and the `duration` in `stats` for Task 10's ceiling formula. A member red here leaves the batch under AC-4 before the PR opens (allowlist row stays, run line recorded); it does not ride. The green set of THIS run is the member set every later task uses, written into the PR body as `MEMBERS` with one line per name (fourteen if none left); Task 9 deletes rows for that set only, and Task 11's census takes its `d`. The oracle run in Task 9 against this same report is the executable assertion of the counts above: a member absent from the report fails there by name, so an empty line here cannot pass silently.

<!-- tasks: depth=2 red-contract -->

## Task 9: the wiring commit — run step, `DATABASE_URL`, oracle rows, `governs`, allowlist (AC-1, AC-5)

<!-- task: red=`pnpm vitest run tests/ci/_metaE2eWorkflowCoverage.test.ts tests/cross-cutting/app-e2e-ci-wiring.test.ts` red-state=authored red-target=`.github/workflows/app-e2e.yml:150` why=`deleting the fourteen members' UNSEEN rows from LOCAL_ONLY_ALLOWLIST makes "every e2e spec is PR-covered or reason-allowlisted" report them dark, because the run step at the cited line names only the batch-1 nine; the wiring test's key-set parity then reds once the run step names them without REQUIRED rows` ac=AC-1,AC-5 -->

**Files:** modify `.github/workflows/app-e2e.yml`, `scripts/check-app-e2e-executed.mjs`, `tests/ci/_workflowCoverageScan.ts`, `tests/ci/_metaE2eWorkflowCoverage.test.ts`.

- [ ] **RED, step 1.** Delete the `UNSEEN` rows of every name in Task 8's `MEMBERS` set, the fourteen below if Task 8 left none under AC-4 (`admin-parse-panel`, `admin-route-boundaries`, `staged-preview`, `admin-settings-admins-refresh`, `dev-capture`, `developer-tier`, `needs-attention-page`, `no-raw-codes`, `published-show-attention`, `roles-settings-layout`, `sign-in-page`, `source-link-dimensional`, `telemetry-layout`, `warning-panel-polish`; the full list in spec section 4, derived there from the runs; a name Task 8 rejected keeps its row, gets no `REQUIRED` row and no `governs` entry, and is named in the PR body with its run line). Run the marker command. Expected: FAIL, "dark specs" naming exactly the deleted paths.
- [ ] **Workflow.** In `app-e2e.yml`: (1) `name:` at line 1 becomes `App e2e (mobile-safari + desktop-chromium)`; (2) the run step at line 150 appends the member paths, same flags, same order of flags; (3) the run step's `env:` gains `DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres` with the lifecycle tap-target step's rationale comment (`.github/workflows/lifecycle-layout-e2e.yml:193`); (4) `timeout-minutes` stays 30 in this commit and moves in Task 10 once measured; (5) the header comment names batch 2, the probe record, and the members. No `if:`, no new `pull_request` key.
- [ ] **Oracle rows.** In `REQUIRED` add one row per member with the Task 8 count and a derivation comment in the existing rows' style (`N cases x P projects, measured <date>` ).
- [ ] **Registry.** Run the marker command. Expected now: the scan rejects the run step for the novel `DATABASE_URL` pair at step scope only if the existing value row's text differs; it does not (same DSN as lifecycle), so the failure reported is the stale `governs` sets. Iterate: run, read each reported expected set, apply, re-run, until the governance-equality and pair-hygiene assertions are green. Do not touch rows the failures do not name.
- [ ] **Class-C rows.** Rewrite the seven rows from `UNSEEN` to a custom reason: the five project-only ones (`admin-dev`, `deep-link-walker`, `help-auth`, `help-mobile`, `help-typography`) in the `attention-modal-gallery` row's shape (`tests/ci/_metaE2eWorkflowCoverage.test.ts:136`), naming the workflow and its invocation line (`.github/workflows/dev-gate-e2e.yml:153` / `.github/workflows/help-affordances.yml:97`), the path filter (and dev-gate's daily schedule), and why it cannot join the required set; the two screenshot ones (`help-screenshots-clock-pipeline`, `screenshots-help-capture`) in the `section-header-visual` row's shape (line 172), naming `.github/workflows/screenshots-drift.yml:118`'s docker block, the `screenshot:help` script (root package.json line 50), the `playwright.screenshots.config.ts` projects (lines 25-26, 44-45), the path filter and schedule. Every row cites spec section 7.3.
- [ ] **GREEN.** Marker command: PASS (coverage: every member covered, no stale or shadowing rows, governance equal; wiring: one invocation, exact project set, `--retries=0`, json reporter, oracle in command position, bare trigger, key-set parity, every floor equals live resolution and is positive).
- [ ] **Oracle discrimination (AC-5).** `node scripts/check-app-e2e-executed.mjs --report test-results/app-e2e-batch2-postfix.json` exits 0 and prints every member; then doctor a copy by removing one member's suites and run again: exit 1 naming that member. Paste both lines.
- [ ] **Commit:** `infra: app-e2e wires batch 2 — the Task 8 member set, DATABASE_URL on the run step, oracle rows, governance, allowlist retired`.

<!-- tasks: end -->

## Task 10: five-green loop and the ceiling (AC-3, AC-4, AC-7; CI evidence)

- [ ] Push, open the PR (`gh pr create`), body per Task 11's shape with "AC-3 in progress".
- [ ] Serial loop, one run at a time, the batch-1 loop (`docs/superpowers/plans/2026-08-09-app-e2e-batch1.md`, Task 7 Step 1b) with ONE substitution: that loop hardcodes `--branch ci/app-e2e-batch1`, which on this branch matches no run and exits after its 200 s window, so the branch is derived, `--branch "$(git branch --show-current)"` (prints `ci/app-e2e-batch2` here). Everything else is byte-for-byte: sha-bound run ids from `gh run list --workflow=app-e2e.yml --event pull_request --json databaseId,headSha` filtered on `git rev-parse HEAD`, duplicate-id refusal, `gh run watch "$id" --exit-status`, `git commit --allow-empty` re-triggers, never a push while a run is live; `cancelled` restarts the count and is never an AC-4 trigger.
- [ ] **Ceiling (AC-7).** From run 1 of the loop: `D=$(gh run view <id> --json jobs --jq '[.jobs[] | select(.name=="app-e2e") | ((.completedAt|fromdate)-(.startedAt|fromdate))] | if length==1 then .[0] else error("app-e2e job count \(length)") end')` and then `[[ "$D" =~ ^[0-9]+$ ]] || { echo "no D: $D"; exit 1; }` is the measured CI duration D in seconds. The selector errors instead of printing nothing when the job is absent (probe 2026-08-22: a jobs array holding only `other` gives `jq: error ... app-e2e job count 0`, rc=5; the same array with `app-e2e` prints `60`, rc=0), so a wrong run id or a renamed job stops the task rather than measuring nothing. Set `timeout-minutes` to `max(30, ceil(D*1.5/60)+5)` (spec 5.3), with a YAML comment naming the run id and D. If that changes the file, it is a content push: land it BEFORE the five-count starts (it restarts the count anyway), not after.
- [ ] **AC-4.** A member red on any counted run: diagnose from the run's own report; a test defect repaired in-branch restarts the count; a flake or app defect removes the member from the run step, restores its allowlist row with the failing run ids as the reason, removes its `REQUIRED` row and `governs` entries, and restarts the count. Record every restart in the PR body.
- [ ] Record the five run ids in the PR body and in section 12 below.

## Task 11: ledger closeout, census, docs (AC-8, AC-9) — BEFORE whole-diff review

**Files:** `BACKLOG.md`, `docs/superpowers/specs/ci/2026-08-21-app-e2e-batch2-design.md` (section 4 receives the Task 8 post-fix line per member), this plan (section 12).

- [ ] Batch-2 paragraph in the row (members, date, PR, the DSN finding and the `DATABASE_URL` carry), the seven class-C reclassifications, the `empty-state-reachability` deferral row (`**Facing:** product`, probe evidence = run-1 rows 2-5/17-20 and the retired-route citation), and the census restated by the spec's section-2 commands at this head, pasted with the commands. Remove the `**Status:** IN PROGRESS · **Branch:** ci/app-e2e-batch2` marker in this same commit (the ratified early-closeout ordering; spec 7.2).
- [ ] **Census, asserted not described (AC-8).** Run at the closeout head and paste the output into the ledger row beside the numbers; every line must print `ok`:

```bash
F=tests/ci/_metaE2eWorkflowCoverage.test.ts
rows(){ awk '/^const LOCAL_ONLY_ALLOWLIST/,/^};/' "$F" | grep -E '^ +"tests/e2e/'; }
cls(){ rows | sed -E 's/^ +"[^"]+":[[:space:]]*//' | awk '{ v=$0; sub(/,$/,"",v); if (v ~ /^(UNSEEN|PATH_GATED|PATH_GATED_BY_EXCLUSION|LOCAL_ONLY_GALLERY_CAPTURE)$/) print v; else print "custom-reason" }'; }
chk(){ n=$(cls | grep -c "^$1\$"); [ "$n" -eq "$2" ] && echo "ok $1=$n" || { echo "FAIL $1=$n expected $2"; exit 1; }; }
d=0   # 14 minus the members at closeout: every Task 8 reject plus every Task 10 AC-4 removal, each named in the PR body with its run line
chk UNSEEN $((2 + d))
chk PATH_GATED 14
chk PATH_GATED_BY_EXCLUSION 10
chk LOCAL_ONLY_GALLERY_CAPTURE 1
chk custom-reason 10
t=$(rows | wc -l | tr -d ' '); [ "$t" -eq $((37 + d)) ] && echo "ok total=$t" || { echo "FAIL total=$t expected $((37 + d))"; exit 1; }
rows | grep -E ': UNSEEN,$' | grep -oE 'tests/e2e/[^"]+'   # must print exactly onboarding-wizard-step1, empty-state-reachability, and the d names from the PR body
```

The expected values are spec AC-8's derivation at `d=0`; `d` is the only input, set from the PR body's member record (every Task 8 reject and every Task 10 AC-4 removal, each with its run line), never from the census output. A mismatch at the recorded `d` is a closeout defect (a row deleted or rewritten that should not have been, or one missed), never a reason to edit `d` or the expected.

- [ ] `pnpm vitest run tests/docs/_metaLedgerInProgress.test.ts tests/docs/_metaInvariant8Closeout.test.ts tests/docs/_metaReviewRoundEconomy.test.ts` green; `pnpm spec:lint` on the spec and this plan, 0 hard.
- [ ] **Commit:** `docs(backlog): record app-e2e batch 2; census restated; marker off`. Push.
- [ ] After every later `origin/main` merge: re-run the set-arithmetic verify (union of BL/DEF ids exact, `comm -12` archived-vs-open empty, in-progress markers zero) and the three meta-tests above.

## Task 12: whole-diff review, CI, merge, teardown (AC-6)

- [ ] Split tight-scope `--stage diff --round 1` dispatches (tests/e2e repairs + helper; workflow + oracle + registry + allowlist + docs), each brief with REVIEWER ONLY, the spec's section-0 lines byte-identical, the do-not-relitigate list (spec 1.1 plus every spec- and plan-round disposition), the sibling-boundary instruction, and the enrolment statement (no `GUARD SURFACE:` line: nothing enrolled, declined in prose with the step3 precedent). Never edit the tree while a dispatch is live.
- [ ] **Convergence, per scope.** Read the dispatch's result file (result.json under its `--out` dir): the stage closes only when BOTH scopes carry `status: verdict` and `verdict: APPROVE` at the SAME head. An accepted finding is repaired, verified by grep, committed in a separate call, and both scopes are re-dispatched at round n+1 with every round-n disposition fenced in the brief; a refuted finding is fenced with its citation. `no_verdict` is infra (the AGENTS.md silent-death note): re-dispatch the same round to a fresh `--out`. Cap 4 per scope; at four counted rounds the `## diff` section of `docs/review-rounds/ci/app-e2e-batch2/0ba72c23774f.md` is owed before the merge. Whether a repair restarts the Task 10 five-green count is decided by a command, not a list: `git diff --name-only <head of the last counted run>..HEAD | grep -vE '^docs/|\.md$'` printing nothing means docs-only, no restart; printing anything means a content push, and the count RESTARTS (Task 10's own rule). That covers the member specs, everything under `tests/e2e/`, `playwright.config.ts` (project resolution and the baseline server, `playwright.config.ts:82`; round 3 caught it missing from the enumerated form, which is why the rule is now the command), `.github/workflows/app-e2e.yml`, `scripts/check-app-e2e-executed.mjs`, `tests/ci/_workflowCoverageScan.ts`, the allowlist, `supabase/seed.ts`, `supabase/migrations/` and anything else the final run loads. A path under `app/`, `components/` or `lib/` in that output is a fence breach (spec 1.1), not a restart. After any restart, re-record the five run ids in the PR body and section 12, and re-run Task 11's set-arithmetic verify and its three meta-tests.
- [ ] CI green on the final head, by name and by conclusion, never by exit code. `sha=$(git rev-parse HEAD); R=repos/edweiss412/FX-Webpage-Template; D=$(mktemp -d)`; `gh api "$R/branches/main/protection/required_status_checks" --jq '.contexts[]' | sort -u > "$D/req"; [ -s "$D/req" ] || { echo "required set unreadable"; exit 1; }` (the required set is read from protection, never retyped, and an empty read, the shape a 403 produces, stops here instead of passing vacuously; twelve names on 2026-08-22); `gh api --paginate "$R/commits/$sha/check-runs?per_page=100" --jq '.check_runs[] | select(.status == "completed" and .conclusion == "success") | .name' | sort -u > "$D/ok"`; then `missing=$(comm -23 "$D/req" "$D/ok"); [ -z "$missing" ] && grep -qx app-e2e "$D/ok" && echo "CI GREEN $sha" || { printf 'NOT GREEN %s\nmissing required: %s\n' "$sha" "$missing"; grep -qx app-e2e "$D/ok" || echo "app-e2e not green"; exit 1; }`. `app-e2e` is advisory (spec 1.1 names its promotion to required as an owner action), so the predicate demands it by name beside the required set; a red, a `cancelled`, or a still-running context all land in `missing`, because only `completed` plus `success` reaches `ok`. Paste the predicate's output line in the PR body. `gh api "$R/commits/$sha/status" --jq '.state'` beside it is informational (Vercel lives there and is not required).
- [ ] `gh pr merge --merge`; in the main checkout `git pull --ff-only`; `git rev-list --left-right --count main...origin/main` = `0 0` AND `git merge-base --is-ancestor <ship-head> main`.
- [ ] Stage 4.4: CronDelete the nudge, clear pane and agent labels, marker `stage: done`.

## 12. Close-out

Lint note: until Task 7 commits the helper module it creates under `tests/e2e/helpers/`, `pnpm spec:lint` on this plan reports 3 hard, all `CITATION_FILE_MISSING` on that one file; they self-resolve at that commit, the shape `docs/superpowers/plans/2026-08-02-copy-deadcode-sweep.md:199` records. The closeout target (0 hard on spec and plan) is measured at Task 11, not at the plan's first commit.

Spec stage: four counted rounds at base `0ba72c23774f`, one finding each on four different axes (class-C sweep, census total, R7 classification, section-2 quoted output); closed DISPOSITIONED rather than CONVERGED by orchestrator ruling at the cap (2026-08-21), because the round-4 residue is a fact settled by the section-2 command and its positive control, and Task 11 asserts the census through the same classifier. Filing: `docs/review-rounds/ci/app-e2e-batch2/0ba72c23774f.md`. The R7 ruling and its admissible falsifier are in spec section 9.

Plan stage, round 1 (2026-08-22, head `e6cbc9630`): NEEDS-ATTENTION, four findings, all accepted and repaired in the next commit. Task 12's CI gate had no executable predicate (now `comm` of the protection-read required set against completed-and-successful check-run names, `app-e2e` demanded by name); Task 12 dispatched one diff round with no convergence branch (now both scopes to APPROVE at one head, cap 4, content repairs restart the five-green count); Task 4 re-derived the badge text where spec R4 requires the +1 and -1 deltas asserted (now asserted on the counts, the badge tied to the post-mutation count); three commands carried a `…` placeholder where an executable line belonged (the Task 3 and Task 7 GREEN peers the reviewer named, and Task 4's `psql`, which the class sweep found). Infra: the reviewer could not run Playwright `--list` in the review sandbox and relied on the probe's dated resolution, with zero code, workflow, script or config changes since the probe head.

Plan stage, round 2 (2026-08-22, head `3a32a44eb`): NEEDS-ATTENTION, four findings, all accepted and repaired in the next commit. Task 4's repair left the two initial literal assertions (line 203, line 235) unreplaced, so the shared-database GREEN could not pass and the rendered before-to-after transition was unproved (now all four literals are replaced, the before state asserted from the pre-navigation count); the AC-4 fallback did not propagate past Task 8 (now Task 8's green set is the `MEMBERS` record every later task uses, Task 9 deletes only that set, and Task 11's census is closed-form in `d`); Task 10 inherited the batch-1 loop with its hardcoded `--branch ci/app-e2e-batch1` (now derived from the checked-out branch, the rest byte-for-byte); Task 10's ceiling selector accepted an absent job (now errors on any count but one, probe pasted; the class sweep also made Task 12's required-set read fail on an empty file). Infra: Playwright `--list` again not replayable in the review sandbox; the dated resolution stood, zero code changes since the probe head.

Plan stage, round 3 (2026-08-22, head `b4be54e15`): NEEDS-ATTENTION, two findings, both accepted and repaired in the next commit. Task 4 named the `wizard_session_id is null` filter on `pending_syncs` only where production applies it to both tables, and prescribed no call-boundary contract (now both filters cited by line, `staged_id` on `pending_syncs`, `{ count, error }` destructured with every error path throwing by name, and the inline `not-subject-to-meta` comment in the crew-page shape; the same one-table wording in spec R4 is corrected in the same commit, a citation fix, not a design change); Task 12's five-green restart set was an enumeration that omitted `playwright.config.ts` (now a `git diff --name-only` derivation, docs-only iff nothing outside `docs/` and `*.md` changed). Infra: the scoped Vitest probe could not collect in the sandbox (`EPERM` on its SSR temp dir); static review.

Plan stage, round 4, the cap (2026-08-22, head `c2bcec02f`): NEEDS-ATTENTION, one finding, accepted and repaired in the next commit: Task 2 changed the line-155 assertion only, while the same case re-asserts the retired `/show/<slug>` path at line 165, so its GREEN of 24 passed was unreachable (now both assertions change, with the `rg` sweep pasted showing exactly two executable sites and four request inputs). The same sweep was run for every other repair task's retired literal (no-raw-codes line 96, roles-settings line 278, admins-refresh line 141, warning-panel `openModal`): one site each. The reviewer reproduced the census, the 114-identity resolution and all fourteen member counts; the heavy reds were not replayed (no Docker socket in the sandbox). Stage closed at the cap, DISPOSITIONED rather than CONVERGED, by orchestrator ruling (2026-08-22): the round-4 residue is a two-line repair whose verifiers are already scheduled, the pasted `rg` sweep settling the sites and the implementation stage's own red-then-green running the case, a stronger verifier than a fifth reader; the 4-4-2-1 curve with every finding accepted and no design re-opened is the convergence shape. The plan-stage section of the filing is in `docs/review-rounds/ci/app-e2e-batch2/0ba72c23774f.md`.

impeccable-gate: N/A — no UI surface

Implementation stage (2026-08-22). Twelve of the fourteen specced members ship. Two left under AC-4
mid-count, both dropped on the procedure and both recorded with every run id: `admin-parse-panel`
(run 32561531983, the page rendering "Admin session unavailable" at its Re-sync assertion) and
`warning-panel-polish` (run 32563705156, announcer empty after Ignore with the R7 gate present,
mechanism undetermined and the section-9 falsifier left OPEN because the CI-posture local
reproduction passed 4 of 4 with `--trace on`). A third red of the same shape, `needs-attention-page`
on run 32564772189, was NOT read as a member defect: three reds across three different members with
one snapshot shape, every one of them green under a local CI-posture reproduction, puts the defect's
domain in the admin loader on that runner rather than in the specs. That classifier correction is
`bl-orch`'s ruling of 2026-08-22, and it ships as its own ledger row
(`BL-ADMIN-LOADER-CI-TRANSIENT`) carrying all three run links, the three local reproductions and both
drops as incidents.

Two repairs the plan did not predict landed as R8 and R9, each first reachable only once an earlier
repair unblocked its case (spec section 4 records both with their measurements), and one CI-only
defect was repaired after the first real run: `developer-tier` asserted a build-time constant that
describes the repo against a server CI builds with the opposite value.

- Five-green run links (AC-3, all `pull_request`, `--retries=0`, head `edc1126c9` and its measurement
  triggers): [32565477244](https://github.com/edweiss412/FX-Webpage-Template/actions/runs/32565477244),
  [32566152871](https://github.com/edweiss412/FX-Webpage-Template/actions/runs/32566152871),
  [32566492382](https://github.com/edweiss412/FX-Webpage-Template/actions/runs/32566492382),
  [32566765144](https://github.com/edweiss412/FX-Webpage-Template/actions/runs/32566765144),
  [32567091035](https://github.com/edweiss412/FX-Webpage-Template/actions/runs/32567091035).
- AC-7 ceiling: D = 435 s measured on run 32558218336; `ceil(435 * 1.5 / 60) + 5 = 16`, floored at
  the existing 30, so `timeout-minutes` stays 30 with the derivation in the workflow comment.
- Review rounds: the corpus at `docs/review-rounds/ci/app-e2e-batch2/` is the authority; this line restates no counts.
