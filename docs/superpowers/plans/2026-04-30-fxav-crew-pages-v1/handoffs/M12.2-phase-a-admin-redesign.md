# M12.2 Phase A — Admin dashboard + per-show reskin — Milestone handoff

**Status:** IMPLEMENTATION COMPLETE; all §14-B gates GREEN; **awaiting CI-green merge to `main`.**
**Branch:** `m12.2-phase-a-admin-redesign` (54 commits vs main: spec draft + 32 spec-fix rounds + plan + 2
plan-fix + ~19 implementation/fix commits; head `9f8d140`). **Implementer:** Opus / Claude Code (UI hard rule).
**Orchestrator:** M12.2 orchestrator session. **Date:** 2026-05-31.

## Spawned by / scope
M12 UX-validation walk admin IA/layout theme (`working-notes.md`). Phase A = dashboard + per-show reskin, status
tokens, `<sheet-name>` fix. **UI-only, no DB writes/migrations.** Phase B (nav + settings + archived
bucket/action + the deferred server-side mutation guards) is a separate plan.

## §1 Artifacts
- Spec: `docs/superpowers/specs/2026-05-31-m12.2-phase-a-admin-dashboard-per-show-design.md` (APPROVED, 32 rounds).
- Plan: `docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/M12.2-phase-a-admin-redesign.md` (APPROVED, 2 rounds).
- Ratified plan amendment 10: `00-overview.md:257` (§9.1/§9.2 IA supersession + line-186 admin-visibility compliance).
- New code: `lib/time/{isoDate,showTimezone,showSpan}.ts`, `lib/admin/{syncStatus,needsAttention}.ts`,
  `components/admin/{StatusIndicator,StatStrip,ShowsTable,NeedsAttentionInbox}.tsx`,
  `app/admin/show/[slug]/resolveOrigin.ts`, `SHEET_PROCESS_FAILED` catalog code (§12.4 lockstep).

## §2 What shipped (per task)
T1 status tokens (calm teal positive, NOT green) + StatusIndicator + DESIGN.md §1.3 amendment · T2 shared time
helpers (behavior-preserving; crew suites green unchanged) · T2.5 `SHEET_PROCESS_FAILED` (§12.4 lockstep) · T3
bounded `fetchDashboardData` + per-row `isLive` + syncStatus/needsAttention + `_metaBoundedReads` guard · T4-T7
StatStrip / ShowsTable / NeedsAttentionInbox / composed two-col Dashboard · T8 `<sheet-name>` interpolation fix +
class-sweep · T9 per-show two-col reskin (archived-first pill, gated crew-link surfaces, read-only archived
ParsePanel, sync footer) + preview-route archived rejection · T10 rotate canonical origin + `isCrewLinkActive` ·
T11-T13 real-browser layout e2e, transition audit, DEFERRED DEF-1/DEF-2.

## §3 Gate results (spec §14-B) — ALL GREEN
| Gate | Result |
|---|---|
| TDD / commit-per-task | ✅ 19 implementation commits |
| typecheck (`pnpm typecheck`) | ✅ |
| lint (changed files) | ✅ (90 pre-existing errors in unrelated files → BACKLOG, not a regression) |
| Full suite (`pnpm test` = vitest) | ✅ 1155/1155 suites · 4483/4483 tests |
| Playwright `admin-layout-dimensions` | ✅ 4/4 — caught + fixed 2 real layout bugs jsdom missed |
| impeccable v3 dual-gate (BOTH external) | ✅ critique no HIGH/CRITICAL · audit 20/20 (2 P3/LOW cleared: em-dash→middot, raw chevron→lucide) |
| Codex impl-diff review | ✅ APPROVE — 3 rounds |
| Whole-phase fresh-eyes | ✅ (the 3-round whole-diff Codex pass) |

## §4 Bugs the gates earned (retrospective — validates two disciplines)
- **Codex impl-diff review** (ratified into A close-out, superseding the brief's B-only carve-out): R1 — crew-link
  gating conflated show-eligibility with token-presence (a transient token-read failure mislabeled a published
  show "inactive" + suppressed the post-rotate URL) → split `isShowEligibleForCrewLink` vs `hasCrewLinkUrl`. R2 —
  share token read twice (page + panel) → concurrent-rotation race → single render-scoped snapshot passed into
  `CurrentShareLinkPanel`. Neither was catchable by spec/plan or impeccable (UX) review.
- **Real-browser Playwright layout task** (AGENTS.md mandate): flex `h-full` suppresses `align-items:stretch`
  (split columns weren't equal-height); `auto` grid tracks misaligned the ShowsTable header vs rows → explicit
  `ROW_GRID` track widths. jsdom structurally cannot catch these.

## §5 Notes / known items (non-blocking)
- **Runner is Vitest, not Jest** — plan wording corrected (`pnpm test` / `pnpm test <path>`).
- **90 pre-existing lint errors** in unrelated files — pre-date Phase A; logged to BACKLOG, not a Phase-A regression.
- **`supabase start` blocker (pre-existing M12.1, not Phase A):** migration `20260527000003_schedule_cron_jobs.sql`
  `raise exception` unless `app.fxav_vercel_url` GUC set. Local fix (CI-blessed per `.github/workflows/screenshots-drift.yml`):
  stash that migration → `supabase start` → set the GUC as `supabase_admin` per-DB default → restore → `supabase
  migration up --include-all` → `pnpm db:seed`.
- **CI parity:** all gates ran locally; the merge PR triggers CI (the separate real-CI gate). Confirm the new
  `admin-layout-dimensions` Playwright e2e is wired into CI or note it as local-only coverage.

## §6 Phase B fold-ins (in `DEFERRED.md`)
- **DEF-1** — rotate/reset RPC server-side `published && !archived` / `FINALIZE_OWNED_SHOW` gate.
- **DEF-2** — archived apply/discard semantics + server guard.
- Plus already-deferred: archived dashboard bucket + archive/unarchive action, persistent nav + mobile tabs +
  user menu, settings rebuild, revoke-hang fix, global AlertBanner placement/density/per-show suppression.
Both DEF-1/DEF-2 are **pre-existing backend gaps, not Phase-A regressions** (Phase A is no-DB-writes; shipped UI
mitigations only).

## §7 Close-out
- [ ] PR `m12.2-phase-a-admin-redesign → main`; merge on CI green.
- [ ] Validation-deploy eyeball of the redesigned admin before M13 launch (M12-walk surface).
- [x] Phase-B fold-ins captured in `DEFERRED.md` (DEF-1/DEF-2).
- Mark CLOSED here after merge.
