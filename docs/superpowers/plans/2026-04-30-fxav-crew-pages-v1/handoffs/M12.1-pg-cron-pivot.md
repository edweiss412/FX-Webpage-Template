# Handoff — M12.1 pg_cron sub-amendment to Solo-Dev UX Validation

**Status:** DRAFT (R0) — pending self-review + adversarial review.
**Spec:** `docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot-design.md`
**Plan:** `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/`
**Parent milestone:** M12 (R70 APPROVED 2026-05-26 @ `ac752d9`)
**Base SHA:** `ac752d9`

---

## §1 — One-paragraph context

M12's Phase 0.A executor surfaced a blocker at Task 0.A.4: `vercel deploy --prod` rejected because `vercel.json` declares 5 sub-daily crons that Vercel's Hobby tier does not allow. Three resolution paths were triaged (upgrade to Pro, move to existing Pro team, strip crons + amendment cycle); a fourth path emerged in orchestrator deliberation — pivot the production cron architecture from Vercel Cron to Supabase `pg_cron` + `pg_net`. The dev authorized Path D; M12.1 is the sub-amendment that ratifies and lands the pivot.

## §2 — Why this is a sub-amendment, not an M12 R71+ round

- **M12 R70 APPROVED** at `ac752d9` (close-out commit). Re-opening M12 to absorb the cron pivot would invalidate that APPROVE state and risk relitigation of the 20 class shapes closed in R6-R70.
- **Sub-amendment dir** is the cleaner pattern: M12.1 lives at `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/` with its own spec, plan, handoff, and convergence log. M12 R70 stays the historical APPROVE marker.
- **Phase 0.A executor remains paused** at the blocker. Once M12.1 executes (5 tasks, 6 commits expected), Phase 0.A resumes at Task 0.A.4 retry.

## §3 — Scope (binding contract between orchestrator + implementer)

Read sub-amendment spec §2.1, §2.2, §2.3 for the spec deltas. Read plan `00-overview.md` + `01-pivot-tasks.md` for the task-level breakdown. Headline:

- 5 tasks (T1-T5), 6 commits expected
- No UI surface, no route-handler code changes
- 2 new migrations (`enable_pg_net`, `cron_secret_vault`) + 1 new migration (`schedule_cron_jobs`)
- 1 config edit (`vercel.json`)
- 1 M12 plan edit (`01-phase0-infra.md` — insert Task 0.A.4.5, update Task 0.A.5)
- 2 new structural meta-tests (`no-vercel-cron`, `pg-cron-coverage`) + 1 canonical job-table JSON (`pg-cron-jobs.json`)

## §4 — Convergence log

This section grows during the adversarial-review loop. Initial state: empty.

| Round | Date | Reviewer | Verdict | Findings (count) | Repair commit(s) | Same-vector status | Notes |
|---|---|---|---|---|---|---|---|
| R0 | 2026-05-26 | this Opus session (orchestrator drafting) | DRAFT | — | `001c8e4` (R0 single-commit drafts) | — | Pre-codex-review state |
| R1 | 2026-05-27 01:25 UTC | codex adversarial-review (job `review-mpndtsfp-i9j2hj`) | needs-attention | 3 (1 HIGH + 2 MED) | _pending repair commit_ | — | **F1 (HIGH, conf 0.97):** T3 SQL used `net.http_post()` but all 7 handlers export `GET` only at HEAD `001c8e4` (cited `app/api/cron/{sync,keepalive,refresh-watch,gc-watch,asset-recovery,diagram-gc,report-reaper}/route.ts` — verified via grep, 7 GET exports, zero POST exports). Runtime blocker. Repair: `http_post` → `http_get` throughout spec §2.3 + §3 + §7 + plan T3 SQL + T4 meta-test (asserts `command contains 'net.http_get('` AND does NOT contain `'net.http_post('`). **F2 (MED, conf 0.95):** pre-existing `tests/api/vercel-crons.test.ts:7-21` asserts vercel.json HAS the M6-era crons block — T1 removal inverts the premise. Repair: retire the test in the T1 commit (`git rm`); T4's `no-vercel-cron.test.ts` is the new inverse contract. **F3 (MED, conf 0.9):** `05-phase0-smokes.md:33,36` references Vercel Cron observability + `vercel.json` cron presence; class-sweep also found `01-phase0-infra.md:75` ("Vercel Cron Jobs run only on production deployments"). Repair: T5 expanded to amend both files. |
| R1-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `4bc8539` | F1 single-instance closed (T4 inverse-pin); F2 single-instance closed (git rm); F3 + class-sweep 4-site sweep closed (T5 covers all) | Single repair commit covers all 3 findings + class-sweep verification |
| R2 | 2026-05-27 | codex adversarial-review (post R1-fix) | needs-attention | 3 (2 HIGH + 1 MED) | _pending repair commit_ | **Same-vector (API surface verification): 2nd recurrence.** R1 F1 (http verb) and R2 F4 (Vault schema name) + R2 F5 (cron.job_run_details column shape) are all the same class — "verify the named API against the actual implementation, not what you remember." If R3 hits this class again, comprehensive re-analysis + structural-defense calibration triggers per AGENTS.md. | **F4 (HIGH, conf ~0.95):** Vault schema is `vault` NOT `supabase_vault` (the extension NAME is `supabase_vault` but its SQL surface lives in `vault`). T2.2 migration + T3 migration + meta-test all referenced `supabase_vault.*` and would fail with "function/relation does not exist". Repair: sweep `supabase_vault.*` → `vault.*` across 13 sites (T2.2 SQL × 2, T3 SQL × 7, T4 meta-test × 1, spec §2.3 × 2, T5 step × 1) + add defensive `create extension if not exists supabase_vault with schema vault;` to T2.2 + correct verification step (`\dn vault` not `\dn supabase_vault`). **F5 (HIGH):** Smoke 3 observability used non-existent columns (`last_start_time`, `last_finish_time`, `jobname` directly on `cron.job_run_details` — actual columns are `start_time, end_time, status, return_message`; jobname requires joining `cron.job`) AND failed to acknowledge pg_net async semantics (`status='succeeded'` proves only enqueue success, not HTTP outcome). Repair: rewrite Smoke 3 with 3-layer observability stack (pg_cron firing → net._http_response HTTP outcome → downstream side effect as binding proof) + correct column names + correct cron.job join. **F6 (MED):** spec §5.1 completeness matrix still says "POST to" + "net.http_post" for all 7 rows — R1-fix incomplete sweep; the matrix is binding (self-review §5.11 cited consistency, T3 commit text cites the matrix). Repair: rewrite all 7 matrix rows to `net.http_get` GET + `(exports GET)` annotation + add explicit pre-commit grep check to §5.11. |
| R2-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `ff18ef1` | All 3 R2 findings closed; class-sweep finds 13 supabase_vault sites + 7 matrix rows + 2 smoke query blocks; structural-defense calibration extends T4 meta-test (vault.* assertion) + adds §5.11 mandatory pre-commit grep check | Single repair commit covers all 3 findings + comprehensive sweep + structural defense |
| R3 | 2026-05-27 | codex adversarial-review (post R2-fix) | needs-attention | 2 (1 HIGH + 1 MED) | _pending repair commit_ | **Same-vector (API surface verification): 3rd recurrence — AGENTS.md threshold crossed.** Comprehensive re-analysis MANDATORY this round; structural defenses ship in R3-fix per "structural-defense calibration." | **F7 (HIGH):** spec §2.2 line 83 amended-smoke-text retained the R0/R1 stale query `select * from cron.job_run_details where jobname = 'fxav_cron_sync'`. R2-fix updated the plan T5 step 4 query (joined form) but missed sweeping back to spec §2.2 — R1-fix incomplete-sweep recurrence. Repair: rewrite spec §2.2 query to the joined `cron.job_run_details jrd join cron.job j on j.jobid = jrd.jobid` form. **F8 (MED):** T4 anti-tautology procedure used `supabase db push` to "re-apply" an edited migration; per Supabase CLI docs, db push applies pending migrations only — already-tracked migrations are NOT re-run. The negative-regression check is a no-op. Repair: rewrite procedure to use `cron.unschedule('fxav_cron_sync')` for live-state mutation (or `supabase db reset` as heavyweight alternative). **Comprehensive re-analysis output:** new spec §4.5 "API surface citation registry" — 16 rows enumerating every external API surface named in M12.1 with canonical docs URL + verified signature/columns. **Structural-defense calibration:** new T4.3 prose-guard test `tests/cross-cutting/pg-cron-pivot-doc-guard.test.ts` asserts 5 forbidden patterns (capturing all R1/R2/R3 finding classes) do not appear in M12.1 docs outside allowlisted finding-history contexts. |
| R3-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `a2c88e8` | All R3 findings closed; comprehensive re-analysis spec §4.5 (16 API surfaces enumerated); structural defense T4.3 prose-guard test pins the class | Single repair commit covers F7 + F8 + §4.5 comprehensive registry + T4.3 prose-guard structural defense |
| R4 | 2026-05-27 | codex adversarial-review (post R3-fix) | needs-attention | 2 (1 HIGH + 1 MED) | _pending repair commit_ | **Mixed: R3 structural defense WORKED for the original API-verification class (no regression of R1/R2/R3 findings) but found 2 NEW class shapes** | **F9 (HIGH):** T4.3 prose-guard regex `db push.*re-?apply` is too broad — flags legitimate Task 0.A.4.5 retry text ("Re-run `npx supabase db push` to re-apply `schedule_cron_jobs`"). Self-inconsistent structural defense. Repair: narrow regex to `db push[^.]{0,150}expect[^.]{0,50}(FAIL|fail)` matching the canonical negative-regression assertion shape only. **F10 (MED):** PostgreSQL LIKE wildcard semantics — `'fxav_cron_%'` has `_` as single-char wildcard. T3 cron.unschedule predicate + 11 other LIKE sites would match e.g. `fxavXcronY...` and accidentally unschedule a future non-M12 cron. Repair: sweep all 12 sites to use escape-clause form `like 'fxav\_cron\_%' escape '\'`. **Class classification:** F10 IS arguably API/syntax-verification class (PostgreSQL LIKE semantics), so this is technically a 4th recurrence — but the structural-defense regex set didn't include LIKE-wildcards. R4-fix extends the prose-guard to a 6th pattern + adds PostgreSQL LIKE row to §4.5 registry. |
| R4-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `7987f81` | F9 narrowed regex + F10 12-site sweep + 6th prose-guard pattern + §4.5 registry row added; structural defense calibration extended | Single repair commit covers both findings + class-sweep + defense extension |
| R5 | 2026-05-27 | codex adversarial-review (post R4-fix) | needs-attention | 3 (2 HIGH + 1 MED) | _pending repair commit_ | **API-verification class did NOT recur** — R3+R4 structural defenses holding. 3 NEW class shapes surfaced: CI wiring + Vault bootstrap semantics + Vercel URL-alias semantics. Two of the three (F12 Vault, F13 Vercel) are operational-semantics-verification — same broad class as API-verification but in a different operational-layer; the prose-guard wouldn't catch them. F11 (CI wiring) is a structural-defense scoping miss (defenses authored but not gated). | **F11 (HIGH):** T4 creates 3 test files but doesn't wire them into CI; `pnpm test:audit:xN` jobs in `.github/workflows/x-audits.yml` are the gating pattern; new tests would be green locally but never gate PRs. Repair: T4.4 step adds new `test:audit:x6-pg-cron-pivot` script + new CI job following x1-x5 pattern; pg-cron-coverage marked local-only (live DB needed); no-vercel-cron + pg-cron-pivot-doc-guard gated in CI. **F12 (HIGH):** PostgreSQL's `CREATE EXTENSION ... WITH SCHEMA` requires the schema to ALREADY EXIST (per PG docs); the T2.2 single-statement `create extension if not exists supabase_vault with schema vault;` cannot bootstrap a fresh environment without vault schema. Repair: two-statement form — `create schema if not exists vault; create extension if not exists supabase_vault with schema vault;`. **F13 (MED):** T3 bakes `app.fxav_vercel_url` into `cron.job.command` at migration time; if operator captures per-deployment URL (`<project>-<hash>-<team>.vercel.app` output by `vercel deploy`) instead of stable project alias (`<project-name>.vercel.app`), cron keeps calling the OLD deployment after every redeploy. Repair: Task 0.A.4 step 4 explicit "stable project alias NOT per-deployment URL"; Task 0.A.4.5 step 2 GUC set to alias; new Task 0.A.5 step 6a verifies baked `cron.job.command` URLs match stable alias. |
| R5-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `7f4a059` | F11 CI wiring (package.json + .github/workflows + local-only marker) + F12 Vault schema bootstrap + F13 stable-alias contract; §4.5 registry +3 rows (now 20 total) | Single repair commit covers all 3 findings + class-sweep + 3 §4.5 rows |
| R6 | 2026-05-27 | codex adversarial-review (post R5-fix) | needs-attention | 3 (1 HIGH + 2 MED) | _pending repair commit_ | **F14 = API-verification class recurrence** (escape clause syntax, introduced in R5-fix new SQL). **F15 = self-inconsistent structural defense** (no-vercel-cron + pg-cron-pivot-doc-guard scan tests/ for forbidden literals but the test files themselves contain those literals as regex definitions). **F16 = operational-semantics** (pg_net 30s timeout shorter than sync route runtime). | **F14 (HIGH):** Task 0.A.5 step 6a verification query used `escape '\\'` (double backslash) — R5-fix new SQL that didn't follow the R4-fix established single-backslash form. PG parses `'\\'` as 2-char string; ESCAPE requires single char; query errors. Repair: replace `\\` → `\` in step 6a; SQL now matches all 11 other LIKE escape-clause sites. Add 7th §4.5 row documenting PG ESCAPE single-char contract. **F15 (MED):** the planned no-vercel-cron + pg-cron-pivot-doc-guard tests scan tests/ for forbidden literals but each test file MUST contain those literals as regex definitions; a naive implementation that scans itself fails itself. Repair: T4.1 step 1 + T4.3 step 2 explicitly require self-exclusion via `if (filePath === __filename) continue;` skip OR `SELF_EXEMPT` constant; anti-tautology assertion pins exactly-one-file exemption. **F16 (MED):** pg_net `timeout_milliseconds := 30000` is shorter than `runScheduledCronSync`'s plausible runtime (sequential per-show + per-file processing with per-file 30s step timeouts at lib/sync/runScheduledCronSync.ts; a multi-file watched folder can exceed 30s easily). Premature pg_net timeout would set `net._http_response.timed_out=true` for a still-running Vercel handler. Repair: bump all 7 timeouts to 300000ms (5 min, matches Vercel Functions' default `maxDuration` per session-context hook); document the rationale in T3 SQL comments + spec §7 + §4.5 row. |
| R6-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `4665a6b` | F14 escape-clause fix + F15 self-exclusion + F16 timeout bump; §4.5 registry +2 rows (now 22 total) | Single repair commit covers all 3 findings |
| R7 | 2026-05-27 | codex adversarial-review (post R6-fix) | needs-attention | 3 (1 HIGH + 2 MED) | _pending repair commit_ | F17 = NEW TDD discipline class (invariant 1 gap). F18 = same-class as R6 F14 (structural defense didn't pin the inverse-escape form). F19 = same-class as R5-R6 incomplete-sweep (Smoke 3 diagnostic prose stale after R6 F16 numeric change). | **F17 (HIGH):** T2.1, T2.2, T5 started with implementation instead of failing test → AGENTS.md invariant 1 violation. Repair: each task gets a Step 0 TDD-red verification — T2.1 cites pg-cron-coverage Layer 0a (`pg_net not yet installed`); T2.2 cites Layer 0b (`vault.secrets entry doesn't exist`); T5 cites pg-cron-pivot-doc-guard assertions A-D (Task 0.A.4.5 heading, CRON_SECRET in 0.A.5 step 3, "Vercel Cron Jobs" NOT in 0.A.4 Step 5, joined-query pattern). T4.2 + T4.3 extended with those positive/negative assertions. **F18 (MED):** T4.3 doc-guard had no inverse-pin for `escape '\\'` double-backslash; the R6 F14 fix swept the SQL but didn't add the structural defense. Repair: add 7th forbidden pattern to T4.3 catching `escape '\\\\'` (4-backslash markdown source = 2-backslash SQL); negative-regression in Step 4 now covers 7 patterns. **F19 (MED):** Smoke 3 diagnostic prose at lines 610-638 still said "30s pg_net timeout" / "handler took >30s" after R6 F16 fixed the T3 SQL to 300s. Repair: 3 prose updates (300s/5min consistent); new §4.5 row for "timeout-prose consistency sweep" so future timeout changes can't leave stale prose. |
| R7-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `06aeb16` | F17 TDD steps + F18 7th forbidden pattern + F19 timeout-prose sweep; §4.5 registry +3 rows (now 25 total) | Single repair commit covers all 3 findings + 3 §4.5 rows |
| R8 | 2026-05-27 | codex adversarial-review (post R7-fix) | needs-attention | 3 (1 HIGH + 2 MED) | _pending repair commit_ | F20 = TDD-coverage gap (same class as R7 F17 — T5's smokes file edit not covered by TDD-red assertions). F21 = operational-semantics (pg_net request_id correlation gap). F22 = local-passes-CI-fails class (new CI gate without dispatch verification path). | **F20 (HIGH):** T5 modifies BOTH `01-phase0-infra.md` AND `05-phase0-smokes.md`, but the T5 Step 0 TDD-red assertions only covered the first file. A developer could skip the smokes edit and TDD-red would still pass. Repair: extend pg-cron-pivot-doc-guard with 5 new assertions (E-I) covering `05-phase0-smokes.md` — 3-layer observability stack, joined query, net._http_response, 300s prose, no stale Vercel-Cron references. T5 Step 0 + T4.3 Step 1a updated to 9 total assertions (A-I across 2 files). **F21 (MED):** pg_net responses can't be correlated to specific cron jobs — `net._http_response` has no URL/jobname column; querying it alone risks misattribution when multiple cron jobs fire in adjacent intervals. Repair: Smoke 3 Layer 2 query now JOINs `net._http_response.id = net.http_request_queue.id` to recover the URL per request_id + filter by URL pattern. Layer 2 diagnostic updated. **F22 (MED):** new CI gate (audit-x6-pg-cron-pivot) added without `workflow_dispatch:` trigger — operator can't on-demand-verify it actually runs in CI before merge. Per AGENTS.md cross-cutting #4 "local-passes-CI-fails." Repair: T4.4 step 2a adds workflow_dispatch trigger; T4.4 step 3a verifies via `gh workflow run x-audits.yml --ref <branch>`. |
| R8-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `9ef95ed` | F20 doc-guard +5 assertions (4 → 9 total) + F21 pg_net JOIN correlation + F22 workflow_dispatch + dispatch verification; §4.5 registry +2 rows (now 27 total) | Single repair commit covers all 3 findings + 2 §4.5 rows |
| R9 | 2026-05-27 | codex adversarial-review (post R8-fix) | needs-attention | 2 (1 HIGH + 1 MED) | _pending repair commit_ | F23 = sequencing bug introduced in R7-R8 (T4 commits doc-guard with A-I assertions that only T5 makes pass → T4-commit CI fails). F24 = pg_net `http_request_queue` deleted on execution (per Supabase docs); R8 F21 JOIN approach returns zero rows for successful requests. | **F23 (HIGH):** R7 F17 + R8 F20 added 9 positive A-I assertions to pg-cron-pivot-doc-guard.test.ts (T4's test file). But T4 commits BEFORE T5; landing T4 puts those assertions in CI before T5 edits the M12 plan tree — assertions fail at T4 commit boundary, breaking the new x6 audit immediately. Repair: split assertion ownership — T5 ships its own dedicated test file `tests/cross-cutting/m12-plan-pg-cron-pivot-amendment.test.ts` containing A-I; T5 commit atomically includes test file + M12 plan edits + package.json x6 script extension. T4 pg-cron-pivot-doc-guard keeps ONLY 7 forbidden-pattern walks (CI-green at T4 commit boundary). **F24 (MED):** R8 F21 fix joined `net._http_response.id = net.http_request_queue.id` to recover URL per response. Per Supabase pg_net docs: "waiting requests are stored in net.http_request_queue and deleted upon execution." Post-execution the queue row is gone — JOIN returns zero rows for successful requests, masking real success as fake "no response." Repair: Smoke 3 Layer 2 uses TWO queries (response inspection + cron.job_run_details with baked-in URL via T3 format()) cross-referenced by timestamp proximity. Durable audit-table approach deferred to BACKLOG.md. |
| R9-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `dda3e42` | F23 T5-owns-its-own-test-file split (commit boundary correction) + F24 timestamp-proximity correlation (queue table deletion semantics); §4.5 row updated for F24 | Single repair commit covers both findings |
| R10 | 2026-05-27 | codex adversarial-review (post R9-fix) | needs-attention | 3 (1 HIGH + 2 MED) | _pending repair commit_ | F25 = TDD-ownership at T2.1/T2.2 boundary (same sequencing class as R9 F23 but at different commit boundary). F26 = self-contradictory regex in T4.3 forbidden patterns (the R6 F18 lookahead used `escape '\\'` double-backslash but the correct form is single-backslash). F27 = Smoke 3 Layer 2 still positioned as pass gate despite R9 F24 documenting correlation limits. | **F25 (HIGH):** T2.1 + T2.2 said "extend pg-cron-coverage.test.ts authored fully in T4.2" but T2.1 commits BEFORE T4, so the migration ships without an atomic red/green test boundary. Repair: incremental ownership — T2.1 authors pg-cron-coverage skeleton + Layer 0a (commits with migration); T2.2 extends with Layer 0b (commits with migration); T3 extends with 7-job assertion (commits with migration); T4.2 finalizes (canonical JOB_TABLE + anti-tautology). Each commit owns its red/green boundary. **F26 (MED):** The 6th forbidden pattern's lookahead required `escape '\\'` (double-backslash) — but that's the BROKEN form caught by the 7th pattern. If implemented literally, the doc-guard would flag every correct `escape '\'` (single-backslash) as a violation OR force the broken form. Repair: rewrite 6th pattern's lookahead to require SINGLE backslash; add 3-state negative-regression (correct passes, unescaped fails, double-backslash fails). **F27 (MED):** R8/R9 Smoke 3 prose said "all 3 layers must agree before declaring Smoke 3 passing," but R9 F24 documented that pg_net correlation is fuzzy (queue table deletion + timestamp proximity). Repair: explicitly downgrade Layers 1 + 2 to DIAGNOSTIC ONLY; Layer 3 (downstream side effect — show appears in /admin Active Shows) is the SOLE BINDING PASS CRITERION. If Layer 3 succeeds, Smoke 3 passes regardless of Layer 1/2 ambiguity. |
| R10-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `a8fa698` | F25 incremental test ownership + F26 LIKE/ESCAPE regex contradiction + F27 Layer 3 sole binding pass; T2.1/T2.2/T3 commits now atomically include pg-cron-coverage extensions; T4.3 negative-regression expanded to 3-state verification | Single repair commit covers all 3 findings |
| R11 | 2026-05-27 | codex adversarial-review (post R10-fix) | needs-attention | 3 (1 HIGH + 2 MED) | _pending repair commit_ | F28 = API-verification class 8th hit (pg_net timeout_milliseconds may be ignored per Supabase docs). F29 = TDD red/green target divergence (red against validation, green against local). F30 = T5 sweep missed Phase 0.F failure-modes section. | **F28 (HIGH):** Spec §4.5 + T3 SQL + Smoke 3 prose treated `timeout_milliseconds := 300000` as enforced 300s timeout with `timed_out=true` as diagnostic signal. Per Supabase pg_net API ref at https://supabase.github.io/pg_net/api/, the parameter may be currently ignored. Repair: §4.5 row + T3 SQL comment + Smoke 3 prose mark timeout as forward-compatible HINT + `timed_out` as DIAGNOSTIC-ONLY (reinforces R10 F27 Layer 3 sole-pass). **F29 (MED):** T2.1 step 0 said red against validation Supabase project + green against local dev project. Different targets break TDD proof (red/green divergence could be environment-induced, not migration-induced). Repair: rewrite T2.1/T2.2/T3 step 0 to mandate SAME target (local Supabase dev via `npx supabase start`); validation project apply happens at Phase 0.A.4.5 (per T5 amendment). **F30 (MED):** T5 step 4 rewrote Smoke 3 lines 33+36 but `05-phase0-smokes.md` Phase 0.F failure-modes section still says "Smoke 3 (cron) doesn't fire = Vercel deployment is Preview" — stale post-pivot. Repair: T5 step 4a adds Phase 0.F failure-modes rewrite + step 5a adds assertion J to doc-guard pinning the post-pivot 3-layer-ladder cause analysis. |
| R11-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `314471b` | F28 timeout-as-hint + F29 same-target TDD + F30 Phase 0.F failure-modes sweep; doc-guard +1 assertion (now 10: A-J); §4.5 row updated for F28 | Single repair commit covers all 3 findings |
| R12 | 2026-05-27 | codex adversarial-review (post R11-fix) | needs-attention | 2 (1 HIGH + 1 MED) | _pending repair commit_ | F31 = operational gap (validation apply doesn't run pg-cron-coverage meta-test). F32 = self-contradiction (00-overview DONE criterion vs R10 F27 Layer 3 sole-pass contract). | **F31 (HIGH):** Task 0.A.4.5 only re-ran db push + minimal sanity checks (jobname/schedule + non-fxav count) — didn't run pg-cron-coverage against the validation Supabase project. Validation-env drift (baked URL, vault access path, route URLs, net.http_post regression) could escape detection. Repair: Task 0.A.4.5 step 5a (new) runs `pnpm test tests/cross-cutting/pg-cron-coverage.test.ts` against validation env post-apply. Analog of cross-cutting #4 "local-passes-CI-fails" extended to DB envs. **F32 (MED):** 00-overview DONE criterion said "cron.job_run_details shows successful invocations" but R10 F27 + R11 F28 established that `succeeded` only proves SQL enqueue, NOT full HTTP/handler success. The DONE criterion could green-light a pivot with every cron HTTP call returning 401/network failure. Repair: rewrite 00-overview DONE gate — 3-part: (1) Phase 0.A.4 deploy green, (2) all structural meta-tests pass at validation env (per Task 0.A.4.5 step 5a + CI x6 audit), (3) Layer 3 downstream side effect observable (show appears in /admin Active Shows under real cron firing). Layers 1+2 explicit DIAGNOSTIC ONLY. |
| R12-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `a898dfa` | F31 Task 0.A.4.5 step 5a + F32 00-overview DONE 3-part criterion; §4.5 row added for validation-env meta-test apply discipline | Single repair commit covers both findings |
| R13 | 2026-05-27 | codex adversarial-review (post R12-fix) | needs-attention | 2 (1 HIGH + 1 MED) | _pending repair commit_ | F33 = doc-guard regex too broad (4th hit of "structural defense over-triggers" class: R4 F9 + R6 F15 + R10 F26 + R13 F33). F34 = TEST_DATABASE_URL convention not specified for validation-env meta-test apply. | **F33 (HIGH):** Doc-guard `net\.http_post` + `supabase_vault\.*` patterns fire on bare prose mentions in inverse assertions, diagnostic drift checks, contract-NOT-contains text — not just SQL-call contexts. CI gate would fail on the plan as-written. Repair: refined regex requires `\(` open-paren (function-call context only); bare prose mentions are allowed. T4.3 Step 4 expanded with bidirectional regression (every pattern has both negative + positive test cases) as structural-defense calibration for the "doc-guard too broad" class which has now recurred 4 times. **F34 (MED):** R12 F31 fix said "point .env.local at validation" but Vitest does NOT auto-load .env.local; existing live-DB tests use TEST_DATABASE_URL with localhost fallback. Operator could think they ran validation gate while silently hitting local Supabase. Repair: explicit `TEST_DATABASE_URL=<pooler-URL> pnpm test ...` command + in-test guard rejecting localhost/127.0.0.1/:54322 targets + requiring validation project ref. |
| R13-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `44740ee` | F33 doc-guard regex narrowing + bidirectional regression (14+ test cases) + F34 TEST_DATABASE_URL command + test-side localhost guard; §4.5 row updated for F34 + new row for bidirectional regression discipline | Single repair commit covers both findings + class-calibration |
| R14 | 2026-05-27 | codex adversarial-review (post R13-fix) | needs-attention | 1 (HIGH) | _pending repair commit_ | **Finding count DROP — only 1 finding (was 2-3/round across R1-R13).** F35 = 5th hit of structural-defense-self-inconsistency class: the R13-fix bidirectional regression EXAMPLES I added inline in plan markdown trigger the doc-guard against itself. | **F35 (HIGH):** R13-fix added paired regression-case examples (Pattern 1 negative: `supabase_vault.create_secret(...)`, Pattern 2 negative: `net.http_post(...)`, etc.) inside the T4.3 Step 4 markdown. The plan markdown is in the doc-guard's walked surface; the literal forbidden-pattern fixtures fire the doc-guard against the plan itself. 5th hit of "structural defense over-triggers / self-inconsistency" class (R4 F9, R6 F15, R10 F26, R13 F33, R14 F35 — each R-fix created or revealed a new instance). Repair: move ALL regression fixtures to the self-excluded test file `tests/cross-cutting/pg-cron-pivot-doc-guard.test.ts` (where literal patterns are intentional as regex source). Plan markdown describes the discipline + named patterns semantically (no literal mentions of the forbidden patterns). Class comprehensive-defense closure: pattern definitions + fixtures consolidated into self-excluded test file; plan describes contract, test file owns literals. |
| R14-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `fda608b` | F35 fixture-relocation (literal forbidden patterns moved from plan markdown to self-excluded test file) + final sweep of inline forbidden-literal mentions in T4.4 commit-message description | Single repair commit covers F35 + class closure |
| R15 | 2026-05-27 | codex adversarial-review (post R14-fix) | needs-attention | 1 (HIGH) | _pending repair commit_ | F36 = 6th hit of structural-defense-self-inconsistency class. R14 F35 fix relocated the R13 F33 / F35 inline examples but missed the EARLIER R10 F26 3-state regression block (3 literal fixtures: correct/unescaped/double-backslash) that was inlined in T4.3 step 1. **Trend signal:** R14 + R15 both returned 1 finding (was 2-3 across R1-R13). Same-class repeated but counts dropping. | **F36 (HIGH):** T4.3 step 1 patterns-table at lines ~455-490 still embedded the R10 F26 3-state literal regression fixtures (correct single-backslash escape + unescaped LIKE + double-backslash escape) directly in plan markdown. R14's class-comprehensive-defense intent — "all fixtures in self-excluded test file" — was incompletely applied; R10 F26 inline cases were missed. Repair: remove the 3-state literal fixtures from plan markdown; replace with semantic state-inventory (state A/B/C descriptions without literal SQL). Test file owns the 3 fixture literals as test cases. |
| R15-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `fec26e6` | F36 complete relocation of R10 F26 3-state regression cases from plan markdown to test file | Single repair commit covers F36 |
| R16 | 2026-05-27 | codex adversarial-review (post R15-fix) | needs-attention | 1 (HIGH) | _pending repair commit_ | F37 = self-contradiction between R11 F29 (T2/T3 local red/green) and R13 F34 (validation-only test guard). Single test file can't both error-on-not-validation AND be runnable locally for TDD. Class: validation-vs-local mode contradiction. **Trend signal extended:** R14, R15, R16 all returned 1 finding (down from R1-R13's 2-3/round). | **F37 (HIGH):** R13 F34 added test-side guard erroring if TEST_DATABASE_URL targets local. R11 F29 required T2/T3 red/green against local DB. Same test can't satisfy both unconditionally. Repair: mode-gated env var `PG_CRON_COVERAGE_TARGET={local,validation}` (default `local`). Mode `local` skips URL-shape guard (T2/T3 local TDD); mode `validation` (set at Task 0.A.4.5 step 5a) enables the guard. Both modes run same Layer 0a/0b/7-job assertions. |
| R16-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `32eb6f6` | F37 mode-gated test target via PG_CRON_COVERAGE_TARGET env var; T2/T3 local invocations preserved; Task 0.A.4.5 step 5a explicit validation-mode invocation; §4.5 row updated | Single repair commit covers F37 |
| R17 | 2026-05-27 | codex adversarial-review (post R16-fix) | needs-attention | 1 (HIGH) | _pending repair commit_ | F38 = validation-mode project-ref guard underspecified (no env-var source for the expected ref); could silently pass against wrong remote project. **Trend signal extended:** R14-R17 = 4 consecutive rounds at 1 finding each. | **F38 (HIGH):** R13 F34 said validation guard "asserts TEST_DATABASE_URL contains validation project ref" but didn't specify how the test gets the ref. Without a source, the guard could only check "not local" — silently allowing a different remote Supabase to pass while real validation project goes unverified. Repair: add `VALIDATION_SUPABASE_PROJECT_REF` env var (already in §9.1.2 canonical contract) to the validation-mode command; test asserts all 4 conditions (TARGET=validation + TEST_DATABASE_URL set + not-local + project-ref env-var set + URL contains that ref). Closes silent-wrong-remote class. |
| R17-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `7090f25` | F38 VALIDATION_SUPABASE_PROJECT_REF env var added to validation-mode command + 4-guard assertion; §4.5 row updated | Single repair commit covers F38 |
| R18 | 2026-05-27 | codex adversarial-review (post R17-fix) | needs-attention | 2 (1 HIGH + 1 MED) | _pending repair commit_ | F39 = parent M12 spec amendments declared in M12.1 spec §2.1/§2.2/§2.3 never scheduled for execution. F40 = baked URL verification check uses impossible exact-match against alias when actual URLs include scheme + path. | **F39 (HIGH):** M12.1 spec declared 3 amendments to the parent M12 spec (§2.1 / §2.2 / §2.3 = §9.1 row + §9.2 smoke 3 + new §9.1.3) but T5's file-modification list only included 2 M12 plan files; the M12 spec itself never got scheduled. Implementing T5 as written leaves M12 spec with stale Vercel Cron contract while plan + runtime pivot. Repair: T5 step 4b (new) explicitly amends `2026-05-19-solo-dev-ux-validation-design.md` for the 3 spec changes; doc-guard assertions K + L + M added pinning the spec edits. **F40 (MED):** Task 0.A.5 step 6a verification said "every baked_url matches the stable project alias <project-name>.vercel.app exactly" — but baked URLs include scheme + path (`https://<alias>/api/cron/<route>`), so exact-match is impossible. Repair: per-row 2-part comparison — (1) scheme + host portion equals stable alias, (2) route path matches jobname per pg-cron-jobs.json. |
| R18-fix | 2026-05-27 | this Opus session (orchestrator repair commit) | — | — | `cc59b13` | F39 T5 schedules M12 spec amendments + 3 new doc-guard assertions (K/L/M) (doc-guard now 13 positive assertions A-M across 3 files); F40 baked URL check rewritten as 2-part host+route comparison | Single repair commit covers both findings |
| R19 | _pending_ | codex adversarial-review (fresh-eyes) | _pending_ | _pending_ | _pending_ | _pending_ | Re-fire post R18-fix; cumulative R1-R18 = 40 findings closed |

(Continues until APPROVE.)

## §5 — Reads list (for downstream implementer once APPROVED)

1. Sub-amendment spec `docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot-design.md` — all sections
2. Plan `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/00-overview.md` — task index, convergence approach
3. Plan `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/01-pivot-tasks.md` — TDD task checklists
4. This handoff §6 (Watchpoints) + §7 (Do-not-relitigate)
5. M12 spec §9.1 + §9.1.2 + §9.2 — the contract surface M12.1 amends

## §6 — Watchpoints

Watch for during self-review + adversarial review:

- **Schedule equivalence drift.** The 7 `cron.schedule()` calls in T3 MUST match `vercel.json`'s schedules at HEAD `ac752d9` byte-for-byte. Numeric sweep per AGENTS.md spec-self-review additions. The T4 meta-test pins this contractually.
- **Bootstrap signing-key cron preservation.** The pre-existing cron at `supabase/migrations/20260504000001_bootstrap_nonces_signing_key.sql:36` is untouched; T3's `cron.unschedule()` is scoped to `jobname like 'fxav_cron_%'`. If a reviewer claims the bootstrap cron is at risk, cite the LIKE clause + the T4 meta-test assertion 4 (non-fxav cron count > 0).
- **Vault secret rotation lifecycle.** Value is populated post-migration (Phase 0.A.5 for validation env; M13 launch task for prod env). Default placeholder forces 401 from /api/cron/* if not populated. If a reviewer asks "what if the secret is wrong?" — that's the fail-loud design intent, not a bug.
- **`app.fxav_vercel_url` GUC ergonomics.** Single operational pre-req before T3 migration applies. Alternative designs (env-var-templated migration; per-row constants) were considered and rejected for simplicity. If a reviewer pushes back on the GUC choice, surface this rationale (don't relitigate).
- **No handler code changes.** Verify `app/api/cron/*/route.ts` files are NOT in any commit's diff. Any reviewer claim that handler code "needs" modification is incorrect; cite `app/api/cron/_auth.ts:3-12` + the 7 handler verification points in spec §1.2.

## §7 — Do-not-relitigate

EXPLICITLY DO NOT RELITIGATE the following, ratified contracts (cite when reviewer pushes back):

- **pg_cron over GitHub Actions / Cloudflare / Upstash / cron-job.org.** Ratified in spec §1.1. Comparison table includes reliability, granularity, vendor surface, free-tier cost. The trade-off basis is documented; do not re-derive.
- **Vault over GUC for `CRON_SECRET` storage.** Ratified in spec §2.3. Rationale: encrypted at rest, rotation-friendly, isolated from `pg_settings` visibility. Reviewer arguing for GUC simplicity should be answered with "Vault chosen per spec §2.3 rationale; defer to BACKLOG if you want to revisit."
- **`vercel.json` crons block is REMOVED (not retained-with-comment, not retained-but-ignored).** Ratified in spec §2.1 + plan T1. Hobby tier rejects deployments declaring crons exceeding daily even when those crons would not fire. Removal is the only deploy-passing path.
- **No route-handler code changes.** Ratified by live-code citation in spec §1.2 + §4. All 7 handlers already accept `Authorization: Bearer $CRON_SECRET` via `_auth.ts`.
- **Sub-amendment dir vs M12 R71+ amendment.** Ratified by orchestrator-user decision 2026-05-26. M12 R70 APPROVE stays intact; M12.1 has its own loop.
- **Stop gates A+B+C (M12 amendment loop) do NOT carry forward.** M12.1 uses standard adversarial-review pattern per AGENTS.md routing.

## §8 — Reviewer-only mandate (every adversarial-review round)

**YOUR ROLE: REVIEWER ONLY.** Do not fix issues, propose patches as commits, or imply changes you will make. Your job is to challenge the implementation approach and surface findings — fixes are the implementer session's job in a separate dispatch. Per AGENTS.md "Cross-CLI orchestrator discipline" + memory `feedback_adversarial_review_runbook`.

## §9 — Expected convergence shape

- **Round count estimate:** 3-8 codex rounds (well-scoped, no UI surface, narrow citations, clear structural defenses). Standard adversarial-review pattern; iterate until APPROVE.
- **Commit count at convergence:** 6 code/migration/test/plan commits (T1, T2.1, T2.2, T3, T4, T5) + N repair commits across rounds + 1 close-out commit (this handoff sign-off).
- **Same-vector recurrence response:** if any class recurs 3+ rounds, comprehensive re-analysis per AGENTS.md `superpowers:writing-plans` discipline; structural-defense calibration in the round that converges (do not wait for another round to confirm).

## §10 — Phase 0.A executor unblock

Once M12.1 reaches APPROVE + the 6 commits land (including T5 which amends the M12 plan), Phase 0.A executor (the original Claude Code session paused at the Vercel deploy gate) resumes with:

1. **Task 0.A.4 retry** — `vercel deploy --prod --yes` succeeds (vercel.json crons block removed in T1); capture `*.vercel.app` URL
2. **Task 0.A.4.5 (added to M12 plan by M12.1 T5)** — populate Vault `fxav_cron_secret` value in the validation Supabase project + set `app.fxav_vercel_url` GUC + apply T2/T3 migrations (one-time DB ops via Supabase SQL editor + `npx supabase db push`; not a code commit)
3. **Task 0.A.5** — env-var wiring per M12 plan, now including `CRON_SECRET` in Vercel Production scope with byte-for-byte match against the Vault value populated in Task 0.A.4.5
4. **Task 0.A.6** — sign-in probe (unchanged) + **observability probe**: confirm at least one cron firing in `cron.job_run_details` for `fxav_cron_sync` (the */5 job; should fire within 5 min of Vault populate)
5. **Block 2 (M11.5 carryovers)** — unchanged per M12 plan

The Phase 0.A dispatch brief at `/tmp/phase0a-dispatch-brief.md` is refreshed post-M12.1 close-out for the new Task 0.A.4.5 + the `CRON_SECRET` addition to Task 0.A.5, but the brief is a session wrapper — the binding contract is the amended M12 plan file (per M12.1 T5).

## §11 — Future-orchestrator notes

If you are a future orchestrator session picking M12.1 cold:

- Convergence log (§4 above) is the single source of truth for round-by-round state
- The do-not-relitigate list (§7) and watchpoints (§6) are load-bearing; cite them when composing repair prompts to codex
- M12 R70 APPROVE state must be preserved; do NOT touch `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/**` files except via M12.1 sub-amendment scope
- The Drive SA key rotation (initiated by user 2026-05-26 in parallel with M12.1 drafting) is independent of M12.1; verify status from user signal before resuming Phase 0.A
