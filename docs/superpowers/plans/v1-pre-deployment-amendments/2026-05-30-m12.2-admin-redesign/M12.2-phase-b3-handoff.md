# Handoff — M12.2 Phase B3: Email-delivery notification subsystem + sync-notification toggles

**Branch:** `m12.2-phase-b3-email-delivery` (worktree `/Users/ericweiss/FX-Webpage-Template-b3`, rebased onto post-B2 `main`).
**Spec:** `docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-02-m12.2-phase-b3-email-delivery-design.md` (Codex-APPROVED R76).
**Plan:** `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b3-email-delivery.md` (Codex-APPROVED plan-R24).
**Execution model:** orchestrator-dispatched (Opus drives Codex backend phase-by-phase + Opus real-DB verify pass per phase; Phase 6 UI Opus-built + external impeccable dual-gate).

---

## 1. Status — ALL PHASES COMPLETE, milestone close-out pending

| Phase | Owner | Commits | Verify |
|---|---|---|---|
| 0 — alert producers + AdminAlertCode union | Codex | `bca306dd`→`fc14eb7d` (7) | 499 unit + tsc |
| 1 — migrations + 5 canonical registries | Codex + Opus | `99d4c5e1`/`d14c2a70`/`33ccb050` + `e40f3b08` | real-DB 131 + static 147 |
| 2 — `lib/notify` delivery infra | Opus (after Codex hang) | `9e7d74c1`→`4f531966` (9) | 48 notify |
| 3 — detection + delivery orchestration | Codex + Opus | chunk1 `b27b3395`→`fb1c19cf`+`9c1c6aac`; chunk2 `01fd09f9`→`58a739fa`+`b3f87f0a` | real-DB 664 |
| 4 — cron route + pg_cron | Codex | `a5b707eb`/`a084f087` | 27 + x6 35 |
| 6 — Preferences toggles (UI) | Opus | `cc406e54`/`72ecaf5b`/`13c644a6`/`a19df519`/`c96327f0` | 72 unit + e2e 7 |

Backend (0–4): Codex-implemented, Opus real-DB-verified. Opus verify passes caught **5 real-DB-only test bugs** Codex's network-blocked sandbox could not (production paths were always correct): shared-DB test isolation (×2), the JS-`Date` microsecond-truncation in a dedup_key, the `admin_alerts_one_unresolved_idx` collision, and two psql-introspection harness bugs.

---

## 2. Plan-wide invariants honored
RD-9 at-least-once + Resend Idempotency-Key sole dup guard; per-submission nonce reissue key (never attempt_count); ONLY Resend 200 records `sent`; maintenance always runs (toggle/provider-independent); microsecond occurrence-epoch keys computed IN SQL (never `Date.parse`); 3-way config-valid; absolute email links (never localhost); no raw codes / no `<…>` placeholders / no em dashes; invariant-9 call-boundary discipline (every notify boundary REGISTERED in `tests/notify/_metaInfraContract.test.ts` or inline `// not-subject-to-meta`); no per-show advisory lock in the notify job; AdminAlertCode union members co-land with write-site per `_metaAdminAlertCatalog:346`; pg_cron scoped unschedule (never `fxav_cron_%` sweep); UI = Opus + impeccable.

## 3. Test commands (real-DB needs the local DB up + `TEST_DATABASE_URL`)
- Backend: `pnpm vitest run tests/notify tests/sync tests/adminAlerts tests/messages/_metaAdminAlertCatalog.test.ts tests/cross-cutting/email-canonicalization.test.ts`
- DB-gated (set `TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres`): the `*.real-DB` blocks in `tests/notify/{candidates,recovery-resolution,email-delivery-failed-reconcile,digest,deliver}` + `tests/db/{b3-app-settings-columns,b3-email-deliveries-schema,notify-toggle-rls,b3-notify-cron-idempotency}`.
- Cross-cutting gates: `pnpm vitest run tests/cross-cutting/{traceability,codes,_canonicalEmailCheckContract,pg-cron-coverage,pg-cron-pivot-doc-guard,auth-chain-audit,no-raw-codes}.test.ts`; `pnpm test:audit:x2-no-raw-codes`; `pnpm test:audit:x6-pg-cron-pivot`; `tests/db/postgrest-dml-lockdown.test.ts`.
- UI: `pnpm vitest run tests/components/notify-toggles.test.tsx tests/admin/notify-toggle-actions.test.ts tests/app/admin/settingsHeader.test.tsx`; e2e `tests/e2e/notify-toggles.spec.ts` (Playwright, needs the :3000 dev server with `.env.local` present + `ENABLE_TEST_AUTH`).
- `pnpm tsc --noEmit`.

## 12. Impeccable dual-gate dispositions (invariant 8 — EXTERNAL attestation)

Both gates were run by FRESH external subagents (not the authoring session, per `feedback_impeccable_external_attestation_required`) on the Phase-6 UI diff (`components/admin/settings/NotifyToggle.tsx` + `app/admin/settings/page.tsx`, with `setAlertOnSyncProblems.ts`/`setDailyReviewDigest.ts` as the write path). 2026-06-03.

- **`/impeccable critique` → PASS.** No HIGH/CRITICAL. The two new Preferences rows (one parameterized `NotifyToggle` used twice) are a faithful, verbatim reuse of the verified `AutoPublishToggle` pattern: same Tailwind classes, the React-19 dispatch-safe `SwitchButton` (disables only on `useFormStatus().pending`), `role="switch"` + `aria-checked`/`aria-busy`/`aria-label`, focus-visible ring, fail-closed degraded path. No em dashes; copy is product-contract-fixed and on-brand; low cognitive load.
- **`/impeccable audit` → PASS.** No HIGH/CRITICAL. Verbatim a11y/responsive/theming mirror: correct ARIA switch semantics, all color tokens resolve with dark-mode + high-contrast variants, motion respects `prefers-reduced-motion`, `min-w-0` text column + `shrink-0` toggle.

**Disposition of LOW notes (none are gate findings; all accepted, no land-now fix):**
1. *"hidden input" docstring (critique)* — MIS-CITE. `NotifyToggle` has no hidden-input reference (only `aria-hidden` on the knob span). No action.
2. *Preferences ordering (critique)* — sync-alert → digest → auto-publish (`page.tsx:94/103/112`) matches the plan ("render both above `AutoPublishToggle`"). Spec-correct. No action.
3. *28px (`h-7`) switch hit-area < WCAG 2.5.5 AAA 44px (audit, LOW)* — verbatim mirror of the already-shipped, already-impeccable-passed `AutoPublishToggle` (`components/admin/settings/AutoPublishToggle.tsx`, same line). Pre-existing pattern characteristic, NOT introduced by B3; changing only the notify rows would diverge from the verified pattern. **Accepted at the pattern level** (a future toggle-pattern-wide a11y pass is the right home, project-wide, not B3).
4. *No `<h2>`/landmark grouping the Preferences cluster (audit, LOW)* — pre-existing IA shape; `AutoPublishToggle` is also a bare `h3`-led sibling. Not introduced by B3. **Accepted as pre-existing.**

No UI mutation resulted from triage → no re-gate required.

## 13. Close-out

### 13a. Whole-milestone fresh-eyes adversarial review — ✅ APPROVED (Codex, 2026-06-03)
Converged after **4 rounds** (full B3 diff vs `530bb742`; fresh-eyes + REVIEWER-ONLY each round). Each round surfaced a genuine, distinct cross-phase integration issue the per-phase reviews missed; all repaired + Opus-real-DB-verified:
- **R1 (HIGH)** — digest delivery bypassed the `email_deliveries` ledger (`defaultDeliverDigest` called `sendEmail` directly; §6 reconciliation derives digest currentness from `kind='digest'` rows nothing wrote). FIXED `6c7c8d48`: shared per-recipient outcome-switch in `deliver.ts`; `deliverDigest` (kind=digest, dedup `digest:<dateET>`, show_id NULL, full idempotency + `EMAIL_DELIVERY_FAILED`); `runDigestNotify` wired to it.
- **R2-A (MEDIUM)** — a sent-race could raise a false `EMAIL_DELIVERY_FAILED`. FIXED `99d0535d`: `upsertFailed` returns whether its `WHERE status<>'sent'` write landed; alert + failed-count gated on it; zero-row conflict = sent-race skip.
- **R2-B (MEDIUM)** — `PARSE_ERROR_LAST_GOOD` double-emitted on the default manual path (`processOneFile_unlocked` + a redundant wrapper). FIXED `f5e4b4e8` (+ `8d78241b` real-DB test-harness `sync_log` fix): wrapper emit gated to injected-`processOneFile` test seams; production default path emits once via `processOneFile_unlocked`; real-DB verified `occurrence_count=1` for PARSE + DRIVE; anti-drift meta-test now encodes the producer topology (shared-delegated vs manual-pre-processing).
- **R3 (HIGH)** — the cron route returned HTTP 200 on infra failures (orchestrators record faults as data, never throw), so pg_cron/pg_net saw scheduler-level success. FIXED `23f75c5d`: route returns 500 when `delivery.kind==='infra_error'` OR any maintenance step is `infra_error` (full recorded result still in the body); deliberate skips (toggle off / config invalid / outside window) stay 200.
- **R4 — APPROVE** (no material findings). Final state: 206 test files / 1852 tests + tsc green; worktree clean.

### 13b. Remaining (NOT yet done)
- [ ] **Real-CI green** — the milestone adds CI surface (notify cron migration, the e2e, x6 timeout assertion). Real GitHub Actions green is a separate gate from local green (`feedback...local-passes-CI-fails`). The pg_cron migration applies against the validation project via `supabase db query --linked` (Phase-0 history divergence blocks `db push`).
- [ ] **PR + merge to `main`.**
- Screenshot baselines: NO regen needed — `/admin/settings` is not in the help-screenshot capture set (captured: `dashboard-overview`, `review-queues`).

## 14. Watchpoints (for the adversarial reviewer — do-not-relitigate)
Spec §13 contracts: RD-9 trim, per-submission nonce, microsecond-epoch keys, maintenance-always, toggle-aware EMAIL_* lifecycles, heartbeat naming (`sync_cron_heartbeat_at`, deliberately non-watermark-shaped), the registry-lockstep class. Plan execution invariants: AdminAlertCode union/write-site co-landing per `_metaAdminAlertCatalog:346` (codes distributed to producer commits); notify infra-contract skeleton-first + per-boundary registration; `resolveAdminAlert.ts` is a NEW throwing helper (single-valued, throws like `upsertAdminAlert:42`). NotifyToggle is a deliberate verbatim mirror of `AutoPublishToggle` (divergence is a finding, matching is correct).
