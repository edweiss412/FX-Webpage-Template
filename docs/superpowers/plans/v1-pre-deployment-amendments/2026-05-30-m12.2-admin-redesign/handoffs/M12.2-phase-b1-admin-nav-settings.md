# Handoff — M12.2 Phase B1: Admin nav shell + settings shell

**Handed off:** 2026-06-01 by Eric Weiss (orchestrator session)
**Implementer:** Opus / Claude Code (UI hard rule — every B1 file is `app/admin/**`, `components/**`, `lib/admin/**`, `lib/messages/**`, `lib/auth/requireAdmin.ts` (test-only hook), `lib/appSettings/`, `lib/drive/`, or `app/globals.css`). Single-implementer; no split.
**Adversarial reviewer (impl-diff):** Codex / codex-companion (per ROUTING.md UI-impl-diff convention).
**Spec:** `docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-31-m12.2-phase-b1-admin-nav-settings-design.md` (APPROVED — Codex 27 rounds + owner sign-off 2026-05-31; amendment 11 RATIFIED).
**Plan file:** `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b1-admin-nav-settings.md` (APPROVED — Codex 13 rounds, 2026-06-01). ~30 TDD tasks across 11 phases.

> **Read order before touching code:** (1) this handoff end-to-end, (2) the B1 spec §1–§11, (3) the plan's "File structure" + "Meta-test inventory" (incl. the **`_metaInfraContract` grep-shape recognition contract** and the **Fail-visible completeness matrix**) + Phase 0, then execute phase by phase. The plan has complete TDD steps with code; this handoff orients you and pre-loads the contracts the 13 review rounds settled so you don't re-derive (or regress) them.

---

## 1. Spec sections in scope

The B1 design spec is self-contained and authoritative; it supersedes master spec §9 on the surfaces below via **plan amendment 11** (`docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/00-overview.md`). Sections:

- B1 §1 — goal & scope (in-scope nav shell + settings shell; deferrals to B2/B3)
- B1 §2 — nav shell (§2.1 destinations, §2.2 modes/720px boundary, §2.3 UserMenu + identity/fault contract, §2.4 NotifBell fail-visible, §2.5 ThemeToggle reuse, §2.6 page-owned `<AdminPageHeader>`, §2.7 admin route-gate topology + error boundaries)
- B1 §3 — settings shell (§3.1 Drive-connection read-only health, §3.2 Administrators embed + self-revoke + revalidation, §3.3 build-gated Developer-tools row)
- B1 §4 — revoke-hang robustness fix
- B1 §5 — guard-condition matrix; §6 — dimensional invariants (Tailwind v4); §7 — transition inventory; §8/§8.1 — testing + meta-test inventory; §10 — resolved decisions; §11 — open questions
- Master spec §9 "Doug's admin UX" (`docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2500`, §9.1 `:2594`, §9.2 `:2625`) — **superseded by amendment 11 on layout/chrome + settings layout**. Crew model on these surfaces = the 2026-05-23 picker-pivot amendment, NOT the stale signed-link spec.

## 2. Acceptance criteria

The B1 amendment spec does not use master-spec `AC-n.m` IDs; its acceptance surface is the **§8 / §8.1 test list**, which the plan maps 1:1 to task steps. The implementer satisfies B1 by completing every plan task (each is failing-test → impl → passing-test → commit) AND the §8 test bullets they encode. The exit criteria (§8 below) are the checkable contract.

## 3. Spec amendments in scope

The three §13.2.3 report-pipeline amendments: **None apply** (B1 does not touch the report pipeline or reaper).

The applicable amendment is **plan amendment 11** (M12.2 Phase B1 admin nav + settings; RATIFIED 2026-05-31 in `00-overview.md`) — it supersedes master spec §9.1/§9.2 on nav/chrome + settings layout. Read it before touching `app/admin/layout.tsx`.

## 4. Pre-handoff state

- [x] Branch `m12.2-phase-b1-admin-nav-settings` checked out (45 commits ahead of `main`; all are docs/spec/plan — NO implementation yet).
- [x] Phase A (M12.2 dashboard + per-show reskin) MERGED to `main` (`b77d7c3`). The Phase-A primitives B1 builds on exist: `components/admin/StatusIndicator.tsx` (status `"live"|"positive"|"review"|"warn"|"idle"`), `components/admin/Dashboard.tsx` (`fetchDashboardData`, `ACTIVE_SHOWS_CAP=500` in `lib/admin/dashboardData.ts:38`), the status tokens in `app/globals.css:67-85`, `lib/admin/syncStatus.ts`, `components/shared/StaleFooter.tsx`.
- [x] Tests currently green on the branch (`pnpm vitest run`, `pnpm typecheck`). Run them first to confirm a clean baseline.
- [x] `supabase start` blocker workaround documented in `[[project_m12_2_phase_a_state]]` (migration `20260527000003_schedule_cron_jobs.sql` needs the `app.fxav_vercel_url` GUC; stash → start → set GUC → restore → `migration up`).
- **Runner is `vitest`, not jest** (plan steps say `pnpm vitest run <path>`).

## 5. Plan-wide invariants that apply (from AGENTS.md §1)

- [x] **TDD per task** (always). EVERY task is failing-test → minimal impl → passing-test → commit. **This was the #1 review vector (R1–R2) — no pass-only tasks.** Audit/coverage tasks are folded into the behavior task they validate; layout-dims / transition-audit / prod-runtime-flip carry an explicit run-→-expect-FAIL → fix → pass + a negative-regression "prove the assertion has teeth" step. Do not "simplify" any of these back to write-test-→-expect-PASS.
- [ ] Per-show advisory lock — **N/A** (B1 mutates nothing under lock; settings is read-only + the existing admin-allowlist RPCs).
- [ ] Email canonicalization at boundary — **N/A for new boundaries** (B1 adds no new email-from-external-source read; the embedded Administrators section reuses the existing `addAdminEmail`/`revokeAdminEmail`/`canonicalize` paths unchanged).
- [ ] No global cursor — **N/A** (B1 doesn't touch sync cursors).
- [x] **No raw error codes in UI (invariant 5)** — applies heavily. Every alert/health/error string flows through `lib/messages/lookup.ts`. B1 adds `getRequiredDougFacing(code): string` (Task 0.7) for string-typed contexts (`aria-label`/`title`/status-line). Verification: grep new components for hardcoded user-facing error strings.
- [x] **Supabase call-boundary discipline (invariant 9)** — applies. See §13 meta-test inventory below.
- [x] **Commit per task** (always) — conventional commits, end every commit message body with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## 6. Watchpoints from prior adversarial review (the 13 plan rounds — DO NOT RELITIGATE)

These are settled contracts. Each was the subject of one or more review rounds; the resolution is in the plan at the cited task. Implement them as written; the impl-diff reviewer is pre-loaded against re-opening them.

1. **No pass-only tasks (R1–R2).** Every task has a real red→green. Audit coverage lives inside the behavior task. Layout-dimensions (Task 9.1) and transition-audit (Task 9.2) expect a real first-run FAIL (Tailwind v4 collapse / un-wired compound transitions) + a component-fix step + negative-regression. prod-runtime-flip (Task 8.4) uses negative-regression as its red step.
2. **Fail-visible completeness (R3–R4).** See the **Fail-visible completeness matrix** in the plan's Meta-test inventory: EVERY infra read (alertCount, AlertBanner, fetchDriveConnectionHealth, getActiveWatchedFolder) is fail-visible on ALL THREE sub-paths — client-construction throw, returned `{error}`, thrown await — and EVERY write UI surface (AddAdminForm, RevokeRowButton, **ReAddRowButton**) renders the inline `ADMIN_EMAIL_WRITE_FAILED`. NotifBell `infra_error` → distinct degraded bell, never a clean no-badge. AlertBanner renders the degraded banner (never `null`) on ANY infra fault incl. construction throw. A positive/degraded bell must never route to an empty `/admin#alerts`.
3. **Invariant-9 destructuring + registry + grep-shape mechanics (R5–R8).** Read the plan's **`_metaInfraContract` grep-shape recognition contract** before writing any registered helper. Hard rules: (a) destructure `{ data, error }` (never bare `{ count, error }`); (b) a registered file MUST contain a grep-recognized supabase await — `await supabase.from(...)` or `await <builderVar>` where `const <builderVar> = supabase…` — so bind a local `const supabase = <client>` (an indirect `await resolvedClient.client.from(...)` is NOT recognized and fails the meta-test); (c) a pure wrapper with no direct supabase await (`fetchEmbeddedAdminEmails` → `await listAdminEmails()`) is NOT an `infraRegistry` row — it carries an inline `// not-subject-to-meta` comment + a behavioral test. **3 infraRegistry rows** (alertCount, fetchDriveConnectionHealth, getActiveWatchedFolder) + **1 wrapper exemption**.
4. **alertCount null-count integrity (R11).** `fetchUnresolvedAlertCount` returns `infra_error` unless `typeof count === "number"`. A null count with no error is NOT a clean zero.
5. **Route-render proof + layer-aware hook (R9–R10).** Task 2.0 adds a test-only `requireAdmin*({ layer: "layout" | "page" })` + header `x-test-force-infra-fail: <layer>` honored by BOTH `requireAdmin` and `requireAdminIdentity` (the existing `x-help-force-infra-fail` is requireAdmin-only and not layer-scoped — preserve it for `/help`). The layout gates with `layer:"layout"`; pages default `layer:"page"`. Task 2.3 forces `x-test-force-infra-fail: page` so the layout succeeds while a page gate throws, proving Next routes the fault to the page boundary.
6. **All admin `error.tsx` boundaries render `ADMIN_ROUTE_LOAD_FAILED` (R12).** B1 REPOINTS the existing `app/admin/settings/admins/error.tsx` from `ADMIN_EMAIL_LIST_FAILED` → `ADMIN_ROUTE_LOAD_FAILED`. After B1 routes the admin-list read through the typed in-section `fetchEmbeddedAdminEmails` wrapper, the boundary only catches route/session faults — NOT list-read failures. `ADMIN_EMAIL_LIST_FAILED` is an **in-section** state only (rendered by `AdministratorsSection` on `infra_error`). Client boundaries use a FIXED code (cannot read `err.code`).
7. **Drive-health worst-of-fleet via untruncatable counts (B1 spec §3.1; hardened in the spec's own 27 rounds).** The pill reflects the WORST active show via `head:true` count queries with WHERE predicates (NOT the capped 500-row render set). Worst-first precedence (tiers 1–7 + positive); status-based tiers (hard-failures 4a-c + `sync_unknown` 5) precede the age-based stale tiers. No-folder short-circuits BEFORE the `drive_watch_channels` read. Status-line copy derives from health/reason — NEVER a static "Connected" prefix on a Warn/infra state. Shared null-guarded `lastReadClause` on every branch. `attentionCount` = winning-reason predicate count, NOT the total fleet.
8. **Build-vs-runtime dev gate (B1 §3.3).** DevToolsRow keys off the artifact-time `DEV_PANEL_PRESENT` (`lib/admin/__generated__/devPanelPresent.ts`, committed `false`), NOT runtime `process.env.ADMIN_DEV_PANEL_ENABLED`. `scripts/with-admin-dev-flag.mjs` writes it at build + restores `false` on exit. Task 8.4 CREATES `tests/e2e/admin-dev.spec.ts` (3 playwright projects target it but the file is currently absent → zero tests today) + a non-empty gate guard.
9. **§12.4 three-lockstep (5 new codes + 1 edit).** Each catalog change lands in ONE commit: master spec §12.4 prose (`docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md`) + `pnpm gen:spec-codes` regen of `lib/messages/__generated__/spec-codes.ts` + the row in `lib/messages/catalog.ts`. The `x1-catalog-parity` gate (`tests/cross-cutting/codes.test.ts`, `pnpm test:audit:x1-catalog-parity`) fails if the three drift.
10. **Data-ownership (B1 §2.6).** `<AdminPageHeader>` is presentational — NO data fetching in the header or layout. Each page passes props it already has (per-show pill/chip via `rightSlot`). The layout owns only nav chrome + the identity read (in its `AdminInfraError` catch) + the alert count.

## 7. Test commands

- Unit/component (vitest): `pnpm vitest run tests/admin/ tests/components/admin/ tests/messages/ tests/app/admin/ tests/appSettings/ tests/drive/ tests/auth/`
- Meta-test: `pnpm vitest run tests/admin/_metaInfraContract.test.ts`
- §12.4 parity gate: `pnpm test:audit:x1-catalog-parity`
- Real-browser layout-dimensions: `pnpm playwright test admin-nav-layout-dimensions`
- Route-render proof: `pnpm playwright test admin-route-boundaries`
- Dev-gate: `pnpm playwright test --project=prod-runtime-flip --project=prod-build --project=dev-build`
- Full suite + gates (close-out): `pnpm vitest run && pnpm typecheck && pnpm lint`
- Screenshots-drift (admin baselines regen — sanctioned amd64 Docker procedure, see Task 10.1 / `[[feedback_help_screenshot_manifest_captures_admin_routes]]`).

## 8. Exit criteria

- [ ] Every task step in the plan checked off (`- [x]`).
- [ ] Every §8 / §8.1 spec test bullet has a passing assertion.
- [ ] `pnpm vitest run && pnpm typecheck && pnpm lint` clean; `x1-catalog-parity` green.
- [ ] Playwright green: `admin-nav-layout-dimensions`, `admin-route-boundaries`, the three dev-gate projects, screenshots-drift.
- [ ] **Real CI green** (separate gate from local — local-passes-CI-fails class). The Playwright dev-gate projects + screenshots-drift workflow pass on GitHub Actions.
- [ ] **Impeccable v3 dual-gate (BOTH external)** — see §12.
- [ ] Cross-model adversarial review on the IMPL DIFF ran to convergence (APPROVE).
- [ ] Whole-milestone fresh-eyes pass APPROVE (integration bugs across nav + settings + revoke + dev-gate).
- [ ] No new `// TODO`/`// FIXME` outside the plan; conventional commits, one task per commit, with the Co-Authored-By trailer.

## 9. Sandbox / git protocol

- [x] **Claude Code:** commits run in-session, no sandbox issue. `git add` + `git commit` per task via `Bash`. (Implementer is Opus/Claude Code per the UI hard rule.)

## 10. Adversarial review handoff

After implementation: summarize what was built, run the impeccable dual-gate (§12), then invoke the cross-model adversarial review on the impl diff (Codex via codex-companion) and iterate to APPROVE. Reviewer is REVIEWER-ONLY (never fixes). Whole-milestone fresh-eyes posture for the close-out round. Log convergence below.

## 11. Cross-milestone dependencies

- **None new.** B1 reuses existing helpers: `requireAdmin`/`requireAdminIdentity`/`AdminInfraError` (`lib/auth/requireAdmin.ts`), `listAdminEmails`/`addAdminEmail`/`revokeAdminEmail`/`AdminEmailsInfraError` (`lib/data/adminEmails.ts`), `getActiveWatchedFolderId`/`createClientResult`/`firstBootEnvFolderId` (`lib/appSettings/getWatchedFolderId.ts`), `rerunSetupServerAction` (`lib/onboarding/serverActions.ts`), `ThemeToggle` (`components/layout/ThemeToggle.tsx`), `canonicalize` (`lib/email/canonicalize.ts`), `nowDate`/`formatRelative` (`lib/time/now.ts`, `lib/time/relative.ts`), `createSupabaseServerClient`/`createSupabaseServiceRoleClient` (`lib/supabase/server.ts`).
- **Deferred OUT of B1 (do not implement — B2/B3 own them):** auto-publish-clean toggle → **B2**; alert-me-about-sync-problems toggle + email delivery → **B3**; archive/unarchive + unpublish/undo + spec §16 DEF-1/2/3 server guards → **B2**. The settings "Preferences" area in B1 is the Developer-tools row only.

## 12. Impeccable evaluation (UI quality gate — AGENTS.md §1 invariant 8)

**REQUIRED — B1 ships UI** (`app/admin/**` non-api, `components/admin/**`, `app/globals.css` `route-enter` keyframe). After per-task implementation closes and BEFORE adversarial review, run `/impeccable critique` AND `/impeccable audit` on the B1 diff surfaces (nav shell, settings panels, error boundaries) with the canonical v3 preflight gates. **Both runs MUST be external** (fresh subagent or user-invoked) — self-attestation by the implementing session fails §1.8 (`[[feedback_impeccable_external_attestation_required]]`). HIGH/CRITICAL + P0/P1 fixed or `DEFERRED.md`-logged. Dispositions block appended below. Note: a critique copy/label rewrite is NOT authoritative over the spec/catalog — spec-check before applying (`[[feedback_impeccable_critique_not_authoritative_vs_spec]]`).

### 12.1 Dispositions (run 2026-06-01, external fresh subagent — both modes)

**Critique: PASS.** Anti-pattern detector `[]` (zero); does not read AI-generated; restrained palette, single FXAV-orange accent, tokens throughout, no em dashes. Findings: P2 per-show header density (→ `DEFERRED.md` B1-D1, measure-first); P2/P3 minor (no action).

**Audit: PASS.** ~18/20 (a11y 4, perf 4, responsive 3, theming 3, anti-patterns 4). One actionable:
- **P1 — `bg-background` dead token** (`AddAdminForm.tsx` inputs): Tailwind v4 has no `--color-background`, so the inputs rendered with no explicit fill. Pre-existing typo; file in B1 scope. **FIXED** → `bg-bg` (commit `fix(admin): AddAdminForm inputs use real bg-bg token`).

No HIGH/CRITICAL. Both gates external (fresh subagent attestation), per §1.8.

### 12.2 Implementation findings (real bugs surfaced + fixed during B1, beyond the plan)

The real-browser e2e batch + the cross-model review caught **three genuine bugs the plan did not anticipate**, all fixed:
1. **Revoke confirm submit never dispatched (pre-existing since M9).** `RevokeRowButton`'s confirm button disabled itself synchronously in its own `onClick` (`disabled={isResolving}` + `setUi("resolving")`), cancelling the React-19 form-action dispatch → the revoke never POSTed; the button stranded on "Revoking…". This is the *actual* root cause of the revoke-hang the spec §4 misattributed to a server throw/hang (the watchdog papered over a never-dispatched submit). **Fixed** → `disabled={isPending}`; e2e (Task 6.3) + negative-regression confirm the POST now fires. See `[[feedback_react_form_action_synchronous_disable_cancels_submit]]`.
2. **Content occlusion in the 640–719px band (`layout.tsx`).** The global `sm:p-page-pad-desktop` shorthand clobbered the `pb-20` bottom-tab-bar reservation before `min-[720px]:pb-0` fired → the fixed bottom bar occluded the last content row. Caught by the Task 9.1 width-sweep; **fixed** via per-edge padding. (Validates the band-sweep mandate; 1-desktop/1-mobile would have missed it.)
3. **Admin-dev build lock in a cleaned distDir (cross-model adversarial HIGH, R1).** `with-admin-dev-flag.mjs` stored its cross-build lock at `.next-prod-flip/.admin-dev-flag.lock`; the prod-runtime-flip build (`NEXT_DIST_DIR=.next-prod-flip`) cleans that distDir via Next 16's `recursiveDelete(distDir, /^(cache|dev|lock)/)` — `.admin-dev-flag.lock` (leading `.`) does NOT match the anchored preserve-regex → deleted mid-hold → concurrent builds race on the renames + `DEV_PANEL_PRESENT` write. Pre-existing; B1 amplified it. **Fixed** → lock moved to repo-root `.build-locks/` (outside every distDir, gitignored), owner-aware PID-stamped release, stale-PID steal, regression test `tests/admin/withAdminDevFlagLockLocation.test.ts`.

Plus the no-raw-codes scanner caught 4 inline-JSX `getRequiredDougFacing("CODE")` calls (extracted to consts) + 8 B1-file lint issues (cleaned). All committed.

Whole-milestone fresh-eyes (Opus, 2026-06-01): APPROVE, no ship-blockers. MEDIUM `SYNC_STATUS_UNKNOWN` latent copy → `DEFERRED.md` B1-D2 (spec-internal tension, invariant 7 — not silently changed). LOW dashboard infra-error double-header → B1-D3. LOW admins deep-link no header → intentional per code comments.

## 13. Meta-test inventory (AGENTS.md writing-plans rule)

- [x] **Supabase call-boundary discipline** — `tests/admin/_metaInfraContract.test.ts` — **EXTEND**. New `infraRegistry` rows (each lands in the helper's creation task, same commit): `fetchUnresolvedAlertCount` (Task 1.1), `fetchDriveConnectionHealth` (Task 5.3), `getActiveWatchedFolder` (Task 5.1). The `fetchEmbeddedAdminEmails` wrapper is NOT a row (inline `// not-subject-to-meta`, Task 6.1). Read the grep-shape recognition contract in the plan first (Watchpoint 3).
- [x] **§12.4 catalog parity** — `tests/cross-cutting/codes.test.ts` (`x1-catalog-parity`) — **EXTEND** via the three-lockstep: 5 new codes (`ADMIN_ALERT_COUNT_FAILED`, `SYNC_STATUS_UNKNOWN`, `ADMIN_DRIVE_HEALTH_UNAVAILABLE`, `ADMIN_EMAIL_WRITE_FAILED`, `ADMIN_ROUTE_LOAD_FAILED`) + 1 edit (`ADMIN_EMAIL_LIST_FAILED` helpfulContext → in-section surfaces).
- [ ] Sentinel hiding — **N/A** (no optional/nullable tile text fields added).
- [ ] admin_alerts catalog completeness — **N/A** (B1 adds no `admin_alerts` rows; it READS the count. New §12.4 codes are Doug-facing by construction.)
- [ ] Advisory-lock topology — **N/A** (no `pg_advisory*` caller).
- [ ] No-inline-email-normalization — **N/A** (no new external-email read).
- **NEW structural guard B1 adds:** `tests/admin/devSpecNonEmpty.test.ts` (Task 8.4) — asserts the prod-runtime-flip Playwright project resolves to ≥1 test (no silently-empty gate).

---

## Convergence log

- Plan adversarial review (pre-implementation): Codex, 13 rounds, converged APPROVE 2026-06-01 (commit `9acd385`). 12 substantive repair rounds across distinct vectors; all settled contracts captured in §6 above.
- **Implementation (2026-06-01, Opus/Claude Code).** All Phases 0–9 implemented TDD task-by-task via fresh subagents under orchestrator review (red→green→commit, one commit per task). Local gates green: `pnpm vitest run` **4633 passed / 0 failed** (with local Supabase up + seeded), `pnpm typecheck` clean, B1-changed-file lint clean, `x1-catalog-parity` green. e2e (real browser, local Supabase): route-render proof (2.3), observable-refresh (6.3), layout-dimensions sweep (9.1) all green. Three real bugs found + fixed (see §12.2).
- **Impl-diff cross-model adversarial review (Codex via codex-companion):** R1 `needs-attention` — 1 HIGH (admin-dev build lock in a cleaned distDir, conf 0.92, verified correct against Next 16.2.4 source) → fixed (`fix(infra): move admin-dev build lock out of the cleaned distDir + owner-aware release`) → **R2 APPROVE** (0 findings; "No ship-blocking correctness, auth, data-integrity, or regression risk"). Reviewer-only; do-not-relitigate brief inlined §6 contracts.
- **Whole-milestone fresh-eyes (Opus, separate pass):** APPROVE, no ship-blockers; 1 MEDIUM + 2 LOW → `DEFERRED.md` (B1-D2, B1-D3) / by-design. 7/7 integration seams clean.
- **Impeccable v3 dual-gate (external fresh subagent):** critique PASS + audit PASS; P1 fixed, P2 → B1-D1. See §12.1.
- **Deferred to CI (authoritative, post-push — AGENTS.md "Local-passes-CI-fails is its own bug class" / handoff §7-§8):** the dev-gate 3-build Playwright run + negative-regression (8.4 Steps 3–4 — failed locally only on build-lock contention from 4 concurrent webServer builds, a local-env fragility; lock-location fix above hardens it); the screenshots-drift admin-baseline regen (Phase 10 — must regen on amd64 per the byte-comparison discipline, not this arm64 host); and the overall **Real CI green** gate. These complete in GitHub Actions after the branch is pushed.
- **PR + Real CI (2026-06-01):** PR [#4](https://github.com/edweiss412/FX-Webpage-Template/pull/4) opened. **CI GREEN at head `6538e90b`:** `X audits` = success, `Screenshots drift` = success (admin baselines regenerated on a native-amd64 runner via the new `screenshots-regen` workflow_dispatch job — user-chosen CI regen over arm64-emulated local), Vercel = success, mergeState CLEAN. **CI-trigger gotcha (resolved):** PR #4 initially ran NO `pull_request` workflows because the branch was behind the moved `main` (mergeState DIRTY → GitHub could not build the merge ref → no `github-actions` check-suite); additionally the `screenshots-regen` bot commit pushed via `GITHUB_TOKEN` does not re-trigger workflows. Resolved by merging `main` into the branch + a non-bot push. See `[[feedback_pr_behind_base_dirty_blocks_pull_request_ci]]`. **Only un-run gate:** the dev-gate 3-build Playwright projects (`prod-runtime-flip`/`prod-build`/`dev-build`) have no CI workflow — deferred to `DEFERRED.md` B1-D4; the build-vs-runtime contract is pinned by `build-artifact-gate.test.ts` + `devSpecNonEmpty.test.ts` + `withAdminDevFlagLockLocation.test.ts`.
- **B1 implementation complete + green; PR #4 ready for owner review/merge.** Open deferrals: B1-D1 (header density), B1-D2 RESOLVED+IMPLEMENTED (SYNC_STATUS_UNKNOWN copy now surfaced), B1-D3 (dashboard infra-error double-header), B1-D4 (dev-gate CI workflow).
- **B1-D2 follow-up implemented (2026-06-01).** Owner ratified surfacing `SYNC_STATUS_UNKNOWN`'s developer-attention copy for the `sync_unknown` Drive-health reason (§3.1 amended, commit `069955f9`). Implemented: `DriveConnectionPanel.deriveStatusLine` `case "sync_unknown"` → `getRequiredDougFacing(health.code)` + last-read clause, before the generic group `default`; TDD red→green, pinned by `DriveConnectionPanel.test.tsx` (asserts the catalog accessor + negative-asserts the generic line). All other `sync_*`/`stale_*` keep the generic line. Gates: vitest green, typecheck clean, B1 lint clean, x1-parity green (no catalog change), no-raw-codes green. Impeccable v3 dual-gate (external) PASS/PASS. `DEFERRED.md` B1-D2 → RESOLVED+IMPLEMENTED. Render-path pin = the unit test (the variable `getRequiredDougFacing(health.code)` is not regex-detectable by `extract-spec-codes`, so no registry change; parity unaffected).
