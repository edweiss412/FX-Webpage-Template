## Phase 3 — MI-11 gate RPCs (Approve / Reject)

**Depends on:** Phases 1–2 (`sync_holds` + `show_change_log` tables/REVOKE/RLS from Phase 1; `mi11_pending` holds written on detect + hold-aware apply from Phase 2). This phase adds the admin-path lock-taking RPCs that resolve an open `mi11_pending` hold.

**Spec:** `docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate-design.md` §5 (gate flow), §4.1 (lock topology), §4.3 (release). **Shared contracts:** `00-overview.md` (RPC sigs `mi11_approve_hold(p_hold_id, p_observed_modified_time, p_expected_base_modified_time)`, `mi11_reject_hold(p_hold_id, p_expected_base_modified_time)` — the `p_expected_base_modified_time` feed-token guard per resolution #26 / PF40; `Disposition` type; `ChangeLogSource`).

> **For agentic workers:** TDD per task — failing test → minimal impl → passing test → commit. Never write impl before the test. Conventional commits (`feat(db):` for migrations, `feat(sync):` for TS orchestration/server-action, `test(...)` for test-only commits). No placeholders.

### Scope (this phase)

- `mi11_approve_hold` + `mi11_reject_hold` SECURITY DEFINER RPCs — **each acquires `pg_advisory_xact_lock(hashtext('show:'||drive_file_id))` itself** (admin path, §4.1), never invoked from within a JS-held show lock.
- **Approve** applies the locked `proposed_value` disposition (`email_change` → set email; `rename` → delete-old + insert-new; `removal` → delete crew row + revoke OAuth claim), writes `show_change_log` (`source='mi11_approve'`), releases the hold.
- **Reject** converts the hold to `kind='undo_override'` pinning `held_value`, writes `show_change_log` (`source='mi11_reject'`); releases per §4.3 on later sheet reconcile (Phase 2 owns that release path — this phase only writes the override).
- **Collision graph (F10/F12/F14):** directed graph over proposed-and-current `email` AND `name` targets; transitive closure; satisfied **self-edges** for unchanged columns; reject whole group `IDENTITY_WOULD_COLLIDE` if any occupied target is owned by a non-vacating / non-held live row; a fully-closed group approved **atomically** with swap-safe **park** (`email`→NULL since the index is partial `WHERE email IS NOT NULL`; `name`→transient `__hold:<uuid>` placeholder since NOT NULL).
- **Two-stage Drive orchestration (F13):** the JS admin server action reads current Drive `modifiedTime` **before** the RPC and passes it as `p_observed_modified_time`; the RPC compares to `base_modified_time` **inside the lock** and rejects `MI11_TARGET_MOVED` ("changed — re-review") on drift. `shows.last_seen_modified_time` alone is insufficient (tested).
- **Drive-read failure (F15):** returned-error OR thrown-error → typed non-mutating "try again"; do **NOT** call the RPC; hold stays `mi11_pending` (invariant 9 call-boundary).

**Out of scope (later phases):** `undo_change` RPC + tombstone (Phase 4); feed read data layer (Phase 5); UI (Phase 6). The `mi11_pending` detect-write + hold-aware apply + reject→override **release-on-reconcile** belong to Phase 2; this phase consumes them.

### Lock topology (this phase's surfaces)

- `mi11_approve_hold` / `mi11_reject_hold` are **lock-taking SECURITY DEFINER** RPCs (blocking `pg_advisory_xact_lock`), `grant execute … to authenticated` / `revoke … from anon` (NOT service_role), body **gates on `public.is_admin()`** first. They are the **single holder** for their hashkey on the admin path. They are invoked ONLY from the admin server action via the **cookie-bound authenticated** Supabase server client (after `requireAdmin`), which holds **no** JS-side show lock around the call (the Drive read precedes the call; the RPC is awaited bare). Task 3.9 extends `tests/auth/advisoryLockRpcDeadlock.test.ts` to pin this.
- The RPC **cannot read Drive** — the Drive `modifiedTime` is read in the server action and passed in (§4.1, F13).

### Ground truth (verified `file:line`)

- `pg_advisory_xact_lock(hashtext('show:' || r.drive_file_id))` loop idiom: `supabase/migrations/20260524000002_claim_oauth_identity.sql:36-38`.
- **Admin-RPC identity contract (00-overview resolution #11 — authoritative).** These RPCs are **admin-user-initiated**, NOT service-role. Follow the existing admin-RPC pattern at `supabase/migrations/20260601000000_b2_show_lifecycle.sql` (`archive_show`): `revoke all on function … from public, anon, authenticated, service_role;` then **`grant execute on function … to authenticated;`** (`:80-81`); body **gates first** `if not public.is_admin() then …` returning a typed forbidden result (`:62`, `:88`); stamps `created_by = public.current_admin_email()`. Invoked from the admin server action via the **cookie-bound authenticated Supabase server client** (the admin's session JWT, `lib/supabase/server.ts`) **after `requireAdmin`** — NOT the service-role client. `is_admin()` + `pg_advisory_xact_lock` both work inside a SECURITY DEFINER RPC invoked via PostgREST authenticated. Do **NOT** grant to / rely on `service_role` for these.
- Unique indexes Approve can collide with: `crew_members_show_email_unique` partial `WHERE email IS NOT NULL` (`supabase/migrations/20260501000000_initial_public_schema.sql:49-51`) and `unique (show_id, name)` (`:43`). Email CHECK `email = lower(trim(email)) and email <> ''` (`:45-47`).
- Claim column to revoke on removal: `crew_members.claimed_via_oauth_at` (set by `claim_oauth_identity`; `supabase/migrations/20260524000001_crew_members_claimed_via_oauth_at.sql`). "Revoke claim" = `claimed_via_oauth_at := NULL`.
- Drive metadata read: `fetchDriveFileMetadata(driveFileId, options): Promise<DriveListedFile>` returns `{ modifiedTime, … }` (`lib/drive/fetch.ts:68-80,35`), throws `DriveFetchError` on failure (`lib/drive/fetch.ts:19`).
- Typed copy via `lib/messages/lookup.ts` (`messageFor(code, params)`, `lib/messages/catalog.ts`) — RPC/server-action results carry a `code: MessageCode`, never raw text (invariant 5). The four new codes (`MI11_TARGET_MOVED`, `MI11_DRIVE_RECHECK_FAILED`, `MI11_HOLD_ALREADY_RESOLVED`, `IDENTITY_WOULD_COLLIDE`) are added in a **single Phase 1 catalog task** (00-overview resolution #12 — full §12.4 three-lockstep there). This phase **only references** them; its catalog steps are **verification-only** (assert the code exists in `catalog.ts`).
- Admin JWT test path: `tests/db/_b2Helpers.ts` `asAdminTx`/`asAdminRpc` set `request.jwt.claims` to `ADMIN_CLAIMS` (`app_metadata.role:'admin'` → `is_admin()` true) inside a `begin` txn with `role='authenticated'` (`:8-12,38-56`); `NON_ADMIN_CLAIMS` (`:17-22`) is the non-admin authed case. This is the real authed path these tests must use — NOT the service-role `sqlClient` directly.
- DB test harness: `tests/db/_b2Helpers.ts` — `sqlClient` (`:30`), `asAdminTx`/`asAdminRpc` set `request.jwt.claims` to `ADMIN_CLAIMS` (`:8-12,38-56`), `seedLiveShowWithToken` (`:147`). New phase-3 seed helpers live in a new `tests/db/_mi11Helpers.ts` reusing `sqlClient`.
- Server-action RPC-call shape (admin, `{data,error}` destructure, invariant 9): `app/admin/show/[slug]/` actions call `supabase.rpc(...)`; this phase adds `approveMi11Hold` / `rejectMi11Hold` server actions in `lib/sync/holds/mi11GateActions.ts`.

### Meta-test inventory (this phase)

- **EXTEND** `tests/auth/advisoryLockRpcDeadlock.test.ts` — add `20260608…_mi11_gate_rpcs.sql` to `migrationFiles`; assert `lockTakingNames` ⊇ `{mi11_approve_hold, mi11_reject_hold}`; assert `lib/sync/holds/mi11GateActions.ts` does NOT call those RPCs inside `withShowAdvisoryLock` (Task 3.9).
- `RPC_GATED_TABLES` rows for `sync_holds`/`show_change_log` already landed in Phase 1; this phase's RPCs are their write entry point (no new registry row, but Task 3.9 documents the gating).

---

### Task 3.1 — `mi11_reject_hold` RPC: convert hold → `undo_override` + write reject log

Smaller surface than Approve; do it first to establish the lock + log idioms.

- [ ] **Test** (`tests/db/mi11_reject_hold.test.ts`): **all three dispositions** (resolution #20 / PF30), each as an admin via the authed path (`asAdminTx`/`ADMIN_CLAIMS`, `tests/db/_b2Helpers.ts:38-56`) — `select public.mi11_reject_hold($1::uuid, $2::timestamptz)` passing the hold's CURRENT `base_modified_time` as `p_expected_base_modified_time` (the happy-path token equals the rendered value, so the feed-token guard passes), NOT the service-role `sqlClient`. Seed crew `Alice (alice@old)` + a hold; reject; assert the resulting `undo_override` hold's `kind`, `domain`, `proposed_value`, and `held_value.baseline`:
  - **`email_change`** (`proposed_value={disposition:'email_change',name:'Alice',email:'alice@new'}`) → resulting hold `kind='undo_override'`, **`domain='crew_email'`**, `proposed_value` NULL, `held_value` has **no `baseline`**; **Alice's crew row email is still `alice@old`** (reject keeps the old identity).
  - **`rename`** (`proposed_value={disposition:'rename',name:'Alicia',email:'alice@new'}`) → resulting hold `kind='undo_override'`, **`domain='crew_identity'`**, `proposed_value` NULL, **`held_value.baseline={kind:'rename', suppressed_added:{name:'Alicia', email:'alice@new'}}`** (the suppressed replacement, derived from the seeded `proposed_value`).
  - **`removal`** (`proposed_value={disposition:'removal'}`) → resulting hold `kind='undo_override'`, **`domain='crew_identity'`**, `proposed_value` NULL, **`held_value.baseline={kind:'removal'}`**; Alice's row still present (reject keeps her).
  - All three: a `show_change_log` row `source='mi11_reject'`, `change_kind`= the structural map (`crew_email_changed`/`crew_renamed`/`crew_removed`), `before_image`=held_value, `status='rejected'`, `entity_ref='Alice'`, **`created_by`= the admin JWT email (`current_admin_email()`)**. Derive expected `baseline` from the seeded `proposed_value`, not literals.
  - **Failure mode caught (PF30):** reject nulls `proposed_value` for ALL dispositions → a folded rename/removal loses its suppression target → the next unchanged sync keeps the old row AND re-adds the rejected replacement (rejected rename goes live + duplicates); or reject silently applies the new email (gate inverted), or leaves the hold `mi11_pending`, or writes no audit row.
- [ ] **Test** (same file): call `mi11_reject_hold` as a **non-admin authenticated** caller (`NON_ADMIN_CLAIMS`, `_b2Helpers.ts:17-22`), passing the hold's current `base_modified_time` as `p_expected_base_modified_time` → returns the typed forbidden result (`errcode='42501'` per the `archive_show` body convention), **no mutation** (hold unchanged, no log row). **Failure mode caught:** a non-admin authenticated PostgREST caller resolves a hold because the body skipped the `is_admin()` gate.
- [ ] **Test** (same file): call `mi11_reject_hold` (as admin) on a **non-existent / already-released** `p_hold_id` (any `p_expected_base_modified_time`, e.g. `now()`) → returns `jsonb {ok:false, code:'MI11_HOLD_ALREADY_RESOLVED'}` and writes **no** `show_change_log` row (idempotent no-op, §5 guard). **Failure mode:** double-reject throws / writes a duplicate rejected log.
- [ ] **Test — stale Reject → MI11_TARGET_MOVED, zero mutation (resolution #26 / PF40)** (same file): seed crew `Alice (alice@old)` + a `mi11_pending` hold as `email_change` (`proposed_value={disposition:'email_change',name:'Alice',email:'alice@new'}`) at `base_modified_time=T0`; **render** the hold and capture `T0` as the expected feed token. SIMULATE a Phase-2 Task-2.8 in-place re-eval: `update sync_holds set proposed_value={disposition:'rename',name:'Alicia',email:'alice@new'}, base_modified_time=T1 where id=holdId` (`T1 > T0` — a later sheet edit flipped the disposition + bumped the anchor). Call `mi11_reject_hold(holdId, T0)` as an admin (`asAdminTx`/`ADMIN_CLAIMS`). Assert: (a) returns `{ok:false,code:'MI11_TARGET_MOVED'}`; (b) **ZERO mutation** — the hold is **still `kind='mi11_pending'`** (NOT converted to `undo_override`), its retargeted `proposed_value`/`base_modified_time` intact; (c) **no `show_change_log` row** for this hold. Derive `T0`/`T1` from the seeded fixture (anti-tautology — never hardcode). Also assert `messageFor('MI11_TARGET_MOVED')` resolves to **non-null** copy (invariant 5 — the code already exists in `catalog.ts` per the Phase 1 catalog task / resolution #12; verification-only, do NOT add the code or run the three-lockstep).
  - **Failure mode caught (PF40):** a stale Reject converts the latest unseen disposition into an `undo_override`, suppressing a rename/removal the admin never reviewed (and with the whole-parse review path gone, there is no recovery surface).
- [ ] **Impl** (`supabase/migrations/20260608000002_mi11_gate_rpcs.sql`): `create or replace function public.mi11_reject_hold(p_hold_id uuid, p_expected_base_modified_time timestamptz) returns jsonb language plpgsql security definer set search_path = public, pg_temp`. Body — **exact lock order (resolution #15 / PF11 CRITICAL — advisory lock BEFORE any row lock, else deadlock vs the sync path under burst):** (1) **first** `if not public.is_admin() then` return the typed forbidden result (mirror `archive_show` `errcode='42501'`, `20260601000000_b2_show_lifecycle.sql:88`); (2) **NON-locking** read — `select … into v_hold from sync_holds where id=p_hold_id` **WITHOUT `for update`** to discover `drive_file_id`/`show_id` (a non-locking exists / `kind='mi11_pending'` pre-check for an early `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}` return is OK here); (3) `perform pg_advisory_xact_lock(hashtext('show:' || v_hold.drive_file_id))`; (4) **RE-select the hold `FOR UPDATE` and RE-validate** (`kind` still `mi11_pending`) — if it no longer holds → `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}` (a concurrent action resolved it between the non-locking read and the lock); (4a) **FEED-TOKEN STALENESS GUARD (resolution #26 / PF40 — optimistic-concurrency against what the admin SAW):** AFTER the `FOR UPDATE` re-select + `kind='mi11_pending'` re-validate, BEFORE the disposition-aware mutation (step 5) — `if v_hold.base_modified_time is distinct from p_expected_base_modified_time then return jsonb_build_object('ok',false,'code','MI11_TARGET_MOVED')` with **NO mutation** (hold stays `mi11_pending`, no `show_change_log` row). **No Drive read needed** — Reject suppresses, it doesn't apply a sheet value; the feed-rendered `base_modified_time` submitted back as `p_expected_base_modified_time` is the only token. Phase 2 Task 2.8 re-evaluates an open hold IN PLACE (flips `proposed_value` `email_change`→`rename`/`removal`, bumps `base_modified_time`), so a stale admin tab that resolves the OLD disposition is caught here. (5) mutate — **DISPOSITION-AWARE (resolution #20 / PF30): READ `proposed_value->>'disposition'` BEFORE clearing it**, then convert the hold to a disposition-appropriate `undo_override` carrying the **same `held_value.baseline` contract `undo_change` uses** (resolution #16) so Phase 2's undo_override apply (resolution #14) honors it — a flat `proposed_value=null` is correct ONLY for `email_change`, else the next unchanged sync re-applies the rejected rename/removal (keeps Alice AND re-adds Alicia → rejected change goes live + duplicates):
  - **`email_change`** → `update sync_holds set kind='undo_override', domain='crew_email', proposed_value=null where id=p_hold_id` (pin the old email; **no baseline**).
  - **`rename`** → `update sync_holds set kind='undo_override', domain='crew_identity', held_value = v_hold.held_value || jsonb_build_object('baseline', jsonb_build_object('kind','rename','suppressed_added', jsonb_build_object('name', v_hold.proposed_value->>'name', 'email', v_hold.proposed_value->>'email'))), proposed_value=null where id=p_hold_id` (old row retained + the **suppressed replacement** recorded in `held_value.baseline`, read from `proposed_value` before nulling it).
  - **`removal`** → `update sync_holds set kind='undo_override', domain='crew_identity', held_value = v_hold.held_value || jsonb_build_object('baseline', jsonb_build_object('kind','removal')), proposed_value=null where id=p_hold_id` (keep the row; Phase 2 releases when the sheet re-adds it).
  In all three, `proposed_value` ends **null**; the baseline lives on `held_value`. Then insert `show_change_log` (`source='mi11_reject'`, **`change_kind`= the disposition→structural map (resolution #13/PF8 — NEVER `MI-*`): `email_change→'crew_email_changed'`, `rename→'crew_renamed'`, `removal→'crew_removed'`**, `entity_ref=v_hold.entity_key`,`before_image=v_hold.held_value`,`after_image=null`,`status='rejected'`, **`created_by=public.current_admin_email()`**); return `{ok:true}`. **Grants (admin-RPC contract — resolution #11 / `archive_show:80-81`):** `revoke all on function … from public, anon, authenticated, service_role; grant execute on function … to authenticated;` — do **NOT** grant to `service_role`.
- [ ] **Commit:** `feat(db): mi11_reject_hold RPC — convert pending hold to undo_override + reject log`

### Task 3.2 — `mi11_approve_hold`: plain `email_change` self-edge approve (F14)

The simplest Approve: single-node group, name unchanged (a satisfied self-edge), no collision.

- [ ] **Test** (`tests/db/mi11_approve_hold.test.ts`): seed show + `Alice (alice@old)` + `mi11_pending` hold with `proposed_value={disposition:'email_change',name:'Alice',email:'alice@new'}`, `base_modified_time=T0`. Call **as an admin via the authed path** (`asAdminTx`/`ADMIN_CLAIMS`). Reading current Drive-`modifiedTime` is the JS layer's job — in the **DB** test pass `p_observed_modified_time = T0` (equal) AND `p_expected_base_modified_time = T0` (the feed-rendered token equals the hold's current base, so the new feed-token guard passes). Call `select public.mi11_approve_hold($1::uuid, $2::timestamptz, $3::timestamptz)`. Assert: (a) Alice's crew row email is now `alice@new`; (b) the hold row is **deleted**; (c) a `show_change_log` row `source='mi11_approve'`, **`change_kind='crew_email_changed'` (structural, NEVER `MI-*` — resolution #13/PF8)**, `status='applied'`,`after_image` reflects `{name:'Alice',email:'alice@new'}`, **`created_by`= the admin JWT email** (`current_admin_email()`). Derive `email` expectation from the seeded `proposed_value`. Add a sibling **non-admin authed** (`NON_ADMIN_CLAIMS`) call → typed forbidden, no mutation (the `is_admin()` gate).
  - **Failure mode caught:** the unchanged-name self-edge is misread as a collision and the ordinary email change is **permanently unapprovable** (F14); or Approve applies but never releases the hold (stuck pending).
- [ ] **Test — claimed Alice → clears `claimed_via_oauth_at`, new email becomes claimable (resolution #27 / PF45)** (same file): seed show + `Alice (alice@old)` **with a NON-NULL `claimed_via_oauth_at`** (set a fixed timestamptz, e.g. `now() - interval '1 hour'`) + a `mi11_pending` `email_change` hold `proposed_value={disposition:'email_change',name:'Alice',email:'alice@new'}`, `base_modified_time=T0`. Approve as an admin (`asAdminTx`/`ADMIN_CLAIMS`, matching modtime — `p_observed_modified_time = p_expected_base_modified_time = T0`). Assert, deriving every expectation from the seeded `proposed_value`/fixture (anti-tautology): (a) Alice's crew row `email = 'alice@new'`; **(b) Alice's crew row `claimed_via_oauth_at IS NULL`**; (c) the hold is deleted + a `source='mi11_approve'`/`change_kind='crew_email_changed'`/`status='applied'` `show_change_log` row exists. **(d) The picker would now ADMIT a fresh claim by the new email** — call `select public.select_identity_atomic($1::text, $2::text, $3::uuid)` (signature confirmed at `supabase/migrations/20260523000007_select_identity_atomic.sql:94`) for Alice's row with the new email and assert **no `PICKER_IDENTITY_CLAIMED`** rejection; if invoking the picker RPC is out of scope for this Phase-3 DB test, the **`claimed_via_oauth_at IS NULL` column assertion (b) is the load-bearing one** and the picker-admits check is belt-and-suspenders. Assert `messageFor('PICKER_IDENTITY_CLAIMED')` resolves non-null where the picker path is exercised (invariant 5 — verification-only, do NOT add the code or run the three-lockstep).
  - **Failure mode caught (PF45):** a claimed Alice approved `alice@old→alice@new` evicts the old session (live email-match re-check) but **leaves the row `claimed`** → `alice@new` gets `PICKER_IDENTITY_CLAIMED` forever (the new email can never select the row — permanently unclaimable).
- [ ] **Test — stale Approve, synced-retarget → MI11_TARGET_MOVED, zero mutation (resolution #26 / PF40 — the case the Drive re-check misses)** (same file): seed show + `Alice (alice@old)` + a `mi11_pending` hold rendered as `email_change` (`proposed_value={disposition:'email_change',name:'Alice',email:'alice@new'}`) at `base_modified_time=T0`; capture `T0` as the feed-rendered `p_expected_base_modified_time`. SIMULATE a **synced retarget** (sheet X→Y, a sync re-evaluated the hold in place — Phase 2 Task 2.8): `update sync_holds set proposed_value={disposition:'rename',name:'Alicia',email:'alice@new'}, base_modified_time=T1 where id=holdId` (`T1 > T0`). Capture Alice's full `crew_members` row before the call. Approve as an admin (`asAdminTx`/`ADMIN_CLAIMS`) with **`p_observed_modified_time = T1` (Drive moved too — the JS action reads the new modtime, so the existing `observed != base` guard PASSES, `T1 == T1`)** and **`p_expected_base_modified_time = T0` (the OLD feed token the admin saw)**. Assert: (a) returns `{ok:false,code:'MI11_TARGET_MOVED'}`; (b) **ZERO mutation** — Alice's `crew_members` row is byte-identical to the captured pre-image (email still `alice@old`, no rename to Alicia), the hold is **still `kind='mi11_pending'`** with its retargeted `proposed_value`/`base_modified_time` intact, **no `show_change_log` row**. Derive `T0`/`T1` + the retargeted name from the seeded fixture (anti-tautology — never hardcode). Also assert `messageFor('MI11_TARGET_MOVED')` resolves to **non-null** copy (invariant 5 — code already in `catalog.ts` per resolution #12; verification-only, do NOT add the code or run the three-lockstep).
  - **Failure mode caught (PF40):** after a synced retarget the Drive re-check sees `observed == base` (`Y == Y`) and applies the retargeted disposition (the rename to Alicia) the admin never saw — only the feed-token guard (`base != expected`) catches it.
- [ ] **Test — malformed `email_change` guard (resolution #22/PF32)** (same file): seed a hold `proposed_value={disposition:'email_change',name:'Alicia',email:'alice@new'}` where `name` (`'Alicia'`) **≠** `entity_key` (`'Alice'`). Approve (matching modtime — `p_observed_modified_time = p_expected_base_modified_time = base_modified_time`, so both staleness guards pass and the malformed-disposition guard is the rejecting one) → returns a typed malformed-disposition error (`{ok:false,code:'IDENTITY_WOULD_COLLIDE'}` or a dedicated malformed code per the catalog), **no mutation** (Alice unchanged, hold still `mi11_pending`, no log row). **Failure mode caught (PF32):** an `email_change` whose `proposed_value.name ≠ entity_key` silently renames the crew member but logs a **non-undoable** `crew_email_changed` (a rename masquerading as an email change — undo can never reverse it).
- [ ] **Test — reservation-collision guard: suppressed distinct row → IDENTITY_WOULD_COLLIDE, zero mutation (resolution #23/PF37)** (same file): seed show + `Alice (alice@old)` + a `mi11_pending` hold for Alice with `proposed_value={disposition:'email_change',name:'Alice',email:'x@new'}`, `base_modified_time=T0`, and **`reservation_collisions=[{name:'Alicia',email:'x@new'}]`** — the suppressed distinct row exactly as Phase 2 Task 2.7 would have recorded it (Alice held → `x@new`; the sheet also added a distinct Alicia: `x@new`, which the reservation suppressed so Alicia never reached `crew_members`). Capture Alice's full `crew_members` row before the call. Call `mi11_approve_hold($1::uuid, $2::timestamptz, $3::timestamptz)` as an admin (`asAdminTx`/`ADMIN_CLAIMS`) with `p_observed_modified_time = T0` AND `p_expected_base_modified_time = T0` (both matching — so both staleness guards pass and the reservation guard is the rejecting guard). Assert: (a) returns `{ok:false,code:'IDENTITY_WOULD_COLLIDE'}`; (b) **ZERO mutation** — Alice's `crew_members` row is byte-identical to the captured pre-image (email still `alice@old`, no rename); (c) **no `show_change_log` row** for this hold; (d) the hold is still `kind='mi11_pending'` with its `reservation_collisions` array **intact** (`=[{name:'Alicia',email:'x@new'}]`, not cleared). Derive the rejected target (`x@new`) and the suppressed name (`Alicia`) from the seeded fixture, never hardcode an expectation the container could satisfy by accident (anti-tautology). Also assert `messageFor('IDENTITY_WOULD_COLLIDE')` resolves to **non-null** copy (invariant 5 — the code already exists in `catalog.ts` per the Phase 1 catalog task / resolution #12; verification-only here, do NOT add the code or run the three-lockstep).
  - **Failure mode caught (PF37):** Approve silently assigns the reserved target (`x@new`) to the held crew (Alice) while a distinct suppressed person (Alicia) never reaches the admin — because `_mi11_collision_group` only inspects live `crew_members` owners and Alicia was never inserted, so the graph is blind to her; without this guard the suppressed collision is invisible at apply time (the RPC cannot re-read Drive, §4.1).
- [ ] **Test — `[]`-approves-normally control (release side, resolution #23/PF37)** (same file): seed an otherwise-identical Alice hold with `proposed_value={disposition:'email_change',name:'Alice',email:'alice@new'}`, `base_modified_time=T0`, and **`reservation_collisions='[]'`** (empty — no suppressed rows; single-node approvable disposition, no live-owner collision). Approve (matching modtime, admin path — `p_observed_modified_time = p_expected_base_modified_time = base_modified_time`). Assert it **approves normally** exactly like Task 3.2's happy-path: Alice's email is now `alice@new`, the hold is deleted, a `source='mi11_approve'`/`change_kind='crew_email_changed'`/`status='applied'` `show_change_log` row exists. **Failure mode caught:** the reservation guard over-rejects — blocking an empty-`reservation_collisions` hold that has no suppressed distinct row — proving the guard fires ONLY when `jsonb_array_length(reservation_collisions) > 0`, not unconditionally.
- [ ] **Impl** (same migration): begin `mi11_approve_hold(p_hold_id uuid, p_observed_modified_time timestamptz, p_expected_base_modified_time timestamptz)`, `security definer`, **`grant execute … to authenticated` / revoke from `service_role`** (resolution #11). Body — **exact lock order (resolution #15 / PF11 CRITICAL — advisory lock BEFORE any row lock):** (1) **first** `if not public.is_admin() then` return the typed forbidden result (`errcode='42501'`, per `archive_show`); (2) **NON-locking** read — `select … into v_hold from sync_holds where id=p_hold_id` **WITHOUT `for update`** to discover `drive_file_id`/`show_id` (non-locking exists / `kind='mi11_pending'` pre-check → early `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}` OK); (3) `perform pg_advisory_xact_lock(hashtext('show:' || v_hold.drive_file_id))`; (4) **RE-select the hold `FOR UPDATE` and RE-validate** under the lock — `kind` still `mi11_pending` (else `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}`) AND **re-read `base_modified_time`/`proposed_value` from the locked row** (never the stale non-locking copy). **Disposition-validity guard (resolution #22/PF32):** if `proposed_value->>'disposition' = 'email_change'` AND `proposed_value->>'name' is distinct from v_hold.entity_key` → return the typed malformed error with **no mutation** (an `email_change` MUST keep the existing name; a name change is a `rename` disposition). Then: **TWO staleness guards (resolution #26 / PF40 — the hold must satisfy `observed == base == expected` to apply):** (a) **existing sheet-moved-unsynced guard** — `if p_observed_modified_time is distinct from v_hold.base_modified_time then return {ok:false,code:'MI11_TARGET_MOVED'}` (no mutation; the sheet moved since the hold was computed and no sync has re-evaluated it); (b) **NEW feed-token guard (synced-retarget the Drive re-check misses)** — right next to (a), `if v_hold.base_modified_time is distinct from p_expected_base_modified_time then return {ok:false,code:'MI11_TARGET_MOVED'}` (zero mutation). Guard (a) alone MISSES the synced-retarget case: sheet X→Y, a sync re-evals the hold to `base=Y`, the admin's stale tab approves, the JS action reads Drive=Y → `p_observed (Y) == base (Y)` passes (a) and applies the retargeted disposition the admin never saw; guard (b) catches it because the feed-rendered `p_expected_base_modified_time` is still the OLD token (`X`'s modtime), so `base (Y) is distinct from expected (X)` → `MI11_TARGET_MOVED`. The resolution #25 closed-group per-member guard (`base_modified_time IS NOT DISTINCT FROM p_observed_modified_time`) is **UNCHANGED** and stays consistent — with `observed == expected` on the submitted hold, group members reconciled to the same modtime stay consistent. **Reservation-collision guard (resolution #23/PF37 — independent of and PRECEDES `_mi11_collision_group`):** AFTER the disposition-validity (PF32) + staleness (MI11_TARGET_MOVED) guards above, but BEFORE building the collision group or any mutation — re-read the locked row's `reservation_collisions` (the `FOR UPDATE` copy, never a stale non-locking copy, consistent with the re-read of `base_modified_time`/`proposed_value` above) and `if jsonb_array_length(v_hold.reservation_collisions) > 0 then return {ok:false,code:'IDENTITY_WOULD_COLLIDE'}` with **no mutation** (crew unchanged, hold still `mi11_pending`, no `show_change_log` row). This guard catches **suppressed distinct-entity rows** that Phase 2 Task 2.7 persisted on the hold (`reservation_collisions=[{name,email},…]`, `'[]'` = none) — a distinct person the hold's proposed email/name reservation suppressed in Phase 2 (e.g. Alice holds → `x@new`; the sheet keeps Alice AND adds a distinct Alicia: `x@new` → Alicia is suppressed, never reaches `crew_members`). Because the RPC cannot re-read Drive (§4.1), the collision is detected at apply time off the persisted array. This is **structurally independent of** `_mi11_collision_group` (Task 3.4): the graph inspects live `crew_members` owner chains; this guard inspects the durable suppressed-row record, which the graph is blind to (the suppressed person was never in `crew_members`). It runs FIRST so a non-empty `reservation_collisions` rejects before any graph traversal or write. Stamp `created_by=public.current_admin_email()` on the `show_change_log` insert. Build the **collision group** (Task 3.4 fills this; for now: single node, self-edge on unchanged `name`, occupied-email check against other rows). Apply `email_change`: `update crew_members set email=(proposed_value->>'email'), claimed_via_oauth_at = null where show_id=v_hold.show_id and name=v_hold.entity_key`. **The `claimed_via_oauth_at = null` clear is load-bearing (resolution #27 / PF45 — the email anchor ALWAYS moves in the MI-11 gate, so the clear is unconditional here):** Approve must end the old OAuth claim when it moves the email anchor (the spec's `Approve → apply new email (evicts/enables claim)` contract — design spec line 75, MI-11 row line 41 "new email unclaimable until applied"). Leaving the OLD claim attached evicts the old-email viewer (live email-match re-check, `resolvePickerSelection.ts:122-143`) BUT keeps the row `claimed`, so the NEW email gets `PICKER_IDENTITY_CLAIMED` (`select_identity_atomic.sql:74-78`, `resolvePickerSelection.ts:110-118`) forever — permanently unclaimable. Delete the hold; insert `show_change_log` with **`change_kind`= the disposition→structural map (`email_change→'crew_email_changed'`, `rename→'crew_renamed'`, `removal→'crew_removed'` — NEVER `MI-*`, resolution #13/PF8)**, `status='applied'`, `created_by=public.current_admin_email()`. Return `{ok:true}`.
- [ ] **Commit:** `feat(db): mi11_approve_hold RPC — plain email_change approve (self-edge, F14)`

### Task 3.3 — Approve dispositions: `rename` and `removal`

- [ ] **Test** (`tests/db/mi11_approve_hold.test.ts`): **rename** — hold `proposed_value={disposition:'rename',name:'Alicia',email:'alice@new'}`. Approve (matching modtime — `p_observed_modified_time = p_expected_base_modified_time = base_modified_time`). Assert: old `Alice` row deleted, new `Alicia` row exists with `email='alice@new'`, hold deleted, **`show_change_log.change_kind='crew_renamed'` (structural, NEVER `MI-*`)**, `status='applied'`, `created_by`= the admin JWT email, `before_image`=old Alice row, `after_image`=`{name:'Alicia',email:'alice@new'}`. **Failure mode:** rename leaves a duplicate `(show_id,name)` row or loses the email on transition (claim never moves).
- [ ] **Test — rename-with-changed-email starts the new identity unclaimed (resolution #27 / PF45)** (same file): seed `Alice (alice@old)` **with a NON-NULL `claimed_via_oauth_at`** + a `mi11_pending` hold `proposed_value={disposition:'rename',name:'Alicia',email:'alice@new'}`, `base_modified_time=T0`. Approve (matching modtime). Assert: old `Alice` row deleted; the new `Alicia` row exists with `email='alice@new'` **AND `claimed_via_oauth_at IS NULL`** (the new-identity row must NOT inherit the deleted row's claim — it starts at the column default). Derive the renamed name + new email from the seeded `proposed_value` (anti-tautology). **Failure mode caught (PF45):** the rename insert copies `claimed_via_oauth_at` from the deleted row → the renamed identity is born already-`claimed` and `alice@new` can never select it (`PICKER_IDENTITY_CLAIMED`).
- [ ] **Test** (same): **removal** — hold `proposed_value={disposition:'removal'}`, Alice had `claimed_via_oauth_at` set. Approve (matching modtime — `p_observed_modified_time = p_expected_base_modified_time = base_modified_time`). Assert: Alice's crew row **deleted**; (claim revoked is implicit in the delete — also assert no residual row). `show_change_log` `change_kind='crew_removed'`, `status='applied'`, `created_by`= the admin JWT email, `before_image`=Alice. **Failure mode:** removal leaves the row (held crew survives) or leaves a claimed orphan.
- [ ] **Impl** (same migration): branch on `proposed_value->>'disposition'`: `rename` → `delete from crew_members where show_id=… and name=v_hold.entity_key; insert into crew_members(...) values(...new name+email, copying ONLY the non-identity fields from the deleted row...)` (rename = delete-old + insert-new per §5.4). **The insert MUST NOT copy `claimed_via_oauth_at` from the deleted row — the new-identity row starts `claimed_via_oauth_at = NULL` (column default), per resolution #27 / PF45.** `claimed_via_oauth_at` is a **claim/identity field, NOT a non-identity field**: the ONLY columns copied from the deleted row are `phone, role, role_flags, date_restriction, stage_restriction, flight_info` (the F17 non-identity set, `00-overview.md:174`); a held rename always carries a changed email (§4.3 would have released a same-email hold), so the new identity is correctly fresh/unclaimed — this is the `Approve → apply new email (evicts/enables claim)` contract (design spec line 75, MI-11 row line 41) on the rename path. `removal` → `delete from crew_members where show_id=… and name=v_hold.entity_key` (cascade/explicit `claimed_via_oauth_at` moot once deleted — the DELETE drops the claim with the row). Each writes a `show_change_log` row with the **disposition→structural `change_kind` map (`rename→'crew_renamed'`, `removal→'crew_removed'` — NEVER `MI-*`, resolution #13/PF8)**, `status='applied'`, `created_by=public.current_admin_email()`.
- [ ] **Commit:** `feat(db): mi11_approve_hold — rename (delete+insert) and removal (delete+revoke) dispositions`

### Task 3.4 — Collision graph: transitive closure + IDENTITY_WOULD_COLLIDE reject

> **Arity convention for every Approve call in Tasks 3.4–3.5:** `mi11_approve_hold($1::uuid, $2::timestamptz, $3::timestamptz)` — `$2 = p_observed_modified_time`, `$3 = p_expected_base_modified_time` (resolution #26 / PF40). For happy-path group approvals (swap/cycle/mixed/removal-node), pass `$2 = $3 = the SUBMITTED hold's current base_modified_time` so both the submitted hold's staleness guards pass and the per-member pass (resolution #25) is what validates the pulled-in members. The two PF40 staleness guards apply to the SUBMITTED hold; the per-member pass (resolution #25) re-checks staleness on pulled-in members against `p_observed_modified_time`.

- [ ] **Test** (`tests/db/mi11_collision_graph.test.ts`): **chain terminating at a non-held live row** — `Alice→bob@x` (held, vacating `alice@old`), but `bob@x` is currently owned by **live crew `Bob` with no open hold**. Approve the Alice hold → returns `{ok:false,code:'IDENTITY_WOULD_COLLIDE'}`, **no mutation** (Alice still `alice@old`, Bob untouched, hold still `mi11_pending`). Derive the conflict target from the seeded rows.
  - **Failure mode caught:** Approve issues a single-statement reassign that hits `crew_members_show_email_unique` mid-statement and aborts the txn with a raw 23505, OR misclassifies a chain ending at a live row as closeable and corrupts Bob.
- [ ] **Test** (same): **self-edge is satisfied, not a collision** — a `rename` hold `Alice→Alice2` keeping `email=alice@old` (email unchanged) must NOT reject on its own email "collision" with itself. **Failure mode:** unchanged-column self-edge misclassified (the F14 class, name-axis variant).
- [ ] **Impl** (same migration, plpgsql helper `_mi11_collision_group(p_show_id, p_hold_id) returns uuid[]`): starting from the approving hold's `proposed_value`, for each of `{email, name}` where the proposed value **differs** from the row's own current value (skip satisfied self-edges), find the current owner row in `crew_members`. If the owner is a member row covered by an open `mi11_pending` hold whose `proposed_value` **vacates** that exact value → add to group + recurse; else → raise the group as **non-closeable**. `mi11_approve_hold` calls this; on non-closeable → `{ok:false,code:'IDENTITY_WOULD_COLLIDE'}` before any write. **Scope note (resolution #23/PF37):** this graph covers **live-owner chains only** — collisions against rows currently in `crew_members`. **Suppressed distinct-entity** collisions (a distinct person the hold's reservation suppressed in Phase 2 Task 2.7, who was never inserted into `crew_members`) are NOT visible to this traversal; they are handled by `mi11_approve_hold`'s separate reservation-collision guard (Task 3.2), which rejects on a non-empty `v_hold.reservation_collisions` BEFORE this graph is built. **Per-member note (resolution #25/PF39):** this helper proves each pulled-in member is still `mi11_pending` + vacates its contested value, but does NOT re-run staleness / reservation-collision / disposition-validity on non-submitted members — `mi11_approve_hold` runs that per-member guard pass over the returned group (Task 3.5 Impl) AFTER this helper returns and BEFORE any park/mutate.
- [ ] **Commit:** `feat(db): mi11 collision graph — transitive closure, self-edge satisfaction, IDENTITY_WOULD_COLLIDE`

### Task 3.5 — Closed-group atomic swap with swap-safe park (two-person, cycle, mixed)

- [ ] **Test** (`tests/db/mi11_collision_graph.test.ts`): **two-person email swap** — `Alice: a@x→b@x` and `Bob: b@x→a@x`, both `mi11_pending`. Approve the group (one call against either hold). Assert: Alice now `b@x`, Bob now `a@x`, **both** holds deleted, two `show_change_log` rows. **Failure mode:** the naive single reassign hits `crew_members_show_email_unique` mid-statement.
- [ ] **Test** (same): **3-way email cycle** — `A:1→2, B:2→3, C:3→1`. Approve → A=2,B=3,C=1, all 3 holds gone. **Failure mode:** cycle not closed (graph stops at depth 2) → IDENTITY_WOULD_COLLIDE false-positive.
- [ ] **Test** (same): **mixed rename + email swap** — exercises NOT-NULL `name` parking: `Alice→Bob` (rename to a name currently held by another vacating member) so the swap must park `name` to `__hold:<uuid>` (NULL is illegal for `name`). Assert final names/emails correct, no `unique (show_id,name)` violation, both holds gone. **Failure mode:** `name` parked to NULL (NOT-NULL violation) or not parked (unique violation).
- [ ] **Test — closed-group reassignment clears the participating node's claim (resolution #27 / PF45)** (same file): seed a two-person email swap — `Alice: a@x→b@x` and `Bob: b@x→a@x`, **both `mi11_pending`, both `base_modified_time=T1`** — and set the participating `email_change` node **Alice's live row `claimed_via_oauth_at` to a NON-NULL value**. Approve the group (one call at `p_observed_modified_time = p_expected_base_modified_time = T1`). Assert: Alice now `b@x`, Bob now `a@x`, both holds deleted, two `applied` `show_change_log` rows — **AND Alice's reassigned row ends `claimed_via_oauth_at IS NULL`** (the step-(1) park cleared `email`/`name` but not the claim; step (3) must clear it). Derive the swap targets from the seeded fixture (anti-tautology). **Failure mode caught (PF45):** the reassign step moves Alice's email anchor (`a@x→b@x`) but leaves her old claim attached → the swapped-in identity is `claimed` and the new email gets `PICKER_IDENTITY_CLAIMED` forever.
- [ ] **Test** (same): **closed group containing a `removal` node (F-PF29; email-only per resolution #22/PF32)** — `Alice`'s hold is `email_change` targeting **ONLY Bob's freed EMAIL** (`proposed_value={disposition:'email_change',name:'Alice',email:'bob@x'}` — **`name` stays `'Alice'`, her own existing name; an `email_change` NEVER renames**), while `Bob`'s hold is `{disposition:'removal'}` (Bob's row deletion vacates his email, so the graph admits him as a vacating node). Approve **either** hold. Assert, atomically: (a) **Bob's row deleted** (claim revoked via the delete); (b) **Alice keeps name `'Alice'`, email set to Bob's freed `bob@x`**; (c) **both** holds deleted; (d) **exactly one `crew_removed` + one `crew_email_changed`** `applied` `show_change_log` row; (e) **both** unique indexes satisfied — no raw 23505. Derive targets from the seeded rows.
  - **Failure mode caught (PF29):** a `removal` node in a closed group is parked-then-reassigned to a NULL/absent target → null-target failure, OR the removed row is left undeleted (collision/corruption), OR the group is wrongly ruled unapprovable.
- [ ] **Test** (same): **name-takeover variant uses a `rename` node, NOT `email_change` (resolution #22/PF32)** — Alice takes Bob's freed **name** via `proposed_value={disposition:'rename',name:'Bob',email:<Alice's new email>}` while Bob's hold is `removal`. Approve → Bob deleted, Alice renamed to `'Bob'`, **one `crew_removed` + one `crew_renamed`** (NOT `crew_email_changed`). **Failure mode:** a name takeover logged as the non-undoable `crew_email_changed` instead of the undoable `crew_renamed`.
- [ ] **Test — stale non-submitted group member → MI11_TARGET_MOVED, zero mutation (resolution #25/PF39)** (same file): seed a two-person email swap — `Alice: a@x→b@x` and `Bob: b@x→a@x`, **both `mi11_pending`** — and set **Alice's `base_modified_time` to an OLDER value (`T0`) than the submitted/observed modtime (`T1`)** (Bob's `base_modified_time=T1`). Approve **Bob** at `p_observed_modified_time=T1` AND `p_expected_base_modified_time=T1` (both matching Bob's own base, so Bob's submitted-hold PF40 staleness guards both pass and the rejecting guard is Alice's, surfaced only by the per-member pass). Assert: (a) returns `{ok:false,code:'MI11_TARGET_MOVED'}`; (b) **ZERO mutation** — Alice still `a@x`, Bob still `b@x`, **BOTH** holds still `kind='mi11_pending'`, **no `show_change_log` row** for either. Derive the stale member, the swap targets, and `T0`/`T1` from the seeded fixture (anti-tautology — never hardcode the expected state). Also assert `messageFor('MI11_TARGET_MOVED')` resolves to **non-null** copy (invariant 5 — the code already exists in `catalog.ts` per the Phase 1 catalog task / resolution #12; verification-only, do NOT add the code or run the three-lockstep).
  - **Failure mode caught (PF39):** the swap applies Alice's stale proposed identity because only the submitted member's (Bob's) staleness guard ran — the closed-group approve never re-checked staleness on the pulled-in member.
- [ ] **Test — collision-blocked non-submitted group member → IDENTITY_WOULD_COLLIDE, zero mutation (resolution #25/PF39)** (same file): same two-person swap (`Alice: a@x→b@x`, `Bob: b@x→a@x`, both `mi11_pending`, both `base_modified_time=T1`), but set **Alice's `reservation_collisions` to `[{name:'Alicia',email:'b@x'}]`** (a suppressed distinct person Phase 2 Task 2.7 recorded on Alice's hold). Approve **Bob** at `p_observed_modified_time=T1` AND `p_expected_base_modified_time=T1` (so both PF40 staleness guards pass on Bob's submitted hold and the rejecting guard is Alice's reservation-collision, surfaced only by the per-member pass). Assert: (a) returns `{ok:false,code:'IDENTITY_WOULD_COLLIDE'}`; (b) **ZERO mutation** — Alice still `a@x`, Bob still `b@x`, **BOTH** holds still `kind='mi11_pending'` with Alice's `reservation_collisions` **intact** (`=[{name:'Alicia',email:'b@x'}]`), **no `show_change_log` row** for either. Derive the swap targets + the suppressed name/email from the seeded fixture (anti-tautology). Also assert `messageFor('IDENTITY_WOULD_COLLIDE')` resolves to **non-null** copy (invariant 5 — verification-only, the code already exists per resolution #12).
  - **Failure mode caught (PF39):** a distinct suppressed person (Alicia) is silently lost because the PF37 reservation-collision guard only ran on the submitted member (Bob) — the closed-group approve applied Alice's blocked identity without re-checking her persisted `reservation_collisions`.
- [ ] **Impl** (same migration): when `_mi11_collision_group` returns a closed group, `mi11_approve_hold` runs a **per-member validation pass** (resolution #25 / PF39) AFTER the group is built but BEFORE the park/mutate sequence below, THEN the **disposition-aware swap-safe sequence** in one txn (already under the show lock).
  - **Per-member validation pass (resolution #25 / PF39 — generalizes the Task 3.2 single-hold guards to EVERY group member).** The submitted hold's guards (staleness, reservation-collision, disposition-validity in Task 3.2) are **necessary but not sufficient** for a group: `_mi11_collision_group` (Task 3.4) only proves each pulled-in member is still `mi11_pending` and *vacates* the contested value — it does NOT re-run staleness, reservation-collision, or disposition-validity on the non-submitted members. Without this pass, approving Bob at Drive modtime `T1` pulls in Alice and applies Alice's **stale** (`base_modified_time=T0`) or **reservation-blocked** (`jsonb_array_length(reservation_collisions) > 0`) identity anyway, because only Bob's guards ran → identity corruption / silently-lost distinct person, defeating F13 + PF37 for non-submitted members. Therefore: `mi11_approve_hold` **RE-selects EVERY hold id in the group `FOR UPDATE`** (the show advisory lock is already held; all group members share `v_hold`'s `drive_file_id`/`show_id`, so the single `p_observed_modified_time` applies to the whole group) and validates EACH member against the **SAME guard set as the submitted hold** — (a) `kind='mi11_pending'`; (b) **staleness** `base_modified_time is not distinct from p_observed_modified_time`; (c) **reservation-collision** `jsonb_array_length(reservation_collisions) = 0`; (d) **disposition-validity** `email_change` ⇒ `proposed_value->>'name' = entity_key`. On ANY member failing → `return` the SPECIFIC typed error with **ZERO mutation** (no `crew_members` / `show_change_log` / `sync_holds` change; all holds stay `mi11_pending`): stale → `{ok:false,code:'MI11_TARGET_MOVED'}`; non-empty `reservation_collisions` → `{ok:false,code:'IDENTITY_WOULD_COLLIDE'}`; not-pending → `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}`; malformed disposition → the malformed/`IDENTITY_WOULD_COLLIDE` code per resolution #22. (`_mi11_collision_group` already covers (a) + vacating; the NEW checks here are (b) staleness, (c) `reservation_collisions`, (d) disposition-validity.) Only once **every** member passes does the swap-safe sequence proceed:
  branching per member's `proposed_value->>'disposition'`: (1) **park every participating LIVE row** — `update … set email=NULL, name='__hold:'||gen_random_uuid()::text` (clears both unique indexes; `email` NULL is index-safe via the partial index, `name` placeholder is unique) — including the removal nodes' rows so their name+email free up before any reassign; (2) **DELETE the `removal` nodes' rows** (revoke claim = the delete), writing a `crew_removed` `applied` log per removal; (3) **assign the `email_change` / `rename` nodes** to their `proposed_value` `email`/`name` (now freed) **AND set `claimed_via_oauth_at = NULL` for every such node** (resolution #27 / PF45 — every `email_change`/`rename` node moved its email anchor, so its old OAuth claim must end; the step-(1) park cleared the unique-index columns `email`/`name` but does NOT by itself clear the claim, so without this the reassigned row stays `claimed` and the new email gets `PICKER_IDENTITY_CLAIMED` forever — `select_identity_atomic.sql:74-78`), writing a `crew_email_changed` / `crew_renamed` `applied` log per node; (4) **delete ALL the group's holds** (the original pending entries resolve). Each log row carries `created_by=public.current_admin_email()`. Wrap so a mid-step failure rolls back the whole group (single RPC txn). **A `removal` node has no final name/email to reassign — step (2) deletes it; it is NEVER carried into step (3).**
- [ ] **Commit:** `feat(db): mi11 closed-group atomic swap-safe park (email→NULL, name→placeholder)`

### Task 3.6 — JS server action: two-stage Drive orchestration (F13 happy path)

- [ ] **Test** (`tests/sync/mi11GateActions.test.ts`): seed a hold with a known `drive_file_id=D1` and a **CURRENT `base_modified_time=T1`**; mock `fetchDriveFileMetadata` → `{modifiedTime: T1}`; mock the Supabase RPC to capture args. Call `approveMi11Hold(holdId, EXPECTED_T0)` — **`holdId` + a CALLER-SUPPLIED expected token `EXPECTED_T0` (≠ the hold's current `T1`)**, no `showId`/`driveFileId` from the caller. Assert: (a) the action did a **non-locking server-side read** of `sync_holds WHERE id=holdId` to discover the **authoritative** `drive_file_id` (PF23 — NOT to source the expected token); (b) `fetchDriveFileMetadata` was called with **that hold's `D1`** (NOT a passed-in value) **before** `supabase.rpc('mi11_approve_hold', …)`; (c) `p_hold_id===holdId`, `p_observed_modified_time===T1` (the freshly-observed Drive modtime), AND **`p_expected_base_modified_time===EXPECTED_T0` — the CALLER-PASSED value, NOT the hold's current `T1` re-read from the pre-read** (resolution #26/PF40: the expected token is what the admin SAW when the feed rendered, forwarded unchanged from the client; the helper must NOT substitute a fresh server read). Result is whatever the RPC mock returns. Call-order spy, not a DB re-read.
  - **Anti-vacuous assertion (the bug this guards):** because the hold's current `base_modified_time` (`T1`) ≠ the caller's `EXPECTED_T0`, a helper that (wrongly) re-read the token from the pre-read would forward `T1` and the RPC's `base IS DISTINCT FROM expected` guard would see `T1==T1` and never fire. Asserting the RPC received `EXPECTED_T0` (the stale client value reaching the RPC **unchanged**) is exactly what lets the RPC detect the synced retarget — it proves the helper forwards the caller token, making the PF40 guard non-vacuous.
  - **Failure mode caught (PF23):** a client-supplied `driveFileId` (or a `holdId` from another show) re-checks the **wrong** file; or the action calls the RPC without the Drive re-check; or passes `shows.last_seen_modified_time` (stale) instead of the freshly-observed Drive value.
- [ ] **Test** (same file): `approveMi11Hold(holdId, EXPECTED_T0)` (a dummy expected-token arg to match the arity) where the hold is **already gone** (non-locking read returns no row) → returns `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}` **WITHOUT** calling `fetchDriveFileMetadata` and **WITHOUT** calling `supabase.rpc` (assert both mocks' call-count === 0). **Failure mode:** the action hits Drive / the RPC for a resolved hold.
- [ ] **Impl** (`lib/sync/holds/mi11GateActions.ts`): `export async function approveMi11Hold(holdId: string, expectedBaseModifiedTime: string | null)` (**`holdId` + the expected feed-token; `drive_file_id`/`show_id` are NEVER taken from the client** — resolution #17/PF23). **`expectedBaseModifiedTime` is the value the admin SAW WHEN THE FEED RENDERED** (`FeedGate.baseModifiedTime`), supplied by the CALLER (Phase 6's server action, from the client-submitted feed gate) and forwarded to the RPC **unchanged** — resolution #26/PF40. **It is NEVER sourced from a fresh server read of the hold**; doing so would make the RPC's `base IS DISTINCT FROM expected` guard vacuous (it would compare the FOR-UPDATE `base_modified_time` against a value re-read from the same row microseconds earlier → always equal → a synced retarget still applies). **Uses TWO clients (resolution #17/PF24):** (0) `await requireAdmin()` (JS session gate). (1) **non-locking server-side read** of `sync_holds WHERE id=holdId` **ONLY to resolve the authoritative `drive_file_id`** (+ `show_id`, PF23) and the early no-row check — **via the SERVICE-ROLE server client (`createSupabaseServiceRoleClient`, `lib/supabase/server.ts` — same posture as the feed read, server-only after `requireAdmin`)**, **NOT** the authenticated client, because `sync_holds` is RLS-locked from `authenticated` (F9 / resolution #10) so an authed-client SELECT would permission-error; destructure `{data,error}` (invariant 9); if no row → return `{ok:false,code:'MI11_HOLD_ALREADY_RESOLVED'}` **before** any Drive call. **Do NOT read `base_modified_time` here to source the expected token** — the token is the caller param. (2) `const meta = await fetchDriveFileMetadata(hold.drive_file_id)` (the hold's authoritative id; wrapped per Task 3.8). (3) the mutation `supabase.rpc('mi11_approve_hold', { p_hold_id: holdId, p_observed_modified_time: meta.modifiedTime, p_expected_base_modified_time: expectedBaseModifiedTime })` (the **caller-supplied** token) goes through the **cookie-bound AUTHENTICATED server client** (the admin's session JWT — the RPC's `is_admin()` gate + advisory lock need the JWT, resolution #11); destructure `{data,error}` (invariant 9), map `error`/`data.ok===false` → typed `{ok:false, code}` via `lib/messages`. `export async function rejectMi11Hold(holdId: string, expectedBaseModifiedTime: string | null)` = `requireAdmin` + authenticated client `supabase.rpc('mi11_reject_hold', { p_hold_id: holdId, p_expected_base_modified_time: expectedBaseModifiedTime })` (the **caller-supplied** token) — **NO Drive read** (Reject suppresses, doesn't apply a sheet value) and **NO pre-read**: the RPC resolves its own `drive_file_id` for the advisory lock and already returns `MI11_HOLD_ALREADY_RESOLVED` for a gone hold, so no service-role lookup is needed (the prior pre-read-for-token was the vacuous-source bug — removed). Neither wraps the RPC in `withShowAdvisoryLock` (the RPC self-locks; Task 3.9 pins this). **Phase 6 wiring (do NOT implement here):** Phase 6's `mi11ApproveAction`/`mi11RejectAction` server actions (resolution #17 — they DELEGATE) pass the **client-submitted `gate.baseModifiedTime`** as `expectedBaseModifiedTime` into these helpers, so the optimistic-concurrency token originates at the rendered feed, not the server.
- [ ] **Test — REAL permission boundary (not mocked; PF24, `tests/sync/mi11GateActions.test.ts` or a sibling `tests/db/` test with a live admin JWT):** seed a `sync_holds` row. (a) Prove `approveMi11Hold(holdId, EXPECTED_T0)` **resolves the hold's `drive_file_id`** (the service-role lookup read succeeds) — mock only `fetchDriveFileMetadata` + the RPC, leave the lookup read against the real DB. (b) In the same test, prove a **direct `authenticated`-client SELECT** on `sync_holds` for that same row is **RLS-denied** (zero rows / permission error) — using the `ADMIN_CLAIMS` authed path (`_b2Helpers.ts`). Asserting both in one test proves the helper MUST use service-role for the lookup and that the authed client genuinely can't read `sync_holds`.
  - **Failure mode caught (PF24):** the helper does the lookup read with the authenticated client → permission-errors for **real** admins under Phase-1 RLS, while the mocked call-order test (Task 3.6) passes — a defect only a live RLS read surfaces.
- [ ] **Commit:** `feat(sync): mi11 approve/reject server actions — two-stage Drive-modtime orchestration`

### Task 3.7 — Stale-target guard (F13): Drive modtime advanced → reject; `last_seen_modified_time` insufficient

- [ ] **Test** (`tests/sync/mi11GateActions.test.ts`): seed a hold (`base_modified_time=T0`); mock `fetchDriveFileMetadata` → `{modifiedTime: T1}` where `T1 > T0`. `approveMi11Hold(holdId, T0)` (caller passes `expectedBaseModifiedTime=T0`, matching the hold's base — so the feed-token guard passes and the `observed != base` guard is the rejecting one) reads the hold's authoritative `drive_file_id`, passes observed `T1`; the (real, against the DB harness) RPC sees `T1 ≠ T0` and returns `{ok:false,code:'MI11_TARGET_MOVED'}`; the action surfaces the typed "changed — re-review" copy; **no mutation** (hold still `mi11_pending`, crew email unchanged).
- [ ] **Test** (`tests/db/mi11_approve_hold.test.ts`): direct-RPC assertion — `mi11_approve_hold(hold, T1, T0)` (`p_observed_modified_time=T1`, `p_expected_base_modified_time=T0` — `p_expected` matches the hold's `base_modified_time=T0`, so the feed-token guard passes and the `p_observed != base` guard is the rejecting one) with `base_modified_time=T0`, while `shows.last_seen_modified_time` is **separately** set to `T0` (== base). Asserts the RPC rejects on the **passed** observed time, proving `last_seen_modified_time` alone would have admitted a stale approve. **Failure mode caught:** the guard reads `last_seen_modified_time` (which misses an edit landing in the sync→approve window) instead of the passed-in observed Drive time.
- [ ] **Impl:** already in Task 3.2's guard (`p_observed_modified_time is distinct from base_modified_time`); this task adds the `last_seen_modified_time`-insufficiency assertion + the server-action surfacing path. Confirm the RPC body never references `shows.last_seen_modified_time` for the guard (grep).
- [ ] **Commit:** `feat(sync): mi11 stale-target guard — reject on observed-Drive-modtime drift (F13)`

### Task 3.8 — Drive reverify failure (F15): returned-error + thrown-error → typed non-mutating

- [ ] **Test** (`tests/sync/mi11GateActions.test.ts`): two cases. (a) `fetchDriveFileMetadata` **throws** `DriveFetchError` (or a 5xx). (b) a returned-error variant — wrap the Drive read so a 403/404/429 surfaces as a discriminable `{ok:false}` rather than a throw, and feed that. In **both** (the hold pre-read succeeds, giving the authoritative `drive_file_id`; the Drive read on THAT id then fails): `approveMi11Hold(holdId, EXPECTED_T0)` returns `{ok:false, code:'MI11_DRIVE_RECHECK_FAILED'}` (typed, `lib/messages`-backed "couldn't re-check the sheet — try again"), the Supabase RPC mock is **never** called, and the hold remains `mi11_pending` (assert RPC-mock call-count === 0).
  - **Failure mode caught:** approve proceeds on a fallback/stale modifiedTime when the Drive read fails, OR a raw infra error (DriveFetchError message) leaks to the admin UI (invariants 5 + 9).
- [ ] **Impl** (`lib/sync/holds/mi11GateActions.ts`): wrap `fetchDriveFileMetadata` in a `try/catch`; on throw OR a returned-error result, **return early** with `{ok:false, code:'MI11_DRIVE_RECHECK_FAILED'}` — do not call `supabase.rpc`. Both the thrown-error and returned-error paths are discriminated (invariant 9). **Catalog: VERIFICATION-ONLY.** The four codes (`MI11_TARGET_MOVED`, `MI11_DRIVE_RECHECK_FAILED`, `MI11_HOLD_ALREADY_RESOLVED`, `IDENTITY_WOULD_COLLIDE`) are added by a **single Phase 1 catalog task** (00-overview resolution #12, full §12.4 three-lockstep there). This task only **asserts each code already exists** in `lib/messages/catalog.ts` (a fast assertion in the message test) and references it — do **NOT** add codes or run the three-lockstep here.
- [ ] **Commit:** `feat(sync): mi11 Drive-reverify failure → typed non-mutating result (F15, invariant 9)`

### Task 3.9 — Extend `advisoryLockRpcDeadlock` for the new RPCs

- [ ] **Test** (extend `tests/auth/advisoryLockRpcDeadlock.test.ts`): add `supabase/migrations/20260608000002_mi11_gate_rpcs.sql` to `migrationFiles`; assert `lockTakingRpcNames()` ⊇ `['mi11_approve_hold','mi11_reject_hold']` (proves both bodies contain `pg_advisory_xact_lock`); add `lib/sync/holds/mi11GateActions.ts` to `sourceFiles` so the existing "no lock-taking RPC inside `withShowAdvisoryLock`" assertion covers the new server actions (single-holder rule, invariant 2 / §4.1).
  - **Failure mode caught:** a future edit wraps `approveMi11Hold`'s RPC call in `withShowAdvisoryLock` → nested holder → deadlock under burst (the M5 R20 class).
- [ ] **Test — lock-order structural guard (resolution #15 / PF11 CRITICAL):** in the **same file** (`tests/auth/advisoryLockRpcDeadlock.test.ts`), add a new `test(...)` that, for **every** lock-taking RPC body returned by `lockTakingRpcNames()`-equivalent parsing of the migration SQL (reuse the existing `stripComments` + `create … function … $$ … $$` block matcher), asserts the body does **NOT** contain a `for update` lexically **before** its first `pg_advisory_xact_lock(` — i.e. `body.search(/\bfor\s+update\b/i)` is either `-1` or **greater than** `body.search(/pg_advisory_xact_lock\s*\(/i)`. Run it over the existing lock-taking migration set **plus** `20260608000002_mi11_gate_rpcs.sql`.
  - **Failure mode caught:** any present-or-future lock-taking RPC that row-locks (`SELECT … FOR UPDATE` on `sync_holds`/`crew_members`/`show_change_log`) before acquiring the show advisory lock → reverses the sync path's order (advisory-then-row) → deadlock under burst. Pins resolution #15 so future RPCs can't regress the order.
- [ ] **Commit:** `test(auth): pin mi11 gate RPCs single-layer + advisory-lock-before-row-lock order (PF11)`

### Task 3.10 — Run the phase test suite + verify

- [ ] `pnpm vitest run tests/db/mi11_reject_hold.test.ts tests/db/mi11_approve_hold.test.ts tests/db/mi11_collision_graph.test.ts tests/sync/mi11GateActions.test.ts tests/auth/advisoryLockRpcDeadlock.test.ts` — all green.
- [ ] Apply `20260608000002_mi11_gate_rpcs.sql` to the validation project (RPCs are functions, not tables — no `validation-schema-parity` table-manifest change, but the RPCs must exist where the lockdown/REVOKE tests run; confirm `pnpm vitest run tests/db/postgrest-dml-lockdown.test.ts` still green since the Phase-1 REVOKEs route writes through these RPCs).
- [ ] **Commit (if any test-only fixups):** `test(sync): mi11 gate-RPC phase suite green`

### Task 3.11 — Phase 3 adversarial review (cross-model)

- [ ] After self-review, invoke the cross-model adversarial review (Codex implements this phase per ROUTING.md, so the reviewer is Opus-side). Reviewer is **REVIEWER ONLY** — surfaces findings, does not patch. Focus surfaces: collision-graph closure correctness (cycles, chains-to-live-row, self-edge satisfaction), swap-safe park ordering vs both unique indexes, the F13 two-stage orchestration boundary (Drive read strictly before the RPC; `last_seen_modified_time` never the guard source), F15 non-mutating + no-RPC-call on Drive failure (both thrown + returned-error), and the lock single-holder topology. **Do-not-relitigate:** the RPC-cannot-read-Drive constraint (§4.1) is ratified — the Drive read MUST live in the JS action; reject any "just read Drive in the RPC" suggestion. Iterate until convergence; escalate only genuine ambiguity. Do not proceed to Phase 4 handoff without this.
