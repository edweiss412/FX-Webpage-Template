{
  "$schema-comment": "M12.1 canonical job table. Spec §2.3 is the human-readable view; this JSON is the machine-readable contract pg-cron-coverage.test.ts reads.",
  "$timeoutMs-comment": "Every job is scheduled with the same pg_net timeout_milliseconds, matching Vercel Functions' 300s default (supabase/migrations/20260527000003_schedule_cron_jobs.sql:15-22). Recorded here so a constant derived from it can be pinned without parsing SQL — see tests/cron/samplingPeriodParity.test.ts.",
  "timeoutMs": 300000,
  "jobs": [
    { "jobname": "fxav_cron_sync",           "schedule": "*/5 * * * *",  "route": "/api/cron/sync" },
    { "jobname": "fxav_cron_keepalive",      "schedule": "0 12 * * *",   "route": "/api/cron/keepalive" },
    { "jobname": "fxav_cron_notify_realtime", "schedule": "*/5 * * * *", "route": "/api/cron/notify?job=realtime" },
    { "jobname": "fxav_cron_notify_digest",  "schedule": "0 * * * *",    "route": "/api/cron/notify?job=digest" },
    { "jobname": "fxav_cron_refresh_watch",  "schedule": "7,22,37,52 * * * *", "route": "/api/cron/refresh-watch" },
    { "jobname": "fxav_cron_gc_watch",       "schedule": "15 * * * *",   "route": "/api/cron/gc-watch" },
    { "jobname": "fxav_cron_asset_recovery", "schedule": "*/15 * * * *", "route": "/api/cron/asset-recovery" },
    { "jobname": "fxav_cron_diagram_gc",     "schedule": "30 * * * *",   "route": "/api/cron/diagram-gc" },
    { "jobname": "fxav_cron_report_reaper",  "schedule": "0 6 * * *",    "route": "/api/cron/report-reaper" }
  ]
}
