# Plan: /help/tour hydration fix + help-pages CI promotion

**Spec:** `docs/superpowers/specs/2026-08-10-help-tour-hydration-fix.md` (APPROVED, cross-model round 6/conversation R8, 0 findings) · **Branch:** `fix/help-tour-hydration` · **Implementer:** Opus / Claude Code (UI surface — AGENTS.md routing hard rule)

**Plan-time red, RUN 2026-08-10 (rule: a red asserting current-tree failure is run before review dispatch):** `BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/help-pages.spec.ts --project=mobile-safari` → **1 failed** (`/help/tour → … no console errors`, `pageErrors` non-empty at `tests/e2e/help-pages.spec.ts:152`), **14 passed**, 26.0 s. This also fixes the executed-count derivation: 15 cases × 1 project.

**Meta-test inventory (declared):** EXTENDS `scripts/check-app-e2e-executed.mjs` `REQUIRED` map (+1 row), the `ENV_KEY_ALLOWLIST` `governs` arrays in `tests/ci/_workflowCoverageScan.ts` (17 pairs), and DELETES one allowlist row in `tests/ci/_metaE2eWorkflowCoverage.test.ts`. Creates no new meta-test. Advisory locks, Supabase boundaries, admin alerts: none applies — the diff touches one MDX page and CI wiring only.

**Layout-dimensions / transition-audit tasks:** N/A per spec (no dimensional invariants, no transitions — both sections state None with reasons).

**e2e harness readiness:** boot is the playwright config's port-3000 webServer (`BASELINE_SERVER_ONLY=1` filter). Honest gate statement (plan R3 F1): the existing spec awaits only `networkidle` (`tests/e2e/help-pages.spec.ts:128`) with no hydration-specific gate — a late hydration error can post-date the assertion window. Task 1 therefore ADDS a hydration gate to the page-error case: after `networkidle`, drive a React-owned STATE TRANSITION and await its rendered effect — click the help sidebar's disclosure button and await its open-state DOM change (an onClick handler firing proves hydration completed; SSR-only DOM cannot respond — R4 F1), then restore the state, THEN read `pageErrors`. No fixed settle beat as the oracle; the interaction round-trip is the gate. Detach-safety: per-route fresh locators (existing pattern).

<!-- tasks: depth=2 -->

## Task 1 — Fix the three expanded cards (expression children)

<!-- task: red=`BASELINE_SERVER_ONLY=1 pnpm exec playwright test tests/e2e/help-pages.spec.ts --project=mobile-safari` ac=AC-1,AC-2 -->

Red (OBSERVED at plan time, above): the `/help/tour` page-errors case fails on the hydration mismatch; the production defect is the markdown-paragraph nesting emitted from `app/help/tour/page.mdx:18`, `app/help/tour/page.mdx:65`, and `app/help/tour/page.mdx:101`.

1. In `app/help/tour/page.mdx`, convert EVERY text child of the three expanded cards ("Review queues", "Preview as crew", "Onboarding wizard") to JSX expression children (`{"…"}`), collapsing each child's internal whitespace runs to single spaces. ALL text children — eyebrow, duration, `<h3>` text, body, CTA — per spec §4 (partial conversion lets Prettier re-expand headings; probe-proven). Hrefs, aria-labels, classNames, and every copy word byte-preserved. The four compact cards untouched.
2. Prettier stability proof (R7 F1, snapshot without a premature commit — R8 F1: invariant 1 commits only after green): STAGE the MDX edit (`git add app/help/tour/page.mdx`), THEN run the repo formatter and assert `git diff --exit-code app/help/tour/page.mdx` (working tree vs the staged snapshot — formatting changed nothing), and `pnpm format:check` green. The task COMMIT happens once, at the end, after step 5.
3. Compile probe (ad hoc, not committed): the §3 probe yields exactly ONE `_components.p` (the intro at `app/help/tour/page.mdx:3`), none nested — before AND after formatting.
4. Green: the red command passes (15/15). Run the full spec once more to confirm no sibling route regressed.
5. Copy-equality check (AC-2): extract text/href/aria/class values before and after; text compared whitespace-normalized, the rest byte-equal.

## Task 2 — CI promotion wiring

<!-- task: red=`pnpm vitest run tests/ci/_metaE2eWorkflowCoverage.test.ts` ac=AC-3 -->

Red is written by this task (invariant-1 shape): deleting the `"tests/e2e/help-pages.spec.ts": UNSEEN` row (`tests/ci/_metaE2eWorkflowCoverage.test.ts:148`) makes the meta-test red because the production line that satisfies it — a `tests/e2e/help-pages.spec.ts` entry in the run command at `.github/workflows/app-e2e.yml:143` — is absent on the live tree (verified absent 2026-08-10).

1. Delete the allowlist row; observe the meta-test red for the stated reason.
2. Add `tests/e2e/help-pages.spec.ts` to the `app-e2e.yml:143` run command (no `--project` changes — mobile-safari only, where the file resolves per `playwright.config.ts:78`).
3. `REQUIRED` oracle row: `"help-pages.spec.ts": 15` (`scripts/check-app-e2e-executed.mjs:30` map; 14 NAV routes + parity guard × 1 project, matching the plan-time run). Update the oracle's seven-spec/54-execution calibration comments (now eight-spec/69).
4. The 17 `governs` arrays (`tests/ci/_workflowCoverageScan.ts:703` region; the spec §5.4b list) each gain `tests/e2e/help-pages.spec.ts`. Verify the exact pair set against the meta-test's own failure output, not the spec list.
5. Header comments: rewrite `app-e2e.yml:7` and `app-e2e.yml:34` (blocker gone), and the `app-e2e.yml:2` seven-spec synopsis → eight. Numeric sweep of the whole workflow header in the same commit.
6. Green: the meta-test passes; `pnpm vitest run tests/cross-cutting/app-e2e-ci-wiring.test.ts` (key parity at :203) passes.

## Task 3 — Impeccable dual gate (invariant 8)

<!-- task: red=`grep -qE "^impeccable-gate: critique=RAN" docs/superpowers/plans/2026-08-10-help-tour-hydration-fix.md` ac=AC-5 -->

Red now (observed at plan time: the LINE-ANCHORED grep exits 1 — the only current occurrence of the pattern is inside this task's own marker comment, which is not at line start; the real §12 marker will be — R2 F1). After filling the §12 marker, RERUN the same command and observe exit 0 (the required green endpoint, R2 F2). Run BOTH halves of the invariant-8 dual gate (`/impeccable`, critique mode then audit mode) on the diff with the canonical v3 setup (context.mjs load then register read). Expected visual delta nil-to-minimal (spec §6); P0/P1 findings fixed or DEFERRED.md-logged. On completion, fill the §12 marker line with the real values (`critique=<RAN|RAN-DEGRADED> audit=<RAN|RAN-DEGRADED> p0=<n> p1=<n> dispositions=<recorded|none>`, the grammar the guard pins at `tests/docs/_metaInvariant8Closeout.test.ts:254`) — the closeout guard accepts only the filled form, which is why this task precedes the docs-suite green in Task 4. A half that degraded takes `RAN-DEGRADED`: the marker is the machine-readable copy of the provenance banner and may not overstate it.

## Task 4 — Parent-entry reconciliation + graduation

<!-- task: red=`sh -c '! grep -q "^## BL-HELP-TOUR-HYDRATION-MISMATCH" BACKLOG.md'` ac=AC-6 -->

Red now (observed: the entry heading exists in BACKLOG.md, so the negated grep exits 1; it exits 0 once the entry graduates — same command, real production line). R1 F4 correction: `pnpm vitest run tests/docs` does NOT discriminate this state (the graduation guard is registry-driven with no inverse archive-completeness check), so it is a step-level green check, not the cycle. Census + registry bookkeeping get their own observable checks in the steps.

1. `BL-E2E-APP-DEPENDENT-SPECS-CI-DARK` (`BACKLOG.md:686` region): heading population **24 → 23** (count the allowlist, per its own rule — the figure is read from the allowlist at implementation time, never from this line; it was 25 → 24 when this plan was written and `origin/main` moved underneath it), rewrite the "Ninth member deferred, not wired" paragraph (`BACKLOG.md:731` region) to record the promotion. Dated historical records untouched.
2. Graduate `BL-HELP-TOUR-HYDRATION-MISMATCH` to `BACKLOG-archive.md` with the fix + promotion recorded; add any registry row the graduation guard requires (check `tests/docs/_metaDeferralLedgerGraduation.test.ts` registry).
3. Observable checks: the red command flips green; `grep -q "23 app-dependent" BACKLOG.md` (census restated — the post-merge count, verified against the allowlist); the graduation registry row present (grep the ID in `tests/docs/_metaDeferralLedgerGraduation.test.ts`); then `pnpm vitest run tests/docs` green as the suite-level belt.
4. Invariant-12 note (R1 F5): the IN PROGRESS marker comes off IN THE SAME COMMIT that archives the entry — the explicitly sanctioned graduation shape — so Task 5 owes NO end-of-PR marker removal; archives reject in-flight entries, so it cannot ride along.

## Task 5 — Five-green wiring bar + merge sequence

<!-- task: red=`sh -c 'SHA=$(git rev-parse HEAD); gh run list --workflow=app-e2e.yml --branch fix/help-tour-hydration --json conclusion,event,headSha --jq "[.[] | select(.event==\"pull_request\")] | .[0:5] | (length==5) and all(.conclusion==\"success\") and (.[0].headSha==\"$SHA\")" | grep -qx true'` ac=AC-6 -->

The `red=` above IS the acceptance command (R1 F1/F2; window semantics settled R5 F1 — five same-sha RUN rows are impossible: `gh run rerun` reuses the run id and only adds an attempt, per the live-tree evidence at `docs/superpowers/plans/2026-07-19-ci-unit-suite-under-5min/00-plan.md:461`, while any commit changes the sha). The bar is therefore batch-1's ACTUAL shape: the FIVE most recent pull_request runs are ALL green (consecutive — a red anywhere in the window fails `all`) AND the newest run is on the FINAL content sha (`.[0].headSha == git rev-parse HEAD`), so the final content is covered and no red is skipped over. The five runs accrue from the PR's ordinary content pushes plus, if short, empty-commit re-triggers — each makes a NEW run row, and each becomes the newest, keeping the head-sha conjunct true. Red NOW (zero runs); constructed-failure probes: zero-run, four-run, mixed-conclusion, and newest-not-on-head JSON all emit `false` (exit 1); five greens with the newest on head emit `true` (exit 0).

1. Open the PR; let `pull_request` runs accumulate; re-trigger via EMPTY COMMITS when the PR's natural pushes leave the window short (each creates a new run row and a new head; the acceptance conjunct tracks the newest — R5 F1; `workflow_dispatch` runs are NOT evidence and `gh run rerun` adds attempts, not runs).
2. Five consecutive green `app-e2e` pull_request runs, zero retries, consecutive with the newest on the final head (R1 F6 + R5 F1): a content push after the window is accepted invalidates it (the newest run moves), so acceptance is re-checked as the LAST act before merge; empty commits are permitted window fillers since the conjunct follows the newest run. **Evidence recording (R6 F1): after the acceptance command emits true, run `gh run list --workflow=app-e2e.yml --branch fix/help-tour-hydration --json databaseId,url,conclusion,event --jq '[.[] | select(.event=="pull_request")] | .[0:5] | .[].url'` and paste the five URLs into the PR body (`gh pr comment`/`gh pr edit`) BEFORE merge — the spec's AC-6 requires the links, not only the Boolean.**
3. Fallback (spec §5, pre-ratified): a help-pages flake inside the window reverts the promotion as a unit (YAML entry, oracle row, 17 governs, allowlist row restored WITH flake reason, parent census restored, comments name the flake) while Tasks 1 + 3 + 4 still ship. **AC-1 stays bound on this path (R3 F2): before merge, run the spec once against a production build with the test-auth contract the suite needs (R7 F2): `pnpm build` then `ENABLE_TEST_AUTH=true TEST_AUTH_SECRET=<the value playwright.config supplies> pnpm start`, then the Task 1 command against it (signInAs requires those vars or the auth route 404s), transcript pasted in the PR — the spec's both-postures requirement does not ride on the reverted CI wiring.** The ARCHIVE entry is written to match reality (R3 F5): Task 4's graduation text records the fix unconditionally and the promotion CONDITIONALLY — on fallback, the same reconciliation commit amends the archive entry AND rewrites the parent entry's ninth-member paragraph to record the ATTEMPTED promotion and the flake (R6 F2: the spec's §5 fallback bookkeeping covers that paragraph too, not only the census and comments), before merge.
4. Whole-diff cross-model review to APPROVE BEFORE the five-green window is accepted (review repairs are content pushes and reset it — R1 F6 ordering). **If any post-gate repair touches `app/help/tour/page.mdx` or any UI surface, RERUN both halves of the invariant-8 gate on the amended diff and refresh the §12 record + marker before the window restarts (R3 F3 — the gated diff must be the merged diff), then rerun `pnpm vitest run tests/docs`.** Real CI green (all workflows); `gh pr merge --merge`; fast-forward main; `git rev-list --left-right --count main...origin/main` == `0  0`. (No end-of-PR ledger-marker step — see Task 4's invariant-12 note.)

<!-- tasks: end -->

## Acceptance criteria (crosswalk to the spec's §8)

- AC-1: zero page errors on /help/tour, mobile-safari, both server postures.
- AC-2: hrefs/aria/classes byte-identical; copy whitespace-normalized-equal.
- AC-3: spec wired in app-e2e.yml, allowlist row gone, coverage meta-test green (fallback reading per spec §5).
- AC-4: no stale blocker comment or contradicting cardinality in the workflow header.
- AC-5: the invariant-8 dual gate (both halves) run with P0/P1 dispositioned.
- AC-6: five-green pull_request bar met with evidence, parent entry restated, graduation registered (fallback reading per spec §5).

## §12 — impeccable gate record

impeccable-gate: critique=RAN audit=RAN-DEGRADED p0=0 p1=2 dispositions=recorded

**Run 2026-08-11 against HEAD `d41cfeb1a`.** Surface: `app/help/tour/page.mdx`, rendered through `app/help/layout.tsx` + `app/help/_components/Sidebar.tsx`. Register: PRODUCT (`reference/product.md`). Setup gates: `context.mjs` context load (PRODUCT.md + DESIGN.md) then register read, both performed.

**Provenance, stated because a silent degradation is a failed run.**

- **critique — dual-agent.** Assessment A (design review) and Assessment B (detector + browser evidence) ran as isolated sub-agents. A's first instance never returned a report; a replacement instance was spawned and delivered, so the assessment kept its isolation rather than collapsing into this context.
- **audit — ⚠️ DEGRADED: single-context**, which is why the marker above reads `audit=RAN-DEGRADED` and not `audit=RAN`. Two sub-agent attempts each did the work and neither returned a report through the message channel. Rather than block the gate, the audit was completed in this context from directly measured evidence (the probes listed below, all run here). Declared rather than hidden — the marker is the machine-readable half of this sentence and must not overstate it.

**Scores.** Critique: **28/40** (Good — the weak rows are Consistency 2, Flexibility 2, Aesthetic/minimalist 2, all driven by one layout defect and one taxonomy split). Audit: **16/20** (Good) — Accessibility 3, Performance 4, Theming 4, Responsive 2, Anti-Patterns 3.

**Anti-patterns verdict: PASSES.** None of the shared absolute bans are present — no side-stripe border (the accent card carries a full `border-accent`), no gradient text, no glassmorphism, no hero-metric template, no numbered section markers, no text overflow at any measured width. Two soft tells, both pre-existing: seven cards with byte-identical anatomy (eyebrow · duration · h3 · body · arrow CTA), and an uppercase tracked eyebrow on every one of them — where the eyebrow says "When something needs you" it routes, where it says "Home screen" it restates the h3. A slot that must be filled seven times is the tell, not the eyebrow.

**Findings — every finding, P0 through P3, with its disposition.**

| # | Sev | Origin | Scope | Finding | Disposition |
| - | --- | ------ | ----- | ------- | ----------- |
| 1 | P1 | audit + critique | PRE-EXISTING | The `md:grid-cols-3` grid (`app/help/tour/page.mdx:53`) sits inside `.help-prose`'s `max-width: 70ch` (`app/globals.css:1148`), so the reading measure is divided three ways. Probed live: 768px → body 104.7px = **10.5ch**, card 811.3px tall; 900px → 14.9ch; 1024px and above → **18.2ch** (the cap binds, not the viewport). `DESIGN.md` §2.5 caps measure at 65–75ch. Mobile (390px, one column) is unaffected. | DEFERRED — `HELPTOUR-CARD-GRID-MEASURE-1` (DEFERRED.md), filed with the probe table and an un-defer trigger. Disposition exception (b): spec §1.1 fences copy and layout changes out of this arc, which converts text children only. The repair is a choice between `md:grid-cols-2` and lifting both grids out of the prose cap — the second touches `.help-prose`, which every `/help/*` page renders through. |
| 2 | P1 | critique | PRE-EXISTING | "Below is every admin screen" (`app/help/tour/page.mdx:3`) is false by one: `app/help/_nav.ts:22-29` declares eight `admin-surface` entries and the page renders seven cards — `/help/admin/settings` has none. | DEFERRED — `HELPTOUR-SETTINGS-CARD-MISSING-1` (DEFERRED.md). Exceptions (a) and (b): adding the card means choosing its group and changing the page's rhythm, softening the intro gives up the completeness promise; both defensible, so it is the page author's call, and §1.1 fences copy changes out regardless. |
| 3 | P2 | audit | PRE-EXISTING | WCAG 2.5.3 Label in Name (Level A): each card's `aria-label` reads "X: read the reference" while the visible CTA reads "Read the X reference →". The accessible name does not contain the visible string, so a voice-control user saying what is on screen gets no match. Also, the `aria-label` suppresses the h3 and duration from the link's accessible name, so a screen-reader user hears seven near-identical strings. | ACCEPTED, not fixed in this arc — aria-labels are byte-preserved by AC-2, and changing them is a copy change (§1.1). Recorded here rather than filed: the repair is one line per card (drop the `aria-label`; the h3 + CTA already name the link) and belongs with finding 2's content pass on the same seven cards. |
| 4 | P2 | critique | PRE-EXISTING | The "Onboarding wizard" card alone gets `border-accent` + an accent eyebrow (`app/help/tour/page.mdx:103,106`). DESIGN.md reserves orange for "this matters now", and this is the card most admins will never need again — the strongest visual emphasis on the least frequent task, with no legend saying what the border means. | ACCEPTED with reason — byte-preserved by AC-2; it is a design-intent question for the page author, not a defect this arc can settle. Contrast is not the issue: `text-accent-on-bg` on `bg-surface-raised` measures 5.57:1, above the 4.5:1 body floor. |
| 5 | P3 | critique | PRE-EXISTING | Card title "Dashboard" diverges from the nav title "Reading the dashboard" (`app/help/_nav.ts:22`) for the same destination. | ACCEPTED with reason — copy, out of scope (§1.1); folds into finding 2's pass. |
| 6 | P3 | audit | PRE-EXISTING | Stale contrast figure in a comment: `app/globals.css` (the `.help-prose` link block) states `--color-accent-on-bg` is **4.11:1** on `--color-bg`; `DESIGN.md:64` states **5.34:1** for the same pair, and measurement agrees with DESIGN.md (5.57:1 against `#FFFFFF`, marginally lower against `#FAFAF9`). The comment's CONCLUSION — do not use accent as a link color — stands on other grounds it also states; only the number is wrong. | ACCEPTED with reason — a comment on a different surface than this diff, no behavioral effect. Recorded so the measurement is not lost; the originating decision already graduated as `BL-ACCENT-ON-BG-AA-CONTRAST` (BACKLOG-archive.md:4983). |

**No finding is `[DIFF]`.** Every row above is present on `origin/main` and survives this branch unchanged. The diff's own visual delta was measured and is nil: all seven cards render body text at `rgb(26,27,31)` (`--color-text`), which is the §6 "nil-to-minimal" expectation VERIFIED rather than assumed.

**Claims raised and refuted — recorded so a later round does not re-derive them.**

- *"The live page still has `p p`=3 / `span p`=6, and the three fixed cards' body text renders in the subtle token while the other four render in the base token."* Those numbers were REAL — of a different branch, and the diagnosis first recorded here ("that is the PRE-fix render") was itself wrong. `playwright.config.ts:265` sets `reuseExistingServer: !process.env.CI`, and :3000 was held by a `next-server` whose cwd is `/Users/ericweiss/FX-worktrees/tap-target-inline-controls` (confirmed with `lsof -a -p <pid> -d cwd`), so every local browser probe in this session that did not relocate the port measured THAT worktree's `/help/tour`, which does not carry this fix. The config's own header comment names the hazard and supplies `E2E_PORT`. Re-measured on a private port against this tree: `p p`=0, `span p`=0, `p div`=0, zero pageerrors, all seven cards at `rgb(26,27,31)`, card heights 324.6 / 402.6 / 350.6 / 298.6 / 324.6 / 298.6 / 298.6 px. The compile probe — a pure function of the file, immune to the contamination — agreed throughout, and so did real CI, where `reuseExistingServer` is false and the spec has now gone green on five consecutive `pull_request` runs. `tests/e2e/help-pages.spec.ts` hardcoded `http://127.0.0.1:3000`, which defeated the escape hatch for the very spec this arc promotes; it now reads `E2E_PORT`, and the first uncontaminated local run of the full spec is 15 passed.
- *"MDX auto-wraps any multi-line bare-string expression child in a spurious `<p>`."* Refuted by the same probes — Prettier's chosen layout puts each long string on its own line inside the braces, and the module still compiles to zero nested paragraphs.
- *"No `:focus-visible` style is declared on the cards; they rely on the UA default."* Refuted: `app/globals.css:788` declares a global `:focus-visible` rule (3px `--color-focus-ring` outline, 2px offset), which the cards inherit.
- *Per-card body-color drift.* Same cause and same correction as the first item — a real measurement of another worktree.

**Positive findings.** Grouping by *when you use it* rather than by system structure maps onto the admin's actual day, which most doc indexes fail at. Body copy answers "why would I open this?" concretely, with no filler sentence in seven cards. Duration labels give a phone user real triage. Zero hard-coded colors — every value is a token with a defined dark variant. No client JS on the page; the diff's only runtime effect is the removal of a failed-hydration client re-render.

**Gate RERUN 2026-08-11 on the amended diff (plan Task 5 step 4).** Two post-gate commits touched the UI surface, so both halves ran again rather than the earlier record being carried forward.

What changed: the Preview-as-crew body string moved from a single-quoted JS string with an escaped apostrophe to a no-substitution template literal (real CI's `sheetIconLinkContainment` guard forbids ANY backslash in an MDX file), and `tests/e2e/help-pages.spec.ts` gained the `E2E_PORT` relocation plus an `aria-label` anchor in `tests/help/heldChangesCopy.test.ts`. The rendered page is byte-identical: copy-equality still reports 66 attribute values byte-equal and text whitespace-normalized equal.

Rerun result — **no new findings, no severity change**. Detector: `[]`, exit 0 on the amended `page.mdx`. Live DOM on a private port (`E2E_PORT=3217`, so the measurement is of THIS tree): `p p`=0, `span p`=0, `p div`=0, zero pageerrors, all seven cards' body text `rgb(26,27,31)`, card heights unchanged at 324.6 / 402.6 / 350.6 / 298.6 / 324.6 / 298.6 / 298.6 px. Full spec on the same private port: 15 passed. Design half: the amendment is a source-quoting change with no rendered delta, so every finding in the table above stands unmoved and none is added. Both halves of the rerun were single-context, which is why the marker keeps `RAN-DEGRADED` on the audit half; the critique half's original dual-agent run is what its `RAN` records, and the rerun did not degrade it.

**Measurements this record rests on** (all run in this session, on this tree): the `@mdx-js/mdx` compile probe; `pnpm format:check`; a Playwright DOM probe signed in as `ADMIN_FIXTURE` reporting nesting counts, computed colors, and card heights (324.6 / 402.6 / 350.6 / 298.6 / 324.6 / 298.6 / 298.6 px at 390px — all clear the 44px floor); a Playwright measure probe at 768 / 900 / 1024 / 1280 / 1440px; and `detect.mjs --json` on `page.mdx`, `Sidebar.tsx`, and `layout.tsx`, all `[]` exit 0.
