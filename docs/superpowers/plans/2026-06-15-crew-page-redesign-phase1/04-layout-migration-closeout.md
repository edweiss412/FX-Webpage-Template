# Crew Show-Page Redesign — Phase 4: Layout + transitions + screenshots + migration + close-out

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Read `00-overview.md` first (binding shared contracts, verified-facts digest, file structure, meta-test inventory), then `02-shell-nav-alert.md` and `03-sections-budget-gear.md` (the components + `data-testid`s this phase asserts against). This repo uses **Vitest** (`pnpm vitest run <path>`), not Jest. Real-browser via `pnpm exec playwright test <path> --project=mobile-safari` (projects: `mobile-safari`, `desktop-chromium`, `dev-build`, `prod-build` — see `playwright.config.ts:37-117`). Every task: failing test (ACTUAL code) → run-to-fail → minimal impl (ACTUAL code) → run-to-pass → commit. NO placeholders. Derive every expected value from the fixture (never hardcode). UI files are Opus-owned (AGENTS.md routing).

**Phase scope (Phase 4 of 4):** the §4.9 Playwright layout-dimensions task (extends `tests/e2e/crew-page.spec.ts`); the §4.10 transition-audit task; the Playwright halves of nav addressability (test 13) + preview-as parity (test 15) + footer report metadata (test 19); the three crew screenshot MANIFEST entries + client-clock freeze + pinned-docker amd64 baselines; the **file-by-file tile migration + deletion** (retarget the tile test suite GREEN first, then delete the 14 `*Tile.tsx` shells + `_ShowBody.tsx` + `lib/show/selectTodayTiles.ts`); and milestone close-out (impeccable v3 dual-gate, cross-model adversarial review, real-CI, merge).

**Inputs (depend on Phases 1-3 being green):** `_CrewShell` + `CrewSubNav` + `CrewSectionTransition` + `RightNowHero` + the five primitives + the six `*Section`s + Budget + Gear emphasis (Phases 2-3); the section-independent server-side projection-alert upsert + the `TILE_PROJECTION_FETCH_FAILED` four-part lockstep + the `upsert_admin_alert` failedKeys-union-merge migration + the meta-test extensions (all GREEN in Phase 2 — **this phase deletes `_ShowBody.tsx`'s always-rendered `notes-tile` alert path, which is only safe because Phase 2's section-independent observability is already live**; §4.13 / §10 sequencing).

**CRITICAL ORDERING (§10 phase 4 / wp-20 — non-negotiable, the spine of this phase):**
1. **KEEP** the non-View modules: `components/tiles/OpeningReelVideo.tsx` (media player, reused by Gear), all `lib/` `load*` data helpers, all `components/shared/` error infra (`WrappedTile`/`TileServerFallback`/`TileErrorBoundary`/`TileErrorFallback`).
2. **RETARGET** the existing tile test suite to the new section/primitive components and keep it GREEN — this is the **structural guarantee** that every tile-level contract a test pins (caps/overflow, Notes truncation, opening-reel URL-strip, sentinel-hiding, AC-pinned `data-testid`s, date/tabular formatting, scope-glyph distinction) is preserved or CI fails. A contract without a test is not relied upon (wp-22).
3. **ONLY THEN** delete the 14 `*Tile.tsx` View shells + `_ShowBody.tsx` + `lib/show/selectTodayTiles.ts` — once nothing imports them AND the Phase-2 prerequisites are confirmed green (server-side projection-alert upsert, `TILE_PROJECTION_FETCH_FAILED` lockstep, `_metaAdminAlertCatalog`/`_metaSentinelHidingContract` extensions, the `upsert_admin_alert` failedKeys-merge migration + its validation-backed dedup test).
4. **No client island, server action, signing secret, or "alert-bridge gate"** gates the deletion (wp-27) — those belong to the reverted R30–R38 design; do not introduce them.

**Live-code anchors (base `a2884c3f`, from 00-overview verified-facts + Phase-4 re-verification 2026-06-18):**
- `tests/e2e/crew-page.spec.ts` (692 lines): seeded-slug helper + `data-testid` markers (head comment `:1-25`); mobile-safari single-writer guard (`testInfo.project.name !== "mobile-safari"` skips at `:139`, `:257`, `:369`, `:396-397`); existing today-band equal-height `getBoundingClientRect()` blocks (the assertions §9 test-12 **replaces** — the band is subsumed by Today). Imports `ADMIN_FIXTURE` from `./helpers/fixtures`, `signInAs`/`signOut` from `./helpers/signInAs`, `admin` from `./helpers/supabaseAdmin`.
- `scripts/help-screenshots.manifest.ts`: `ManifestEntry` type (`:10-20` — `key, route, fixture, frozenClockInstant, viewport, theme?, waitFor?, captureSelector?, expectStableMs?`); `DESKTOP`/`MOBILE` (`:22-23`, MOBILE = `390×844`); `RPAS_CENTRAL_2026` fixture + slug + preview crew id (`:25-32`); `MID_SHOW_INSTANT = "2026-03-24T15:00:00.000Z"` (`:33`); `stableUuid` (`:38-48`); `MANIFEST` array (`:50-87`, 4 admin entries, **no crew entry**).
- `scripts/help-screenshots.ts`: imports `CAPTURE_LAUNCH_ARGS` (`:6`) + `MANIFEST` (`:7`); freezes BOTH clocks — `context.clock.install({ time: new Date(entry.frozenClockInstant) })` (`:191`) AND `page.setExtraHTTPHeaders({ "X-Screenshot-Frozen-Now": entry.frozenClockInstant })` (`:198-199`); launches via `chromium.launch({ args: CAPTURE_LAUNCH_ARGS })` (`:224-225`). Entrypoint `pnpm screenshot:help` (`package.json:39`, runs `playwright.screenshots.config.ts` projects `screenshots-help` + `screenshots-help-capture` under `ENABLE_TEST_AUTH=true`).
- `scripts/capture-launch-args.ts`: `CAPTURE_LAUNCH_ARGS` (`:23-29`) — pinned by `tests/help/capture-script.test.ts`; Playwright `use.launchOptions` does NOT reach the script's own `chromium.launch` (PR #22 trap).
- `.github/workflows/screenshots-drift.yml`: pinned Docker `mcr.microsoft.com/playwright:v1.59.1-jammy --platform linux/amd64` (`:39`), `pnpm screenshot:help` inside it (`:42`), `git diff --exit-code public/help/screenshots/` (`:45`); `workflow_dispatch:` enabled (`:6`).
- Manifest pin tests (Vitest): `tests/help/_metaScreenshotManifest.test.ts` — fixture-resolves (`:98-103`), route-resolves-to-App-Router-page (`:106-111`), light+dark WebPs exist once captured (`:114-125`), no orphan WebPs (`:154-165`), **`every manifest key is consumed by at least one help MDX <Screenshot name>` (`:168-176`)**, light/dark byte-distinct (`:128-152`); `tests/help/manifest-shape.test.ts` (required-fields shape `:17-35`); `tests/help/screenshot-coverage.test.ts` Half A (`:50-69` — every `<Screenshot name>` ref resolves to a manifest key, the *inverse* direction) + Half B on-disk WebP existence (`:73-86`). **⚠️ See Task 6 — the MDX-consumer assertion (`:168`) conflicts with spec §4.11's "No /help MDX change required"; resolved via a documented crew-key exemption in the SAME commit.**
- Tile inventory (`components/tiles/`, 16 files): 14 View shells to delete — `AudioScopeTile, VideoScopeTile, LightingScopeTile, ContactsTile, CrewTile, DiagramsTile, FinancialsTile, LodgingTile, NotesTile, OpeningReelTile, PackListTile, ScheduleTile, ShowStatusTile, TransportTile, VenueTile` — **plus `OpeningReelVideo.tsx` (KEEP)**. Tile tests (`tests/components/tiles/`): `_metaSentinelHidingContract.test.ts` (extended Phase 2), `_pureRenderCompliance.test.ts`, `CardinalityCapBoundary.test.tsx`, `DiagramsTile.test.tsx`, `OpeningReelTile.test.tsx`, `OpeningReelVideo.test.tsx`, `ScopeTileIcons.test.tsx`, `SentinelHidingClass.test.tsx`.
- Importers of the to-be-deleted modules: `app/show/[slug]/[shareToken]/page.tsx` + `_ShowBody.tsx`, `app/admin/show/[slug]/preview/[crewId]/page.tsx` (swapped off `ShowBody` in Phase 2/3), `components/tiles/OpeningReelTile.tsx` (imports `OpeningReelVideo` — the importer is deleted, the imported KEEP file is re-pointed to Gear), `lib/show/selectTodayTiles.ts`, and tests `tests/show/selectTodayTiles.test.ts`, `tests/show/resolvedArmCrewMembersGuard.test.tsx`, `tests/show/page-today-uses-now-utility.test.ts`, `tests/components/_metaPickerRoleChipContract.test.ts`, and the 6 tile-test files above.
- `_ShowBody.tsx` (to delete): malformed-projection guard `:115-117`, `nowDate()` `:133`, `notes-tile` catch-all `:403-448`, `ShowRealtimeBridge renderVersion` `:469`, Footer report props `:516-535`. Every contract here is ported into `_CrewShell` in Phase 2 (verified before this phase deletes the file).

---

### Task 1: Layout dimensions — real-browser §4.9 invariants (test 12)

**Files:** `tests/e2e/crew-page.spec.ts` (modify — replace the today-band equal-height blocks with the redesigned-layout `describe`)

> **jsdom is insufficient and MUST NOT be used here.** jsdom does not run layout — it never computes `getBoundingClientRect()`, never resolves Tailwind v4 flex/grid, and **this project's Tailwind v4 does not default `.flex` to `align-items: stretch`** (AGENTS.md / `memory/feedback_tailwind_v4_flex_items_stretch.md`), so an equal-height collapse passes every unit test and only a real browser render catches it. This task runs against the `mobile-safari` Playwright project (+ a `≥720px` viewport resize for the desktop-only invariants), reads `getBoundingClientRect()` on every documented `data-testid`, and asserts `child.height === parent.height` (and width where stated) within **0.5px tolerance**. Derive expected dimensions from the rendered parent, never hardcode pixel values (the band-sweep mutates seeded state — `tests/e2e/crew-page.spec.ts:246` single-writer pattern).

**The exact §4.9 Dimensional invariants (VERBATIM — assert each):**

1. **Today quick-cards row** (Tonight / Where / Need-something): equal heights == row height (`items-stretch` + `h-full`), ±0.5px, across the band sweep.
2. **Crew two columns** (Show crew | Key contacts): equal column heights at ≥720px (`items-stretch` + `h-full`); **<720px they stack single-column**, height unconstrained (no equal-height constraint).
3. **Gear scope cards**: equal heights within their row when ≥2 render.
4. **RightNowHero**: `min-h` == 176px (`--spacing-right-now-min-h`) held constant through a state crossfade (assert height stable before/after a forced state change, ±0.5px).
5. **Sub-nav**: bottom tab-bar full-viewport-width, bottom-anchored, each tab `flex-1` equal width + full-bar height (`self-stretch`); respects `env(safe-area-inset-bottom)`. Top tabs ≥44px tap height.
6. **KeyTimesStrip** rows align (label left / value right, `tabular-nums` value column).
7. **Single-column sections** — Schedule day cards, Venue blocks, and Travel blocks render in a **single column at all widths** (vertical stack, height unconstrained); no equal-height invariant applies. Any future multi-column variant must add its own invariant here.

- [ ] In `tests/e2e/crew-page.spec.ts`, **remove** the legacy today-band equal-height blocks (the `getBoundingClientRect()` band assertions the §9 inventory says are subsumed by Today) and **add** a `test.describe("crew redesign layout invariants (§4.9 / test 12)")` gated to `mobile-safari` (`if (testInfo.project.name !== "mobile-safari") return;` per the existing single-writer pattern at `:139`/`:257`/`:396`). Sign in + navigate to a seeded crew route at `?s=today`, freeze the browser clock to a `show_day_n` instant (`page.clock.setFixedTime`, mirroring the screenshot harness) so the hero state is deterministic. Write ACTUAL assertions, one per invariant, each citing the invariant number in the test name. _Failure mode: a Tailwind-v4 stretch/collapse bug that jsdom cannot see ships an unequal-height card row / collapsed tab-bar to mobile crew._
  - **Inv 1 (Today quick-cards):** read the bounding boxes of `[data-testid="today-quick-cards"]` (the row) and each `[data-testid^="today-card-"]` child; assert each child `height` equals the tallest child within 0.5px AND equals the row content height. Derive the expected from the measured row, not a literal.
  - **Inv 2 (Crew columns):** navigate `?s=crew`; at a `≥720px` viewport (`page.setViewportSize({ width: 760, height: 1000 })`) assert the two `[data-testid="crew-column"]` boxes have equal `height` within 0.5px; then at `390px` assert they stack (top of column 2 `.top` ≥ bottom of column 1 `.bottom`, i.e. NOT side-by-side) and do NOT assert equal height.
  - **Inv 3 (Gear scope cards):** navigate `?s=gear`; collect `[data-testid="gear-scope-card"]`; when `count >= 2` assert all heights equal within 0.5px.
  - **Inv 4 (Hero min-h through crossfade):** navigate `?s=today`; read `[data-testid="right-now-hero"]` height (assert `>= 176 - 0.5`); force a state change (advance `page.clock` past a day/segment boundary + dispatch `visibilitychange`) and re-read; assert the height is stable within 0.5px across the crossfade.
  - **Inv 5 (bottom tab-bar):** at `390px`, read `[data-testid="crew-sub-nav"]` bottom-bar box; assert `width === viewport.width` (±0.5px), `bottom` anchored to the viewport bottom (account for `env(safe-area-inset-bottom)` — assert `bottom <= viewport.height` and bar is fixed), each `[data-section]` tab `width` equal within 0.5px (`flex-1`) AND `height === bar.height` (`self-stretch`). Separately at `≥720px` assert each top tab's box `height >= 44`.
  - **Inv 6 (KeyTimesStrip alignment):** with a fixture carrying ≥2 anchors, read each `[data-anchor]` row; assert the label column left edges align (equal `.left` within 0.5px) and the value column right edges align (equal `.right` within 0.5px).
  - **Inv 7 (single-column sections):** for `?s=schedule`, `?s=venue`, `?s=travel`, assert the section's direct block children share the same `.left` (single column — no two blocks side-by-side: for any pair, one's `.top >= the other's .bottom - 0.5`).
- [ ] Run to fail: `pnpm exec playwright test tests/e2e/crew-page.spec.ts --project=mobile-safari` (fails — sections/`data-testid`s assert against the redesigned DOM; if the layout has a collapse bug it fails on the equal-height assertion).
- [ ] Fix any real layout collapse surfaced (add `items-stretch` on the flex parent + `h-full`/`self-stretch` on children per the invariant; these are UI edits in `components/crew/**` — Opus-owned). Do NOT loosen the tolerance to make a real collapse pass.
- [ ] Run to pass: `pnpm exec playwright test tests/e2e/crew-page.spec.ts --project=mobile-safari`. `pnpm tsc --noEmit`. Commit `test(crew-page): real-browser §4.9 dimensional invariants (replaces today-band)`.

---

### Task 2: Transition audit — §4.10 inventory (test 14)

**Files:** `tests/components/crew/transitionAudit.test.tsx` (new — static + jsdom structural audit), `tests/e2e/crew-page.spec.ts` (modify — the on-nav animation + compound halves)

> Two layers: (a) a **structural audit** that lists every `AnimatePresence`, ternary render (`{cond ? <A/> : <B/>}`), and conditional block (`{cond && <C/>}`) in the three crew client components (`CrewSectionTransition`, `RightNowHero`, `CrewSubNav`) and asserts each has appropriate `exit`/`initial`/`animate` props OR is deliberately instant; (b) a **real-browser** half asserting the `CrewSectionTransition` crossfade actually animates on nav and the **compound** transitions behave (theme-toggle-during-nav; re-enter Today; hero state-change mid section-swap). The structural audit reads component source as text + renders in jsdom to inspect the framer props; the animation-fires + compound halves are Playwright (jsdom cannot observe a real crossfade).

**The exact §4.10 Transition inventory (VERBATIM — the audit table this task pins):**

| Transition | Treatment |
| --- | --- |
| section ↔ section (any of 7×6/2 pairs) | **one uniform rule**, implemented by the **`CrewSectionTransition`** client wrapper (§4.1, keyed by `?s=`, `initial={false}` first paint): crossfade + 4px translateY, `--duration-normal` (220ms) `--ease-out-quart`; reduced-motion → instant (token-driven) |
| RightNow hero 12-state body swaps | **reuse** `transitionTreatment` + the 66-pair table (`rightNowTransitions.ts`) wholesale; existing `AnimatePresence mode="wait" initial={false}` |
| tab active ↔ inactive | desktop: accent 2px bottom-border (opacity 0→1 active, 1→0 inactive); mobile: accent background fill; text color constant; `--duration-fast` `--ease-out-quart`; instant under reduced-motion |
| Budget tab appears ↔ absent | instant — changes only on data (server render); no animation |
| KeyTimesStrip present ↔ omitted | instant — server render |
| Gear emphasis (viewer's card highlight) | instant — server render (no client toggle) |
| theme toggle during nav (compound) | independent: `data-theme` swap is instant CSS-var; section crossfade unaffected |
| hero state-change mid section-swap (compound) | only Today renders the hero; leaving Today unmounts it (no concurrent hero+section animation); re-entering Today mounts fresh with `initial={false}` first paint |
| any × reduced-motion | all motion via duration tokens → collapses to 0ms (`globals.css:341-347`) |

> `initial={false}` on first paint everywhere (no animating-from-hidden SSR; the project's known framer trap — first paint at rest, animate only post-mount nav).

- [ ] Write `tests/components/crew/transitionAudit.test.tsx`. _Failure mode: an `AnimatePresence`/ternary/conditional ships animating-from-hidden SSR (invisible without JS — the M12.11 framer trap), has an orphaned `exit`, or a section swap ships instant while the inventory expects a crossfade (or vice-versa)._
  - **Static enumeration:** `readFileSync` each of `components/crew/CrewSectionTransition.tsx`, `components/crew/RightNowHero.tsx`, `components/crew/CrewSubNav.tsx`; assert every `AnimatePresence` occurrence carries `mode="wait"` and an `initial={false}` (or a sibling `initial={false}` on the keyed child); assert no `motion.*` with an `initial=` that is a non-`false` object on a first-paint element without a guard (the framer SSR-invisible trap); assert `CrewSectionTransition`'s `motion.div` has `animate` AND `exit` (crossfade requires the exit). Cite each inventory row the audit covers in the test name.
  - **Render-shape (jsdom):** render `CrewSectionTransition` with `usePrefersReducedMotion` mocked `true` → assert the wrapper element is STILL rendered (never branch the returned tree SHAPE on reduced-motion — render the wrapper, collapse only the duration; M12.11 gotcha). Render with reduced-motion `false` → assert the motion props carry the `--duration-normal` value.
  - **Instant-by-design rows:** assert there is NO `AnimatePresence`/`motion.*` wrapping the Budget-tab presence, the KeyTimesStrip presence, or the Gear-emphasis highlight (those are server renders — an animation wrapper there would be a bug). Grep the section/primitive sources to confirm.
- [ ] Run to fail: `pnpm vitest run tests/components/crew/transitionAudit.test.tsx`.
- [ ] Fix any finding (add the missing `exit`/`initial={false}`; remove an erroneous animation wrapper on an instant row). UI edits in `components/crew/**`.
- [ ] Run to pass: `pnpm vitest run tests/components/crew/transitionAudit.test.tsx`.
- [ ] Add the real-browser compound half to `tests/e2e/crew-page.spec.ts` (mobile-safari gated): (a) tab from `today`→`venue`, assert the `[data-testid="crew-section-transition"]` opacity animates (sample `getComputedStyle` opacity mid-transition < 1 with motion enabled) and settles to a fully-rendered Venue; (b) **theme-toggle-during-nav (compound):** start a `?s=` nav, toggle `ThemeToggle` mid-transition, assert `data-theme` flips instantly AND the section still settles (no stuck/aborted crossfade); (c) **re-enter Today (compound):** `today`→`venue`→`today`, assert the hero re-mounts (present) and does not animate-from-hidden (first paint at rest, `initial={false}`); (d) **hero state-change mid section-swap:** force a hero state change while leaving Today, assert no concurrent hero+section animation error (hero unmounts cleanly on leaving Today).
- [ ] Run to pass: `pnpm exec playwright test tests/e2e/crew-page.spec.ts --project=mobile-safari -g "transition"`. `pnpm tsc --noEmit`. Commit `test(crew-page): §4.10 transition audit (structural + compound real-browser)`.

---

### Task 3: Nav addressability + preview-as parity + footer report metadata (real-browser; tests 13/15/19)

**Files:** `tests/e2e/crew-page.spec.ts` (modify — add the Playwright halves; the unit halves landed in Phase 2 Tasks 6/12/13)

> Phase 2 pinned the URL-building (test 13 tab-click unit), the preview-as swap (test 15 unit), and the footer report-prop port (test 19 at the shell). This task adds the **real-browser** halves: deep-link SSR + back-button history + scroll/no-blank-flash (test 13); the preview-as route actually renders `CrewShell` with a resolving `?s=` (test 15); the footer override id is present in the rendered preview-as DOM (test 19).

- [ ] Add to `tests/e2e/crew-page.spec.ts` (mobile-safari gated):
  - **Test 13 (nav addressability):** deep-link `/show/<seeded>/<token>?s=venue` → the Venue section SSRs (`[data-testid="section-venue"]` present on first paint, before hydration — assert via `page.goto` then immediate content check); tap the `schedule` tab → URL gains `?s=schedule` and the section swaps **without a full reload** (assert no navigation event / the `[data-testid="crew-shell"]` element identity persists); **back-button traverses section history** (today→venue→schedule, `page.goBack()` twice → venue, then off-page); refresh holds the section; a deep-link `?s=venue&gate=skip` keeps **both** params after the picker round-trip; a **tab-click** from `?s=venue&gate=skip` retains `gate=skip` (URL has both); section change resets scroll to top AND the prior section stays mounted during the round-trip (no blank flash — assert the outgoing section is still in the DOM until the incoming commits). _Catches: client-only state; broken/clobbered back-button; `s`/`gate` clobber; blank-flash on nav._
  - **Test 15 (preview-as parity):** sign in as admin, navigate `/admin/show/<slug>/preview/<crewId>?s=venue` → `[data-testid="crew-shell"]` renders (NOT the old flat-grid `[data-testid="tile-grid"]`), `[data-testid="section-venue"]` resolves, and the `PreviewBanner` (`[data-testid="admin-preview-banner"]`) sits above. Default (no `?s=`) → `[data-testid="section-today"]`. _Catches: preview-as left on the old flat-grid `ShowBody`._
  - **Test 19 (footer report metadata, preview-as):** on the preview-as route, read the Footer report metadata in the DOM → assert `reportSurfaceIdOverride` is present as `admin-preview-footer-<slug>-<crewId>` (via the data attribute the Footer renders / the report autocapture payload); on a normal crew route assert the override id is absent. _Catches: preview-as bug reports mis-filed as generic crew-surface reports._
  - **Test 36(b) (projection alert fires on render, never on prefetch — §4.13/§4.1a; deferred here from Phase 2 Task 11).** This is **structural**, two assertions (the §9 form: "assert structurally that the dynamic render does not run on a prefetch path, and that section nav uses `router.push` / `prefetch={false}`"). Add a unit test `tests/components/crew/noPrefetchAlert.test.tsx`: **(i)** `readFileSync('components/crew/CrewSubNav.tsx')` → assert section activation goes through `router.push` (or any `<Link>` to a `?s=` URL carries `prefetch={false}`) — there is **no** auto-prefetching `<Link>` to a section URL (a prefetch of a section URL must not be triggerable from the sub-nav). **(ii)** assert the route is **dynamic**: `readFileSync('app/show/[slug]/[shareToken]/page.tsx')` (and the preview-as page) reads a request-scoped input that forces dynamic rendering (the picker cookie via `cookies()` and/or `X-Screenshot-Frozen-Now` via `headers()`, or an explicit `export const dynamic = "force-dynamic"`) — so Next renders only the static `loading.tsx` boundary on prefetch and never runs the `_CrewShell` render (and thus never the `upsertAdminAlert` side-effect). State in the test body **why** this proves no phantom prefetch alert: the upsert is a side-effect of the `_CrewShell` render, which a dynamic route does not execute during prefetch. _Catches: the projection alert firing on a prefetch render (phantom alerts / `last_seen_at` churn for a section the user never opened); a `<Link prefetch>` to a section URL or a statically-prerenderable route making prefetch run the dynamic render + write._
- [ ] Run to fail: `pnpm vitest run tests/components/crew/noPrefetchAlert.test.tsx && pnpm exec playwright test tests/e2e/crew-page.spec.ts --project=mobile-safari -g "nav addressability|preview-as|footer report"`.
- [ ] Fix any real defect surfaced (a missing `data-testid`, a non-SSR section, a dropped param). Most ports landed in Phase 2/3 — this is the integration proof.
- [ ] Run to pass. `pnpm tsc --noEmit`. Commit `test(crew-page): real-browser nav addressability + preview-as parity + footer report metadata + prefetch-no-alert (test 36b)`.

---

### Task 4: Crew screenshot MANIFEST entries + client-clock freeze (manifest only — no baselines yet)

**Files:** `scripts/help-screenshots.manifest.ts` (modify), `tests/help/_metaScreenshotManifest.test.ts` (modify — crew-key MDX-consumer exemption, see Task 6 note), `tests/help/screenshot-coverage.test.ts` (modify if its Half-A direction also gates crew keys)

> §4.11: add `crew-today-mobile`, `crew-gear-mobile`, `crew-schedule-mobile` to `MANIFEST`. Route `/show/<seeded>/<token>?s=<section>`, **MOBILE 390×844**, `captureSelector: "[data-testid=crew-shell]"`, `frozenClockInstant` chosen to land a `show_day_n` state on the seeded fixture. The capture harness already freezes BOTH clocks (`scripts/help-screenshots.ts:191` `context.clock.install` for the hero's client `new Date()` + `:198-199` `X-Screenshot-Frozen-Now` header for the server Schedule pin) and consumes `CAPTURE_LAUNCH_ARGS` (`:224-225`) — **no per-config `launchOptions`** (the PR #22 trap). This task lands the manifest entries + their meta-test gating; baselines land in Task 5 (separated so the byte-comparison discipline is its own reviewable step).
>
> **The seeded crew route needs a slug + a DETERMINISTIC shareToken + a `show_day_n` instant (R5-MEDIUM-2 — the token must be deterministic or the capture 404s/times out).** The live seed creates `show_share_tokens` via the DB default/trigger using `gen_random_bytes` — **non-deterministic**, so a hardcoded or `stableUuid`-derived token in the manifest will NOT match the reseeded DB and `scripts/help-screenshots.ts` (which navigates `entry.route` literally) will 404 / hang on `[data-testid=crew-shell]`. **Concrete fix — pin a deterministic share token in the seed:**
> - In `supabase/seed.ts`, for the `RPAS_CENTRAL_2026` screenshot fixture show ONLY, **explicitly insert a fixed `share_token`** (override the `gen_random_bytes` default) — a constant like `SCREENSHOT_CREW_TOKEN` that satisfies the `TOKEN_RE` format (grep `TOKEN_RE` for the exact charset/length, e.g. `lib/auth/picker/clearIdentity.ts`). Export the constant (or re-declare it identically in the manifest).
> - Add a seed-parity test (extend the existing seed/fixture test) asserting `select share_token from show_share_tokens where show_id = <fixture>` equals `SCREENSHOT_CREW_TOKEN` — so a future seed change that drops the override fails CI (not a silent screenshot 404).
> - The manifest uses `route: \`/show/${RPAS_CENTRAL_2026_SLUG}/${SCREENSHOT_CREW_TOKEN}?s=...\`` — both named constants, no literal. Reuse `MID_SHOW_INSTANT = "2026-03-24T15:00:00.000Z"` (`:33`) for `frozenClockInstant` (mid-show for that fixture). (If the seed cannot set a deterministic token without breaking the picker-epoch rotation, fall back to: extend `scripts/help-screenshots.ts` to RESOLVE the live seeded token for the fixture show before navigating — query `show_share_tokens` at capture time and substitute into the route. Pick ONE approach and pin it with a test; do not leave the token source unspecified.)

- [ ] Implement the deterministic token source (seed override + parity test) per the note above BEFORE adding the manifest entries.
- [ ] **Capture as a CREW viewer, NOT the admin branch (R14-MEDIUM — the load-bearing one).** `scripts/help-screenshots.ts:197` runs `signInAs(page, ADMIN_FIXTURE)` before EVERY capture, and `resolveShowPageAccess` returns the **admin** arm for an admin session — so a naive `/show/<slug>/<token>` capture would render `CrewShell` with `viewer.kind === "admin"` (wrong Budget gating, wrong flags, no crew identity chip): the drift gate would baseline the ADMIN view and pass while the real crew mobile UI regresses. Make the crew captures render the **crew arm**:
  - **Primary (faithful crew render):** for the crew manifest entries, the harness must establish a **crew picker session** instead of (or after clearing) the admin auth — set the `__Host-fxav_picker` cookie (`lib/auth/picker/cookieEnvelope.ts:4` `COOKIE_NAME`; sign the envelope for a **seeded, deterministic, NON-LEAD** crew member on the `RPAS_CENTRAL_2026` show) before navigating to `/show/<slug>/<token>?s=...`. This requires: (a) the seed includes a deterministic non-lead crew member (pin its id like the preview crew id, `scripts/help-screenshots.manifest.ts:38-48`); (b) per-entry auth in `help-screenshots.ts` (a `viewer: "crew" | "admin"` field on the manifest entry, or a separate crew-capture path) that sets the picker cookie + does NOT `signInAs(ADMIN_FIXTURE)` for crew entries. The result renders the true crew experience: identity chip present, NO admin-preview banner, Budget tab absent (non-lead), crew section gating.
  - **Acceptable fallback (document the delta):** if the picker-cookie harness change is too invasive for this phase, capture the crew sections via the **admin-preview route** `/admin/show/<slug>/preview/<crewId>` (admin-authed, which the harness already is) for a seeded **non-lead** crewId — this renders `CrewShell` as the previewed crew (`admin_preview` → `isAdmin=false`, the crew's flags, so Budget/gating ARE the crew experience), at the cost of an added `PreviewBanner` + `identityChip=null` (a documented header delta). Name the entries `crew-preview-*` if using this path so the baseline's surface is honest.
  - **GUARD (mandatory either way):** add an assertion (in `tests/help/_metaScreenshotManifest.test.ts` or a capture smoke test) that the `crew-*` baselines render the intended **non-admin** viewer state — the captured/rendered DOM contains the crew identity chip (primary path) OR the preview banner (fallback path), and for a non-lead fixture the Budget tab is **absent** — so the test FAILS if a crew route is captured through the plain admin branch (`viewer.kind === "admin"`, Budget tab present, no chip/banner).
- [ ] Add three entries to `MANIFEST` (`scripts/help-screenshots.manifest.ts:50-87`), pattern-matching the existing `MOBILE` + `captureSelector` shape. Use the named `RPAS_CENTRAL_2026_SLUG` + `SCREENSHOT_CREW_TOKEN` constants (no hardcoded literal). _Failure mode: a non-deterministic seed token making the route 404 / time out on `[data-testid=crew-shell]`; a per-config `launchOptions` that never reaches the script's `chromium.launch` (PR #22); a hero baseline that drifts because the client clock wasn't frozen; a `frozenClockInstant` outside the fixture range (`scripts/help-screenshots.ts:58` throws)._
  - `crew-today-mobile`: route `/show/${SEEDED_CREW_SLUG}/${SEEDED_CREW_TOKEN}?s=today`, fixture `RPAS_CENTRAL_2026`, `frozenClockInstant: MID_SHOW_INSTANT`, `viewport: MOBILE`, `captureSelector: "[data-testid=crew-shell]"`.
  - `crew-gear-mobile`: same route with `?s=gear` (the highest-deviation section).
  - `crew-schedule-mobile`: same route with `?s=schedule`.
- [ ] Update `tests/help/_metaScreenshotManifest.test.ts` so the new crew keys pass: the route-resolves-to-App-Router-page test (`:106-111`) — `/show/[slug]/[shareToken]` IS a real App Router page, so the crew routes resolve; the fixture-resolves test (`:98-103`) — `RPAS_CENTRAL_2026` already resolves. **THE BLOCKER:** the `every manifest key is consumed by at least one help MDX <Screenshot name>` test (`:168-176`) and `screenshot-coverage.test.ts` Half A — spec §4.11 says **"No /help MDX change required (crew page is not in the admin help tree); manifest + drift CI only."** Resolve by adding a documented `CREW_DRIFT_ONLY_KEYS` exemption set (the three crew keys) excluded from the MDX-consumer assertion, with a comment citing §4.11 ("crew-page screenshots are drift-CI baselines, not help-MDX-embedded — they have no `<Screenshot name>` consumer by design"). **Surface this to the user as a spec-vs-meta-test tension (see final summary) — do not silently widen the exemption beyond these three keys.**
- [ ] Run to pass (manifest-shape + manifest-integrity, baselines not yet captured so the WebP-existence assertions short-circuit on `screenshotsDirActive()`): `pnpm vitest run tests/help/_metaScreenshotManifest.test.ts tests/help/manifest-shape.test.ts tests/help/screenshot-coverage.test.ts tests/help/capture-script.test.ts`. `pnpm tsc --noEmit`. Commit `feat(crew-page): crew-today/gear/schedule-mobile screenshot manifest entries (drift-CI only, §4.11 exemption)`.

---

### Task 5: Generate crew baselines via pinned-docker amd64 + verify drift gate

**Files:** `public/help/screenshots/crew-{today,gear,schedule}-mobile-{light,dark}.webp` (new — 6 committed WebPs)

> **Byte-comparison CI gate discipline (AGENTS.md / `memory/feedback_byte_comparison_ci_gates_pin_capture_environment.md`):** baselines MUST be generated from the PINNED Docker image (`mcr.microsoft.com/playwright:v1.59.1-jammy`) with `--platform linux/amd64`, **never from this dev host** (macOS/arm64 produces different bytes from the x64-Linux CI runner even with the identical image tag — Rosetta vs arm64-native raster paths diverge). The `screenshots-drift.yml` job (`.github/workflows/screenshots-drift.yml:39-45`) runs `pnpm screenshot:help` inside exactly that image and `git diff --exit-code public/help/screenshots/`. Generate the baselines the same way the gate verifies them.

- [ ] Generate the 6 crew WebPs by running the capture inside the pinned image on an amd64 surface, mirroring the workflow's invocation exactly (so the committed bytes match what CI will re-capture):
  ```bash
  docker run --rm --platform linux/amd64 --network host \
    -v "$PWD:/work" -w /work -e CI=true \
    mcr.microsoft.com/playwright:v1.59.1-jammy \
    bash -lc "apt-get update && apt-get install -y postgresql-client && corepack enable && pnpm install --frozen-lockfile && bash scripts/ci/supabase-local-bootstrap.sh && pnpm screenshot:help"
  ```
  (Local Supabase must be seeded inside the container per the workflow's `supabase-local-bootstrap.sh` step so the seeded crew slug/token resolve; the seed runs `db:seed`.)
- [ ] Stage ONLY the six new crew WebPs: `git add public/help/screenshots/crew-today-mobile-*.webp public/help/screenshots/crew-gear-mobile-*.webp public/help/screenshots/crew-schedule-mobile-*.webp`. Confirm `git status` shows no other WebP changed (a changed admin baseline means a host-architecture re-render polluted the tree — `git restore` it).
- [ ] Verify the manifest WebP-existence + light/dark-byte-distinct assertions now pass: `pnpm vitest run tests/help/_metaScreenshotManifest.test.ts tests/help/screenshot-coverage.test.ts` (the `screenshotsDirActive()`-gated checks at `:114-125`/`:128-152`/`:154-165` now run against the new files; light/dark must be byte-distinct — the hero/section has theme-dependent color).
- [ ] **`local capture overwrites the x64-Linux baseline → git restore` caveat:** if you EVER run `pnpm screenshot:help` directly on this macOS/arm64 host (e.g. to preview), it overwrites the committed x64-Linux WebPs with host-architecture bytes that look like a diff but are not a real change. After any local capture, restore the committed bytes: `git restore public/help/screenshots/`. Only the pinned-docker amd64 capture produces commit-correct bytes.
- [ ] Commit `chore(crew-page): pinned-docker amd64 baselines for crew-today/gear/schedule-mobile`.

---

### Task 6: Resolve the screenshot-manifest MDX-consumer meta-test tension (consolidates with Task 4 if landed together)

**Files:** `tests/help/_metaScreenshotManifest.test.ts` (modify), `tests/help/screenshot-coverage.test.ts` (modify), `DEFERRED.md` or the milestone handoff §12 (document the disposition)

> **This is the one genuine spec-vs-live-code conflict in Phase 4** (flagged in the summary). `_metaScreenshotManifest.test.ts:168-176` asserts *every* manifest key has a help-MDX `<Screenshot name>` consumer; `screenshot-coverage.test.ts:50-69` asserts the inverse (every `<Screenshot>` ref resolves to a manifest key — that direction is satisfied automatically, no MDX ref → no violation). Spec §4.11 explicitly says crew screenshots are **drift-CI-only** with **no MDX change**. If Task 4 already landed the `CREW_DRIFT_ONLY_KEYS` exemption, this task only documents the disposition + adds a structural guard that the exemption stays bounded; if Task 4 deferred the exemption, land it here first.

- [ ] Confirm the `CREW_DRIFT_ONLY_KEYS` exemption (the three crew keys) is the SOLE delta to `_metaScreenshotManifest.test.ts:168` — assert in a small meta-meta check that the exemption set equals exactly the three crew keys (so a future drift-only key must be added consciously, not by accident). _Failure mode: the exemption silently grows to hide a real admin screenshot that lost its MDX consumer._
- [ ] Document the disposition in the milestone handoff §12 (findings + dispositions): "Spec §4.11 ratifies crew screenshots as drift-CI-only (no help-MDX home, since the crew page is not in the admin help tree). The `every manifest key is consumed by a help MDX <Screenshot name>` invariant is narrowed to admin keys via `CREW_DRIFT_ONLY_KEYS`; drift coverage is preserved by `screenshots-drift.yml` (the `git diff --exit-code` byte gate runs regardless of MDX consumption)."
- [ ] Run to pass: `pnpm vitest run tests/help/`. `pnpm tsc --noEmit`. Commit `test(crew-page): bound crew screenshot keys to a drift-CI-only exemption (§4.11)`.

---

### Task 7: Retarget the tile test suite to the new section/primitive components — KEEP GREEN (wp-20 step b; tests 27/26/14-glyph + sentinel)

**Files:** `tests/components/tiles/CardinalityCapBoundary.test.tsx` (modify → retarget to sections/primitives), `tests/components/tiles/ScopeTileIcons.test.tsx` (retarget → Gear scope cards), `tests/components/tiles/SentinelHidingClass.test.tsx` (retarget → sections), `tests/components/tiles/_pureRenderCompliance.test.ts` (retarget walk), the `OpeningReelTile.test.tsx` URL-strip assertions (retarget → Gear opening-reel block); KEEP `tests/components/tiles/OpeningReelVideo.test.tsx` (player retained) + `tests/components/tiles/_metaSentinelHidingContract.test.ts` (already walks `components/crew/` from Phase 2)

> **This is the structural guarantee (wp-22) — the tile tests carry years of hardened Codex-round micro-contracts; retargeting them to the new components and keeping them GREEN is how every tile-level contract is preserved-or-CI-fails.** Do this BEFORE any `*Tile.tsx` is deleted (Task 8). Each retargeted test now renders the new section/primitive instead of the old tile, asserting the SAME contract against the new DOM (derive expected from the fixture; anti-tautology — assert against the data source / clone-and-strip sibling label nodes).

- [ ] **`CardinalityCapBoundary.test.tsx` → retarget the cap-1/cap/cap+1 matrix (test 27)** to the new homes: Crew roster cap 8 (`CREW_INLINE_CAP`) in the Crew section; Key contacts cap 6 (`CONTACTS_INLINE_CAP`, stub `data-testid="contacts-overflow-stub"`) in Crew; Show notes 8-source (`SOURCE_CAP`) + 280-char (`TRUNCATE_AT`, `data-testid="notes-item-truncated"`, `<details>`) in Today; Pack-list cap 12 (`CASE_CAP`) in Gear. Assert ≤cap → no overflow affordance; >cap → exactly `cap` inline + the stub with count `= length − cap` (never hardcoded; the affordance appears at `> cap`, not `>= cap`). _Failure mode: unbounded mobile scrolls / lost overflow + truncation affordances after the tile views are deleted._
- [ ] **`OpeningReelTile.test.tsx` URL-strip → retarget (test 26)** to the Gear opening-reel block: `event_details.opening_reel` carrying a Drive/Docs URL → the rendered crew DOM contains **no** `https://`, `drive.google.com`, or `docs.google.com` substring (via `stripOpeningReelText`); the proxied `OpeningReelVideo` player still renders the video. _Failure mode: leaking raw Drive URLs when porting off `OpeningReelTile`._
- [ ] **`ScopeTileIcons.test.tsx` → retarget (test 14 glyph row / §4.16)** to the Gear scope cards: Audio/Video/Lighting keep distinct icons (`Volume2`/`Video`/`Lightbulb`). _Failure mode: the three scope cards becoming visually indistinguishable._
- [ ] **`SentinelHidingClass.test.tsx` + `_pureRenderCompliance.test.ts` → retarget the walk** to the new section/primitive components (the `_metaSentinelHidingContract` directory walk already extended in Phase 2; this is the per-field rendering assertion suite). Keep `OpeningReelVideo.test.tsx` unchanged (the player is KEPT).
- [ ] **Move `components/tiles/OpeningReelVideo.tsx`** to its Gear home if cleaner (`components/crew/` per 00-overview) and re-point its importer (the Gear opening-reel block) + `OpeningReelVideo.test.tsx`. If moving, update the import path everywhere; if leaving in place, keep the import as-is (00-overview allows either — "move to `components/crew/` if cleaner").
- [ ] Run to fail then pass each retargeted suite: `pnpm vitest run tests/components/tiles/`. _The whole `tests/components/tiles/` directory must be GREEN against the new components with NO `*Tile.tsx` View shell deleted yet — that green state is the precondition for Task 8._
- [ ] `pnpm tsc --noEmit`. Commit `test(crew-page): retarget tile test suite to new sections/primitives (keep green pre-deletion)`.

---

### Task 8: Delete the 14 `*Tile.tsx` shells + `_ShowBody.tsx` + `selectTodayTiles.ts` (wp-20 step c — ONLY after Tasks 7 + Phase-2 prereqs green)

**Files (delete):** `components/tiles/{AudioScopeTile,VideoScopeTile,LightingScopeTile,ContactsTile,CrewTile,DiagramsTile,FinancialsTile,LodgingTile,NotesTile,OpeningReelTile,PackListTile,ScheduleTile,ShowStatusTile,TransportTile,VenueTile}.tsx`, `app/show/[slug]/[shareToken]/_ShowBody.tsx`, `lib/show/selectTodayTiles.ts`. **KEEP:** `components/tiles/OpeningReelVideo.tsx` (or its moved location), all `components/shared/` error infra, all `lib/` `load*` helpers. **Files (delete/retarget tests):** `tests/show/selectTodayTiles.test.ts`, and any of `tests/show/resolvedArmCrewMembersGuard.test.tsx` / `tests/show/page-today-uses-now-utility.test.ts` / `tests/components/_metaPickerRoleChipContract.test.ts` that reference the deleted modules.

> **Pre-delete gate (verify ALL true before deleting a single file):**
> 1. Task 7 GREEN — the retargeted tile test suite passes against the new components.
> 2. **Phase-2 prerequisites confirmed live (re-run, do not assume):** the server-side projection-alert upsert (Phase 2 Task 11, test 16) is green; the `TILE_PROJECTION_FETCH_FAILED` four-part lockstep (Phase 2 Task 8) is green incl. `x1-catalog-parity`; `_metaAdminAlertCatalog` + `_metaSentinelHidingContract` extensions are green; the `upsert_admin_alert` failedKeys-merge migration + `tests/db/upsert-admin-alert-dedup.test.ts` are green **against the validation project**. _This is the §4.13/§10 sequencing spine: the section-independent observability MUST be live before `_ShowBody.tsx`'s always-rendered `notes-tile` alert path is removed._
> 3. Nothing imports the deletion targets except the deletion targets themselves (grep before delete).
> 4. **Field-port + containment ports green (R11):** the **Header status-pill** port (Phase-2 Task 14, D-2/wp-18 — `ShowStatusTile`'s pill has a home) and the **per-block `WrappedSection` error boundaries** (Phase-3 Task 9, §4.13/wp-13 — every throwable section block keeps `TILE_SERVER_RENDER_FAILED` containment) are both green. Deleting `ShowStatusTile` / the tile shells before these would drop the status pill (field-port regression) and/or lose per-block render-throw containment.

- [ ] **Confirm the pre-delete gate.** Run: `pnpm vitest run tests/components/tiles/ tests/components/crew/crewShell.test.tsx tests/messages/_metaAdminAlertCatalog.test.ts tests/cross-cutting/codes.test.ts tests/admin/upsertAdminAlert.test.ts` and `TEST_DATABASE_URL=<validation> pnpm vitest run tests/db/upsert-admin-alert-dedup.test.ts`. ALL green. Then `grep -rln "selectTodayTiles\|_ShowBody\|from \"@/components/tiles/\(Audio\|Video\|Lighting\)ScopeTile\|ContactsTile\|CrewTile\|DiagramsTile\|FinancialsTile\|LodgingTile\|NotesTile\|OpeningReelTile\|PackListTile\|ScheduleTile\|ShowStatusTile\|TransportTile\|VenueTile" app components lib tests` — every remaining hit must be a deletion target or a test being deleted/retargeted in this task.
- [ ] **Write the failing structural-deletion test FIRST (R2-HIGH-2 — TDD-per-task, invariant 1).** Create `tests/migration/crew-redesign-cleanup.test.ts` as the pre-written contract the deletion satisfies:
  ```ts
  import { existsSync } from "node:fs";
  import { execSync } from "node:child_process";
  const DELETED = [
    ...["AudioScope","VideoScope","LightingScope","Contacts","Crew","Diagrams","Financials","Lodging","Notes","OpeningReel","PackList","Schedule","ShowStatus","Transport","Venue"].map((t) => `components/tiles/${t}Tile.tsx`),
    "app/show/[slug]/[shareToken]/_ShowBody.tsx", "lib/show/selectTodayTiles.ts",
  ];
  const RETAINED = [
    "components/tiles/OpeningReelVideo.tsx", // (or its moved location — update if moved into components/crew/)
    "components/shared/WrappedTile.tsx", "components/shared/TileServerFallback.tsx",
  ];
  test("obsolete tile shells / _ShowBody / selectTodayTiles are deleted", () => {
    expect(DELETED.filter((p) => existsSync(p))).toEqual([]);
  });
  test("retained modules survive the migration (no over-deletion)", () => {
    expect(RETAINED.filter((p) => !existsSync(p))).toEqual(RETAINED.filter(() => false)); // all present
    for (const p of RETAINED) expect(existsSync(p)).toBe(true);
  });
  test("no source/test file imports a deleted module", () => {
    // ripgrep returns non-zero (exit 1) when there are NO matches — that is the PASS condition.
    // EXCLUDE this test's OWN file (R3-MEDIUM-2): it contains the module names in its DELETED list +
    // this regex, so without the glob it would self-match forever after the real modules are deleted.
    // Scan only IMPORT statements (`from "..."` / `import(...)`) so a stray mention can't false-positive.
    const shells = ["AudioScope","VideoScope","LightingScope","Contacts","Crew","Diagrams","Financials","Lodging","Notes","OpeningReel","PackList","Schedule","ShowStatus","Transport","Venue"].join("|");
    let out = ""; try {
      out = execSync(
        `rg -l --glob '!tests/migration/crew-redesign-cleanup.test.ts' ` +
        `"(from\\s+['\\"]|import\\(['\\"])(@/)?(lib/show/selectTodayTiles|app/show/\\[slug\\]/\\[shareToken\\]/_ShowBody|components/tiles/(${shells})Tile)" ` +
        `app components lib tests`,
        { encoding: "utf8" }
      );
    } catch { out = ""; }
    expect(out.trim()).toBe("");
  });
  ```
  _Failure mode (what this catches): orphaned imports of a deleted module; accidental over-deletion of retained infra (`OpeningReelVideo`, `WrappedTile`, `TileServerFallback`); a tile shell left behind. This is the pre-written contract — it FAILS now (old files present) and turns green only when the deletion + retarget is complete and clean._
- [ ] Run to verify it FAILS (old files still present): `pnpm vitest run tests/migration/crew-redesign-cleanup.test.ts` → FAIL (the `DELETED` files exist; imports remain).
- [ ] Delete the 14 `*Tile.tsx` files + `_ShowBody.tsx` + `lib/show/selectTodayTiles.ts`. Delete `tests/show/selectTodayTiles.test.ts`. Retarget or delete the other test files that referenced the deleted modules (retarget if they assert a contract the new components still own — that retarget already happened in Task 7; delete if the contract is gone with the old TODAY-band promotion — e.g. `page-today-uses-now-utility.test.ts`'s `selectTodayTiles` coupling is superseded by the hero's client clock, wp-18). If `OpeningReelVideo.tsx` was moved, update the `RETAINED` path in the test.
- [ ] Run to verify it PASSES: `pnpm vitest run tests/migration/crew-redesign-cleanup.test.ts` → PASS (deleted absent, retained present, no imports).
- [ ] **Verify test 29 post-migration** (two distinct alert codes, no clobber): with `_ShowBody.tsx` gone and `_CrewShell` the sole projection-alert producer, render `_CrewShell` with BOTH a projection `tileErrors` AND a wrapped-block render throw in one render → **two** distinct unresolved `admin_alerts` rows (`TILE_PROJECTION_FETCH_FAILED` with its `failedKeys` + `TILE_SERVER_RENDER_FAILED` with its `tileId`), neither clobbering the other's context; repeated same-`failedKeys` renders do not inflate `occurrence_count` beyond the 10-min debounce window. If a `tests/components/crew/` test does not already pin this against the post-deletion code, add it here. _Failure mode: deleting `_ShowBody.tsx`'s `notes-tile` path silently losing the always-on projection alert; a shared code clobbering one context; occurrence inflation._
- [ ] Run to pass: `pnpm vitest run` (full suite) + `pnpm tsc --noEmit` + `pnpm build` (no orphaned imports, no build break, no dropped Opening Reel media — wp-20). Confirm `tests/e2e/crew-page.spec.ts` still passes (`--project=mobile-safari`).
- [ ] Commit `feat(crew-page): delete 14 tile shells + _ShowBody + selectTodayTiles (section-independent observability live)`.

---

### Task 9: Milestone close-out — impeccable dual-gate + adversarial review + real-CI + merge

**Files:** the milestone handoff doc §12 (findings + dispositions), `DEFERRED.md` (any deferred findings)

> **This is its own gate — a whole-milestone fresh-eyes pass, distinct from the per-task gates** (AGENTS.md / `memory/feedback_whole_milestone_closeout_gate.md`). Per-task reviews catch surface bugs in scope; integration bugs (cross-section composition, copy violations on shipped surfaces, missing guards, cross-phase contradictions) need a separate whole-milestone pass. Order (invariant 8): impeccable dual-gate FIRST (before adversarial), then cross-model adversarial review, then real-CI, then merge.

- [ ] **Impeccable v3 dual-gate (invariant 8) — EXTERNAL attestation, NOT self-attestation** (`memory/feedback_impeccable_external_attestation_required.md`): run `/impeccable critique` AND `/impeccable audit` on the crew-redesign UI diff (every file under `app/show/[slug]/[shareToken]/**` except none-here-are-api, `components/crew/**`, `app/show/[slug]/[shareToken]/loading.tsx`, any `app/globals.css` `@theme` token added, `DESIGN.md` changes) via a **fresh subagent or user-invoked** session (the Opus session that wrote the UI cannot self-attest). Run both with the canonical v3 preflight gates (PRODUCT.md → DESIGN.md → register → preflight signal). HIGH + CRITICAL findings are fixed OR explicitly deferred via a `DEFERRED.md` entry. Record findings + dispositions in the handoff §12. _This runs BEFORE adversarial review._
- [ ] **Spec-check any impeccable copy/label rewrite** before shipping it (`memory/feedback_impeccable_critique_not_authoritative_vs_spec.md`): critique knows UX, not the product contracts (the §4.3 hero copy map, the §4.18 no-em-dash rule, the catalog `helpfulContext`). A critique rewrite that contradicts the spec is dispositioned "keep spec copy," cited.
- [ ] **Cross-model adversarial review (whole-milestone, fresh-eyes)** — invoke the `adversarial-review` skill (Codex, since the implementer is Claude). Brief: fresh-eyes posture ("treat the entire 4-phase diff as if unseen"); scope = the whole crew-redesign milestone (Phases 1-4); the `EXPLICITLY DO NOT RELITIGATE` block citing the spec's do-not-relitigate watchpoints at `file:line` — **wp-27** (the projection-alert write is render-bound server-side by design; a finding asking to move it client-side / server-action / signed-ticket / commit-bound is out of contract — re-introduces the reverted R30–R38 cascade), **wp-4** (Gear shows all scope to everyone — ratified, not a leak), **wp-2** (Doors / labeled Crew-call have no source — intentionally dropped), **wp-8** (`dates.loadIn` in jsonb, no table migration — intended), **wp-20** (file-by-file migration with `OpeningReelVideo` kept — intended, not a scope cut); REVIEWER-ONLY framing ("do not fix; surface findings — fixes are a separate implementer dispatch"). Iterate repair-rounds until APPROVE (repair-thread isolation: `--fresh` first round, `--resume-last` subsequent). Do NOT proceed to merge without APPROVE.
- [ ] **Real-CI green is its own close-out gate** (`memory/feedback_local_passes_ci_fails_is_its_own_bug_class.md`): push the branch; confirm GitHub Actions green INCLUDING `screenshots-drift` (the crew baselines from Task 5 must survive the in-CI re-capture byte gate — `git diff --exit-code public/help/screenshots/`). Use `workflow_dispatch` (`.github/workflows/screenshots-drift.yml:6`) to fire the drift job on demand if needed. Local + adversarial-APPROVE is necessary but NOT sufficient — real CI is a separate gate.
- [ ] **Merge to main** once impeccable dual-gate PASS + adversarial APPROVE + real-CI green. Open the PR (`commit-commands:commit-push-pr` or `gh`), body cites the four-phase scope + the do-not-relitigate dispositions + the §4.11 screenshot-MDX exemption (Task 6). Commit/PR per invariant 6 conventional-commits; PR body ends with the Claude Code attribution footer.

---

## Phase exit criteria + milestone close-out checklist

All GREEN before the milestone is marked closed in its handoff doc:

1. **Layout dimensions (test 12)** — `pnpm exec playwright test tests/e2e/crew-page.spec.ts --project=mobile-safari` passes; every §4.9 invariant (1-7, verbatim above) is asserted in a real browser with `getBoundingClientRect()` ±0.5px; jsdom is NOT used for any of them. The legacy today-band equal-height blocks are removed (subsumed by Today).
2. **Transition audit (test 14)** — `tests/components/crew/transitionAudit.test.tsx` enumerates every `AnimatePresence`/ternary/conditional in `CrewSectionTransition`/`RightNowHero`/`CrewSubNav` and pins each against the §4.10 inventory (verbatim above); the real-browser compound transitions (theme-toggle-during-nav, re-enter Today, hero-state-change-mid-swap) pass; no animating-from-hidden SSR (`initial={false}` everywhere); no animation wrapper on the instant-by-design rows.
3. **Nav addressability + preview-as parity + footer report metadata (tests 13/15/19)** — the real-browser halves pass: deep-link SSR + back-button history + scroll/no-blank-flash; preview-as renders `CrewShell` with a resolving `?s=`; the preview-as footer carries `admin-preview-footer-<slug>-<crewId>`.
4. **Screenshots** — `crew-today-mobile`/`crew-gear-mobile`/`crew-schedule-mobile` are in `MANIFEST` (MOBILE 390×844, `captureSelector=[data-testid=crew-shell]`, `show_day_n` `frozenClockInstant`); BOTH clocks frozen (client `page.clock`/`context.clock.install` + `X-Screenshot-Frozen-Now`); `CAPTURE_LAUNCH_ARGS` consumed (no per-config `launchOptions`); 6 baselines committed, generated from the PINNED `mcr.microsoft.com/playwright:v1.59.1-jammy --platform linux/amd64` image (NOT a dev host); the local-capture-overwrites caveat is documented; light/dark are byte-distinct.
5. **Screenshot-MDX tension resolved (Task 6)** — the `CREW_DRIFT_ONLY_KEYS` exemption is bounded to exactly the three crew keys; the disposition is documented in the handoff §12 citing §4.11; drift coverage is preserved by `screenshots-drift.yml`.
6. **Tile migration (wp-20, ordered)** — (a) `OpeningReelVideo` + `lib/` `load*` + `components/shared/` error infra KEPT; (b) the retargeted tile test suite GREEN against the new components BEFORE any deletion; (c) the 14 `*Tile.tsx` shells + `_ShowBody.tsx` + `lib/show/selectTodayTiles.ts` deleted, with the Phase-2 prerequisites confirmed live (projection-alert upsert + `TILE_PROJECTION_FETCH_FAILED` lockstep + meta-test extensions + the failedKeys-merge migration + validation-backed dedup test — §4.13/§10 sequencing); `pnpm build` + full `pnpm vitest run` + `pnpm tsc --noEmit` clean; no client island / server action / signing secret gated the deletion (wp-27).
7. **Test 29 verified post-migration** — two distinct alert codes (`TILE_PROJECTION_FETCH_FAILED` + `TILE_SERVER_RENDER_FAILED`) occupy separate unresolved rows with no clobber; same-`failedKeys` repeat renders do not inflate `occurrence_count` beyond the debounce window.
8. **Close-out gate (its own whole-milestone fresh-eyes pass)** — impeccable v3 dual-gate (critique + audit, **external** attestation) PASS on the UI diff with HIGH/CRITICAL fixed-or-deferred; cross-model adversarial review APPROVE (whole-milestone, with the wp-27/4/2/8/20 do-not-relitigate block); **real CI green including `screenshots-drift`**; then merged to main.
9. **Do-not-relitigate (wp-27 / §4.13):** the projection-alert write stays server-side render-bound in `_CrewShell`. No finding moving it client-side / server-action / signed-ticket / commit-bound is in contract. Cite this watchpoint if it recurs.
