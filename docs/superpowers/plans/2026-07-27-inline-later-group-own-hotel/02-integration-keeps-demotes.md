# Task 2: Integration — wire the detector; tier-1 keeps, tier-2 demotes, parity negatives

**Files:**

- Create: tests/parser/inlineLaterGroupOwnHotel.test.ts
- Modify: `lib/parser/blocks/hotels.ts` — `buildInlineReservations` later-segment region loop (the line-717 region; group 0 untouched) + D7 verdict wiring + degraded-segment tier-gate (row-observable demote; the evidence SCAN is Task 5)
- Modify: `docs/superpowers/specs/parser/2026-07-25-hotel-ambiguity-coverage-design.md` — §3.1 row 7 one-line amendment pointer (spec §6.3 row u; wording verbatim from feature spec line-200 region: `Amended by docs/superpowers/specs/parser/2026-07-27-inline-later-group-own-hotel-design.md §5: a tier-1 kept later group is standalone-parsed and its exit IS evaluated; inherited later groups remain never-emit.`) — the feature spec requires this pointer "in the same commit that changes the behavior"; THIS task's implementation commit is that commit
- Modify: `lib/parser/blocks/hotels.ts:756-767` region comment — rewrite the "later groups inherit" comment to describe the detector routing and cite the feature spec (feature spec line-200 companion requirement; same behavior-change commit)

**Interfaces:**

- Consumes: `classifyLaterSegment` / `LaterSegmentOutcome` (Task 1, exact signature in 01-detector-core.md); `parseHotels(markdown, _version, agg?)` public API (`lib/parser/blocks/hotels.ts:65-70`); `newAggregator` (`lib/parser/warnings.ts:22`).
- Produces: tier-1 later groups whose row is `outcome.build.row` with `hotel_name = outcome.hotelText`, `hotel_address = null`, then conf-stripped/split by the EXISTING per-row `stripHotelNameConf` pass (`lib/parser/blocks/hotels.ts:831` pattern, first-stash-wins line 845); D7 verdict wiring (a kept group's exit IS evaluated — existing `HOTEL_GUEST_SPLIT_AMBIGUOUS` stash path applies to the kept build, observable via the aggregator NOW); degraded-segment tier-gate (a later segment with ≥2 `Check In` matches on D1-normalized text demotes a tier-1-qualifying outcome to tier 2's inherit — row-observable). NO stash slots for the two NEW codes land here — stash + emit both land in Task 4 where they are warning-observable (plan R1 finding 3).

**Test harness (top of the new file):**

```ts
import { describe, expect, it } from "vitest";
import { parseHotels } from "@/lib/parser/blocks/hotels";
import { newAggregator } from "@/lib/parser/warnings";

const HEAD = "Hyatt Regency 100 Main St John Smith - 1001 Check In: 3/1/26 Check Out: 3/2/26 ";
const cell = (later: string) => `| Hotel Stays | ${HEAD}${later} |`;
const parse = (later: string) => {
  const agg = newAggregator();
  const rows = parseHotels(cell(later), "v1", agg);
  return { rows, warnings: agg.warnings };
};
const rowsOf = (later: string) => parse(later).rows;
```

(`newAggregator` + `agg.warnings.filter((w) => w.code === ...)` is the live corpus-golden pattern, `tests/parser/hotelAmbiguityCorpusGolden.test.ts:44-51`; `agg.warnings` is already `ParseWarning[]` via `lib/parser/warnings.ts:18` — no extra type import, `ParseWarning` lives in `lib/parser/types.ts:67` not warnings.ts (plan R2 f1). The aggregator import exists from Task 2 so the `describe.skip` warning-cardinality block typechecks the moment it is written; its assertions activate in Task 4.)

## Oracle groups (each `it()` = one spec §8.1 row, byte-exact input + assertions from the row)

Every row below names its spec §8.1 anchor (bold row name). The row text is normative: copy its later-segment input(s), its today-parse literals (for byte-parity demotes), and its assertion clauses verbatim. Assertions on kept rows check `hotel_name`, `hotel_address`, `names`, `check_in`, `check_out` against the row's stated literals; assertions on demotes check the FULL row array deep-equals the row's stated today-parse shape (derived from the row's probe clause), plus `NO row whose hotel_name/hotel_address contains <the row's buried-name tokens>`. Warning-cardinality clauses (`exactly one SUSPECTED@i`, `OWN@i`, `ZERO OWN`) go into the `describe.skip("warning cardinality (unskipped by Task 4)")` block with the same row names. EXISTING-code clauses (the 7b guest warning on a kept row) are ACTIVE now — the emitters already exist.

1. **Keeps (tier 1):** backlog clobber B1z; two-guest keep ×2 (hyphen + glued-em corpus form); tail keep (R12 f1); D4b tail arms — **Address tail, comma-less city (comma before state)** (the longer-match tie-break kill) AND **Address tail, fully comma-less US** (the arm-3 kill; two distinct §8.1 rows, plan R2 f4) / comma-led / Canadian / arm-3 cap-boundary (R19) / ZIP+4 ×2 / unit ×2; FL keeps ×2 (R37); exact-3 keeps ×2 (R37 f4); overlap keeps ×2 (R40); arm-1 syntax matrix (12 materializations, R45/R46); Florence keep (R40); zero-city keeps ×2 (R32); 3-word name-region keep; FOUR-word corpus-brand keep (R47); quoted corpus keep (R47); smart-quote keep (R48); below-threshold guest keep (R33 documented limit); divider strip ×3 dash forms NAME-asserted (R45); tier-1 ZIP+4 post-marker keep (R18 f2); ZIP+4-then-suffix-word keep (R52); dash-before-letters note keep (R52); position-0 all-address keep; entity-split final checkout keep (R26); **ZIP arm, numeric brand unsplit** (probe B6 cell — `hotel_name === "Hotel 71 Chicago, IL 60601"`, `hotel_address === null`; kills ZIP-match-splits-name implementations); **Lowercase Canadian POSTAL keeps** (R31 f2 FIRST branch — `m5v 2t6` with uppercase `ON` state token); **Lowercase-state glued booking KEEPS** (R30 f1 documented limit — glued Hilton carries ONLY lowercase-state postal text, outside every recognizer); **Bare-name POST-MARKER glue still KEEPS** (R20 f2 documented limit — segment fully tier-1-qualifying, evidence-less booking glued after its marker; `hotel_name === "Marriott Downtown"`, glued checkout visible on the kept row; OWN@1 clause in the skip block); **ZIP+4 rejection boundaries, tier-1 path — FIFTH materialization** (`99999-1234A` still KEEPS: `1234A` defeats the dash family's trailing `\b`).
2. **Demotes (tier 2):** suffix-only; city collision (R4); unconsumed postal (R2); ZIP-less city ×2 (R3); guard-(c) note text + conservatism; counterfeit-postal unit ×3 (R22); postal-then-trailing-unit (R22/R39); post-postal unit designation (R39); a0 bare-alias ×8 + dotted c0 pin (R41/R42); Steve alias-prefix (R34); hash counterfeit ×3 (R23); trailing-letter dash-run ×2 (R25); bare-conf ×2 (R24); dash-street ×2 (R27); guest-before-dash-street ×3 incl. en-dash (R27/R49); guests-inside-address ×3 (R36); MLK street conservatism (R36); ZIP-arm boundary 4-keep/5-demote (R40/R47); unconfirmed-guest five-word ×3 provenance paths (R31/R47); second-postal ×2 (R38); Doug c1 / Alice c2 (R32); suffixed-lc + lowercase-casing family (R30); glued-missing-checkin (R12 f2); conf-glue ×3 dash forms (R13/R49); hash/bare glue (R17); degraded-segment demote (R11 — the tier-gate lands in THIS task; a degraded later segment's tier-1-qualifying outcome demotes to inherit, row-observable); tab-entity-split third marker degrade (R36 f2 — the `Check&#9;In` third marker degrades its segment on D1 text; integration row moved here from Task 5, plan R11 f2); D4 smaller-index precedence (R16 f4); post-prefix street/postal-only ×2 (R16 f3); post-guest arm (R5); dash-glued street ×3 forms (R29/R49); word-glued street ×3 forms (R55); suffix-surname ×2 forms (R52 documented limit); digit-run prose ×2 forms (R54/R55 documented limit); **Unit tail then COMMA-LESS city lead-in DEMOTES** (R16 f1 shape, oracle flipped by the R17 f2 reversal — arm 2's mandatory comma rejects; plan R1 f4: this is a tier-2 row, NOT a keep); **Optional-comma guest-eating REJECTED** (R17 f2 — on NO kept row does `hotel_address` contain `John Smith`); **Bare unit-terminal demotes** (S8 — `Suite 400` consumed but no postal terminator); **Lowercase PROVINCE demotes** (R31 f2 SECOND branch — lowercase `on` state token defeats every anchor); **ZIP+4 rejection boundaries, tier-1 path — first FOUR materializations** (`99999-12345`, en dash, spaced separator, `123456-1234` — each demotes; kill-claims per the row's R43-f2 scoping).
3. **Parity negatives (byte-identical to today, zero new rows/changes):** bare-name post-marker GLUE-FREE negatives — **Bare-name post-guest hotel stays silent** (documented limit) and **Lowercase-state post-guest hotel stays tier-3 silent** (R30 f1 casing sibling); tier-2 word arm; tier-3 silence (B5 consultants shape); no-tier-1 parity (B5 + 2-group below-threshold cell); single-group cell untouched; entity-split second-marker routing negative (R24 f2); quote-split second-marker routing negative ×3 forms (R50); tab raw-gate parity (R39); row 0 always untouched; **Position-0 address in group 0** (R13 f3 pin — detector does not run on group 0); **Advertised remediation clears the warning** (R8/R18 f1 — the structured HOTEL table literal from the spec row parses to two clean reservations; row assertions active now, ZERO-new-codes clause in the skip block).

Concrete failure modes: group 1 kills every under-keeping mutant (each keep row names the specific mutant its assertions fail — e.g. a 3-cap guard (d), a straight-quote-only D1, an unrestricted or over-restricted neutralizer, a ZIP-splitting arm); group 2 kills every over-keeping/corrupting mutant (each demote row's kill-claim clause is part of the row); group 3 kills scope creep onto shipped single-group/routing/group-0 behavior.

## Steps

- [ ] **Step 1: Write the failing integration tests** — the harness above + one `it()` per row listed, in three `describe` blocks (`keeps`, `demotes`, `parity negatives`), plus the `describe.skip("warning cardinality (unskipped by Task 4)")` block containing the per-row NEW-code warning-count assertions (`parse(later).warnings.filter((w) => w.code === "...").length === n` — never `.some()`). Inputs and expected literals copied from the spec rows. The 7b kept-row guest-warning assertions (existing code) are ACTIVE, not skipped.
- [ ] **Step 2: Run, verify the red/green split** — `pnpm vitest run tests/parser/inlineLaterGroupOwnHotel.test.ts`. Expected: every KEEP row FAILS (detector unwired — the task's red); demote rows and parity negatives PASS ALREADY (they pin today's byte behavior — they are regression pins, not this task's red; plan R1 finding 3 wording corrected). The task's red→green evidence is the keep group plus the demote rows' warning clauses activating in Task 4.
- [ ] **Step 3: Commit failing tests** — `git add tests/parser/inlineLaterGroupOwnHotel.test.ts && git commit -m "test(parser): integration oracles for inline later-group own-hotel keeps/demotes/parity"`
- [ ] **Step 4: Wire the detector** — in `buildInlineReservations`' later-segment loop: call `classifyLaterSegment(rawSegment, ordinal, contextYear)` BEFORE the all-names guard (lines 735-753); tier 1 replaces the group's provisional row with `build.row` + `hotel_name = hotelText` / `hotel_address = null`; tier 2 keeps today's inherit; tier 3 unchanged. Degraded segments (≥2 markers on D1 text) demote tier-1-qualifying outcomes (spec scope A tier-gate; the evidence scan itself is Task 5). Group 0 and single-group cells never call the detector. Rewrite the lines 756-767 comment block; add the §3.1 row-7 pointer line to the parent spec.
- [ ] **Step 5: Run to green** — `pnpm vitest run tests/parser/inlineLaterGroupOwnHotel.test.ts tests/parser/inlineLaterGroupDetector.test.ts`. Expected: PASS (skip block still skipped).
- [ ] **Step 6: Full parser suite + typecheck + parent-spec lint** — `pnpm typecheck && pnpm vitest run tests/parser && pnpm spec:lint docs/superpowers/specs/parser/2026-07-25-hotel-ambiguity-coverage-design.md`. Expected: PASS / 0 hard (corpus goldens unchanged — no fixture reaches tier 1/2 per §9).
- [ ] **Step 7: Commit** — `git add lib/parser/blocks/hotels.ts docs/superpowers/specs/parser/2026-07-25-hotel-ambiguity-coverage-design.md && git commit -m "feat(parser): wire classifyLaterSegment into buildInlineReservations (tier-1 keeps + parent-spec row-7 pointer)"`
