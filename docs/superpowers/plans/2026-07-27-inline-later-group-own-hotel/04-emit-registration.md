# Task 4: Emit + registration — stash kinds, two emitters, full §6.3 fan-out, ONE implementation commit

Plan R1 findings 1–3 merged the old Tasks 4+5: the walker's `isMessageCode` gate (`tests/parser/_metaTransformSitesWalker.test.ts:81-90`) means `TRANSFORM_SITES` rows cannot land before catalog registration; `scripts/extract-internal-code-enums.ts:68-74` scans `lib/parser` for LITERAL `code: "..."` properties, so `pnpm gen:internal-code-enums` produces the new codes only AFTER the emitters exist; and the three-lockstep rule (AGENTS.md) binds §12.4 + `gen:spec-codes` + catalog into one commit. Every same-commit clause therefore resolves into a SINGLE implementation commit carrying stashes, emitters, and every §6.3 registration row (a–w, minus row u which Task 2 lands with the behavior change per the feature spec's line-200 requirement, and minus the Task-6 BACKLOG ADD).

**Files:**

- Create: tests/parser/inlineLaterGroupEnvelope.test.ts
- Modify: tests/parser/inlineLaterGroupOwnHotel.test.ts — UNSKIP the `describe.skip("warning cardinality (unskipped by Task 4)")` block (Tasks 2–3 rows)
- Modify: `lib/parser/blocks/hotels.ts` — per-row stash slots for both codes, toPending plumbing, `TRANSFORM_SITES` +2 rows at the line 1182 registry (row k)
- Modify: `lib/parser/warnings.ts` — two emitters beside the existing hotel emitters (`HOTEL_GUEST_SPLIT_AMBIGUOUS` const line 239/emit line 240, address emitter line 389 are the templates); LITERAL `code: "HOTEL_INLINE_GROUP_OWN_HOTEL"` / `code: "HOTEL_INLINE_GROUP_HOTEL_SUSPECTED"` properties (row j)
- Modify: master spec `docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md` §12.4 (+2 rows, helpfulContext byte-identical to feature spec §7 C-OWN-5 / C-SUS-5) — row a
- Regenerate: `pnpm gen:spec-codes` → `lib/messages/__generated__/spec-codes.ts` — row b
- Modify: `lib/messages/catalog.ts` (+2 entries mirroring the 9-key sibling `lib/messages/catalog.ts:1383-1396`, incl. C-OWN-6/C-SUS-6 longExplanation, C-OWN-10/C-SUS-10 helpHref) — row c
- Regenerate: `pnpm gen:internal-code-enums` (AFTER the emitters land — the extractor scans emitter literals) — row d
- Modify: `lib/parser/dataGaps.ts` — GAP_CLASSES +2 rows, each with `label` AND `plural` (C-OWN-9/9p, C-SUS-9/9p — clause-shaped labels cannot take the default bare `s`, `lib/parser/dataGaps.ts:72`); line 29 comment "55-code"→"57-code" — rows e/g
- Modify: `tests/parser/dataGaps.test.ts` — 35→37 at line 43/line 44/line 45/line 157; line 75 enumeration +2; line 83 total 5→7; EXTEND the `dataGapClassDetails` count-two test (line 196 describe, line 95 label-lookup pattern) with both new codes asserting the count-two summary renders `reservations given their own hotel from a shared line` / `reservations may show the wrong hotel` — never a bare-`s` malformation (spec §8.4 pluralization oracle; plan R1 finding 7) — row n
- Modify: `tests/parser/dataGapsClassCompleteness.test.ts` — line 36 35→37, line 68 "(55)"→"(57)", line 204 name → "total 57 (37/7/2/11)", line 205 35→37, line 209 55→57 — row o
- Modify: `tests/parser/_metaTransformSitesWalker.test.ts` — required list +2 (lines 44-49 REQUIRED_DECLARATIONS), five→eight at line 15/line 42/line 95; the lines 15-17 doc-comment enumeration rewritten to all EIGHT codes (line 98/line 102 hits from the sweep are the `required` loop identifiers — untouched) — row l
- Modify: `lib/parser/ambiguityCodes.ts` + `tests/parser/ambiguityCodes.test.ts` — both codes added; line 16 "exactly the five"→"exactly the seven" — rows f/p
- Modify: `tests/messages/warningCardCopyRegistry.ts` — codes set 42→44 (line 4), `EXPECTED_TRIGGER_CONTEXT` +2 (line 49), NEW `EXPECTED_HELPFUL_CONTEXT` map with both new codes' §4.2 strings, `EXPECTED_CORPUS_WARN_CODES` unchanged (line 127 — zero corpus hits, §9) — row h
- Modify: `tests/messages/_metaWarningCardCopy.test.ts` — NEW byte-parity loop over `EXPECTED_HELPFUL_CONTEXT` beside the `triggerContext` loop (line 79 block) — row v (R57)
- Modify: `docs/superpowers/specs/2026-07-20-warning-card-copy-restore.md` — §3.1 count 42→44 + sorted enumeration; §4.2 rows 43/44 (helpfulContext byte-identical C-OWN-5/C-SUS-5, triggerContext byte-identical C-OWN-4/C-SUS-4); §4.3 emitter inventory +2; parser-emitter enumeration 29→31 with both names (line 60); count-bearing hits from the banked sweep (00-overview): edit line 44, line 53, line 60, line 64 ("All 42"→44), line 103 ("13 of the 42"→"of the 44"), line 161 region (+rows 43/44), line 165 provenance note (+"rows 43–44 added by this feature's spec"), line 207 "(× 42 in the current universe)"→44 and "(13 in the current universe)" stays (sync-code count unchanged); NON-edits with dispositions: line 58 "42 excluded codes" (a measured exclusion count, not the registry), line 148 "| 29 |" (a table row number) — row v
- Modify: `BACKLOG.md` — DELETE the `BL-PARSER-INLINE-LATER-GROUP-OWN-HOTEL` entry (heading at line 994, body through line 998; re-grep the heading first) — row m, REQUIRED in this commit by feature spec §6.2 ("The BACKLOG row is deleted in the same commit" as TRANSFORM_SITES + walker list)
- Create: tests/messages/inlineLaterGroupCopy.test.ts — every §7 string verbatim (C-OWN-1..10, C-SUS-1..10 incl. the two null rows), asserted against string literals never imported from the catalog (parent spec §8.5 rule) — §8.4
- NEW oracle: `INTERNAL_CODE_ENUMS` membership for both codes with source `parse_warnings.code` — in tests/parser/internalCodeEnums.test.ts per the existing membership-oracle pattern (row j oracle)
- Rows q–u and w: remaining registry rows per spec §6.3 verbatim (u = Task 2, landed with the behavior commit; w = `OPERATOR_ACTIONABLE_ANCHORED`/`CELL_ANCHORED_CODES` N/A — no edit, declared)

**Interfaces:**

- Consumes: Tasks 2–3 wiring (tier outcomes, kept rows); `collapse` (`lib/parser/useRawContentHash.ts:16-18`, used at `lib/parser/warnings.ts:255`); `ParseAggregator` (`lib/parser/warnings.ts:17`).
- Produces: two ParseWarning codes emitted with LITERAL strings, severity `warn`, field `address` (label "hotel name and address", `step3Buckets.ts:129-135` mapping), message copy per spec §7 with `<raw, collapsed>` = `collapse(<raw segment/cell>)` (whitespace fold, `&#10;` stays LITERAL — NOT D1); detector-path stash discipline (OWN on kept rows, SUSPECTED on degraded-demote rows; first-stash-wins per-row sink, `lib/parser/blocks/hotels.ts:845` pattern). Scope-A/B scan stashes land in Task 5 on top of this plumbing.

## Oracle groups (spec §8.1/§8.4 rows, byte-exact)

1. Envelope OWN and SUSPECTED on D1-affected inputs (`&#10;`, doubled space, quotes): `rawSnippet` === the RAW segment/cell bytes AND `!== normalized`; surviving `index`; verdict fields (D7 — `build.judgedGuestBoundary` mapping; 7b guest warning); roleToken-absent asserts (R43).
2. Null group-0 `baseName` envelope (R16 f5).
3. Stash order both slots (probe B6 cell — address-shape-unsplit path; first-stash-wins per-row sink).
4. Field-label render (`field: "address"` bucket mapping).
5. Emission order vs cardinality (the 5-group literal row).
6. UNSKIPPED Task-2/3 warning-cardinality block: every `exactly one SUSPECTED@i` / `OWN@i` / `ZERO OWN` clause, asserted as `warnings.filter((w) => w.code === "HOTEL_INLINE_GROUP_HOTEL_SUSPECTED").length` etc. — never `.some()`.
7. §8.4 copy oracles: every §7 row byte-exact; count-two pluralization via `dataGapClassDetails`; `INTERNAL_CODE_ENUMS` membership (source `parse_warnings.code`).

Concrete failure modes: normalized-persisting emitters (byte asserts fail), double emission (cardinality), wrong stash order, shared-const code strings (extractor regenerates row-p WITHOUT the codes → membership oracle fails; `serializeParseWarning` would blank them at the telemetry boundary, R18 f4), spurious warnings on parity negatives (ZERO-new-codes asserts now enforced at the warning layer), bare-`s` plural malformation, partial fan-out (count gates and x1 catalog parity fail the moment codes exist without every registration row — which is why this is ONE commit).

## Steps

- [ ] **Step 1: Write failing tests** — tests/parser/inlineLaterGroupEnvelope.test.ts, tests/messages/inlineLaterGroupCopy.test.ts, tests/parser/internalCodeEnums.test.ts (or extend the existing membership file if one exists — re-grep first), and UNSKIP the cardinality block. Run `pnpm vitest run tests/parser/inlineLaterGroupEnvelope.test.ts tests/parser/inlineLaterGroupOwnHotel.test.ts tests/messages/inlineLaterGroupCopy.test.ts`; expected FAIL (no emitters, no catalog entries, no stashes).
- [ ] **Step 2: Commit failing tests** — `git add tests/parser/inlineLaterGroupEnvelope.test.ts tests/parser/internalCodeEnums.test.ts tests/messages/inlineLaterGroupCopy.test.ts tests/parser/inlineLaterGroupOwnHotel.test.ts && git commit -m "test(parser): envelope, copy, membership + warning-cardinality oracles for HOTEL_INLINE_GROUP_* codes"`
- [ ] **Step 3: Apply EVERY registration + implementation row above** — order inside the working tree: emitters + stashes first, then `pnpm gen:spec-codes && pnpm gen:internal-code-enums` (confirm both diffs contain exactly the two new codes), then the test-registry/count-gate/doc edits. Copy strings from spec §7 byte-exact (C-OWN-5 = 294 chars; caps 300/160; banned-vocab list includes "structured").
- [ ] **Step 4: Run to green** — `pnpm typecheck && pnpm vitest run tests/parser tests/messages tests/cross-cutting`. Expected: PASS (x1 catalog parity, count gates, walkers, card-copy fixtures, envelope, cardinality all green).
- [ ] **Step 5: spec:lint both edited spec docs** — `pnpm spec:lint docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md` and `pnpm spec:lint docs/superpowers/specs/2026-07-20-warning-card-copy-restore.md` → 0 hard each. `npx prettier --write BACKLOG.md`.
- [ ] **Step 6: Commit (single)** — `git add lib/parser/blocks/hotels.ts lib/parser/warnings.ts lib/parser/dataGaps.ts lib/parser/ambiguityCodes.ts lib/messages/catalog.ts lib/messages/__generated__/spec-codes.ts lib/parser/__generated__ tests/parser/dataGaps.test.ts tests/parser/dataGapsClassCompleteness.test.ts tests/parser/_metaTransformSitesWalker.test.ts tests/parser/ambiguityCodes.test.ts tests/messages/warningCardCopyRegistry.ts tests/messages/_metaWarningCardCopy.test.ts docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md docs/superpowers/specs/2026-07-20-warning-card-copy-restore.md BACKLOG.md && git commit -m "feat(parser): emit + register HOTEL_INLINE_GROUP_* codes across catalog, gaps, walkers, card-copy registry"` (verify the generated-enums path with `git status --short` first — the `gen:internal-code-enums` output file location is authoritative over this list).
