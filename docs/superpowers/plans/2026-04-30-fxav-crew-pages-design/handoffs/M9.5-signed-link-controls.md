# Handoff — M9.5: Signed-link admin controls (Revoke all + Issue new)

**Drafted:** 2026-05-20 by orchestrator
**Status:** SCOPED — awaiting writing-plans session for task breakdown
**Implementer:** TBD (UI surface → Opus per AGENTS.md UI-hard-rule)
**Adversarial reviewer:** cross-model reviewer after implementation
**Plan file:** N/A — focused UI+RPC coda to M9, mirrors the [M6.5 amendment-9 coda](M6.5-amendment-9.md) shape

---

## 0. Why this exists

M9-polish closed at SHA `7931420` (tag `m9-completed`) covering 12 clusters of polish on already-shipped surfaces. M11 user-facing-docs Phase E authored `/help/admin/sharing-links` against master spec §5.2 / §7.2 and Codex adversarial review R3 caught (filed as M11-E-D1, HIGH) that the documented admin labels — "Issue new link", "Revoke all links", "Copy share link" — do not appear in shipped admin UI code. Spec is canonical (AGENTS.md §1.7); the help docs follow the spec; M9 was scoped to polish on shipped surfaces, not net-new admin UI; so the spec-vs-shipped gap routes here.

**v1-blocking subset** (this milestone):

1. **Revoke all links** — security/recovery. If a signed link leaks in production, Doug must be able to invalidate every outstanding JWT for the affected (show, crew_name) tuple without leaving the admin UI. Spec §5.2 / §7.2 floor-bump (`revoked_below_version = current_token_version`).
2. **Issue new link** — recovery after revoke or after the post-revoke "no live link" state. Spec §5.2 / §7.2 (`UPDATE crew_member_auth SET current_token_version = max_issued_version + 1, max_issued_version = max_issued_version + 1`).

**Post-v1 polish** (routed to [BACKLOG.md](../../BACKLOG.md), NOT this milestone):

- **Copy share link** affordance (convenience — Doug can hand-derive the URL today). See BL-COPY-SHARE-LINK.

**Already shipped, no work needed here:**

- **Issue first link** — issued automatically by the onboarding-wizard Finalize step when a new crew row is first persisted (per spec §5.2 floor mechanism). The "first" vs "new" label distinction is rendered conditionally based on `max_issued_version` (the spec at line 1100 says: *"the admin UI for that crew row shows a single primary action labeled 'Issue first link' (or 'Issue new link' if `max_issued_version > 1` from prior life)"*). This milestone ships the single labeled affordance with both branches; only the post-finalize entry-point is net-new.

---

## 1. Spec sections in scope

- **§4.1** — `crew_member_auth` schema fields `current_token_version`, `max_issued_version`, `revoked_below_version`. Already in production schema (M2); no DDL changes.
- **§5.2** — sharing-links floor-bump mechanism. The SQL primitives are documented at spec lines 1085-1097 (no-live-link transition + Issue-new bump) and exercised today by `lib/sync/applyStaged.ts` (sync-side floor management on crew add/remove). M9.5 ships the **admin-initiated** counterparts.
- **§7.2** — per-show panel crew section admin UI. Spec lines 1100, 2272, 2274 enumerate the labels and SQL contracts:
  - "Issue new link": `UPDATE crew_member_auth SET current_token_version = max_issued_version + 1, max_issued_version = max_issued_version + 1 WHERE show_id = $1 AND crew_name = $2`
  - "Revoke all links": `UPDATE crew_member_auth SET revoked_below_version = current_token_version WHERE show_id = $1 AND crew_name = $2`
- **§8** Realtime — auth-only mutations (Issue New Link, Revoke All) MUST propagate to open viewer pages within 2s. The composite `viewer_version_token` already incorporates `crew_member_auth.last_changed_at` via the `crew_member_auth_bump_last_changed_at` BEFORE-UPDATE trigger (per spec §4.1 line 242 / M3+M4 batch-14 finding 2), so a `crew_member_auth` UPDATE fires the publish helper and the catch-up token closes the (T0,T1) race. **M9.5 must extend the existing realtime invalidation tests to cover the new admin-initiated paths.**
- **§12.4** — message catalog. Existing codes `LINK_REVOKED_FLOOR` + `LINK_REVOKED_SURGICAL` cover the **viewer-facing** 410 response when a revoked link is opened. **New codes likely needed:** admin-side success confirmations (`ADMIN_LINK_REVOKED_OK`, `ADMIN_LINK_ISSUED_OK`) routed through the canonical `messageFor()` path (no raw strings — AGENTS.md §1.5).
- **§17.1** — AC numbering: AC-5.2 (sharing-link floor mechanism) and AC-7.2 (per-show panel) both exist; this milestone closes the **admin-initiated** branches of those ACs that M9 left unshipped. No new AC numbering required; the writing-plans session confirms which ACs need amendment-style annotation vs which were already worded broadly enough.

## 2. Acceptance criteria (proposed — confirm in writing-plans)

- **AC-9.5-1** — Per-show panel crew section renders, for every active crew member row: a primary action labeled "Issue new link" (or "Issue first link" if `max_issued_version === 1` AND row is in no-live-link state) AND a secondary action labeled "Revoke all links". The "Revoke all links" action is disabled when the row is already in the no-live-link state.
- **AC-9.5-2** — "Issue new link" calls a SECURITY DEFINER RPC `issue_new_link_rpc(p_show_id, p_crew_name)` that holds `pg_advisory_xact_lock(hashtext('show:' || drive_file_id))`, performs the floor-aware UPDATE, returns the new token-version, and emits an admin-log structured entry. RPC is `is_admin()`-gated.
- **AC-9.5-3** — "Revoke all links" requires two-tap confirmation (mirrors the C4 `ResolveAlertButton` pattern + C9 `RevokeRowButton` pattern); the second tap calls SECURITY DEFINER RPC `revoke_all_links_rpc(p_show_id, p_crew_name)` that holds the same per-show advisory lock, performs the floor bump, returns the new floor value, and emits an admin-log structured entry.
- **AC-9.5-4** — Both RPCs propagate via Supabase Realtime Broadcast (§8) within 2s to any open viewer page that depends on the affected row. The `crew_member_auth` UPDATE trigger fires the publish helper without M9.5-side wiring; AC validates the path end-to-end.
- **AC-9.5-5** — Both RPCs surface admin-facing errors via the catalog (no raw error codes); infra faults route through the `AdminInfraError` discriminated-union pattern established by M9 C9.
- **AC-9.5-6** — Realtime catch-up race test extension — extends the existing `crew_member_auth.last_changed_at` race coverage to assert that an admin Issue-New-Link click during an open viewer session invalidates the viewer's link within 2s without `page.reload()`.

## 3. Spec amendments in scope

- [ ] No spec amendments. M9.5 implements existing canonical §5.2 / §7.2. If the writing-plans session uncovers a spec gap (e.g., admin-side error code shape, two-tap confirm UX), file an amendment under `docs/superpowers/specs/amendments/`.

## 4. Pre-handoff state

- [x] M9 closed at `7931420` (tag `m9-completed`).
- [x] M11 user-facing-docs Phase E in flight; M11-E-D1 / M11-E-D3 / M11-E-D4 filed against the spec-vs-shipped gap.
- [x] `crew_member_auth` schema exists (M2) with all three version columns.
- [x] Sync-side floor mutations exist (`lib/sync/applyStaged.ts`) and are exercised by `tests/sync/applyStaged.authFloors.test.ts` + `tests/sync/applyStaged.test.ts`.
- [x] Viewer-side floor enforcement exists (`tests/auth/validateLinkSession.test.ts`).
- [x] Per-show advisory-lock helper exists (`lib/db/advisoryLock.ts` — M2-D6 closure).
- [x] Admin-only RPC pattern exists (`lib/data/adminEmails.ts` + `upsert_admin_email_rpc` / `revoke_admin_email_rpc` from M9 C9) — direct template for the two new RPCs.
- [x] Two-tap admin confirm pattern exists (`components/admin/ResolveAlertButton.tsx` from M9 C4; `app/admin/settings/admins/RevokeRowButton.tsx` from M9 C9) — direct template for the Revoke confirmation surface.
- [ ] Per-show panel crew section does NOT exist as a discrete sub-section of `app/admin/show/[slug]/page.tsx` today — the page renders the parse panel and alerts but not a per-crew-row list with action affordances. Writing-plans session confirms whether the section is a new component or an extension of an existing one.

## 5. Plan-wide invariants that apply

- [x] **TDD per task** — every task starts with a failing test.
- [x] **Per-show advisory lock** — both RPCs hold `pg_advisory_xact_lock(hashtext('show:' || drive_file_id))`. **Single-holder rule:** the RPC is the lock holder; no JS-side wrapper around the RPC call. The existing structural meta-test at `tests/auth/advisoryLockRpcDeadlock.test.ts` pins the topology and must be extended with rows for both new RPCs.
- [ ] **Email canonicalization** — N/A; no raw-email boundary added.
- [ ] **No global cursor** — N/A.
- [x] **No raw error codes in UI** — new admin success/failure codes route through `lib/messages/catalog.ts` and surface via `messageFor()` / `ErrorExplainer`. M5-D8 + X.2 discipline applies.
- [x] **Commit per task** — conventional commits, one task per commit.
- [x] **Spec is canonical** — §5.2 / §7.2 wins on any disagreement; open a question instead of silently fixing.
- [x] **UI quality gate (impeccable dual-gate)** — the per-show panel crew section is a UI surface; `/impeccable critique` + `/impeccable audit` run before close-out, **externally** (subagent or user-invoked, per feedback memory `feedback_impeccable_external_attestation_required.md`). Both gates re-run on any post-review fix commits.
- [x] **Supabase call-boundary discipline** — new data-layer helpers in `lib/data/signedLinks.ts` (or equivalent) follow the `{ data, error }` + typed-infra-error pattern. New helpers register in `tests/auth/_metaInfraContract.test.ts` OR carry inline `// not-subject-to-meta` comments with explicit reasons.

## 6. Watchpoints from prior adversarial review

These are pre-loaded concerns the implementer should expect Codex to raise; addressing them up front saves rounds.

1. **Single-holder advisory-lock topology.** The M5 R20 CRITICAL deadlock was caused by nested holders (JS wrapper + in-RPC). Both M9.5 RPCs MUST be the sole lock holder for their hashkey on their invocation path. The writing-plans session must enumerate the existing holders on each affected hashkey and confirm M9.5 is the sole holder for these specific paths. Extend the structural meta-test.
2. **Floor-bump atomicity.** The "Issue new link" UPDATE atomically bumps both `current_token_version` AND `max_issued_version` (spec §5.2 line 1095-1097). A non-atomic write — e.g., separate UPDATEs — leaves a window where `current_token_version > max_issued_version` (impossible per the invariant), or `current_token_version <= revoked_below_version` (the no-live-link state). Single-statement UPDATE only.
3. **Realtime invalidation timing.** The composite `viewer_version_token` race was fixed in M3+M4 batch-14 via the `crew_member_auth.last_changed_at` BEFORE-UPDATE trigger. Both new RPC UPDATEs fire that trigger by touching the row. The integration test must assert this is still true — a "shortcut" UPDATE that bypasses the trigger (e.g., direct catalog manipulation) would silently break realtime.
4. **No-live-link state UI affordance gating.** Spec line 1100: "It does NOT show 'Copy share link' until the row leaves the no-live-link state." Even though M9.5 doesn't ship copy-share, the state-machine condition (no-live-link ↔ `current_token_version === revoked_below_version`) must be computed and exposed so the BACKLOG copy-share work can consume it correctly. The "Revoke all" button must be **disabled** in no-live-link state (nothing to revoke); "Issue new link" is the only live action.
5. **Leaked-link middleware interaction.** Spec §7.2 Branch A (lines 1959-1975) handles the case where `?t=` leakage triggers an auto-floor-bump into the no-live-link state. The M9.5 admin UI must render correctly when a row arrives at no-live-link via the middleware path (not just via admin click). Existing `tests/auth/leakedLinkMiddleware.test.ts` is the pin; M9.5 may need to extend it with a UI-render assertion or add a sibling test.
6. **Admin-log structured entry.** Per the `BL-OPS-LOG` BACKLOG entry, durable admin-facing operator logging is not yet implemented. M9.5 can either (a) emit via `admin_alerts` (informational severity) which IS shipped, OR (b) emit via `console.error` with structured payload (acceptable until BL-OPS-LOG lands). The writing-plans session picks one; emitting nothing is not acceptable for security-sensitive admin actions.
7. **Two-tap confirm UX parity.** The M9 C4 R3 / C9 R10 lessons on `useFormStatus` (M9-D-C4-1 memory) apply: the Revoke confirm button derives "Revoking…" state from `useFormStatus().pending`, not a local UI state flag. Mirrors `components/admin/ResolveAlertButton.tsx` and `app/admin/settings/admins/RevokeRowButton.tsx`.

## 7. Meta-test inventory

Per AGENTS.md writing-plans rule, declare which structural meta-tests M9.5 CREATES or EXTENDS:

- **EXTENDS** `tests/auth/advisoryLockRpcDeadlock.test.ts` — add rows for `issue_new_link_rpc` + `revoke_all_links_rpc` (single-holder topology pinned).
- **EXTENDS** `tests/auth/_metaInfraContract.test.ts` — register the new `lib/data/signedLinks.ts` helpers OR carry inline `not-subject-to-meta` comments.
- **EXTENDS** `tests/messages/_metaAdminAlertCatalog.test.ts` (if M9.5 emits via `admin_alerts`) — register any new admin-log codes.
- **NO NEW META-TEST** required — the existing meta-test family covers all M9.5 surfaces. If the writing-plans session uncovers a new structural class (e.g., "every admin SECURITY DEFINER RPC that mutates `crew_member_auth` must hold the per-show advisory lock"), file a new meta-test.

## 8. Out of scope (explicitly)

- **Copy share link** affordance — see [BACKLOG.md](../../BACKLOG.md) entry `BL-COPY-SHARE-LINK`.
- **ActiveShowsPanel row actions** (`Open`, `Preview as`, `Re-sync`, `Archive` shortcuts) — see BACKLOG entry `BL-ADMIN-DASHBOARD-ROW-ACTIONS`.
- **Per-show sync-health-history + parse-warnings-history sections** — see BACKLOG entry `BL-ADMIN-PER-SHOW-HISTORY`.
- **Bulk operations** (revoke all links for an entire show; issue new links for an entire show at once) — not in spec.
- **Time-limited link issuance** — spec does not include a TTL parameter on `signLinkJwt()`; link lifetime is governed by `crew_member_auth` version state, not JWT `exp`.

## 9. Closes (DEFERRED tracking)

When M9.5 ships, these entries' "Re-open trigger" conditions are satisfied:

- `docs/superpowers/plans/2026-05-12-user-facing-docs/DEFERRED.md` — **M11-E-D1** (HIGH) — Issue new + Revoke all subset. Copy-share stays open with BACKLOG.md as its home.

Updates to `docs/superpowers/plans/2026-05-12-user-facing-docs/DEFERRED.md` happen as part of M9.5 close-out, not at draft time.

## 10. Next action

A writing-plans session converts this handoff into a phased task breakdown with TDD red→green→commit loops per task. Recommended phases:

- **Phase 1** — DB layer: two SECURITY DEFINER RPCs + advisory-lock topology + meta-test extension.
- **Phase 2** — Data layer: `lib/data/signedLinks.ts` typed wrapper + per-RPC status whitelists + infra-contract meta-test extension.
- **Phase 3** — UI: per-show panel crew section + Issue-new + Revoke-all two-tap + `useFormStatus` discipline + impeccable dual-gate.
- **Phase 4** — Realtime integration test + leaked-link middleware UI-render extension.
- **Phase 5** — Cross-model adversarial review + close-out.

Estimated convergence: 3-5 adversarial review rounds (mid-range — security-sensitive admin paths historically attract reviewer attention; the existing C9 / C4 templates reduce surface novelty).
