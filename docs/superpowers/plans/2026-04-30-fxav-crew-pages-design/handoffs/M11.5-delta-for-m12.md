# M11.5 close-out delta-list for M12 amendment session

**Authored:** 2026-05-25 by orchestrator (Opus 4.7) at close-out HEAD `b4b2c38`, after R7 APPROVE on cross-model adversarial review.
**Diff range:** `6e4bd77..b4b2c38` — 323 files changed, +11,459 / −19,764 lines (net deletion; pivot replaced more than it added).
**Read this FIRST** before opening the M12 spec for amendment. Every M12 reference to per-crew signed links, `crew_member_auth`, `validateLinkSession`, or the M9.5 wizard flow is now stale. Inheriting M11.5's do-not-relitigate dispositions saves the M12-amendment session ≥3 rounds.

---

## 1. Changed contracts (signature/shape diff)

- `lib/auth/resolveShowViewer.ts` — **DELETED** in G1 (`05ecf7e`). Pre-pivot `ShowViewer` union with `crew_link` / `crew_google` arms is gone. M12 references to `resolveShowViewer` need replacement: page route uses `lib/auth/picker/resolveShowPageAccess.ts`; API routes use `lib/auth/picker/resolvePickerSelection.ts`. (E1 + G1)
- `lib/data/listShowsForCrew.ts` — `(supabase)` arg is now a **cookie-bound** Supabase client (`createSupabaseServerClient()`); service-role caller fails. (E2)
- `lib/data/getShowForViewer.ts` — `LINK_NO_CREW_MATCH` rejection replaced with `PICKER_CREW_MEMBER_WRONG_SHOW`. (R1 cascading)
- `lib/messages/__generated__/spec-codes.ts` — regenerated; 6 new picker codes added (`OAUTH_IDENTITY_CLAIMED`, `PICKER_BOOTSTRAP_RPC_FAILED`, `PICKER_BOOTSTRAP_RESOLVE_SHOW_FAILED`, `CALLBACK_CLAIM_THREW`, `PICKER_EPOCH_RESET`, `PICKER_SELECTION_RACE`); 18 M9.5 catalog entries removed (`LEAKED_LINK_DETECTED`, `LEAKED_LINK_REVOCATION_FAILED`, `CSRF_DENIED`, `CSRF_NONCE_EXPIRED`, `CSRF_KEY_ROTATED`, `LINK_REVOKED_FLOOR`, `LINK_REVOKED_SURGICAL`, `LINK_EXPIRED`, `LINK_VERSION_MISMATCH`, `LINK_NO_CREW_MATCH`, `LINK_SESSION_KEY_ROTATED`, `LINK_REDEEM_KEY_ROTATED`, `LINKED_ASSET_DRIFTED`, 5 `ADMIN_LINK_*` codes). `SHOW_FIRST_PUBLISHED` + `SHOW_UNPUBLISHED` copy rewritten (signed-link → share-token URL references). `SHOW_REALTIME_JWT_RENEWED` retained as null-stub log identifier (Supabase realtime, NOT M9.5). (C0-pre + R1)
- `lib/adminAlerts/upsertAdminAlert.ts` — `AdminAlertCode` union: +6 new (above) ; −16 removed; signature stable (single object arg `{ showId, code, context }`). (C0-pre)
- `lib/auth/validateNextParam.ts:16` — allowlist regex is now `^/(show/[a-z0-9-]+/[0-9a-f]{64}|admin(/.*)?|me(/.*)?)$/`. Slug-only `/show/<slug>` and legacy `/show/<slug>/p` REJECTED. (C5.5)
- `lib/auth/picker/cookieEnvelope.ts` — **NEW namespace**. `COOKIE_NAME = '__Host-fxav_picker'`; `MAX_SAFE_T_MILLIS = Number.MAX_SAFE_INTEGER` (P-R15 CRITICAL); HMAC-signed envelope; UUID selection-keys; LRU eviction. (B1)
- `lib/auth/picker/resolvePickerSelection.ts` — **NEW**. 7-arm union. Signature: `(input: { showId: string; cookie: string | undefined }): Promise<ResolvePickerSelectionResult>`. Constructs BOTH service-role + cookie-bound clients; fail-closed on every Supabase boundary. (B2 + P-R30/P-R31)
- `lib/auth/picker/resolveShowPageAccess.ts` — **NEW**. 11-arm union. Step 4(e) GOOGLE_NO_CREW_MATCH is TERMINAL no_auth/google_mismatch (does NOT fall through to step 5). Allowlisted importer: page route ONLY. (B7 + P-R27 Fix-1)
- `lib/auth/picker/selectIdentity.ts` — **NEW**. Server Action; cookie.t sourced from `out_observed_at_millis` (NOT Date.now per §10.1). PICKER_IDENTITY_CLAIMED throws NEXT_REDIRECT to `/auth/sign-in`. (B3 + B3-pre)
- `lib/auth/picker/clearIdentity.ts` — **NEW**. Both `clearIdentity` (base; revalidates) and `clearIdentityAndSkip` (atomic clear+redirect ?gate=skip; Mode B gate). (B4 + P-R29 Fix-3)
- `lib/auth/picker/cleanupStaleEntry.ts` — **NEW**. Compare-and-delete; emits PICKER_SELECTION_RACE on cleaned path via inner try/catch best-effort. (B5)
- `lib/auth/picker/resetPickerEpoch.ts` — **NEW**. Uses `requireAdminIdentity()` (NOT `requireAdmin()` — needs admin email for `admin_email_hash`); emits PICKER_EPOCH_RESET. (B6 + P-R19)
- `lib/auth/picker/rotateShareToken.ts` — **NEW**. Wraps `rotate_show_share_token` RPC. (B6)
- `lib/auth/picker/intentToken.ts` — **NEW**. HMAC-signed bootstrap intent for C6 picker-bootstrap. (C6)
- `lib/auth/picker/validatePickerAssetSession.ts` — **NEW**. Asset-route helper. (D3)
- `lib/email/hashForLog.ts` — **NEW**. SHA-256 hex; module-load throws if `HASH_FOR_LOG_PEPPER` unset or <32 chars. (A6.5)
- `lib/env/pickerCookieSigningKey.ts` — **NEW**. Throws if `PICKER_COOKIE_SIGNING_KEY` unset or wrong format. (B1 helper)
- `lib/data/loadShowShareToken.ts` — **NEW**. `requireAdmin()` gate; `createSupabaseServerClient()` cookie-bound RPC caller; calls `admin_read_share_token(p_show_id uuid) returns text`. (F2.5)
- `lib/auth/validateGoogleSession.ts:48` — signature unchanged `(req, {showId}): GoogleSessionValidationResult`. Allowlisted importers (H2): picker-bootstrap (C6), resolveShowPageAccess (B7), callback (C7), `/me` (E2). BAN: 6 §6 data API routes.
- `app/auth/sign-out/route.ts` — same-origin gate preserved (R22 F2); try/catch around `signOut()` + `teardownFailureHtml()`; Max-Age=0 picker cookie clear on success; redirect from `request.url` NOT env var. (G0e0 + P-R11 Fix-1)

## 2. Retired routes / files (M12 references must drop)

- `app/show/[slug]/p/page.tsx` — slug-only signed-link page. DELETED at `05ecf7e`. (G1)
- `app/show/[slug]/p/Bootstrap.tsx` — DELETED at `05ecf7e`. (G1)
- `app/show/[slug]/p/actions.ts` — DELETED at `05ecf7e`. (G1)
- `app/show/[slug]/page.tsx` — slug-only crew page. DELETED at `05ecf7e`; renamed-and-modified path is `app/show/[slug]/[shareToken]/page.tsx`. (C1)
- `app/api/auth/redeem-link/route.ts` — DELETED at `05ecf7e`. (G1)
- `app/auth/clear-session/route.ts` — DELETED at `05ecf7e`. (G1)
- `app/admin/show/[slug]/IssueLinkButton.tsx` — DELETED at `05ecf7e`. (G1)
- `app/admin/show/[slug]/RevokeAllLinksButton.tsx` — DELETED at `05ecf7e`. (G1)
- `app/admin/show/[slug]/actions.ts` — DELETED at `05ecf7e` (M9.5 link-issue/revoke actions). (G1)
- `lib/auth/validateLinkSession.ts` — DELETED at `05ecf7e`. (G1)
- `lib/auth/validateCrewAssetSession.ts` — DELETED at `05ecf7e`. (G1)
- `lib/auth/resolveShowViewer.ts` — DELETED at `05ecf7e`. (E1 + G1)
- `lib/auth/bootstrapCookie.ts` — DELETED at `05ecf7e`. (G1)
- `lib/auth/cookies.ts` — DELETED at `05ecf7e`. (G1)
- `lib/auth/jwt.ts` — DELETED at `05ecf7e`. (G1)
- `lib/data/loadShowCrewWithAuth.ts` — DELETED at `05ecf7e`. (G1)
- `lib/data/signedLinks.ts` — DELETED at `05ecf7e`. (G1)
- `crew_member_auth` table + indexes + RLS policies — DROPPED at `6335a4e` via `supabase/migrations/20260523000099_cutover_drop_m9_5.sql`. (G2 + G3)
- Pre-pivot signed-link tests (under `tests/auth/`, `tests/components/`, `tests/cross-cutting/fixtures/auth-x3/`, `tests/db/`, `tests/e2e/`, `tests/data/`, `tests/realtime/`) — DELETED. Exact list: `git show --name-status 05ecf7e | grep '^D'`. (G1)

## 3. New admin surfaces (UI ↔ Server Action ↔ RPC triples)

- `app/admin/show/[slug]/ResetPickerEpochButton.tsx` (two-tap confirm) ↔ `resetPickerEpoch` (`lib/auth/picker/resetPickerEpoch.ts`) ↔ `reset_picker_epoch_atomic(p_show_id uuid) returns int` (F2 + B6 + A3)
- `app/admin/show/[slug]/RotateShareTokenButton.tsx` (two-tap confirm + new-URL Copy + `router.refresh()` on success) ↔ `rotateShareToken` (`lib/auth/picker/rotateShareToken.ts`) ↔ `rotate_show_share_token(p_show_id uuid) returns table(new_share_token text, new_epoch int)` (F3 + B6 + A4)
- `app/admin/show/[slug]/CurrentShareLinkPanel.tsx` (read-only display with isolated `ShareLinkCopyButton.tsx` client island; updates on revalidate after Rotate) ↔ `loadShowShareToken` (`lib/data/loadShowShareToken.ts`) ↔ `admin_read_share_token(p_show_id uuid) returns text` — **GRANT EXECUTE TO authenticated only; service_role EXCLUDED** per P-R17 Fix-2 (F2.5)
- `components/admin/PerShowCrewSection.tsx` — simplified; per-row Issue/Revoke buttons REMOVED; section-level Reset + Rotate + Current-share-link mounted (F1 + F4)

## 4. New env vars (M12 deployment plan picks up)

- `HASH_FOR_LOG_PEPPER` — 32+ chars; required by `lib/email/hashForLog.ts` module-load gate. Set via `.env.local` (already registered in `.env.local.example`). Failure mode if unset: module-load throws → admin_alert emissions in C7/B5/B6 fail; `tests/setup.ts` seeds for tests. (A6.5)
- `PICKER_COOKIE_SIGNING_KEY` — 64 hex chars (e.g., `openssl rand -hex 32`); required by `lib/env/pickerCookieSigningKey.ts`. Failure mode: helper throws on access → resolvePickerSelection / page-route picker arms surface `INFRA_ERROR` / `infra_error`. (B1)

## 5. New structural meta-tests (M12 amendments must NOT break)

- **H1** `tests/auth/_metaPickerCookieContract.test.ts` — pins `MAX_SAFE_T_MILLIS = Number.MAX_SAFE_INTEGER`, UUID selection-key format, HMAC signature verification, cookie-mutator allowlist (5 surfaces). (H1)
- **H2** `tests/cross-cutting/no-jwt-surface.test.ts` — allowlist of `validateGoogleSession` importers; ban on 6 §6 data APIs. Extended in R1 with `FORBIDDEN_JWT_CUTOVER_SUBSTRINGS` (15 entries: `LEAKED_LINK_DETECTED`, `CSRF_DENIED`, ... `max_issued_version`) scanning app/lib/components/middleware + migrations post-cutover. Migration carve-out: cutover migration `20260523000099` exempt; migrations with later timestamp banned. (H2 + R1)
- **H3** `tests/auth/_metaInfraContract.test.ts` — Supabase boundary registry. Extended in R4 with structural scan for `createSupabase{Server,ServiceRole}Client` constructors OUTSIDE active try blocks; production-file registry. (H3 + R4)
- **H4** `tests/auth/advisoryLockRpcDeadlock.test.ts` — extended with `claim_oauth_identity` multi-show lock topology (P-R17 Fix-1); single-holder pinning; lock-ordering meta-test; cross-call-site sweep. (H4)
- **H5** `tests/cross-cutting/picker-resolver-callsite-contract.test.ts` — allowlist `auth_email_canonical` import in `resolvePickerSelection` ONLY; data-API call-site boundaries. (H5)
- **H6** `tests/components/_metaPickerRoleChipContract.test.ts` — pins IdentityChip props + Header right-slot integration shape. (H6)
- **H7** `tests/messages/_metaAdminAlertCatalog.test.ts` — admin-alert producer registry with bidirectional catalog↔producer matching (P-R14). Extended in R1 with orphan-detection scoped to `AdminAlertCode` union members. (H7 + R1)
- **H8** `tests/cross-cutting/identity-invalidated-two-reasons-doc-guard.test.ts` — 9 FORBIDDEN_PATTERNS catching stale single-reason / 401-for-identity_invalidated / step 4(e) fall-through / Mode-B form-action wrong-wiring at CI time. (H8 + P-R33)
- **R5-added** `tests/cross-cutting/package-scripts-target-existing-files.test.ts` — parses package.json scripts, extracts explicit `.test.ts`/`.test.tsx` vitest targets (now including double-/single-/back-quoted forms), asserts paths exist. (R5 + R6 quoted-path extension)
- **R6-added** `tests/cross-cutting/auth-chain-audit.test.ts` — invokes `auditProjectAuthChains()` + `auditAuthSource()`; asserts `PROTECTED_ROUTES` registry covers picker-bootstrap/version/report/`[shareToken]`. (R6)
- **R6-added** audit-symbol registry inside `package-scripts-target-existing-files.test.ts` — pins that every `test:audit:*` script's target files INVOKE the named audit (e.g., `test:audit:x3-trust-domain` targets must reference `auditProjectAuthChains` or `auditAuthSource`). Covers 7 audits: x1-catalog-parity, x2-no-raw-codes, x3-trust-domain, x4-no-global-cursor, x5-email-canonicalization, traceability, branch-protection. (R6)

## 6. Inherited do-not-relitigate dispositions (M12 amendment session)

The M12 amendment session inherits these. Re-flagging any of them in M12 review wastes rounds.

**From M11.5 plan-adversarial-review convergence (81 combined rounds; pre-implementation):**

1. **transitionManifestRow CAS race** — `BL-WIZARD-SESSION-CAS-TURNOVER-RACE` in `BACKLOG.md`. M-series onboarding scope, NOT R41. Re-surfaced 15+ times in pre-impl review; orchestrator ruled out-of-scope.
2. **A6 `viewer_version_token` `:` separator** (spec §6.1:496) — wins over plan SQL example at :691 (`-`).
3. **B3-pre two-step lock acquisition** (spec §6.2:1163-1165 R41-R33 critical-fix sequence) — wins over plan SQL example at :1616.
4. **E1 `lib/auth/resolveShowViewer.ts` deletion gated on D-series callers** — initially conditional; ultimately deleted in G1.
5. **B7 step 4(e) GOOGLE_NO_CREW_MATCH → TERMINAL no_auth/google_mismatch** — `lib/auth/picker/resolveShowPageAccess.ts:176-178`. Does NOT fall through to step 5. Shared-device leak vector.
6. **B6 `resetPickerEpoch` uses `requireAdminIdentity()` not `requireAdmin()`** — needs admin email for `admin_email_hash` alert context.
7. **A9 / F2.5 GRANT EXECUTE TO authenticated; service_role EXCLUDED** — service-role has NO JWT → `is_admin()` silent NULL leak. P-R17 Fix-2.
8. **C7 NO cookie writes** — R41-R6 DB-only contract.
9. **Cookie.t sources ALWAYS from RPC return, NEVER `Date.now()`** — §10.1 JS-clock ban. H1 + H5 pin.
10. **B2 7-arm union + B7 11-arm union with 2-reasons `identity_invalidated`** — spec §6.1. H8 doc-guard pins the 2-reasons contract across docs/app/lib/components/tests.
11. **6 R41 admin_alert producers** per P-R26 email-posture matrix — 3 email-bearing (`user_email_hash` / `attempted_email_hash` / `admin_email_hash`) + 3 email-less.
12. **Per-row OAUTH_IDENTITY_CLAIMED emission with INNER try/catch** — P-R8 Fix-3 + P-R20 Fix-2 isolation.
13. **M11.5-PLAYWRIGHT-HELPERS deferral** — 4 `.skip` scenarios in `tests/e2e/picker-flow.spec.ts`; helper layer DEFERRED with concrete trigger "before M11.5 deployment to prod, or M12 e2e suite picks up alongside."
14. **5 impeccable v3 dispositions** — `M11.5-IMP-1..5` in `DEFERRED.md` (M12 UX validation triggers for SignInOrSkipGate reassurance footer, picker-show-strip, /me TerminalFailure dedup, DESIGN.md contrast amendments, admin Reset/Rotate UX polish).

**From M11.5 whole-milestone adversarial review (R1–R7 close-out; post-implementation):**

15. **R1 catalog cleanup** — closed at `4756f45 / 355b61a / d9fcb73`. 18 catalog entries deleted; copy rewrites on SHOW_FIRST_PUBLISHED + SHOW_UNPUBLISHED; H2 / H7 extended.
16. **R2 asset 410 envelope** — closed at `9e30a32 / 1e64d2e`. `showUnavailable()` helper emits `PICKER_SHOW_UNAVAILABLE` envelope on non-admin unpublished branch. Admin + revision-mismatch bodyless `gone()` preserved (spec §1501 carve-out).
17. **R3 affordance matrix tokenized form** — closed at `e7561d1 / 1d22f5c`. Matrix row + shape-test regex updated.
18. **R4 Supabase constructor-inside-try** — closed at `df6399b / 51c85a1 / b7a99ee`. 5 surfaces fixed; H3 extended with constructor-outside-try structural scan.
19. **R5 broken x3-trust-domain CI script** — closed at `b6e969c / d611f8b`. Script repointed at picker-era coverage; first structural defense (`package-scripts-target-existing-files.test.ts`).
20. **R6 X.3 audit invocation gap** — closed at `ad1a764 / 4a26b7d / b4b2c38`. `auth-chain-audit.test.ts` restores `auditProjectAuthChains` + `auditAuthSource` invocations; audit-symbol registry covers 7 `test:audit:*` scripts; quoted-path coverage extended.

## 7. Notes for the M12 amendment session

- M12 ("all-at-once UX validation") was scoped to ride on top of M11.5's picker pivot. Confirm the M12 spec doesn't reference any item in §2 above — if it does, the M12 spec needs amendment FIRST before any M12 implementation work.
- The 6 impeccable v3 deferrals (`M11.5-IMP-1..5` in `DEFERRED.md`) and the Playwright helpers deferral (`M11.5-PLAYWRIGHT-HELPERS`) name M12 as a candidate home. The M12 spec amendment can either pick them up explicitly OR explicitly re-defer; current state is "concrete trigger filed, no commitment yet."
- The pivot ratified 0 amendments during implementation (all 5 named deviations cited to pre-existing spec/plan text). Convergence was structural per the 81-round pre-implementation adversarial review + 7-round post-implementation whole-milestone review.
- Net commit count from `6e4bd77` (R41 spec/plan convergence head) → `b4b2c38` (M11.5 close-out HEAD): ~70 implementation commits + ~15 orchestrator handoff-doc commits.
