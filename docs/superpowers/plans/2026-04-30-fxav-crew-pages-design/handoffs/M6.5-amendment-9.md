# Handoff — M6.5: Amendment 9 first-seen auto-publish + unpublish undo

**Handed off:** 2026-05-12 by orchestrator
**Implementer:** GPT-5.5 / Codex CLI, backend-only single implementer
**Adversarial reviewer:** cross-model reviewer after implementation
**Plan file:** N/A — focused backend coda to M6

---

## 1. Spec sections in scope

- §4.1 — `shows` schema: add nullable `unpublish_token uuid` and `unpublish_token_expires_at timestamptz`, with both set together or both null.
- §4.6 — `admin_alerts` catalog: add `SHOW_FIRST_PUBLISHED` and `SHOW_UNPUBLISHED`.
- §6.4 / §6.8 — first-seen staging flow and MI-1..MI-14 invariants: MI-1..5b hard-fail, MI-6..14 stage, all-pass live first-seen auto-applies.
- §7.3 — route surface: add `POST /api/show/[slug]/unpublish?token=<uuid>`.
- §12.4 — messages catalog: add `SHOW_FIRST_PUBLISHED`, `SHOW_UNPUBLISHED`, `UNPUBLISH_TOKEN_CONSUMED`, and `UNPUBLISH_TOKEN_EXPIRED` copy as needed by producers.
- §17.1 — amended AC-6.11 first-seen flow and 24h unpublish undo.

## 2. Acceptance criteria

- AC-6.11 — first-seen sheet flow per Amendment 9: MI-1..MI-5b fail to `pending_ingestions`; MI-6..MI-14 stage with the specific MI sentinel and no `FIRST_SEEN_REVIEW`; all-pass live first-seen sheets auto-apply, create a `shows` row, stamp a 24h unpublish token, emit `SHOW_FIRST_PUBLISHED`, and create no `pending_syncs` row.
- AC-6.11 undo path — `POST /api/show/[slug]/unpublish?token=<valid>` archives the show, revokes issued links for that show, consumes the token, emits `SHOW_UNPUBLISHED`, and invalidates Realtime subscribers.
- AC-6.11 consumed branch — subsequent calls with a consumed token return `400 UNPUBLISH_TOKEN_CONSUMED`.
- AC-6.11 expired branch — calls after 24h return `400 UNPUBLISH_TOKEN_EXPIRED` and clear the token columns.
- AC-6.11 onboarding exception — `mode='onboarding_scan'` still stages first-seen sheets with `ONBOARDING_SCAN_REVIEW`.

## 3. Spec amendments in scope

- [ ] Amendment 1 — listForRepo recovery contract — N/A, M8 only.
- [ ] Amendment 2 — created_at horizon + lease-expired reaper predicate — N/A, M8 only.
- [ ] Amendment 3 — `lease_holder` ownership protocol — N/A, M8 only.
- [x] Amendment 9 — first-seen auto-publish + 24h email-undo.

Full Amendment 9 text:

> Spec §5.2 / §6.8 — FIRST_SEEN_REVIEW becomes auto-publish with 24h email-undo (applies to M6 onward; ratified 2026-05-09). Doug-validation confirms his preferred workflow is frictionless if no issues: sheets should go live the moment he drags them into the watched folder; the folder is the publish gate.
>
> New first-seen routing precedence:
>
> 1. First-seen + MI-1..MI-5b hard fail -> UPSERT `pending_ingestions`, no auto-apply.
> 2. First-seen + MI-1..MI-5b pass + MI-6..MI-14 trip -> stage with the relevant MI sentinel, not `FIRST_SEEN_REVIEW`.
> 3. First-seen + all MI-1..MI-14 pass -> auto-apply via Phase 2. Show goes live immediately. `SHOW_FIRST_PUBLISHED` lands in `admin_alerts` with info severity and future push email carries a 24h unpublish-undo link.
>
> On Phase 2 auto-apply for a first-seen sheet, write `shows.unpublish_token uuid` and `shows.unpublish_token_expires_at = now() + interval '24 hours'`. The token is single-use and consumed on first redemption.
>
> The confirmation email contains `POST /api/show/[slug]/unpublish?token=<uuid>`. The endpoint validates token match, non-expiry, and non-consumption. On success it archives the show, revokes signed links issued for this show, consumes the token, and emits `SHOW_UNPUBLISHED`.
>
> After 24h or consumption, the unpublish path is closed; further recovery uses the standard admin archive flow.
>
> `ONBOARDING_SCAN_REVIEW` is unchanged. Wizard-discovery first-seen sheets keep explicit-review semantics.
>
> `FIRST_SEEN_REVIEW` is retired. No new code path emits it; the §12.4 row remains only as migration reference. `SHOW_FIRST_PUBLISHED` is an `admin_alerts` code, not a `triggered_review_items` entry.

## 4. Pre-handoff state

- [x] M6 closed at `ae6f0b8` with Amendment 9 deferred as M6-D12.
- [x] Current spec already includes amended AC-6.11.
- [x] `lib/sync/phase1.ts`, `lib/sync/runScheduledCronSync.ts`, `lib/sync/runManualSyncForShow.ts`, `lib/sync/runPushSyncForShow.ts`, and `lib/sync/runOnboardingScan.ts` exist.
- [x] `lib/db/advisoryLock.ts` and sync-side `lib/sync/lockedShowTx.ts` exist.
- [x] Backend-only: no UI files are in scope.

## 5. Plan-wide invariants that apply

- [x] TDD per task — every task starts with a failing test.
- [x] Per-show advisory lock — applies. Auto-publish writes `shows`, `crew_members`, and `crew_member_auth` under `withPostgresSyncPipelineLock` / `withShowLock`. Unpublish will use a single JS-side show lock holder; no nested RPC lock holder.
- [ ] Email canonicalization — N/A, no raw-email boundary added.
- [ ] No global cursor — N/A beyond preserving existing sync behavior; verify no `lastPollAt`.
- [x] No raw error codes in UI — route producer codes must exist in `lib/messages/catalog.ts`; no UI render work in M6.5.
- [x] Commit per task — conventional commits, one task per commit.
- [x] Supabase call-boundary discipline — new Supabase-touching helpers get `_metaInfraContract` rows or inline `not-subject-to-meta` comments.

## 6. Watchpoints from prior adversarial review

- Single-holder advisory-lock rule — only `lockedShowTx` issues `pg_advisory*` SQL for sync runtime. New auto-publish and unpublish paths must extend `tests/sync/_advisoryLockSingleHolderContract.test.ts`.
- Phase1Result discriminated union — live first-seen all-pass gets a new auto-publish-ready branch. Do not let `FIRST_SEEN_REVIEW` survive in live-path producers.
- Schema migration idempotency — migration must use `add column if not exists`, `drop constraint if exists`, `add constraint`, and `create unique index if not exists`.
- `shows.unpublish_token` nullable, default NULL — set only on first-publish auto-apply, clear on unpublish/expiry.
- Token expiry — exactly 24h from first-publish.
- Consumed-token branch — amended AC-6.11 specifies `400 UNPUBLISH_TOKEN_CONSUMED`, not 410.
- Expired-token branch — amended AC-6.11 specifies `400 UNPUBLISH_TOKEN_EXPIRED`, not 410.
- Link revocation scope — canonical amendment says revoke signed links for the show; shipped auth validity is governed by `crew_member_auth.current_token_version` and `link_sessions`. Implement with token-version invalidation for all auth rows on the show and delete/update sessions where the schema supports it; document the exact choice in tests.
- Onboarding-scan exception — `runOnboardingScan` keeps `ONBOARDING_SCAN_REVIEW`.
- Realtime invalidation — must fire after auto-publish and after unpublish.
- UI copy for ParsePanel or admin feed display is out of scope for M6.5; defer to M9 polish if needed.

## 7. Test commands

- `pnpm test tests/db/schema-introspection.test.ts`
- `pnpm test tests/sync/phase1.test.ts tests/sync/onboarding.test.ts`
- `pnpm test tests/sync/runScheduledCronSync.test.ts tests/sync/runManualSyncForShow.test.ts tests/sync/runPushSyncForShow.test.ts`
- `pnpm test tests/messages/_metaAdminAlertCatalog.test.ts tests/messages/catalog.test.ts`
- `pnpm test tests/sync/_metaInfraContract.test.ts tests/sync/_advisoryLockSingleHolderContract.test.ts`
- `pnpm test`
- `pnpm lint`
- `pnpm typecheck`

## 8. Exit criteria

- [x] Amended AC-6.11 covered by tests for hard fail, MI stage, auto-publish, consumed token, expired token, success, idempotency/race, and onboarding exception.
- [x] `FIRST_SEEN_REVIEW` is not emitted by live paths.
- [x] `SHOW_FIRST_PUBLISHED` and `SHOW_UNPUBLISHED` are produced and registered.
- [x] `DEFERRED.md` M6-D12 moved to In progress, with orchestrator to backfill final SHA after closure.
- [x] M6 handoff references updated to point to M6.5 closure.
- [x] `pnpm test && pnpm lint && pnpm typecheck` clean.
- [x] Cross-model adversarial review converged.
- [x] Working tree clean.

## 9. Sandbox / git protocol

- [x] Codex CLI with relaxed sandbox: commits run in-session. Verify with `git status` before commits.
- [x] Invocation discipline: every `codex exec` call must close stdin (`< /dev/null`).

## 10. Adversarial review handoff

After implementation, run a cross-model review loop against the M6.5 diff and log convergence here. The user explicitly requested this despite the task body also saying not to run a self-review; treat the reviewer as external cross-model review.

## 11. Cross-milestone dependencies

- Push email delivery for `SHOW_FIRST_PUBLISHED` is future notification work, not M6.5.
- ParsePanel/admin UI refinements are M9 polish unless a backend test requires catalog-only copy.
- M8 bug-report pipeline is out of scope.

## 12. Impeccable evaluation

N/A — backend-only coda. No files under `app/` except `app/api/**`, no `components/`, no design tokens, no `DESIGN.md`.

## 13. Meta-test inventory

- Extend `tests/sync/_metaInfraContract.test.ts` with any new Supabase-touching helper.
- Extend `tests/sync/_advisoryLockSingleHolderContract.test.ts` with auto-publish and unpublish lock holders.
- Extend `tests/messages/_metaAdminAlertCatalog.test.ts` with `SHOW_FIRST_PUBLISHED` and `SHOW_UNPUBLISHED`.
- None applies for sentinel-hiding and no-inline-email-normalization unless implementation touches those surfaces.

## Convergence log

- **R1 cross-model review (Claude Opus, 2026-05-12): APPROVE.** Reviewer checked the `5e977d5..HEAD` M6.5 diff for Amendment 9 behavior, advisory-lock topology, transaction atomicity, route semantics, schema mirror parity, Supabase/meta registry coverage, admin-alert catalog coverage, and UI/M8 scope boundaries. Findings were informational only: `SHOW_UNPUBLISHED` / token-error severities default to warning, expiry is JS-computed, and slug pre-read uses a separate postgres client before the locked transaction.
