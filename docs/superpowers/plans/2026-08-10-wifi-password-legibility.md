# Plan: Wi-Fi password transcription affordance (+ trailing-prose closure)

**Spec:** `docs/superpowers/specs/2026-08-10-wifi-password-legibility.md` (APPROVED, round 17, 0 findings — the async-copy semantics, geometry, sentinel path, and spike evidence are all settled; do not relitigate any §1.1 or round-repaired decision) · **Branch:** `feat/wifi-password-legibility` · **Implementer:** Opus / Claude Code (crew UI — routing hard rule)

**Meta-test inventory (declared):** EXTENDS `tests/components/crew/sections/VenueSection.wifiRoom.test.tsx` + the crew wifi suites (fixture fallout enumerated in spec §6: `tests/parser/blocks/event.test.ts:33`, `tests/crew/wifiDisplay.test.ts:44`, the `tests/crew/wifiDisplay.test.ts:233` region, and `EXPECTED_SPLITS` + census at the `tests/crew/wifiDisplay.test.ts:89` region), `tests/e2e/crew-page.spec.ts` (+ `scripts/check-crew-e2e-executed.mjs` floor recalibration), `tests/e2e/standalone.config.ts` (new registered harness + `node scripts/check-standalone-baseline.mjs --write`), the font-wait `CALLERS` registry (`tests/e2e/_metaFontWaitCoverage.test.ts:31`), and the `BACKLOG_GRADUATED` registry (two rows). CREATES the shared copy-feedback timing constant module under `lib/` (spec §4.2 — `ShareLinkCopyButton`'s `2_000` literal migrates in the same PR; DEFERRED.md:242 trigger declared fired). Advisory locks / Supabase / admin alerts: none applies.

**Plan-time red transcript (run 2026-08-11, this worktree; Task 4 grep re-run 2026-08-13):** `grep -rn "CopyFactValue" components/` → no matches (island absent); `grep -n "code\|copyLabel" components/crew/primitives/FactRows.tsx` → neither flag exists; the spec-time spike passes (`docs/superpowers/specs/probes/2026-08-10-wifi-ownership-spike.test.tsx`, 1 passed, rerun under tests/components/); Task 4's declared red command — the full-grammar line-anchored marker grep quoted verbatim in Task 4's task comment — run against this file → `exit=1` (marker line absent).

**Layout-dimensions task:** REQUIRED — folded into Tasks 2-3 with the spec's Dimensional Invariants verbatim: 44×44 target (`--spacing-tap-min`), 36×28 margin box (`-my-2 -ml-2`), `size-7` tile, right edge pinned to the row edge, row height == icon-row height (±0.5px, same-row with/without counterfactual) PLUS the bare-text-row-strictly-shorter inequality, target within the `SectionCard` `getBoundingClientRect()` border box, disjoint from every other row's rect in BOTH topologies (mid-list and `last:pb-0` last-row) AND from every other interactive element's rect, focused-ring disjointness with the focus state itself asserted (`outline-style: none` + non-none `box-shadow`; border box inflated 4px on the value side, gap-3.5 arithmetic), 40-char wrap containment at the 390px viewport with a >1-line client-rects proof that the value actually wraps.

**Transition-audit task:** REQUIRED — the spec's inventory table verbatim in Task 1's suite: idle→copied (instant swap), copied→idle (shared-constant timeout), idle↔focus-visible, copied↔focus-visible; compounds: re-tap while copied (append + reset), prop change while copied (reset + corrective entry), overlapping same-value and different-value inversions (§4.2's ONE value-only rule), remount trace (ledger routing).

<!-- tasks: depth=2 -->

## Task 1 — FactRows flags + CopyFactValue island (unit/component)

<!-- task: red=`pnpm vitest run tests/components/crew/primitives/copyFactValue.test.tsx` ac=AC-1,AC-2,AC-3 -->

Red is written SUITE-FIRST (invariant-1): the new suite encodes the spec's settled contracts and fails because none of it exists (plan-time transcript above). Cases: `code: true` → `code-value` on the value span (scoped by testid); control renders only with non-empty `copyLabel`; the §4.2 DOM shape (conditional `flex min-w-0 items-center justify-end gap-3.5` wrapper; control-free consumers byte-identical); Class B adapted geometry classes pinned including `rounded-md` and the `text-text` glyph token; the full §4.2 ring string pinned; writeText spy-argument byte oracle (`ORDTG.` fixture); the ONE value-only resolution rule (same-value inversion → two "Copied." entries, no corrective; different-value inversion → corrective + copied-clear); every non-timeout exit from copied appends the corrective entry; layout-effect ownership (remount trace per the committed spike's regression form); rendered `role="log"` region (`label="Copy confirmations"`, `testId="venue-wifi-copy-log"`) with before/after append assertions; fake-timer contracts (shared-constant timeout reset; re-tap restart); clipboard-rejection idle path; the control's accessible name asserted UNCHANGED while in the copied state (feedback rides the log region, never the name); a shared-constant consumer pin: both `CopyFactValue` and `ShareLinkCopyButton` import the timing constant from the `lib/` module (module-source assertion, so a re-inlined literal fails); and a sibling-churn keying case that FAILS under the current label-plus-index row key at `components/crew/primitives/FactRows.tsx:61` — render the testId'd password row among siblings, remove a preceding sibling on rerender, and assert the island instance survived (its copied state persists across the churn), which the index-derived key breaks and the `testId` key preserves.

1. `FactRow` gains `code?: boolean` + `copyLabel?: string`; row keying by `testId` when present (stable across sibling churn).
2. New island under `components/crew/primitives/` (`"use client"`): module write ledger + layout-effect active-owner registration, value-only resolution truth, corrective entry, announce region, shared timing constant (created under `lib/`, `ShareLinkCopyButton:109` literal migrated + its suite green).
3. `VenueSection.tsx:262` push site gains `code: true, copyLabel: "Copy the Wi-Fi password"`.
4. Sentinel fixture: `N/A` password → no password row AND the raw `Crew Wi-Fi` fallback present (spec §7's fail-soft pin).
5. Timing-inventory trigger (DEFERRED.md:242's "third timing constant" un-defer condition) is OPERATIONALIZED here, not just declared: sweep the codebase for copy-feedback timing literals, record `Step1Share.tsx:38` (`COPY_FEEDBACK_RESET_MS = 2200`) as the remaining un-migrated peer, and update the DEFERRED.md timing-inventory entry in place with the reconciliation (trigger fired; the two migrated consumers; the recorded 2200ms peer with its file:line). The consumer pin test in the suite above is the "pinned by a test rather than maintained by hand" half of the trigger.
6. Green: the new suite passes; `pnpm vitest run tests/crew tests/components tests/parser` green UNCHANGED (this task touches NO show fixture and NO seed — every Waldorf-dependent expectation stays untouched and green; all fixture fallout belongs to Task 2). `tests/components` (not `tests/components/crew`) so the existing admin ShareLink suites prove the constant migration. Commit: `feat(crew-page): wifi password code styling + CopyFactValue island`.

## Task 2 — Production-route e2e (crew-page.spec.ts)

<!-- task: red=`sh -c 'CREW_E2E_ONLY=1 pnpm exec playwright test tests/e2e/crew-page.spec.ts --project=mobile-safari'` ac=AC-1,AC-2 -->

Red is OBSERVABLE AFTER Task 1, because its axis is the SEED, not the island: the new describe locates the live Waldorf password row (`code-value` + copy control on the production crew route) — and the Waldorf seed's internet cell is still the flattened prose at task start, so no parsed credential pair exists, no password row renders, and every new case fails on row-absence. Run the new describe, OBSERVE that red, THEN change the seed cell to the parsing credential pair `SSID: WaldorfMeeting Password: Astoria2026` (spec §6) AND MATERIALIZE it — the e2e reads the Waldorf show from the local Supabase, not from fixture files, and `supabase/seed.ts` runs only when invoked, so re-run `pnpm db:seed` after the fixture edit; only then can the marked command flip green. The seed change and ALL its dependent expectation updates land in this task's single commit: the spec §6 fixture-fallout inventory (`tests/parser/blocks/event.test.ts:33`, `tests/crew/wifiDisplay.test.ts:44`, the `tests/crew/wifiDisplay.test.ts:233` region, `EXPECTED_SPLITS` + census at the `tests/crew/wifiDisplay.test.ts:89` region), the existing crew-page wifi assertions, and the oracle floor in `check-crew-e2e-executed.mjs` recalibrated from a real run.

New-case inventory (beyond floor/pinning/containment): bare-text row height strictly LESS THAN the copy-control row's height (the with/without counterfactual measured as an inequality, not only equality of the shared row); the copy target's rect disjoint from EVERY other interactive element's rect on the page (all `a`/`button` rects collected in one evaluation), not merely from other FactRows; the 40-char wrap case executed at the 390px project viewport asserting the value span actually occupies >1 line (client-rects count ≥2) before asserting containment.

Workflow-comment reconciliation in the same commit: `.github/workflows/crew-e2e.yml:45-50` purpose comment and the measured `148 tests` statement at lines 166-169 are re-measured and updated to the new count — the REQUIRED-map recalibration does not cover comments, so this is an explicit step. Commit: `test(crew-page): wifi credential seed + live password-row geometry cases`.

## Task 3 — Standalone geometry harness

<!-- task: red=`sh -c 'pnpm exec playwright test --config tests/e2e/standalone.config.ts && node scripts/check-standalone-baseline.mjs'` ac=AC-2 -->

Red is OBSERVABLE AFTER Task 1, because its axis is REGISTRATION, not the island — and the marked command itself carries both red and green: write the spec file AND its `standalone.config.ts` `testMatch` allow-list line (a spec file that merely exists runs nowhere — the config's own warning), then run the declared red command. It runs the FULL standalone config (every member, never a targeted single-spec invocation — the zero-argument checker compares the produced report against the COMPLETE baseline, so a partial report can never satisfy it) and then the checker; the checker FAILS because `tests/e2e/standalone-baseline.json` does not yet contain the new member — that exit is the observed red of the marked command. Green: `node scripts/check-standalone-baseline.mjs --write` regenerates the baseline from the full-config resolution, then the SAME marked command (full run + checker) exits 0.

Harness-readiness contract (mandatory, per the existing member pattern): the spec boots its OWN http server in `beforeAll` on an ephemeral port (`listen(0)`), serving an esbuild-bundled LIVE entry that renders the real `FactRows` + `CopyFactValue` island under the production compiled CSS (live-entry rule); readiness gate = a Playwright locator wait for the copy control rendered AND enabled (island hydration proof, distinct from `document.fonts.ready`, which is ALSO awaited + the font-wait `CALLERS` registry row added); detach-safety = every geometry evaluation re-queries its element by testid inside the `evaluate` call — no element handles held across renders.

Cases: same-row with/without counterfactual height equality AND the bare-text-row-shorter inequality, mid-list vs last-row (`last:pb-0`) topologies, focused-ring disjointness measured with the focus state ASSERTED first (`outline-style: none` and a non-none `box-shadow` on the focused control, so the ring being measured is proven to exist), 40-char wrap with the >1-line client-rects proof. Commit: `test(crew-page): standalone wifi password-row geometry harness`.

## Task 4 — Impeccable gate, graduations, merge

<!-- task: red=`grep -qE "^impeccable-gate: critique=(RAN|RAN-DEGRADED) audit=(RAN|RAN-DEGRADED) p0=[0-9]+ p1=[0-9]+ dispositions=(recorded|none)$" docs/superpowers/plans/2026-08-10-wifi-password-legibility.md` ac=AC-4,AC-5 -->

Red now (full-grammar line-anchored grep, exit 1 — plan-time transcript). Run both halves of the invariant-8 dual gate (canonical v3 setup); fill §12 (marker + full P0-P3 table; P0/P1 fixed or DEFERRED.md only); RERUN the same grep and observe exit 0; commit the gate record (`docs(crew-page): wifi impeccable dual-gate record`). Then: PREPARE the graduation content UNCOMMITTED (BOTH entries — `BL-VENUE-WIFI-PASSWORD-TRANSCRIPTION-LEGIBILITY` and `BL-WIFI-FLATTENED-TRAILING-PROSE` with the §3 probe recorded — archive moves + two `BACKLOG_GRADUATED` rows + marker removal); whole-diff cross-model review to APPROVE with the staged diff in scope (UI-touching repairs re-gate + refresh §12); run `pnpm vitest run tests/docs` green AGAINST THE STAGED GRADUATION CONTENT, and only then commit it as the PR's LAST commit (`docs: graduate the two wifi backlog entries`) — the green precedes the commit, per the test-implementation-green-commit order; real CI green; `gh pr merge --merge`; fast-forward main; `0  0`.

<!-- tasks: end -->

## Acceptance criteria (crosswalk to the spec's §8)

- AC-1: `code-value` on the password value; SSID row untouched.
- AC-2: the copy control per the settled geometry/behavior contracts, all Dimensional Invariants measured in both homes.
- AC-3: clipboard-unavailable path silent and safe.
- AC-4: both entries graduate with registry rows; markers off in the graduation commit.
- AC-5: the invariant-8 dual gate (both halves) run with P0-P3 dispositioned.

## §12 — impeccable gate record

The marker line lands here, filled, at Task 4 completion, followed by the full findings table.
