MISMATCH BACKLOG.md:269 claim=two(2) noun=sites list@271 count=3(raw 3)
   TEXT: stion and not a straight fix, which is why it is filed rather than swept.** The two adjacent sites disagree on purpose, and the disagreement is documented:
   ITEMS: The `crew_identity` restore branch (`:477`) also retains `ro | The rename-fold path (`:392-397`) does the opposite: it take | WM-F6 (`:308`) deliberately prefers held values in its own n

PARITY BACKLOG.md:315 claim=Two(2) noun=consequences list@317 count=2(raw 2)
   TEXT: matches Tailwind class idiom in the source text. Two consequences:
   ITEMS: **Per-file, not per-overlay.** A second, undispositioned ove | **Tailwind-only recognition.** An overlay written with inlin

MISMATCH BACKLOG.md:922 claim=Six(6) noun=rounds list@924 count=8(raw 8)
   TEXT: **The design is done and should be implemented as specified, not re-derived.** Six rounds of work, preserved:
   ITEMS: **Writers are DERIVED from the exports of `lib/sync/syncLog. | **Emission vs construction:** `makePostgresSyncLogSink(sql)( | **Only inline object literals are judged.** Measured on the  | **Exemption is site-precise**, never a file-level registry:  | **Three disjoint markers**, none substitutable: `run-level-s | **Entry-point rule keys on the SINK PARAMETER**, not transit | **Scope bound:** roots `lib`, `app/api`, `app/admin`; ~150 l | Mechanism: `walkSourceFiles` (`lib/messages/__internal__/wal

MISMATCH BACKLOG.md:1086 claim=7(7) noun=sheets list@1088 count=11(raw 11)
   TEXT: e v2 template (each tagged with its current source reality, verified across the 7 real sheets):**
   ITEMS: **Crew CALL TIME** (labeled) — GENUINELY ABSENT in every she | **DOORS time** (labeled) — GENUINELY ABSENT; only "Registrat | **Hotel room-type** — ABSENT everywhere. | **Hotel check-in / check-out TIME-of-day** — ABSENT everywhe | **Reliably-FILLED AGENDA tab (run-of-show titles/rooms)** —  | **Per-crew FLIGHT details** (flight #, airport, arrive/depar | **Crew Wi-Fi SSID + password** — reliable in only 2 of 7 (ot | **Venue street address + loading dock** — present in the old | **Room-within-venue name** — lives in EVENT DETAILS / sectio | **Key contacts (client / venue / in-house AV) phone + email* | **Parking detail** — present in ~4 of 5; standardize.

PARITY BACKLOG.md:1120 claim=Two(2) noun=traps list@1122 count=2(raw 2)
   TEXT: **Two traps for whoever picks this up**, both hit while resolving the merge that found it:
   ITEMS: The active ledger uses `### ` headings and the archive uses  | Archive PROSE cross-references entry ids, so a substring tes

PARITY BACKLOG.md:1190 claim=Two(2) noun=coincidences list@1192 count=2(raw 2)
   TEXT: **Two coincidences hid this**, and either alone kept the suite green for the wrong reason:
   ITEMS: Day 1's `7:30am` equals the seed's own show-start anchor. | Day 2's `8:00am` equals noon-UTC rendered in America/New_Yor

PARITY BACKLOG.md:1255 claim=Two(2) noun=obstacles list@1257 count=2(raw 2)
   TEXT: Two obstacles, both in the harness rather than the code:
   ITEMS: The banner mounts only on a REFUSAL, and the shared modal ha | Its anchor — the StatusStrip — renders BELOW the clip window

PARITY docs/agents/adversarial-round-economy-2026-07-31.md:34 claim=Two(2) noun=lanes list@36 count=2(raw 2)
   TEXT: Two lanes:
   ITEMS: **Lane 1 — behavior facts.** "What does the live parser do o | **Lane 2 — future hypotheticals.** The corpus cannot close t

PARITY docs/agents/codex-silent-death-2026-07-24.md:5 claim=two(2) noun=sources list@7 count=2(raw 2)
   TEXT: Evidence: `~/codex-silent-death-2026-07-24/` (read-only), **plus** two sources the brief did not know had survived:
   ITEMS: `/private/tmp/claude-501/-Users-ericweiss-FX-Webpage-Templat | `~/.codex/sessions/**` — **not** rotated. 651 `codex_exec` s

PARITY docs/agents/observe-cli.md:28 claim=Two(2) noun=consequences list@30 count=2(raw 2)
   TEXT: side its own INSERT — `(select id from public.shows where drive_file_id = $1)`. Two consequences worth knowing before you read an empty result as health:
   ITEMS: A row written **before** its show existed carries `show_id I | Rows written before 2026-08-09 all carry `show_id IS NULL`, 

PARITY docs/audits/e2e-real-world-variation-preparedness-2026-07-07.md:5 claim=five(5) noun=investigations list@7 count=5(raw 5)
   TEXT: **Method:** five parallel investigations at working-tree HEAD `dea2d9adc` (branch `fix/hotels-single-card-flatten`; its uncommitted `step3Revi
   ITEMS: Re-verification of all 17 findings + Tier-4 bullets of `edge | Admin UX flow audit (onboarding, review wizard, correction l | Crew-page degraded-data render audit (all sections + picker  | Seam audit: full signal-routing matrix (every parser/sync si | Live-sheet variation probe: all 7 real shows' INFO tabs read

PARITY docs/audits/edge-case-preparedness-audit-2026-07-04.md:78 claim=Three(3) noun=patterns list@80 count=3(raw 3)
   TEXT: Three cross-cutting patterns explain most of the table above:
   ITEMS: **Single-literal anchors.** Version markers (one row label), | **The quiet middle band.** Signals exist (warnings, dataGaps | **Corpus-shaped everything.** Regexes, vocabs, and the test 

PARITY docs/audits/help-readability-audit-2026-06-22.md:84 claim=Two(2) noun=notes list@85 count=2(raw 2)
   TEXT: **Two implementation notes:**
   ITEMS: Scope so the component-local margins win: author the rules w | **Errors-page cleanup:** delete `prose prose-neutral max-w-n

PARITY docs/audits/sheet-data-grounding-audit-2026-06-18.md:317 claim=Two(2) noun=alarms list@319 count=2(raw 2)
   TEXT: **Two false alarms the end-to-end run *corrected*:**
   ITEMS: **`template_version` works** — detects correctly (v2/v4) on  | **`agenda_links` works** — populates 6/7 (the only miss, Eas

PARITY docs/review-rounds/ci/app-e2e-batch1/76a614309434.md:52 claim=Two(2) noun=rules list@54 count=2(raw 2)
   TEXT: Four rounds went to this class (R4, R5, R6, R7). Two rules worth carrying forward:
   ITEMS: **Prose must not restate a fact a machine already holds** —  | **A checkbox is ticked by the event, and ideally by the code

PARITY docs/review-rounds/feat/backlog-quick-wins/59cdc8407814.md:40 claim=three(3) noun=lessons list@42 count=3(raw 3)
   TEXT: into three kinds with three different lessons:
   ITEMS: **Structural (R4, R5a, R5b, R6)** — the guard could not SEE  | **Vocabulary (R8, R9)** — the guard SAW the property but kne | **Verification (R7)** — no mutant at all. The reviewer read 

PARITY docs/review-rounds/feat/m-wave-ui/fc4902004b78.md:37 claim=Four(4) noun=rules list@39 count=4(raw 4)
   TEXT: Four rules come out of it:
   ITEMS: **A guard that passes on a clean tree has proved nothing.**  | **A premise that validates something ADJACENT to the case is | **Counts written from estimation are wrong; counts derived f | **The fix's own tidy-up is a defect site.** Two of the sharp

MISMATCH docs/review-rounds/feat/ops-log-code-emits/61281c23e8ce.md:13 claim=three(3) noun=times list@15 count=7(raw 7)
   TEXT: /agents/writing-plans.md` already names. The progression is one class, repaired three times at widening scope:
   ITEMS: R2 broke a **denylist** ("no `error` key, no sentinel, no `p | R3 broke the **context-only accept-set** that replaced it, b | R4 broke the **third copy** of the assertion list, still sit | R5 broke the **fixture**: the case reaching `client_email_mi | R6 found two of the four mutants **not executable at the sit | R7 found the accept-set applied in Tasks 1, 2 and 4 but **no | R8 found the fail-open premise **vacuous at site 1** (`stamp

PARITY docs/review-rounds/feat/ops-log-code-emits/61281c23e8ce.md:55 claim=three(3) noun=instances list@57 count=3(raw 3)
   TEXT: d the fix as "one definition, referenced", and the implementation still shipped three fresh instances of it, because the spec-stage lesson was written as a statement about the ACCEPT-SET's field list and the di
   ITEMS: **R1 — the record set.** Every helper filtered captured reco | **R2 — the case set.** The R1 repair converted the five OAut | **R3 — the path set.** Every case drove a REFUSAL, so all we

PARITY docs/review-rounds/feat/review-round-economy/48b280b949cc.md:27 claim=Four(4) noun=rules list@29 count=4(raw 4)
   TEXT: Four mechanizable rules come out of it, all cheap and all applied here:
   ITEMS: **A recognizer bounded by a NUMBER will be found by the next | **A key that can ALIAS is not a clock.** Count recency cost  | **A per-site sweep of a value-domain defect does not converg | **When four rounds land on one function, the mechanism is wr

PARITY docs/review-rounds/refactor/classname-array-join-cn/9bd0a8456151.md:158 claim=three(3) noun=calls list@160 count=3(raw 3)
   TEXT: **Judgment** — three calls where a rule would not have helped:
   ITEMS: **The threat-model fence held, and then I chose to cross it. | **The inert-connector disposition.** C1's `max-w-[60px]` is  | **`x`/`y` recorded but not compared.** A locally-captured su

MISMATCH docs/superpowers/handoffs/2026-07-03-nullcode-batch2-handoff.md:14 claim=10(10) noun=sites list@15 count=3(raw 3)
   TEXT: **Scope:** 10 sites across **3 `app/` non-api files**:
   ITEMS: `app/admin/actions.ts` — 1 site (`ADMIN_RESOLVE_CANONICAL_EM | `app/admin/show/[slug]/page.tsx` — 8 sites (`ADMIN_SHOW_*`) | `app/show/[slug]/[shareToken]/_CrewShell.tsx` — 1 site (`CRE

PARITY docs/superpowers/handoffs/2026-07-25-newtab-announcement-handoff.md:644 claim=three(3) noun=ways list@646 count=3(raw 3)
   TEXT: flipped, the `href` fixture decisive — and then broke the shape model three ordinary ways:
   ITEMS: a regex literal: `/^FetchPriority$/.test(name)` | an unquoted property key: `{FetchPriority: true}[name]` | reusing an already-excluded spelling: `name === "static"`

PARITY docs/superpowers/handoffs/2026-07-25-newtab-announcement-handoff.md:744 claim=Three(3) noun=instances list@746 count=3(raw 3)
   TEXT: Three more instances of the same edit-by-addition habit, all in that section:
   ITEMS: a sentence saying three source-reading models failed and the | the R9-era claim that the tripwire "covers every name-produc | a bullet presenting the literal tripwire as the guarantee.

PARITY docs/superpowers/handoffs/2026-07-25-newtab-announcement-handoff.md:1113 claim=Three(3) noun=findings list@1115 count=3(raw 3)
   TEXT: Three code findings, each a member of a class this PR had already visited:
   ITEMS: **Shadowing forms the scope walk missed:** a loop binding (` | **`rendersNothing` missed prefix booleans and literal-test c | **`<rp>` was absent from the non-rendered set**, which is `d

PARITY docs/superpowers/handoffs/2026-07-25-newtab-announcement-handoff.md:1862 claim=three(3) noun=states list@1864 count=3(raw 3)
   TEXT: So the order is load-bearing, and all three states are pinned:
   ITEMS: a decidable string ending in a space separates (`{" "}`, `{" | otherwise, a child that renders nothing is transparent — loo | otherwise, decidable rendered text is adjacency

MISMATCH docs/superpowers/handoffs/2026-07-26-agenda-perday-fold-handoff.md:155 claim=16(16) noun=tests list@157 count=2(raw 2)
   TEXT: either clause of `if (R.size === 0 || located.size !== R.size)` left all 16 tests green:
   ITEMS: the completeness clause had no isolated case at all; every f | the "never returns an empty subset" test used the label `"Da

PARITY docs/superpowers/handoffs/2026-07-26-agenda-perday-fold-handoff.md:239 claim=Three(3) noun=things list@241 count=3(raw 3)
   TEXT: ## Three things this PR taught about its own tests
   ITEMS: **An oracle that shares the implementation's blind spot prov | **Later guards silently kill earlier ones.** Twice, adding a | **A mutation that does not apply is indistinguishable from a

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/01-foundation.md:1022 claim=Two(2) noun=variants list@1023 count=2(raw 2)
   TEXT: ext `PULL SHEET` (case-insensitive); subsequent rows are 5-column positional. **Two corpus variants observed**:
   ITEMS: **Variant A** (verified `2024-05-east-coast-family-office.md | **Variant B** (verified `2025-05-redefining-fixed-income-pri

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/03-04-tiles.md:522 claim=6(6) noun=cases list@523 count=6(raw 6)
   TEXT: - [ ] **Step 3: Compound transitions** — 6 representative cases:
   ITEMS: `Any → unknown` mid-`pre_travel → travel_in_day` crossfade ( | `viewer_off_day → show_day_n` mid-`show_day_n → show_day_n+1 | `viewer_unconfirmed → viewer_off_day` mid-`pre_travel → trav | `Any → unknown` then `unknown → recovered` while role demoti | Date prop change AND `viewer.date_restriction` change AND `c | Sync update mid-state with field-level pulse animation queue

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/03-04-tiles.md:544 claim=two(2) noun=predicates list@545 count=9(raw 9)
   TEXT: tions in the matrix. The corrected audit adds a 2 × 2 transition table over the two branch predicates `(driverNameMatch, anyScheduleTagMatch)`:
   ITEMS: `(false, false) → (true, false)`: viewer becomes the assigne | `(false, false) → (false, true)`: viewer is added to a `sche | `(true, false) → (false, false)`: viewer is removed as drive | `(false, true) → (false, false)`: viewer's name is removed f | `(true, false) ↔ (false, true)`: the OR predicate's net resu | `(true, true) → (true, false)` / `(true, true) → (false, tru | `(true, true) → (false, false)`: both branches flip false in | **Compound transitions involving transport reassignment** —  | **End-to-end live-sync test**: write at least ONE Playwright

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/03-04-tiles.md:552 claim=2(2) noun=cases list@553 count=2(raw 2)
   TEXT:  - **Compound transitions involving transport reassignment** — at least 2 cases must be exercised against a live sync:
   ITEMS: **Schedule-tag flip mid `crew_members.name` change**: a sync | **`role_flags[]` capability flip while transport visibility 

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/03-04-tiles.md:565 claim=three(3) noun=invariants list@567 count=5(raw 8)
   TEXT: mn count, tile min-height, and equal first-row heights. The full §8.4 list adds three more invariants the test must enforce:
   ITEMS: **Right Now card full-width across all breakpoints**: at eve | **Tile grid columns**: 2 cols < 640px, 3 cols 640–1024px, 4  | **Tile min-height 96px**. | **240px internal-overflow rule**: any tile whose intrinsic c | **Footer sticky-vs-flow behavior**: on a short-content fixtu

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/03-04-tiles.md:767 claim=TWO(2) noun=refs list@769 count=2(raw 2)
   TEXT:  bridge MUST therefore implement renewal as a single-flight state machine using TWO refs:
   ITEMS: `currentChannelGenerationRef = useRef<number>(0)` — monotoni | `isRenewingRef = useRef<boolean>(false)` — set to `true` at 

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/05-auth.md:254 claim=three(3) noun=outcomes list@255 count=3(raw 3)
   TEXT:  Each validator returns one of three outcomes:
   ITEMS: `{ kind: 'success', viewer: .. }` — auth passes; chain stops | `{ kind: 'continue' }` — this branch doesn't apply (e.g., no | `{ kind: 'terminal_failure', status, code }` — chain stops w

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/05-auth.md:315 claim=FIVE(5) noun=guards list@316 count=6(raw 6)
   TEXT: `..`; `/admin\..\..\foo` is structurally similar). The corrected validator runs FIVE pre-allowlist guards in order, then matches a NARROWER allowlist against the **canonicalized** pathname:
   ITEMS: **Reject if non-string / empty / null / undefined** → return | **Reject if input contains any backslash `\`, percent-encode | **Parse via `new URL(input, process.env.NEXT_PUBLIC_SITE_ORI | **Reject if `parsed.origin !== process.env.NEXT_PUBLIC_SITE_ | **Reject if `parsed.pathname === '/show/<slug>/p'` for any s | **Match `parsed.pathname` against the canonicalized allowlis

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/06-drive-sync.md:372 claim=TWO(2) noun=helpers list@374 count=2(raw 2)
   TEXT: 9. To make lock-acquisition the route's responsibility cleanly, this task ships TWO helpers:
   ITEMS: **`runManualSyncForShow_unlocked(tx, driveFileId, mode='manu | **`runManualSyncForShow(driveFileId, mode='manual')`** — the

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/06-drive-sync.md:586 claim=three(3) noun=transitions list@587 count=3(raw 3)
   TEXT:  3. `gcWatchChannels` — three transitions:
   ITEMS: `superseded → stopped`: best-effort `channels.stop` then sta | **`orphaned → stopped`**: best-effort `channels.stop` (Drive | delete `stopped` rows older than 7d.

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/06-drive-sync.md:616 claim=Two(2) noun=shapes list@618 count=2(raw 2)
   TEXT: Apply route MUST disambiguate by source context BEFORE reading `pending_syncs`. Two distinct call shapes:
   ITEMS: **Dashboard / live-row Apply** (route resolves `source_scope | **Wizard step-3 Apply** (route receives `wizardSessionId` fr

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/07-asset.md:74 claim=Two(2) noun=patterns list@75 count=3(raw 3)
   TEXT: `linkedFolderItems[]`: **download by immutable revision, not by current head**. Two acceptable patterns:
   ITEMS: **Pattern A (preferred)** — `revisions.get(fileId, headRevis | **Pattern B (fallback if revisions.get is unavailable for th | **Never** trust a `files.get(modifiedTime,trashed)` pre-chec

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/08-bug-report.md:176 claim=three(3) noun=values list@177 count=3(raw 3)
   TEXT: - **Fail-closed contract**: `findIssueByMarker` returns one of three values:
   ITEMS: `{ url }` — issue found within the recovery window. Recovery | `null` — pagination completed cleanly AND no matching issue  | throws `LookupInconclusive` — pagination errored, was rate-l

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/08-bug-report.md:1148 claim=Three(3) noun=amendments list@1150 count=3(raw 9)
   TEXT: **Three concrete amendments to land:**
   ITEMS: **Recovery path** — replace `searchIssuesByMarker` (eventual | **Reaper predicate** — replace `processing_lease_until < now | **`lease_holder` ownership protocol** — the spec currently s

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/09-10-admin.md:770 claim=two(2) noun=parts list@772 count=2(raw 6)
   TEXT: The fix has two parts:
   ITEMS: **Scan manifest table** — a new `onboarding_scan_manifest` r | **Provenance on `pending_ingestions`** — add `wizard_session

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/09-10-admin.md:776 claim=three(3) noun=classes list@777 count=3(raw 3)
   TEXT:  - AC-10.3: every sheet appears with correct status badge across all three classes:
   ITEMS: **Parsed and ready** — row in `pending_syncs` with current ` | **Couldn't parse** — row in `pending_ingestions` with curren | **Skipped (not a Google Sheet)** — row in `onboarding_scan_m

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/09-10-admin.md:1366 claim=TWO(2) noun=paths list@1367 count=2(raw 2)
   TEXT: oug's review before crew see content. The dashboard retry route MUST split into TWO paths discriminated by whether a `shows` row already exists for `drive_file_id`:
   ITEMS: **First-seen path** (`NOT EXISTS(SELECT 1 FROM shows WHERE d | **Existing-show path** (`EXISTS(SELECT 1 FROM shows WHERE dr

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/11-cross-cutting.md:441 claim=two(2) noun=heuristics list@443 count=2(raw 2)
   TEXT: in classification + AST control-flow audit.** Earlier drafts of this task tried two heuristics:
   ITEMS: "First occurrence of any validator before any of four sinks" | "Per-route ExpectedChain tuples + comprehensive sinks via `t

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/11-cross-cutting.md:1500 claim=three(3) noun=layers list@1501 count=4(raw 4)
   TEXT: - [x] **Step 2: Failing test — three layers of audit:**
   ITEMS: **Schema audit (positive allowlist over `information_schema. | **Code audit via `ts-morph` (replaces the earlier grep) — to | **Semantic data-flow audit.** Naming heuristics alone fail w | **DDL guard via Postgres event trigger — global, allowlist-b

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/11-cross-cutting.md:1976 claim=six(6) noun=assertions list@1977 count=7(raw 7)
   TEXT: - [ ] **Step 2: Failing test** — six positive assertions across the boundary layers:
   ITEMS: **Parser canonicalization at every write site.** Static anal | **DB-write-helper defensive canonicalization.** Static analy | **DB schema CHECK exact-expression match.** SQL introspectio | **RLS helper definition.** `SELECT proname, pronargs FROM pg | **Validator canonicalization.** `lib/auth/validateGoogleSess | **Reports `reported_by` canonicalization.** `lib/reports/sub | **`admin_alerts.context` JSONB email canonicalization.** Any

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:105 claim=4(4) noun=categories list@107 count=4(raw 4)
   TEXT: **Triage outcome (4 categories):**
   ITEMS: **DELETED 2026-05-25** (2 suites — visibility predicates ful | **PARKED on `M11.5-PLAYWRIGHT-HELPERS`** (2 suites — genuine | **PARTIAL-PORT — visibility predicate redundant, integration | **FULL-MIGRATE to jsdom + RTL** (10 suites — no equivalent c

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:107 claim=2(2) noun=suites list@108 count=4(raw 4)
   TEXT: - **DELETED 2026-05-25** (2 suites — visibility predicates fully redundant with `tests/visibility/` unit layer):
   ITEMS: `tests/e2e/scope-tiles.spec.ts` (whole file — A1/V1/L1/LEAD  | `tests/e2e/status-financials.spec.ts:99` FinancialsTile bloc | `playwright.config.ts` testMatch regex updated to drop `scop | Note: `docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:113 claim=2(2) noun=suites list@114 count=2(raw 2)
   TEXT: - **PARKED on `M11.5-PLAYWRIGHT-HELPERS`** (2 suites — genuine signed-in/role-spoof or real-browser-layout dependencies):
   ITEMS: `tests/e2e/role-spoof.spec.ts:72` — asserts `?role=` URL par | `tests/e2e/layout-dimensions.spec.ts:208` — Task 4.13 / §8.4

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:117 claim=3(3) noun=suites list@118 count=3(raw 3)
   TEXT: ARTIAL-PORT — visibility predicate redundant, integration assertions are not** (3 suites — keep `test.describe.skip` in place; pick up alongside the migrate-now batch below):
   ITEMS: `tests/e2e/transport-tile.spec.ts:225` — visibility branches | `tests/e2e/empty-state.spec.ts:88` — opening_reel hide/rende | `tests/e2e/pack-list.spec.ts:219` — visibility (3 schedule p

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:122 claim=10(10) noun=suites list@123 count=12(raw 12)
   TEXT: - **FULL-MIGRATE to jsdom + RTL** (10 suites — no equivalent component-test coverage exists; estimated ~34h):
   ITEMS: `tests/e2e/right-now.spec.ts:128` (Task 4.11 Today hero; par | `tests/e2e/right-now-transitions.spec.ts:152` (§8.2 66-pair  | `tests/e2e/right-now-transitions.spec.ts:289` (§8.2 6 compou | `tests/e2e/theme-toggle.spec.ts:57` (data-theme flip + local | `tests/e2e/status-financials.spec.ts:72` ShowStatusTile (COI | `tests/e2e/crew-page.spec.ts:559` LodgingTile (~2h) | `tests/e2e/crew-page.spec.ts:596` VenueTile (~2h) | `tests/e2e/crew-page.spec.ts:619` CrewTile (~3h; no equivale | `tests/e2e/crew-page.spec.ts:652` ContactsTile (~2h) | `tests/e2e/schedule-tile.spec.ts:123` ScheduleTile (date_res | `tests/e2e/notes-tile.spec.ts:144` NotesTile (4-source aggre | `tests/e2e/crew-page.spec.ts:508` layout shell — actually a 

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:379 claim=Five(5) noun=findings list@381 count=5(raw 5)
   TEXT: **Description:** Five small audit findings deferred:
   ITEMS: `<details>` UA marker not styled / no `list-style: none` res | SignInButton inline error not associated with button via `ar | AlertBanner `role="status" aria-live="polite"` on SSR-only r | Bootstrap connecting state has no `aria-live` for state tran | Sign-in page `<header>` lacks `aria-labelledby` (only matter

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:473 claim=2(2) noun=findings list@475 count=2(raw 2)
   TEXT: **c195747 critique dispositions (2 findings; both DEFERRED with rationale):**
   ITEMS: **P2 — Double-tap window between pending=false and banner re | **P3 — No live-region announcement on pending→idle failure t

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:486 claim=Four(4) noun=rounds list@487 count=3(raw 3)
   TEXT: **Status:** **Resolved.** Four review rounds converged the dead-href class:
   ITEMS: R12/R13 caught that `href="/admin"` 404'd because the route  | R14 caught the same class in the auth-redirect default `DEFA | **R15** caught that `/admin/dev` is itself build-gated out o

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:766 claim=8(8) noun=fixes list@767 count=8(raw 8)
   TEXT: **Landed (8 fixes, regression-pinned):**
   ITEMS: A1 hotels: yearless inline `Check In: M/D` back-fills the ye | A2 hotels: 5-col multi-reservation per-reservation check-out | B1 transport: `assigned_names` skips the col0 stage label (r | B2 transport: v1 vehicle read from the `| Transportation | V | B3 transport: exporter v2 header (`TRANSPORTATION` in col1)  | B4 transport: v4 plain (exporter) header + body-row driver ( | C1 rooms: v4 General Session detected under the column-dupli | C2 rooms: phantom `Additional Room Name(s)` suppressed via c

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md:787 claim=three(3) noun=reasons list@788 count=3(raw 3)
   TEXT: `-in-names leak + glued names — see #8). The conf# **value** is NOT stored, for three compounding reasons surfaced across the adversarial review:
   ITEMS: **R1** — a row-level `confirmation_no` reaches EVERY listed  | **R2** — guests are sometimes space-delimited (raw RPAS `Dou | **R4 (decisive)** — even a *correctly* single-guest conf# is

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:44 claim=3(3) noun=routes list@45 count=3(raw 3)
   TEXT: - **Task 10.6 §A — Pending-panel + global admin-alert action routes (3 NEW routes):**
   ITEMS: `app/api/admin/pending-ingestions/[id]/retry/route.ts` — POS | `app/api/admin/pending-ingestions/[id]/discard/route.ts` — P | `app/api/admin/admin-alerts/[id]/resolve/route.ts` — **GLOBA

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:50 claim=two(2) noun=functions list@51 count=3(raw 3)
   TEXT: (rendered post-onboarding) both call SERVER ACTIONS, not API routes. §A exports two functions:
   ITEMS: `startOverServerAction()` — wraps `purgeAndRotateOnboardingS | `rerunSetupServerAction()` — runs the checkpoint-aware suppr | **Why this matters for the split:** §B's `<StartOverButton>`

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:54 claim=2(2) noun=routes list@55 count=2(raw 2)
   TEXT: - **Task 10.10 §A — LIVE first-seen staged routes (2 NEW routes):**
   ITEMS: `app/api/admin/show/staged/[stagedId]/apply/route.ts` — **TH | `app/api/admin/show/staged/[stagedId]/discard/route.ts` — PO

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:432 claim=two(2) noun=scenarios list@434 count=3(raw 3)
   TEXT: Two coordinated terminals running against the same branch creates two scenarios this protocol must address:
   ITEMS: **Pin-2 contract changes after §B has started against it.**  | **Workspace topology.** §A and §B share the SAME git branch  | **What §B does when blocked on Pin-2.** If §B reaches a poin

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:759 claim=Three(3) noun=commits list@760 count=3(raw 3)
   TEXT:  interrupted the session before commit; work was stashed and recovered on host. Three commits landed:
   ITEMS: **5a39eba** `test(onboarding): pin watched_folder_id preserv | **4a3c577** `feat(sync): inline rescan on wizard revision_ra | **0d9ad1b** `feat(admin): StagedReviewCard handles wizard re

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:767 claim=4(4) noun=findings list@768 count=4(raw 4)
   TEXT: ld all pass; `lastPollAt` + `WATCHED_DRIVE_FOLDER_ID` grep gates return zero. **4 close-out hygiene findings:**
   ITEMS: **F1 (BLOCKING-as-written)** §7 raw-code static-grep gate ma | **F2 (BLOCKING-as-written)** §7 `/admin/dev` static-grep gat | **F3 (BLOCKING-as-written)** §7 + §8 exit-criteria reference | **F4 (non-blocking accuracy)** pnpm lint passes but 2 of the

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:774 claim=Two(2) noun=findings list@775 count=2(raw 2)
   TEXT: copy, and class-sweep records all consistent with the code and tests inspected. Two close-out hygiene findings:
   ITEMS: **L HIGH (post-R1 regression)** — R1 F3 fix was incomplete.  | **J MINOR (doc hygiene)** — DEFERRED.md left M10-D-PHASE2-1 

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:779 claim=Two(2) noun=findings list@780 count=2(raw 2)
   TEXT: AD ae4f533).** Verdict: NEEDS-ATTENTION. Functional axes A, C-H, K all APPROVE. Two findings:
   ITEMS: **B/I HIGH (real spec violation that survived 3 prior review | **J MINOR (docs hygiene)** — DEFERRED.md M10-D-PHASE1-1 "sug

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:786 claim=4(4) noun=rounds list@787 count=1(raw 1)
   TEXT: t by the fresh-eyes pass despite surviving 7 rounds of Pin-2 cross-CLI review + 4 rounds of close-out:
   ITEMS: **D/E HIGH (real protocol bug, §A territory)** — `app/api/ad

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:828 claim=2(2) noun=clusters list@829 count=7(raw 7)
   TEXT: ED at 259fb6f.** Phase 3 SHA range `5b13f5a..259fb6f` spans 7 §B commits across 2 impeccable clusters (I-6 + I-5) plus the cross-CLI review fix passes:
   ITEMS: `e8eca04` feat(admin): help/tour/help-affordance for §9.0.1  | `9a36419` feat(admin): preview-as impersonation via identity | `58ed907` fix(admin): impeccable + Codex R1 dispositions | `8de1d09` fix(admin): impeccable + Codex R2 dispositions | `7e5a9e7` fix(admin): R3 dispositions — ReportModal explaine | `662fb9c` fix(admin): R4 dispositions — preview report autoc | `259fb6f` fix(admin): R5 disposition — Footer report-surface

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:839 claim=4(4) noun=rounds list@840 count=4(raw 4)
   TEXT: + memory `feedback_impeccable_external_attestation_required.md`) cleared across 4 rounds:
   ITEMS: **R1**: 1 CRITICAL + 4 HIGH + 4 P0/P1. CRITICAL: Tour step 1 | **R2**: 1 CRITICAL (regression from R1). Tour step 4 `&ldquo | **R3**: SHIP (no must-fix). Class-sweep confirmed no peer HT | **R4**: SHIP (no must-fix). Verified R3 fixes survived and t

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:891 claim=four(4) noun=rounds list@892 count=4(raw 4)
   TEXT:  Playwright e2e, `5ddd2c5` lint/exactOptional fixes. Adversarial review (Codex, four iterative rounds) closed three §B dispositions:
   ITEMS: **R1 (MEDIUM)** — `ONBOARDING_OPERATOR_ERROR` catalog dougFa | **R2 (HIGH)** — `<OnboardingWizard>` rendered `startOverServ | **R3 (MEDIUM)** — `?show_finalize=true` was authoritative; h | **R4 (MEDIUM, §A-OWNED — routed to §A):** Codex flagged that

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M10-onboarding.md:928 claim=3(3) noun=findings list@929 count=5(raw 5)
   TEXT: (handoff doc only):** Codex round-1 critique on §0. Verdict: needs-attention. **3 HIGH findings** resolved before kickoff:
   ITEMS: F1 (file-ownership gaps): added Task 10.4 §A pending_ingesti | F3 (Pin-2 contract surface gaps): added request/response sha | F8 (test ownership omitted): added explicit §A test ownershi | **2 MINOR findings** resolved: F4 (manifest reuse misstated  | **2 APPROVE findings** (no action): F2 (Pin-1 narrow scope O

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M11.5-crew-auth-pivot.md:97 claim=TWO(2) noun=modes list@98 count=2(raw 2)
   TEXT: k C5** — `app/show/[slug]/[shareToken]/_SignInOrSkipGate.tsx` Server Component. TWO modes (P-R28 Fix-2):
   ITEMS: Mode A (`reason: 'first_contact'`): Skip + Sign-in CTAs. | Mode B (`reason: 'google_mismatch'`): "Signed in as someone 

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M11.5-crew-auth-pivot.md:439 claim=36(36) noun=rounds list@441 count=7(raw 7)
   TEXT: Surface-by-surface failure modes recorded across 45 spec rounds + 36 plan rounds:
   ITEMS: **`select_identity_atomic` timestamp source** — pre-R41-R23, | **PostgreSQL banker's rounding (R41-R30 HIGH)** — `::bigint` | **Cookie decoder unix-seconds cap (P-R15 CRITICAL)** — pre-f | **Service-role bypass of `is_admin()` (P-R17 Fix-2 + P-R30 F | **Shared-device identity leak vector (P-R27/P-R30 CRITICAL)* | **Stale-text drift across spec/plan amendments** — 6 rounds  | **transitionManifestRow CAS race** (commits `273d657` + `26a

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M11.5-crew-auth-pivot.md:573 claim=four(4) noun=contracts list@574 count=4(raw 4)
   TEXT: surfaces against the prior round's findings. Outcome: **PASS (converges)**. All four contracts landed cleanly:
   ITEMS: `title=` removed from both URL `<code>` elements (no residua | `aria-live` removed from Copy buttons; remaining `aria-live` | "Last attempt:" prefix removed from both refused banners (pa | `setResult(null)` at idle→confirm transition does NOT collid

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:162 claim=two(2) noun=defenses list@164 count=2(raw 2)
   TEXT: gh R5). The vector was declared **UNRESOLVED for per-instance patching** at R5; two structural defenses were **SPECIFIED** in the M12 plan's R5 amendment and are planned for Phase 0.C authoring per `DEFERRED.md
   ITEMS: **`tests/cross-cutting/validation-tooling-tz-pin.test.ts`**  | **`tests/cross-cutting/email-canonicalization.test.ts`** (ex

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:219 claim=40(40) noun=rounds list@221 count=4(raw 4)
   TEXT: Per AGENTS.md mandatory cross-CLI step + user R0 authorization (40 rounds, fresh-eyes per round, anchored on milestone-base `b4b2c38`):
   ITEMS: **Invocation:** manual `codex-companion adversarial-review - | **Focus text** must include: fresh-eyes posture; full §4 do- | **Iteration:** until APPROVE OR 40-round cap. Each round is  | **Round handoff:** entries appended inline to the convergenc

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:283 claim=4(4) noun=sub-classes list@284 count=4(raw 4)
   TEXT: ec + plan tree `.md` files (strips `§15` audit trail; excludes `handoffs/`) for 4 forbidden-pattern sub-classes:
   ITEMS: **F3-class:** `iPhone.*claim.*claimed_after_pick` (same-line | **F4-class:** `Reset.*session_mismatch` (same-line within 20 | **F5-class:** bare `session_mismatch` (±5-line proximity) wi | **Paranoia:** `\bRotate(?:ShareToken)?(?:Button)?\b.*claimed

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:605 claim=7(7) noun=results list@606 count=7(raw 7)
   TEXT:  **(B) Enlarged F10-class (A) audit — Dim 1-7 results:**
   ITEMS: Dim 1 (parameterization integrity, 6 prose surfaces): PASS ( | Dim 2 (canonical rejected-domain set across peers): PASS (R1 | Dim 3 (predicate (k) verb form): PASS. | Dim 4 (test fixture-vs-RPC enforcement parity): PASS. | Dim 5 (claim baseline reset on every reseed): PASS. | **Dim 6 (canonical contract TABLES — §-numbered tables enume | **Dim 7 (cross-section prose vs amendment consistency):** **

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:702 claim=2(2) noun=tests list@703 count=3(raw 3)
   TEXT: - **F21-class structural defense (commit 52):** 2 new tests in `reseed-clears-oauth-claim-doc-guard.test.ts`:
   ITEMS: **Live-spec assertion** (`F21-class prose-consistency`): gre | **Negative-case unit test**: 4 broken fixtures trigger the r | RED demonstration witnessed during authoring: commit-48 rewr

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:1094 claim=Two(2) noun=patterns list@1095 count=4(raw 4)
   TEXT: ense, not a 5+ round case demanding model inversion like F10-class was at R27). Two new regex patterns added to `F21_FORBIDDEN_PATTERNS` in `tests/cross-cutting/reseed-clears-oauth-claim-doc-guard.test.ts` to c
   ITEMS: `producer-table:reports-only-target` — matches phrasings ass | `producer-table:singular-failure-state` — matches phrasings  | Both regexes carry the same escape hatches as the prior 4 pa | **The §4.2 row's note about `feedback_inbox` BACKLOG-only** 

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12-solo-dev-ux-validation.md:2153 claim=4(4) noun=peers list@2154 count=5(raw 5)
   TEXT:  - **(B) Sweep — 4 ACTIONABLE peers fixed in same commit:**
   ITEMS: plan 02:484 registry bullet list | plan 02:446 PostgREST DML lockdown contract narrative | plan 02:663 RED→GREEN regression-baseline narrative | plan 02:681 embedded commit-message body | `00-overview.md:128` meta-test routing-table description

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12.1-pg-cron-pivot.md:152 claim=6(6) noun=commits list@154 count=0(raw 5)
   TEXT: Once M12.1 reaches APPROVE + the 6 commits land (including T5 which amends the M12 plan), Phase 0.A executor (the original Claude Code session paused at the Vercel
   ITEMS: 

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M12.1-pg-cron-pivot.md:180 claim=5(5) noun=classes list@181 count=6(raw 6)
   TEXT: - 5 same-vector recurrence classes tracked and closed structurally:
   ITEMS: **API-verification class (8 hits, R1-R10 sweep):** Vault sch | **Structural-defense-self-inconsistency class (13 hits throu | **TDD-discipline class (1 hit, R7 F17):** task started with  | **Operational-semantics class (3 hits, R5 R8 R9):** pg_net a | **CI-gating-enforcement class (8 hits through R33):** workfl | **Secret-leakage class (2 hits, R23 F47 + R24 F48):** SQL ed

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M3-admin-upload-test.md:195 claim=Two(2) noun=items list@197 count=2(raw 2)
   TEXT: ion `/admin/dev` panel ready for M4 (crew page tiles) and downstream consumers. Two non-blocking advisory items remain for future milestones:
   ITEMS: `supabase/migrations/20260502000000_dev_schema_clone.sql:415 | `tests/e2e/helpers/supabaseAdmin.ts:52-55` — `snapshotPublic

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M5-auth.md:435 claim=three(3) noun=branches list@436 count=3(raw 3)
   TEXT: ). The redeem-link route's row-vs-cookie-vs-active kid comparison discriminates three branches per AC-5.13:
   ITEMS: cookie kid ≠ row kid → `CSRF_DENIED` (forgery — kid mismatch | cookie kid = row kid AND cookie kid = active kid → proceed; | cookie kid = row kid AND cookie kid ≠ active kid → `CSRF_KEY

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M5-auth.md:669 claim=Two(2) noun=classes list@671 count=2(raw 2)
   TEXT: Two recurring classes drove almost all of the early findings; both were closed structurally rather than per-instance:
   ITEMS: **"Auth helper masks infra fault as benign auth signal"** (r | **"`admin_alerts.upsert` with a code that has `null` dougFac

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M6-drive-sync.md:188 claim=Three(3) noun=facts list@190 count=3(raw 3)
   TEXT: ision id at all — this is structural, not a permission-level or scope artifact. Three Google API facts collectively close the door:
   ITEMS: **`files.get(... headRevisionId, md5Checksum ...)`** — Drive | **`drive.revisions.list`** — structurally empty for Workspac | **Sheets API v4** — `spreadsheets.get`, `spreadsheets.values

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M6-drive-sync.md:713 claim=Three(3) noun=classes list@715 count=3(raw 3)
   TEXT: after the xlsx fetch AND again before entering the Phase-1/Phase-2 transaction. Three classes trigger `STAGED_PARSE_REVISION_RACE` (NOT generic `drive_error`):
   ITEMS: (a) **post-fetch token mismatch** — `metadata.modifiedTime`  | (b) **xlsx export path failure mid-flight** — `files.get` re | (c) **post-enrichment token mismatch** — token moved between

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M6-drive-sync.md:751 claim=four(4) noun=variants list@752 count=4(raw 4)
   TEXT: ; an Apply that does NOT mutate `shows.diagrams` MUST NOT touch the column. The four variants split:
   ITEMS: `DIAGRAMS_EMBEDDED_REVISIONS_UNAVAILABLE` — technical failur | `DIAGRAMS_EMBEDDED_NONE_FOUND` — operator-confirmation; **mu | `DIAGRAMS_LINKED_FOLDER_DRIFT_PENDING` — operator-confirmati | `REEL_DRIFT_PENDING` — operator-confirmation; persist ALL FO

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M6-drive-sync.md:1200 claim=4(4) noun=meta-tests list@1201 count=4(raw 4)
   TEXT: - **4 structural meta-tests** prevent the recurring classes from regressing:
   ITEMS: `tests/sync/_advisoryLockSingleHolderContract.test.ts` (lock | `tests/sync/_scopeCheckContract.test.ts` (sheet-scope check  | `tests/messages/_metaAdminAlertCatalog.test.ts` (producer-wr | structural sweep for `last_sync_status` enum compliance (R13

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M7-asset.md:228 claim=Three(3) noun=options list@229 count=3(raw 3)
   TEXT:  Claude Code"). **Resolution required at M7 kickoff before starting Task 7.9.** Three options:
   ITEMS: **(a) Carve Task 7.9 off to Opus.** Backend Tasks 7.1–7.8 sh | **(b) Amend ROUTING.md.** Update the M7 row to "split-mode:  | **(c) Roll Task 7.9 into M9 polish.** Defer the gallery / ag

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M8-bug-report.md:170 claim=six(6) noun=amendments list@172 count=0(raw 6)
   TEXT: The other six plan amendments are NOT M8's responsibility:
   ITEMS: 

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M8-bug-report.md:268 claim=3(3) noun=rounds list@269 count=4(raw 4)
   TEXT: iew fires. **M8-specific candidate vectors** (each could plausibly recur across 3 rounds):
   ITEMS: The 6 `LookupInconclusive` discriminator codes' routing to ` | The 4 Cases (A/B/C/Reaped) in `handleTailUpdateMiss` × 2 cal | The DB-time-vs-`Date.now` horizon classification (every plac | The state-gated `admin_alerts` UPSERT pattern (two-gate retr

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M8-bug-report.md:296 claim=Three(3) noun=places list@297 count=3(raw 3)
   TEXT: 16. **Amendment 2 — combined predicate horizon enforcement.** Three places enforce the 24h `created_at` horizon and they MUST agree:
   ITEMS: `expiredLeaseRetry` entry check (rejects past-horizon rows w | Lease-claim UPDATE WHERE clause (`AND created_at >= now - in | Reaper DELETE WHERE clause (`AND created_at < now - interval

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M9-polish.md:71 claim=TWO(2) noun=amendments list@73 count=2(raw 2)
   TEXT: 12 at 9.0 close (revised after Round 1 adversarial review at SHA 00620cb)** — **TWO new spec amendments ARE in M9 scope**:
   ITEMS: **§14.3 admin allow-list mechanism** (C9 cluster prereq). Re | **§12.4 catalog new rows for AGENDA_* crew-facing codes** (M

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M9-polish.md:96 claim=four(4) noun=tasks list@97 count=5(raw 5)
   TEXT: - [ ] **NEW M9 modules / routes** (concrete file list deferred to 9.0; the four stated tasks are minimally):
   ITEMS: `components/shared/StaleFooter.tsx` (Task 9.1) | `components/shared/TileServerFallback.tsx` + `components/sha | `lib/messages/catalog.ts` extensions + new field `helpfulCon | Per-tile data-loader / view-component splits (Task 9.2) — ev | Additional surfaces for whichever deferrals 9.0 routes into 

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M9-polish.md:1403 claim=four(4) noun=clusters list@1404 count=4(raw 4)
   TEXT: The four largest sub-shape-required clusters (C1/C3/C4/C9) converged in the autonomous-push session 2026-05-15→2026-05-17:
   ITEMS: **C1 (Crew-page IA)** — 8 rounds + a Next.js 16.0.0 → 16.2.4 | **C3 (Auth flow + /me page + Bootstrap)** — 16 rounds. The l | **C4 (AlertBanner queue + Resolve confirm + raised_at)** — 3 | **C9 (Admin allow-list runtime-mutable)** — 11 rounds. Resol

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M9-polish.md:1429 claim=seven(7) noun=rounds list@1430 count=9(raw 9)
   TEXT: 4. **Whole-M9 cross-model adversarial review** ran across seven rounds:
   ITEMS: **R1** (`f669e18`): HIGH spec-integration drift — C9 amendme | **R2** (`8d8a183`): HIGH Task 9.C9.0.5 runtime RLS behaviora | **R3** (`69d4c6f`): HIGH R2 probe false-passed on NOT NULL c | **R4** (`d22ea75`): HIGH M9-routed DEFERRED entries not mark | **R5** (`e005c03`): HIGH M7-D3 falsely marked RESOLVED at R4 | **R6** (`07fa01d`): HIGH handoff §11 close-out summary still | **R7** (`1842cc6`): HIGH final close-out undercounted M9-D-C | **R8** (`5fdd3bd`): HIGH AC-9.2 empty-state reachability e2e | **R9** (this commit): HIGH SignInButton legacy test asserted

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M9.5-plan.md:3132 claim=4(4) noun=tests list@3133 count=3(raw 3)
   TEXT: 4. **Anti-tautology rule** (Phase 4 UI tests):
   ITEMS: Tests stub the data layer / action mocks; they assert agains | Realtime test (Task 5.1) asserts against the BROADCAST event | Leaked-link UI test (Task 5.2) uses fixture values construct

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/M9.5-plan.md:3139 claim=four(4) noun=families list@3140 count=4(raw 4)
   TEXT: 6. **Meta-test inventory** — all four families addressed:
   ITEMS: `advisoryLockRpcDeadlock` extension: Task 1.4 + Task 3.2 ✓ | `_metaInfraContract` extension: Task 2.4 ✓ | `_metaAdminAlertCatalog` extension: N/A per Task 3.3 (no adm | `no-raw-codes-audit` (X.2): auto-covered via catalog routing

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/X-closeout.md:33 claim=four(4) noun=meta-tests list@35 count=4(raw 4)
   TEXT: The loop produced four contract-level meta-tests that close the recurring-bug classes structurally (rather than per-instance):
   ITEMS: **`tests/sync/_insertParamsArityContract.test.ts`** (R2) — p | **`tests/sync/_phase2ArgsParityContract.test.ts`** (R3, wide | **`tests/sync/_inTxAdminAlertContract.test.ts`** (R6) — walk | **`tests/cross-cutting/_canonicalEmailCheckContract.test.ts`

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/X2-no-raw-codes.md:47 claim=Three(3) noun=phases list@48 count=3(raw 3)
   TEXT: awl"). Crawls every fixture-seeded route + admin route + asset 410/401 surface. Three crawl phases per element:
   ITEMS: **1a textContent** — every element's textContent + every chi | **1b user-visible attributes** — `aria-label`, `title`, `alt | **1c live DOM properties** — `inputElement.value`, `textarea

MISMATCH docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/X5-email-canonicalization.md:56 claim=6(6) noun=fixtures list@57 count=11(raw 11)
   TEXT: ion fixtures** — under `tests/cross-cutting/fixtures/email-canonicalization/` (~6 positive failure-mode fixtures + their counterparts):
   ITEMS: `bad-parser-raw-email.ts.fixture` (parser block assigns `ema | `bad-db-insert-raw-email.ts.fixture` (DB write helper INSERT | `bad-jsonb-context-raw-email.ts.fixture` (`admin_alerts` UPS | `bad-validator-no-canonicalize.ts.fixture` (`validateGoogleS | `bad-reports-reported-by-raw-email.ts.fixture` (crew-path `r | `bad-inline-toLowerCase.ts.fixture` (`const e = raw.toLowerC | `good-canonicalized-parser.ts.fixture` (parser block correct | `good-canonicalized-db-write.ts.fixture` (DB write helper co | `good-canonicalized-jsonb-context.ts.fixture` (`admin_alerts | `good-validator-canonicalizes.ts.fixture` (validator correct | `good-reports-crew-uses-id.ts.fixture` (crew-path uses `crew

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/X6-traceability.md:482 claim=Three(3) noun=points list@484 count=3(raw 3)
   TEXT: Three data points in series:
   ITEMS: **X.4** R1 REQUEST_CHANGES → R2 APPROVE. Semantic data-flow  | **X.5** R1 REQUEST_CHANGES → R2 APPROVE. DB introspection +  | **X.6** R1 APPROVE. THREE concurrent surfaces (traceability 

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/handoffs/X6-traceability.md:661 claim=four(4) noun=scenarios list@663 count=4(raw 4)
   TEXT: **H1-4 closure — mental trace of all four unreachable-Supabase scenarios:**
   ITEMS: `SUPABASE_URL` unset + drift detected → `localSupabaseReason | `SUPABASE_URL=http://127.0.0.1:54321` + drift → "local dev U | `SUPABASE_URL=http://prod.supabase.co` + RPC throws (network | Reachable production Supabase + RPC succeeds + drift detecte

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/notification-design-memo.md:26 claim=Two(2) noun=facts list@28 count=2(raw 2)
   TEXT: IRST_SEEN_REVIEW, MI-6..MI-14 stagings, MI-1..MI-5b hard fails, system errors). Two facts make this assumption fragile:
   ITEMS: **Doug's surface is Drive.** His natural action is "edit a s | **The dashboard is invisible until needed.** It's a destinat

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/notification-design-memo.md:86 claim=two(2) noun=layers list@88 count=2(raw 2)
   TEXT:  all stage-for-approval events from a single parse run"). The push surface adds two MANDATORY layers on top:
   ITEMS: **Per-show coalescing across syncs.** If Show X already has  | **Cross-show daily digest is the default for tier-2.** Tier-

PARITY docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/shape-sessions/2026-05-13-pinch-zoom-lightbox.md:145 claim=two(2) noun=factors list@147 count=2(raw 2)
   TEXT: d a Playwright real-browser test for synthetic multi-touch pinch. During craft, two factors deferred this:
   ITEMS: **No existing diagram-lightbox e2e fixture.** Adding one req | **CDP multi-touch synthesis is brittle.** Playwright's `page

PARITY docs/superpowers/plans/2026-07-18-modal-close-exit-anim/00-overview.md:69 claim=two(2) noun=halves list@71 count=2(raw 2)
   TEXT: sert a synchronous close. Under the animation they must assert the contract's **two halves**, which is stronger coverage than before, not weaker:
   ITEMS: **Reduced motion → still instant.** Override `window.matchMe | **Motion enabled → exit first, close after.** With the defau

PARITY docs/superpowers/plans/2026-07-18-modal-header-reconciliation/01-shell-and-strip.md:42 claim=three(3) noun=tests list@43 count=3(raw 3)
   TEXT: - [ ] **Step 2: write the three tests.**
   ITEMS: **T-SUBHEADER-SLOT:** `subHeader` provided → an element with | **T-SUBHEADER-FALSEY:** `subHeader={false}` → NO band elemen | **T-STEP3-INVARIANT:** two SCOPED assertions, NOT a whole-pa

PARITY docs/superpowers/plans/2026-07-18-modal-header-reconciliation/03-resync.md:120 claim=four(4) noun=drifts list@121 count=4(raw 4)
   TEXT: - [ ] **Step 9: land all four registry drifts IN THIS COMMIT.**
   ITEMS: **D1 —** delete the `"ReSyncButton.tsx"` row from `accent-bu | **D2 —** update `app/help/admin/per-show-panel/page.mdx` cop | **D3 —** run `pnpm vitest run tests/styles/_metaDestructiveC | **D4 —** rescope `admin-parse-panel.spec.ts:269-274` from `o

PARITY docs/superpowers/plans/2026-07-19-admin-modal-realtime-refresh.md:496 claim=Three(3) noun=discoveries list@498 count=3(raw 3)
   TEXT: oracle floors stand (`realtimeOracle.ts` committed with the raw frames quoted). Three implementation-relevant discoveries, each folded in:
   ITEMS: **`SUPABASE_JWT_SECRET` + `SUPABASE_REALTIME_ISS` were UNSET | **Local Realtime speaks the Phoenix V2 ARRAY serializer** (` | **The probe (and Task 4's spec) MUST `settleDashboardAdminSt

PARITY docs/superpowers/plans/2026-07-19-codex-guard/00-plan.md:206 claim=two(2) noun=tests list@207 count=2(raw 2)
   TEXT: and these strengthenings to A4's two tests:
   ITEMS: scenario 1 additionally asserts `calls[0]!.cwd === run.cwdDi | scenario 2's last message keeps the fence + emphasis cases a

PARITY docs/superpowers/plans/2026-07-19-codex-guard/00-plan.md:602 claim=Three(3) noun=pieces list@603 count=3(raw 3)
   TEXT: - [ ] **Step 3: Implement.** Three pieces:
   ITEMS: Resume branch in `selectRung` (after cache branch, before ge | The `external_signal` mapping in `runAttempt`'s post-loop bl | Signal handlers — emergency TERM+KILL sweep (no grace), live

MISMATCH docs/superpowers/plans/2026-07-19-crew-row-controls/plan.md:897 claim=three(3) noun=tests list@898 count=2(raw 2)
   TEXT: - [ ] **Step 5: Update the three pinning tests.**
   ITEMS: `tests/styles/_metaDestructiveConfirm.test.ts`: after the Pi | `tests/app/admin/showReviewModalLoader.test.tsx` + `tests/co

MISMATCH docs/superpowers/plans/2026-07-20-share-hub-fidelity-fixes/00-overview.md:56 claim=6(6) noun=re-runs list@57 count=1(raw 1)
   TEXT:  so no registry row and no occurrence index shifts. **Task 6 re-runs it to prove this.**
   ITEMS: `tests/admin/no-inline-email-normalization.test.ts` — no ema

PARITY docs/superpowers/plans/2026-07-20-warning-card-copy-restore.md:444 claim=two(2) noun=mounts list@445 count=2(raw 2)
   TEXT: ling tests first — real browser.** Extend `_compactAlertCardLiveEntry.tsx` with two additional mounts (the harness never references `compactTrigger` — the prop stays internal to `CompactAlertHelp` after Step 4,
   ITEMS: `<div data-testid="mount-warning-card">` mounting the REAL ` | `<div data-testid="mount-attention-banner">` mounting the RE

MISMATCH docs/superpowers/plans/2026-07-20-warning-surface-trim/plan.md:168 claim=six(6) noun=fixtures list@169 count=4(raw 4)
   TEXT: Five states, six fixtures:
   ITEMS: (a) List with `here > 0` AND rows elsewhere: the list render | (b) Silent: the body container's `childNodes` is empty while | (c) Elsewhere: the line's `textContent` equals `Nothing else | (d) Clean with zero warnings and (e) Clean with warnings tha

PARITY docs/superpowers/plans/2026-07-21-attention-scenario-gallery-handoff.md:240 claim=Two(2) noun=failures list@242 count=2(raw 2)
   TEXT: Two compounding failures, both mine:
   ITEMS: **A citation I did not verify.** The spec cited `ResolveAler | **A tautological test.** My containment test injected a synt

PARITY docs/superpowers/plans/2026-07-21-attention-scenario-gallery.md:1938 claim=two(2) noun=shapes list@1939 count=2(raw 2)
   TEXT: 4: Add the two branch tests that Step 3 does not cover.** These distinguish the two failure shapes, which are **not** the same branch:
   ITEMS: **Zero-write failure** — the first Supabase call returns `{  | **Post-commit partial** — an early call succeeds and a later

PARITY docs/superpowers/plans/2026-07-22-dev-modal-capture/HANDOFF.md:45 claim=8(8) noun=findings list@46 count=8(raw 8)
   TEXT: n rebuild, allSettled single-flight, explicit null feed). HOSTS+TESTS BLOCKING, 8 findings — triage:
   ITEMS: F3 "deleted focus suites" REFUTED: origin/main had moved (PR | F1 call-boundary: 5 claimed sites — 4 already destructured ` | F2 published sentinel-retry re-mounts the kebab row per atte | F4 teardown failure-safety: collect-errors-throw-at-end clea | F5 dummy-panel leak: tagged + torn down per test; threading  | F6 exact published-allowlist key-set pin added. | F7 concurrency tautology removed; timer proof now `vi.getTim | F8 env-stub hygiene: afterEach unstub, no hard delete of run

MISMATCH docs/superpowers/plans/2026-07-22-dev-modal-capture/SPIKE.md:35 claim=9(9) noun=implementations list@37 count=4(raw 4)
   TEXT: ## Operational findings (bind the Task 7-9 implementations)
   ITEMS: **React reconciliation wipes foreign DOM injected into the p | **tsx-serialized `page.evaluate` callbacks need `window.__na | **html2canvas ESM bundle exposes `.default`** — unwrap when  | Dashboard state: real-app modal requires `settleDashboardAdm

PARITY docs/superpowers/plans/2026-07-22-warning-announcer-copy/plan.md:32 claim=two(2) noun=tests list@33 count=2(raw 2)
   TEXT: - [ ] **Step 1: Author two tests.**
   ITEMS: Announcer: appended as the FINAL test of the existing serial | Reveal: its own `test.describe` with its OWN seeded show rou

PARITY docs/superpowers/plans/2026-07-22-warning-panel-polish/handoff.md:10 claim=seven(7) noun=changes list@12 count=7(raw 7)
   TEXT: The seven owner-ratified changes graduating warning-surface-trim (2026-07-21) deferrals:
   ITEMS: Popover trigger-context + follow-up as two paragraphs (`Hove | Count-tuple live region on the published Parse-warnings pane | Follow-up sentence excluded from per-card `aria-describedby` | Seamless extras container in the Silent state (`opts.seamles | Callout actionability gate (`INFO_CODE_ACTIONABILITY` + two- | Pointer sentence names elsewhere sections, bolded + tappable | Stale `data-warning-index` comment corrected (spec §3.6)

PARITY docs/superpowers/plans/2026-07-23-gallery-action-outcomes/CLOSEOUT.md:38 claim=6(6) noun=findings list@39 count=6(raw 6)
   TEXT: - **R1 (Codex, fresh-eyes): NEEDS-ATTENTION, 6 findings — all verified real, all repaired:**
   ITEMS: F1 (P1) archive-refusal scenario was unreachable: `finalizeO | F2 (P2) resolve reachability accepted actionable HOLDS, whic | F3 (P2) malformed-payload gaps: resync success `outcome` now | F4 (P2) sweep could not detect an unreachable scripted contr | F5 (P3) shrink e2e test now asserts `data-gallery-scripted-w | F6 (P2) final full-suite green evidence recorded below.

PARITY docs/superpowers/plans/2026-07-23-sharehub-focus-pass.md:262 claim=three(3) noun=bullets list@263 count=3(raw 4)
   TEXT: In `DEFERRED-archive.md`, append to the Share hub section the original three bullets plus final dispositions:
   ITEMS: P1 caret anchor: RESOLVED (`fix/sharehub-caret-anchor`, comm | P2 focus-ring inconsistency: RESOLVED by the two-tier pass ( | P3 caret `shadow-popover`: RATIFIED no-shadow (rotated-diamo

MISMATCH docs/superpowers/plans/2026-07-24-attention-index.md:91 claim=two(2) noun=files list@93 count=3(raw 3)
   TEXT: ferent component and stay untouched. The dropdown-heading consumers are exactly two test files:
   ITEMS: `tests/components/admin/showpage/attentionMenuGroups.test.ts | `tests/dev/fullSplitCompositeRender.test.tsx` — **repaired H | `tests/components/admin/showpage/pageTransitions.test.tsx:14

PARITY docs/superpowers/plans/2026-07-24-attention-index.md:317 claim=three(3) noun=invariants list@319 count=3(raw 3)
   TEXT: **8b — live harness, the three open-menu invariants.** Add a `describe` block to `tests/e2e/attention-pill-focus.spec.ts` (`getBoundingClientRect`, 0.5px t
   ITEMS: every needs-you row height ≥ 44px — the harness's initial st | the scroll region clips at `max-h-96` with 12 needs-you rows | a long title does not widen the panel past `w-[min(400px,cal

PARITY docs/superpowers/plans/2026-07-24-gallery-global-scope-exclusion/00-plan.md:377 claim=two(2) noun=pins list@379 count=2(raw 2)
   TEXT: Plus the two existing composite pins, updated to their post-removal values:
   ITEMS: `tests/dev/fullSplitComposite.test.ts:28` drops `"SYNC_STALL | `tests/dev/fullSplitCompositeRender.test.tsx:56` and its `it

PARITY docs/superpowers/plans/2026-07-24-picker-flow-app-bugs.md:302 claim=Two(2) noun=facts list@304 count=2(raw 2)
   TEXT: finding 4). But R3 finding 3 showed the first version of this task overclaimed. Two facts from the repo's own coverage machinery bound what is achievable:
   ITEMS: `crew-e2e.yml` has a `pull_request.paths` filter, and the sc | The repo already has the right vocabulary for what it does b

PARITY docs/superpowers/plans/2026-07-24-picker-flow-app-bugs.md:316 claim=Two(2) noun=mechanics list@317 count=2(raw 2)
   TEXT: , so leaving it as `UNSEEN` would be a false annotation in the other direction. Two mechanics matter here and are easy to get backwards:
   ITEMS: **The row must stay.** `scanWorkflowCoverage` excludes path- | **The flip is not gate-enforced.** Both category constants a

PARITY docs/superpowers/plans/2026-07-24-picker-flow-app-bugs.md:387 claim=Two(2) noun=dispatches list@388 count=2(raw 2)
   TEXT: local-passes-CI-fails class this project treats as its own gate (R4 finding 5). Two dispatches, both **blocking** — a `workflow_dispatch` run is asynchronous and not merge-blocking, so firing and moving on woul
   ITEMS: `dev-gate-e2e.yml`, always: it is `workflow_dispatch`-only ( | `crew-e2e.yml`, if the PR's path filter did not already trig

PARITY docs/superpowers/plans/2026-07-24-sharehub-viewport-popover-and-archive-copy.md:115 claim=three(3) noun=dispositions list@117 count=3(raw 3)
   TEXT: Each registry row carries a file and one of three dispositions with a reason string:
   ITEMS: `placement-module` — resolves geometry through `lib/popover/ | `fit-within-clip` — clip-safe via `useFitWithinClip` instead | `not-clip-constrained` — anchored overlay that is not inside

PARITY docs/superpowers/plans/2026-07-25-section-header-rebuild-and-phantom-spacers-closeout.md:151 claim=Two(2) noun=properties list@153 count=2(raw 2)
   TEXT: Two properties of this design would otherwise read as issues and must not be relitigated:
   ITEMS: **The icon replaces the words “In sheet.”** Requested direct | **The name + count are centred while section bodies are left

MISMATCH docs/superpowers/plans/2026-07-25-section-header-rebuild-and-phantom-spacers.md:345 claim=3(3) noun=was list@346 count=2(raw 2)
   TEXT:  **Two seams must be opened, and round 3 was right that the plan had not named them:**
   ITEMS: **BellPanel's action row is unreachable as written.** `Actio | **AdminNav needs `PathnameContext`, NOT `AppRouterContext`**

MISMATCH docs/superpowers/plans/2026-07-26-agenda-perday-viewer-fold.md:167 claim=five(5) noun=rounds list@168 count=6(raw 6)
   TEXT:  five review rounds did not name.*
   ITEMS: **the discrimination that guard depends on**: a restriction  | all four positional-fallback conditions negated independentl | **NO null-element guard test, and that is deliberate (review | **the positional fallback indexes the FULL aggregate day lis | `{ kind: "all" }` is returned, never `{ kind: "subset"; rows | every expected index derived from fixture dimensions, never 

MISMATCH docs/superpowers/plans/2026-07-26-sheet-icon-link-affordance-class.md:42 claim=three(3) noun=anchors list@44 count=7(raw 7)
   TEXT: Beyond the three anchors, the box→overlay change touches these companion surfaces, each assigned to a task below:
   ITEMS: `PublishedReviewModal.tsx:36` — `ExternalLink` import orphan | 44px-box companion comments: `PublishedReviewModal.tsx:9` an | `components/admin/showpage/ShowReviewModalSkeleton.tsx:73-83 | `components/admin/wizard/Step3ReviewModal.tsx:467-470` — dev | `tests/components/admin/showpage/publishedReviewModal.test.t | `tests/e2e/_sectionHeaderCellHarness.tsx:294-315` — the harn | `tests/e2e/_publishedReviewModalHarness.tsx` lines 336-411 a

PARITY docs/superpowers/plans/2026-07-27-export-blank-row-segmentation/00-overview.md:104 claim=two(2) noun=surfaces list@106 count=2(raw 2)
   TEXT: Red-first, two test surfaces:
   ITEMS: `tests/drive/exportSheetToMarkdown.test.ts` additions (spec  | `tests/drive/round-trip-fixture.test.ts` EXTENSION (spec §6 

MISMATCH docs/superpowers/plans/2026-07-31-archive-lifecycle-race-cluster.md:167 claim=17(17) noun=hits list@168 count=4(raw 4)
   TEXT: ARCHIVE-ARMED-CONCURRENT-REFRESH" BACKLOG.md BACKLOG-archive.md docs/` returned 17 hits, dispositions:
   ITEMS: BACKLOG lines 165, 937, and 943 — the three active entries:  | `BACKLOG-archive.md:1027` — historical reference inside an a | `docs/superpowers/specs/2026-07-24-archive-row-menu-idiom.md | `docs/superpowers/specs/2026-07-31-archive-lifecycle-race-cl

MISMATCH docs/superpowers/plans/2026-08-01-ledger-guard-mdast-rewrite.md:52 claim=EIGHT(8) noun=hits list@53 count=3(raw 3)
   TEXT:  command `rg -n "BL-LEDGER-GUARD-MDAST-REWRITE" --no-heading | cut -d: -f1,2` — EIGHT hits, per-hit dispositions (plan-r1 F3):
   ITEMS: root `BACKLOG.md` line 7 — reconciliation-note prose recordi | root `BACKLOG.md` line 11 — the entry heading: MOVES to arch | design spec lines 5 and 164, this plan (title/charter lines 

PARITY docs/superpowers/plans/2026-08-02-copy-deadcode-sweep.md:13 claim=two(2) noun=files list@15 count=2(raw 2)
   TEXT: **CREATES one guard, in two files** — the layer split the repo already uses for `tests/docs/_invariant8Closeout.ts` + `tests/docs/_metaInvariant8Closeout.
   ITEMS: `tests/components/_orphanedComponents.ts` — the pure core (t | `tests/components/_metaOrphanedComponents.test.ts` — the gua

MISMATCH docs/superpowers/plans/2026-08-02-copy-deadcode-sweep.md:20 claim=four(4) noun=registries list@22 count=5(raw 5)
   TEXT: **EXTENDS four existing registries:**
   ITEMS: `tests/messages/popoverContextCopy.test.ts` — one `FROZEN` r | `tests/messages/_metaEmphasisRenderContract.test.ts` — remov | `tests/help/page-per-show-panel.test.tsx` — two new prose as | `tests/docs/_metaDeferralLedgerGraduation.test.ts` — three ` | `tests/docs/_metaLedgerReferentialIntegrity.test.ts` — **act

PARITY docs/superpowers/plans/2026-08-02-copy-deadcode-sweep.md:106 claim=two(2) noun=comments list@107 count=2(raw 2)
   TEXT: 6. Correct the two stale header comments, both measured:
   ITEMS: `components/admin/StagedReviewCard.tsx` header currently rea | `components/admin/ChangesFeed.tsx` header currently justifie

PARITY docs/superpowers/plans/2026-08-03-app-wide-font-binding.md:53 claim=THREE(3) noun=places list@55 count=3(raw 3)
   TEXT: **Register it in THREE places — the Playwright project, a CI workflow, and the coverage census.** Spec review R2 caught that an earlier draft did o
   ITEMS: `playwright.config.ts:78-79`, the `desktop-chromium` `testMa | `.github/workflows/crew-e2e.yml`'s explicit spec list, plus  | `tests/ci/_metaE2eWorkflowCoverage.test.ts`'s allowlist — cr

MISMATCH docs/superpowers/plans/2026-08-03-apply-undo-audit-fidelity.md:549 claim=Three(3) noun=constraints list@551 count=4(raw 4)
   TEXT: Three verified constraints decide where each test lives:
   ITEMS: **`capabilityRoleChangesForNotice` is module-local, not expo | **`FakePhase2Tx` has no `holdPort`** — zero occurrences in ` | **`Phase2Result` is a discriminated union.** Its `stale` bra | **The crew helpers are `crew(name, email?)` (`tests/sync/pha

PARITY docs/superpowers/plans/2026-08-03-apply-undo-audit-fidelity.md:764 claim=Two(2) noun=repairs list@766 count=2(raw 2)
   TEXT: **Two different repairs, and the distinction is the task.**
   ITEMS: **Seven are mechanical.** Pass `identityLinkRenames` alongsi | **One is not.** The REPLACED-shape test (`tests/db/undo-chan

PARITY docs/superpowers/plans/2026-08-03-apply-undo-audit-fidelity.md:1141 claim=Three(3) noun=traps list@1143 count=3(raw 3)
   TEXT: **Context — read the spec's §2.3 in full before starting. Three traps:**
   ITEMS: **The helper already exists.** `emitDeferredRoleFlagsNotice` | **Both obvious emit placements are wrong.** Per row is insid | **The carrier must cross the commit boundary.** Not `PerRowR

PARITY docs/superpowers/plans/2026-08-03-close-v1-override-wont-build.md:17 claim=Two(2) noun=consequences list@19 count=2(raw 2)
   TEXT: Two mechanical consequences the implementer must respect:
   ITEMS: **The declared branch must exist on `origin` while the marke | **The status line must NOT travel into the archive.** Archiv

PARITY docs/superpowers/plans/2026-08-03-close-v1-override-wont-build.md:174 claim=two(2) noun=invariants list@176 count=2(raw 2)
   TEXT: d **RED = 4 hits**, at exactly L171, L205, L211, L212. **GREEN = 0 hits**, with two invariants alongside it:
   ITEMS: `grep -c 'BL-VERSION-AMBIGUOUS-V1-OVERRIDE' "$D"` still retu | L23 and L201 still contain "deferred" and are untouched — th

PARITY docs/superpowers/plans/2026-08-03-finalize-cas-source-anchors.md:202 claim=two(2) noun=cases list@204 count=2(raw 2)
   TEXT: Extend the Task-2 file with two cases, both running Phase B then Phase D end to end:
   ITEMS: **Refresh:** live show seeded with `shows.source_anchors = P | **Wipe guard:** live show seeded with `shows.source_anchors 

PARITY docs/superpowers/plans/2026-08-03-lead-capability-prose-settle.md:577 claim=Two(2) noun=rounds list@579 count=2(raw 2)
   TEXT: contract test, NOT a RED.** Create `tests/db/isAdminRoleFlagsContract.test.ts`. Two rounds shaped it:
   ITEMS: **R6** refuted scanning a migration file: slicing one histor | **R7** refuted asking the database *lexically*: a definition

PARITY docs/superpowers/plans/2026-08-03-modal-freshness-cue.md:207 claim=Three(3) noun=details list@209 count=3(raw 3)
   TEXT: Three details are easy to get subtly wrong and each has a test aimed at it:
   ITEMS: The per-batch expiry effect must NOT return a cleanup, or ar | The attribute value flips PER SECTION, read out of that sect | The announcement's inner node is keyed by batch, not by text

PARITY docs/superpowers/plans/2026-08-03-needs-attention-holds-rollup.md:719 claim=nine(9) noun=orders list@720 count=9(raw 9)
   TEXT: e LITERAL anchors (exact Step 1 insertions; one regex provably cannot match all nine word orders):
   ITEMS: `components/admin/Dashboard.tsx` tooltip: `held crew identit | `app/admin/needs-attention/page.tsx` tooltip: `held crew ide | dashboard mdx inventory slice (above "Changes and review"):  | dashboard mdx live-change slice (below it): `That held chang | review-queues mdx table slice (above "Live-show changes"): ` | review-queues mdx bullet slice (below it): `It also appears  | `app/help/daily-rhythm/page.mdx`: `Held crew identity change | `app/help/admin/settings/page.mdx`: `crew identity changes t | `app/help/tour/page.mdx`: `crew identity changes held for yo

PARITY docs/superpowers/plans/2026-08-03-needs-attention-holds-rollup.md:802 claim=three(3) noun=defects list@804 count=3(raw 3)
   TEXT: **The three defects that were real code bugs**, as opposed to test-strength findings:
   ITEMS: **Rejection containment (round 1, 2 HIGH).** The two holds a | **Copy that was factually false (rounds 1 and 3, 2 MEDIUM).* | **Mobile overflow (round 2, 1 HIGH).** Hold summaries embed 

PARITY docs/superpowers/plans/2026-08-03-orphan-components-lead-prose-closeout.md:32 claim=Three(3) noun=checks list@34 count=3(raw 3)
   TEXT: Three independent checks agree:
   ITEMS: **Bucket classification** (`git diff origin/main -- 'app/**' | **Comment-stripped equality** (Assessment B): every survivin | **Reachability**: no JSX render site and no import specifier

MISMATCH docs/superpowers/plans/2026-08-03-orphan-components-lead-prose.md:139 claim=two(2) noun=kinds list@140 count=3(raw 3)
   TEXT:  not TDD). Every row is one of two kinds, and the KIND is a field, not a comment:
   ITEMS: `archive` — a path GLOB whose every match is a dated record  | `line` — one historical line inside a LIVE file, keyed by th | `pending` — a live reference, keyed the same way, owned by `

PARITY docs/superpowers/plans/2026-08-03-picker-signin-flow-cluster.md:185 claim=Two(2) noun=guards list@187 count=2(raw 2)
   TEXT: Two route-level guards stay, because they pin different seams:
   ITEMS: **Intent binding.** A `t` token signed for a different slug  | **Allow-list pass-through.** `?s=schedule&evil=1` → emitted 

PARITY docs/superpowers/plans/2026-08-03-pre-review-gate-arms.md:51 claim=Two(2) noun=defects list@53 count=2(raw 2)
   TEXT: led, depth=2, tasks=7` — and after the repairs below reports **zero findings**. Two real defects surfaced only when the rules were executed rather than read:
   ITEMS: The AC boundary rule rejected `**Verify.** AC-14.` — `.` is  | `TASK_MARKER_ORPHANED` overlapped the form codes for a malfo

PARITY docs/superpowers/plans/2026-08-03-scanner-precision-cluster.md:37 claim=three(3) noun=registries list@39 count=3(raw 3)
   TEXT: **EXTENDS three existing registries:**
   ITEMS: `tests/dev/attentionScenariosWarnings.test.ts` — residue tes | `tests/docs/_metaLedgerReferentialIntegrity.test.ts` — eight | `tests/docs/_ledgerMdast.walker.test.ts` — plants P1–P4 and 

PARITY docs/superpowers/plans/2026-08-03-scanner-precision-cluster.md:155 claim=Three(3) noun=bindings list@157 count=3(raw 3)
   TEXT: **Three bindings this change must satisfy:**
   ITEMS: **`tests/cross-cutting/cron-run-summary-scanner-safety.test. | **`tests/cross-cutting/no-raw-codes.test.ts:34`** asserts `I | **`buildForbiddenCodeIndex`** (`tests/cross-cutting/no-raw-c

MISMATCH docs/superpowers/plans/2026-08-03-scanner-precision-cluster.md:181 claim=two(2) noun=ones list@182 count=3(raw 3)
   TEXT:  to contain an instance. One case per family A-a…A-g. The two load-bearing ones:
   ITEMS: **A-b (AC-A5)** a factory returning `Alias["warnings"][numbe | **A-c (AC-A6)** `{ severity: WARN_SEVERITY, code: "X", messa | **A-f (AC-A7)** a site whose `code` resolves to no literal —

PARITY docs/superpowers/plans/2026-08-04-backlog-convergence-c-guards-closeout.md:27 claim=Three(3) noun=entries list@29 count=3(raw 3)
   TEXT: Three entries under-described their own fix, and each gap was itself a defect:
   ITEMS: **The comparator** needed three things its entry never menti | **The rebuild-exhaustion emit** exposed a real gap in the in | **The canonical-class entry's prescribed fix does not apply 

PARITY docs/superpowers/plans/2026-08-04-harness-font-fidelity.md:1374 claim=Two(2) noun=consequences list@1376 count=2(raw 2)
   TEXT: runs once against its own harness document and once against the running server. Two consequences:
   ITEMS: `assertMonoPartition(page, route)` must treat **a page match | The **freshness assertion does not run in the fixture**. "Ev

PARITY docs/superpowers/plans/2026-08-04-harness-font-fidelity.md:1770 claim=Two(2) noun=constraints list@1772 count=2(raw 3)
   TEXT: **The marker comes off HERE, and this task is deliberately LAST.** Two constraints force it:
   ITEMS: `tests/docs/_metaLedgerInProgress.test.ts:77-81` asserts "ar | AGENTS.md invariant 12 puts removal in the PR's **last commi

PARITY docs/superpowers/plans/2026-08-04-harness-font-fidelity.md:1870 claim=Three(3) noun=settles list@1872 count=3(raw 3)
   TEXT: **Three things this settles that were open or wrong:**
   ITEMS: **The overrides are the latin-derived figures** (89.79 / 22. | **`DESIGN.md:141`'s `size-adjust: 107.89%` is CORRECT and cu | **The generated family is lowercase `inter`**, derived from 

PARITY docs/superpowers/plans/2026-08-04-review-round-economy.md:4415 claim=Two(2) noun=edits list@4417 count=2(raw 2)
   TEXT: ange rather than a diff; the AGENTS.md prose in Step 5 is the durable contract. Two edits inside each enumerated `$CLAUDE_CONFIG_DIR/hooks/review-convergence-gate.sh`:
   ITEMS: Replace the inferring tally. The current assignment greps th | Replace the `~$FINDINGS findings` fragment in the block mess

MISMATCH docs/superpowers/plans/2026-08-04-review-round-economy.md:4446 claim=three(3) noun=fixtures list@4448 count=5(raw 6)
   TEXT: ur prior `.review/` dirs) and confirm the block message appears. Run it against three prepared fixtures:
   ITEMS: Prior rounds whose terminal messages carry `FINDINGS: <n>` l | Prior rounds whose messages carry none — must print `not dec | **The divergence case, which is the reason this task exists: | **Rollout recovery, where the terminal message is not an att | **An exhausted round whose final attempt is empty.** `attemp

PARITY docs/superpowers/plans/2026-08-04-review-round-economy.md:4804 claim=Three(3) noun=defects list@4806 count=3(raw 3)
   TEXT: **Three defects the verification pass caught in this plan's own first draft**, recorded because each is a class the project's writin
   ITEMS: **An invented fixture shape.** Every wrapper test snippet us | **A test directory that runs in the wrong project.** `BASE_I | **`guardVersion` typed as a string** from the spec's schema 

PARITY docs/superpowers/plans/2026-08-06-l-wave/plan.md:53 claim=Three(3) noun=commands list@55 count=3(raw 3)
   TEXT: at `docs/superpowers/plans/2026-08-06-l-wave/l1-coverage-sweep-2026-08-06.txt`. Three enumeration commands (all re-run at execution and the table below re-verified against their output):
   ITEMS: Unconditional suites: `grep -rln "describe\.skip(\|test\.fix | Aliased/conditional: `grep -rn "? describe : describe\.skip" | Prose sweeps: the phrase-family grep (widened: `compound tes

PARITY docs/superpowers/plans/2026-08-06-l-wave/plan.md:107 claim=3(3) noun=archives list@109 count=3(raw 3)
   TEXT: ### Task L6 — newly-L classification stamps + 3 executed archives
   ITEMS: Stamp all 11 stamp-only entries per the spec §2.1.6 table (` | Execute the archives (archive RED each): BL-ADOPTION-PIN-REA | Stamp BL-WATCH-PROMOTION-ACTIVATION-RACE PARKED (spec §2.1.7

PARITY docs/superpowers/plans/2026-08-07-classname-array-join-cn.md:182 claim=three(3) noun=kinds list@183 count=3(raw 3)
   TEXT:  and assert each is one of the three kinds the base is measured to contain:
   ITEMS: a non-empty string literal; | one of the eight proven-truthy identifiers from spec §4.1's  | a conditional (ternary) expression — possibly nested — **eve

PARITY docs/superpowers/plans/2026-08-07-classname-array-join-cn.md:304 claim=Two(2) noun=modes list@306 count=2(raw 2)
   TEXT:  original branch base.** Two failure modes bracket the correct value:
   ITEMS: Re-resolving `HEAD` at the second invocation reads the alrea | Hard-coding the pre-rebase branch base (`61281c23e`) is wron

PARITY docs/superpowers/plans/2026-08-07-classname-array-join-cn.md:562 claim=Two(2) noun=steps list@564 count=2(raw 2)
   TEXT: neither default project — probed `{mobile:false, desktop:false}`. Two steps, both required:
   ITEMS: **The name must not substring-match an existing alternative. | **Wire it into CI — invocation only, and deliberately NO pat

PARITY docs/superpowers/plans/2026-08-07-classname-array-join-cn.md:725 claim=three(3) noun=things list@727 count=3(raw 3)
   TEXT: **Then hold the three things this change could disturb:**
   ITEMS: **Geometry.** Re-run Task 5's spec. Every recorded box match | **Operands.** Re-run `node scripts/verify-cn-operand-parity. | **Transitions.** Spec §9.5 declares no transition is added, 

MISMATCH docs/superpowers/plans/2026-08-07-ops-log-code-emits.md:69 claim=Two(2) noun=cases list@71 count=1(raw 1)
   TEXT:  `LogRecord[]`, dynamic `import` of the route, `resetLogSink()` in a `finally`. Two cases:
   ITEMS: Drive `app/auth/callback/route.ts`'s `GET` with a valid `cod

PARITY docs/superpowers/plans/2026-08-07-ops-log-code-emits.md:268 claim=two(2) noun=conditions list@270 count=2(raw 2)
   TEXT: **Premise (mandatory).** The assertions rest on two conditions, both stated via `premise`/`premiseHolds` from `tests/_shared/premise.ts` immediately above the negative assertion:
   ITEMS: `process.env.GOOGLE_SERVICE_ACCOUNT_JSON` contains the senti | the captured records filtered to `ONBOARDING_OPERATOR_ERROR`

PARITY docs/superpowers/plans/2026-08-07-round-economy-followups.md:297 claim=Two(2) noun=properties list@299 count=2(raw 2)
   TEXT: Two pipeline properties worth knowing, both discovered by running a review this way:
   ITEMS: **A codex-guard review of a MERGED branch mints a phantom ar | **The reviewer could not run the test suite.** Its own repor

PARITY docs/superpowers/plans/2026-08-09-admin-nav-badge-suspense.md:32 claim=Two(2) noun=artifacts list@34 count=2(raw 2)
   TEXT: Two measurement artifacts worth recording, because each produced a false reading first:
   ITEMS: **The dev server 403s subresources whose `Origin` is `127.0. | **A click on a `<Link>` before hydration is a native full-do

PARITY docs/superpowers/plans/2026-08-09-app-e2e-batch1.md:572 claim=Two(2) noun=invariants list@574 count=2(raw 3)
   TEXT: Two invariants this order satisfies, and how:
   ITEMS: **Invariant 12** — the marker rides the branch for as long a | **The reviewed diff is the merged diff** (plan R1 finding 9)

PARITY docs/superpowers/plans/2026-08-09-next-1630-wedge-remeasure.md:163 claim=three(3) noun=facts list@164 count=3(raw 3)
   TEXT: Then the per-sample classifier. Its three anchor facts were settled by a LOCAL EMPIRICAL PROBE against this worktree's own Playwright (1.59.1, CI=1, trace=on, repeat-
   ITEMS: Runner-side `console.log` output lands in the **`test.trace` | A FAILED test invocation's `test-results/` dir contains an * | Repeat dirs are named `<base>` (repeat 0) then `<base>-repea

PARITY docs/superpowers/plans/2026-08-09-next-1630-wedge-remeasure.md:386 claim=three(3) noun=terminals list@387 count=3(raw 4)
   TEXT: **Red routing (spec §6 — three terminals: GREEN / PRE-EXISTING-RED / ESCALATED):**
   ITEMS: `screenshots-drift.yml` red AT its byte-comparison step (act | Any other red (including screenshots-drift red BEFORE compar | Merge requires all six at GREEN or PRE-EXISTING-RED.

MISMATCH docs/superpowers/plans/2026-08-09-resurrect-mobile-safari-e2e.md:53 claim=11(11) noun=archives list@55 count=1(raw 1)
   TEXT: | `BACKLOG.md` entry + the spec doc's self-references | Task 11 archives entry (step 4); spec is this arc's artifact |
   ITEMS: **Registry count reconciliation (authored AND run):** allowl

PARITY docs/superpowers/plans/2026-08-09-resurrect-mobile-safari-e2e.md:97 claim=four(4) noun=parts list@99 count=4(raw 4)
   TEXT: ll` testid visible before every toggle interaction. Contract per spec §3.2 (all four parts):
   ITEMS: Persistence BOTH directions: light→dark tap → `data-theme` f | No-FOUC first-body-child oracle. **As-drafted (superseded):* | A11y states across the cycle: light mode `aria-pressed="fals | Tap target ≥44×44 (`--spacing-tap-min`, DESIGN.md §3).

PARITY docs/superpowers/plans/admin/2026-07-03-developer-tier/07-structural-and-e2e.md:30 claim=four(4) noun=enforcements list@32 count=4(raw 4)
   TEXT:  row per §6 surface: `{ id, file, consumerKind, gate, declaredPosture }`), then four enforcements:
   ITEMS: **Server-action gate coverage (AST).** For each registered d | **Admin-gate assertion:** `app/admin/settings/admins/actions | **Route/page coverage:** `PROTECTED_ROUTES` contains the dev | **Mutation-RPC SQL guard:** read `supabase/migrations/202607

MISMATCH docs/superpowers/plans/admin/2026-07-15-extend-role-scope-vocab/tasks-09-12.md:208 claim=10(10) noun=deltas list@209 count=5(raw 5)
   TEXT: Same step structure as Task 10 with these deltas:
   ITEMS: Provenance reads the wizard session's STAGED parse warnings  | Follow-up = re-stage through the existing staging entry poin | Staged `state: "applied"` = re-stage completed and the refre | Registry row `{ file: "app/admin/onboarding/_actions/roleTok | Commit: `feat(admin): mapRoleTokenStaged wizard create actio

PARITY docs/superpowers/plans/admin/2026-07-16-destructive-confirm-pass.md:330 claim=three(3) noun=changes list@332 count=3(raw 8)
   TEXT: All four get the same three changes (repeat per file — no sharing):
   ITEMS: **Restyle confirm-go** (the `bg-accent … text-accent-text …  | **Open focus (C3):** `const cancelRef = useRef<HTMLButtonEle | **Close focus (C5, two-phase):** `const triggerRef = useRef<

MISMATCH docs/superpowers/plans/admin/2026-08-03-undo-success-announcement.md:150 claim=two(2) noun=sites list@152 count=0(raw 2)
   TEXT: ## Task 7 — Thread `announceLabel` at the two call sites
   ITEMS: 

MISMATCH docs/superpowers/plans/admin/2026-08-03-undo-success-announcement.md:165 claim=seven(7) noun=branches list@167 count=0(raw 2)
   TEXT: ## Task 8 — Survival probes (spec §3.7, seven branches)
   ITEMS: 

MISMATCH docs/superpowers/plans/alerts/2026-07-04-alert-resolve-truthing/plan.md:302 claim=three(3) noun=cases list@304 count=0(raw 1)
   TEXT: ess to the real signature — the contracts are: default resolver behaves per the three cases, and a throwing resolver never collapses the run.)
   ITEMS: 

MISMATCH docs/superpowers/plans/ci/2026-07-06-mutation-testing-harness.md:1201 claim=4(4) noun=tests list@1203 count=0(raw 1)
   TEXT: mutant, correct `domains`, unique `siteId` — and adjust the mechanics. The Task-4 tests pin the contract.)*
   ITEMS: 

PARITY docs/superpowers/plans/ci/2026-08-04-source-mutation-guard-gate.md:95 claim=Three(3) noun=assertions list@97 count=3(raw 3)
   TEXT: Three assertions, because set equality alone is not enough either:
   ITEMS: `emitted === declared` as sets; | **at least one site per declared operator**, which catches a | an `integer-literal` site for a numeric inside a `PropertyAs

MISMATCH docs/superpowers/plans/crew/2026-06-15-crew-page-redesign-phase1/01-parser-context.md:613 claim=2(2) noun=begins list@615 count=0(raw 7)
   TEXT: All must be true before Phase 2 begins:
   ITEMS: 

MISMATCH docs/superpowers/plans/crew/2026-06-15-crew-page-redesign-phase1/02-shell-nav-alert.md:450 claim=4(4) noun=deletes list@452 count=9(raw 9)
   TEXT: the following GREEN before Phase 3, and (per §4.13/§10 sequencing) BEFORE Phase 4 deletes the `_ShowBody.tsx` `notes-tile` alert path:
   ITEMS: `pnpm vitest run tests/crew/ tests/components/crew/ tests/me | `pnpm vitest run tests/db/upsert-admin-alert-dedup.test.ts`  | `pnpm typecheck` clean. | **The four lockstep layers moved together** in one commit (T | **The server-side projection-alert upsert is live and sectio | `_metaSentinelHidingContract` now walks `components/crew/` ( | Both routes (crew `page.tsx` + preview-as) render `CrewShell | **Do-not-relitigate (wp-27 / §4.13):** the alert write stays | The OLD migration file `20260505000000_upsert_admin_alert.sq

MISMATCH docs/superpowers/plans/crew/2026-06-15-crew-page-redesign-phase2/02-migration-projection.md:165 claim=4(4) noun=cases list@166 count=7(raw 7)
   TEXT: **Decoder contract (per spec §4.2 `decodeRunOfShow` + test 4 deep-decoder cases):**
   ITEMS: `null` → `{ value: null, corrupt: false }` (legitimate empty | Top-level not a plain object (array, string, number, boolean | Per key: not a `YYYY-MM-DD` ISO date → that key dropped, `co | Per day value: not an array → that day dropped, `corrupt = t | Per entry: must be a plain object whose `title` is a REAL st | A day left with ZERO valid entries after filtering → that ke | `{}` (empty object) and an all-empty-after-filter object → `

PARITY docs/superpowers/plans/crew/2026-07-03-per-crew-picker-reset/plan.md:265 claim=4(4) noun=surfaces list@266 count=4(raw 4)
   TEXT: **Step 4: Register the RPC in `tests/auth/advisoryLockRpcDeadlock.test.ts` (all 4 surfaces, spec §7.6):**
   ITEMS: (a) add `"supabase/migrations/20260703000001_reset_crew_memb | (b) add `expect(lockTakingNames).toContain("reset_crew_membe | (c) add the same migration path to `lockTakingMigrations` fo | (d) add `"lib/auth/picker/resetCrewMemberSelection.ts"` to t

MISMATCH docs/superpowers/plans/crew/2026-07-06-venue-card-redesign/00-plan.md:933 claim=four(4) noun=tests list@935 count=0(raw 2)
   TEXT: ion height is still driven by the stretched parent — geometry holds. Verify all four venue tests green.)
   ITEMS: 

MISMATCH docs/superpowers/plans/crew/2026-08-09-private-image-pipeline.md:191 claim=7(7) noun=applies list@193 count=2(raw 2)
   TEXT: already ratified, so invariant 7 applies and they file rather than land:
   ITEMS: *The active lightbox slide pins the ORIGINAL with no progres | *The 16px blur edge is too coarse for line-art* and, at full

PARITY docs/superpowers/plans/data-quality/2026-06-29-finalize-blocker-show-title.md:356 claim=four(4) noun=sites list@357 count=4(raw 5)
   TEXT: Swap the four render sites (keep `key`, `data-testid`, `RescanSheetButton driveFileId` unchanged):
   ITEMS: `FinalizeButton.tsx:281` → `<span className="font-medium">{f | `FinalizeButton.tsx:310` → `<span className="font-medium">{r | `RunFinalCASButton.tsx:121` → `<span className="font-medium" | `ResumeFinalizeButton.tsx:136` → `<span className="font-medi

PARITY docs/superpowers/plans/data-quality/2026-07-02-dq-report-ignore/PLAN.md:1253 claim=three(3) noun=edits list@1254 count=3(raw 3)
   TEXT: - [ ] **Step 3: Modify the component** — three edits:
   ITEMS: Add to the props type: `renderItemControls?: (w: ParseWarnin | **Change the `<li>` key from the index-based `${w.code}-${i} | Inside the `.map((w, i) => …)`, after the `Open in Sheet` `<

PARITY docs/superpowers/plans/data-quality/2026-07-04-resync-quality-gate/00-plan.md:434 claim=three(3) noun=guards list@436 count=3(raw 3)
   TEXT: et last_sync_status='shrink_held'`) and a new `last_sync_status` literal; **all three of these guards fail in Task 1's commit unless updated in the same commit.** The implementer MUST verify each (running the e
   ITEMS: **`tests/sync/runScheduledCronSync.test.ts:624`** ("producti | **`tests/sync/_secondCopyApplyTripwire.test.ts:46`** (`ALLOW | **`tests/db/showCacheRevalidateCoverage.test.ts:56`** (`runS

PARITY docs/superpowers/plans/data-quality/2026-07-04-version-detection-confidence-gate.md:744 claim=Two(2) noun=things list@746 count=2(raw 2)
   TEXT: Two things surfaced only when the FULL suite ran (the plan's per-task gates ran scoped subsets):
   ITEMS: **Design revert: ambiguous parses best-effort, not a stub.** | **Missing touchpoint: `app/help/errors/_families.ts`.** `tes

PARITY docs/superpowers/plans/data-quality/2026-07-16-cron-wizard-owned-skip.md:28 claim=Two(2) noun=groups list@30 count=2(raw 2)
   TEXT: Two test groups by pre-implementation expectation, enumerated in Step 6:
   ITEMS: **MUST-BE-RED before implementation** (they specify the new  | **EXPECTED-GREEN before implementation** (regression pins on

MISMATCH docs/superpowers/plans/data-quality/2026-07-16-cron-wizard-owned-skip.md:45 claim=2(2) noun=reviewers list@46 count=5(raw 5)
   TEXT: - Produces (Task 2 and reviewers rely on these exact names):
   ITEMS: union member `"wizard_owned"` in `PerFileProcessorResult["re | `readPendingWizardSessionId(supabase: SyncSupabaseClient): P | `readWizardPendingSyncOwnership(supabase, driveFileId, wizar | `readWizardPendingIngestionOwnership(supabase, driveFileId,  | `readWizardDeferralOwnership(supabase, driveFileId, wizardSe

PARITY docs/superpowers/plans/data-quality/2026-07-17-use-raw-callout-preview-demotion.md:232 claim=THREE(3) noun=buckets list@233 count=3(raw 4)
   TEXT: For each hit, classify into THREE buckets:
   ITEMS: **(a) Wizard-callout mount** — a test that renders the callo | **(b) Callout preview / container** — merely renders the cal | **(c) ⚠️ STANDALONE control tests — DO NOT TOUCH.** `tests/c

PARITY docs/superpowers/plans/db-lockdown-cluster/plan.md:60 claim=four(4) noun=assertions list@62 count=4(raw 4)
   TEXT: **Red:** write four assertions with `PUBLIC_TABLE_CLASSIFICATION = {}`:
   ITEMS: **A** every `ADMIN_TABLES` entry is classified `admin_only`/ | **B** every `admin_only`/`deny_all`-classified table is in ` | **C** every live `public` relation with `relkind IN ('r','p' | **Tripwire** parse §4.3's declared counts (`docs/superpowers

PARITY docs/superpowers/plans/nav-perf/2026-06-23-nav-perf-phase2-ui-feedback/00-plan.md:39 claim=4(4) noun=files list@40 count=4(raw 4)
   TEXT: - [ ] **Step 3: Implement the 4 files**, each mirroring its route's loaded shape with `Skeleton` primitives (heights/widths from spec §5), wrapped in `LoadingSh
   ITEMS: `app/admin/settings/admins/loading.tsx` — `testId="admin-adm | `app/admin/show/staged/[stagedId]/loading.tsx` — `testId="st | `app/admin/show/[slug]/preview/[crewId]/loading.tsx` — `test | `app/help/loading.tsx` — `testId="help-loading"`; mirror the

MISMATCH docs/superpowers/plans/nav-perf/2026-06-23-nav-perf-tag-caching/00-plan.md:155 claim=two(2) noun=layers list@156 count=4(raw 4)
   TEXT: - [ ] **Step 1: Write the meta-test (spec §6 — two layers).**
   ITEMS: Registry layer: for each `disposition:"revalidate"` file, as | Discovery layer (SITE-level, Codex plan-R1 MED): run the spe | RPC layer: for each `WRITING_RPCS` name, the wrapper call si | **Multi-branch files (Codex plan-R3 MED):** a file's `siteCo

PARITY docs/superpowers/plans/nav-perf/2026-06-23-nav-perf-tag-caching/00-plan.md:167 claim=Four(4) noun=sites list@168 count=4(raw 4)
   TEXT: its own lock/tx fully resolves**, using `revalidateOnApplied(result)` (Task 2). Four sites:
   ITEMS: **Cron:** `runScheduledCronSync` has TWO post-lock write bra | **Webhook:** `runPushSyncForShow.ts` — after its `withPipeli | **Manual sync:** `runManualSyncForShow` (`:273`) — after its | **Retry:** the retry ROUTE(s) (`app/api/admin/pending-ingest

MISMATCH docs/superpowers/plans/observability/2026-06-29-centralized-logging-foundation.md:1383 claim=13(13) noun=adds list@1385 count=0(raw 1)
   TEXT: (Task 13 adds `requestId: scanRequestId` to this call.)
   ITEMS: 

PARITY docs/superpowers/plans/observability/2026-06-30-observability-console-migration-phase4.md:324 claim=three(3) noun=cases list@325 count=3(raw 3)
   TEXT:  spot-tests** (`tests/observe/serverMigrationSpot.test.ts`, mock `@/lib/log`) — three cases pinning the non-trivial decisions:
   ITEMS: **(a) Error → reserved field:** `app/api/admin/sync/[slug]/r | **(b) bracket-prefix removed + structured context:** `lib/ag | **(c) `console.log` → `log.info`:** `lib/sync/enrichAgenda.t

PARITY docs/superpowers/plans/observability/2026-06-30-observability-console-migration-phase4.md:355 claim=three(3) noun=assertions list@356 count=3(raw 3)
   TEXT: - [ ] **Step 1: failing test** — three assertions:
   ITEMS: (a) read `eslint.config.mjs`, assert the override `files` ar | (b) a `ts-morph` `Project` over `app/`+`lib/`+`components/`  | (c) **REAL negative control** — extract the detector into a 

MISMATCH docs/superpowers/plans/observability/2026-07-04-mutation-surface-observability.md:480 claim=2(2) noun=mutations list@481 count=1(raw 1)
   TEXT: ### Task 10: dev/actions (2 mutations)
   ITEMS: `parseAndStage` → `DEV_PARSE_STAGED`; `resetDevSchema` → `DE

PARITY docs/superpowers/plans/observability/2026-07-04-telemetry-admin-dev-gate/00-overview.md:5 claim=Two(2) noun=changes list@7 count=2(raw 2)
   TEXT: **Goal:** Two related changes to the admin surface, both building on the just-shipped developer tier (PR #286):
   ITEMS: **Part A** — Rename/relocate `/admin/observability` → `/admi | **Part B** — Restrict ALL admin-roster management (Add / Rev

MISMATCH docs/superpowers/plans/observability/2026-07-04-telemetry-admin-dev-gate/02-partB-app.md:49 claim=three(3) noun=changes list@50 count=4(raw 4)
   TEXT: - **`SELF_REVOKE_FORBIDDEN`** — three copy changes:
   ITEMS: helpfulContext "Other-revoke (a rogue admin revoking a peer, | `dougFacing` "…Ask another **admin** to do it if you need to | `longExplanation` "…ask another **admin** to revoke you." →  | Rationale: after Part B only a developer can revoke; a devel

PARITY docs/superpowers/plans/observability/2026-07-04-telemetry-admin-dev-gate/03-partA-rename.md:34 claim=two(2) noun=files list@35 count=2(raw 2)
   TEXT: **A. git mv the two route files (+ testids):**
   ITEMS: `app/admin/observability/page.tsx` → `app/admin/dev/telemetr | `app/admin/dev/observability-dim/page.tsx` → `app/admin/dev/

PARITY docs/superpowers/plans/observability/2026-07-04-telemetry-admin-dev-gate/03-partA-rename.md:38 claim=8(8) noun=items list@39 count=8(raw 8)
   TEXT: **B. §2.1a comprehensive gate inventory (all 8 items, atomically):**
   ITEMS: **`scripts/with-admin-dev-flag.mjs` disable list (`:44-55`)* | **`app/globals.css:20`** — the `@source not "../app/admin/de | **`tests/admin/build-artifact-gate.test.ts` (`:101`, `:120`, | **`tests/cross-cutting/no-raw-codes-audit.ts:319`** `.filter | **e2e `a[href*='/admin/dev']` count-zero assertions** — `tes | **`tests/e2e/admin-banner.spec.ts:14,379`** — confirm the Pr | **Comment/prose carve-outs** (BLANKET-absence claims only) — | **`tests/auth/developerGatingContract.test.ts:110`** `observ

MISMATCH docs/superpowers/plans/observability/2026-07-05-admin-outcome-behavioral-coverage-registry/00-overview.md:17 claim=6(6) noun=rows list@19 count=4(raw 4)
   TEXT: ## Task 2 — RED: remove 6 grandfather rows + flip pin
   ITEMS: Delete the 6 action rows from `ADMIN_OUTCOME_BEHAVIOR_GRANDF | Update the doc-comment (`exemptions.ts:118-123`): "…24 pre-e | Flip the pin `adminOutcomeBehavior.test.ts:953` `toBe(30)` → | Confirm the suite is **RED**: the Task-18 coverage test (`:9

PARITY docs/superpowers/plans/observability/2026-07-05-crew-picker-observability/plan.md:216 claim=three(3) noun=wrappers list@217 count=3(raw 5)
   TEXT: 83`) is unchanged (no emit). Add `// no-telemetry:` comments INSIDE each of the three exported wrappers:
   ITEMS: `clearIdentity` (`:47-50`): `// no-telemetry: FormData-parse | `clearIdentityAndSkip` (`:53-59`): `// no-telemetry: FormDat | `clearIdentityCore` (`:61-67`): `// no-telemetry: try/catch 

PARITY docs/superpowers/plans/observability/2026-07-05-crew-picker-observability/plan.md:310 claim=two(2) noun=wrappers list@311 count=2(raw 4)
   TEXT: 81`, `:83`) are unchanged (no emit). Add `// no-telemetry:` comments INSIDE the two exported wrappers:
   ITEMS: `cleanupStaleEntry` (`:29-49`): `// no-telemetry: FormData-p | `cleanupStaleEntryCore` (`:51-59`): `// no-telemetry: try/ca

MISMATCH docs/superpowers/plans/observability/2026-07-07-flow6-signal-visibility/plan.md:498 claim=3(3) noun=its list@500 count=0(raw 1)
   TEXT: heterogeneous entry is fine under `as const`; the `gateExempt` reads in Tasks 2/3 already tolerate its absence on the other 25.)
   ITEMS: 

PARITY docs/superpowers/plans/observability/2026-07-08-flow6.2-monitor-digest/PLAN.md:31 claim=two(2) noun=tiers list@33 count=2(raw 2)
   TEXT: implementation that omits a filter still passes. So every signal is tested at **two tiers**:
   ITEMS: **Pure-helper unit tests** (fast, no DB): `monitorDigest.ts` | **DB-integration filter tests** (`*.db.test.ts`, real local 

PARITY docs/superpowers/plans/observability/2026-07-08-flow6.2-monitor-digest/PLAN.md:1014 claim=three(3) noun=cases list@1015 count=3(raw 3)
   TEXT: - [ ] **Step 1: Write the failing test** — three cases:
   ITEMS: (a) monitor present → `renderDigest` receives it (email cont | (b) **monitor absent/null → context is BYTE-IDENTICAL to tod | (c) `dedupKey` still `digest:${dateET}` in both cases.

MISMATCH docs/superpowers/plans/observability/2026-07-09-admin-outcome-behavior-batch2/00-overview.md:27 claim=16(16) noun=rows list@29 count=3(raw 3)
   TEXT: ### Substep B — RED (transient, NOT committed): remove 16 grandfather rows + flip pins
   ITEMS: Delete the 16 route rows from `ADMIN_OUTCOME_BEHAVIOR_GRANDF | Flip pins `24`→`8`: `adminOutcomeBehavior.test.ts:1443` (Tas | Confirm the suite is **RED**: Task 18 coverage test names th

MISMATCH docs/superpowers/plans/observability/2026-07-26-watch-renewal-lifecycle.md:158 claim=two(2) noun=implementations list@160 count=3(raw 3)
   TEXT: there are several is that the obvious single assertion is satisfied by at least two wrong implementations (plan review R1b finding 4):
   ITEMS: after `promoteSettings` swaps the watched folder, no row for | **preservation:** the newly promoted folder's own active row | **same-transaction, observing BOTH writes:** when the promot

PARITY docs/superpowers/plans/observability/2026-07-26-watch-renewal-lifecycle.md:166 claim=TWO(2) noun=parts list@168 count=2(raw 2)
   TEXT: **Implementation** — spec §3.2.4. TWO parts ship; a third was designed and descoped (see below):
   ITEMS: promotion supersedes old-folder `active` rows and orphans ol | `activatePending` returns its affected-row count and `activa

PARITY docs/superpowers/plans/observability/2026-08-09-sync-log-show-attribution.md:126 claim=Four(4) noun=mutants list@127 count=4(raw 4)
   TEXT: **Four pre-dispatch mutants** (mandatory for string-presence guards; record each result in the commit):
   ITEMS: (a) empty the `show_id` column from the insert literal → ass | (b) append a suffix to the literal → assertion must fail (gu | (c) put `show_id` in a SQL comment inside the literal → asse | (d) vary the drive file id between one with a committed `sho

MISMATCH docs/superpowers/plans/observability/2026-08-09-sync-log-show-attribution.md:489 claim=5(5) noun=applies list@491 count=3(raw 3)
   TEXT: very run, and a prune returning a wrong count stays green. The same repair Task 5 applies to the new sync-log suite applies here, and this task owns it:
   ITEMS: Wrap the prune call in a transaction that is ALWAYS rolled b | Replace `n >= 1` with an exact oracle. The function's return | Keep the survival assertions scoped to the fixture marker, w

PARITY docs/superpowers/plans/observability/2026-08-09-sync-log-show-attribution.md:527 claim=Two(2) noun=corrections list@529 count=2(raw 2)
   TEXT: Two corrections, both required:
   ITEMS: **Export the variable for the whole task, not per-command (p | Record the `system_identifier` the pre-flight resolved, in t

PARITY docs/superpowers/plans/parser/2026-06-27-parser-typo-tolerance-pr-d3.md:24 claim=four(4) noun=guardrails list@26 count=4(raw 4)
   TEXT: ## Behavior contract (the four hard guardrails)
   ITEMS: **Exact-real wins; sentinel-aware (mirror PR-D1 exactly).**  | **Phantom-room guard (rooms-specific).** `parseV4Rooms` drop | **Agg threading.** Add `agg?: ParseAggregator` to `parseV4Ro | **Exact-map completeness.** `V4_LABEL_TO_FIELD` carries the 

PARITY docs/superpowers/plans/parser/2026-06-29-parser-info-tab-fidelity.md:541 claim=three(3) noun=assumptions list@542 count=3(raw 3)
   TEXT: ets MCP on sheet `1XQ44uxc44pToYxQnYw4OG9V6DjE7bC5EU08o5iFpxz4`, re-confirm the three content assumptions still hold and the implementation matches:
   ITEMS: `INFO` DRESS block (rows ~27-28) has `Set/Strike:` + `Show:` | `GEAR` has `LUNCH SESSION - GRAND BALLROOM C`; `INFO` has `L | `INFO` row 1 banner is proper-cased; `GEAR` `Event Name:` is

MISMATCH docs/superpowers/plans/parser/2026-06-30-room-detail-unrendered.md:231 claim=3(3) noun=adds list@233 count=0(raw 1)
   TEXT: n the index access, which is itself the red state; it goes green only once Step 3 adds the entry.)
   ITEMS: 

PARITY docs/superpowers/plans/parser/2026-07-05-parser-anchor-deliteralization/00-plan.md:189 claim=three(3) noun=functions list@190 count=3(raw 3)
   TEXT: - [ ] **Step 3: Implement** the three functions in `lib/parser/blocks/rooms.ts`, copying spec §2.2 (c2):
   ITEMS: `hasRoomFieldBlock(lines, i)`: scan `k = i+1..`; `t = lines[ | `precededByBoundary(lines, i)`: `i===0` || prev not `|`-star | `isRoomHeader(lines, i)` = `isRoomHeaderShape(col0Of(lines[i

PARITY docs/superpowers/plans/parser/2026-07-06-bo-venue-header-anchor/plan.md:24 claim=six(6) noun=cases list@25 count=6(raw 8)
   TEXT: T a Drive render — header comment says so). One INFO-style sheet containing the six fixture cases (spec §6):
   ITEMS: `SALON ABCD&#10;60' x 45'` (col-duplicated header) directly  | `| PROJECTION SCREEN | 5' x 9' |` (`label|value`) directly a | `| 4' X 8' RISER | staging |` (`label|value`) elsewhere, no  | `GRAND HALL&#10;DAY 1 & 2` + a `BO Setup`/`BO Video` block ( | `MERIDIAN&#10;40' x 30'` + `BO Setup | m-setup` / `BO Audio  | `ORCHID&#10;50' x 40'` + `BO Setup | orchid-setup` block, th

PARITY docs/superpowers/plans/parser/2026-07-25-hotel-ambiguity-coverage.md:11 claim=Two(2) noun=vectors list@13 count=2(raw 2)
   TEXT: versarial rounds / 69 findings**, all verified against live code, none refuted. Two vectors never closed in prose:
   ITEMS: **copy truth** — a string asserts something about the algori | **test-oracle discrimination** — a wrong implementation pass

MISMATCH docs/superpowers/plans/schedule/2026-06-22-per-day-schedule-keytimes.md:750 claim=2(2) noun=tests list@751 count=3(raw 3)
   TEXT: - [ ] Run the merge tests and the existing Phase-2 wiring tests; confirm pass (and that the existing `:188-202` assertions, which read `.title`/`.start`, are updated to `.entries[
   ITEMS: First update the legacy assertions at `tests/parser/parseShe | `pnpm vitest run tests/parser/parseSheet.test.ts tests/parse | Expected: `PASS` — all parseSheet + parseAgenda suites green

MISMATCH docs/superpowers/plans/schedule/2026-06-22-per-day-schedule-keytimes.md:963 claim=6(6) noun=passes list@964 count=2(raw 2)
   TEXT: ape and the new `sd()` shape both flow through `decodeRunOfShow`; with the Task-6 decoder this passes at runtime, but the `ShowForViewer.runOfShow` declared type is still `Record<string, AgendaEntry[]>`, so `r.
   ITEMS: `pnpm vitest run tests/data/getShowForViewerRunOfShow.test.t | Expected: `FAIL` — TypeScript error `Property 'showStart' do

MISMATCH docs/superpowers/plans/schedule/2026-06-22-per-day-schedule-keytimes.md:971 claim=4(4) noun=cases list@972 count=2(raw 2)
   TEXT: l `getShowForViewerRunOfShow` suite (new ScheduleDay cases + the pre-existing D-4 projection cases must all stay green):
   ITEMS: `pnpm vitest run tests/data/getShowForViewerRunOfShow.test.t | Expected: `PASS` — all suites green, including the legacy `e

MISMATCH docs/superpowers/plans/schedule/2026-06-30-dress-anchor-and-per-card-report-fieldref.md:432 claim=7(7) noun=files list@433 count=4(raw 4)
   TEXT: - [ ] **Step 3: Implement.** In each of the 7 section files:
   ITEMS: Import `CardHeaderActions` and the type: `import { CardHeade | Add `cardReport?: CardReportContext` to the section's `Props | Replace each `action={<SourceLink driveFileId={X} anchor={Y} | The 23 sites: BudgetSection ×1 (`budget-main`), CrewSection 

PARITY docs/superpowers/plans/schedule/2026-07-03-agenda-link-not-clickable-plan.md:32 claim=4(4) noun=cases list@33 count=4(raw 4)
   TEXT: Result`/`makeClient` helpers), add `describe("AGENDA_LINK_NOT_CLICKABLE")` with 4 cases, asserting against the RETURNED `result.warnings` and the forensic sink:
   ITEMS: bare filename `makeResult([{ label: "Day 1 Agenda", url: "ag | http URL `{ label: "Day 1 Agenda", url: "https://example.com | uppercase scheme `{ label: "Day 1 Agenda", url: "HTTPS://exa | undefined url `{ label: "Day 1 Agenda" }` → HAS `AGENDA_LINK

PARITY docs/superpowers/plans/schedule/2026-07-03-stage-filtered-schedule/PLAN.md:461 claim=4(4) noun=files list@462 count=4(raw 4)
   TEXT: - [ ] **Step 3: Minimal implementation** (thread all 4 UI files):
   ITEMS: `buildRightNowContext.ts`: add `stageRestriction?: StageRest | `ScheduleSection.tsx:95`: `const { dateRestriction, isAdmin, | `TodaySection.tsx:249-254`: add `ctx.stageRestriction` as th | `_CrewShell.tsx:222`: add `stageRestriction: ctx.stageRestri

MISMATCH docs/superpowers/plans/schedule/2026-07-05-schedule-show-day-labels/00-plan.md:441 claim=8(8) noun=requires list@443 count=1(raw 3)
   TEXT: The gates run in exactly this order — invariant 8 requires the UI dual-gate BEFORE any cross-model review of the implementation:
   ITEMS: Tasks 1–4 (TDD impl, commit per task).

PARITY docs/superpowers/plans/step3-onboarding/2026-06-11-onboarding-fixups/05-f5-cas-race.md:335 claim=six(6) noun=layers list@336 count=6(raw 6)
   TEXT: - [ ] **GREEN (one commit, six lockstep layers).**
   ITEMS: **Master spec §12.4 row** — insert directly under the `WIZAR | **helpfulContext YAML appendix** (block opening at line 3031 | `pnpm gen:spec-codes` → regenerated `lib/messages/__generate | **`lib/messages/catalog.ts`** row adjacent to `WIZARD_SESSIO | **`AdminAlertCode` union** member in `lib/adminAlerts/upsert | **Meta-test registry**: append `"WIZARD_SESSION_SUPERSEDED_R

MISMATCH docs/superpowers/plans/step3-onboarding/2026-06-23-onboarding-step3-review-redesign.md:168 claim=four(4) noun=cases list@169 count=5(raw 5)
   TEXT: - [ ] **Step 1: Failing tests** (in one file, four cases):
   ITEMS: first-seen + `wizard_approved=true` → `shows` row created `p | first-seen + `wizard_approved=false` → `shows` row created ` | existing-show + checked → `shows_pending_changes` shadow ins | existing-show + **unchecked** → **no** shadow inserted, **no | **Data-loss guard (the sequencing-hazard regression test):**

MISMATCH docs/superpowers/plans/step3-onboarding/2026-06-23-onboarding-step3-review-redesign.md:314 claim=3(3) noun=surfaces list@316 count=0(raw 1)
   TEXT: ### Task D7: impeccable dual-gate — Step-3 surfaces
   ITEMS: 

PARITY docs/superpowers/plans/step3-onboarding/2026-07-03-step3-modal-followups/plan.md:444 claim=Two(2) noun=halves list@445 count=2(raw 2)
   TEXT: - [ ] **Step 1: failing tests** (`tests/reports/nullShow.test.ts`). Two harness halves:
   ITEMS: **Route validation (§K5)** — model on `tests/reports/auth.te | **Formatters + call-site guards (§K6)** — model on `tests/re

PARITY docs/superpowers/plans/step3-onboarding/2026-07-06-pull-sheet-archived-tab-override.md:550 claim=three(3) noun=edits list@552 count=3(raw 3)
   TEXT: ### Step 5.2 — Impl (all three edits, same commit)
   ITEMS: **§12.4 prose** — add two rows (do NOT `prettier` the master | Run `pnpm gen:spec-codes` → regenerates `spec-codes.ts` with | **`catalog.ts`** — add both rows mirroring the existing `PUL

PARITY docs/superpowers/plans/step3-onboarding/2026-07-07-flow3-correction-loop-clarity/plan.md:596 claim=two(2) noun=ways list@598 count=2(raw 2)
   TEXT: Dispose of each HIGH/CRITICAL one of two ways — TDD is non-negotiable even for late visual/copy fixes:
   ITEMS: **Fix in-branch = a new TDD micro-task.** Do NOT batch fixes | **Defer = docs-only commit.** If a finding is deferred, add 

MISMATCH docs/superpowers/plans/step3-onboarding/2026-07-16-withdraw-rescan-guard.md:211 claim=three(3) noun=forms list@212 count=4(raw 4)
   TEXT: - [ ] **No doubled taps on the rescan testid remain — all three forms present in the tree** (multiline `-U`; default ripgrep does NOT match `\n`; each pattern was verified to match the pre
   ITEMS: `act`-wrapped pair (the 8 `RescanSheetButton.test.tsx` sites | Adjacent `fireEvent.click` pair (`step3ReviewModal.transitio | e2e consecutive locator-chain pair: `rg -U -n 'rescan-sheet- | Because a leftover second tap lands on a now-`disabled={pend

PARITY docs/superpowers/plans/step3-onboarding/2026-07-17-auto-applied-header-parity/00-plan.md:22 claim=three(3) noun=constraints list@23 count=3(raw 3)
   TEXT: Ordering rationale — three constraints:
   ITEMS: **Row + call site + walker seed are ONE commit (Task 1).** ` | **The gap adjacency test reuses locked seeds, so the seed mu | Both commits fully green (Vitest + the relevant e2e).

PARITY docs/superpowers/plans/step3-onboarding/2026-07-17-auto-applied-header-parity/00-plan.md:61 claim=THREE(3) noun=files list@62 count=3(raw 3)
   TEXT: e under `app/` (except `app/api/**`) or `components/`, so the gate scope is ALL THREE changed such files:
   ITEMS: `components/admin/RecentAutoAppliedStrip.tsx` — primary visu | `components/admin/NeedsAttentionInbox.tsx` — the gap fix. | `app/help/_affordanceMatrix.ts` — under `app/`, so in scope 

PARITY docs/superpowers/plans/step3-onboarding/2026-07-17-psat1-durable-override-dto/00-plan.md:688 claim=Two(2) noun=targets list@689 count=2(raw 2)
   TEXT: Two distinct coverage targets, kept in separate tests so each has its own fail-first:
   ITEMS: **Context consumption** — `PackListBreakdown`'s S5 `RescanSh | **Provider wiring (THIS task's real deliverable)** — `ShowRe

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/03-time-utility.md:509 claim=Two(2) noun=options list@510 count=2(raw 2)
   TEXT:  Required approach: **keep `StaleFooter` synchronous**. Two options to eliminate the `new Date()` default:
   ITEMS: **Option A (PREFERRED):** make the `now` prop **required**.  | **Option B:** introduce a thin async server wrapper `<StaleF

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/06-screenshot-harness.md:94 claim=TWO(2) noun=entries list@95 count=2(raw 2)
   TEXT:  - Add TWO project entries:
   ITEMS: **Setup project** `screenshots-help-setup` with `testMatch:  | **Capture project** `screenshots-help` with `testMatch: /hel

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/06-screenshot-harness.md:277 claim=Four(4) noun=assertions list@279 count=4(raw 8)
   TEXT: Per spec §7.1 test 9. Four assertions:
   ITEMS: Every manifest entry's `fixture` resolves to either `fixture | **r2 fix per F-r1 finding 5 (MEDIUM): every manifest entry's | Every manifest entry has BOTH light + dark WebPs on disk (ga | No orphan WebPs on disk — every `<key>-{light,dark}.webp` fi

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/06-screenshot-harness.md:296 claim=two(2) noun=halves list@297 count=2(raw 2)
   TEXT: Per spec §7.1 test 8 (TDD-compliant split per r5). The full test has two halves:
   ITEMS: **Half A (F.8 commit, TDD-green):** every `<Screenshot name= | **Half B (F.11 commit, after captures):** the on-disk WebP e

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/07-affordance-retrofit.md:324 claim=two(2) noun=asserts list@325 count=3(raw 3)
   TEXT: ssages/renderer-emits-learn-more.test.tsx`) that mounts the rendering helper in two contexts and asserts:
   ITEMS: In `/admin/*` context: helper emits `<a>` with `data-testid= | In `/show/<slug>` context: helper does NOT emit the `<a>` | In `/admin/show/<slug>/preview/<crew-id>` context: helper do

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/09-close-out.md:221 claim=9(9) noun=files list@223 count=0(raw 11)
   TEXT: When all 9 phase files are complete, verify the entire milestone:
   ITEMS: 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/DEFERRED.md:18 claim=four(4) noun=surfaces list@19 count=4(raw 4)
   TEXT: (concrete-but-larger-scope-than-close-out):** Master-spec §13.1 defines exactly four bug-report surfaces, all SHOW-SCOPED:
   ITEMS: Live parse feedback (admin, per-warning button in §9.2 parse | Per-crew preview banner (admin, scoped to show + crew member | Crew page footer in preview mode (admin, scoped to previewed | "Something looks wrong?" footer button on crew pages (crew, 

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/DEFERRED.md:48 claim=two(2) noun=rows list@49 count=3(raw 3)
   TEXT:  (ShowsTable/NeedsAttentionInbox replacing ActiveShowsPanel/PendingPanel) broke two SIBLING matrix rows that were never deferred:
   ITEMS: `help-affordance--dashboard-active-shows--tooltip` — carried | `help-affordance--dashboard-pending-ingestion--tooltip` — ca | The walker (`tests/e2e/deep-link-walker.spec.ts`, `help-docs

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/DEFERRED.md:141 claim=four(4) noun=labels list@143 count=3(raw 3)
   TEXT: -out audit (M9 completed at SHA `7931420`, tag `m9-completed`) reclassified the four documented labels by v1-status:
   ITEMS: **"Revoke all links" + "Issue new link"** — v1-blocking (sec | **"Issue first link"** — already shipped via the onboarding- | **"Copy share link"** — post-v1 polish. **Re-routed to [BACK

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/A-foundation.md:285 claim=Two(2) noun=flakes list@287 count=2(raw 2)
   TEXT: Two project-level test-isolation flakes that surfaced during Phase A close-out verification (NOT caused by Phase A but exposed by 
   ITEMS: **M11-A-D4** (auth-gate Layer 2 mock leakage) — RESOLVED at  | **M11-A-D5** (e2e tile-grid hydration jitter) — RESOLVED at 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/A-foundation.md:294 claim=Three(3) noun=meta-observations list@296 count=3(raw 3)
   TEXT: Three meta-observations from Phase A's review iterations worth recording for future phases:
   ITEMS: **Impeccable v3 token-discipline does NOT natively catch DES | **Codex companion `--background` mode can crash mid-run afte | **AGENTS.md §1.8 every-UI-mutation-re-triggers-the-gate rule

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/C-time-utility.md:38 claim=Two(2) noun=options list@39 count=2(raw 2)
   TEXT: pp/show/[slug]/page.tsx` AND `app/admin/show/[slug]/preview/[crewId]/page.tsx`. Two options for the migration:
   ITEMS: **Option A (preferred):** make `ShowBody` async (`export asy | **Option B:** hoist `today` to the caller — `page.tsx` and `

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/C-time-utility.md:169 claim=Two(2) noun=migrations list@170 count=2(raw 2)
   TEXT: s/components/StaleFooter.test.tsx` renders it through `@testing-library/react`. Two acceptable migrations:
   ITEMS: **Option A (preferred):** make `now` a REQUIRED prop. Every  | **Option B:** ship a thin async wrapper `<StaleFooterServer 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/D-components.md:301 claim=three(3) noun=sub-scans list@303 count=3(raw 3)
   TEXT:  of the side-stripe class. The meta-test is a Node-environment Vitest file with three sub-scans across `app/help/_components/*.tsx`:
   ITEMS: **Side-stripe ban (DESIGN.md L242):** asserts no `border-(l| | **`text-accent-text` only on `bg-accent`:** asserts every cl | **`text-text-subtle` not on action targets:** asserts every 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/D-components.md:315 claim=Three(3) noun=meta-observations list@317 count=3(raw 3)
   TEXT: Three meta-observations from Phase D's 8-round Codex convergence worth carrying forward to Phase E and beyond:
   ITEMS: **DESIGN.md line 33 contains a math error** that must be res | **Plan-body code-as-prescription anti-pattern.** D.1, D.2, D | **Same-vector recurrence rule has a "process drift" sub-clas

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-12-user-facing-docs/handoffs/E-content.md:333 claim=Five(5) noun=class-vectors list@335 count=5(raw 5)
   TEXT: Five class-vectors worth carrying forward to Phase F and beyond:
   ITEMS: **Adversarial-review scoping discipline is a recurring failu | **Plan-body / spec / shipped-code three-way drift is systemi | **Plan body's per-task code snippets are templates, not cont | **Catalog backfill biconditional has cascade effects.** E.13 | **Per-page external impeccable §1.8 dual-gate cadence is the

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/01-phase0-infra.md:467 claim=two(2) noun=pairs list@468 count=2(raw 2)
   TEXT: rastchecker/`) against the live tokens in `app/globals.css` `@theme` block. The two pairs:
   ITEMS: `text-text on bg-stale-tint` — read both color values from ` | `text-text-subtle on bg-surface-sunken` — same procedure.

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/03-phase0-tooling-reseed.md:105 claim=9(9) noun=aliases list@107 count=9(raw 9)
   TEXT: Each R-combo's `crewMembers` is built from the **9 role-variant aliases** per spec §3.2:
   ITEMS: `alias_5a_lead` — `["LEAD"]` | `alias_5b_lead_a1` — `["LEAD","A1"]` | `alias_5c_bo_lead` — `["BO","LEAD"]` | `alias_6a_a1` — `["A1"]` | `alias_6b_v1` — `["V1"]` | `alias_6c_l1` — `["L1"]` | `alias_6d_bo` — `["BO"]` | `alias_6e_a1_l1` — `["A1","L1"]` | `alias_6f_empty` — `[]`

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/03-phase0-tooling-reseed.md:676 claim=two(2) noun=clients list@677 count=3(raw 3)
   TEXT:  2. **Interleave a concurrent mint:** issue two RPC calls concurrently via two independent Supabase clients —
   ITEMS: Client A: `validation_finalize_all_atomic(['R1'], $TODAY_ISO | Client B: `mint_validation_fixture_atomic('R2', <R2_payload> | The test does NOT need deterministic ordering — PostgreSQL's

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/06-phase1-matrix-walk.md:165 claim=three(3) noun=legs list@167 count=0(raw 2)
   TEXT: **Baseline preamble (run once before all three legs):**
   ITEMS: 

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/07-iteration-and-final-sweep.md:177 claim=7(7) noun=modes list@179 count=2(raw 3)
   TEXT: ## Phase 7 failure modes
   ITEMS: **The final sweep keeps surfacing new MUST-FIX.** Each itera | **The dev cannot write the subjective sign-off sentence.** T

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/DEFERRED.md:3 claim=Three(3) noun=buckets list@5 count=3(raw 3)
   TEXT: Per memory `feedback_deferral_discipline.md`. Three buckets:
   ITEMS: **Land-now small fixes** — handled inline during iteration;  | **DEFERRED.md (this file)** — work that WILL be done with a  | **`BACKLOG.md` (project root)** — speculative work that MIGH

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/handoffs/Phase-0.A-block-2-closeout.md:33 claim=4(4) noun=surfaces list@35 count=4(raw 4)
   TEXT: ile (`tests/cross-cutting/no-jwt-surface.test.ts`), but my original grep missed 4 more surfaces with hardcoded `"middleware.ts"` in their scan arrays:
   ITEMS: `tests/messages/catalog.test.ts:71` — x1 catalog parity audi | `tests/auth/advisoryLockRpcDeadlock.test.ts:48` — advisory-l | `tests/cross-cutting/codes.test.ts:10-11` — x1 ACTIVE_PRODUC | `scripts/extract-spec-codes.ts:36` — RENDERED_CONTEXT_ROOTS 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/handoffs/Phase-0.A-block-2-closeout.md:98 claim=3(3) noun=helpers list@99 count=3(raw 3)
   TEXT: 4. **Helper-layer enablement requires writing 3 new helpers** (NOT a 1-line `.skip` flip):
   ITEMS: `tests/e2e/helpers/seedShowWithCrew.ts` — writes `shows` + ` | `tests/e2e/helpers/seedPickerCookie.ts` — writes a `__Host-f | `tests/e2e/helpers/claimStamp.ts` — sets `crew_members.claim

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation/handoffs/Phase-0.B-closeout.md:69 claim=8(8) noun=prescribes list@71 count=3(raw 3)
   TEXT: The M12 plan §0.B.2 Step 8 prescribes a **3-layer** defense in `tests/db/postgrest-dml-lockdown.test.ts`:
   ITEMS: **Layer 1:** `pg_catalog.has_table_privilege` via psql — cat | **Layer 2:** Admin-authenticated PostgREST INSERT/UPDATE/DEL | **Layer 3:** Tightened anon + authenticated PostgREST probes

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:3730 claim=three(3) noun=ways list@3732 count=3(raw 3)
   TEXT: servation contract.** The pre-pivot `app/auth/sign-out/route.ts` is hardened in three ways that MUST be preserved across the R41 rewrite, NOT dropped:
   ITEMS: **Same-origin gate** (R22 F2 / R15 #1; pre-pivot at `app/aut | **Try/catch around `supabase.auth.signOut()`** (pre-pivot at | **Teardown-failure HTML response** (pre-pivot at `:24-65`) —

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:4406 claim=2(2) noun=restores list@4407 count=14(raw 14)
   TEXT: cker-bootstrap ONLY" was too tight and contradicted the H2 allowlist; P-R21 Fix-2 restores the correct allowlist.)
   ITEMS: **identity_invalidated resolver arm** is a single union arm  | **Schema partial UNIQUE index** `crew_members_show_email_uni | **§6.0 Timestamp Defense Contract** — cookie.t comes from `o | **Per-row OAUTH_IDENTITY_CLAIMED emission** (P-R8 Fix-3) — o | **R41 admin_alerts producer count = SIX** (post-P-R24): `OAU | **Hashed-email contract scopes to the THREE email-bearing pr | **Sign-out preservation contract** (P-R10 Fix-2) — same-orig | **Tokenized URL allowlist** (P-R12 Fix-1) — `validateNextPar | **Reset RPC topology** (SECURITY DEFINER + in-DB `is_admin() | **API consumer 401/410/500 matrix** — every §6 API route dis | **Shows DML lockdown** — `crew_members`, `crew_member_auth`, | **decodePickerCookie** validates UUID format + integer range | **Archived shows return 404 for ALL viewers** (including adm | **subscriber-token shape** `{ jwt, exp }` with `role: 'authe

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/01-pivot-tasks.md:502 claim=8(8) noun=patterns list@503 count=8(raw 8)
   TEXT:  For all 8 forbidden patterns, the test file owns paired regression cases — semantic descriptions only here, fixture literals in the test f
   ITEMS: **Pattern 1 (Vault extension-name-as-schema drift):** negati | **Pattern 2 (HTTP POST verb drift):** negative case = SQL fu | **Pattern 3 (jobname-on-job_run_details column-shape drift): | **Pattern 4 (non-existent pg_cron column names drift):** neg | **Pattern 5 (db-push-reapply migration assumption):** negati | **Pattern 6 (unescaped LIKE wildcard):** negative case = une | **Pattern 7 (double-backslash ESCAPE clause):** negative cas | **Pattern 8 (secret-bearer reveal via decrypted-secrets SELE

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/01-pivot-tasks.md:1034 claim=10(10) noun=assertions list@1035 count=10(raw 10)
   TEXT:  O + P + Q + R + S (R11 F30 + R18 F39 + R29 F57 + R31 F60 + R33 F62 fix).** Add 10 assertions to T5's `tests/cross-cutting/m12-plan-pg-cron-pivot-amendment.test.ts`:
   ITEMS: Assertion J: 05-phase0-smokes.md Phase 0.F failure-modes sec | Assertion K (R18 F39): M12 spec line ~801 (§9.1 Vercel proje | Assertion L (R18 F39): M12 spec line ~850 (§9.2 smoke 3) doe | Assertion M (R18 F39): M12 spec contains a new section headi | Assertion N (R29 F57): M12 spec §9.1.1 "Canonical CI gate in | Assertion O (R29 F57): master spec's AC-X.6 paragraph contai | Assertion P (R29 F57 structural defense): `loadRequiredCheck | Assertion Q (R31 F60): master spec `BRANCH_PROTECTION_DRIFT` | Assertion R (R31 F60 structural defense — full-surface sweep | Assertion S (R33 F62 structural defense — full-surface sweep

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot/DEFERRED.md:3 claim=Three(3) noun=buckets list@5 count=3(raw 3)
   TEXT: Per memory `feedback_deferral_discipline`. Three buckets:
   ITEMS: **Land-now small fixes** — handled inline during iteration;  | **DEFERRED.md (this file)** — work that WILL be done with a  | **`BACKLOG.md` (project root)** — speculative work that MIGH

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b1-admin-nav-settings.md:65 claim=Two(2) noun=rules list@66 count=3(raw 3)
   TEXT: a type checker. Every registered helper MUST satisfy it or the meta-test fails. Two hard rules learned from the live test body:
   ITEMS: **A registered file MUST contain ≥1 grep-recognized supabase | **A pure WRAPPER with no direct supabase await** (e.g. `fetc | Every recognized await AND every builder-assignment line (`<

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b1-admin-nav-settings.md:692 claim=3(3) noun=swaps list@694 count=0(raw 1)
   TEXT: /`<div id="alerts">` chrome; only the identity read + catch copy change. (Phase 3 swaps the chrome for `<AdminNav>`.)
   ITEMS: 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b1-admin-nav-settings.md:1536 claim=three(3) noun=builds list@1537 count=3(raw 3)
   TEXT: r project** (`test.info().project.name` branches, since one file runs under all three builds): 
   ITEMS: **`dev-build`** (built `ADMIN_DEV_PANEL_ENABLED=true`): `/ad | **`prod-build`** (built UNSET): `/admin/dev` 404s AND `/admi | **`prod-runtime-flip`** (built UNSET, started with runtime `

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b2-show-lifecycle.md:1305 claim=3(3) noun=findings list@1307 count=3(raw 3)
   TEXT: **R1 (Codex, 2026-06-01) → needs-attention. 3 findings, all genuine; all fixed in this round.**
   ITEMS: **[high] `archive_show` stale pre-lock `archived` read (TOCT | **[high] `unarchive_show` unconditional rotation on a non-ar | **[medium] Archive finalize-owned refusal was not actually t

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-phase-b2-show-lifecycle.md:1313 claim=3(3) noun=findings list@1315 count=3(raw 3)
   TEXT: **R2 (Codex, 2026-06-01) → needs-attention. 3 findings, all genuine; all fixed in this round.**
   ITEMS: **[high] `_b2Helpers.seedShow` could not seed against the li | **[high] DEF-1 (rotate/reset) had the same pre-lock-stale-st | **[medium] Task 1.1 imported a nonexistent `@/tests/db/_help

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/M12.2-recon1-banner-quieting.md:582 claim=3(3) noun=commits list@584 count=0(raw 1)
   TEXT: ### Task 3 commits — EXACTLY TWO (no per-subtask commits)
   ITEMS: 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/handoffs/M12.2-phase-a-admin-redesign.md:44 claim=two(2) noun=disciplines list@45 count=2(raw 2)
   TEXT: ## §4 Bugs the gates earned (retrospective — validates two disciplines)
   ITEMS: **Codex impl-diff review** (ratified into A close-out, super | **Real-browser Playwright layout task** (AGENTS.md mandate):

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/handoffs/M12.2-phase-b1-admin-nav-settings.md:118 claim=three(3) noun=bugs list@119 count=3(raw 3)
   TEXT: The real-browser e2e batch + the cross-model review caught **three genuine bugs the plan did not anticipate**, all fixed:
   ITEMS: **Revoke confirm submit never dispatched (pre-existing since | **Content occlusion in the 640–719px band (`layout.tsx`).**  | **Admin-dev build lock in a cleaned distDir (cross-model adv

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/handoffs/M12.2-phase-b2-show-lifecycle.md:186 claim=3(3) noun=findings list@187 count=4(raw 4)
   TEXT: - **R4** — needs-attention, 3 findings, all land-now fixed:
   ITEMS: F1 [HIGH] `unarchive_show` could revive a drifted `archived= | F2 [MED] `readfinalizeowned_b2` (SECURITY DEFINER, granted a | F3 [MED] committed `.codex-companion-b2/**` per-run state →  | Migration `supabase/migrations/20260602000000_b2_r4_unarchiv

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate/00-overview.md:142 claim=Two(2) noun=points list@143 count=2(raw 2)
   TEXT: Two entry points per show hashkey, single-holder each, never nested:
   ITEMS: **Sync/apply path** — JS holder via `lib/sync/lockedShowTx.t | **Admin path** (`mi11_approve_hold`/`mi11_reject_hold`/`undo

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate/00-overview.md:260 claim=2(2) noun=honors list@261 count=3(raw 3)
   TEXT: _value.baseline` contract that `undo_change` uses** (resolution #16), and Phase 2 honors it via the existing undo_override apply (resolution #14):
   ITEMS: reject `email_change` → `undo_override` `domain='crew_email' | reject `rename` → `undo_override` `domain='crew_identity'`,  | reject `removal` → `undo_override` `domain='crew_identity'`,

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate/02-decision-rule-and-hold-aware-apply.md:188 claim=Two(2) noun=sub-cases list@189 count=3(raw 3)
   TEXT: TICALLY to an undo-produced one (the `held_value.baseline` shape is identical). Two next-sync sub-cases:
   ITEMS: **Rejected folded RENAME** — produce the hold via `mi11_reje | **Rejected REMOVAL** — produce the hold via `mi11_reject_hol | These exercise the SAME `undo_override` machinery as (b)/(d)

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate/04-undo-and-tombstone.md:16 claim=2(2) noun=releases list@17 count=3(raw 3)
   TEXT: e 1's DDL is untouched). It captures the **undone change's signature** so Phase 2 releases against **what the sheet asserts**, not against `held_value`:
   ITEMS: removal (Direction A, `crew_removed`): `baseline = {kind:'re | rename (Direction A, `crew_renamed`): `baseline = {kind:'ren | add (Direction B, tombstone): `baseline = {kind:'add', added

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate/04-undo-and-tombstone.md:317 claim=2(2) noun=releases list@318 count=3(raw 3)
   TEXT: ests/db/undo-change-direction-a.test.ts` three next-sync cases that prove Phase 2 releases against the **sheet signature** (`held_value.baseline`), not against `held_value`:
   ITEMS: **(a) undo-removal holds across an unchanged sheet.** Seed ` | **(b) undo-rename suppresses a DIFFERENT-named replacement.* | **(c) release on reconcile.** From (a): next sync where the 

PARITY docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate/04-undo-and-tombstone.md:384 claim=Three(3) noun=cases list@385 count=3(raw 3)
   TEXT: it with an arbitrary `p_change_log_id` (do NOT trust the feed's action gating). Three cases, each an **applied** row:
   ITEMS: a `crew_email_changed` row with `entity_ref='Alice'` (Alice  | an applied `section_shrunk` row (`before_image IS NULL`) → s | an applied `field_changed` row (`before_image IS NULL`) → sa

MISMATCH docs/superpowers/plans/v1-pre-deployment-amendments/2026-06-10-mobile-needs-attention.md:102 claim=two(2) noun=head-counts list@103 count=5(raw 5)
   TEXT: loadNeedsAttention.ts`: move `Dashboard.tsx:326-460` (the two bounded queries + two head-counts + existence lookup + `buildNeedsAttention` call) **verbatim**, with exactly these deltas:
   ITEMS: signature `export async function loadNeedsAttention(opts: {  | when `opts.supabase` is omitted: `let supabase; try { supaba | `RENDER_CAP` literals in `.limit(...)` become `opts.cap + 1` | **count integrity (R2-F3):** after each head-count query, re | keep every existing `{ data, error }` destructure + try/catc

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:651 claim=Three(3) noun=layers list@653 count=3(raw 3)
   TEXT: Three independent layers protect financials specifically:
   ITEMS: **Physical separation** — `financials`, `parse_warnings`, an | **RLS** — `shows_internal` has admin-only RLS policies. Even | **Server-side filter at fetch** — the entitlement-aware fetc

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:930 claim=THREE(3) noun=prongs list@931 count=3(raw 3)
   TEXT: surface is gated on `watched_folder_id IS NOT NULL`). The recovery contract has THREE prongs:
   ITEMS: **"Start over" button on every `/admin` GET when `watched_fo | **Re-run Setup remains the post-onboarding affordance.** Whe | **24-hour auto-rotate on every wizard step page-load.** Serv

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:998 claim=Three(3) noun=triggers list@1000 count=3(raw 3)
   TEXT: Three triggers, in order of latency:
   ITEMS: **Drive push notifications (`files.watch`) — primary, sub-se | **Vercel Cron `*/5 * * * *` reconciliation — fallback.** Run | **Manual sync trigger** at `/admin/show/<slug>` — operator-i

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1010 claim=Three(3) noun=points list@1012 count=4(raw 4)
   TEXT: **Three entry points** (all call into the same per-file processor at step 3):
   ITEMS: **`runScheduledCronSync()`** — invoked by Vercel Cron in `mo | **`runOnboardingScan(folderId)`** — invoked by the §9.0 onbo | The promotion of pending → active happens at wizard exit (§9 | **`runManualSyncForShow(driveFileId, mode = "manual" | "reco

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1047 claim=two(2) noun=values list@1048 count=3(raw 3)
   TEXT: heet_unavailable`-recovery runs skip the gate. The watermark is the **higher of two values** to prevent re-staging an unchanged file while Doug is reviewing the prior stage:
   ITEMS: **`shows.last_seen_modified_time`** — what's currently live. | **`pending_syncs.staged_modified_time`** — what's currently  | **Effective watermark** = `GREATEST(last_seen_modified_time,

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1082 claim=three(3) noun=outcomes list@1083 count=3(raw 3)
   TEXT:  **Run §6.8 minimum-invariant check on the `ParseResult`.** Pick exactly one of three outcomes:
   ITEMS: **MI-1..MI-5a fail (hard fail)** → in the same transaction: | **MI-6..MI-14 trip (stage)** → behavior splits on whether a  | **All invariants pass** → continue to phase 2 within the sam

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1422 claim=two(2) noun=cases list@1424 count=2(raw 2)
   TEXT: This handles two cases:
   ITEMS: Doug introduces a new field in a future template (e.g., `Spo | A typo Doug doesn't realize (e.g., `Hotal Contact Info` in p

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1473 claim=Three(3) noun=sources list@1474 count=3(raw 3)
   TEXT: 2. **Date restriction.** Which calendar days the crew member works. Three sources, in order of precedence:
   ITEMS: **Parens with day list in the name cell**: `Calvin Saller (6 | **2026 `\***`flag in role with no parens**:`Calvin Saller`+  | **No flag, no parens** → `{kind: "none"}`.

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1608 claim=three(3) noun=outcomes list@1610 count=3(raw 3)
   TEXT: **Minimum invariants for v1.** Each invariant has one of three outcomes:
   ITEMS: **Hard fail** → `parse_error`, watermark NOT advanced, prior | **Stage for approval** → write to `pending_syncs` (see §6.8. | **Pass** → proceed to destructive transaction.

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1646 claim=two(2) noun=queries list@1648 count=2(raw 2)
   TEXT: by source context BEFORE reading. The server's first action is therefore one of two scoped queries:
   ITEMS: **Dashboard / live-row Apply** (route originates from `/admi | **Wizard step-3 Apply** (route originates from the wizard UI

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1855 claim=5(5) noun=columns list@1856 count=5(raw 5)
   TEXT: - Subsequent rows are positional (5 columns): **`packed_flag | qty | item | sub_cat | cat`**:
   ITEMS: col 1 (packed_flag): `FALSE`/`TRUE` checkbox indicator (info | col 2 (qty): integer (parseable as number; null if blank) | col 3 (item): line-item name (REQUIRED; rows with empty col  | col 4 (sub_cat): nullable sub-category (e.g., `SPEAKERS / MO | col 5 (cat): top-level category (e.g., `AUDIO`, `VIDEO`, `BA

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1895 claim=three(3) noun=states list@1896 count=3(raw 3)
   TEXT: expiry destroy bytes already promoted)**: every ledger row is in exactly one of three states, transitioned via atomic UPDATEs with WHERE-clause state guards:
   ITEMS: **(a) `unclaimed`**: `claim_token IS NULL AND claimed_at IS  | **(b) `claimed`**: `claim_token IS NOT NULL AND claimed_at I | **(c) `committing_delete`**: `claim_token IS NOT NULL AND cl

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1932 claim=Two(2) noun=sub-states list@1933 count=4(raw 4)
   TEXT:  **Partial-failure retry path.** Two terminal sub-states exist:
   ITEMS: `snapshot_status = 'partial_failure'` — at least one entry h | `snapshot_status = 'partial_failure_restage_required'` — at  | **Crucially, the prior snapshot's bytes are NOT immediately  | This means a transient Drive/Storage failure can never perma

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:1960 claim=four(4) noun=columns list@1962 count=4(raw 4)
   TEXT:  `shows.opening_reel_head_revision_id`, AND `shows.opening_reel_mime_type` (all four columns per §4.1):
   ITEMS: **Null reel staged** — if `parseResult.openingReel === null` | **Re-fetch under the lock** — if `parseResult.openingReel != | **Pinned-tuple comparison** — drift case if ANY of: | **Success path** — pin tuple matches AND mimeType is video: 

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2001 claim=TWO(2) noun=checks list@2002 count=2(raw 2)
   TEXT: origin`. **The POST MUST originate same-origin.** The server enforces this with TWO independent checks before the per-request authz flow runs:
   ITEMS: **Origin/Sec-Fetch check.** The route REJECTS any request wh | **Bootstrap nonce (multi-instance + show-bound; signing-key-

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2316 claim=12(12) noun=steps list@2318 count=3(raw 3)
   TEXT: **Implementation note:** all 12 steps are encapsulated in a single `lib/auth/validateLinkSession(req)` helper that returns a **tri-state outcome** (locked by p
   ITEMS: `{ kind: 'success', viewer: { kind: 'crew'; crewMemberId; sh | `{ kind: 'continue', priorFailure?, clearCookie?: true }` —  | `{ kind: 'terminal_failure', status, code, message }` — rese

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2338 claim=Two(2) noun=mechanisms list@2340 count=3(raw 3)
   TEXT: Two independent mechanisms invalidate prior links:
   ITEMS: **Routine rotation (`current_token_version` bump)** — Doug's | **Surgical revocation (`revoked_links` row, exact version)** | **Wildcard revocation via floor (`crew_member_auth.revoked_b

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2344 claim=TWO(2) noun=columns list@2346 count=2(raw 2)
   TEXT: riber JWTs after rotation. To make rotation actually atomic, the schema carries TWO bound key-id columns:
   ITEMS: `link_sessions.signing_key_id text NOT NULL` — captured at r | `app_settings.active_signing_key_id text NOT NULL DEFAULT 'k

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2436 claim=2(2) noun=cols list@2438 count=6(raw 6)
   TEXT: **Tile grid (2 cols on mobile, 3+ on desktop):**
   ITEMS: **Lodging tile** — your hotel only (filtered from `hotel_res | **Venue tile** — name, address line, loading dock, "Open in  | **Schedule tile** — days filtered by `date_restriction.kind` | `audioScopeVisible = hasA1 || hasLead` — LEAD is scope-tile- | `videoScopeVisible = hasV1 || hasLead` — LEAD is scope-tile- | `lightingScopeVisible = hasL1 || hasLead` — LEAD is scope-ti

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2642 claim=two(2) noun=queues list@2644 count=2(raw 2)
   TEXT: **Panel 2 — Sheets we couldn't auto-apply.** Combines two queues, each with distinct row actions:
   ITEMS: **`pending_ingestions` rows** (brand-new sheets that hard-fa | **`pending_syncs` rows for first-seen shows** (the show has 

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:2659 claim=Four(4) noun=sub-sections list@2661 count=4(raw 4)
   TEXT: Four sub-sections (the first appears only when a `pending_syncs` row exists for this show):
   ITEMS: **Staged review (when `pending_syncs.drive_file_id = shows.d | **Sync health** — last 5 sync attempts with status and durat | **Parse warnings** — list of `parse_warnings` entries. Each  | **Crew preview links** — each crew member's name with a "Pre

PARITY docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:3592 claim=FOUR(4) noun=causes list@3593 count=4(raw 4)
   TEXT: olen can detect that and run the orphan-cleanup branch. **0-row tail UPDATE has FOUR causes** — re-read the row (with `SELECT github_issue_url, show_id`) and disambiguate:
   ITEMS: **Case A:** stored `github_issue_url` equals my just-created | **Case B:** stored `github_issue_url` is set but differs fro | **Case C:** stored `github_issue_url IS NULL` → another work | **Case Reaped:** the re-read returned no row → the daily rea

MISMATCH docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:3843 claim=THREE(3) noun=branches list@3844 count=4(raw 4)
   TEXT: - AC-5.11 `?t=` URL compromise-event handler covers ALL THREE branches per §7.2:
   ITEMS: **Branch A** (`jwt.tokenVersion === current_token_version`): | **Branch B** (`jwt.tokenVersion < current_token_version`): m | **Branch C** (`jwt.tokenVersion > current_token_version`, fu | All three branches use the canonical `LEAKED_LINK_DETECTED` 

MISMATCH docs/superpowers/specs/2026-07-18-modal-close-exit-anim.md:120 claim=two(2) noun=paths list@122 count=6(raw 6)
   TEXT: orithm (S1–S4, no per-state branching).** Ordering matters and mirrors what the two existing drag-release paths already do (`:282`, `:304` both set `transition` before mutating `transform`):
   ITEMS: **Snapshot first.** Read the *computed* `transform` and `opa | **Neutralize** `animation = "none"` and `transition = "none" | **Pin the snapshot** as inline `transform`/`opacity` — the e | **Force a style flush** (read a layout property) so the star | **Set the exit transition** — `transform`/`opacity` over the | **Apply the exit values** (table above).

PARITY docs/superpowers/specs/2026-07-18-modal-header-reconciliation-mock/README.md:20 claim=Three(3) noun=renderings list@22 count=3(raw 3)
   TEXT: Three modal-header renderings at a fixed 1024px panel width:
   ITEMS: **Locked — published header in the wizard's frame** (`#1a`)  | **Wizard · Step 3 review** — faithful recreation of today's  | **Published show** — faithful recreation of today's publishe

PARITY docs/superpowers/specs/2026-07-18-modal-header-reconciliation.md:1031 claim=Three(3) noun=pins list@1033 count=3(raw 3)
   TEXT: Three things this pins, each a way the move can go wrong:
   ITEMS: **The third arm becomes `null`**, not an empty element. A no | **The `overview-sheet-sync` wrapper STAYS.** It is not empti | **No Re-sync button remains in any arm.** Keeping one to avo

PARITY docs/superpowers/specs/2026-07-18-modal-header-reconciliation.md:1322 claim=three(3) noun=branches list@1323 count=3(raw 3)
   TEXT:  All three branches, explicitly:
   ITEMS: **shrink confirm:** Re-sync → "Keep current version" → "Appl | **error:** Re-sync → "Dismiss sync error" → Copy | **success:** Re-sync → "Dismiss sync result" → Copy

PARITY docs/superpowers/specs/2026-07-19-admin-modal-realtime-refresh.md:164 claim=four(4) noun=advisories list@166 count=4(raw 4)
   TEXT: Round-6 APPROVE carried four advisories; the plan MUST incorporate them:
   ITEMS: The e2e quiet window restarts not only on request activity b | "Never cached" (§4.1) gets a pin: a unit/source-scan asserti | The loader error-path unit tests assert the emit's full fiel | Reconnect detection in the e2e includes WebSocket close/erro

PARITY docs/superpowers/specs/2026-07-19-modal-skeleton-close.md:7 claim=two(2) noun=usages list@9 count=2(raw 2)
   TEXT: `ShowReviewModalSkeleton` has two usages (`components/admin/showpage/ShowReviewModalSkeleton.tsx:26`):
   ITEMS: **Server Suspense fallback** — `app/admin/page.tsx:168` moun | **Client optimistic copy** — `components/admin/ShowsTable.ts

PARITY docs/superpowers/specs/2026-07-19-show-modal-prefetch.md:13 claim=Three(3) noun=moves list@15 count=3(raw 3)
   TEXT: Three moves, no new data layer:
   ITEMS: **Viewport prefetch.** BOTH dashboard modal-opening row Link | **Revalidate-on-open.** `PublishedReviewModal` (client shell | **Cache posture.** No `staleTimes` override. `prefetch={true

PARITY docs/superpowers/specs/2026-07-19-show-modal-prefetch.md:38 claim=Three(3) noun=cases list@39 count=3(raw 3)
   TEXT: seShowModalNav().close` strip `show` from the URL (`useShowModalNav.ts:30-36`). Three distinct cases, each pinned (§6.4):
   ITEMS: **Refresh lands during exit** (URL still `/admin?show=<slug> | **Refresh lands after the visual close, before/around the UR | **Reduced motion**: `onClose` fires immediately (exit collap

PARITY docs/superpowers/specs/2026-07-20-alert-popover-context-design.md:123 claim=Four(4) noun=assertions list@125 count=4(raw 4)
   TEXT: Four assertions, all walking the LIVE catalog so a new code fails by default:
   ITEMS: **Completeness.** For every `MessageCatalogEntry` with `help | **Per-string validity.** For every entry that satisfies rule | **Exemption and authored copy are mutually exclusive.** An e | **Ledger is closed and non-vacuous.** Every `code` in `POPOV

PARITY docs/superpowers/specs/2026-07-20-alert-popover-context-design.md:130 claim=Two(2) noun=consumers list@131 count=2(raw 2)
   TEXT: es, exempt)` that returns a list of violations (each `{ rule, code, detail }`). Two consumers:
   ITEMS: the **live meta-test** calls it on the real `MESSAGE_CATALOG | **synthetic-input unit tests** call it on hand-built fixture

PARITY docs/superpowers/specs/2026-07-20-attention-alert-routing.md:95 claim=Two(2) noun=gaps list@97 count=2(raw 2)
   TEXT: **Residual risk, stated honestly (R4#1).** Two gaps remain, and neither is closed by claiming otherwise:
   ITEMS: **Discovery is bounded by the NAME, so anything that writes  | **For a site whose code argument is non-literal, the walker 

PARITY docs/superpowers/specs/2026-07-20-attention-scenario-gallery-design.md:13 claim=Two(2) noun=gaps list@15 count=2(raw 2)
   TEXT: Two distinct gaps:
   ITEMS: **No sweep.** There is no way to see every state at once and | **No drive.** There is no way to put a chosen state in front

PARITY docs/superpowers/specs/2026-07-20-attention-scenario-gallery-design.md:312 claim=Three(3) noun=consequences list@314 count=3(raw 3)
   TEXT: onents/admin/showpage/AttentionMenu.tsx:108`). No portal, no `position: fixed`. Three consequences the previous revision got wrong:
   ITEMS: It is **absolutely positioned, therefore out of flow** — it  | Its width is `min(400px, 100vw - 32px)` — sized off the **vi | The scroll threshold is the list's `max-h-96` (384px). `MENU

PARITY docs/superpowers/specs/2026-07-20-local-suite-wallclock.md:40 claim=four(4) noun=reads list@41 count=4(raw 4)
   TEXT: - Generator inputs (verified read-surface; none of the four reads `process.env`):
   ITEMS: `generate-admin-tables.ts`: spec (`SPEC_PATH`, line 3) → `li | `extract-watermark-symbols.ts`: spec (line 4) → `lib/audit/w | `extract-email-boundaries.ts`: spec (line 4) + plan `11-cros | `generate-traceability.ts`: spec (line 7) + plan-dir corpus 

PARITY docs/superpowers/specs/2026-07-20-show-alert-compact.md:62 claim=Two(2) noun=shapes list@63 count=2(raw 2)
   TEXT: er-warning-text/25 px-3 py-1.5`, `flex flex-wrap items-center gap-x-4 gap-y-1`. Two entry shapes:
   ITEMS: **Label/value entry** (default): caps micro-label `text-[10p | **Sentence entry** (HealthAlertsPanel detail/follow-up templ

PARITY docs/superpowers/specs/2026-07-20-show-alert-compact.md:242 claim=two(2) noun=axes list@244 count=2(raw 2)
   TEXT:  an ORTHOGONAL dimension, not a peer state. The model is therefore a product of two independent axes:
   ITEMS: **Resolve axis (card-owned):** `D` default → `Rp` request in | **Popover axis:** `closed` ↔ `open`.

MISMATCH docs/superpowers/specs/2026-07-20-show-alert-compact.md:295 claim=three(3) noun=rounds list@297 count=4(raw 4)
   TEXT: atch geometry prose a third time — AGENTS.md caps design-correctness vectors at three prose rounds and prescribes a spike instead — this spec DESCOPES the policy:
   ITEMS: Every card passes `align="right"` and OMITS `placement`, inh | Known residual, recorded rather than solved: AttentionBanner | This residual is PRE-EXISTING for every HoverHelp consumer i | §9 therefore asserts what CAN be proven without the policy: 

PARITY docs/superpowers/specs/2026-07-20-show-scoped-alert-copy-design.md:43 claim=three(3) noun=arguments list@45 count=3(raw 3)
   TEXT: dminAlerts/deriveMessageParams.ts:285-289`. Three callers, all passing the same three arguments:
   ITEMS: `lib/adminAlerts/fetchPerShowAlerts.ts:170` — show-scoped al | `lib/admin/bellFeed.ts:293` — global bell feed | `components/admin/telemetry/HealthAlertsPanel.tsx:78` — deve

PARITY docs/superpowers/specs/2026-07-20-show-scoped-alert-copy-design.md:193 claim=Two(2) noun=rows list@195 count=2(raw 2)
   TEXT: Renaming the button falsifies user-visible copy that quotes it. Two catalog rows quote "Mark resolved" verbatim:
   ITEMS: `lib/messages/catalog.ts:2189` (`ADMIN_ALERT_NOT_FOUND`) — " | `lib/messages/catalog.ts:2192` (`ALERT_REQUIRES_SHOW_SCOPED_

PARITY docs/superpowers/specs/2026-07-20-warning-surface-trim-design.md:37 claim=Two(2) noun=properties list@39 count=2(raw 2)
   TEXT: sBySection` (`lib/admin/step3SectionStatus.ts:84`) routes warnings to sections. Two properties decide everything downstream:
   ITEMS: it drops info-severity entirely (`lib/admin/step3SectionStat | every warn-severity warning lands in a bucket, its mapped se

PARITY docs/superpowers/specs/2026-07-20-warning-surface-trim-design.md:48 claim=Three(3) noun=consequences list@50 count=3(raw 3)
   TEXT: Three consequences of the duplication:
   ITEMS: A warning fixed or ignored in its section still sits in the  | The more final-looking surface is the powerless one. | `railCount: (s) => s.warnings.length` (`components/admin/wiz

PARITY docs/superpowers/specs/2026-07-20-warning-surface-trim-design.md:110 claim=three(3) noun=readers list@112 count=3(raw 3)
   TEXT: **Distribution to the three readers:**
   ITEMS: **Panel body list.** The gate joins `Step3SectionChromeConte | **Panel body empty state.** `routedWarnings` joins the same  | **Rail count.** `railCount` widens to `(d: SectionData, opts

PARITY docs/superpowers/specs/2026-07-20-warning-surface-trim-design.md:292 claim=Three(3) noun=findings list@294 count=3(raw 3)
   TEXT: transcript covers all 16 claim clusters and is attached to the review dispatch. Three findings changed the design rather than confirming it:
   ITEMS: `warningsBySection` routes unmapped warn warnings to the `wa | `DOUG_EXCLUDED_CODES` has zero production consumers, which r | The registry's `render` and `railCount` receive only `Sectio

PARITY docs/superpowers/specs/2026-07-21-attention-needs-attention-split.md:13 claim=Two(2) noun=defects list@15 count=2(raw 2)
   TEXT: dash) and the pill shows an "N clearing" line (`PublishedReviewModal.tsx:724`). Two defects:
   ITEMS: **The copy lies.** Most of the bucket does need a human. `re | **The bucket is a dead end.** The operator sees a count with

MISMATCH docs/superpowers/specs/2026-07-21-attention-needs-attention-split.md:82 claim=four(4) noun=states list@84 count=6(raw 6)
   TEXT: Replace the four mutually-exclusive states with a composed pill that always shows what is present:
   ITEMS: Derive `needsLookCount` and `selfHealCount` from `live` (§3. | **Interactive pill** renders when `actionable.length > 0 OR  | **Non-interactive `{selfHealCount} monitoring`** pill segmen | **Degraded** (`alertsDegraded && everything === 0`): "Alerts | **In sync** (all counts 0, not degraded): "In sync" (unchang | **Key fix:** `needsLookCount` and `selfHealCount` are NO LON

MISMATCH docs/superpowers/specs/2026-07-21-attention-needs-attention-split.md:249 claim=10(10) noun=codes list@250 count=4(raw 4)
   TEXT: 3. **Action resolution — exhaustive, table-driven over ALL 10 linked codes.** For each linked code in §2 (rows 1-10), assert the exact resolved `{label, href, external}`:
   ITEMS: 6 sheet codes → `openSheet`: `href === https://docs.google.c | 4 `showAnchor` codes + `RESYNC_SHRINK_HELD` → `href === /adm | `ASSET_RECOVERY_BYTES_EXCEEDED` → resolves to NO action (nul | Failure mode caught: a code silently shipping as a read-only

PARITY docs/superpowers/specs/2026-07-21-settings-attention-gallery-link.md:75 claim=two(2) noun=links list@77 count=2(raw 2)
   TEXT: `<div className="flex flex-wrap items-center gap-2">` containing two links:
   ITEMS: Existing: `data-testid="admin-dev-tools-open"`, `href="/admi | New: `data-testid="admin-dev-tools-gallery"`,

PARITY docs/superpowers/specs/2026-07-21-unread-callout-dedup.md:12 claim=five(5) noun=problems list@14 count=5(raw 5)
   TEXT: sheet rows **twice**, and three smaller alert-surface issues sit alongside. The five problems, numbered canonically (referenced by these numbers everywhere below):
   ITEMS: **Problem 1 (the bug): duplicate render.** The unparsed `raw | **Problem 2: count desync.** The "Parse warnings" section he | **Problem 3: vocabulary.** The same concept was named three  | **Problem 4: opaque clearing pill.** The modal header pill r | **Problem 5: orphan chrome.** The bottom callout has no rail

PARITY docs/superpowers/specs/2026-07-21-unread-callout-dedup.md:24 claim=Five(5) noun=links list@25 count=5(raw 5)
   TEXT: he callout rendered, so removing the callout cannot hide any unrecognized row.* Five verified links:
   ITEMS: **Single origin, co-emitted.** `emitUnknownField` (`lib/pars | **Atomic co-persistence.** Both columns are written in one u | **Total routing to a rendered bucket.** `warningsBySection`  | **Superset on the cap axis.** The deleted callout truncated  | **Distinct rows are never collapsed.** `operatorActionableWa

PARITY docs/superpowers/specs/2026-07-21-warning-card-identity-placement.md:78 claim=Three(3) noun=cues list@80 count=3(raw 3)
   TEXT: Three cues place that group at the top level:
   ITEMS: the extras wrapper is `mt-3 flex flex-col gap-3 border-t bor | the eyebrow is `text-xs font-semibold uppercase tracking-eye | it mounts OUTSIDE the roster's bordered container, as a sibl

MISMATCH docs/superpowers/specs/2026-07-21-warning-card-identity-placement.md:305 claim=four(4) noun=changes list@307 count=3(raw 3)
   TEXT: **Scope boundary, stated precisely (R3 BLOCKING).** The four changes do NOT share a scope:
   ITEMS: **Change 2 (instance copy) is UNIVERSAL.** `warningCardCopyF | **Change 3 (under-row placement) is PUBLISHED-ONLY** — gated | **Change 4 (group nesting) is PUBLISHED-ONLY** — gated by `r

MISMATCH docs/superpowers/specs/2026-07-22-attention-gallery-gap-fill-design.md:43 claim=3(3) noun=renders list@45 count=5(raw 5)
   TEXT: ### §3.1 Tier 3 renders in the switcher
   ITEMS: Delete the `tier === 3` skip in `partitionScenarios` (`build | Widen `GallerySwitcherScenario.tier` to `1 | 2 | 3` (`galler | `resolveInitialScenario` needs NO edit: it already keys off  | Materialize (`MaterializeCard`, `applyAttentionScenario`) is | The tier-3 `galleryIdentity` divergence note (`tier3.ts:52-5

PARITY docs/superpowers/specs/2026-07-22-hoverhelp-smart-position.md:12 claim=Two(2) noun=consequences list@14 count=2(raw 2)
   TEXT: solutely IN FLOW below the trigger (`top-[calc(100%+6px)]`, HoverHelp.tsx:249). Two consequences:
   ITEMS: **Overlap** — on compact alert cards the `?` trigger sits at | **Clipping** — inside a scrolling surface the popover is vis

PARITY docs/superpowers/specs/2026-07-22-hoverhelp-smart-position.md:115 claim=two(2) noun=formulas list@116 count=2(raw 2)
   TEXT: **Host coordinate conversion (corrected per R2 F3 — two exact formulas, no shared approximation):**
   ITEMS: **Panel host** (containing block = the panel, which is `posi | **Body host** (body is `position: static` under Tailwind pre

PARITY docs/superpowers/specs/2026-07-22-modal-state-coverage-design.md:354 claim=12(12) noun=findings list@356 count=12(raw 12)
   TEXT: **R1 (Codex via codex-guard, 2026-07-22): BLOCKING, 12 findings — all repaired.**
   ITEMS: F1 (BLOCKING, MENU_CAP not a modal cap): already fixed in th | F2 (BLOCKING, schedule recipe wrong vs renderer): recipe reb | F3 (BLOCKING, lifecycle contradictions unguarded): archived⊃ | F4 (NA, three lifecycle claims stale): archived row no longe | F5 (BLOCKING, sync labels uncovered): roster expanded to all | F6 (NA, 99+ is per-class): recipe rewritten to guarantee per | F7 (NA, ignored-warning guards): blank-snippet, fingerprint- | F8 (NA, diagram + under-row shapes): full persisted-image sh | F9 (NA, missed static states): roster + volumes gained pack- | F10 (NA, changelog matrix ambiguous): explicit 7-row matrix, | F11 (NA, `isCrewDomainChangeKind` ownership): helper + set m | F12 (NIT, stale claims): venue-null contingency dropped (leg

PARITY docs/superpowers/specs/2026-07-22-warning-panel-polish-design.md:43 claim=three(3) noun=edits list@45 count=3(raw 3)
   TEXT: **Change.** One additive prop, three call-site edits:
   ITEMS: `HoverHelp` gains optional `afterBodyText?: string`. **Delib | `CompactAlertHelp` gains optional `afterBodyText?: string |  | `PerShowActionableWarnings` stops joining: `popoverBody` rev

PARITY docs/superpowers/specs/2026-07-23-crew-warning-attachment.md:10 claim=Two(2) noun=causes list@12 count=2(raw 2)
   TEXT: roup) renders as a floating banner between the Crew card and the Contacts card. Two causes:
   ITEMS: Under-row placement (`warningsByCrewKey`) is gated to exactl | The section warning group renders via `renderSectionExtras` 

PARITY docs/superpowers/specs/2026-07-23-sharehub-focus-pass.md:32 claim=Two(2) noun=defects list@34 count=2(raw 2)
   TEXT: Two defects:
   ITEMS: **Inconsistency.** Three treatments in one 308px panel with  | **Dark-mode white halo.** Tailwind's `ring-offset-2` without

MISMATCH docs/superpowers/specs/2026-07-23-warning-trim-undefer-design.md:145 claim=two(2) noun=tests list@147 count=5(raw 5)
   TEXT: ## 5. Item 4 — Wizard-unchanged proof (two tests)
   ITEMS: **Blind spot being closed:** `tests/components/admin/stagedC | **Test A — ParsePanel composition** (new file tests/componen | **Test B — wizard warnings-branch pin** (same new file, seco | **Anti-tautology:** expected row/card counts derive from the | **Ordering within this PR:** both tests land and pass BEFORE

PARITY docs/superpowers/specs/2026-07-24-attention-index-consolidation.md:166 claim=Two(2) noun=filters list@168 count=2(raw 2)
   TEXT: Two independent existing filters do it:
   ITEMS: `DOUG_EXCLUDED_CODES` is every `severity: "info"` code UNION | `PICKER_EPOCH_RESET` has an explicit clause in `deriveAttent

PARITY docs/superpowers/specs/2026-07-24-dq-eyebrow-divider-and-confirm-bar-design.md:56 claim=Two(2) noun=pins list@58 count=2(raw 2)
   TEXT: Two facts this pins:
   ITEMS: The collapse is total below 480 and absent at/above it. `sm` | Shortening the chip copy alone does **not** fix it — the swe

PARITY docs/superpowers/specs/2026-07-24-gallery-alert-producer-parity.md:161 claim=Three(3) noun=rules list@163 count=3(raw 3)
   TEXT: ScenarioAttention.ts:39-42`) stays legal and still wins over the derived value. Three rules constrain it, closing the "synthetic state production cannot produce" gap:
   ITEMS: **Code restriction.** `crewMatch` is legal ONLY on a code th | **Context agreement — no exceptions.** When a row declares b | **Identity agreement.** For `AMBIGUOUS_EMAIL_BINDING`, the d

MISMATCH docs/superpowers/specs/2026-07-24-picker-flow-app-bugs.md:35 claim=two(2) noun=breaks list@37 count=3(raw 3)
   TEXT: w.md` ("Empirical spike before speccing stateful/race/framework surfaces"), the two behavioral breaks were verified against the live chain before drafting, not reasoned from prose:
   ITEMS: **Host flip.** The SKIP comment at `tests/e2e/picker-flow.sp | **Mismatch ordering.** Read directly from the resolver: `lib | **GET-form query discard.** The SKIP comment at `tests/e2e/p

MISMATCH docs/superpowers/specs/2026-07-24-picker-flow-app-bugs.md:115 claim=three(3) noun=layers list@117 count=1(raw 1)
   TEXT: Anti-tautology, in three layers, because a walked-file count alone proves only that traversal worked (R1 finding 6):
   ITEMS: **Detector fixtures.** The matcher is a pure exported functi

PARITY docs/superpowers/specs/2026-07-24-picker-flow-app-bugs.md:177 claim=three(3) noun=rounds list@179 count=3(raw 3)
   TEXT:  imaginary but that the fix could not be specified correctly at this scope, and three rounds of review said so:
   ITEMS: R1 finding 4 asked for the gate, on the correct observation  | R2 finding 3 showed the table let a contradictory `sec-fetch | R3 findings 1 and 2 showed the repaired table was still inte

PARITY docs/superpowers/specs/2026-07-24-picker-flow-app-bugs.md:191 claim=Three(3) noun=things list@193 count=3(raw 3)
   TEXT: ate keeps rendering until expiry — after which the stale picker entry resolves. Three things make this an accepted limitation rather than a defect to design around:
   ITEMS: **It is not a new exposure, though it does shorten the windo | **No ordering fixes it.** Both mutations ride the same respo | **The precedent is identical.** `app/auth/sign-out/route.ts`

PARITY docs/superpowers/specs/2026-07-24-picker-flow-app-bugs.md:289 claim=three(3) noun=stubs list@291 count=3(raw 3)
   TEXT: Un-skip three stubs in `tests/e2e/picker-flow.spec.ts` (they run under the `mobile-safari` project, `playwright.config.ts:54`, whose `test
   ITEMS: `tests/e2e/picker-flow.spec.ts:84` first-contact, then Googl | `tests/e2e/picker-flow.spec.ts:180` Mode B "Continue as gues | `tests/e2e/picker-flow.spec.ts:241` claimed row, then `/auth

PARITY docs/superpowers/specs/2026-07-24-picker-flow-app-bugs.md:292 claim=Three(3) noun=assertions list@293 count=3(raw 3)
   TEXT: cker-interstitial-root` visible, and Alice's entry gone from the picker cookie. Three assertions are **added**, because reaching the picker once is exactly what the rejected backlog design also achieved and is 
   ITEMS: no cookie satisfying `isSupabaseAuthCookieName` remains in t | the person then taps the **unclaimed** roster row (Bob) and  | the page is reloaded with a bare `page.goto(urlA)` carrying 

PARITY docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:58 claim=two(2) noun=things list@60 count=2(raw 2)
   TEXT: The item asks for two things:
   ITEMS: Replace the arbitrary `max-w-[16rem]` with a named token (ap | Add an explicit `min-w` to the crew-page link, which sets `m

MISMATCH docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:320 claim=Three(3) noun=lines list@322 count=2(raw 2)
   TEXT: **Stale cross-references the rename leaves behind.** Three comment lines in another test name the old filename and both deleted components, and go stale silently because comments do n
   ITEMS: `tests/app/admin/rotateShareToken.test.tsx:9` — names ShareC | `tests/app/admin/rotateShareToken.test.tsx:10` and `tests/ap

PARITY docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:327 claim=two(2) noun=pixels list@329 count=2(raw 2)
   TEXT: t the token changes, so a button still reading "Copied" asserts something false two pixels from the cue announcing the change:
   ITEMS: a render-phase reset of `copied` when `url` changes, so no f | a captured-url check in the async handler, so a `writeText` 

PARITY docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:338 claim=three(3) noun=items list@340 count=3(raw 3)
   TEXT: ACKLOG.md` — the whole `## Crew-page share-link chrome` section is removed (all three items close in this diff), with each item's resolution recorded in `BACKLOG-archive.md` following the archive's existing per
   ITEMS: `BL-CREWPAGE-ROTATE-URL-FLASH` becomes RESOLVED, citing this | `BL-CREWPAGE-SHARE-CHIP-TOKEN-DISCIPLINE` becomes RESOLVED B | `BL-CREWPAGE-ROTATE-FOCUS-MGMT` becomes CLOSED, SUPERSEDED, 

PARITY docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:547 claim=Two(2) noun=properties list@549 count=2(raw 2)
   TEXT: taled popover, with its `tests/e2e/_hoverHelpGeometryLiveEntry.tsx` entry file. Two properties are load-bearing and both must be copied:
   ITEMS: the real component tree is bundled with VERSION-PINNED esbui | the token CSS is compiled from `app/globals.css` through the

PARITY docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:552 claim=four(4) noun=points list@554 count=4(raw 4)
   TEXT: **Wiring — four points, all required, none discoverable by convention.** A spec file that merely exists proves nothing here (`tests/e2e/stand
   ITEMS: the spec file; | an entry in the `testMatch` allow-list at `tests/e2e/standal | a dedicated workflow naming the spec, modelled on `.github/w | a row in the `_metaE2eWorkflowCoverage` registry. That guard

PARITY docs/superpowers/specs/2026-07-24-share-link-chrome-backlog-design.md:585 claim=two(2) noun=things list@587 count=2(raw 2)
   TEXT: But the record now shows two clearly separated things:
   ITEMS: **The DESIGN has been confirmed correct in every round since | **What has kept failing is the ORACLE PROSE** — the attempt 

PARITY docs/superpowers/specs/2026-07-24-sharehub-viewport-popover-and-archive-copy.md:320 claim=two(2) noun=cases list@322 count=2(raw 2)
   TEXT: T-HUB-ZORDER (`ShareHub.tsx:51`) must still pass after the `z-30` removal, plus two added cases:
   ITEMS: With the hub popover open, the header attention menu's panel | With the attention menu open, clicking `share-hub-backdrop` 

PARITY docs/superpowers/specs/2026-07-24-test-safety-hardening-batch.md:223 claim=Two(2) noun=sites list@225 count=2(raw 2)
   TEXT: Two fail-closed catch sites map **any** `prepareOnboardingFiles` throw to `DRIVE_FETCH_FAILED`:
   ITEMS: `lib/onboarding/rescanWizardSheet.ts:143-158` (BACKLOG cites | `app/api/admin/onboarding/finalize/route.ts:811-829` (inline

PARITY docs/superpowers/specs/2026-07-25-newtab-announcement-family.md:38 claim=Two(2) noun=reasons list@40 count=2(raw 2)
   TEXT: Two reasons the narrow count undercounts:
   ITEMS: **Conditional spreads** (4 anchors) apply the attribute thro | **`app/` is in the family** (1 anchor). **CrewPageLink.tsx:2

MISMATCH docs/superpowers/specs/2026-07-25-section-header-rebuild-and-phantom-spacers.md:444 claim=Two(2) noun=rules list@446 count=3(raw 3)
   TEXT: logical if the expected centre is computed from the rendered name being tested. Two rules:
   ITEMS: **Centring oracle is FORMULA-derived, not render-derived.**  | **The count's rendered width is part of the oracle, and it i | **Tolerances are numbers, not a reference.** §3.1.5 lists of

PARITY docs/superpowers/specs/2026-07-26-agenda-perday-viewer-fold.md:327 claim=two(2) noun=functions list@329 count=2(raw 2)
   TEXT: `lib/crew/agendaDayForToday.ts` already solves day→date matching, split across two functions:
   ITEMS: `parseIsoFromDayLabel` (`lib/crew/agendaDayForToday.ts:36`)  | The positional fallback lives inside `agendaSessionsForToday

MISMATCH docs/superpowers/specs/2026-07-26-agenda-perday-viewer-fold.md:359 claim=Two(2) noun=constraints list@361 count=3(raw 3)
   TEXT: variant beside it, reusing `parseIsoFromDayLabel` and the same positional rule. Two constraints:
   ITEMS: The positional fallback must index against the **full show d | **Fail open per link.** `hasAgenda` allows multiple PDFs; on | **Fail open on PARTIAL resolution too — corrected in review 

PARITY docs/superpowers/specs/2026-07-26-agenda-perday-viewer-fold.md:657 claim=Two(2) noun=consequences list@659 count=2(raw 2)
   TEXT: Two consequences worth stating rather than discovering:
   ITEMS: **A `<details>` the viewer opened stays open across the refr | **The marker must update on refresh even while the row stays

PARITY docs/superpowers/specs/2026-07-26-agenda-perday-viewer-fold.md:732 claim=Two(2) noun=shapes list@734 count=2(raw 2)
   TEXT: ate is still announced, must be checked in a real browser, not asserted here.** Two shapes are acceptable and the layout task decides between them:
   ITEMS: `<summary><h3>Day 1</h3> <span>count</span></summary>` — kee | `<summary>` carrying the text directly, with the `<h3>` move

PARITY docs/superpowers/specs/2026-07-26-childless-growable-static-guard-design.md:50 claim=eight(8) noun=sites list@52 count=8(raw 8)
   TEXT: The eight non-bulk sites, verified against the live tree:
   ITEMS: `components/admin/BulkIgnoreControls.tsx:199` — `<span aria- | `components/admin/wizard/step3ReviewSections.tsx:2248` — `<s | `components/admin/OnboardingWizard.tsx:196` — step connector | `components/crew/RightNowHero.tsx:549` — progress segment `< | `components/admin/showpage/ShowReviewModalSkeleton.tsx:152`  | `components/admin/telemetry/EventFilters.tsx:74` — `<FilterT | `components/crew/primitives/PersonRow.tsx:163` — `<div class | Additionally sized (not a candidate): `components/crew/primi

PARITY docs/superpowers/specs/2026-07-26-childless-growable-static-guard-design.md:86 claim=four(4) noun=shapes list@88 count=4(raw 4)
   TEXT: Every growable candidate must match one of four shapes:
   ITEMS: **Possibly-childed.** Any static JSX/text child, or any expr | **Painted childless (DOM tags only) — idiom membership, not  | **Registered component.** EVERY childless growable component | **Exemption comment.** `childless-growable-ok: <non-empty re

MISMATCH docs/superpowers/specs/2026-07-26-gallery-screenshot-capture-design.md:335 claim=Three(3) noun=layers list@337 count=1(raw 1)
   TEXT: TDD per task (invariant 1). Three layers:
   ITEMS: **DB-free unit tests** (run in `pnpm test`):

PARITY docs/superpowers/specs/2026-07-26-header-probe-residual-closure-design.md:9 claim=Four(4) noun=findings list@11 count=4(raw 4)
   TEXT: nvergent vector: assertions vacuous under configurations the tests never enter. Four findings were recorded rather than fixed:
   ITEMS: Real-route width chain anchors only 375/640/1280; 320 and 43 | The CSS active state and simultaneous hover+focus are not sw | SVG SMIL (`<animateTransform>`) moves geometry while every C | Exotic paint suppression (`clip-path` variants, partial mask

PARITY docs/superpowers/specs/2026-07-26-stripcomments-shared-design.md:140 claim=Five(5) noun=families list@142 count=5(raw 5)
   TEXT: ts/styles/_metaDoublePrefixColorToken.test.ts:46-65`, cannot false-positive). **Five detector families** (R2 F2 widened):
   ITEMS: **Block-comment regex literals** — the `/\*[\s\S]*?\*\//`, ` | **Line-comment replace idioms** — `//`-to-EOL AND `--`-to-EO | **Two-char scanner literals** — a string literal exactly `"/ | **Line-start skip filters** — `startsWith` with argument exa | **Name family** — definitions matching `strip\w*[Cc]omment\w

PARITY docs/superpowers/specs/2026-07-26-stripcomments-shared-design.md:149 claim=two(2) noun=lists list@151 count=2(raw 2)
   TEXT: hit in a file other than `tests/_shared/stripComments.ts` must appear in one of two lists (R2 F5 split):
   ITEMS: **`STANDING_ALLOWLIST`** — permanent rows, each `{file, mark | **`PENDING_MIGRATIONS`** — the migration-window scaffold: on

MISMATCH docs/superpowers/specs/2026-07-27-duration-tokens-emit-no-css.md:72 claim=four(4) noun=names list@74 count=2(raw 2)
   TEXT: p output dir; measured ≈130ms). Asserts on the compiled output, for EACH of the four names:
   ITEMS: a `.duration-<name>` rule exists with `transition-duration:  | the theme layer declares `--transition-duration-<name>: var(

PARITY docs/superpowers/specs/2026-07-27-duration-tokens-emit-no-css.md:81 claim=Two(2) noun=assertions list@83 count=2(raw 2)
   TEXT:  carries a literal `duration-fast` class (`components/crew/CrewSubNav.tsx:86`). Two assertions:
   ITEMS: default media: `getComputedStyle(tab).transitionDuration` eq | after `page.emulateMedia({ reducedMotion: "reduce" })` + rel

PARITY docs/superpowers/specs/2026-07-27-export-blank-row-segmentation.md:11 claim=Two(2) noun=modes list@13 count=2(raw 2)
   TEXT: as the **only** delimiter. Two silent failure modes:
   ITEMS: **Fuse:** a stray value in a spacer row joins two adjacent s | **Split:** a blank row inserted mid-section severs the tail 

PARITY docs/superpowers/specs/2026-07-31-judgment-chip-newtab-suffix-design.md:66 claim=Three(3) noun=labels list@68 count=3(raw 3)
   TEXT: Three labels interpolate user-supplied text and then append the literal suffix:
   ITEMS: `components/admin/SheetIconLink.tsx:92-95` — `trimmed` (show | `components/admin/wizard/step3ReviewSections.tsx:3675` — `ar | `components/admin/wizard/Step3SheetCard.tsx:152` — `aria-lab

PARITY docs/superpowers/specs/2026-07-31-judgment-chip-newtab-suffix-design.md:88 claim=three(3) noun=sites list@90 count=3(raw 3)
   TEXT: Applied at the three interpolation sites, value-position only — the literal suffix in each template stays, so the static guard's per-branch phras
   ITEMS: `SheetIconLink`: strip inside the existing `trimmed` computa | Diagram alt anchor: `alt` → conditional on the **stripped**  | `Step3SheetCard`: strip `title` in the label; a stripped-to-

PARITY docs/superpowers/specs/2026-08-01-announce-a11y-pass-design.md:105 claim=two(2) noun=values list@107 count=2(raw 2)
   TEXT: econcile branch, when the accepted seed actually CHANGES the held token between two non-null values:
   ITEMS: accept branch taken (`initialEpoch >= p.epoch`), and | `p.token !== null && initialToken !== null && initialToken !

MISMATCH docs/superpowers/specs/2026-08-01-announce-a11y-pass-design.md:138 claim=11(11) noun=runs list@140 count=5(raw 5)
   TEXT: with a timer, extend the surface's existing auto-revert tests (every one of the 11 files already runs `vi.useFakeTimers` + a 4s advance around the revert) with:
   ITEMS: **Expiry announces:** arm, advance past `ARM_REVERT_MS` → th | **Every explicit disarm path is silent, including after the  | **Re-arm audibility (R1 F2):** expire → re-arm → expire; ass | **Row 1 exclusivity (R1 F3):** with ≥2 groups rendered, expi | **Row 6 row-branch negative:** the `asRow` archive variant n

PARITY docs/superpowers/specs/2026-08-01-focus-ring-a11y-pass-design.md:71 claim=three(3) noun=oracles list@73 count=3(raw 3)
   TEXT: mily pairs (the §4.3 allowlist, imported) out of `app/globals.css`, then assert three oracles:
   ITEMS: **Ratification pin:** the light declaration is exactly `#E06 | **Dark-pair identity:** the media-query dark value and the d | **Matrix floor:** every ring × backdrop pair (alpha-composit

MISMATCH docs/superpowers/specs/2026-08-01-invariant8-closeout-enforcement-design.md:132 claim=7(7) noun=limits list@134 count=5(raw 5)
   TEXT: ## 7 Documented limits (honest ceiling — accepted, not findings)
   ITEMS: The guard proves a unit CARRIES a well-formed gate claim, no | A UI diff shipped with no plan document is invisible (§1.1.8 | Ledger growth is policed by PR review (prose reason), not me | The N/A form's truth ("no UI surface") is not diffed against | `RAN-DEGRADED`'s justification prose is unread by the guard.

PARITY docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:304 claim=3(3) noun=findings list@305 count=3(raw 3)
   TEXT: - **R1 — NEEDS-ATTENTION, 3 findings, all ACCEPTED, none refuted.**
   ITEMS: **F1 (HIGH) — the replacement copy preserved an unsupported  | **F2 (HIGH) — basename matching lets a genuine orphan read a | **F3 (LOW) — three census figures were wrong.** Accepted and

MISMATCH docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:308 claim=5(5) noun=findings list@309 count=6(raw 6)
   TEXT: - **R2 — NEEDS-ATTENTION, 5 findings, all ACCEPTED, none refuted.**
   ITEMS: **F1 (HIGH) — path resolution still permitted false greens.* | **F2 (HIGH) — the artifacts activated an undeclared, already | **F3 (MEDIUM) — the "exactly two sites tree-wide" census was | **F4 (LOW) — orphan-component rationale leaked into the `BL- | **F5 (LOW) — the plan's whole-diff review brief still said f | Reviewer-confirmed and not to be re-derived: the corrected h

MISMATCH docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:315 claim=6(6) noun=findings list@316 count=7(raw 7)
   TEXT: - **R3 — BLOCKING, 6 findings, all ACCEPTED, none refuted.**
   ITEMS: **F1 (P0) — Task 4 mutated three tracked documents with no f | **F2 (HIGH) — the single-candidate resolver still had two es | **F3 (HIGH) — the acceptance grep could never pass.** `rg -n | **F4 (MEDIUM) — two stale numbers survived R2's re-measureme | **F5 (LOW) — the R2 F4 repair left its rejected rationale in | **F6 (LOW) — filing chronology stale in three places** (spec | Reviewer-confirmed and not to be re-derived: §1.1 item 13's 

MISMATCH docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:323 claim=2(2) noun=findings list@324 count=3(raw 3)
   TEXT: - **R4 — NEEDS-ATTENTION, 2 findings, both ACCEPTED, none refuted.**
   ITEMS: **F1 (HIGH) — family (g)'s first form did not close the clas | **F2 (LOW) — two plan references still carried the pre-(g) f | Reviewer-confirmed and not to be re-derived: Task 4's RED is

MISMATCH docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:327 claim=2(2) noun=findings list@328 count=4(raw 4)
   TEXT: - **R5 — NEEDS-ATTENTION, 2 findings, both ACCEPTED, none refuted.**
   ITEMS: **F1 (HIGH) — the two shape pins did not cover compiler conf | **F2 (LOW) — four statements still called every family a syn | Also landed this round, unprompted by a finding: **§3.4, an  | Reviewer-confirmed and not to be re-derived: the merge-base 

MISMATCH docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:332 claim=3(3) noun=findings list@333 count=4(raw 4)
   TEXT: - **R6 — NEEDS-ATTENTION, 3 findings, all ACCEPTED, none refuted.**
   ITEMS: **F1 (HIGH) — the compiler-option pin read the config file i | **F2 (LOW) — the spike's coverage claim and the escape count | **F3 (MEDIUM) — Task 1's verification could pass with the `l | Reviewer-confirmed and not to be re-derived: the component c

MISMATCH docs/superpowers/specs/2026-08-02-copy-deadcode-sweep-design.md:338 claim=4(4) noun=findings list@339 count=6(raw 6)
   TEXT: - **R7 — NEEDS-ATTENTION, 4 findings, all ACCEPTED, none refuted.**
   ITEMS: **F1 (HIGH) — the compiler resolver was right but the compil | **F2 (MEDIUM) — the residual-assumption list was incomplete  | **F3 (MEDIUM) — Task 1's regen check could not be green wher | **F4 (LOW) — four current-design counts were stale** after t | Reviewer-confirmed and not to be re-derived: the algorithm r | Reviewer-confirmed at R1 and not to be re-derived: the §2.3 

MISMATCH docs/superpowers/specs/2026-08-03-app-wide-font-binding.md:24 claim=Three(3) noun=findings list@26 count=2(raw 2)
   TEXT: Three findings, all load-bearing:
   ITEMS: **The crew binding works.** Next 16's `next/font/google` reg | **The non-crew tree renders the system fallback.** `185.53px

PARITY docs/superpowers/specs/2026-08-03-app-wide-font-binding.md:79 claim=Two(2) noun=measurements list@81 count=2(raw 2)
   TEXT:  swap is layout-affecting even though it is not a dimensional-invariant change. Two measurements carry it, and the distinction matters (review R2 was right that an unconditional "is proven" here contradicted §4
   ITEMS: **Unconditional:** §4.1 measures rendered TEXT ADVANCE with  | **Conditional:** §4.2's group-title row runs only under its 

PARITY docs/superpowers/specs/2026-08-03-app-wide-font-binding.md:211 claim=Two(2) noun=things list@213 count=2(raw 2)
   TEXT: **Disposition: still a documented limit, but on the honest reasoning.** Two things separate them:
   ITEMS: **The sign-out page's `system-ui, sans-serif` is defensible  | **The other three get browser defaults — a serif — which nob

PARITY docs/superpowers/specs/2026-08-03-app-wide-font-binding.md:222 claim=three(3) noun=reasons list@224 count=3(raw 3)
   TEXT: whitespace-nowrap` plus truncation on the closed-set group titles. Rejected for three reasons, recorded so it is not relitigated:
   ITEMS: After §2 the wide-fallback path does not exist on any Next-r | `truncate` on the group title would make `tests/e2e/section- | It changes what a real user sees (an ellipsis) to change wha

PARITY docs/superpowers/specs/2026-08-03-apply-undo-audit-fidelity-design.md:91 claim=Two(2) noun=things list@93 count=2(raw 2)
   TEXT: `appliedCrewMembers` explained by something other than a real capability loss?" Two things explain it:
   ITEMS: **The rename landed** — the successor row carries the capabi | **The row survived without being in the applied list.** `app

PARITY docs/superpowers/specs/2026-08-03-apply-undo-audit-fidelity-design.md:181 claim=Two(2) noun=mechanisms list@182 count=2(raw 2)
   TEXT: nal envelope returned THROUGH `withRowTx`, so it crosses the commit boundary.** Two rejected mechanisms and why, since each looks right:
   ITEMS: **Not `PerRowResult`.** It is serialized verbatim into the p | **Not a passed-in accumulator mutated inside the row functio

MISMATCH docs/superpowers/specs/2026-08-03-harness-font-fidelity-design.md:177 claim=Two(2) noun=changes list@179 count=1(raw 1)
   TEXT: Two changes close it, and the spec requires both:
   ITEMS: **The harness-emitted face uses `font-display: block`, not `

MISMATCH docs/superpowers/specs/2026-08-03-inter-numeral-disambiguation-design.md:86 claim=Two(2) noun=files list@88 count=5(raw 5)
   TEXT: gainst `git ls-files`. Do not "fix" them into backticks — the linter will fail. Two files, both under node_modules/next/dist:
   ITEMS: **`unicode-range` per file: not expressible.** `declarations | **`adjustFontFallback` defaults ON** and, unlike the Google  | **The `variable` token keeps its two-entry shape.** The toke | **`weight` and `style` must be passed explicitly.** The load | **The generated family name changes case**, from the literal

PARITY docs/superpowers/specs/2026-08-03-inter-numeral-disambiguation-design.md:292 claim=Two(2) noun=oracles list@294 count=2(raw 2)
   TEXT: Two oracles replace it, each matched to what the feature actually changes:
   ITEMS: **`zero` — a pixel oracle.** Render `0` twice in identically | **`ss04` — a width oracle, which does work here.** The same 

PARITY docs/superpowers/specs/2026-08-03-inter-numeral-disambiguation-design.md:513 claim=Two(2) noun=changes list@515 count=2(raw 2)
   TEXT: e rounds you stop patching prose or spellings and close the class structurally. Two changes do that:
   ITEMS: **The value is tokenized by `postcss-value-parser`, not by h | **The parser has its own test table.** Every mutant from rou

PARITY docs/superpowers/specs/2026-08-03-inter-numeral-disambiguation-design.md:612 claim=Three(3) noun=families list@614 count=3(raw 3)
   TEXT: ed. Round 11's guard was real but narrow, and its stated guarantee overreached. Three families escaped, each demonstrated with live emitted HTML:
   ITEMS: **`.mdx` was not scanned at all.** The repository has 13 rou | **Quoted and computed property spellings.** `"fontFeatureSet | **Inline `font` and `all` resetters were not covered**, thou

MISMATCH docs/superpowers/specs/2026-08-03-inter-numeral-disambiguation-design.md:636 claim=three(3) noun=probes list@638 count=2(raw 2)
   TEXT: That is correct, and it is the reason to stop. Two of the three probes are decidable and are fixed:
   ITEMS: `setProperty("FONT-FEATURE-SETTINGS", …)` — the CSS name is  | `setProperty("all", "initial")` — a reset with no `style=` r

PARITY docs/superpowers/specs/2026-08-03-inter-numeral-disambiguation-design.md:643 claim=Two(2) noun=things list@645 count=2(raw 2)
   TEXT: **Two things replace the closure claim:**
   ITEMS: **A runtime assertion.** `tests/e2e/font-binding.spec.ts` wa | **A documented limit, stated plainly.** *An inline style con

PARITY docs/superpowers/specs/2026-08-03-lead-capability-prose-settle-design.md:86 claim=two(2) noun=reasons list@88 count=2(raw 2)
   TEXT: resolved, and converge structurally rather than retail. It is descoped here for two reasons beyond round count:
   ITEMS: **It is a different class, not a wider instance of this one. | **Its extent is unbounded and unmeasured.** Every sweep so f

PARITY docs/superpowers/specs/2026-08-03-lead-capability-prose-settle-design.md:212 claim=Three(3) noun=findings list@214 count=3(raw 3)
   TEXT: Three findings settle it:
   ITEMS: **The claim is false.** §1.1 item 1. | **It inverts a real implication.** `lib/visibility/scopeTile | **It is reaching for something true.** LEAD *does* confer st

MISMATCH docs/superpowers/specs/2026-08-03-lead-capability-prose-settle-design.md:293 claim=Three(3) noun=sweeps list@295 count=2(raw 2)
   TEXT: **The methodological finding, which is the row's most valuable content.** Three hand-run sweeps under-counted this class, and each failure was a *pattern* failure rather than an effort failure:
   ITEMS: Sweeping `e2e|E2E` misses claims phrased "the audit suite",  | Scoping the sweep to files the branch already had reason to 

PARITY docs/superpowers/specs/2026-08-03-modal-freshness-cue.md:14 claim=two(2) noun=legs list@16 count=2(raw 2)
   TEXT: This spec adds two legs, both one-shot and both scoped to the modal:
   ITEMS: **Visual.** A flash-then-fade cue on the panel card of every | **Announcement.** An sr-only `role="status"` region naming w

PARITY docs/superpowers/specs/2026-08-03-modal-freshness-cue.md:97 claim=Six(6) noun=findings list@99 count=6(raw 6)
   TEXT: Six findings, each of which decides something below:
   ITEMS: **P2 answers the brief's open-time question directly.** The  | **P2b, corrected and narrowed by the round-1 review.** The a | **P3** is the target behaviour: a single edited field lights | **P4 is the one that keeps the cue honest.** `shows.last_che | **P5 sizes the strobe risk empirically: every one of the ren | **P6** shows a lifecycle flip is not section content. Publis

MISMATCH docs/superpowers/specs/2026-08-03-modal-freshness-cue.md:160 claim=Three(3) noun=mechanisms list@162 count=4(raw 4)
   TEXT: the third one closed the class structurally rather than naming another field.** Three mechanisms, each of which turns "a reviewer notices" into "a test fails":
   ITEMS: **Resolvers and derivations are IMPORTED, never re-typed — a | **Caps are IMPORTED, never re-typed.** Every list body slice | **Render normalization runs through the SHIPPED function.**  | **Auxiliary models are narrowed to their rendered fields, at

PARITY docs/superpowers/specs/2026-08-03-modal-freshness-cue.md:339 claim=Three(3) noun=consequences list@341 count=3(raw 3)
   TEXT: Three consequences of that choice, each verified rather than assumed:
   ITEMS: An outline draws OUTSIDE the border box and is not clipped b | `outline-color` is an animatable property, which is why the  | The attribute rule is UNLAYERED, like the two flash blocks i

PARITY docs/superpowers/specs/2026-08-03-modal-freshness-cue.md:697 claim=Two(2) noun=cases list@699 count=2(raw 2)
   TEXT: run line is already wired into `.github/workflows/published-modal-e2e.yml:149`. Two cases are added to that spec, so again no new wiring:
   ITEMS: **E1** a write that changes one section, then a broadcast: t | **E2** a broadcast with no content change: no card ever carr

PARITY docs/superpowers/specs/2026-08-03-nojs-loading-shell-notice-design.md:188 claim=Two(2) noun=consequences list@190 count=2(raw 2)
   TEXT: Two consequences, both load-bearing:
   ITEMS: **The component test must render through `react-dom/server`. | **No existing jsdom test can see the notice**, which is why 

MISMATCH docs/superpowers/specs/2026-08-03-nojs-loading-shell-notice-design.md:263 claim=three(3) noun=steps list@265 count=2(raw 2)
   TEXT: **Registration is three steps, and the second is the whole point of the test.**
   ITEMS: `playwright.config.ts:78` — add the spec to the `desktop-chr | `.github/workflows/admin-layout-e2e.yml` — add it to that jo

PARITY docs/superpowers/specs/2026-08-03-picker-signin-flow-cluster-design.md:560 claim=three(3) noun=things list@562 count=3(raw 3)
   TEXT: That commit does three things:
   ITEMS: **Graduate two entries** to `BACKLOG-archive.md` — `BL-PICKE | **Amend, not graduate, `BL-PICKER-CLEANUP-REVALIDATE-QUERY-V | **Correct a dangling citation.** `BL-PICKER-CLAIMED-ROW-PEND

MISMATCH docs/superpowers/specs/2026-08-03-pre-review-gate-arms-design.md:654 claim=six(6) noun=mutants list@656 count=7(raw 7)
   TEXT: he dispatch refuses (item 4). Seven assertions, because the obvious three leave six live mutants:
   ITEMS: **The sum of the emitted blocks is `<= 200,000` bytes, notic | **The `summary:` line is present in every emitted block, tru | **The retained body lines are a prefix of the untruncated bo | **A request whose reports cannot all be seated at their fram | **The head survives intact in every emitted block** — all th | **The summary is the last line and the notice is immediately | **The notice's arithmetic is truthful**: `N` equals the byte

PARITY docs/superpowers/specs/2026-08-03-scanner-precision-cluster-design.md:274 claim=two(2) noun=arms list@276 count=2(raw 2)
   TEXT: `tests/messages/_metaParseWarningSiteCoverage.test.ts`, two arms:
   ITEMS: **G1 — root coverage.** Runs the shared recognizer over the  | **G2 — completeness.** Asserts every code the recognizer fin

PARITY docs/superpowers/specs/2026-08-03-scanner-precision-cluster-design.md:424 claim=Three(3) noun=conditions list@426 count=3(raw 3)
   TEXT: Three conditions, each closing a measured failure mode:
   ITEMS: **Ledger files only** — a bullet in a plan or a spec is neve | **First child of the first paragraph** — an id mid-sentence  | **Stop at the first heading** — a non-id heading opens a sec

PARITY docs/superpowers/specs/2026-08-03-undo-success-announcement-design.md:277 claim=Two(2) noun=properties list@279 count=2(raw 2)
   TEXT: Two properties make this immune rather than merely further away:
   ITEMS: **The region is always the first child, and `{children}` alw | **The wrapper sits above the layout's own branching.** `app/

PARITY docs/superpowers/specs/2026-08-03-undo-success-announcement-design.md:368 claim=Three(3) noun=properties list@370 count=3(raw 3)
   TEXT: Three independent properties, each checkable rather than argued:
   ITEMS: **The region's node is never replaced after hydration.** Eac | **The state owner outlives every surface that can announce.* | **The async continuation does not depend on its caller survi

PARITY docs/superpowers/specs/2026-08-04-backlog-convergence-design.md:19 claim=Three(3) noun=units list@21 count=3(raw 3)
   TEXT: Three units, shipped in order:
   ITEMS: **Unit A — filing bar + probe-first demotions.** Policy: a l | **Unit B — ledger mass metric.** Repo: `pnpm ledger:mass` (s | **Unit C — small-game sweep.** Close all closable XS/S entri

PARITY docs/superpowers/specs/2026-08-04-backlog-convergence-design.md:92 claim=two(2) noun=things list@94 count=2(raw 2)
   TEXT:  next number rather than trusting this spec's snapshot. The mass revision ships two things:
   ITEMS: **Grammar alignment.** Rev 14's entry recognizer requires an | **Mass display.** A weighted-mass figure beside each queue's

PARITY docs/superpowers/specs/2026-08-04-backlog-convergence-design.md:133 claim=two(2) noun=decisions list@135 count=2(raw 2)
   TEXT: Batch 2 (2026-08-04, after review R1 surfaced two further embedded decisions):
   ITEMS: **BL-TASKCONTRACT-SORT-COMPARATOR-EQUALKEY → CLOSE.** Make t | **BL-X5-ROLE-TOKEN-DECIDED-BY-BOUNDARY → CLOSE (dedicated br

PARITY docs/superpowers/specs/2026-08-04-guard-premise-reachability-design.md:48 claim=Two(2) noun=blockers list@50 count=2(raw 2)
   TEXT: **Enrollment does not cost one registry row today.** Two blockers, neither in that spec's documented-limits table (§7):
   ITEMS: `tests/mutation/source/mutantOverlay.config.ts` declares no  | The same config declares no `testTimeout`. The root sets 30 

PARITY docs/superpowers/specs/2026-08-04-guard-premise-reachability-design.md:165 claim=three(3) noun=ways list@167 count=3(raw 3)
   TEXT: Every survivor is dispositioned exactly one of three ways, and the count of each is stated in the plan:
   ITEMS: **killed** — a new test in the surface's own suite, which is | **`equivalent`** — an argument that the mutant cannot change | **`accepted-gap`** — a real gap, deliberately not repaid her

PARITY docs/superpowers/specs/2026-08-04-guard-premise-reachability-design.md:222 claim=Three(3) noun=terms list@224 count=3(raw 3)
   TEXT: Three terms, defined so the checker has no latitude:
   ITEMS: **test** — a call to `it` or `test`, including `.each` / `.s | **reachable body set** — every declaration reachable from th | **exemption** — a `// no-premise: <reason>` line comment ins

PARITY docs/superpowers/specs/2026-08-04-guard-premise-reachability-design.md:287 claim=two(2) noun=modes list@289 count=2(raw 2)
   TEXT:  them is what made §3.3.3 and §4 disagree** (spec R7). A recognizer has exactly two failure modes and this spec now names both:
   ITEMS: **Unclassifiable** — the checker *recognizes* a provenance-b | **Undetected** — the checker does not recognize the construc

PARITY docs/superpowers/specs/2026-08-04-guard-premise-reachability-design.md:380 claim=Five(5) noun=assertions list@382 count=5(raw 5)
   TEXT: Five assertions exist solely to keep the checker from reporting green on nothing:
   ITEMS: the enrolled-suite set is non-empty; | the set of tests examined is non-empty; | **per enrolled suite**, the number of tests classified as en | a **fixture matrix over every provenance × form cell** of §3 | an **unclassifiable fixture per form in that closed list** (

MISMATCH docs/superpowers/specs/2026-08-05-m-wave-decisions-brief.md:36 claim=19(19) noun=sites list@37 count=2(raw 2)
   TEXT:  own success flips; AdminAnnounceProvider is the shipped pattern. (~19 sites; class entry.)
   ITEMS: BL-BULK-UNDO-ANNOUNCE-UNMOUNT — instance of the class above  | BL-FRESHNESS-PROJECTION-NARROWING — per projection: byte-ide

MISMATCH docs/superpowers/specs/2026-08-05-m-wave-design.md:52 claim=7(7) noun=entries list@54 count=6(raw 6)
   TEXT: ### §2.2 W-UI (7 entries on `feat/m-wave-ui`, Opus, impeccable dual-gate)
   ITEMS: **BL-ADMIN-BADGE-CONTRAST-TOKEN.** New `@theme` token pair i | **BL-ANNOUNCE-REGION-UNMOUNT-CLASS + BL-BULK-UNDO-ANNOUNCE-U | **BL-FRESHNESS-PROJECTION-NARROWING.** Per the entry's own c | **BL-CREW-UNKNOWN-ASTERISK-TODAY-DATES.** Ratified: suppress | **BL-HELP-UI-LABEL-CROSSWALK-EXACT-MATCH.** Live bug (2026-0 | **BL-RESYNC-REGRESSED-JUMP-LINK.** Ratified: amend + link (§

PARITY docs/superpowers/specs/2026-08-05-m-wave-design.md:82 claim=5(5) noun=entries list@84 count=5(raw 5)
   TEXT: ### §2.3 W-GUARDS (5 entries on `feat/m-wave-guards`)
   ITEMS: **BL-HARNESS-FIXTURE-ENFORCEMENT.** Make `tests/e2e/helpers/ | **BL-HEADER-REACT-RECONCILE-HARNESS.** Hydrated React harnes | **BL-LEDGER-DISCOVERY-FAMILY-SCOPED.** PROBE FIRST (the entr | **BL-E2E-LAYOUT-FIXED-WAIT-RESIDUE.** Replace the three fixe | **BL-CATALOG-PARTITION-WARNING-CLASS.** Add the partition fi

PARITY docs/superpowers/specs/2026-08-05-m-wave-design.md:91 claim=2(2) noun=entries list@93 count=2(raw 2)
   TEXT: ### §2.4 W-PARITY (2 entries on `feat/m-wave-parity`)
   ITEMS: **BL-VALIDATION-PARITY-FUNCTIONS-UNCHECKED.** Ratified: sign | **BL-FONT-STYLESHEET-GRAPH-FIDELITY.** Ratified: built artif

MISMATCH docs/superpowers/specs/2026-08-06-arc-b-review-infra.md:34 claim=Two(2) noun=numbers list@36 count=9(raw 9)
   TEXT: es the extractor cannot place are REPORTED as unplaced, never silently skipped. Two consequences the numbers force:
   ITEMS: **A legacy baseline ratchet, not 300+ waivers.** The corpus' | **Per-rule accept-sets, keyed on structure (the entry's own  | **Fence eligibility (what is "code"), pinned exactly (R1 F4) | **Attribution (which file a fence appends to), pinned exactl | **`UNIMPORTED_IDENTIFIER`:** fires only on module-shaped fen | **`DUPLICATE_IMPORT`:** the same IMPORTED binding (import st | **`MANGLED_TEMPLATE`:** markdown-escape artifacts inside cod | **`UNCHECKED_INDEX`:** the pattern is PINNED HERE (R1 F4), n | **`FENCE_EM_DASH`:** the canonical hard em-dash CLASS inside

PARITY docs/superpowers/specs/2026-08-07-classname-array-join-cn.md:133 claim=Two(2) noun=consequences list@135 count=2(raw 2)
   TEXT: Two consequences, both load-bearing:
   ITEMS: Every one of the sites becomes visible, because at every sit | Identifier arguments stay dark. That is §9.2 / R6 — a pre-ex

PARITY docs/superpowers/specs/2026-08-07-projection-financials-viewer-independent-design.md:17 claim=Two(2) noun=consequences list@19 count=2(raw 2)
   TEXT: inancials() : Promise.resolve(undefined)` (`lib/data/getShowForViewer.ts:762`). Two consequences:
   ITEMS: **Raise delay (the BL entry's premise, accepted at v1).** A  | **Resolve defect (post-entry regression, found this arc).** 

PARITY docs/superpowers/specs/2026-08-07-step3-a11y-cluster.md:20 claim=two(2) noun=shapes list@22 count=2(raw 2)
   TEXT: out of the branches that found them. They share two root shapes:
   ITEMS: **Interactive targets below the project's 44×44px floor** (` | **A heading level skipped** on the Step-3 page: `h1` followe

PARITY docs/superpowers/specs/2026-08-09-crew-chrome-wizard-connector.md:7 claim=Three(3) noun=entries list@9 count=3(raw 3)
   TEXT: I half of the quick-wins-2 pass (mech half: `2026-08-09-quick-wins-2-mech.md`). Three claimed entries (invariant 12, marked on `docs/quick-wins-2-specs`):
   ITEMS: `BL-CREW-FOOTER-OBSCURED-BY-FIXED-BOTTOM-BAR` (MEDIUM) — arc | `BL-CREW-FOOTER-NOT-ANCHORED-SHORT-CONTENT` — archive on §2  | `BL-WIZARD-CONNECTOR-MAXW-INERT` — archive on §3.

MISMATCH docs/superpowers/specs/2026-08-09-help-report-surface-design.md:66 claim=two(2) noun=sites list@68 count=3(raw 3)
   TEXT: ### §2.3 Route — `app/api/report/route.ts` (two sites)
   ITEMS: `app/api/report/route.ts:36-41` null-show allowance: `if (bo | **Non-show invariant enforced server-side (R2 F2 + R3 F2):** | `app/api/report/route.ts:121` auth branch: `if (body.surface

PARITY docs/superpowers/specs/2026-08-09-quick-wins-2-mech.md:42 claim=Three(3) noun=parts list@44 count=3(raw 3)
   TEXT: Three parts, per the entry's Work section:
   ITEMS: **Sweep.** Every class-string arrow use of a declared `@them | **Truthful comment — stating the MEASURED mechanism, not the | **Derived guard — an arrow-form ban over declared `@theme` t

PARITY docs/superpowers/specs/admin/2026-06-30-admin-shows-status-column-design.md:111 claim=two(2) noun=elements list@113 count=2(raw 2)
   TEXT: `getByTestId` never matches two elements:
   ITEMS: **Inline pill** — `<StatePill place="inline" />` at the exis | **Column pill** — `<StatePill place="column" />` in the new 

MISMATCH docs/superpowers/specs/admin/2026-07-01-shows-720-title-floor-design.md:38 claim=7(7) noun=lines list@40 count=5(raw 5)
   TEXT: `min-[720px]:` class utilities** (across 7 lines; the `min-[960px]` occurrences stay):
   ITEMS: `ROW_GRID` (`ShowsTable.tsx:65`): `min-[720px]:grid`, `min-[ | Mobile sub-line (`ShowsTable.tsx:447`): `min-[720px]:hidden` | Dates / Crew / Sync cells (`ShowsTable.tsx:461,465,470`): `m | Chevron cell (`ShowsTable.tsx:485`): `min-[720px]:block` → ` | Update the `ROW_GRID` comment block (`ShowsTable.tsx:58-64`)

PARITY docs/superpowers/specs/admin/2026-07-03-developer-tier.md:32 claim=Two(2) noun=postures list@33 count=2(raw 2)
   TEXT: 4. **Two postures, deliberately different:**
   ITEMS: **Access** gates (`requireDeveloper`/`requireDeveloperIdenti | **Visibility** gates use a boolean `isCurrentUserDeveloper()

MISMATCH docs/superpowers/specs/admin/2026-07-03-developer-tier.md:281 claim=three(3) noun=things list@283 count=4(raw 4)
   TEXT: sture ∈ {boundary-500 | route-500-json | inline-typed-exception}`). It enforces three things:
   ITEMS: **Server-action gate coverage (AST, the R5 gap).** Using `ts | **Route/page coverage.** The route/page surfaces (dev page,  | **Error-posture completeness.** Every surface has a `declare | **Mutation-RPC authorization guard (Codex spec R9+R10).** Sc

PARITY docs/superpowers/specs/admin/2026-07-07-admin-field-overrides.md:154 claim=four(4) noun=phases list@155 count=4(raw 4)
   TEXT:  the **final** display names are all distinct; the writes reach that state in **four phases**, each individually constraint-safe:
   ITEMS: **DELETE** every genuine-removal row by id (frees the names  | **Park** every *surviving row whose name changes* (override  | **INSERT** every next-only (new) member at its final name —  | **Assign finals:** `UPDATE crew_members SET name = <finalDis

PARITY docs/superpowers/specs/admin/2026-07-07-admin-field-overrides.md:386 claim=two(2) noun=paths list@388 count=2(raw 2)
   TEXT: The two paths must **not** share a live-row updater, and — critically — the sync crew path must **not** use the old name-keyed delete/
   ITEMS: **Sync path.** `show`/`hotel` overrides = a **pure ParseResu | **RPC path = DB immediate-apply.** `applyOverrideToLiveRow(t

MISMATCH docs/superpowers/specs/admin/2026-07-15-extend-role-scope-vocab.md:247 claim=three(3) noun=rounds list@249 count=10(raw 10)
   TEXT: ). This block is the COMPLETE end-to-end telemetry contract (consolidated after three same-vector rounds — R5 F3, R7 F1, R8 F2/F4/F5 — per the AGENTS.md comprehensive re-analysis rule):
   ITEMS: **Overlay** (§6, pure, gate-free): returns `applied[]` with  | **Gate (computed inside `phase2`):** emission-worthiness is  | **Args:** `Phase2Args` gains TWO explicit fields: `roleToken | **Result:** `Phase2Result.appliedRoleMappings` carries POST- | **Emission:** every `Phase2Result`-consuming commit surface  | **Context:** `{ token, grants, newMemberCount }`. Catalog do | **Steady-state silence + lifecycle tests (§13):** unchanged- | §12.4 prose row in `docs/superpowers/specs/2026-04-30-fxav-c | Additional gates (per the new-code checklist): `pnpm gen:int | dougFacing is the name-free string in point 6 above (superse

MISMATCH docs/superpowers/specs/admin/2026-07-16-consolidated-admin-show-page.md:367 claim=two(2) noun=phases list@369 count=0(raw 2)
   TEXT: Single spec, two plan phases:
   ITEMS: 

PARITY docs/superpowers/specs/admin/2026-07-16-consolidated-admin-show-page.md:387 claim=Three(3) noun=relocations list@389 count=3(raw 3)
   TEXT: Three relocations have since hollowed out the Overview rail section described in §5.1/§6:
   ITEMS: **Re-sync control → StatusStrip** (modal-header-reconciliati | **Share & access cluster → the status band's ShareHub popove | **This amendment:** the standalone **open-sheet link** is de

MISMATCH docs/superpowers/specs/admin/2026-07-16-destructive-confirm-pass.md:7 claim=Four(4) noun=idioms list@9 count=5(raw 5)
   TEXT: Four hand-rolled confirm idioms coexist with no shared recipe, and several destructive controls have no guard at all:
   ITEMS: **No danger visual language.** The destructive confirm-go bu | **Unguarded irreversible one-taps.** "Permanently ignore" (` | **Inverted focus bug.** `CleanupAbandonedFinalizeButton.tsx: | **FLOW4-4:** `RecentAutoAppliedStrip.tsx` `confirmUndoAll` ( | **FLOW4-6:** `confirmUndoAll` unmounts the confirm panel whi

MISMATCH docs/superpowers/specs/admin/2026-07-16-role-vocab-settings-desktop-grid.md:38 claim=two(2) noun=layouts list@40 count=8(raw 8)
   TEXT:  or a duplicated edit panel would double the state surface and drift). One DOM, two layouts:
   ITEMS: The `<li>` (`RoleMappingRow.tsx:133-136`) keeps `flex flex-c | The mobile header wrapper (`RoleMappingRow.tsx:137`, token + | Explicit cell placement at ≥760px (DOM order is token, meta, | Full-width sub-rows at ≥760px via `min-[760px]:col-span-4` ( | Edit/confirm mode note: at ≥760px, when `mode !== "view"` th | Edit-label swap inside the SAME button (`:170-172`): the but | `page.tsx:30`: `max-w-2xl` → `max-w-3xl`. | `RolesSettingsView.tsx` is unchanged (list `ul`, empty state

PARITY docs/superpowers/specs/admin/2026-07-17-casp2-finalize-inflow-chip.md:97 claim=two(2) noun=assertions list@98 count=2(raw 2)
   TEXT: ze share the EXACT positioning class set") is now false by design. Replace with two assertions:
   ITEMS: **Error skin** still carries every `POPOVER_POSITION` token  | **Finalize chip** carries **none** of `{absolute, inset-x-0,

MISMATCH docs/superpowers/specs/admin/2026-07-17-destruct1-armed-reflow.md:34 claim=two(2) noun=buttons list@36 count=3(raw 3)
   TEXT: Make the two discard buttons **stack full-width on narrow viewports** so the armed morph happens inside a position-stable box:
   ITEMS: Add `basis-full sm:basis-auto` to **both** buttons in the ex | Effect `< sm` (viewport < 640px, Tailwind v4 default `sm` br | Effect `≥ sm`: `basis-auto` restores content-width sizing; t

PARITY docs/superpowers/specs/admin/2026-07-17-devtier-toggle-help.md:42 claim=Two(2) noun=gates list@44 count=2(raw 2)
   TEXT: The extended copy renders **only for developers**. Two independent gates guarantee it:
   ITEMS: The sentence lives in the `viewerIsDeveloper ? … : …` **deve | The Developer toggle it describes is itself developer-only (

PARITY docs/superpowers/specs/admin/2026-07-17-sync-cell-edited-checked.md:12 claim=Two(2) noun=problems list@14 count=2(raw 2)
   TEXT: ucket, renders `Synced {relative}` where the relative time is `last_synced_at`. Two problems:
   ITEMS: `Synced {relative}` reads as "we last checked N ago," but it | There is no per-show signal for "when did the cron last succ

PARITY docs/superpowers/specs/admin/2026-07-17-sync-cell-edited-checked.md:134 claim=two(2) noun=places list@136 count=2(raw 2)
   TEXT: `SyncCell` is a single component rendered in two places:
   ITEMS: **Desktop** (`≥768px`): inside the `shows-sync-{slug}` grid  | **Mobile** (`<768px`): inside the stacked meta sub-line (`Sh

PARITY docs/superpowers/specs/admin/2026-07-25-destruct-thumb-order-drift-guard.md:135 claim=Two(2) noun=edits list@137 count=2(raw 2)
   TEXT: Two edits to `components/admin/PendingPanelDiscardButtons.tsx`:
   ITEMS: **Swap the DOM order** so Ignore precedes Defer inside the e | **Delete `basis-full sm:basis-auto`** from both buttons, so 

PARITY docs/superpowers/specs/admin/2026-07-25-destruct-thumb-order-drift-guard.md:263 claim=Two(2) noun=adjustments list@265 count=2(raw 2)
   TEXT: Existing tests keep their assertions; only the armed-label string changes. Two adjustments were required by the reorder, both real rather than cosmetic:
   ITEMS: The persistent-status test reached the live region via `btn. | The DESTRUCT-1 class test asserted `basis-full sm:basis-auto

PARITY docs/superpowers/specs/alerts/2026-07-03-admin-alert-auto-resolution.md:248 claim=Two(2) noun=triggers list@250 count=2(raw 2)
   TEXT: Two triggers, both global-scoped (`show_id IS NULL`, matching the raise):
   ITEMS: **Verified delivery:** in the webhook route, once the delive | **Stale channel:** in the watch-reconcile paths that already

PARITY docs/superpowers/specs/alerts/2026-07-03-route-sync-problems-to-needs-attention.md:25 claim=three(3) noun=layers list@27 count=3(raw 3)
   TEXT: ill-degraded show through the product's own surfaces. It is enforced at exactly three layers, all app-level:
   ITEMS: **UI:** the per-show alert row renders **read-only** for the | **API routes:** the show-scoped and global alert-resolve rou | **Shared helper:** `resolveAdminAlert(s)` **rejects** these 

MISMATCH docs/superpowers/specs/alerts/2026-07-03-route-sync-problems-to-needs-attention.md:121 claim=two(2) noun=streams list@123 count=4(raw 4)
   TEXT: Alongside the two pending streams (each await wrapped per invariant 9, registered in `tests/admin/_metaInfraContract.test.ts`), add:
   ITEMS: **Empty-set short-circuit:** if `INBOX_ROUTED_CODES.length = | **Rows:** `admin_alerts` select `id, code, raised_at, show_i | **Head-count:** `admin_alerts` `select("id, shows!inner(id)" | Map rows → `NeedsAttentionSyncProblemInput` (extract `contex

PARITY docs/superpowers/specs/alerts/2026-07-04-alert-at-a-glance-identity.md:206 claim=three(3) noun=codes list@207 count=3(raw 3)
   TEXT: **Producer changes required: three alert codes** (resolves Codex F13 + F18 + F20) —
   ITEMS: `OAUTH_IDENTITY_CLAIMED` (`app/auth/callback/route.ts`) gain | `PICKER_BOOTSTRAP_RPC_FAILED` (`app/api/auth/picker-bootstra | `WIZARD_SESSION_SUPERSEDED_RACE` — **all four** emitters (re

MISMATCH docs/superpowers/specs/alerts/2026-07-04-alert-audience-split.md:384 claim=two(2) noun=counts list@385 count=4(raw 4)
   TEXT:  paths is wrong on two counts (R5 findings 1 + 3):
   ITEMS: **Authorization (R5 finding 1):** `resolveAdminAlertFormActi | **Navigation (R5 finding 3):** the per-show route | **Both global and show-scoped** health rows resolve through  | **Call-boundary contract (R8 finding 2), mirroring `resolveA

PARITY docs/superpowers/specs/alerts/2026-07-04-alert-audience-split.md:471 claim=three(3) noun=surfaces list@473 count=3(raw 3)
   TEXT: The three surfaces to guard (each already reads or can cheaply read the target row's code):
   ITEMS: `resolveAdminAlertFormAction` (`app/admin/actions.ts:43`) —  | `app/api/admin/admin-alerts/[id]/resolve/route.ts` — the uni | `app/api/admin/show/[slug]/alerts/[id]/resolve/route.ts` — s

MISMATCH docs/superpowers/specs/alerts/2026-07-16-notify-email-batching.md:88 claim=two(2) noun=files list@90 count=3(raw 3)
   TEXT: **N≥2 batch templates** (new functions in the same two template files):
   ITEMS: `published` (subject em-dash-free, count first): | `sync_problems`: | `stuck_files`:

PARITY docs/superpowers/specs/alerts/2026-07-17-role-flags-notice-lead-only-doug.md:15 claim=three(3) noun=incoherences list@17 count=3(raw 3)
   TEXT: -split sweep. That classification, applied to a Doug-directed message, produces three incoherences (all catalog-derived, no per-code hardcoding):
   ITEMS: **Not dismissible.** Both resolve routes 403 any health code | **Renders red critical.** `rowTone` (`BellPanel.tsx:128-132` | **Dev-directed action.** `isHealth` rows render the "View in

PARITY docs/superpowers/specs/alerts/2026-07-24-alert-autoresolve-tile-and-report-family.md:308 claim=Two(2) noun=defects list@310 count=2(raw 2)
   TEXT: repair exists.** Two independent defects:
   ITEMS: **Cross-clock comparison.** `observedAt` would come from the | **Request start is the wrong instant.** `observedAt` is capt

PARITY docs/superpowers/specs/ci/2026-06-26-ci-speedup-pr-e-design.md:12 claim=Two(2) noun=inefficiencies list@14 count=2(raw 2)
   TEXT: .1–8.8m) is the **sole** merge-critical check (quality 1.9m, x-audits all <1m). Two measured inefficiencies in the critical path:
   ITEMS: **The two shard legs are persistently imbalanced (~2 min).** | **`validation-report-fixtures.test.ts` (~76s) spawns 42–66 `

PARITY docs/superpowers/specs/ci/2026-07-06-mutation-harness-sharding.md:74 claim=three(3) noun=changes list@76 count=3(raw 3)
   TEXT: Identical classification logic with three changes:
   ITEMS: **Slice filter:** computes `const A = computeShardAssignment | **Async with periodic yields (Codex spec-R1 #1):** a fully s | **Progress logging:** at each 5,000-mutant yield and at comp

PARITY docs/superpowers/specs/ci/2026-07-06-mutation-testing-harness.md:62 claim=Two(2) noun=predicates list@64 count=2(raw 2)
   TEXT: Two more predicates are needed so an unchanged payload does not mask a **loss of observability** (a mutation that silently *remove
   ITEMS: **`signalEq(baseline, mutant)`** ≙ the **full** signal chann | **`newSignalFired(baseline, mutant)`** ≙ `∃ key : mutant.cou

MISMATCH docs/superpowers/specs/ci/2026-07-06-mutation-testing-harness.md:83 claim=six(6) noun=operators list@85 count=7(raw 7)
   TEXT:  like any corrupting operator. Only `trailing-whitespace` remains cosmetic. The six audit-named corrupting operators:
   ITEMS: **`header-typo`** — apply a single Damerau edit (adjacent-ch | **`ref-sub`** — replace one non-empty table body-cell value  | **`unicode-inject`** — insert a zero-width non-joiner (U+200 | **`column-shift`** — prepend an empty leading column (`| x | | **`blank-row`** — two sub-variants: **`blank-row:inject`** i | **`merged-cell`** — delete one interior pipe of a body row w | **`section-reorder`** (RECLASSIFIED CORRUPTING 2026-07-06, w

PARITY docs/superpowers/specs/ci/2026-07-06-mutation-testing-harness.md:149 claim=2(2) noun=pipes list@151 count=2(raw 2)
   TEXT:  | `merged-cell` | data row | a **data row** with ≥3 cells (≥2 interior pipes) |
   ITEMS: **`domains(site)` — one uniform helper covering intra-sectio | `floorEligible(O) = { D ∈ risk-critical : ∃ O-applicable sit

PARITY docs/superpowers/specs/ci/2026-07-06-mutation-testing-harness.md:178 claim=two(2) noun=components list@179 count=2(raw 2)
   TEXT: over the mutant-vs-baseline diff of a **static committed fixture**, composed of two redacted diff components:
   ITEMS: **Payload diff:** per changed payload JSON-path (sorted by p | **Full signal-object diff — aligned exactly to `signalEq` (C

MISMATCH docs/superpowers/specs/ci/2026-07-19-ci-unit-suite-under-5min.md:27 claim=3(3) noun=commitments list@29 count=0(raw 3)
   TEXT: ## 2. Program overview (phases 2–3 are commitments, not designs)
   ITEMS: 

MISMATCH docs/superpowers/specs/ci/2026-07-19-ci-unit-suite-under-5min.md:95 claim=three(3) noun=steps list@97 count=7(raw 7)
   TEXT: Pull is ~38s of the 71s boot. Mechanism (three new steps around the boot step):
   ITEMS: **Restore** (`actions/cache@v4`, before boot, **`id: supabas | **Load** (plain step, cache-hit only via `if: steps.supabase | **Save-prep** (after the vitest step, miss-only via `if: ste | The bootstrap script itself is **unchanged**; `supabase star | **Soft-failure inventory (exhaustive):** exactly two `|| tru | **Cache-save timing caveat:** a cache entry is uploaded only | The keep-or-revert decision and its measurement definition l

PARITY docs/superpowers/specs/ci/2026-07-19-ci-unit-suite-under-5min.md:109 claim=Two(2) noun=shapes list@111 count=2(raw 2)
   TEXT: numeration, disk, docker save, zstd) costs only the cache entry, never the leg. Two shapes are load-bearing:
   ITEMS: **`set -o pipefail`** — GitHub's default `run:` shell is `ba | **tmp-then-`mv`** — a save that dies mid-stream must not lea

MISMATCH docs/superpowers/specs/ci/2026-07-19-ci-unit-suite-under-5min.md:129 claim=two(2) noun=meta-tests list@131 count=4(raw 4)
   TEXT: This phase **extends** two existing structural meta-tests; it creates none:
   ITEMS: `tests/cross-cutting/unit-suite-shard-topology.test.ts` — | `tests/cross-cutting/vitest-shard-balance.test.ts` — N loops | `tests/cross-cutting/vitest-shard-balance.test.ts` file-set  | Unaffected and must stay green: `tests/cross-cutting/vitest-

PARITY docs/superpowers/specs/ci/2026-07-20-serial-parallel-reclassification-retrospective.md:84 claim=two(2) noun=bugs list@86 count=2(raw 2)
   TEXT: did exactly their job and caught two real classification bugs before merge:
   ITEMS: `tests/scripts/validation-report-fixtures.test.ts` reaches p | `tests/cross-cutting/auth-chain-audit.test.ts` is a concurre

PARITY docs/superpowers/specs/ci/2026-07-20-unit-suite-project-split.md:72 claim=two(2) noun=invariants list@74 count=2(raw 2)
   TEXT: The two invariants:
   ITEMS: `vitest-projects-partition.test.ts` — every non-nightly test | `unit-suite-shard-topology.test.ts` — each job runs exactly 

PARITY docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:18 claim=Three(3) noun=mechanisms list@20 count=3(raw 3)
   TEXT: A test that no workflow invokes is not coverage. It is a file that once passed. Three independent mechanisms put a suite in that state here, and each has produced a live defect:
   ITEMS: **A Playwright config nothing invokes.** `tests/e2e/standalo | **A workflow run-list nothing added to.** `tests/e2e/admin-l | **A CI-conditional vitest exclusion nobody watches.** `tests

PARITY docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:72 claim=Three(3) noun=counts list@74 count=3(raw 3)
   TEXT: Three counts that are easy to conflate, so state them separately:
   ITEMS: **15** specs go dark → covered (16 dark branches, less `pack | **29** allowlist rows are deleted — the dark ones plus those | **`packlist-rescan-recovery` keeps its row**, correctly: aft

PARITY docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:326 claim=Two(2) noun=assertions list@328 count=2(raw 2)
   TEXT: Two rotted assertions:
   ITEMS: `tests/e2e/attention-modal-gallery.spec.ts:398` — a substrin | `tests/e2e/attention-modal-gallery.spec.ts:265` — `toHaveCou

MISMATCH docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:380 claim=three(3) noun=rounds list@382 count=5(raw 5)
   TEXT: lled. It was descoped because its safety *guarantee* could not be made sound in three rounds:
   ITEMS: A proxy can be consumed **without being invoked**: `flags.co | A strict throw-on-any-read stub fixes that for 4 of 5 probed | Path rules overmatch, with two named instances: `lib/drive/d | A packages-and-builtins-only rule set has no overmatch surfa | A sentinel-based guard detects only preselected sentinels, s

PARITY docs/superpowers/specs/ci/2026-07-26-ci-dark-coverage-design.md:442 claim=two(2) noun=findings list@444 count=2(raw 2)
   TEXT: rsarial pass**, recorded here rather than presented as an approval. It produced two findings, both applied above:
   ITEMS: The exact-string `--config` rule needed an explicit statemen | "The retiring workflows lose no coverage" was an assertion. 

PARITY docs/superpowers/specs/ci/2026-07-26-ci-dark-descoped-closeout-design.md:548 claim=two(2) noun=entries list@550 count=2(raw 2)
   TEXT: ### §6.2 Dispositions for the two entries
   ITEMS: `tests/admin/test-auth-gate.test.ts` — **primary: delete its | `tests/cross-cutting/email-canonicalization.test.ts` — a ded

PARITY docs/superpowers/specs/ci/2026-08-01-ci-cross-step-env-guard-design.md:168 claim=four(4) noun=findings list@170 count=4(raw 4)
   TEXT: **R1 (Codex, 2026-08-01, VERDICT: BLOCKING; all four findings probe-backed and ACCEPTED):**
   ITEMS: **BLOCKING — map-presence treated as "modeled."** Two live e | **NEEDS-ATTENTION — probe semantics.** `PATH=/fake:$PATH` as | **NEEDS-ATTENTION — uniform fail-closed claim vs L3.** Stati | **NIT — §2.0 cross-reference** said L6 where the asymmetry l

PARITY docs/superpowers/specs/ci/2026-08-02-ci-boot-overlap-implementation.md:8 claim=three(3) noun=things list@10 count=3(raw 3)
   TEXT:  accept gate are RATIFIED and are not reopened here. This spec exists to do the three things that document says must happen before it ships, and nothing else:
   ITEMS: redo the write-surface audit against the real `allowBuilds`  | reconcile the design against the workflow topology as it exi | state the meta-test inventory and accept criteria against th

MISMATCH docs/superpowers/specs/ci/2026-08-02-ci-static-env-injection-design.md:240 claim=2(2) noun=as list@242 count=1(raw 1)
   TEXT: tants demonstrated against the SHIPPED guard, ACCEPTED; every round-1 and round-2 repair re-verified as still closing its mutant):**
   ITEMS: **P1 — the fixture suite never pinned traversal past the FIR

PARITY docs/superpowers/specs/ci/2026-08-04-review-round-economy.md:59 claim=Three(3) noun=consequences list@61 count=3(raw 3)
   TEXT: Three consequences, all load-bearing:
   ITEMS: **This spec never infers a finding shape.** Everything the r | **The existing hook's finding tally is 48% accurate.** `$HOM | **The wrapper silently drops bold verdict lines.** `parseVer

PARITY docs/superpowers/specs/ci/2026-08-04-review-round-economy.md:272 claim=Two(2) noun=constructs list@274 count=2(raw 2)
   TEXT: RDICT:` line inside an inline code span on a prose line is still read as prose. Two block constructs are recognized only incidentally:
   ITEMS: **Link reference definitions and setext headings are not par | **Block-quote containers are modelled, but the gap they clos

PARITY docs/superpowers/specs/ci/2026-08-04-review-round-economy.md:431 claim=Two(2) noun=layers list@433 count=2(raw 2)
   TEXT: Two more layers, both from findings this section already earned:
   ITEMS: **Adoption boundary.** Fixture with merges before and after  | **`stage: "task"` never obliges.** Fixture arc with four `ta

PARITY docs/superpowers/specs/ci/2026-08-04-source-mutation-guard-gate.md:297 claim=Two(2) noun=cases list@299 count=2(raw 2)
   TEXT: Two cases, because they are the thesis in miniature — a test that exists, is well named, and pins strictly less than its name clai
   ITEMS: **`tests/specLint/taskContract.test.ts:106`** — `it("M36/AC- | **`tests/specLint/taskContract.test.ts:56`** — `it("M27/AC-3

PARITY docs/superpowers/specs/ci/2026-08-09-app-e2e-batch1-design.md:55 claim=Two(2) noun=consequences list@56 count=2(raw 2)
   TEXT: wCoverageScan.ts`, `ENV_KEY_ALLOWLIST`) — the scan fails closed on novel pairs. Two registry consequences, both landed in the same commit as the workflow (spec R1 finding 1):
   ITEMS: **`governs` extension on every reused pair.** Each allowlist | **One NEW value row.** The step-level `PLAYWRIGHT_JSON_OUTPU

PARITY docs/superpowers/specs/ci/2026-08-09-libdata-call-boundary-metatest-design.md:69 claim=Three(3) noun=consequences list@71 count=3(raw 3)
   TEXT: Three consequences worth stating, because they are visible in the shipped self-tests:
   ITEMS: **The literal is the COOKED value** (`arg.text`) — the name  | **`isStringLiteralLike` draws the static/dynamic line for us | **Two limits are gone**, not narrowed: generic segments cont

MISMATCH docs/superpowers/specs/ci/2026-08-09-libdata-call-boundary-metatest-design.md:130 claim=17(17) noun=sites list@132 count=4(raw 4)
   TEXT: Registry contents (all 17 sites; the plan enumerates the exact pin regexes, typechecked):
   ITEMS: `lib/data/listShowsForCrew.ts` — 2 pin rows: `/const\s+\{\s* | `lib/data/loadShowShareToken.ts` — 1 pin row: the try/catch  | `lib/data/getShowForViewer.ts` — 10 pin rows: per-site resul | `lib/data/adminEmails.ts` — 4 `coveredBy` rows: the `admin_e

PARITY docs/superpowers/specs/ci/2026-08-09-resurrect-mobile-safari-e2e-design.md:69 claim=two(2) noun=classes list@70 count=2(raw 2)
   TEXT:  seed. Plus SIX statically-skipped blocks in two distinct classes:
   ITEMS: **Five pre-redesign tile blocks** (`test.describe.skip`, lin | **One §4.10 transition-audit block** (`test.describe .skip`,

PARITY docs/superpowers/specs/ci/2026-08-09-resurrect-mobile-safari-e2e-design.md:252 claim=two(2) noun=residues list@254 count=2(raw 2)
   TEXT: ### 3.3 Close the two cheap residues in-arc
   ITEMS: **pack-list residue:** one component test in `tests/componen | **layout-dimensions residue:** one e2e test appended to the 

PARITY docs/superpowers/specs/crew/2026-07-06-venue-card-redesign-design.md:90 claim=three(3) noun=layers list@91 count=3(raw 3)
   TEXT: Render decision (pure, deterministic — always all three layers, no branching state):
   ITEMS: **Fallback tile (base layer, always painted)** — the mock's  | **Map image (overlay)** — an `<img>` whose `src` is our same | **Directions affordance (top overlay)** — when `mapHref` is 

PARITY docs/superpowers/specs/crew/2026-07-09-flow8-self-serve-trio.md:22 claim=Three(3) noun=gaps list@24 count=3(raw 3)
   TEXT: n't see my stuff" path lands on a guided affordance, and no fail-open remains._ Three gaps stand between today's crew self-serve surface and that bar:
   ITEMS: **8.2 (fail-open — security):** `resolveViewerContext` (`lib | **8.1 (picker hardening — UI):** `_PickerInterstitial.tsx` r | **Transport visibility (8.4) — entirely out of scope for thi

PARITY docs/superpowers/specs/crew/2026-07-09-flow8-self-serve-trio.md:174 claim=Two(2) noun=transforms list@175 count=2(raw 2)
   TEXT: Two transforms, order-stable:
   ITEMS: **Sentinel-guard:** drop any row where `shouldHideGenericOpt | **Dedup by id:** keep the first occurrence of each `row.id`;

PARITY docs/superpowers/specs/crew/2026-07-09-flow8.3b-transport-id-resolution.md:316 claim=TWO(2) noun=rows list@317 count=2(raw 2)
   TEXT: getShowForViewer.test.ts` + fixture `tests/fixtures/showForViewer.ts`), seeding TWO crew rows: the **owner** (a name-override row `name="Doug Newname"`, `sheet_name="Doug Larson"`, that is the garbled driver `d
   ITEMS: assert the returned `ShowForViewer` carries `viewerId` and ` | **privacy regression (Codex plan-review R5 + R11b):** assert

PARITY docs/superpowers/specs/crew/2026-07-09-flow8.4-transport-assignee-warning.md:19 claim=two(2) noun=premises list@21 count=2(raw 2)
   TEXT: ew_member` ids at enrich, persist the id set, match visibility by id") rests on two premises the live code invalidates:
   ITEMS: **No crew_member id exists at enrich time.** `ParseResult.cr | **Match-by-id at render is behavior-neutral.** Resolving `dr

PARITY docs/superpowers/specs/crew/2026-07-09-flow8.4-transport-assignee-warning.md:44 claim=Two(2) noun=primitives list@46 count=2(raw 2)
   TEXT:  independently instead of false-warning as an overflow. Classify each sub-name. Two primitives:
   ITEMS: **Significant tokens** of a name = normalized (lowercase, di | **`namesRefer(roster, candidate)`** — the unchanged fuzzy ma

PARITY docs/superpowers/specs/crew/2026-07-16-accent-contrast-token-pass.md:23 claim=Two(2) noun=findings list@25 count=2(raw 2)
   TEXT: Two adjacent telemetry findings ride the same pass by user decision (2026-07-16):
   ITEMS: **TEL-1 (accent dilution):** `components/admin/telemetry/Eve | **TEL-2 (badge escalation):** `components/admin/telemetry/Ev

PARITY docs/superpowers/specs/crew/2026-07-17-cardreport-tap-targets.md:12 claim=Two(2) noun=affordances list@14 count=2(raw 2)
   TEXT: Two affordances sit in every source-backed crew card header `action` slot:
   ITEMS: `SourceLink` (`components/crew/primitives/SourceLink.tsx:40` | `CardReportTrigger` (`components/shared/CardReportTrigger.ts

PARITY docs/superpowers/specs/crew/2026-07-17-cardreport-tap-targets.md:90 claim=two(2) noun=cards list@92 count=2(raw 2)
   TEXT: Add two context cards next to the existing measured cards (the file is a dev-only, build-gated route — absent from production artifact
   ITEMS: **`card-actions-up`** — an interactive `tel:` `KeyValueRows` | **`card-actions-down`** — a replica of the bare schedule hea

PARITY docs/superpowers/specs/crew/2026-07-17-rpc-crew-lifecycle-guard-design.md:104 claim=Three(3) noun=predicates list@106 count=3(raw 3)
   TEXT: he `shows.picker_epoch` column (the M9.5 crew-auth table was dropped — see §4). Three predicates, all computed from the **live catalog**:
   ITEMS: `DIRECT_MUTATOR(f)` — `pg_get_functiondef(f)` contains a DML | `CLIENT_REACHABLE(f)` — `has_function_privilege(role, f, 'EX | `IS_TRIGGER(f)` — `f.prorettype = 'pg_catalog.trigger'::regt

PARITY docs/superpowers/specs/crew/2026-07-17-venue-card-vcr2-vcr3-fixes-design.md:91 claim=three(3) noun=rows list@92 count=3(raw 3)
   TEXT: - **§8 (Transition inventory).** Add three rows:
   ITEMS: `theme unresolved (stripe base only) → theme resolved (<img> | `theme unresolved → resolved, link-only case (query === "" & | `map region: query-backed (<img> + stripe) ↔ mapHref-only (s

MISMATCH docs/superpowers/specs/crew/2026-08-09-crew-wifi-room-enrichment-design.md:7 claim=two(2) noun=graduations list@9 count=4(raw 4)
   TEXT: Two display-time enrichments to the crew page, plus two ledger graduations the corpus probe forced:
   ITEMS: **Wi-Fi SSID/password structured split** — a display-time pa | **Room-within-venue surfacing** — the room name is ALREADY p | **`BL-FLIGHT-LEG-ORIENTATION` graduates as OBSOLETE** — its  | **`BL-CREW-FIELD-ENRICHMENT` graduates on this arc's merge**

PARITY docs/superpowers/specs/crew/2026-08-09-private-image-pipeline-design.md:94 claim=two(2) noun=sites list@96 count=2(raw 2)
   TEXT: **Wiring — two call sites (companion-surface census, exhaustive):**
   ITEMS: `lib/sync/snapshotAssets.ts` — after fingerprint/md5 verific | `lib/sync/assetRecovery.ts` — the recovery path uploads reco

PARITY docs/superpowers/specs/crew/2026-08-09-private-image-pipeline-design.md:102 claim=Two(2) noun=edits list@104 count=2(raw 2)
   TEXT: ts:276`) — so any variant object would trip `manifest_mismatch` before cutover. Two mandated edits:
   ITEMS: **(a) Expected-count SQL counts variants too:** each of the  | **(b) The storage adapter paginates:** `defaultStorage.list`

PARITY docs/superpowers/specs/data-quality/2026-06-29-finalize-approval-decision-race-design.md:13 claim=two(2) noun=holders list@15 count=2(raw 2)
   TEXT: ### 1.1 The two holders of the same `show:` lock
   ITEMS: **finalize:** `selectFinishableCleanRows` runs in the **oute | **approve / unapprove:** both serialize on the **same** `sho

PARITY docs/superpowers/specs/data-quality/2026-07-02-dq-report-ignore.md:14 claim=two(2) noun=actions list@16 count=2(raw 2)
   TEXT: Add two operator actions to each **operator-actionable** Data-quality card:
   ITEMS: **Report** — a quiet control that opens the existing `Report | **Ignore / Un-ignore** — dismiss a warning into a collapsibl

MISMATCH docs/superpowers/specs/data-quality/2026-07-04-data-quality-badge-full-warn-class.md:180 claim=Three(3) noun=surfaces list@181 count=5(raw 5)
   TEXT: Three surfaces render the breakdown and ALL must route through this helper (no direct `dataGapClassDetails().map().join()`):
   ITEMS: **Badge** (`DataQualityBadge`): `aria-label`/`title` = `"${t | **Per-show sub-line** (`PerShowAlertSection`, ~:301): same h | **Held-row chip** (`ShowsTable` `DataGapsChip`, :244): `titl | Cap default **4**; count desc then registry order. Total-cou | **Guard:** `cap ≤ 0` or a summary with `total === 0` → empty

PARITY docs/superpowers/specs/data-quality/2026-07-04-version-detection-confidence-gate-design.md:12 claim=two(2) noun=ways list@14 count=2(raw 2)
   TEXT: 3`), each of which branches its extraction logic on it. Detection is fragile in two ways the audit flagged:
   ITEMS: **Finding #1 — v1 is a catch-all.** `detectVersion` returns  | **Finding #2 — v4/v2 each hinge on ONE marker.** v4 requires

MISMATCH docs/superpowers/specs/data-quality/2026-07-04-version-detection-confidence-gate-design.md:103 claim=4(4) noun=gates list@105 count=5(raw 5)
   TEXT: ery item lands in ONE commit** (this is a §12.4 code; per the "New §12.4 code = 4 CI gates beyond the 3-way lockstep" rule, x2 and the full-suite run are NOT optional):
   ITEMS: `docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md` —  | `pnpm gen:spec-codes` → regenerates `lib/messages/__generate | `lib/messages/catalog.ts` — hand-authored `MESSAGE_CATALOG[" | Producer literal `code: "VERSION_AMBIGUOUS"` under `lib/pars | **`pnpm gen:internal-code-enums` → regenerate and commit `li

PARITY docs/superpowers/specs/data-quality/2026-07-05-finalize-resume-deadlock.md:24 claim=two(2) noun=actions list@26 count=2(raw 2)
   TEXT: offers exactly two actions and BOTH are gated shut in this state:
   ITEMS: **Resume publishing** → `POST /api/admin/onboarding/finalize | **Discard this setup** → `cleanupAbandonedFinalize`

PARITY docs/superpowers/specs/data-quality/2026-07-10-crew-rename-shrink-gate.md:9 claim=Two(2) noun=consequences list@11 count=2(raw 2)
   TEXT: Two consequences:
   ITEMS: **Silent removal on a published show** — the exact class the | **Picker continuity loss.** The apply path is delete+insert 

PARITY docs/superpowers/specs/data-quality/2026-07-10-structural-transform-use-raw.md:12 claim=Three(3) noun=transforms list@14 count=3(raw 3)
   TEXT: Three parser transforms **structure** a sheet cell into sub-fields, and each can get the structuring wrong even when the sheet is 
   ITEMS: **Room header split** — `splitRoomHeader(raw, kind)` (`lib/p | **Hotel guest/address glue** — `parseGuestCell(cell)` (`lib/ | **Inverted dates** — date parsing reads tokens as M/D/Y; whe

PARITY docs/superpowers/specs/data-quality/2026-07-10-structural-transform-use-raw.md:43 claim=two(2) noun=homes list@45 count=2(raw 2)
   TEXT: ecycle phases** (verified against the onboarding flow), so the decision set has two storage homes and a one-way migration at finalize:
   ITEMS: **Wizard (Step-3 review) is PRE-CREATE.** No `shows`/`shows_ | **Per-show admin page is POST-PUBLISH.** `shows` + `shows_in

PARITY docs/superpowers/specs/data-quality/2026-07-10-structural-transform-use-raw.md:239 claim=two(2) noun=surfaces list@241 count=2(raw 2)
   TEXT: The two surfaces differ only in which server action `onToggle` binds to and which handle it carries:
   ITEMS: **Wizard judgment callout** — `components/admin/wizard/step3 | **Per-show admin page** — `components/admin/PerShowActionabl

MISMATCH docs/superpowers/specs/data-quality/2026-07-15-feed-disposition-accept.md:67 claim=two(2) noun=axes list@68 count=3(raw 3)
   TEXT: - Mode boundaries — the two axes are independent and their render rules are total:
   ITEMS: **Accept button:** renders iff `entry.acceptable` (which by  | **"Accepted" tag:** renders iff `entry.acknowledgedAt !== nu | `acceptable` and `acknowledgedAt !== null` are mutually excl

PARITY docs/superpowers/specs/data-quality/2026-07-16-use-raw-wizard-full-list-toggle.md:31 claim=two(2) noun=props list@33 count=2(raw 2)
   TEXT: dfid: string; warnings: ParseWarning[] }` (`step3ReviewSections.tsx:2386`). Add two optional props, exactOptionalPropertyTypes discipline (present or ABSENT, never explicit `undefined` — same doc contract as `S
   ITEMS: `useRawDecisions?: UseRawDecision[]` — staged decisions for  | `wizardSessionId?: string` — the session that binds the stag

PARITY docs/superpowers/specs/data-quality/2026-07-17-badge-affordance-a11y.md:12 claim=Two(2) noun=findings list@14 count=2(raw 2)
   TEXT: e Flow-4 roster-shift signal with a single amber `TriangleAlert` glyph (`:55`). Two deferred findings:
   ITEMS: **FLOW4-2** — the roster/gap breakdown reaches sighted users | **FLOW4-3** — one amber glyph conflates "parse gaps" and "ro

MISMATCH docs/superpowers/specs/data-quality/2026-07-17-dq-group-active-by-code.md:88 claim=8(8) noun=files list@89 count=6(raw 6)
   TEXT: **Affected existing tests (≈8 files):**
   ITEMS: `tests/components/admin/bulkIgnoreControls.test.tsx` — rewri | `tests/app/admin/perShowPage.test.tsx` — active-section stru | `tests/admin/perShowActionableKeyStability.test.tsx`, `tests | `tests/dataQuality/bulkIgnoreGroups.test.ts` — unchanged (gr | `tests/styles/_metaDestructiveConfirm.test.ts` — the `BulkIg | `tests/parser/parseWarningDeepLinkRender.test.tsx` — verify 

MISMATCH docs/superpowers/specs/data-quality/2026-07-17-use-raw-callout-preview-demotion.md:13 claim=3(3) noun=renders list@15 count=2(raw 2)
   TEXT: docs/superpowers/specs/2026-07-16-use-raw-wizard-full-list-toggle.md`) the Step-3 wizard renders **two** live control instances for any warning that falls in the first `CALLOUT_MAX_ENTRIES = 3` of its section:
   ITEMS: The section's amber **`SectionFlagCallout`** preview (`compo | The uncapped **`WarningsBreakdown`** full list (`:2306`), wh

PARITY docs/superpowers/specs/data-quality/2026-07-17-use-raw-callout-preview-demotion.md:119 claim=two(2) noun=mounts list@120 count=2(raw 2)
   TEXT: re behavioral proof — TWO fixtures are mandatory (one per removed mount).** The two callout mounts gate on **different** warning codes, so a single fixture cannot prove both are gone:
   ITEMS: `UseRawControlBoundary` always renders `<UseRawControl>` (`c | `RoleRecognizeControlBoundary` returns `null` unless `warnin

PARITY docs/superpowers/specs/data-quality/2026-07-17-use-raw-control-site-a11y-copy.md:14 claim=Two(2) noun=findings list@16 count=2(raw 2)
   TEXT: ecognize-role controls, kept alongside the capped `SectionFlagCallout` preview. Two impeccable dual-gate findings were deferred:
   ITEMS: **USE-RAW-FULL-LIST-2 [P2, a11y].** For a warning appearing  | **USE-RAW-FULL-LIST-3 [P2, copy].** The line `"These are inf

PARITY docs/superpowers/specs/data-quality/2026-07-18-callout-preview-action-cue.md:31 claim=two(2) noun=controls list@33 count=2(raw 2)
   TEXT: tions.tsx:~2393`) would render an interactive control for it. There are exactly two controls, each self-hiding out-of-scope warnings:
   ITEMS: **`RoleRecognizeControlBoundary`** renders iff `warning.code | **`UseRawControlBoundary` → `UseRawControl`** renders an int

PARITY docs/superpowers/specs/data-quality/2026-07-18-callout-preview-action-cue.md:131 claim=two(2) noun=boundaries list@132 count=2(raw 2)
   TEXT: ance.test.tsx`: asserts `warningOffersFix`'s verdict stays in lockstep with the two boundaries' actual render gates —
   ITEMS: use-raw branch ≡ `deriveUseRawControlState(w, decision, fals | role branch ≡ `RoleRecognizeControlBoundary` non-null gate, 

PARITY docs/superpowers/specs/data-quality/2026-07-25-secondary-drive-id-nonblank.md:57 claim=two(2) noun=as list@59 count=2(raw 2)
   TEXT: That scope rule was a reasonable line to draw once. It has two defects as a *durable* mechanism:
   ITEMS: **It is prose, not executable.** Nothing in the repo compare | **It has already drifted.** `public.onboarding_rebuild_attem

PARITY docs/superpowers/specs/data-quality/2026-07-26-driveid-guard-cluster-design.md:122 claim=Two(2) noun=failures list@124 count=2(raw 2)
   TEXT: and throw unless the result equals the pinned constant. Two distinguishable failures:
   ITEMS: **connection/query failure** → error says the identity probe | **mismatch** → error prints BOTH identifiers and the remedia

PARITY docs/superpowers/specs/data-quality/2026-07-26-driveid-guard-cluster-design.md:299 claim=two(2) noun=floors list@301 count=2(raw 2)
   TEXT: Anti-vacuity (two independent floors, neither a bare count):
   ITEMS: **Manifest-derived expected set**: every column in the commi | **Dev-slice anchor** (R1 finding 3 — a presence floor let an

PARITY docs/superpowers/specs/data-quality/2026-08-03-close-v1-override-wont-build.md:84 claim=two(2) noun=things list@86 count=2(raw 2)
   TEXT: So the entry's premise conflates two different things:
   ITEMS: **"No markers registered today"** — true of a hypothetical l | **"No registrable structure"** — what the entry needs to be 

PARITY docs/superpowers/specs/data-quality/2026-08-03-close-v1-override-wont-build.md:111 claim=two(2) noun=steps list@113 count=2(raw 2)
   TEXT: Concretely, two steps on the same entry:
   ITEMS: **Declare** (first commit). The entry's `**Status:** OPEN.`  | **Graduate** (second commit). The entry leaves `BACKLOG.md` 

PARITY docs/superpowers/specs/db/2026-08-02-db-lockdown-trio-design.md:141 claim=two(2) noun=overstates list@143 count=2(raw 2)
   TEXT: -DML-LOCKDOWN` backlog entry. What it buys differs by class, and conflating the two overstates it:
   ITEMS: **Class (a), the three RPC-gated tables** (`reports`, `repor | **Class (b), the five with no RPC at all** (`sync_log`, `syn

PARITY docs/superpowers/specs/db/2026-08-02-db-lockdown-trio-design.md:235 claim=Three(3) noun=reconciliations list@237 count=3(raw 3)
   TEXT: Three reconciliations, run against `information_schema` / `pg_class` / `pg_policies`:
   ITEMS: **A — forward.** Every `ADMIN_TABLES` entry is classified `a | **B — reverse.** Every table classified `admin_only` or `den | **C — total.** Every live relation in `public` that can carr

PARITY docs/superpowers/specs/db/2026-08-02-db-lockdown-trio-design.md:310 claim=two(2) noun=postures list@312 count=2(raw 2)
   TEXT: DMIN_TABLES` member assert `pg_class.relrowsecurity = true`, AND exactly one of two declared postures:
   ITEMS: **`admin_only`** — exactly one policy, named `admin_only`, ` | **`deny_all`** — **zero** policies. Under enabled RLS this d

PARITY docs/superpowers/specs/db/2026-08-02-db-lockdown-trio-design.md:317 claim=Three(3) noun=assertions list@319 count=3(raw 3)
   TEXT: **Three assertions the current probe lacks**, each closing a probe-demonstrated blind spot (§2.4):
   ITEMS: **`relrowsecurity`** — `ALTER TABLE ... DISABLE ROW LEVEL SE | **Policy count** — Postgres ORs permissive policies together | **Non-vacuous behavioral arm** — the existing arm asserts `n

PARITY docs/superpowers/specs/db/2026-08-02-db-lockdown-trio-design.md:362 claim=Two(2) noun=cells list@364 count=2(raw 2)
   TEXT: Two cells cannot be produced as stated, and the spec says so rather than claiming them:
   ITEMS: **`email_deliveries` SELECT is grant-layer, not RLS** (R4 fi | **`app_settings` INSERT is structurally unavailable** (R4 fi

PARITY docs/superpowers/specs/master-spec-patches/2026-05-12-catalog-agenda-codes.md:11 claim=two(2) noun=states list@13 count=2(raw 2)
   TEXT: Add two crew-facing display codes to §12.4 covering the two crew-recoverable error states `AgendaPdfViewer` surfaces when the agenda asset proxy at `app/api/asset/agenda/[show]/[id]/rout
   ITEMS: **`AGENDA_GONE_FOR_CREW`** — HTTP **410 OR 403** from the pr | **`AGENDA_UNAUTHENTICATED`** — HTTP **401** from the proxy. 

PARITY docs/superpowers/specs/master-spec-patches/2026-05-12-catalog-agenda-codes.md:87 claim=two(2) noun=things list@89 count=2(raw 2)
   TEXT: After ratification, two things happen in order:
   ITEMS: The two row lines above are inserted into `docs/superpowers/ | This amendment file remains in place as the historical recor

PARITY docs/superpowers/specs/nav-perf/2026-06-23-nav-perf-phase2-ui-feedback.md:41 claim=4(4) noun=routes list@42 count=4(raw 4)
   TEXT: **D — the 4 routes lacking `loading.tsx` (all `force-dynamic`):**
   ITEMS: `app/admin/settings/admins/` (page `force-dynamic` L21) — `< | `app/admin/show/staged/[stagedId]/` (L33) — `<main data-test | `app/admin/show/[slug]/preview/[crewId]/` (L51) — fragment:  | `app/help/` — `app/help/layout.tsx` (`force-dynamic` L13) re

PARITY docs/superpowers/specs/nav-perf/2026-06-23-nav-perf-tag-caching.md:82 claim=TWO(2) noun=layers list@83 count=2(raw 2)
   TEXT: acheRevalidateCoverage.test.ts` (registry-style, mirrors `_metaInfraContract`), TWO layers:
   ITEMS: **Registry layer** — an explicit registry of the known apply | **Discovery layer (the gap-closer)** — a repo scan over `lib

MISMATCH docs/superpowers/specs/observability/2026-06-29-centralized-logging-foundation-design.md:391 claim=5(5) noun=handlers list@392 count=1(raw 1)
   TEXT:  = **5 handlers.**
   ITEMS: **Explicit capture (streamed/deferred bodies, where ALS may 

PARITY docs/superpowers/specs/observability/2026-07-01-durable-outcome-telemetry-design.md:11 claim=Two(2) noun=deliverables list@13 count=2(raw 2)
   TEXT: Two coupled deliverables:
   ITEMS: **The `info`+`code` durable-outcome convention**, codified a | **Instrument the identified gaps** from the audit: the 6 adm

PARITY docs/superpowers/specs/observability/2026-07-01-watch-channel-health-design.md:9 claim=five(5) noun=defects list@11 count=5(raw 5)
   TEXT: _ORPHANED` admin alert (Google Drive push-subscription registration failed) has five compounding defects, confirmed by live investigation on 2026-07-01:
   ITEMS: **Jargon copy.** The shipped copy says "cron" (`lib/messages | **Misleading verb.** The banner's "Resolve" button (`compone | **No recovery path.** A failed subscription becomes `status= | **No admin self-service.** There is no in-app action that re | **Live incident.** On the validation deployment, `DRIVE_WEBH

PARITY docs/superpowers/specs/observability/2026-07-01-watch-channel-health-design.md:43 claim=two(2) noun=keys list@44 count=2(raw 2)
   TEXT: he alert context written by `markWatchOrphanedWithTx` (`watch.ts:348-360`) with two new keys, written by **every** producer (uniform shape is what makes render-time derivation safe, since the upsert RPC replace
   ITEMS: `error_class`: the classification above. | `error_message`: `redactWatchError(String(err?.message ?? er

PARITY docs/superpowers/specs/observability/2026-07-02-cron-empty-id-throw-attribution-design.md:9 claim=Two(2) noun=defects list@11 count=2(raw 2)
   TEXT: Two coupled defects, both confirmed against live data (deployed SHA `0ff1ab7f`, verified current via `/api/health` — **no deploy-l
   ITEMS: **Recurring unhandled empty-ID Drive 404.** `cron.sync` thre | **#227 S1 throw-attribution never fires for this class.** Of

PARITY docs/superpowers/specs/observability/2026-07-02-observability-coverage-carveout-design.md:17 claim=Two(2) noun=branches list@19 count=2(raw 2)
   TEXT: Two in-flight feature branches (spec/plan stage, no implementation code yet) will restructure surfaces that overlap other audit ga
   ITEMS: `worktree-watch-channel-health` → Drive webhook + watch-chan | `worktree-published-toggle` → publish/unpublish/archive/unar

MISMATCH docs/superpowers/specs/observability/2026-07-02-observability-coverage-carveout-design.md:81 claim=two(2) noun=branches list@83 count=6(raw 6)
   TEXT: **Change — split the one emission into the existing two verdict branches (`:168` / `:180`):**
   ITEMS: **404/400 branch** (`if (status === 404 || status === 400)`, | **else branch** (403/429/5xx/timeout/`null`, `:180`): `log.w | **Hardcode `verdict` per branch** ("known_stale" / "unknown" | `driveErrorStatus` returns `number | null` (`lib/drive/fetch | **No `showId`/`requestId` in scope** — `enrichAgenda`'s para | Other three `known_stale` sites (non-PDF `:202`, download in

MISMATCH docs/superpowers/specs/observability/2026-07-03-nullcode-forensic-batch2-design.md:134 claim=25(25) noun=lines list@136 count=2(raw 2)
   TEXT: level, text }` instead of deleting it). (The plan gives the full helper; it is ~25 lines mirroring `stripLogEmissionCalls`.) Then for **every** registry row, assert:
   ITEMS: **Exactly one** span `S` in `collectLogSpans(src)` satisfies | **Global uniqueness:** the code literal `"<CODE>"` occurs **

PARITY docs/superpowers/specs/observability/2026-07-03-telemetry-access-layer-design.md:21 claim=Two(2) noun=properties list@23 count=2(raw 2)
   TEXT: tap reliably. Two properties define "reliably":
   ITEMS: **Stable interface** — one schema-aware module is the only t | **Discoverability** — agents use the sanctioned path (docume

PARITY docs/superpowers/specs/observability/2026-07-04-mutation-surface-observability.md:336 claim=Two(2) noun=kinds list@337 count=2(raw 2)
   TEXT:  string }>` — the ONLY way an admin surface skips registry+behavioral. Two kinds:
   ITEMS: **`delegator`** (thin admin surface forwarding to an already | **`read-only`** (an admin-gated exported action that perform

PARITY docs/superpowers/specs/observability/2026-07-04-telemetry-rename-admin-dev-gate.md:7 claim=Two(2) noun=changes list@9 count=2(raw 2)
   TEXT: Two related changes to the admin surface, both building on the just-shipped developer tier (PR #286):
   ITEMS: **Part A — Rename `/admin/observability` → `/admin/dev/telem | **Part B — Restrict ALL admin management (Add / Revoke / Re-

PARITY docs/superpowers/specs/observability/2026-07-04-telemetry-rename-admin-dev-gate.md:119 claim=three(3) noun=controls list@120 count=3(raw 3)
   TEXT: from `app/admin/settings/page.tsx:150` and the admins deep-link page). Gate the three management controls on it:
   ITEMS: `AddAdminDisclosure` (`:149`) → rendered only when `viewerIs | `RevokeRowButton` (`:194`) → rendered only when `viewerIsDev | `ReAddRowButton` (`:220`) → rendered only when `viewerIsDeve

PARITY docs/superpowers/specs/observability/2026-07-05-crew-picker-observability.md:40 claim=two(2) noun=mechanisms list@42 count=2(raw 2)
   TEXT: mutation boundary. This is durable and catalog-exempt by two existing mechanisms:
   ITEMS: **Durable:** `shouldPersist` persists an `info` record **iff | **§12.4-scanner-exempt:** the code literal lives **inside a 

PARITY docs/superpowers/specs/observability/2026-07-05-crew-picker-observability.md:164 claim=Two(2) noun=tests list@166 count=2(raw 2)
   TEXT: Two tests reference the ledger's contents and MUST be updated in the same change:
   ITEMS: **`tests/log/mutationSurface/exemptions.test.ts:18-40` (Code | **`_metaMutationSurfaceObservability.test.ts:551-572`** — th

PARITY docs/superpowers/specs/observability/2026-07-05-wire-silent-channels-design.md:11 claim=three(3) noun=modes list@13 count=3(raw 3)
   TEXT: The edge-case audit found three ingestion failure modes that happen with **zero pushed signal** — the pipeline does the right structural thing (drops/degrad
   ITEMS: **#15 — `UNEXPECTED_PARENT` fully silent.** `listFolder` (`l | **#14 — first-seen hard-fail "raises no admin alert."** The  | **#16 — degraded-parse dataGaps never push.** A live publish

PARITY docs/superpowers/specs/observability/2026-07-05-wire-silent-channels-design.md:39 claim=Two(2) noun=mechanisms list@40 count=2(raw 2)
   TEXT: Bell notification center** (PR #324, `project_bell_notification_center_pr324`). Two mechanisms, both DB-driven and both independent of `candidates.ts`:
   ITEMS: **Realtime ping (push):** a statement trigger `admin_alerts_ | **Feed visibility (what the ping reveals):** `get_bell_feed_

PARITY docs/superpowers/specs/observability/2026-07-06-telemetry-console-redesign.md:433 claim=TWO(2) noun=files list@434 count=2(raw 2)
   TEXT: - **The DB coverage is split across TWO files to match the CI topology (Codex plan-R2 F1)** — the unit-suite job boots LOCAL supabase and has NO validation Supabase R
   ITEMS: `tests/db/telemetryConsoleReads.test.ts` — **direct-SQL beha | `tests/db/telemetryConsoleReads.rpc.test.ts` — the **real `c

MISMATCH docs/superpowers/specs/observability/2026-07-06-telemetry-console-redesign.md:456 claim=two(2) noun=functions list@457 count=5(raw 5)
   TEXT: rehensive closure, do not relitigate per-instance).** The full contract for the two functions:
   ITEMS: `validation-schema-parity` does NOT cover functions (columns | Existence: `to_regprocedure('<sig>')` not null for both. | Privilege: `has_function_privilege('service_role','<sig>','E | Behavior (deterministic, isolated): `admin_event_stats_24h`  | Runtime path / deployment proof: a real `createSupabaseServi

PARITY docs/superpowers/specs/observability/2026-07-07-flow6-signal-visibility.md:127 claim=three(3) noun=updates list@129 count=3(raw 3)
   TEXT: . Three-way lockstep (cross-cutting discipline "§12.4 catalog row edits require three lockstep updates"):
   ITEMS: **Spec §12.4** row in `docs/superpowers/specs/2026-04-30-fxa | `pnpm gen:spec-codes` → regenerates `lib/messages/__generate | **Catalog** row in `lib/messages/catalog.ts` (full shape per

MISMATCH docs/superpowers/specs/observability/2026-07-07-flow6-signal-visibility.md:219 claim=2(2) noun=items list@221 count=3(raw 3)
   TEXT: ### 5.4 Part-2 open items (resolved in its own plan)
   ITEMS: Watermark storage vs `email_deliveries` timestamp precision. | Digest window / cadence interaction (`runNotify.ts:390` `out | Whether roster-change acknowledgement (Flow-4 `acknowledge_c

PARITY docs/superpowers/specs/observability/2026-07-08-flow6.2-monitor-digest.md:306 claim=three(3) noun=cases list@307 count=3(raw 3)
   TEXT: 9. **`syncLogSink.persistence.test.ts`** (§3.2 sink fix, `warningsFor` only) — three cases:
   ITEMS: (a) entry carrying `parseWarnings` (incl. a `*_AUTOCORRECTED | (b) entry with `payload` but NO `parseWarnings` (e.g. the ex | (c) entry with `parseWarnings` and NO `payload` → `parse_war

PARITY docs/superpowers/specs/observability/2026-07-16-digest-autofix-per-show-detail.md:13 claim=Two(2) noun=defects list@15 count=2(raw 2)
   TEXT: Two defects:
   ITEMS: **No attribution/detail.** `renderMonitorSection` sub-block  | **Count inflation.** `accumulateAutoFixes` sums `summarizeAu

MISMATCH docs/superpowers/specs/observability/2026-07-24-watch-reconcile-backoff-design.md:427 claim=6(6) noun=halves list@429 count=2(raw 2)
   TEXT: dder is actually in play** — that is, when there is no live channel. R3 finding 6 showed both halves of the R2 draft could be false:
   ITEMS: `renewal_failing` is a **sampled** state, not current livene | The count must not be presented as total attempts. `consecut

PARITY docs/superpowers/specs/observability/2026-07-24-watch-reconcile-backoff-design.md:442 claim=two(2) noun=contracts list@444 count=2(raw 2)
   TEXT:  "short", day: "numeric", hour: "numeric", minute: "2-digit" })`, with the same two contracts that function carries:
   ITEMS: **Non-finite parse guard.** `Date.parse` returning `NaN` ren | **Hydration.** The timestamp renders inside `<time dateTime=

PARITY docs/superpowers/specs/observability/2026-07-26-watch-reconcile-backoff-v2-design.md:431 claim=three(3) noun=pins list@432 count=3(raw 3)
   TEXT: derive expected time through the same formatter). Class 18 additionally carries three pins:
   ITEMS: the `infra_error` → hidden mapping asserted at the BELL load | the same mapping at the SETTINGS read → prop `null`, panel r | **the production `recordAttempt: true` opt-in pinned at both

PARITY docs/superpowers/specs/observability/2026-07-26-watch-renewal-lifecycle-design.md:26 claim=two(2) noun=gives list@28 count=2(raw 2)
   TEXT:  entry states "24 futile `files.watch` calls/day per stuck folder". Reading the two consumers together gives twice that, because a stuck folder is attempted on BOTH surfaces of the same tick:
   ITEMS: `refreshWatchSubscriptions` finds the expired-but-active row | `reconcileWatchChannels` then computes `live` via `hasLiveAc

PARITY docs/superpowers/specs/observability/2026-07-26-watch-renewal-lifecycle-design.md:303 claim=Three(3) noun=rules list@305 count=3(raw 3)
   TEXT: Three rules follow from the table, and each has a reason that is easy to lose:
   ITEMS: **A failed read renews nothing.** Fail-open is what unbounde | **When something WAS due, the failure is recorded.** Otherwi | **When nothing was due, it is NOT recorded.** The renewal qu

PARITY docs/superpowers/specs/observability/2026-07-26-watch-renewal-lifecycle-design.md:328 claim=two(2) noun=counts list@330 count=2(raw 2)
   TEXT: Scope` discriminant to it and called the addition "additive". That was wrong on two counts, and the correction is the reason this section exists:
   ITEMS: It is not additive to the **tests**. `RefreshResult` is deep | It is not additive to the **public cron response**. `tests/c

PARITY docs/superpowers/specs/observability/2026-07-26-watch-renewal-lifecycle-design.md:370 claim=Three(3) noun=rounds list@372 count=3(raw 3)
   TEXT: Three consecutive review rounds found a race here, and each repair introduced the next round's defect:
   ITEMS: **R2** — a pending row that already exists when promotion ru | **R3** — a pending row created AFTER promotion commits is no | **R4** — that subquery reads a READ COMMITTED snapshot, so a

PARITY docs/superpowers/specs/observability/2026-07-26-watch-renewal-lifecycle-design.md:413 claim=three(3) noun=counts list@415 count=3(raw 3)
   TEXT: not reach the credential fetch. **That design is withdrawn.** It was refuted on three counts, and the third is a correctness hazard rather than a shortfall:
   ITEMS: **It cannot cancel what it abandons.** The wrapper surrounde | **It does not close the gap it was justified by.** The crede | **It bounded nothing the per-call option does not already bo

PARITY docs/superpowers/specs/observability/2026-08-09-sync-log-show-attribution-design.md:32 claim=Two(2) noun=defects list@34 count=2(raw 2)
   TEXT: Two secondary defects on the same table, both real independent of any read surface:
   ITEMS: **No index.** `public.sync_log` has no index beyond its prim | **No retention.** No pruning exists. `prune_app_events` cove

MISMATCH docs/superpowers/specs/observability/2026-08-09-sync-log-show-attribution-design.md:338 claim=Three(3) noun=hazards list@340 count=4(raw 4)
   TEXT: **Three hazards that are design-level, and therefore stay here:**
   ITEMS: **`TEST_DATABASE_URL` is the validation project**, and `lib/ | **The pre-existing meta-guards do not cover this, and extend | **Anti-vacuity must key on CI presence, not on a configured  | **`querySyncLog` defaults `sinceHours` to 24** (`lib/observe

PARITY docs/superpowers/specs/observability/2026-08-09-sync-log-show-attribution-design.md:381 claim=Two(2) noun=consequences list@383 count=2(raw 2)
   TEXT: Two consequences, both deliberate:
   ITEMS: **Adding `logSync` to that site does not make it log.** The  | **The third duration-capture boundary (R7 F3) is moot until 

PARITY docs/superpowers/specs/parser/2026-06-26-parse-warning-deeplinks-design.md:18 claim=Two(2) noun=gaps list@20 count=2(raw 2)
   TEXT: Two independent gaps make operator-actionable warnings invisible for the steady-state (cron) ingestion path:
   ITEMS: **Anchors are populated on the onboarding scan path only.**  | **No cron-reachable surface renders per-warning deep links.*

PARITY docs/superpowers/specs/parser/2026-06-27-deeplink-parser-fidelity-batch-design.md:16 claim=Two(2) noun=pin-tests list@17 count=2(raw 2)
   TEXT: - **Two structural pin-tests gate the anchored set's membership and must be bumped in lockstep with any addition** (AGENTS.md meta-tes
   ITEMS: `tests/parser/operatorActionableWarnings.test.ts` — `contain | `tests/drive/showDayTimeAnchors.test.ts` — `hasCellAnchoredW

PARITY docs/superpowers/specs/parser/2026-06-27-parser-typo-tolerance-design.md:15 claim=two(2) noun=prs list@17 count=2(raw 2)
   TEXT:  gate infra) + P1 (safe surfaces) + P2 (field-alias chokepoint)**, delivered as two sequential PRs:
   ITEMS: **PR-A = P0 + P1** — the shared gate utility, the vocab/excl | **PR-B = P2** — wire an opt-in fuzzy fallback into the field

PARITY docs/superpowers/specs/parser/2026-06-27-schedule-strike-loadout-inference-design.md:17 claim=three(3) noun=elements list@19 count=3(raw 3)
   TEXT: This feature derives and renders three new schedule elements **from data the parser already extracts**:
   ITEMS: **SET day** — its load-in / setup time(s) surfaced as **synt | **Strike** entries — one per room, from each room's already- | **Load-Out** entry — from the transportation **"Pick Up Venu

MISMATCH docs/superpowers/specs/parser/2026-06-27-set-cell-derived-labels-design.md:20 claim=6(6) noun=reads list@22 count=4(raw 4)
   TEXT: -test-shows` (`1iU80Y2mqYmkCuBQYer0TEF1fta6fDp1C`) via the Drive connector (all 6 reads succeeded):
   ITEMS: **0 / 16 shows** have a SET cell with **>2** colon-clocks. M | **0 / 16 shows** carry a **non-generic** label (no session/r | The only label-before-shaped multi-time cell in the entire c | The dominant shape is a single load-in clock (`"11:00 AM LOA

PARITY docs/superpowers/specs/parser/2026-06-27-set-cell-derived-labels-design.md:137 claim=two(2) noun=modes list@138 count=2(raw 2)
   TEXT: The colon-terminated trigger is the exact fix for two failure modes:
   ITEMS: **Trailing labels (R9–R14):** a back-scan applied to time-fi | **Leading provenance (R1 P1b):** `"As per Alyssa email 4/29 

PARITY docs/superpowers/specs/parser/2026-06-27-stage-word-typo-tolerance-design.md:12 claim=two(2) noun=symptoms list@14 count=2(raw 2)
   TEXT: **exact-spelling** stage-word matching in `lib/parser/personalization.ts`, with two distinct symptoms on the *same* crew role cell:
   ITEMS: **`UNKNOWN_ROLE_TOKEN` cascade** (`extractRoleFlags`): `FULL | **Silent `stage_restriction` mis-parse** (`extractStageRestr

PARITY docs/superpowers/specs/parser/2026-06-28-gear-parser-fidelity-design.md:27 claim=Two(2) noun=causes list@28 count=2(raw 2)
   TEXT: Two compounding causes (both verified in code):
   ITEMS: **Header matches the wrong block, then breaks before the val | **Terminator collision (secondary hazard for the fix).** The

PARITY docs/superpowers/specs/parser/2026-06-28-gear-parser-fidelity-design.md:113 claim=3(3) noun=fixes list@115 count=3(raw 3)
   TEXT: ### 3.5 Bug 3 fixes
   ITEMS: **3a** (`rooms.ts`, GS-block field parsing around `parseGsRo | **3b** (`pull-sheet.ts` variant detection `:137-202`): make  | **3c** (`emptyState.ts:52`): add `"-"` and `"—"` to `GENERIC

PARITY docs/superpowers/specs/parser/2026-06-30-transport-loadout-contact-design.md:10 claim=two(2) noun=surfaces list@12 count=2(raw 2)
   TEXT: Capture the `Load Out:` secondary-transporter contact and render it on the two transport surfaces the primary driver already uses:
   ITEMS: **Crew** — a "Load out" row in the "Getting there" card (`co | **Operator (Step-3 review modal)** — three rows in `Transpor

PARITY docs/superpowers/specs/parser/2026-07-02-diagrams-embedded-images.md:20 claim=four(4) noun=stages list@22 count=4(raw 4)
   TEXT: The crew diagram pipeline has four stages that already work end-to-end for **linked-folder** items:
   ITEMS: **Discovery** — enumerate the DIAGRAMS-tab image objects (`e | **Capture** — download the approved bytes and upload to priv | **Serve** — a signed-URL proxy over the `diagram-snapshots`  | **Render** — the crew gallery (`components/crew/DiagramsBloc

PARITY docs/superpowers/specs/parser/2026-07-05-parser-anchor-deliteralization.md:36 claim=three(3) noun=sites list@38 count=3(raw 3)
   TEXT: e names, `MABEL` and `LAUDERDALE` (from the single 2024 east-coast fixture), at three sites:
   ITEMS: **`extractGsBlock` terminator** (`lib/parser/blocks/rooms.ts | **v1 room loop** (`:825`): `const mabelRe = /^\|\s*(MABEL\s+ | **`NEXT_ROOM_HEADER_RE`** (`:880-881`): `/^\|\s*(GENERAL\s+S

PARITY docs/superpowers/specs/parser/2026-07-05-parser-anchor-deliteralization.md:173 claim=Two(2) noun=modes list@174 count=2(raw 2)
   TEXT: emit a bogus room). Then the (non-empty) identity must NOT be a section banner. Two match modes (adversarial-review R33 f1 — a bare `startsWith` for generic tokens is TOO BROAD: it would drop real compound room
   ITEMS: **PREFIX-match (identity `startsWith`)** — ONLY the room/sec | **EXACT-match (identity `===`)** — the bare generic section 

PARITY docs/superpowers/specs/parser/2026-07-05-parser-anchor-deliteralization.md:223 claim=three(3) noun=regexes list@225 count=3(raw 3)
   TEXT: personalization.ts:158-169`) recognizes exactly three hand-picked phrasings via three regexes:
   ITEMS: `FULL_STAGE_ONLY_PATTERN` (`:53-54`) — `Load In / Set / Stri | `LOAD_IN_SET_ONLY_PATTERN` (`:55`) — anchored `^- Load In /  | `LOAD_OUT_STRIKE_ONLY_PATTERN` (`:56`) — anchored `^- Load O

PARITY docs/superpowers/specs/parser/2026-07-05-parser-anchor-deliteralization.md:274 claim=Three(3) noun=branches list@275 count=3(raw 3)
   TEXT: LE token NEVER makes a clause malformed (R22 f1); it is preserved to `cleaned`. Three mutually-exclusive branches, keyed on the token partition:
   ITEMS: **Valid restriction (explicit)** — ≥1 STAGE token AND ZERO U | **Malformed (some-unknown)** — ≥1 STAGE token AND ≥1 UNKNOWN | **No-stage (ROLE clause)** — body contains ZERO STAGE tokens

PARITY docs/superpowers/specs/parser/2026-07-06-bo-show-prefixed-breakout-header.md:10 claim=Two(2) noun=headers list@12 count=2(raw 2)
   TEXT: Two real breakout-room headers in `fixtures/shows/raw/2025-03-dci-rpas-central.md` carry a **show-code prefix** before the `BREAKO
   ITEMS: `2025-03-dci-rpas-central.md:207` — `| RPAS BREAKOUT 1&#10;L | `2025-03-dci-rpas-central.md:152` — `| RPAS BREAKOUT 2&#10;L

PARITY docs/superpowers/specs/parser/2026-07-06-bo-show-prefixed-breakout-header.md:19 claim=Two(2) noun=gates list@21 count=2(raw 2)
   TEXT: Two case-sensitive gates in `lib/parser/blocks/rooms.ts` are anchored on `BREAKOUT` sitting immediately after the leading `|`/kind
   ITEMS: **`boBlockRe`** (`rooms.ts:1080`): `const boBlockRe = /^\|\s | **`splitRoomHeader` step-1 strip** (`rooms.ts:1385`): `.repl

PARITY docs/superpowers/specs/parser/2026-07-06-bo-venue-header-anchor.md:25 claim=two(2) noun=shows list@27 count=2(raw 2)
   TEXT: Live-sheet audit (gsheets MCP) of the two representative real shows:
   ITEMS: **East Coast** (`1N1PKmhcvLAn5UwHLn4Rplm1yeVeYMvwfL3eOzB4Mcn | **RPAS** (`1vyZMRTqeFAJgocbSJM2_HDDMsUUJFBiLKk6WKq-dUYo`, v4

PARITY docs/superpowers/specs/parser/2026-07-06-bo-venue-header-anchor.md:111 claim=two(2) noun=shapes list@112 count=2(raw 2)
   TEXT: An asset/gear row that a dims-blind gate would fabricate has one of two shapes, **both rejected without any dims-token reasoning**:
   ITEMS: `| PROJECTION SCREEN | 5' x 9' |` — a `label \| value` pair  | A gear-grid row on the PULL SHEET / GEAR tab (`lib/parser/bl

PARITY docs/superpowers/specs/parser/2026-07-06-mi1-early-return-e2e.md:24 claim=Two(2) noun=things list@26 count=2(raw 2)
   TEXT: Two things remain unproven by a **fast, DB-free, hermetic** test at the parser boundary:
   ITEMS: **The early-return stub CONTRACT (shape, not just code).** ` | **The parser→invariants COMPOSITION without the DB / dev-too

PARITY docs/superpowers/specs/parser/2026-07-06-mi1-early-return-e2e.md:34 claim=two(2) noun=assertions list@36 count=2(raw 2)
   TEXT: Add one test file that closes the gap with two assertions, using only REAL production functions (no hand-fed hardError, no invalid cast):
   ITEMS: **Stub-contract assertion.** For several genuinely-garbage i | **Composed-hard_fail assertion.** The REAL stub, bridged thr

PARITY docs/superpowers/specs/parser/2026-07-06-widen-dims-address-date.md:22 claim=three(3) noun=shapes list@23 count=3(raw 3)
   TEXT: `normalizeDate` gains three additional shapes, all routed through the SAME calendar-validity round-trip and returning the same ISO output:
   ITEMS: **ISO** `YYYY-MM-DD` (4-digit year 2000–2099; e.g. `2026-07- | **Long-form month name**: `Month D, YYYY` and `D Month YYYY` | **Dash slash-equivalent (CELL-ONLY)**: `M-D-YYYY` with a **4

MISMATCH docs/superpowers/specs/parser/2026-07-06-widen-dims-address-date.md:120 claim=Two(2) noun=matchers list@121 count=3(raw 3)
   TEXT:  Two composed matchers built from those fragments:
   ITEMS: `DIMS_START` (partial — "a dims token begins here") = `(?:\d | `DIMS_OPERAND` (either kind) = `(?:\d+\s*(?:['′]|ft\b)|\d{2, | `DIMS_FULL` (2-or-3-operand capture) = `(\bDIMS_OPERAND\s*[x

PARITY docs/superpowers/specs/parser/2026-07-09-flow8.3-venue-timezone.md:137 claim=two(2) noun=paths list@138 count=2(raw 2)
   TEXT: Wired at the two paths where a geocode was *attempted*:
   ITEMS: **cache-hit** (`:86-90`): after setting `city`, call `applyT | **live-success** (`:113-118`): cache-write now includes `res

MISMATCH docs/superpowers/specs/parser/2026-07-09-parser-property-fuzz-design.md:123 claim=two(2) noun=tiers list@124 count=3(raw 3)
   TEXT: - **Attributable signal**, two tiers:
   ITEMS: **Entity-attributable warning:** same section AND identifies | **Section-structural warning:** a code from an explicit allo | **`raw_unrecognized`:** row's `block` field maps to the enti

PARITY docs/superpowers/specs/parser/2026-07-09-parser-property-fuzz-design.md:151 claim=two(2) noun=exits list@152 count=2(raw 2)
   TEXT: 3. Triage, exactly two exits:
   ITEMS: **Parser bug** (input is inside dial contract): fix via TDD; | **Dial overreach** (generator claimed contract the code neve

MISMATCH docs/superpowers/specs/parser/2026-07-09-parser-property-fuzz-design.md:156 claim=2(2) noun=additions list@158 count=3(raw 3)
   TEXT: ### 6.1 Phase-2 exporter additions (designed, not planned here)
   ITEMS: Grid arbitrary: model → 2-D cell grid with merged-region sim | Exporter Tier-1: `synthesizeMarkdownFromXlsx` never throws;  | Chain Tier-2: plant-and-find through exporter+parser compose

PARITY docs/superpowers/specs/parser/2026-07-25-hotel-ambiguity-coverage-design.md:315 claim=Two(2) noun=rules list@317 count=2(raw 2)
   TEXT: Per the same-vector rule, the fix is structural rather than another reword. **Two binding rules for every string in this feature:**
   ITEMS: **Copy states what the operator should CHECK, never what the | **Every string below is byte-for-byte normative** and is ass

PARITY docs/superpowers/specs/parser/2026-07-27-inline-later-group-own-hotel-design.md:151 claim=four(4) noun=dates list@153 count=4(raw 4)
   TEXT:  full postal address `Marriott Downtown 200 Oak Ave, Chicago, IL 60601` AND all four dates year-suffixed (`Check In: 3/1/26 … Check Out: 3/4/26`), per §8.1's date convention (today B1z clobbers identically to B
   ITEMS: B1z row 1 becomes `hotel "Marriott Downtown"`, `addr "200 Oa | B1 itself (suffix-only `200 Oak Ave`) is tier 2 under S8: ro | B6's later group keeps `hotel_name "Hotel 71 Chicago, IL 606 | The guest-name-in-address leak of B2 is impossible: `hotelTe

PARITY docs/superpowers/specs/parser/2026-08-07-parser-mutation-wave-design.md:144 claim=Two(2) noun=things list@146 count=2(raw 2)
   TEXT: n. The plain `modal - 1` rule needs no refinement to pass the calibration gate. Two things the spec records rather than inherits silently (probe §13.B verdict):
   ITEMS: **The zero is partly a conversion artifact.** The measured r | **The corpus is the acceptance test:** zero `ROW_CELLS_FUSED

PARITY docs/superpowers/specs/parser/2026-08-09-warning-shape-mutation-stability.md:17 claim=two(2) noun=functions list@19 count=2(raw 2)
   TEXT: The harness scores a mutant from two functions:
   ITEMS: `signalEq` — full deep-equal over the signal channel (`tests | `newSignalFired` — **code-count only** (`oracle.ts:51-58`), 

PARITY docs/superpowers/specs/parser/2026-08-09-warning-shape-mutation-stability.md:61 claim=Two(2) noun=corrections list@63 count=2(raw 2)
   TEXT: Two corrections to round 1, both found by review rather than by me:
   ITEMS: Round 1 asserted that `snippet` only **+ per occurrence** wo | Only the fully bare warning reaches 7/7, and it is the one s

PARITY docs/superpowers/specs/parser/2026-08-09-warning-shape-mutation-stability.md:88 claim=Two(2) noun=guardrails list@89 count=2(raw 2)
   TEXT: Two guardrails, because this edits the shared measuring instrument:
   ITEMS: The change is **equality-tier only**. `newSignalFired` stays | The ≤178 `signal_loss` rows it can affect are **re-verified 

PARITY docs/superpowers/specs/parser/2026-08-09-warning-shape-mutation-stability.md:258 claim=Two(2) noun=things list@260 count=2(raw 2)
   TEXT: Two things follow, and the second was not anticipated by either session:
   ITEMS: **The bound holds exactly.** Zero verdict changes outside th | **The migration is 143 rows, not 7.** 143 EXISTING ledger ro

PARITY docs/superpowers/specs/parser/probes/2026-08-07-mutation-wave-corpus-probe.md:131 claim=Two(2) noun=caveats list@133 count=2(raw 2)
   TEXT: corpus is not the threat model.** Two caveats the spec must record rather than inherit silently:
   ITEMS: **The 0% is partly a conversion artifact.** Column uniformit | **A real merged cell does not present as short-by-one at all

MISMATCH docs/superpowers/specs/schedule/2026-06-22-per-day-schedule-keytimes-design.md:12 claim=10(10) noun=fixtures list@14 count=3(raw 3)
   TEXT:  Redefining Fixed Income, Fixed Income Trading, FinTech 2026, RIA 2025) and all 10 committed fixtures:
   ITEMS: **Set key-time loses its date.** Key Times renders `Set = "9 | **Show-day TIME column dropped wholesale.** Each SHOW DAY ro | **No per-show-day Show time anywhere.** Key Times collapses 

PARITY docs/superpowers/specs/schedule/2026-06-22-per-day-schedule-keytimes-design.md:20 claim=two(2) noun=shapes list@21 count=2(raw 2)
   TEXT: Show-day TIME cells come in two shapes (both verified live):
   ITEMS: **Titled agenda** (Consultants, RPAS, FinTech): `"7:15am - R | **Bare window** (RIA `"7:30am - 5:50pm"`, Asset-Mgmt `"8:00 

PARITY docs/superpowers/specs/schedule/2026-06-22-per-day-schedule-keytimes-design.md:201 claim=Three(3) noun=gates list@202 count=3(raw 3)
   TEXT: Three gates:
   ITEMS: **`runOfShow` data** (`showStart`/`window`/`entries`) lives  | **The per-day Show anchors + room `show_time` fallback** (§5 | **`unknown_asterisk` → the ENTIRE strip is suppressed** (`re

PARITY docs/superpowers/specs/schedule/2026-06-26-agenda-pdf-schedule-design.md:23 claim=two(2) noun=forms list@24 count=2(raw 2)
   TEXT: - `AGENDA LINK` cells come in **two forms**:
   ITEMS: **Drive smart-chips** (e.g. "II – Redefining Fixed Income /  | **Plain Drive URLs** in the cell text (e.g. "II – Fixed Inco

PARITY docs/superpowers/specs/schedule/2026-06-26-agenda-pdf-schedule-design.md:184 claim=two(2) noun=methods list@185 count=2(raw 2)
   TEXT:  has **no byte-download and no chip method today**, so the spec extends it with two methods, implemented by **every** `DriveClient` impl:
   ITEMS: `downloadFileBytes(fileId: string): Promise<{ kind: 'bytes'; | `getAgendaChips(spreadsheetId: string): Promise<{ kind: 'row

PARITY docs/superpowers/specs/schedule/2026-06-26-agenda-pdf-schedule-design.md:200 claim=three(3) noun=edits list@201 count=3(raw 3)
   TEXT: n.tsx:326`, passing `agendaLinks: data.show.agenda_links`). The move requires **three coordinated edits**, not just relocating the JSX:
   ITEMS: **Add** `AgendaEmbed` (multi-doc) to `ScheduleSection`, read | **Remove** `AgendaEmbed` + the `agendaLinks` prop from `Diag | **Update** `VenueSection`'s show-gate (`VenueSection.tsx:185

PARITY docs/superpowers/specs/schedule/2026-06-27-agenda-pdf-schedule-merge.md:30 claim=Two(2) noun=facts list@32 count=2(raw 2)
   TEXT: Two facts make the card empty:
   ITEMS: **Extraction never runs for the staged show during the wizar | **The card has no agenda surface.** `Step3SheetCard` breakdo

PARITY docs/superpowers/specs/schedule/2026-06-27-agenda-pdf-schedule-merge.md:46 claim=two(2) noun=pivots list@48 count=2(raw 2)
   TEXT: Adversarial review (16 rounds) drove two pivots, both ratified by the user:
   ITEMS: **Round 1 — crew already renders the agenda.** The crew Sche | **Rounds 4–16 — inline extraction during the scan can't clea

PARITY docs/superpowers/specs/schedule/2026-06-29-unified-show-day-timeline-design.md:71 claim=Two(2) noun=consequences list@72 count=2(raw 2)
   TEXT: erwise TodaySection renders the **existing `RunOfShowList(todays)` unchanged**. Two consequences, both intentional:
   ITEMS: **Crew-only path is render-identical** to today (no reorder, | **No "ghost activation":** filtering null-time sessions in `

PARITY docs/superpowers/specs/schedule/2026-06-30-dress-anchor-and-per-card-report-fieldref.md:14 claim=two(2) noun=places list@16 count=2(raw 2)
   TEXT: (`components/crew/primitives/SourceLink.tsx`). The value and the link come from two different places on the INFO tab, and they have drifted apart:
   ITEMS: **Value source:** the standalone `DRESS` block, parsed by `p | **Link source:** `CARD_REGION_MAP["today-dress"] = "details"

PARITY docs/superpowers/specs/schedule/2026-06-30-event-details-unrendered-design.md:14 claim=two(2) noun=surfaces list@15 count=2(raw 2)
   TEXT: Surface the captured-but-hidden `event_details` specs on two surfaces, from a single shared whitelist:
   ITEMS: **Crew page** — a new "Tech specs" card in GearSection rende | **Step-3 review modal** — extend `EventDetailsBreakdown` to 

PARITY docs/superpowers/specs/schedule/2026-07-03-stage-filtered-schedule.md:166 claim=THREE(3) noun=callers list@168 count=3(raw 3)
   TEXT: **`resolveKeyTimes` has THREE callers — all must pass the stage restriction, or the default `{ kind: "none" }` re-opens the leak.** `resolveViewerContext`
   ITEMS: `ScheduleSection.tsx:95,108` — this file **destructures**: ` | `TodaySection.tsx:170,249-254` — this file **binds `ctx`**:  | `components/right-now/buildRightNowContext.ts:85` — **indire

MISMATCH docs/superpowers/specs/schedule/2026-07-05-schedule-show-day-labels.md:108 claim=10(10) noun=chars list@109 count=2(raw 2)
   TEXT: imension change. The label string grows from "Show" (4 chars) to "Show Day N" (~10 chars), rendered in the SAME element with the SAME classes:
   ITEMS: Crew `DayCard`: the phase line is `inline-flex items-center  | Wizard eyebrow: `EYEBROW_CLASS` on a block `<span>` in a `fl

PARITY docs/superpowers/specs/schedule/2026-08-02-agenda-fold-seeded-e2e-webkit-design.md:23 claim=four(4) noun=answers list@25 count=4(raw 4)
   TEXT: all four answers = the recommended option):
   ITEMS: **Autonomous ship approved.** Full pipeline through merged P | **Placement: extend `tests/e2e/stage-restricted-crew-schedul | **CI wiring: add the file to the crew-e2e run command** (`.g | **WebKit rider shape: a scoped project in `tests/e2e/standal

PARITY docs/superpowers/specs/schedule/2026-08-02-agenda-fold-seeded-e2e-webkit-design.md:226 claim=Two(2) noun=controls list@228 count=2(raw 2)
   TEXT: Two independent controls:
   ITEMS: **Admin test:** same seeded show, same `?s=schedule` navigat | **Cross-viewer pair (§3.2):** Fiona and Theo have complement

PARITY docs/superpowers/specs/step3-onboarding/2026-06-23-onboarding-scan-progress-stream.md:17 claim=two(2) noun=phases list@19 count=2(raw 2)
   TEXT: `runOnboardingScan` (`lib/sync/runOnboardingScan.ts:974-988`) has two phases:
   ITEMS: **Prepare** (`prepareOnboardingFiles`, `:891-925`) — lists t | **Stage** (`scanPreparedFiles`, `:804-869`) — fast DB stagin

PARITY docs/superpowers/specs/step3-onboarding/2026-06-23-onboarding-step3-review-redesign.md:187 claim=TWO(2) noun=points list@188 count=2(raw 2)
   TEXT: **This predicate must change in TWO enforcement points, in lockstep (codex R1 HIGH):**
   ITEMS: **UI gate.** `fetchStep3Data`'s `allResolved` (`OnboardingWi | **Server gate.** `unresolvedManifestCount` (today counting `

PARITY docs/superpowers/specs/step3-onboarding/2026-06-29-per-sheet-rescan-wizard.md:42 claim=Three(3) noun=units list@44 count=3(raw 3)
   TEXT: Three units, each independently testable:
   ITEMS: **`rescanWizardSheet(driveFileId, wizardSessionId, deps): Pr | **`POST /api/admin/onboarding/rescan-sheet`** — `app/api/adm | **`RescanSheetButton`** — `components/admin/RescanSheetButto

PARITY docs/superpowers/specs/step3-onboarding/2026-06-30-step3-publish-streaming-progress.md:82 claim=three(3) noun=pieces list@84 count=3(raw 3)
   TEXT: Refactor `handleOnboardingFinalize` (`finalize/route.ts:1029`) into three shared pieces; behavior of the non-streaming path is unchanged:
   ITEMS: `resolveFinalizer(runtime): Promise<{ email: string } | { er | `executeFinalizeBatch(runtime, finalizerEmail, callbacks?):  | `handleOnboardingFinalize(request, deps)` (non-streaming, **

PARITY docs/superpowers/specs/step3-onboarding/2026-06-30-step3-publish-streaming-progress.md:85 claim=two(2) noun=hooks list@86 count=2(raw 2)
   TEXT: ) + the `catch → errorResponse(500, …)` wrapper (`1194-1208`), unchanged EXCEPT two additive callback hooks:
   ITEMS: **`listed` emission point — single, early, before any branch | After each `perRow.push(...)` inside the loop (`finalize/rou

PARITY docs/superpowers/specs/step3-onboarding/2026-06-30-step3-publish-streaming-progress.md:147 claim=two(2) noun=calls list@149 count=2(raw 2)
   TEXT:  an optional `onPhase?: (p: FinalizeCasPhase) => void` parameter, threaded with two additive calls (no-op when absent):
   ITEMS: `onPhase?.("applying")` immediately before the shadow-apply  | `onPhase?.("publishing")` immediately before `publishApplied

PARITY docs/superpowers/specs/step3-onboarding/2026-07-01-unknownfield-row-anchor.md:67 claim=Two(2) noun=properties list@69 count=2(raw 2)
   TEXT:  **provenance** (that the single match is the row the parser actually flagged). Two independent properties close the wrong-cell risk:
   ITEMS: **Over-inclusive extractor bound** (above): the parser's emi | **(label, value) match**: even under a hypothetical bound di

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-02-step3-review-modal-redesign.md:12 claim=3(3) noun=details list@14 count=5(raw 5)
   TEXT: Replace the wizard Step-3 details overlay (`components/admin/wizard/Step3DetailsDialog.tsx`, mounted by `Step3SheetCard.tsx:1715-1764`) with a full review
   ITEMS: **Header:** eyebrow "Review before publishing", sheet-title  | **Two-pane body (desktop):** left rail index of sections gro | **Narrow body (< two-pane threshold):** rail collapses to a  | **Phone (`< sm`):** bottom sheet with a real drag-to-dismiss | **Footer:** parse-status note, "Re-scan this sheet" (existin

PARITY docs/superpowers/specs/step3-onboarding/2026-07-03-step3-modal-followups.md:102 claim=Two(2) noun=details list@103 count=2(raw 2)
   TEXT: herwise `snapshotFetchEmbeddedImageBytesTimed(stub)` (no `fetchXlsxBytes` dep). Two contract details the helper imposes:
   ITEMS: **Throws are NOT fail-soft in the helper** — it returns `nul | **Return type is a union** — `Promise<SnapshotAssetBytes | n

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-03-step3-modal-followups.md:280 claim=two(2) noun=specs list@281 count=5(raw 5)
   TEXT: Real-browser (extend the two existing standalone-config specs + harnesses; `tests/e2e/_step3ReviewModalHarness.tsx` / `_step3ReviewModalLiveEntry.tsx`):
   ITEMS: Nav race: click a far section; sample `aria-current` on rail | Indicator: after click+settle, indicator `getBoundingClientR | Jump-link: click a callout "View details" → scroller lands w | Footer no-shift: stub `fetch` in the live entry; click Re-sc | Layout: diagrams grid no-horizontal-scroll assertion per §I 

PARITY docs/superpowers/specs/step3-onboarding/2026-07-04-step3-review-page-variant-b.md:14 claim=Four(4) noun=changes list@16 count=4(raw 4)
   TEXT: Four visible changes, full-fidelity, reconciling every real state:
   ITEMS: **Stepper** — redesign the shared `StepIndicator` (numbers-o | **Header** — `h1` "Review what we found" + a **composed summ | **Sheet list** — replace the responsive grid of expandable ` | **Sticky publish bar** — a bottom bar with the live selected

PARITY docs/superpowers/specs/step3-onboarding/2026-07-05-step3-review-consolidation.md:11 claim=four(4) noun=surfaces list@13 count=4(raw 4)
   TEXT: Today `/admin` renders one of four surfaces depending on the finalize checkpoint (`app/admin/page.tsx:157-208`):
   ITEMS: `checkpoint.status === "in_progress"` → `<FinalizeInProgress | `checkpoint.status === "all_batches_complete"` → `<ReadyToPu | `checkpoint.status === "final_cas_done"` → `<DashboardWithHe | `checkpoint === null` → `<OnboardingWizard>` (the wizard, in

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-05-step3-review-consolidation.md:236 claim=three(3) noun=surfaces list@237 count=4(raw 4)
   TEXT: t. The full freeze list (HIGH R8 — the prior spec only froze the modal, leaving three live race surfaces):
   ITEMS: **Publish checkbox** (`PublishCheckbox`, `Step3SheetCard.tsx | **Row-level `Re-scan this sheet`** (`RescanSheetButton`, `St | **Inline blocking-row controls** (`HardFailedActions` / `Man | **`Review →`** trigger AND **every modal mutator** — `Approv

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-05-step3-review-consolidation.md:284 claim=3(3) noun=modes list@286 count=4(raw 4)
   TEXT: The unified Step-3 has these modes; each row + footer element belongs to exactly one:
   ITEMS: **Pre-finalize** (`checkpoint null`): rows Ready (checkbox)  | **Mid-finalize** (`in_progress`): rows Live / **Ready-to-pub | **Batches-complete** (`all_batches_complete`): rows Live / * | **Modal** (any mode, Needs-review row): resolution UI; indep

PARITY docs/superpowers/specs/step3-onboarding/2026-07-07-flow1-onboarding-empty-state-alert.md:12 claim=Three(3) noun=gaps list@14 count=3(raw 3)
   TEXT: Flow 1 ("Add a new show": Drive drop → scan → wizard) grades **B**. Three gaps drop it below A−:
   ITEMS: **1.1 (UI).** A scan that stages nothing renders as a muted  | **1.2 (UI).** Step 1 already covers folder creation ("or mak | **1.3 (backend + catalog).** The onboarding scan's `hard_fai

PARITY docs/superpowers/specs/step3-onboarding/2026-07-07-flow1-onboarding-empty-state-alert.md:160 claim=two(2) noun=layers list@161 count=2(raw 2)
   TEXT: The card's lower region has **two structural layers** (matching the existing component, `Step2Verify.tsx:397-483`):
   ITEMS: **Submitting:** the **progress readout** REPLACES the entire | **Not submitting** (`idle` / `success` / `error`): an OPTION

PARITY docs/superpowers/specs/step3-onboarding/2026-07-07-flow3-correction-loop-clarity.md:19 claim=three(3) noun=surfaces list@20 count=3(raw 3)
   TEXT: **In scope:** copy + small layout additions on three existing surfaces:
   ITEMS: Per-show admin page "Data quality" section — `app/admin/show | Onboarding wizard step-3 `WarningsBreakdown` — `components/a | Changes feed row — `components/admin/ChangeFeedEntry.tsx`.

PARITY docs/superpowers/specs/step3-onboarding/2026-07-07-flow4-auto-applied-strip-roster-badge.md:13 claim=Two(2) noun=gaps list@15 count=2(raw 2)
   TEXT: Flow 4 is "re-sync that changed data on a live show." Two gaps remain after 4.1:
   ITEMS: **4.2** — When the cron sync auto-applies an unambiguous cre | **4.3** — A show whose **roster** (crew membership) shifted 

PARITY docs/superpowers/specs/step3-onboarding/2026-07-07-flow4-auto-applied-strip-roster-badge.md:133 claim=Two(2) noun=properties list@134 count=2(raw 2)
   TEXT: the group's DISPLAYED ids, ≤ `STRIP_RENDER_CAP`; per-row Accept → `ARRAY[id]`). Two properties hold together:
   ITEMS: **Visibility-safe (R4-F1):** because `p_ids` is only ids the | **Bounded (R3-F3):** the id set is capped at `STRIP_RENDER_C

PARITY docs/superpowers/specs/step3-onboarding/2026-07-14-recent-auto-applied-redesign.md:15 claim=4(4) noun=files list@17 count=4(raw 4)
   TEXT: **In scope (4 source files):**
   ITEMS: `lib/admin/loadRecentAutoApplied.ts` — add a PII-safe, name- | `components/admin/RecentAutoAppliedStrip.tsx` — render each  | `components/admin/AcceptChangeButton.tsx` — add an **optiona | `components/admin/UndoChangeButton.tsx` — same optional `str

PARITY docs/superpowers/specs/step3-onboarding/2026-07-16-onboarding-sheet-unreadable-hybrid-lifecycle.md:11 claim=two(2) noun=properties list@13 count=2(raw 2)
   TEXT: T_UNREADABLE` (emitted at `app/api/admin/onboarding/scan/route.ts:299-311`) has two operator-hostile properties, both confirmed against live code:
   ITEMS: **Not actionable in place.** Context carries only `folder_id | **Goes stale while the condition heals.** The cron tick firs

PARITY docs/superpowers/specs/step3-onboarding/2026-07-16-onboarding-sheet-unreadable-hybrid-lifecycle.md:89 claim=Two(2) noun=functions list@91 count=2(raw 2)
   TEXT: Two exported functions:
   ITEMS: **`resolveOpenUnreadableAlertUnconditionally()`** — scan-rou | **`resolveUnreadableAlertIfHealed(input)`** — cron observer,

PARITY docs/superpowers/specs/step3-onboarding/2026-07-16-onboarding-sheet-unreadable-hybrid-lifecycle.md:168 claim=Two(2) noun=regressions list@169 count=2(raw 2)
   TEXT: g → untouched even when all ids satisfied; empty/missing ids array → untouched. Two negative regressions pinned by review R1:
   ITEMS: **stale-staged (finding 1)**: seed a LIVE `pending_syncs` ro | **same-id replacement race (finding 2)**: read the open row,

PARITY docs/superpowers/specs/step3-onboarding/2026-07-16-withdraw-rescan-guard.md:19 claim=three(3) noun=outcomes list@20 count=3(raw 3)
   TEXT: RescanDecisionUnderLock` (`lib/onboarding/applyRescanDecisionUnderLock.ts`) has three non-failure outcomes (union at lines 46-53), selected by the `isDirty` gate at lines 286-289:
   ITEMS: `clean_restamped` — previously-ready + clean → **approval re | `clean_unchecked` — was not ready → stays unapproved (lines  | `dirty_demoted` — demotes `wizard_approved` (lines 291-303).

PARITY docs/superpowers/specs/step3-onboarding/2026-07-17-archived-tab-resolve-box-offer.md:14 claim=Two(2) noun=facts list@16 count=2(raw 2)
   TEXT: Two facts make the decision hard to find:
   ITEMS: **The "Resolve before publishing" box cannot show it.** That | **The box usually is not even present.** `resolution` is def

PARITY docs/superpowers/specs/step3-onboarding/2026-07-17-auto-applied-header-parity.md:12 claim=Two(2) noun=defects list@14 count=2(raw 2)
   TEXT: edsAttentionInbox`, and the `RecentAutoAppliedStrip` ("Recently auto-applied"). Two defects, reported from a live screenshot on the validation deploy:
   ITEMS: **Detached gap.** When the inbox is populated, a large empty | **Header asymmetry.** The "Needs attention" header carries a

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-17-autoapplied-kinddot-nonocolor-tell.md:99 claim=three(3) noun=entries list@101 count=4(raw 4)
   TEXT: Live-code verification found three entries stale on `origin/main` (plus KINDDOT-1, resolved by this PR):
   ITEMS: **AUTOAPPLIED-COLLAPSE-1** ("collapsed header hides change k | **AUTOAPPLIED-COLLAPSE-2** ("panel mounts/unmounts instantly | **AUTOAPPLIED-REDESIGN-2** ("singleton group renders card-in | **KINDDOT-1** — mark ✅ RESOLVED by this PR.

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-17-autoapplied-strip-polish.md:324 claim=3(3) noun=kinds list@325 count=2(raw 2)
   TEXT:  stays compact on a 320px phone. (Realistically ≤3 distinct kinds co-occur.)
   ITEMS: **Unknown kind** — any `changeKind` outside `KIND_ORDER` map | **The dots are a non-authoritative triage preview, not a saf

PARITY docs/superpowers/specs/step3-onboarding/2026-07-17-psat1-durable-override-dto.md:194 claim=Two(2) noun=assertions list@196 count=2(raw 2)
   TEXT: — source_anchors threading")` block; the SELECT-column assertion is at `:291`). Two assertions, both **fail-first on `origin/main`**:
   ITEMS: **SELECT column present:** after `fetchStep3Data(SESSION_ID) | **Durable value threaded onto the row:** seed `pending_syncs

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-17-s3c-review-a11y.md:24 claim=two(2) noun=sites list@26 count=3(raw 3)
   TEXT: The tone helper and the two dot render sites:
   ITEMS: `components/admin/review/ShowReviewSurface.tsx:227-231` — `d | `components/admin/review/ShowReviewSurface.tsx:683-688` — ** | `components/admin/review/ShowReviewSurface.tsx:746-751` — **

MISMATCH docs/superpowers/specs/step3-onboarding/2026-07-17-wizard-blocker-modal-design.md:153 claim=two(2) noun=surfaces list@155 count=7(raw 7)
   TEXT:  mounts on top of the open review modal. Defined behavior (no suppression — the two are independent surfaces owned by different components):
   ITEMS: **Z-order:** the blocker modal portals to `document.body` an | **Focus:** `useDialogFocus` moves focus INTO the blocker pan | **Escape:** `Step3ReviewModal`'s Escape listener is a **bubb | **Scroll lock:** each modal saves `document.body.style.overf | **Single exposed modal root (AT — resolves the two-`aria-mod | **Cleanup ORDER (focus continuity) — CORRECTED at implementa | This compound is exercised by a component test (§10.11), whi

PARITY docs/superpowers/specs/step3-onboarding/2026-07-17-wizard-blocker-modal-design.md:174 claim=two(2) noun=things list@176 count=2(raw 2)
   TEXT: **Real-browser Playwright task (mandatory, jsdom insufficient) — proves the two things Doug's bug and §7a require:**
   ITEMS: **No footer growth (the actual regression):** measure `wizar | **Viewport-pinned + top-of-stack:** with the blocker open, a

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-12-user-facing-docs-design.md:519 claim=Three(3) noun=classes list@520 count=3(raw 3)
   TEXT: nism:** the test reads §5.6's matrix (as a typed `affordanceMatrix.ts` import). Three row classes:
   ITEMS: **Concrete-testid rows** (E2E via Playwright): walks each ro | **Template-family row (UNIT-LEVEL, not E2E — r7):** the rend | **Negative-assertion row** (E2E): navigates as a signed-link

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation-design.md:32 claim=Four(4) noun=bug-classes list@34 count=4(raw 4)
   TEXT: Four bug-classes the dev-in-a-real-browser pass is shaped to surface, and which automated layers structurally cannot:
   ITEMS: **Composition / cross-surface friction.** Each surface passe | **Craft gap.** Heuristic-pass UI (impeccable APPROVED) that  | **Real-device behavior.** Playwright at 390px viewport is no | **Reality-of-data / cold-start unfamiliarity.** Fixtures are

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation-design.md:377 claim=THREE(3) noun=edits list@378 count=3(raw 3)
   TEXT: 3. **Master spec §4.3 amendment (THREE edits — picker-pivot rebase)** —
   ITEMS: **§4.3 bullet list (prose track)** — `docs/superpowers/specs | **§4.3 footnote (NEW — picker-pivot drift documentation)** — | **§4.1 schema section — `create table validation_state` defi

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation-design.md:514 claim=three(3) noun=dispositions list@516 count=3(raw 3)
   TEXT: ndidate row produced by the derivation lands in MATRIX-INVENTORY.md with one of three dispositions:
   ITEMS: `INCLUDED` — exercised in matrix walk. Mapped to one of band | `EXCLUDED — <reason>` — explicitly out of scope. Reason cite | `BAND-OVERLAP — <other row id>` — captured by another includ

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation-design.md:783 claim=10(10) noun=days list@785 count=3(raw 3)
   TEXT:  (R23 amendment; R25 governance refinement).** If Phase 0 has not closed within 10 calendar days of starting (excluding standard infrastructure-provisioning latency — Supabase project creation, Drive service ac
   ITEMS: **Defer non-essential validation tooling.** `validation:repo | **Split Phase 0 into its own prerequisite milestone.** Re-nu | **Re-scope the walk.** If tooling is genuinely consuming the

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation-design.md:1393 claim=Three(3) noun=options list@1394 count=3(raw 3)
   TEXT: ster-spec amendment decision (α + footnote hybrid; user-ratified 2026-05-26).** Three options were on the table during amendment kickoff:
   ITEMS: **(α)** Naïve bump 21→22 in §4.3 prose / 84→88 in AC-2.5, le | **(β)** Honest count with lockstep cleanup — drop the 4 M9.5 | **(γ)** Document-as-stale — leave master count at 21; surfac

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-19-solo-dev-ux-validation-design.md:1427 claim=Two(2) noun=drifts list@1429 count=2(raw 2)
   TEXT: .C DDL/RPC/CHECK/policy/GRANT reference was audited against spec §3.3.2 + §3.3. Two additional spec-vs-plan drifts surfaced (both adjacent to F7, not standalone findings):
   ITEMS: `validation_finalize_all_atomic` RPC contract — plan `03-pha | check-seed predicate (i) — plan `03-phase0-tooling-reseed.md

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:66 claim=two(2) noun=surfaces list@68 count=2(raw 2)
   TEXT: url(hmac_sha256(payload, secret))>`. **R41-R20 + R41-R22 mint-path contract** — two surfaces mint a valid cookie:
   ITEMS: **`selectIdentity` Server Action** (bypass-pick path): uses  | **`/api/auth/picker-bootstrap` Route Handler** (lazy-mint pa

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:483 claim=two(2) noun=things list@485 count=2(raw 2)
   TEXT:  JWT-gated subscription via `/api/realtime/subscriber-token`) is **kept**. Only two things change:
   ITEMS: **Auth source for the subscriber-token endpoint.** Pre-pivot | **`viewer_version_token` SQL function.** Per migration `2026

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:1508 claim=two(2) noun=sub-cases list@1509 count=3(raw 3)
   TEXT: **Google-session auto-resolution (R41-R2 revised — redirect-bootstrap flow)** — two sub-cases:
   ITEMS: **Sub-case A: cookie already present** (normal flow — user s | **Sub-case B: cookie missing** (edge: Doug added user to sho | **Negative test (P-R27 Fix-1 hardened)**: Google session who

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:1515 claim=two(2) noun=states list@1516 count=2(raw 2)
   TEXT: us states removed)** — picker rendered with a roster containing crew members in two states:
   ITEMS: **State A (OAuth-claimed)**: `claimed_via_oauth_at IS NOT NU | **State B (clean unclaimed)**: `claimed_via_oauth_at IS NULL

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:1524 claim=Three(3) noun=scenarios list@1525 count=3(raw 3)
   TEXT: ss pick for Alice's crew row; Alice's `claimed_via_oauth_at` is currently NULL. Three transaction interleave scenarios:
   ITEMS: **Scenario A: lock-serial, no contention** — T0 Mallory's se | **Scenario B: R41-R23 CRITICAL — claim begins first but bloc | **Scenario C: R41-R22 same-millisecond tie** — Mallory's obs

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:1540 claim=Three(3) noun=scenarios list@1541 count=4(raw 4)
   TEXT: s the prior fill-in-the-blanks tests (which over-promised cross-handler merge). Three scenarios using FIXED `Cookie:` request headers (modeling browser snapshot at request-send time, NOT live merge):
   ITEMS: **Callback writes NO cookies, EVER**: fixture: user has crew | **Picker-bootstrap writes ONE SHOW's entry**: fixture: user  | **Picker-bootstrap for Show-X racing selectIdentity for Show | **Same-show race, picker-bootstrap vs selectIdentity for Sho

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md:1546 claim=three(3) noun=sub-scenarios list@1547 count=4(raw 4)
   TEXT: aim picker cookie is invalidated after OAuth claim (R41-R8 HIGH regression)** — three sub-scenarios:
   ITEMS: **Sub-A: a user picked bypass identity, then a different use | (Sub-B REMOVED — R41-R35; fixture relied on a duplicated-ema | **Sub-C: the OAuth user's OWN cookie is preserved (negative  | **R41-R30 fractional-millisecond regression (NEW)**: DB-leve

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot-design.md:16 claim=Three(3) noun=paths list@18 count=3(raw 3)
   TEXT: Three resolution paths were considered (orchestrator triage, 2026-05-26):
   ITEMS: **A.** Upgrade `eric-weiss-projects` → Vercel Pro ($20/mo).  | **B.** Move project to an existing Pro team. Foreclosed — `v | **C.** Strip frequent crons from `vercel.json` and defer smo

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot-design.md:41 claim=Two(2) noun=findings list@43 count=2(raw 2)
   TEXT: Two findings from live-code grep (2026-05-26, pre-draft citation pass per AGENTS.md "live-code citation pass" discipline):
   ITEMS: **All 7 cron route handlers already validate `Authorization: | **`pg_cron` extension is already installed** and `cron.sched

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-26-pg-cron-pivot-design.md:149 claim=Five(5) noun=tasks list@151 count=5(raw 5)
   TEXT: Five sub-amendment tasks (sequenced):
   ITEMS: **M12.1.T1 — `vercel.json` crons removal.** Single-commit ed | **M12.1.T2 — `pg_net` extension + Vault `fxav_cron_secret` e | **M12.1.T3 — 7× `cron.schedule()` migration.** One migration | **M12.1.T4 — Structural defenses (meta-tests).** Two test fi | **M12.1.T5 — M12 plan amendment.** Edit `docs/superpowers/pl

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-31-m12.2-phase-a-admin-dashboard-per-show-design.md:412 claim=two(2) noun=streams list@413 count=2(raw 2)
   TEXT:  model is exactly **two streams**:
   ITEMS: `pending_ingestions` (→ `pending_ingestion` items), | `pending_syncs` (→ classified into `first_seen` / `existing_

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-05-31-m12.2-phase-b1-admin-nav-settings-design.md:237 claim=three(3) noun=helpers list@238 count=4(raw 4)
   TEXT: ies an inline `// not-subject-to-meta` comment + a behavioral test. Each of the three REGISTERED helpers MUST (a) destructure `{ data/count, error }`, (b) distinguish returned-error from thrown-builder/await an
   ITEMS: **NotifBell shared alert-count helper** (§2.4) — `admin_aler | **`fetchDriveConnectionHealth()`** (§3.1) — composes the NEW | **`fetchEmbeddedAdminEmails()`** (§3.2) — wraps `listAdminEm | **`getActiveWatchedFolder()`** (§3.1) — the NEW sibling of `

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-02-m12.2-phase-b3-email-delivery-design.md:232 claim=three(3) noun=layers list@234 count=3(raw 3)
   TEXT: sages/catalog.ts:54-64`). x1 catalog-parity asserts all fields agree across the three lockstep layers, so the spec pins each field (final copy in the catalog; em-dash-free):
   ITEMS: **`SYNC_STALLED`** (show-wide; AlertBanner/NotifBell + realt | **`EMAIL_DELIVERY_FAILED`** (in-app only — never emailed): | **`EMAIL_NOT_CONFIGURED`** (in-app + optional settings hint)

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-02-m12.2-recon1-alert-banner-quieting-design.md:142 claim=Two(2) noun=triggers list@143 count=2(raw 2)
   TEXT: owned by the **browser/DOM**, not React, so a re-render does NOT auto-reset it. Two distinct remount triggers are needed, because the banner is mounted in the **persistent** `app/admin/layout.tsx` (which Next.j
   ITEMS: **Alert identity change (F9)** — resolve→next alert, new ale | **Same-alert route navigation (F17)** — operator expands on 

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate-design.md:109 claim=Two(2) noun=points list@110 count=3(raw 3)
   TEXT: - **Lock topology + PostgREST lockdown (single-holder rule, invariant 2).** Two entry points write `sync_holds`, each acquiring the per-show advisory lock at **exactly one** layer, never nested:
   ITEMS: **Sync/apply path** — the JS wrapper already holds `pg_advis | **Admin actions** (Approve/Reject/Undo) run OUTSIDE any JS-h | **PostgREST DML lockdown:** `REVOKE INSERT, UPDATE, DELETE O

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate-design.md:125 claim=two(2) noun=directions list@126 count=2(raw 2)
   TEXT: - **`crew_identity` hold — two directions (undo, per §6.3):**
   ITEMS: **held-present** (undo of a removal/rename): `held_value=<pr | **held-absent / tombstone** (undo of a `crew_added`): `held_

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-08-sync-changes-feed-identity-gate-design.md:202 claim=Two(2) noun=directions list@203 count=4(raw 4)
   TEXT: lied` **crew-domain** entry → **Undo** (admin-gated, lock-taking RPC per §4.1). Two symmetric directions:
   ITEMS: **Undo of a removal or rename** (`crew_removed` / MI-12/13/1 | **Undo of an add** (`crew_added`, finding F11): there is **n | The show **keeps syncing**; only the held entity is pinned ( | **Release** per §4.3: when the sheet's value for that entity

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-10-mobile-needs-attention-design.md:69 claim=Two(2) noun=deviations list@70 count=2(raw 2)
   TEXT:  (`.limit(cap + 1)`), two head-counts, existence lookup, `buildNeedsAttention`. Two deliberate deviations from "verbatim":
   ITEMS: `cap` replaces the hardcoded `RENDER_CAP` in the queries and | **Count integrity (R2-F3):** the current block falls back fr

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-10-onboarding-fixups-design.md:12 claim=two(2) noun=writers list@14 count=2(raw 2)
   TEXT: The onboarding wizard's two apply writers persist **only `shows` columns** and silently drop every child table in the staged `parse_result`:
   ITEMS: **First-seen branch:** `applyFirstSeenDraft` — `app/api/admi | **Existing-show branch (Phase D):** `applyShadow` — `app/api

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-10-onboarding-fixups-design.md:55 claim=11(11) noun=holds list@57 count=11(raw 11)
   TEXT: se2.ts:328-335` `applyParseResult` with `holds: { port, baseModifiedTime }`, MI-11 holds written before the apply at `phase2.ts:297-335`, `writeAutoApplyChanges` feed rows at `phase2.ts:337-350`):
   ITEMS: **Unit boundary:** extract the "apply a staged parse_result  | **Source-scoped live-partition cleanup — the WHOLE class, no | **Shadow payload completeness (R1 finding 1).** The payload  | **Concrete MI-11 boundary (R4 finding 2).** The legacy whole | **Fail-closed on missing/corrupt review items (R2 finding 1) | **Required test (R1 finding 1):** an existing-show wizard ap | `baseModifiedTime` for the holds context = the payload's `st | The Phase D staleness gate is REPLACED, not preserved (R21 f | **Per-row commit independence is RATIFIED, not an oversight  | Feed + MI-11 behavior follows D-2: notable auto-applied chan | Audit attribution unchanged (payload's `applied_by_email` / 

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-11-affordance-matrix-realignment-design.md:81 claim=Two(2) noun=props list@83 count=2(raw 2)
   TEXT: Two new optional props; all existing call sites compile unchanged:
   ITEMS: `rootTestId?: string` — placed on the root wrapper, which ** | `learnMore?: { href: string }` — when present, the popover b

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-15-crew-page-redesign-phase1-design.md:133 claim=Two(2) noun=changes list@135 count=2(raw 2)
   TEXT: Two changes, both small and bounded:
   ITEMS: **`buildRightNowContext` reads `rooms`.** New signature: `{  | **Dates parser captures the load-in TIME column.** `parseV2V

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-15-crew-page-redesign-phase1-design.md:384 claim=two(2) noun=surfaces list@385 count=2(raw 2)
   TEXT: **Validation-apply matrix — two distinct surfaces, do not conflate:**
   ITEMS: **Function migration** (`upsert_admin_alert` dedup, §6b) — * | **Parser / jsonb change** (`dates.loadIn`, §6a) — **no migra

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-15-crew-page-redesign-phase1-design.md:461 claim=three(3) noun=layers list@462 count=3(raw 3)
   TEXT: 16. **Section error containment** (§4.13) — three layers:
   ITEMS: **Projection alert (section-independent, render-bound, serve | **Visual fallback (active section).** With the relevant sect | **Render throw.** A throw injected into a wrapped block → `T

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-15-crew-page-redesign-phase1-design.md:499 claim=4(4) noun=phases list@500 count=4(raw 4)
   TEXT: Single milestone on `feat/crew-page-redesign`, ~4 phases:
   ITEMS: **Parser + context** — dates `loadIn` capture (TDD) + `build | **Shell + nav + primitives + projection alert** — `CrewShell | **Sections** — the six sections + Budget; Gear emphasis; emp | **Layout/transition/screenshots + close-out** — Playwright d

MISMATCH docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-19-crew-mock-fidelity.md:76 claim=Three(3) noun=misses list@77 count=4(raw 4)
   TEXT: ile fixed bottom bar (`:127-132`), **both text-only, with no width container**. Three real misses:
   ITEMS: **Desktop off-center.** The desktop `<nav>` has NO `max-widt | **Desktop missing icons.** The mock `.navtab` renders a **16 | **Mobile missing icons + smooshed.** The mock mobile bar `.b | **Preserve** the existing URL allow-list discipline (`naviga

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-21-split-wide-natural-height.md:14 claim=two(2) noun=columns list@16 count=2(raw 2)
   TEXT: esktop review the owner observed dead space below the short column whenever the two columns were very unbalanced:
   ITEMS: **Schedule:** the ~3-row "Daily call times" card stretched t | **Crew:** the ~2-contact "Key contacts" card stretched to th

PARITY docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-23-parse-data-quality-warnings-design.md:19 claim=Three(3) noun=mutations list@21 count=3(raw 3)
   TEXT: eted tabs, inserted rows, mixed date formats, curly quotes all parsed clean). **Three mutations dropped data with zero operator signal and the show still published** (`parse_warnings = []`, `last_sync_status = 
   ITEMS: **VB08** — a crew phone cell containing `"call John"` (non-n | **VB09** — an unknown section `CATERING | NAME | PHONE` appe | **VB10** — the hotel/lodging block cleared entirely: hotels 

PARITY docs/vision.md:82 claim=Five(5) noun=principles list@84 count=5(raw 5)
   TEXT: ## Five principles
   ITEMS: **Replace the spreadsheet — don't reproduce it.** If a desig | **Glanceable on the floor.** Lead with the answer; bury the  | **One accent, used sparingly.** FXAV orange means "this matt | **Both modes equally beautiful.** Each is designed against i | **Plain language, never technical chrome.** Empty states, er
