# M12.3 — Admin UI fidelity v2

**Date:** 2026-06-03 · **Owner:** Opus + impeccable v3 dual-gate (UI invariant 8) · **Routing:** UI → Opus.

**Source of need:** Post-RECON-1 live review of the validation deployment (`fxav-crew-pages-validation.vercel.app`) against the design bundle (`.validation-local/design-admin/fxav-crew-pages/project/` — `Admin Redesign.html` + `screenshots/`). The design bundle is the **visual target to recreate, NOT code to copy**. The §E-ratified drops (rail nav → top nav, density/accent toggles) still hold; this milestone matches the **top-nav variant** of the design.

**Design density tokens (from `Admin Redesign.html`):** default `--pad:20px --gap:16px --section:30px`; **compact `--pad:14px --gap:11px --section:22px`**. The "more compact" directive = move admin spacing toward the compact scale (smaller tile padding, tighter gaps, smaller section gaps) without breaking the existing token system (DESIGN.md). Prefer existing tokens; if a denser token is needed, add it to the theme, never hardcode.

**Scope split (owner, 2026-06-03):**
- **This doc = COSMETIC/LAYOUT batch** (ship now): items 1–8, 10, 12, 14 + 15a (hide-when-empty).
- **Features (separate specs):** #11 mobile "Needs attention" page; #13 granular per-change staged-review (+ #15b redundancy/removal) — `#13` gated on a backend-feasibility check first.
- **Investigate (separate):** #9 notify-prefs degraded read on the live deploy (production bug).

---

## Cosmetic/layout items (acceptance per item)

### Banner (global `AlertBanner`)
1. **Dashboard-only.** Remove `<AlertBanner/>` from the persistent `app/admin/layout.tsx` (currently the `<div id="alerts"><AlertBanner/></div>` mount, ~:116). Mount it on the **dashboard page only** (`app/admin/page.tsx`). Settings + per-show no longer show the global banner (per-show keeps its own "Alerts for this show" section — avoids the double-render seen on the live deploy). **Supersedes RECON-1 spec §8 "single layout mount" + "verify per-show inherits it"** — owner reversal: the global banner is a dashboard concern. Keep the `#alerts` scroll-anchor on the dashboard's inbox so the queue chip's `/admin#alerts` still lands.
2. **Position: under the "Dashboard" title.** Render the banner BELOW the `<AdminPageHeader title="Dashboard">` and ABOVE the stat strip (matches the prototype dashboard, where the calm strip sits between the page header and the stat cards). Keep the `AlertBannerRouteBoundary` remount boundary (still needed for query/alert-identity collapse within the dashboard).
3. **Collapsed line = first complete sentence**, not a mid-word truncation. Extract `lib/messages/collapsedSummary.ts`: `firstSentence(s)` = first match of `/^(.*?[.!?])(?=\s|$)/` (the `(?=\s|$)` lookahead skips decimals like "1.5"; em-dashes are preserved), fallback to the whole string; `stripEmphasis(s)` removes `**bold**`, `*em*`, AND `_em_` using CommonMark-style word-boundary rules for underscore (so internal underscores in tokens like `(SW-POST_SHOW)` are NOT stripped). Collapsed line = `stripEmphasis(firstSentence(dougFacing))`; keep `truncate` as a safety net. Full message stays in the expanded panel (ErrorExplainer). Unit-test the helper with: decimals, em-dash, underscore-with-internal-underscore, double-asterisk, no-period fallback, empty.

### Dashboard
4. **Content full-width on desktop.** Raise/remove the `max-w-*` cap on the dashboard container so the content uses the full width like the design (the stat strip + Active-shows/Needs-attention two-column use the available width).
10. **Active shows table → design formatting + working search.** Restyle `ShowsTable` to the design's clean table (header row + light row dividers, denser rows) instead of heavy boxed/card rows. Ensure the "Find" search is present AND **actually filters** the show list by name (verify the current control isn't a non-functional placeholder; wire client-side filter if needed).
14. **Global compactness** (dashboard portion): tighten stat tiles, table rows, and section gaps toward the compact density scale.

### Settings (whole-page restructure to the design)
5. **LP1 — "ADMIN" eyebrow.** Add an eyebrow to `components/admin/nav/AdminPageHeader.tsx` (`<p>` uppercase, `tracking-eyebrow`, `text-text-subtle`, text "Admin"), rendered above the title — but ONLY when there is no `crumb` (dashboard + settings get the eyebrow; per-show already has the "Admin › Active shows" breadcrumb, so suppress the eyebrow there to avoid redundancy). The prior reconciliation mis-reported this as shipped; it never rendered.
6. **LP2 — "Preferences" header** outside/above the toggle group.
7. **LP3 — toggle icons + grouped card.** Group the three Preferences rows (Alert-me, Auto-publish, Developer-tools) into ONE bordered card with internal dividers; each row gets a leading lucide icon (bell / sparkles / shield-like). NotifyToggle/AutoPublishToggle/DevToolsRow change from standalone `<section>` cards to rows within the group; add an `icon` prop.
8. **Drive connection panel layout.** Group both buttons ("Open folder" + "Re-run setup") together on the **right** with the helper text on the left (one row, buttons grouped), and style "Re-run setup" as a **neutral/outline** button (not orange/accent) to match the design.
12. **Settings page restructure:** (a) **left-align** the content (remove the centering / left-anchor the container); (b) **section titles OUTSIDE the cards** — "Drive connection", "Administrators", "Preferences" as headings above their cards; (c) **"Add admin" button** in the Administrators heading row (outside the card, right); (d) **Add-admin form hidden** until "Add admin" is pressed (disclosure).
14. **Global compactness** (settings portion): tighten card padding + gaps toward the compact scale.

### Per-show
15a. **Hide "Parse warnings"/staged-review section when empty** (no staged changes) — quick win. (Full redesign + redundancy-with-alerts removal = #13/#15b feature spec.)
16. **Per-show header → design treatment.** The live header renders the full long crew URL in a wide text input + a "Slug: …" subtitle; the design uses a **compact crew-link chip** (short display + copy affordance, not the full URL) and a **client · dates** subtitle. Restyle: compact chip, subtitle = client · dates, tighten to the compact density. **Supersedes the RECON-2/G3 "accept per-show subtitle drift" disposition** (owner now prefers the design header).
18. **Remove the "Slug: <slug>" line from the per-show header entirely** (owner: Doug is a technical layman; the slug is meaningless to him). The slug stays in the URL/routing + any developer surface, but is NOT shown in the per-show header chrome.

### Header / brand
17. **FXAV brand icon missing.** `AdminNav` renders "FXAV" as text only (`AdminNav.tsx:52-54`); the design shows an orange brand mark left of "FXAV ADMIN". **Asset (owner-provided): `public/brand/fxav-wordmark.png`** — an orange infinity/lens mark over "FX Audio Visual" (vertical lockup). Crop the orange mark to a square icon (`public/brand/fxav-icon.png` via `sips`/ImageMagick) and render it (`next/image`, ~24-28px, `alt=""` decorative since "FXAV" text follows) before the "FXAV" wordmark + Admin badge in the nav brand link. Mobile top bar gets the same mark.

---

## Invariants preserved
- No raw error codes (invariant 5) — banner copy stays catalog-sourced.
- Fail-visible degraded path, two-tap resolve form boundary, AlertBannerRouteBoundary remount-collapse (now scoped to the dashboard).
- TDD per change; impeccable v3 dual-gate (external) + cross-model adversarial review of the diff before merge.
- Real-browser layout verification (the design density + table + settings restructure need a Playwright render check, not just jsdom).

## Out of scope (separate)
- #9 notify-prefs degraded read (investigate/fix on the validation project).
- #11 mobile Needs-attention page (feature spec).
- #13 granular per-change staged-review + #15b removal (feature spec, backend-feasibility-gated).

## CI note (2026-06-03)

Admin screenshot baselines (`dashboard-overview` + `review-queues-empty-state`, dark+light) were regenerated on the pinned amd64 runner (`bf767bd8`) after the dashboard restructure. Required fixing `screenshots-regen.yml` to hold aside BOTH cron migrations (it was missing B3 `20260602000005`), syncing it with `screenshots-drift.yml` (`bc3ff12d`).

- Final regen `34547fa1` (2026-06-03): dashboard-overview baselines refreshed after the eyebrow-color (text-faint) + timezone date-correction (formatDateRange UTC) changes. Gates: impeccable dual-gate PASS + cross-model adversarial review APPROVED at R4 (R1 Find-capped, R2 e2e-route, R3 date-TZ fixed).
