# M-wave 2 — second READY + decision-unlocked mid-size implementation wave

**Date:** 2026-08-09 · **Arc branch:** `docs/m-wave-2-spec` · **Status:** DRAFT (pre-review)

## §0 Why this wave exists, and its baseline

The first M-wave (`docs/superpowers/specs/2026-08-05-m-wave-design.md`, closed 2026-08-06 with PR #717/#718) cleared the then-READY mid-size queue. The tier has since refilled — decomposition children, new filings from the parser-mutation and quick-wins arcs, and L-wave resizes. This wave is the same screen re-run: every open M entry that is READY to implement or was blocked on exactly one product decision the user has now made. Pool selection and every product decision were ratified by the user on 2026-08-09 (two AskUserQuestion batches, captured in `docs/superpowers/specs/2026-08-09-m-wave-2-decisions-brief.md`, committed with this spec); the wave runs fully autonomously under the AGENTS.md autonomous-ship gate.

Baseline census (`pnpm ledger:mass`, 2026-08-09 19:05 EDT, main at post-#752 `d2a31e4aa`): **77 open entries (XS 4 / S 24 / M 36 / L 13), mass 365, unsized 0, severity-unrecognized 0.** AC-PROG pins against these numbers.

Wave pool: **18 claimed entries** — 16 implementation entries forming 15 work items (the two RIGHTNOW rows are one piece of work by their own text) plus 2 docs-only demotes. All 18 carry invariant-12 markers on this branch (Stage-0 commit `8f6378056`, pushed; `pnpm ledger:claims --check` over all 18 returned `no collision`). Six themed units:

| Unit | Branch | Entries | Gate |
|---|---|---|---|
| W-DOCS | `docs/m-wave-2-spec` (this branch: spec + brief + demote closures) | BL-PICKER-CLEANUP-REVALIDATE-QUERY-VARIANT, UNDO-UNCATALOGUED-CODE-CARD-1 | `impeccable-gate: N/A — no UI surface` |
| W-PARSE | `feat/m2-payload-hygiene` | BL-ZERO-WIDTH-POST-PARSE-ENRICHMENT, BL-MUTATION-DRIFT-TRIAGE | `impeccable-gate: N/A — no UI surface` |
| W-SYNC | `feat/m2-sync-fault-codes` | BL-PREPARE-INTERNAL-FAULT-KIND, BL-CRON-WORKBOOK-FAULT-CODE, BL-SOURCE-ANCHORS-STALE-AFTER-FAILED-GID-FETCH | impeccable dual-gate ONCE, on `/help/errors` only (§2.3 — new-code help-family rows land in `app/help/errors/_families.ts`, categorically UI; parser-mutation-wave §1.1 item 8 precedent) |
| W-E2E | `feat/m2-e2e-infra` | BL-RIGHTNOW-SECTION57-FIXTURE-INERT + BL-RIGHTNOW-RECOVERY-CASE-NEEDS-RESTRICTED-VIEWER (one work item), BL-FONT-CENSUS-ORACLE-FLAKE-BLOCKS-CREW-E2E, BL-PG-CRON-COVERAGE-UNRUN | `impeccable-gate: N/A — no UI surface` |
| W-GUARDS | `feat/m2-guard-precision` | BL-POPOVER-REGISTRY-PER-FILE-AND-TAILWIND-ONLY, BL-CROSSWALK-HAYSTACK-RENDERED-TEXT-ONLY | `impeccable-gate: N/A — no UI surface`, CONTINGENT (§2.5: flips to the dual gate if crosswalk triage corrects `app/help/**` copy) |
| W-UI | `feat/m2-ui-cluster` | BL-ADMIN-SEMANTIC-Z-INDEX-SCALE, STEP3-GALLERY-TAP-TARGETS-1 (item d), SHARELINK-CONSTANTS-INVENTORY-1, SHEETLINK-SUBTLE-ACTION-CLASS-1, BL-GLYPHS-OUTSIDE-INTER-SUBSET | impeccable dual-gate (critique + audit); Opus implements (UI hard rule) |

## §1.1 Resolved scope — do not relitigate

All ratified 2026-08-09 by the user unless another source is cited. The brief at `docs/superpowers/specs/2026-08-09-m-wave-2-decisions-brief.md` is the capture of record.

1. **Pool = the §0 table, no more.** Entries screened out stay out: 3 claimed in flight, 2 owned by the ratified parser-mutation wave (`docs/superpowers/specs/parser/2026-08-07-parser-mutation-wave-design.md` — do not double-schedule its branches 4–5), 10 fenced with unfired triggers, and 3 skipped by explicit answer (items 5–7 below). The brief's screen-summary section lists every excluded id.
2. **BL-CRON-WORKBOOK-FAULT-CODE → `PARSE_ERROR_LAST_GOOD`,** keyed on the `WorkbookSynthesisError` type (`lib/drive/exportSheetToMarkdown.ts:325`). User ratification. The entry's own deferral reason ("changes a live crew-visible sync contract and belongs in its own spec") is discharged by this spec.
3. **BL-SOURCE-ANCHORS-STALE → sibling column** `shows.source_anchors_modified_time`. User ratification. The in-jsonb stamp alternative is closed — do not re-propose it (it changes the map shape for every reader).
4. **BL-GLYPHS-OUTSIDE-INTER-SUBSET → widen the Inter subset.** Regenerate the shipped woff2 to cover the probe-derived fallback set (partition rule in §2.6 — the acceptance set is re-derived at implementation time, never a trusted count); markup untouched. The lucide glyph-to-icon migration is explicitly NOT chosen and is not this wave's business.
5. **BL-TAP-TARGET-STRUCTURAL-GUARD → skipped, stays open.** The non-literal-className policy decision remains unmade; nothing in this wave touches it.
6. **BL-OPS-LOG-DASHBOARD-BANNER → skipped, stays open.** Its bundle partner `BL-ADMIN-PER-SHOW-HISTORY` is claimed in flight; the bundle question is settled after that branch lands.
7. **TRAVEL-SUPPRESSION-PARTIAL-EXPLANATION-1 → skipped, stays deferred, flagged for follow-up** (user's words: "skip this wave, flag for followup"). The follow-up is recorded as §4 limit 6: a dedicated crew-page suppression-explanation pass bundled with `TRAVEL-FLIGHT-SUPPRESSED-LEGIBILITY-1`.
8. **Demotes ride along under already-ratified filing-bar policy** (AGENTS.md ledger filing bar; M-wave-1 §1.1 item 8 precedent): BL-PICKER-CLEANUP-REVALIDATE-QUERY-VARIANT (its own body records the premise REFUTED 2026-08-03 and directs readers not to act on it as written), UNDO-UNCATALOGUED-CODE-CARD-1 (its own body: "every code reachable from these call sites has a catalog row today"). Dispositions in §2.1.
9. **Dedup: the two RIGHTNOW rows are ONE task family** — their own bodies cross-declare it ("The two rows are one piece of work"). Two entries close together.
10. **Fully autonomous; both user review gates (spec, plan) WAIVED** (brief item 8, user's words: "Yes, fully autonomous. This fable session owns spec(s) + plan(s), launch new pane(s) for opus implementation(s) + closeout(s)"). Stop only for a genuinely new question; an ambiguity that maps to a decision above is NOT a stop.
11. **Routing:** spec + plan authored in this Fable session; implementation + closeout in NEW Opus pane(s). UI work is Opus-owned per the AGENTS.md hard rule (satisfied — the implementer IS Opus).
12. **All AGENTS.md invariants bind** — worktree-only (11), ledger claims + push (12), TDD (1), conventional commits (6), impeccable dual-gate on UI (8), Supabase call-boundary registry (9), mutation-surface observability (10), §12.4 lockstep triple, validation-schema-parity for the new column, class-sweep discipline.
13. **This wave does not redesign any ledger, catalog, or guard grammar beyond what an entry names.** The popover-registry re-key and crosswalk-haystack rebuild are entry-named work, not grammar drift.

## §2 Per-entry contracts

Entry bodies in `BACKLOG.md`/`DEFERRED.md` are the spec-of-record for mechanics (each carries its own citations, verified at filing; the load-bearing ones re-verified 2026-08-09 in this spec's pre-draft citation pass). This section states what the wave ADDS: the ratified decision, scope boundary, and acceptance shape.

### §2.1 W-DOCS (2 demote closures on this branch; docs-only — preflight skip declared in the PR body)

- **BL-PICKER-CLEANUP-REVALIDATE-QUERY-VARIANT.** Archive per the filing bar: the entry's own 2026-08-03 correction refutes its stated mechanism (`revalidatePath` tags are pathname-only under the installed Next 16.2.10 — probe quoted in-body), and the worst observable outcome is "a cleared stale-entry hint lingering until the next navigation." The archive preserves verbatim: the refutation block, the three recorded obstacles (NEXT_REDIRECT swallowed by `cleanupStaleEntryCore`'s catch; bare-canonical redirect landing on `<SignInOrSkipGate>`; the e2e's `picker_epoch` write tripping invariant 2), and the entry's own re-attempt rule ("any future attempt starts by MEASURING what screen actually renders after a stale cleanup"). Re-open trigger: the next change to the stale-cleanup path, or a report of a stale hint persisting.
- **UNDO-UNCATALOGUED-CODE-CARD-1.** Archive per the filing bar: no live surface — the entry's own text records that every code reachable from the feed call sites has a catalog row today (the four rows cited in the entry at `lib/messages/catalog.ts:902` and its three sibling anchors), so the empty-card render is unreachable without a future code addition. The un-defer trigger ("any new code reachable from a feed action, or the next `lib/messages` pass") rides in the archive entry; the proper fix shape (resolve the code before deciding to render) is preserved for that pickup.

Archive convention: full original body to the owning archive file with a dated resolution paragraph; flight markers stripped in the move (`tests/docs/_metaLedgerInProgress.test.ts` rejects in-flight archives).

### §2.2 W-PARSE (`feat/m2-payload-hygiene`)

- **BL-ZERO-WIDTH-POST-PARSE-ENRICHMENT.** Strip zero-width characters at the point Drive-supplied strings enter the persisted payload — the five sites the entry enumerates across `lib/sync/enrichWithDrivePins.ts` (sheetTab both branches, image alt, folder-item alt), `lib/drive/exportSheetToMarkdown.ts` (archived-tab `tabName`/`headerPreviews`), attached via `lib/sync/pullSheetOverride.ts`. One shared helper with the parser's `stripZeroWidth` (`lib/parser/index.ts:559`; same character class as `clean()` at `lib/parser/blocks/_helpers.ts:50`) so the boundaries cannot drift — the helper is exported from ONE module and imported at the parser and sync/drive layers, never re-typed. **Uniqueness scope (spec r1 F7): the no-second-literal claim covers `lib/sync/**` and `lib/drive/**` (the modules this work touches) plus the two parser sites the helper consolidates.** Deliberate independent mirrors are enumerated and stay: `tests/parser/mutation/oracle.ts:81` (its own comment: "local so the oracle imports nothing from the parser" — oracle independence is the point), `lib/parser/blocks/hotelConfTokens.ts:125` (token-level strip predating this work; plan decides consolidate-or-exempt with reason), and test-fixture literals under `tests/**` (assertion inputs, not boundaries). The AC states this scoped form. Guard: a payload-level test in the shape of the entry's named `tests/parser/payloadZeroWidth.test.ts` precedent, covering the ENRICHED payload (post-`enrichWithDrivePins`, post-archived-tab attachment), not the parse output. **The archived-tab fingerprint rekey decision, resolved here:** stripping changes fingerprints only for payloads that actually contained zero-width characters in Drive-derived fields; the consequence is one extra staged re-sync diff per affected show, surfaced through the normal review flow — a one-time documented note in the entry's archive (§4 limit 2), NO migration and NO fingerprint-compat shim. Plan-time discovery of additional Drive-string entry points joins the same helper in the same commit (class-sweep at round 0).
- **BL-MUTATION-DRIFT-TRIAGE.** Mechanism-triage the 143 `text_drift` rows: each row's note gains its mechanism name (from the entry's machine-derived histogram: 125 snippet-moved, 14 reorder-only, 4 `blockRef.index`-moved), replacing the `[re-kinded by classifier; mechanism triage owed, …]` migration marker. The histogram is already derived; the work is confirming 143 derived classifications row-by-row, not deriving them. Accept shape: zero remaining migration markers in `tests/parser/mutation/knownHoles.ts` (grep-provable), harness green, the 35 rows that stayed `signal_loss` untouched (out of scope by the entry's own text). If any row's confirmation DISAGREES with its derived shape, that row is escalated in the PR body rather than silently re-shaped — a mis-anchor-shaped drift is a likely-regression per the owning spec (`docs/superpowers/specs/parser/2026-08-09-warning-shape-mutation-stability.md` §11.5) and would need extraction, not relabeling.

### §2.3 W-SYNC (`feat/m2-sync-fault-codes`)

- **BL-PREPARE-INTERNAL-FAULT-KIND.** Extend `PrepareOnboardingFileError.kind` from the two-member union `"drive_fetch" | "parse"` (`lib/sync/runOnboardingScan.ts:1164-1172`, repeated in `asPrepareError`) with `"internal"`, thrown by the four post-parse internal helpers the entry names (`applyRoleTokenMappings`, `reconcileIncludedTab`, `discardAndRerun`'s fix-up, `finalizeArchivedTabs`). Mapped to the NEW §12.4 code **`ONBOARDING_INTERNAL_ERROR`** (spec r1 F6 — named here, not left to the plan; verified 2026-08-09 zero collisions in `lib/messages/catalog.ts` and master-spec §12.4), copy telling the operator to contact the developer (precedent row: `ONBOARDING_FINALIZE_INTERNAL_ERROR`, `lib/messages/catalog.ts:891`); finalize severity stays `error`. Full §12.4 lockstep triple (master-spec §12.4 prose + `pnpm gen:spec-codes` regen + `lib/messages/catalog.ts` row, same commit) plus the four-gate CI fan-out the entry defers, plus the help-family row in `app/help/errors/_families.ts`.
- **BL-CRON-WORKBOOK-FAULT-CODE.** In the cron per-file loop's catch (`lib/sync/runScheduledCronSync.ts:3915-3925` locator; anchor by the `classifySyncFailure(error)` call), a `WorkbookSynthesisError` (`lib/drive/exportSheetToMarkdown.ts:325`) now reports `PARSE_ERROR_LAST_GOOD` (`lib/messages/catalog.ts:187` — existing code, existing copy; NO new §12.4 row) instead of falling through to `SYNC_FILE_FAILED`. Outcome stays `parse_error`-family; last-good payload behavior unchanged — this is a code-selection change, not a pipeline change. Tests: a cron-path unit that plants a synthesis failure and asserts the recorded code, plus a negative row proving a non-synthesis throw still classifies via `classifySyncFailure`.
- **BL-SOURCE-ANCHORS-STALE-AFTER-FAILED-GID-FETCH.** Ratified schema: sibling column `shows.source_anchors_modified_time` (timestamptz, nullable — NULL means "stamp unknown", which the reader treats as mismatch → fallback). **Legacy-row initialization (spec r1 F1):** the migration backfills the stamp for every existing row holding a non-empty `source_anchors` with that row's `last_seen_modified_time` — a one-time grandfather that keeps every working deep link working. This deliberately blesses any ALREADY-stale legacy anchor until its next real anchor write re-stamps honestly; that window is exactly the pre-existing accepted limit the entry documents, now bounded instead of permanent, and it rides in §4 as limit 9. Without the backfill, NULL→fallback would regress every populated legacy anchor below the watermark skip (`lib/sync/perFileProcessor.ts:337`) indefinitely. Writers: every path that WRITES a non-empty `source_anchors` stamps the `modifiedTime` the anchors were computed from; every path that deliberately PRESERVES the old map preserves the old stamp (the coalesce pattern extends to the new column). **The writer census includes `scripts/backfill-validation-source-anchors.ts`** (spec r1 F1 — a live anchor writer outside `lib/`). Its stamp source is special-cased (spec r4 F2): the validation fixture pins `last_seen_modified_time = now()` (`supabase/migrations/20260527210000_mint_validation_fixture_atomic.sql:139`), a synthetic watermark with no equality relationship to any Drive revision — a Drive-revision stamp there would deterministically mismatch and demote every link the script exists to create. The script therefore stamps the row's own `last_seen_modified_time` — with a TOCTOU guard (spec r5 F1: the script fetches Drive bytes BEFORE its transaction, so a sync committing revision R2 in that window would let R1 anchors carry an R2-matching stamp): the script reads the watermark W1 BEFORE the Drive fetch, then inside the locked transaction re-reads it as W2; if W1 ≠ W2 it writes the anchors with a NULL stamp and prints a re-run warning — conservative demote plus surfaced signal, never a fresh-stamped stale map; if equal, it stamps W1. Sync-path writers keep the Drive `modifiedTime` they actually processed. AC-M3 adds a validation-backfill row (post-script, unraced: stamp matches watermark and the link is a deep link) and a raced-window unit row (W1 ≠ W2 → NULL stamp, warning emitted). **Reader placement (spec r1 F2, widened r3 F1): the comparison is ONE shared pure helper applied at EVERY anchor projection, not a single query's behavior.** A helper (working name `freshSourceAnchors(anchors, stamp, lastSeen)` — returns the map when the stamps match, an empty map otherwise, NULL stamp = mismatch) lives beside the builder and is applied wherever a `shows.source_anchors` map leaves the data layer: `lib/data/getShowForViewer.ts` (crew path — the projection already carries `driveFileId` + `sourceAnchors`) AND `components/admin/review/publishedAdapter.ts:101` (admin published-review path — the snapshot RPC hands it the full show row incl. both timestamps, and it currently forwards `source_anchors` uncompared; spec r3 F1). Plan-time class-sweep enumerates every consumer of a `source_anchors`-derived map feeding `buildSheetDeepLink` (known third site: the wizard step-3 chrome at `components/admin/wizard/step3ReviewSections.tsx:912`, whose anchors come from the just-computed staging scan — each such site either applies the helper or carries an inline fresh-by-construction reason). Downstream consumers (`SourceLink` → `buildSheetDeepLink`, structural guard at `lib/sheet-links/buildSheetDeepLink.ts:22`) take their existing no-anchor `#gid=0` fallback arm unchanged — no new prop threads through crew primitives. Integration proof (not just builder units): a crew-page render test pair — mismatched stamp asserts the rendered sheet link is the `#gid=0` fallback; matched stamp asserts the deep link — plus an admin published-review row proving the adapter path demotes on mismatch. **DB completeness matrix (plan expands each cell to a task):**

  | Layer | Action |
  |---|---|
  | DDL | one migration adding the nullable timestamptz column PLUS the legacy backfill statement (stamp = `last_seen_modified_time` where `source_anchors` is non-empty); apply local → `pnpm gen:schema-manifest` → surgical validation apply (three-step checklist, AGENTS.md) |
  | CHECK | N/A — nullable timestamp, no domain constraint |
  | Writers | cron apply (the coalesce sites at `lib/sync/runScheduledCronSync.ts:3073` and `lib/sync/runScheduledCronSync.ts:1527`), wizard scan (`runOnboardingScan.ts:1350` degrade path), both finalize flows, `pending_syncs.source_anchors` staging carry-through, and `scripts/backfill-validation-source-anchors.ts` — each enumerated at plan time with its stamp source |
  | Readers | the shared freshness helper applied at `lib/data/getShowForViewer.ts` AND `components/admin/review/publishedAdapter.ts` (+ the plan-time sweep of every other `source_anchors` consumer); `buildSheetDeepLink` and the crew primitives are UNCHANGED |
  | RLS/grants | column inherits the table's existing policies — N/A beyond parity proof |
  | Tests | unit rows: match → deep link; mismatch → `#gid=0`; NULL stamp → `#gid=0`; preserved-map path keeps old stamp; fresh-write path stamps new |
  | Frontend | none — the fallback is the builder's existing behavior arm |

  Advisory-lock note (invariant 2): every RUNTIME writer above already runs inside the per-show advisory-lock transaction on its existing path; the new column write joins those statements with no topology change there. **The migration backfill is itself a new writer and takes the lock itself (spec r2 F1):** it iterates the affected rows acquiring `pg_advisory_xact_lock(hashtext('show:' || drive_file_id))` per row before its `UPDATE`, exactly the shipped remediation-migration precedent (`supabase/migrations/20260611000001_onboarding_fixups_remediation.sql:62`). Single-holder rule holds — the migration runs standalone, in-migration is its only holder layer for those hashkeys. AC-M3 requires the lock to be provable in the migration text.

### §2.4 W-E2E (`feat/m2-e2e-infra`)

- **RIGHTNOW pair (one work item).** First step is DISCOVERY, stated as such: identify the RightNowHero's actual anchor source under a real crew viewer (the probe proved `shows_internal.run_of_show` is not it on this route). Then: per-test `crew_members` row + email-matched test-auth session harness on the `tests/e2e/stage-restricted-crew-schedule.spec.ts` pattern (its header explains why email-matched Google session, not a picker cookie, under mobile-safari). **Fixture writes run under the per-show advisory lock (spec r1 F3 — invariant 2 admits no exception for fixture writes, `tests/e2e/helpers/lockedCrewRestriction.ts:117-120` is the compliant precedent and the single JS-side holder layer for these hashkeys).** The unlocked seed helper this pattern would otherwise reuse, `tests/e2e/helpers/seedShowWithCrew.ts` (bare PostgREST `shows` delete/insert + `crew_members` insert, no lock), is a live invariant-2 violation on a surface this branch builds on: W-E2E repairs it to the locked pattern in the same PR (class-sweep at round 0), and the plan carries the advisory-lock holder-topology enumeration per the writing-plans rule. Reconciliation note: `BL-LOCKED-FIXTURE-HELPER-TARGETS-REMOTE-DB` is claimed on `fix/quick-wins-2-mech`; W-E2E merges `origin/main` and reconciles if that lands first. Re-point the fixture at the real source with values chosen to be DISCRIMINATING (no value may coincide with the seed's show-start anchor or with a clock rendering — the two coincidences that hid this; anti-tautology rule applied to fixtures); drive the recovery case through a restricted crew viewer entering `viewer_off_day` via real resolution. The suite un-skips and wires into `crew-e2e.yml` ONLY when its assertions are proven to move with the fixture (flip test: change the fixture, watch the assertion fail). If discovery shows the hero's anchor source cannot be driven per-test (e.g., it is derived state with no seedable input), the honest outcome is a resized entry naming exactly that — stated here so the plan carries both branches.
- **BL-FONT-CENSUS-ORACLE-FLAKE.** First step is the entry's own: determine WHY the registered-face query fails on a document the element walk could read (the `enforce` throw at `tests/e2e/helpers/fontFidelityFixture.ts:400` already distinguishes pre- from post-navigate). Fix at the fixture level — e.g., await font-readiness (`document.fonts.ready`-class gate) or retry the sample on a live document — WITHOUT weakening the fail-loud posture: a document that still cannot be read after the readiness gate still fails the run. Accept shape: the mechanism is named in the fix commit with a reproduction note, and 5 consecutive green crew-e2e runs post-fix (the bar the entry was blocking).
- **BL-PG-CRON-COVERAGE-UNRUN (residual).** Per-job smoke tests proving each live cron job actually FIRES (the wired-in-CI half closed 2026-07-26/27; text assertions cannot catch a commented-out `net.http_get` body). Shape: for each `cron.job` row, a smoke that observes the job's side effect (the sync path's existing smoke is the template). Jobs whose side effect cannot be observed cheaply get an explicit documented-limit row in the suite naming why — never a silent skip (consequence bound: every job proven or named, no job silently untested). Plan enumerates the live job set at draft time.

### §2.5 W-GUARDS (`feat/m2-guard-precision`)

Both entries are guard-precision work; the finding-admissibility contract and convergence criteria for their review dispatches are in §6's review-brief note.

- **BL-POPOVER-REGISTRY-PER-FILE-AND-TAILWIND-ONLY.** Re-key `tests/components/admin/showpage/_metaPopoverPlacementContract.test.ts`'s registry by OVERLAY (stable per-element marker) rather than by file, and widen `looksLikeAnchoredScroller` to inline-style positioning (`style={{ position: "absolute", … }}`) alongside the Tailwind idiom. Accept-set keyed on structure (position + overflow declarations, however expressed in JSX), not on class-string spelling. Re-disposition the seven registered files under the new key. The entry's two reviewer probes (undispositioned second overlay in a registered file; inline-style mutant) become the guard's executable self-test shapes — planted positive AND negative, same commit (writing-plans repair-economy rule 4). Consequence bound: every detected overlay is dispositioned by name or fails the guard; anything the classifier cannot read is REPORTED unclassified, never silently passed. Threat-model fence: accidental authoring by an ordinary contributor; adversarial obfuscation (computed style objects assembled at runtime, spread-in props) is out of scope and files to the registry header as a documented limit.
- **BL-CROSSWALK-HAYSTACK-RENDERED-TEXT-ONLY.** Rebuild the crosswalk haystack from RENDERED TEXT ONLY via the TypeScript AST, keyed on node kind AND RENDER POSITION (spec r2 F3 + r3 F2 — kinds alone over-include: a bare `const auditKind = "Phantom"` initializer or a `data-testid` attribute would falsely attest a label, silent false assurance). A string/template node contributes to the haystack iff it is: (a) a `JsxText` child; (b) a `StringLiteral`/`NoSubstitutionTemplateLiteral`/template fragment (`TemplateHead`/`TemplateMiddle`/`TemplateTail` — the L-wave scanner's probed lesson that `isStringLiteralLike` drops fragments, `docs/superpowers/specs/2026-08-06-l-wave-design.md` §2.3) appearing as or inside a JSX expression CHILD; or (c) the value of a user-visible JSX attribute from a named allowlist (`aria-label`, `aria-description`, `title`, `alt`, `placeholder`, `label`) — `data-testid`, `className`, and every other attribute are OUT. Seed evidence for (b): `components/admin/settings/DriveConnectionPanel.tsx` renders "Connected · " and "Connection needs attention" as `TemplateHead` fragments in render position while `app/help/admin/settings/page.mdx` bolds both labels. Copy defined in constants and rendered indirectly is deliberately EXCLUDED from the automatic haystack and handled loud, not silent: a label attested only via such a constant FAILS, and its disposition is a reasons-required "indirect copy source" registry row citing the render site (or corrected copy). Under-inclusion surfaces as a failing label that gets dispositioned; over-inclusion would attest silently — the design chooses the loud side of that asymmetry (the consequence bound). In `tests/help/_metaUiLabelCrosswalk.test.ts`. Add the third-party-UI carve the entry requires: a registry with its own reason field for labels naming OTHER products' controls (`**Share**`/`**Viewer**` are Google Drive's controls — correct copy, never candidates; `tests/help/_uiLabelExceptions.ts` cannot hold them since its rows must cite `DEFERRED.md M11-E-D<N>` ids). Triage every newly-failing label in the same PR: real drift → corrected copy; third-party → carve row with reason. The existing executable documented-limit pin (the test's own bare-identifier residual pin) flips from "fails the day this is fixed" to green — that flip is the RED for this work. CONTINGENT gate flip: if any triage corrects copy in `app/help/**` mdx, this branch touches an invariant-8 surface and its `impeccable-gate:` line flips to the dual gate before merge (§0 table note).

### §2.6 W-UI (`feat/m2-ui-cluster`, Opus, impeccable dual-gate)

Pre-code mechanical UI gate applies to every item (em-dash ban, apostrophe literals, 44px floors, canonical token classes); contrast meta-test for any new/repurposed color token.

- **BL-ADMIN-SEMANTIC-Z-INDEX-SCALE.** `--z-*` semantic tokens in `app/globals.css` `@theme` (verified 2026-08-09: zero `--z-` tokens exist today), banded per the impeccable general rules (dropdown / sticky / modal-backdrop / modal / toast / tooltip — exact band set fixed at plan time from the live overlay census). Tree-wide sweep: every raw numeric z-index on an overlay surface moves to a band — **BOTH idioms, Tailwind `z-*` utilities AND inline `zIndex:` style declarations (spec r2 F2: `components/admin/PreviewBanner.tsx:69` carries `style={{ position: "sticky", top: 0, zIndex: 100 }}` and joins the census; a utility-only guard would green while it stays numeric).** Census at plan time; `PublishedToggle`, `ReSyncButton`, `BellPanel`, `FinalizeButton`, `PreviewBanner` are known sites. A numeric that maps to no band is a design question resolved in the PR, not left mixed. Guard: a structural test asserting no raw numeric z-index remains on the swept surfaces in EITHER idiom (accept-set: the band tokens, however applied; everything else fails by name with a reasons-required exemption registry).
- **STEP3-GALLERY-TAP-TARGETS-1, item (d).** The remaining item: three affordance vocabularies in one Step-3 row slot (bare-text "View", bordered "Review", two inline error actions — `docs/superpowers/plans/admin/2026-08-02-step3-live-render-cluster.md` §12) + a bordered blocking card nested inside a bordered attention plate, resolved TOGETHER as the entry's own un-defer trigger demands ("the next deliberate visual pass on the Step-3 row" — this is that pass). **Falsifiable acceptance shape (spec r1 F4), so "resolved under the dual gate" is not tautological:** (i) ONE affordance vocabulary for every action in the row slot — after the pass, all row-slot actions share one visual class family (assertable: the set of distinct action treatments in the slot has size 1; no bare-text action renders beside a bordered action); (ii) nested chrome flattens to AT MOST one bordered container level inside the row slot (assertable: no bordered card renders inside another bordered container within the slot). WHICH class family wins and which border yields are the in-branch design calls, made under the dual gate against the six-variant seeded gallery (the surface that exposed it) — the acceptance shape above is what the audit checks. Items (a)-(c) are SHIPPED (2026-08-08) and are not re-opened. Entry archives with (d)'s resolution recorded.
- **SHARELINK-CONSTANTS-INVENTORY-1.** `DESIGN.md` §5.5 becomes the pinned interaction-timing inventory, with the population DERIVED, never hand-listed (spec r3 F4 — a hand-authored sweep and a test generated from it share the same omissions; the guard must not be circular). **Source universe and forms, fixed here:** a committed scanner over `app/**`+`components/**` (`.ts`/`.tsx`) matching (1) numeric-literal delays to `setTimeout`/`setInterval` (incl. `window.`-prefixed), (2) numeric-initialized constants — module-local OR exported (spec r4 F1: the seed cases `CLOSE_DELAY_MS`, `COPY_FEEDBACK_RESET_MS`, `DEBOUNCE_MS` are all non-exported module consts; an export-only form cannot see its own seed list; the r4 probe's fifteen non-exported named timings are the seed census) — with the name pattern CASE-INSENSITIVE over any numeric-initialized binding (const/let/default parameter): identifier ends in `ms`/`delay`/`duration`/`timeout`/`seconds` under camelCase or SNAKE_CASE (spec r5 F2: `submitTimeoutMs = 30_000`, a camelCase default parameter in `components/shared/ReportModal.tsx`, escaped the SNAKE-only form) — and (3) numeric `duration:` values in motion/transition props. **Totality rule, closing the silent-residual path (spec r5 F2):** the scanner ALSO walks every `setTimeout`/`setInterval` delay argument; one that is neither a numeric literal (form 1) nor an identifier resolving to a covered binding (form 2) is emitted as UNCLASSIFIED and FAILS the inventory test until dispositioned (rename into the pattern, or a reasons-required registry row) — every timer delay in the universe is therefore literal, resolved, or reported by name; none passes silently. The inventory meta-test derives its expected population FROM the scanner's output (derived cover, per the class-sweep derivation rule) and fails by name on any in-scope constant absent from §5.5; a planted unlisted constant is the guard's premise fixture. Scope fence lives IN the scanner with reasons, not in prose: `lib/**` infra budgets (drive/sync/agenda/notify/geocoding timeouts) are excluded as non-interaction timing; anything else excluded needs a reasons-required registry row. Known members the entry names: `ARM_REVERT_MS` = 4000 (`lib/admin/destructiveConfirm.ts`, consumed in `components/admin/BlockedRowResolver.tsx` — in scope as UI interaction despite its `lib/` home, via an explicit include), the bare `2_000` clipboard reset (`app/admin/show/[slug]/ShareLinkCopyButton.tsx:108`); review r3's probe adds the classes `RightNowHero` 60_000 tick, `step3ReviewSections` 5_000 reset, `HoverHelp` `CLOSE_DELAY_MS`, `Step1Share` `COPY_FEEDBACK_RESET_MS`, `ShowRealtimeBridge` `DEBOUNCE_MS`, `AutoRefreshControl` interval, `PageTransition` `duration: 0.22` — dispositioned by the scanner, not by this list.
- **SHEETLINK-SUBTLE-ACTION-CLASS-1.** Recolor the four surviving `text-text-subtle` icon-only action targets (`components/admin/review/ModalCloseButton.tsx`, `components/admin/RescanSheetButton.tsx`, `components/admin/BellPanel.tsx` bell-panel-close, `components/admin/HelpSheet.tsx`) to the action-affordance class the sheet-link branch established, closing the modal-header inversion the entry measured. Ripple obligation, enumerated up front: ModalCloseButton feeds the byte-for-byte header baselines and the modal suites — every affected pinned test updates in the same commit, and any captured help screenshots regenerate FROM the pinned Playwright Docker image with `--platform linux/amd64` (byte-comparison discipline), never from the dev machine.
- **BL-GLYPHS-OUTSIDE-INTER-SUBSET.** Ratified: widen the subset. Regenerate the shipped Inter subset woff2 THROUGH its existing generator and identity pipeline (spec r2 F4 — the asset has an authoritative generator and a committed identity contract; a hand-regenerated file would diverge from both): widen the Unicode ranges in `scripts/subset-inter.sh` (which pins output filename + ranges), regenerate via the script, and update every member of the identity ripple in the same commits — `app/fonts.css:28`, `components/FontPreload.tsx`, `tests/helpers/fontManifest.ts` (path/URL/harness filename/digest), `public/fonts/PROVENANCE.md`, `tests/styles/fontLoadingMutants.test.ts`, and the `PINNED_RANGE_COVERAGE` lockstep in `tests/styles/fontFeatureAvailability.test.ts` (which fails on any declared-range change until updated). **Acceptance set derived from the probe, not a hardcoded count (spec r1 F5 — the entry says "24 distinct glyphs" but its own inline list enumerates 23; the discrepancy is recorded, never resolved by trusting either number):** step 1 of the branch re-runs the entry's fontTools + source-scan probe with a WIDENED census universe (spec r3 F3 — the entry's probe read only `.ts`/`.tsx` under `app/`+`components/`, missing shipped rendered content): tracked `.ts`/`.tsx` AND `.mdx` under `app/`+`components/`, plus CSS `content:` declarations in `app/globals.css` and sibling stylesheets (known escapees: `⋮` U+22EE in five help MDX pages, `⌄`/`⌃` U+2304/U+2303 as generated content at `app/globals.css:785` and `app/globals.css:788` — all three absent from the shipped subset, fontTools-verified in review r3). The probe produces the CURRENT fallback-codepoint set; that set partitions against the full InterVariable face into (a) codepoints Inter carries — every one of these MUST be in the regenerated subset, and (b) codepoints Inter itself lacks (emoji-class: 🔗 🔒 ☎ ✉ and any peers) — these CANNOT be closed by subset widening and are recorded with their site lists as the archive entry's residue note (§4 limit 7), not silent scope creep into an icon migration (§1.1 item 4 fences that). Proof: the re-run probe output is committed showing partition (a) fully present in the shipped subset; the font-census e2e stays green.

### Dimensional Invariants

This wave introduces NO new fixed-dimension parent with flex/grid children. W-UI's items recolor, retoken, and regenerate assets; the Step-3 (d) resolution is chrome/vocabulary unification on existing slots. If any plan task or implementation introduces a fixed-dimension parent→child relationship after all, that task adds the relationship here plus the real-browser `getBoundingClientRect` assertion per the writing-plans layout-dimensions rule — absence of entries in this section is a claim the W-UI dual-gate audit checks.

### Transition Inventory

No new or changed visual STATE ships in any unit: token swaps, recolors, a font asset, tap-target chrome unification, e2e/test/guard code, and ledger prose have no state pairs. The Step-3 (d) work unifies affordance VOCABULARY on existing states — if its resolution adds a state or an animation, the inventory gains its pairs first (same contract as M-wave-1 §2.2).

## §3 Sequencing + claim-handoff protocol

1. `docs/m-wave-2-spec` (this branch) carries spec + brief + plan + the two W-DOCS archives, and currently claims all 18 entries (Stage-0 commit `8f6378056`, pushed).
2. HANDOFF-BY-OVERLAP, the order-binding protocol the M-wave ratified and the L-wave §3 restates: FIRST each unit branch is created off `origin/main`; each runs `pnpm ledger:claims --check <its ids>` expecting exit 1 naming `docs/m-wave-2-spec` and ONLY it (the planned-handoff signature; any other branch = real collision, stop). Each marks its subset `**Status:** IN PROGRESS · **Branch:** <unit branch>`, commits, pushes. Unit subsets: W-PARSE 2, W-SYNC 3, W-E2E 4, W-GUARDS 2, W-UI 5 — 16, plus the 2 W-DOCS ids which stay on this branch and archive here. 18 total, matching §0 exactly; no entry the wave edits is ever undeclared on origin.
3. THEN this branch's last pre-merge commit removes its markers for the 16 handed-off ids (the 2 W-DOCS ids archive in the same commit train, markers stripped in the move). At no instant is any entry undeclared on origin; the transient dual-declaration is the designed handoff state.
4. This branch's PR (spec + brief + plan + demote archives + marker handoff) merges first — docs-only, preflight skip declared in the PR body. Then units implement in order W-PARSE → W-SYNC → W-E2E → W-GUARDS → W-UI (largest visual churn last, on a quiet base), each: worktree, Stage-0 claims already held, TDD, cross-model review, real CI green → merge → `0 0`. Later units merge `origin/main` after each earlier unit lands (BACKLOG/archive conflicts resolve mechanically — both sides' edits stand).
5. Each unit strips its markers in its last pre-merge commit as its entries archive (invariant 12).

## §4 Documented limits (this wave's own)

1. **The two demotes leave durable records** reachable by grepping the entry id (archive + preserved triggers); a wrong demotion is recoverable by construction.
2. **Zero-width strip rekeys archived-tab fingerprints once** for payloads that carried zero-width characters in Drive-derived fields; the consequence is one extra staged re-sync diff per affected show, surfaced through the normal review flow. No migration, no compat shim (§2.2).
3. **The crosswalk's bare-identifier residual persists only until W-GUARDS lands;** after the AST rebuild the new residual is the third-party carve itself — a label wrongly placed in the carve is invisible to the guard. The carve's reason field and review are the mitigation; stated in the registry header.
4. **The popover classifier reads declared JSX, not runtime style.** Computed style objects and spread-in props are out of its reach and are named in the registry header as the documented limit (§2.5 threat-model fence).
5. **Per-job cron smokes cover jobs whose side effects are observable;** any job that is not gets a named documented-limit row in the suite, never a silent skip (§2.4).
6. **Follow-up flag (user-directed):** the crew-page suppression-explanation pass — TRAVEL-SUPPRESSION-PARTIAL-EXPLANATION-1 (M) + TRAVEL-FLIGHT-SUPPRESSED-LEGIBILITY-1 (S), one pattern across Schedule / Today / Travel, mockups included — is queued as its own future mini-arc. Recorded here so the skip cannot read as a drop.
7. **Emoji-class glyphs outside Inter's own coverage** stay in fallback faces after the subset widening; site list rides in the GLYPHS archive entry (§2.6).
8. **BL-FONT-CENSUS-ORACLE-FLAKE's accept bar (5 consecutive greens) is a statistical claim,** not a proof of no residual flake; a later recurrence re-opens at honest scope with the diagnosis note as its head start.
9. **The legacy anchor-stamp backfill blesses any already-stale legacy anchor** (stamp initialized to `last_seen_modified_time` at migration time) until that show's next real anchor write re-stamps honestly. This is the entry's own pre-existing accepted-limit window, now bounded per-show instead of permanent; the alternative — NULL for legacy rows — would break every working legacy deep link (§2.3).

## §4.5 Ratified answers (2026-08-09, capture of record: the decisions brief)

1. Pool: **full — all 5 implementation units + 2 ride-along demotes.**
2. Cron workbook fault: **`PARSE_ERROR_LAST_GOOD` keyed on `WorkbookSynthesisError`.**
3. Source-anchors stamp: **sibling column `shows.source_anchors_modified_time`.**
4. Glyphs: **widen the Inter subset; no icon migration.**
5. Tap-target guard: **skip, stays open.**
6. Ops-log dashboard banner: **skip, stays open pending its in-flight bundle partner.**
7. Travel suppression: **skip, flagged for a dedicated follow-up pass.**
8. **Fully autonomous** (user's words in the brief); Fable authors spec + plan, new Opus pane(s) implement + close out.

## §5 Meta-test / registry inventory (pre-declared for the plan)

- **CREATES:** the enriched-payload zero-width guard (§2.2); the z-index band guard + exemption registry (§2.6); the DESIGN.md §5.5 constants-inventory meta-test (§2.6); per-job cron smokes (§2.4).
- **EXTENDS / RE-KEYS:** `_metaPopoverPlacementContract` (per-overlay key + inline-style recognition, §2.5); `_metaUiLabelCrosswalk` (AST rendered-text haystack + third-party carve, §2.5); `tests/parser/mutation/knownHoles.ts` notes (§2.2); the font-census suite (fixture readiness repair, §2.4).
- **Invariant-9:** the W-E2E harness adds crew-viewer session seeding — any new Supabase call sites there get registry rows or inline `// not-subject-to-meta:` reasons; W-SYNC's writers are existing registered paths.
- **Invariant-10:** no new mutation surface (fault-kind mapping and code selection are error-path telemetry on existing instrumented paths; the column write rides existing cron/wizard mutations). A plan-time discovery to the contrary adds the registry row in the same commit.
- **§12.4 lockstep:** ONE new row (the `internal` fault-kind code, §2.3) with regen + catalog + help-family row; `PARSE_ERROR_LAST_GOOD` is an existing row, untouched prose.
- **Validation-schema-parity:** the new column follows the three-step checklist (local apply → manifest regen → surgical validation apply) in the W-SYNC PR.

## §6 Acceptance criteria

- **AC-M1 (W-DOCS):** both demotes archived with preserved triggers/obstacles; ledger meta-suites green; markers handled per §3.
- **AC-M2 (W-PARSE):** the five Drive-string sites strip via the ONE shared helper — import-graph provable at the §2.2 uniqueness scope (no second character-class literal in `lib/sync/**`/`lib/drive/**` or the two consolidated parser sites; the §2.2 enumerated mirrors exempt with reasons); the enriched-payload guard fails on a planted zero-width fixture at every covered field and passes clean; the 143 triage notes carry mechanisms with zero migration markers remaining; harness green; disagreeing rows (if any) escalated, not relabeled.
- **AC-M3 (W-SYNC):** `internal` kind thrown by all four named helpers with the new code through the full lockstep + fan-out; planted `WorkbookSynthesisError` on the cron path records `PARSE_ERROR_LAST_GOOD` and a non-synthesis throw still records via `classifySyncFailure`; the column ships with every §2.3 matrix cell green including validation parity AND the legacy backfill (a pre-migration populated-anchor row deep-links identically post-migration; the backfill's per-show advisory lock is present in the migration text per the §2.3 remediation precedent); deep-link unit rows (match / mismatch / NULL / preserve / fresh-write) pass; the §2.3 crew-page integration pair passes (mismatched stamp renders the `#gid=0` fallback, matched stamp renders the deep link) plus the admin published-review demote row; the shared freshness helper is applied (or a fresh-by-construction reason recorded) at every plan-swept `source_anchors` consumer; the validation-backfill rows pass (unraced: stamp matches watermark, deep link; raced: NULL stamp + surfaced warning); `/help/errors` dual-gate run recorded.
- **AC-M4 (W-E2E):** the RIGHTNOW suite's assertions are proven fixture-driven (flip test recorded), un-skipped, and wired — or the entries resize to the named discovery outcome; every fixture write in the new/repaired harness runs under the per-show advisory lock with `seedShowWithCrew.ts` repaired to the locked pattern; the font-oracle mechanism is named with a reproduction note and crew-e2e shows 5 consecutive greens post-fix; every live cron job has a firing smoke or a named documented-limit row.
- **AC-M5 (W-GUARDS):** the popover registry is per-overlay keyed with both probe shapes as executable self-tests (positive and negative); the inline-style mutant is detected; the crosswalk haystack is AST rendered-position-only per §2.5 (a planted non-rendered constant matching a help label does NOT attest it — the guard's negative premise fixture), the old residual pin flips green, every newly-failing label is dispositioned (corrected copy, third-party carve row, or indirect-copy-source row citing the render site) in the same PR; contingent gate flip honored if help mdx changed.
- **AC-M6 (W-UI):** z-band tokens exist with the sweep complete over BOTH idioms (utilities and inline `zIndex:`) and the band guard green; Step-3 (d) meets the §2.6 falsifiable shape (one action treatment in the row slot, at most one bordered level) under the dual gate and archives; §5.5 inventory test green with its population derived from the committed scanner (planted unlisted constant fails by name); the four icon-only targets recolored with every pinned baseline/test updated in the same commits; the §2.6 probe partition (a) fully present in the shipped woff2 (re-run probe output committed) with partition (b) recorded as the residue note, the regeneration made THROUGH `scripts/subset-inter.sh` with the full identity ripple (manifest digest, FontPreload, PROVENANCE, loading mutants, `PINNED_RANGE_COVERAGE`) updated in the same commits, and font-census green; impeccable dual-gate run with P0/P1 fixed or DEFERRED-entried.
- **AC-PROC:** claim handoff per §3 with no undeclared instant; markers stripped in each PR's last pre-merge commit; conventional commits; TDD per task; `impeccable-gate:` marker lines per unit per invariant 8.
- **AC-PROG:** at wave close, `pnpm ledger:mass` totals strictly below the §0 baseline — entries < 77 AND mass < 365 — recomputed and recorded in the closeout. (Exact deltas depend on the RIGHTNOW discovery branch and any honest resizes; the strict decrease is the pinned claim, the closeout records the arithmetic.)
- **Review-brief note (W-PARSE guard, W-GUARDS, W-UI band guard dispatches):** each adversarial-review brief for a guard/detector surface states the §2.5/§2.6 consequence bounds and threat-model fences as its convergence criteria, carries the finding-admissibility contract (probe-backed findings only; conservative-demote-plus-signal files to documented limits), and instructs the reviewer to exhaust each vector per round. Enumerations are never the convergence criterion.

## §7 Impeccable gate

Per-unit as the §0 table states: W-UI carries the dual gate; W-SYNC runs it ONCE scoped to `/help/errors` (the new help-family row); W-GUARDS is `N/A — no UI surface` CONTINGENT on its help-mdx triage outcome; W-DOCS, W-PARSE, W-E2E are `impeccable-gate: N/A — no UI surface`. If any task unexpectedly touches an invariant-8 surface, that unit's gate flips to the dual gate before merge.
