# Needs-attention holds rollup — design

**Date:** 2026-08-03. **Backlog:** `BACKLOG.md` § `BL-NEEDS-ATTENTION-HOLDS-ROLLUP` ("pending MI-11 holds do not surface on the needs-attention page"). **Origin deferral:** `docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-10-mobile-needs-attention-design.md` §11.

Pending MI-11 identity holds (`sync_holds`, `kind='mi11_pending'`) are visible only inside the show they belong to (the per-show changes feed). The needs-attention rollup — the `/admin/needs-attention` page, the dashboard inbox, the AdminNav badge, and the digest email — does not know they exist. An unopened show can therefore sit on a held identity change indefinitely with zero cross-show signal. This spec adds the missing cross-show holds read and rolls open holds into all four surfaces.

## 1. Goals

- An open MI-11 hold surfaces as a card in the needs-attention item list (page + dashboard inbox), counts toward the AdminNav badge and the mobile summary card, and appears in the digest email, until it is approved or rejected.
- Approve/reject stay where they live today (the per-show changes feed inside the `?show=` modal); the rollup only announces and deep-links.
- No DB changes: no migrations, no new RPCs, no RLS edits, no new mutation surfaces.

### 1.1 Resolved scope — do not relitigate

Ratified by the owner during brainstorming on 2026-08-03 (this document is the ratification record; the mockup artifact the choices were made against is session-local and not repo state):

| #   | Decision                                                                                                                                                                                                                                                                                                       | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | **Surfaces: Option C** — page, dashboard inbox, badge, AND digest email all include open holds.                                                                                                                                                                                                                 | Owner choice over "page only" and "inbox surfaces only". Badge/page lockstep is preserved by construction (§6).                                                                                                                                                                                                                                                                                                                                                           |
| R2  | **Digest includes a hold only while it is uncleared.**                                                                                                                                                                                                                                                          | Automatic: approve deletes the hold rows (`supabase/migrations/20260608000002_mi11_gate_rpcs.sql:455`), reject converts `kind` to `'undo_override'` (`supabase/migrations/20260608000002_mi11_gate_rpcs.sql:70`). Every read in this spec filters `kind='mi11_pending'`, so a cleared hold vanishes from all four surfaces with no acknowledgement bookkeeping. Do not propose an acknowledged/snoozed state; none is wanted.                                              |
| R3  | **Grouping: one card per show.** A show with exactly one open hold renders that hold's summary; a show with N>1 renders a count line.                                                                                                                                                                           | Owner choice ("Style 3") over per-hold cards and always-count cards.                                                                                                                                                                                                                                                                                                                                                                                                      |
| R4  | **Progressive disclosure without actions ("Reveal A").** Expanding a multi-hold card reveals the per-hold catalog summaries — the SAME generated copy the per-show feed shows, which for an email change includes the old and new addresses (R8) — plus a link to the show. NO inline approve/reject in the rollup. | The owner's ratified choice was disclosure-without-actions versus disclosure-with-inline-approve/reject; the owner explicitly declined inline actions. "Names only" in the session shorthand meant "no action controls", not a redaction contract — the identical summaries are already visible to the same admin audience in the per-show feed. Do not propose moving `Mi11GateActions` into the inbox — that duplicates a gated mutation surface for no ratified need.  |
| R5  | **Read path: service-role read inside the existing loaders** (approach 1). No SECURITY DEFINER RPC, no PostgREST grant change.                                                                                                                                                                                  | `sync_holds` is deliberately service-role-only (`supabase/migrations/20260608000000_sync_holds.sql:46-47`, F9 read posture per the header comment at `20260608000000_sync_holds.sql:4`); the per-show feed already reads it exactly this way (`lib/sync/feed/readShowChangeFeed.ts:37`).                                                                                                                                                                                  |
| R6  | **Archived shows are excluded** from all four surfaces.                                                                                                                                                                                                                                                          | Consistency with the sync-problem stream's `shows.archived=false` filter (`lib/admin/loadNeedsAttention.ts:213`). An archived show's holds become reachable again by unarchiving; a terminal archived show's rows die with the show (`on delete cascade`, `supabase/migrations/20260608000000_sync_holds.sql:8`).                                                                                                                                                         |
| R7  | **Stream totals count shows-with-open-holds, not hold rows** (§6).                                                                                                                                                                                                                                               | Keeps cards, badge, `totalCount`, and `overflowCount` arithmetic mutually honest — one card per show means one count unit per show.                                                                                                                                                                                                                                                                                                                                       |
| R8  | **Per-hold copy is generated by `shapeHoldEntry`, never re-authored.**                                                                                                                                                                                                                                           | The gallery-parity contract: hold summaries are "GENERATED … from the disposition, never authored, so a catalog that invented its own copy would diverge on sight" (`lib/sync/feed/shapeHoldEntry.ts:4-5`). The rollup reuses the same generator; only the multi-hold count line and the disclosure affordance copy are new (§7).                                                                                                                                         |

### 1.2 Adversarial-round triage record

Round 1 (Codex, 2026-08-03, BLOCKING, 12 findings) — all accepted and repaired; dispositions:

| Finding                                                                                               | Disposition                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F1 digest 20-item slice could silently drop holds                                                       | → D8: digest call threads an explicit uncapped `cap`. Per-email bounding stays at the template's `DIGEST_MAX_SHOWS` slice with its source-derived overflow (`lib/notify/templates/digest.ts:151`).                                                                              |
| F2 R4 "names only" contradicted §7 rendering full summaries                                             | → R4 reworded to its ratified substance (no action controls; summaries are the R8 generated copy).                                                                                                                                                                              |
| F3 embed/Date-typed rows reaching `groupHoldRows`                                                       | → §4 adapter contract; readers flatten + normalize. Sharpened again in round 2 (G1): the digest adapter normalizes via `asIso`, not a SQL text cast.                                                                                                                            |
| F4 D1 vs §6 contradiction on count-helper reuse                                                         | → count helper CALLS `loadOpenIdentityHolds` and counts groups (§6).                                                                                                                                                                                                            |
| F5 cap-order divergence between two hold queries                                                        | → removed by the F4 resolution; determinism at the cap boundary sharpened in round 2 (G7).                                                                                                                                                                                      |
| F6 transition inventory missed states                                                                   | → §7 three-state inventory; refresh-persistence claim re-scoped in round 2 (G3).                                                                                                                                                                                                |
| F7 disclosure control contract unspecified                                                              | → §7 island contract; `CollapsePanel` `label` added in round 2 (G4).                                                                                                                                                                                                            |
| F8 `holdCount` NaN/zero guards                                                                          | → `holdCount` is DERIVED as `summaries.length` everywhere; not an input.                                                                                                                                                                                                        |
| F9 test-plan gaps                                                                                       | → §9 rows 7–14 + the e2e clear-through step.                                                                                                                                                                                                                                    |
| F10 bounded-reads registry + count-helper infra row                                                     | → §9 registry inventory (new module row; EXPANDED `loadNeedsAttentionCount` contract).                                                                                                                                                                                          |
| F11 `sourceTotals` undecided                                                                            | → D9 `holdShows`; constructor enumeration completed in round 2 (G6).                                                                                                                                                                                                            |
| F12 missing completeness matrix                                                                         | → §11.                                                                                                                                                                                                                                                                          |

Round 2 (Codex, 2026-08-03, BLOCKING, 10 findings) — all accepted and repaired; dispositions:

| Finding                                                                                    | Disposition                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1 (BLOCKING) SQL text cast produces non-ISO strings; sort-incompatible with `asIso` streams | → §4/§8: the digest adapter normalizes `created_at` through the digest's existing `asIso` (`lib/notify/digest.ts:76-78`) — SAME representation as its other streams. No text casts. §9.12 asserts cross-stream ordering with a hold interleaved between pending items.                                                                              |
| G2 (HIGH) mobile summary card not threaded                                                   | → §7: `NeedsAttentionSummaryCard` gains `identityHoldTotal` + a fourth breakdown chip; `Dashboard` threads it. §9.6.                                                                                                                                                                                                                               |
| G3 (HIGH) refresh-preserves-expansion is an unverified framework contract                    | → §7 Transition Inventory re-scoped: expansion persistence across a refresh is explicitly NOT a contract (UNRATIFIED either way); the contract is correct MODE rendering after any refresh. The plan's transition-audit task PROBES actual behavior in a real browser and pins whichever it observes.                                              |
| G4 (MEDIUM) `CollapsePanel` requires a `label`                                               | → §7 specifies the exact `label` (truthy-title fork). `components/admin/CollapsePanel.tsx:27-39` verified.                                                                                                                                                                                                                                         |
| G5 (MEDIUM) empty-string `title` ambiguates accessible names                                 | → §7: ALL accessible names use the truthy-title fork (`title ? … (slug) : … slug`), mirroring `NeedsAttentionInbox.tsx:126-133`; visible line keeps `title ?? slug`. §9.5 pins the empty-string branch.                                                                                                                                            |
| G6 (MEDIUM) `sourceTotals` constructors not enumerated                                       | → §8 names both literals — invalid-recipient `no_send` (`lib/notify/digest.ts:127`) and the monitor-only synthesized model (`lib/notify/runNotify.ts:457`) — both gain `holdShows: 0`; §9.13 pins them.                                                                                                                                            |
| G7 (MEDIUM) cap-boundary row selection nondeterministic                                      | → §4/§8: both transports order `created_at desc, id asc` BEFORE the limit, making boundary membership deterministic for a given data state. §9.9 fixtures ties at the boundary.                                                                                                                                                                    |
| G8 (MEDIUM) digest holds-leg fault untested                                                  | → §9.14: selective holds-query failure (other digest queries succeed) resolves to the digest's TYPED infra result (corrected in round 3/4; §8, §9.14).                                                                                                                                                                                                                                  |
| G9 (MEDIUM) six stale help/tooltip copy surfaces                                             | → §7 copy-layer subsection lists all six; §11 gains a help/copy row; the §7 "no page-level change" claim corrected (the page tooltip changes).                                                                                                                                                                                                     |
| G10 (LOW) stale row numbering in §1.2                                                        | → F9 disposition and §11 both say rows 7–14; e2e is its own labeled step.                                                                                                                                                                                                                                                                          |

Round 3 (Codex, 2026-08-03, BLOCKING, 5 findings) — all accepted and repaired; dispositions:

| Finding                                                                 | Disposition                                                                                                                                                                                             |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1 (BLOCKING) §9.14 demanded a reject; `buildDigestModel` never throws   | → typed `{ kind: "infra_error" }` expectation everywhere (G8 row, §8, §9.14).                                                                                                                            |
| H2 (HIGH) e2e prescribed the materializer (single-hold-only) + chip route | → e2e seeds `sync_holds` directly (existing suite idiom); chip asserted on `/admin` at 390px.                                                                                                            |
| H3 (MEDIUM) repeated `CollapsePanel` needed `region={false}`              | → §7 panel contract + §9.5 no-region assertion.                                                                                                                                                          |
| H4 (MEDIUM) contradictory collapse row                                    | → row split: user-toggle morph vs refresh-remount instant; probe records the live path.                                                                                                                  |
| H5 (HIGH) copy surfaces had no failing test                               | → §9.14a source-walking copy contract; §11 row updated.                                                                                                                                                  |

Round 4 (Codex, 2026-08-03, NEEDS-ATTENTION, 5 findings) — all accepted and repaired; dispositions:

| Finding                                                              | Disposition                                                                                                                                                                     |
| ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1 (HIGH) G8 triage row still said "rejects the build"                | → row corrected (the round-3 edit had silently missed the reformatted table cell).                                                                                              |
| I2 (MEDIUM) "never throws" overbroad                                  | → §8 scope note: typed contract covers query faults; the pre-existing construction/shutdown escape sites are enumerated and out of scope.                                        |
| I3 (HIGH) approve-based clear-through unseedable                      | → e2e clears via REJECT (Drive-independent, no crew row needed); rationale + citations inline in §9 e2e.                                                                        |
| I4 (HIGH) per-file copy match let dual-passage files stay half-stale   | → §9.14a: eight passage-anchored assertions with per-passage slicing; "six" wording corrected to "eight passages across six files".                                             |
| I5 (LOW) NaN/negative totals unstated                                  | → §7 guard conditions: producer-derived finite counts, builder trusts like existing streams, chip's >0 branch makes "NaN held" unrenderable.                                     |

Round 5 (Codex, 2026-08-03, NEEDS-ATTENTION, 2 findings) — both accepted and repaired; dispositions:

| Finding                                                        | Disposition                                                                                                                                                  |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| J1 (HIGH) dashboard inventory would stay stale around our edit | → §7: the passage is repaired in full (five kinds, sync-problem bullet added, false self-clearing sentence replaced); §9.14a gains two supplementary assertions. |
| J2 (MEDIUM) new numeric strings lack tabular figures            | → §7: `tabular-nums` on the count line and the cap line; §9.5 asserts the class on both.                                                                     |

Round 6 (Codex, 2026-08-03, NEEDS-ATTENTION, 4 findings) — all accepted and repaired; dispositions:

| Finding                                                    | Disposition                                                                                                                          |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| K1 (HIGH) tour-page review-queues passage missed           | → ninth copy passage + ninth assertion (§7, §9.14a); the one-show claim reworked.                                                    |
| K2 (MEDIUM) J1 supplementary assertions too weak           | → §9.14a: five-bullet count, sync-problem bullet anchor, replacement-sentence presence.                                              |
| K3 (MEDIUM) CollapsePanel spacing topology unspecified     | → §7 Dimensional Invariants: no-gap island root + internal pt-3 wrapper per the component contract and precedent.                     |
| K4 (MEDIUM) byte-identical claim impossible with new field | → D4/§9.2: additive-field-only contract; regression test compares a fixture-built expected shape, never a self-comparison.            |

Round 7 (Codex, 2026-08-03, NEEDS-ATTENTION, 3 findings) — all accepted and repaired; dispositions:

| Finding                                                   | Disposition                                                                        |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| L1 (HIGH) tour assertion left the one-show claim unpinned  | → ninth assertion gains the negative half (stale sentence ABSENT).                 |
| L2 (MEDIUM) compound row ambiguous when count drops to 1   | → mode boundary always wins; row reworded, audit task inherits it.                 |
| L3 (LOW) §11 said six surfaces                             | → nine passages across seven files.                                                |

## 2. Current state (verified 2026-08-03)

- **Store.** `public.sync_holds` (`supabase/migrations/20260608000000_sync_holds.sql:6-19`): `id, show_id → shows(id) on delete cascade, drive_file_id, domain, entity_key, held_value jsonb, proposed_value jsonb, base_modified_time, kind, reservation_collisions, created_at, created_by`. `kind in ('mi11_pending','undo_override')` (`20260608000000_sync_holds.sql:27`); `mi11_pending` rows carry `proposed_value->>'disposition' in ('email_change','rename','removal')` (`20260608000000_sync_holds.sql:30-37`); unique `(show_id, domain, entity_key)` (`20260608000000_sync_holds.sql:39-41`); RLS enabled, all revoked from `anon, authenticated`, granted to `service_role` (`20260608000000_sync_holds.sql:45-47`).
- **Only existing read:** `readShowChangeFeed(showId)` (`lib/sync/feed/readShowChangeFeed.ts:80`) — per-show, service-role client built by `createFeedSupabaseClient` (`readShowChangeFeed.ts:37`), holds query selects `id, entity_key, held_value, proposed_value, base_modified_time, created_at` filtered `eq("show_id", showId).eq("kind", "mi11_pending")` (`readShowChangeFeed.ts:121-127`). No cross-show read exists in `lib/` or `app/` (backlog row, re-verified).
- **Hold → display shaping:** `shapeHoldEntry(hold)` (`lib/sync/feed/shapeHoldEntry.ts:90`) produces `summary` via `renderPendingSummary` (`shapeHoldEntry.ts:40`) from the catalog codes `mi11_pending_email_change` (`shapeHoldEntry.ts:46`), `mi11_pending_rename_folded` (`shapeHoldEntry.ts:67`), `mi11_pending_rename` (`shapeHoldEntry.ts:73`), `mi11_pending_removal` (`shapeHoldEntry.ts:80`), interpolating `{name}/{old}/{new}` from `entity_key`/`held_value`/`proposed_value`. Input type `HoldRow` (`shapeHoldEntry.ts:11-18`) — note `created_at: string`.
- **Rollup assembly:** `loadNeedsAttention` (`lib/admin/loadNeedsAttention.ts:30`) reads three streams on a cookie-bound `createSupabaseServerClient` — `pending_ingestions`, `pending_syncs`, inbox-routed `admin_alerts` — each as a bounded row read (`.limit(cap + 1)`) plus an exact head-count, then delegates to the pure `buildNeedsAttention` (`lib/admin/needsAttention.ts:231`). Item variants: `pending_ingestion`, `first_seen`, `existing_staged`, `sync_problem` (`lib/admin/needsAttention.ts:83-120`). Merged sort: newest-first on `sortKey` (string `localeCompare`), tie-break `id` ascending (`needsAttention.ts:272-275`); single slice to `cap`, which DEFAULTS to `RENDER_CAP = 20` when the caller passes none (`needsAttention.ts:74-76`, `needsAttention.ts:279`); the page threads `PAGE_RENDER_CAP = 100` (`needsAttention.ts:23`).
- **Badge:** `loadNeedsAttentionCount` (`lib/admin/needsAttentionCount.ts:11`) — head-counts only, cookie client, consumed by the admin layout badge and `app/api/admin/needs-attention-count/route.ts`.
- **Mobile summary card:** `NeedsAttentionSummaryCard` (`components/admin/NeedsAttentionSummaryCard.tsx:5-17`) receives per-stream totals (`ingestionTotal`, `syncTotal`, `syncProblemTotal`, `autoAppliedCount`) and renders a breakdown; `Dashboard` threads the totals (`components/admin/Dashboard.tsx:738-744`).
- **Digest:** `buildDigestModel` (`lib/notify/digest.ts`) queries `pending_ingestions`/`pending_syncs` via its raw `sql` tag, normalizes timestamps through `asIso` (`digest.ts:76-78` — postgres.js returns `Date` for `timestamptz`), feeds `buildNeedsAttention` with NO `cap` (`digest.ts:200`) and no `syncProblems` (defaulted `[]`, `lib/admin/needsAttention.ts:67-70`), groups via `groupTitleFor`/`itemCopy`/`slugFor` (`lib/notify/digest.ts:82-108`), no-sends on zero items (`digest.ts:228`) and on an invalid recipient with a literal zero `sourceTotals` (`digest.ts:127`), and records `sourceTotals: { ingestions, syncs, shows }` (`digest.ts:19-24`) which `deliver.ts` persists into delivery context (`lib/notify/deliver.ts:711`). `runNotify` synthesizes an empty digest model with the same literal shape for monitor-only sends (`lib/notify/runNotify.ts:457`). The email template applies its own `DIGEST_MAX_SHOWS` slice with a source-derived overflow count (`lib/notify/templates/digest.ts:151`).
- **Deep link:** `/admin?show=<slug>` mounts the published review modal (`app/admin/page.tsx:161`), which mounts `ChangesSection` (`components/admin/showpage/PublishedReviewModal.tsx`) — the surface with the feed and `Mi11GateActions` approve/reject.
- **Card precedent:** the `sync_problem` card in `NeedsAttentionInbox` (`components/admin/NeedsAttentionInbox.tsx:113-135`): bordered tile, `CardHeader` with `status="warn"`, `title ?? slug` fallback line, copy line, `Link` whose `aria-label` uses the truthy-title fork (`NeedsAttentionInbox.tsx:126-133`). `NeedsAttentionInbox` is a SERVER component (no `"use client"`; `ItemCard` in the same file, `NeedsAttentionInbox.tsx:60`).
- **Disclosure precedent:** `IgnoredSheetsDisclosure` (`components/admin/IgnoredSheetsDisclosure.tsx:12`) — a client island owning ONLY open/closed state; server-rendered children passed by slot; real `<button>` with `aria-expanded` + `aria-controls` pointing at an always-mounted `CollapsePanel` region (which REQUIRES an accessible `label`, `components/admin/CollapsePanel.tsx:27-39`); decorative chevron `aria-hidden`; `min-h-tap-min` tap floor; `focus-visible:ring-2 focus-visible:ring-focus-ring` focus classes (`IgnoredSheetsDisclosure.tsx:60`).
- **Seeding:** the dev materializer inserts `sync_holds` rows (`lib/dev/materialize/run.ts:252`) as the write precedent, but its scenario gate cannot express a multi-hold show (tier-3-only, single-hold scenarios); the e2e seeds `sync_holds` directly per the existing suite idiom.

## 3. Design decisions

<!-- spec-lint: ignore — the D1 row names the planned new file lib/admin/identityHolds.ts -->

| #   | Decision                                                                                                                                                                                                                                                                                                                                                        | Rationale                                                                                                                                                                                                                                                                                        |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | **One pure grouping core + one PostgREST reader, consumed by BOTH admin helpers.** Pure `groupHoldRows` (new file lib/admin/identityHolds.ts) turns flat, pre-normalized hold+show rows into per-show groups. `loadOpenIdentityHolds` (same file) is the service-role PostgREST reader; `loadNeedsAttention` uses its groups, and `loadNeedsAttentionCount` calls the SAME function and takes `groups.length`. The digest reuses only `groupHoldRows`, over rows from its own `sql` query. | One query and one grouping semantics for page, inbox, and badge — lockstep and overflow behavior identical by construction. Shaping ≤200 summaries for a badge count is negligible waste at FXAV scale.                                                                                          |
| D2  | **The holds read is one bounded read, not rows+head-count.** Up to `HOLDS_ROW_CAP = 200` rows ordered `created_at desc, id asc`; the stream total = number of distinct show groups in the read. No separate head-count probe.                                                                                                                                    | The stream total must count SHOWS (R7); PostgREST cannot head-count a distinct column. The secondary `id asc` order makes row membership at the cap boundary deterministic for a given data state (G7). Table structurally small (unique `(show_id, domain, entity_key)`); §10 documents the cap. |
| D3  | **The two admin helpers construct a service-role client for the holds leg only**; existing cookie-client legs untouched. Any holds-leg fault follows each helper's existing infra posture (§8).                                                                                                                                                                  | R5. Precedent for a server-only admin read on service-role: `readShowChangeFeed` (`lib/sync/feed/readShowChangeFeed.ts:37`). Both helpers already run strictly server-side behind admin gating.                                                                                                  |
| D4  | **Builder input is pre-grouped and pre-normalized.** `buildNeedsAttention` gains optional `identityHolds: NeedsAttentionIdentityHoldInput[]` + `totalCounts.identityHolds`; both default to `[]`/`0`. Compatibility contract (corrected R6-K4): with the new inputs omitted, the output is UNCHANGED except for the one additive field `identityHoldTotal: 0` — items, ordering, counts, and every pre-existing field are identical; "byte-identical" is impossible literally because §5 adds the field to every result.                                                                                                                     | Mirrors the `syncProblems` optionality contract (`lib/admin/needsAttention.ts:67-70`). Grouping in the builder would force the digest's raw-SQL rows through a PostgREST-shaped interface.                                                                                                       |
| D5  | **Card `sortKey` = the group's newest `created_at`, as a string in the SAME representation as the sibling streams of that call.** PostgREST path: the raw PostgREST string. Digest path: the digest's `asIso` normalization (`lib/notify/digest.ts:76-78`) — never a SQL text cast, whose `YYYY-MM-DD HH:MM:SS+TZ` form breaks `localeCompare` ordering against ISO strings (G1). | Sort keys only ever compare WITHIN one `buildNeedsAttention` call, so per-call representation consistency is the actual invariant; each reader owns delivering it.                                                                                                                               |
| D6  | **Disclosure is client-side expand/collapse of already-loaded summaries — no lazy fetch.** The loader always carries every group's per-hold summaries (bounded by D2's row cap).                                                                                                                                                                                 | At ≤200 rows total there is nothing worth a second round-trip; keeps the inbox a pure render of `NeedsAttention`.                                                                                                                                                                                |
| D7  | **No new instrumentation.** All new code paths are reads; the only mutation surfaces in the feature's orbit (`mi11_approve_hold` / `mi11_reject_hold` RPC callers) are untouched.                                                                                                                                                                                | Invariant 10 applies to mutation surfaces; there are none here. Invariant 2 (advisory locks) likewise N/A — no writes to locked tables.                                                                                                                                                          |
| D8  | **The digest's builder call threads an explicit cap equal to the sum of its stream input lengths** (`ingestions.length + syncs.length + identityHolds.length`), so the builder slice can never drop an item from the digest.                                                                                                                                     | F1: the digest previously inherited the DEFAULT 20-item slice (`needsAttention.ts:74-76`, `digest.ts:200`). Per-email bounding remains at the template layer (`lib/notify/templates/digest.ts:151`) — honest truncation, not silent. Side effect (deliberate, documented): pending-stream items beyond 20 also stop being silently dropped from the digest. |
| D9  | **`DigestModel.sourceTotals` gains `holdShows: number`** (count of hold GROUPS fed to the builder). ALL THREE production constructors update: the computed totals in `buildDigestModel`, the invalid-recipient `no_send` literal (`lib/notify/digest.ts:127`), and the monitor-only synthesized model (`lib/notify/runNotify.ts:457`) — the latter two as `holdShows: 0`. | F11+G6: `sourceTotals` is persisted durable telemetry (`lib/notify/deliver.ts:711`); a holds-only digest must not record zeros with an unexplained show count. Additive field; the literal constructors are compile-forced but enumerated so no `0` lands unreviewed.                            |

## 4. Read path (new file lib/admin/identityHolds.ts)

<!-- spec-lint: ignore — code block sketches the planned new module's exports -->

```ts
// Flat, reader-normalized input row. READERS own flattening + normalization:
// groupHoldRows never sees a nested embed, a Date, or a null slug.
export type IdentityHoldRow = {
  id: string;
  show_id: string;
  slug: string; // readers drop slug-less rows before this layer
  title: string | null;
  entity_key: string;
  held_value: unknown;
  proposed_value: Disposition; // shapeHoldEntry's own input contract
  base_modified_time: string | null; // display-irrelevant here; never sorted, never a gate token in the rollup
  created_at: string; // ALWAYS a string — never a Date (D5); representation per transport (D5)
};

export type IdentityHoldGroup = {
  showId: string;
  slug: string;
  title: string | null;
  summaries: string[]; // newest-first; length >= 1; each from shapeHoldEntry(row).summary
  newestCreatedAt: string; // created_at of the group's newest hold, in the transport's representation
};
// NOTE: no holdCount field ANYWHERE — every layer derives it as summaries.length (F8).

export const HOLDS_ROW_CAP = 200;

export function groupHoldRows(rows: IdentityHoldRow[]): IdentityHoldGroup[];
export async function loadOpenIdentityHolds(): Promise<
  { kind: "ok"; groups: IdentityHoldGroup[] } | { kind: "infra_error"; message: string }
>;
```

- **Query** (service-role client, `createSupabaseServiceRoleClient` at `lib/supabase/server.ts:79`):
  `from("sync_holds").select("id, show_id, entity_key, held_value, proposed_value, base_modified_time, created_at, shows!inner(slug, title)").eq("kind", "mi11_pending").eq("shows.archived", false).order("created_at", { ascending: false }).order("id", { ascending: true }).limit(HOLDS_ROW_CAP + 1)`.
  The secondary `id` order runs BEFORE the limit, so which rows occupy the boundary is deterministic for a given data state (G7). Every await destructures `{ data, error }`; returned-error, thrown-error, and client-construction-throw all map to `{ kind: "infra_error" }` (invariant 9). Registry row in `tests/admin/_metaInfraContract.test.ts`.
- **Reader-side adaptation (the F3/G1 contract).** `loadOpenIdentityHolds` flattens each row's `shows` embed handling BOTH object and array shapes (the existing defensive pattern at `lib/admin/loadNeedsAttention.ts:264-276`); a row whose embed yields no `slug` is skipped with `log.warn("identity hold missing show slug", { source: "admin.loadOpenIdentityHolds", holdId })`. PostgREST returns `created_at` as a string already; the reader passes it through untouched. The DIGEST transport (§8) receives postgres.js `Date` values and normalizes `created_at` (and `base_modified_time`, uniformity only) through the digest's existing `asIso` (`lib/notify/digest.ts:76-78`) — the SAME representation as the digest's other stream sort keys, so cross-stream `localeCompare` ordering inside the digest's builder call is coherent (G1). No SQL text casts.
- **Row-shape guard.** A `mi11_pending` row without a recognized `disposition` cannot exist under the shape CHECK (`20260608000000_sync_holds.sql:30-37`); defensively, `shapeHoldEntry`'s removal fallback arm (`shapeHoldEntry.ts:79-84`) renders any unrecognized shape, so no throw path exists.
- **Overflow.** If more than `HOLDS_ROW_CAP` rows come back: `log.warn("sync_holds row cap exceeded", { source: "admin.loadOpenIdentityHolds" })`, drop the sentinel row, group what was read. Consequence documented ONCE in §10 and shared by page, inbox, and badge (they all consume this one function, D1). Never an error, never silent.
- **Grouping (`groupHoldRows`, pure).** Group by `show_id` preserving input order (already newest-first from either reader's ordering): the first row of a group supplies `slug`/`title`/`newestCreatedAt`; each row contributes `shapeHoldEntry(row).summary` in order. Groups order newest-first by construction; `summaries[0]` is the newest hold's summary. Empty input → `[]`. The function trusts the `IdentityHoldRow` contract (readers pre-filter); it performs no logging.

## 5. Builder + item variant (`lib/admin/needsAttention.ts`)

```ts
export type NeedsAttentionIdentityHoldInput = IdentityHoldGroup; // structural re-export

// new item variant
| {
    variant: "identity_hold";
    key: string;              // `hold-show:${showId}`
    showId: string;
    slug: string;
    title: string | null;
    summaries: string[];      // newest-first; length >= 1
    copy: string;             // summaries.length === 1 ? summaries[0] : count line (§7)
    activityAt: string | null;
  }
```

- `BuildNeedsAttentionInput` gains `identityHolds?: NeedsAttentionIdentityHoldInput[]` and `totalCounts.identityHolds?: number` (defaults `[]` / `0` — D4).
- Merged entry: `sortKey = newestCreatedAt ?? ""`, `id = showId` (tie-break axis), one entry per group. A group with `summaries.length === 0` is skipped (defensive; unreachable from `groupHoldRows`). There is NO `holdCount` input to validate — the count is `summaries.length` (F8), so NaN/zero/negative counts are unrepresentable.
- `copy`: `summaries.length === 1` → `summaries[0]` verbatim (R8). Otherwise the §7 count line with `summaries.length` interpolated.
- Output `NeedsAttention` gains `identityHoldTotal: number` (mirrors `syncProblemTotal`, `lib/admin/needsAttention.ts:131`); `totalCount` adds the stream total (§6).

## 6. Count semantics (lockstep across surfaces)

Single definition: **the identity-holds stream total is the number of non-archived shows with at least one open `mi11_pending` hold** (R7) — operationally, `loadOpenIdentityHolds().groups.length`.

| Surface                                                             | Source of the number                                                                                                                                                                                              |
| -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Page / dashboard `totalCount`, `overflowCount`, `identityHoldTotal` | `loadOpenIdentityHolds()` inside `loadNeedsAttention`, threaded as `totalCounts.identityHolds = groups.length`                                                                                                    |
| Mobile summary card                                                  | The SAME `identityHoldTotal` threaded through `Dashboard` into `NeedsAttentionSummaryCard` (§7, G2)                                                                                                               |
| AdminNav badge (`loadNeedsAttentionCount`)                            | THE SAME `loadOpenIdentityHolds()` call; holds contribution = `groups.length`. No second query exists (D1), so filters, ordering, and cap behavior cannot diverge from the page — including above the row cap (G7). |
| Digest                                                                | `groupHoldRows(adaptedSqlRows).length` (its own transport, same pure core; recorded as `sourceTotals.holdShows`, D9)                                                                                              |

## 7. UI: cards, disclosure, summary card, copy surfaces

New `identity_hold` branch in `NeedsAttentionInbox` (`components/admin/NeedsAttentionInbox.tsx`), modeled on the `sync_problem` card (`NeedsAttentionInbox.tsx:113-135`): bordered tile, `CardHeader` `status="warn"`, label `Held change` when `summaries.length === 1`, `Held changes` otherwise; VISIBLE title line `title ?? slug` (the existing card convention, empty string included); ACCESSIBLE names always use the truthy-title fork `title ? `… for ${title} (${slug})` : `… for ${slug}`` mirroring `NeedsAttentionInbox.tsx:126-133` (G5). Then:

- **Single hold (`summaries.length === 1`).** Copy line = `summaries[0]` (`text-sm text-text-subtle`). Footer `Link` to `/admin?show={slug}` — text `Review →`, `data-testid={`needs-attention-link-identity-hold-${showId}`}`, `aria-label` per the truthy-title fork (`Review held change for …`). No disclosure affordance. Entirely server-rendered.
- **Multiple (`summaries.length > 1`).** Copy line = count line: `` `${summaries.length} held changes waiting` `` with `tabular-nums` on the rendering element (DESIGN.md mandates tabular figures for every rendered count; the existing chips at `components/admin/NeedsAttentionSummaryCard.tsx:56-72` are the pattern). Below it, a NEW client island `IdentityHoldDisclosure` (new file components/admin/IdentityHoldDisclosure.tsx) mirroring the `IgnoredSheetsDisclosure` composition (`components/admin/IgnoredSheetsDisclosure.tsx:12`): the island owns ONLY open/closed state; disclosed content passed as server-rendered `children`.
  - **Trigger:** a real `<button type="button">` with visible text `Show details` (collapsed) / `Hide details` (expanded), decorative chevron (`aria-hidden`, rotates with state), `aria-expanded={open}`, `aria-controls={panelId}` where `panelId = `identity-hold-panel-${showId}``, `min-h-tap-min` tap floor, and the precedent's focus classes (`IgnoredSheetsDisclosure.tsx:60`). `aria-label` = truthy-title fork: `` title ? `Show details for ${summaries.length} held changes for ${title} (${slug})` : `Show details for ${summaries.length} held changes for ${slug}` `` (Hide variant likewise) — begins with the visible text, satisfying label-in-name.
  - **Panel:** always-mounted `CollapsePanel` with `id={panelId}`, `region={false}` (this card repeats up to 20 dashboard / 100 page times; the component mandates the opt-out for many-panel callers to avoid landmark proliferation — `components/admin/CollapsePanel.tsx:40-47`, precedent `components/admin/RecentAutoAppliedStrip.tsx:453-460`; the toggle's `aria-expanded` + `aria-controls` remain the accessible wiring), AND its type-required `label` (`components/admin/CollapsePanel.tsx:27-39`, G4; inert while `region` is false): truthy-title fork `` title ? `Held changes for ${title} (${slug})` : `Held changes for ${slug}` ``. Contents: the `summaries` list, each its own line (`text-sm text-text-subtle`), capped at `HOLD_SUMMARIES_RENDER_CAP = 10` lines followed by a literal line `` `and ${summaries.length - 10} more` `` (also `tabular-nums`) when longer.
  - **Footer `Link`** (same contract as the single-hold card; plural aria-label `Review held changes for …` via the truthy-title fork) sits OUTSIDE the panel, visible in both disclosure states.
- **Card `data-testid`:** `needs-attention-item-identity-hold-{showId}` (both modes).

**Mobile summary card (G2).** `NeedsAttentionSummaryCard` (`components/admin/NeedsAttentionSummaryCard.tsx:5-17`) gains a required `identityHoldTotal: number` prop and a fourth breakdown chip following the existing chips' exact markup/class pattern; chip text `` `${identityHoldTotal} held` `` with `aria-label` `` `${identityHoldTotal} held identity changes` `` (rendered only when > 0 IF the existing chips hide at zero — match whichever zero-behavior the existing chips implement, verified at plan time against `NeedsAttentionSummaryCard.tsx:54-69`). `Dashboard` threads the new total where it threads the other three (`components/admin/Dashboard.tsx:738-744`). A holds-only state therefore shows a non-empty breakdown.

**Copy surfaces (G9).** Nine existing user-facing passages across seven files enumerate what the inbox/digest contains and MUST be updated in the same change (all verified 2026-08-03): the dashboard inbox tooltip (`components/admin/Dashboard.tsx:769-772`), the needs-attention page tooltip (`app/admin/needs-attention/page.tsx:64-67`), the dashboard help card inventory + live-change description (`app/help/admin/dashboard/page.mdx:35-43` and `page.mdx:51-53`) — the inventory passage is repaired in FULL, not just appended to (R5-J1): it currently says "Three kinds of cards" while four variants already ship and omits the sync-problem card entirely, and its "Nothing here clears itself." claim is false for sync-problem cards, which clear automatically (`components/admin/NeedsAttentionInbox.tsx:113-125`). The edit sets the count to five, adds BOTH missing bullets (sync problem: clears on its own when the underlying problem resolves; held identity change: decided from the show page), and replaces the self-clearing sentence with per-kind accuracy, the review-queues overview + live-change guidance (`app/help/admin/review-queues/page.mdx:5-12` and `page.mdx:50-60`), the daily-rhythm inbox description (`app/help/daily-rhythm/page.mdx:12-14`), the settings daily-digest description (`app/help/admin/settings/page.mdx:33-38`), and the tour's review-queues card (`app/help/tour/page.mdx:29-34`) whose "first-seen sheets ... and staged edits" enumeration plus "Each queue is scoped to one show" claim both go stale against a cross-show holds rollup (R6-K1) — the edit adds held identity changes to the enumeration and reworks the one-show sentence to say the Needs attention list collects them across shows. Each gains a mention of held identity changes in its existing voice; no em-dashes; catalog discipline unchanged (prose, not codes). NOTE: this corrects the earlier claim of "no page-level change beyond loader threading" — the PAGE's tooltip copy changes; routes and layout do not.

**Guard conditions.** `totalCounts.identityHolds` / `identityHoldTotal` are producer-derived (`groups.length` at every call site) — always finite non-negative integers; the builder trusts them exactly as it trusts the existing streams' totals (`lib/admin/needsAttention.ts:334-345`, no re-validation, R4-I5 ratified). The summary-card chip renders ONLY inside its `> 0` branch, and `NaN > 0` is false, so a corrupt value degrades to chip-absent — the literal string "NaN held" is unrenderable; the card's pre-existing `totalCount` NaN behavior is out of scope. `title` null → `slug` renders on the visible line; ALL accessible names fork on truthiness so an empty-string `title` never yields a dangling label (G5). `summaries` empty → builder skipped the group (§5). `activityAt` null or empty → builder maps empty `sortKey` to `null` (`lib/admin/needsAttention.ts:283`); `CardHeader` omits the timestamp for null `activityAt` (`NeedsAttentionInbox.tsx:47`). `summaries.length` is the ONLY count anywhere (F8).

**Mode boundaries.** Two modes, forked EXACTLY on `summaries.length === 1`: single (no island, no panel, no toggle — the copy line IS the summary) and multi (count copy + island + panel). Shared elements: tile frame, `CardHeader`, title line, footer link. The island exists only in multi mode.

### Transition Inventory

Visual states: **single**, **multi-collapsed**, **multi-expanded**. Expansion state is client state inside the island. **Whether that state survives a `router.refresh()` re-render is explicitly NOT a contract of this design (UNRATIFIED either way, G3)** — the modal open path triggers refreshes (`components/admin/showpage/PublishedReviewModal.tsx:166-177`), and the contract below is stated so BOTH persistence outcomes are correct-by-spec. The plan's transition-audit task probes the actual behavior in a real browser and pins whichever it observes.

| From → To                        | Treatment                                                                                                                                                                                       |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| single → multi-collapsed         | instant — a data refresh re-renders the card with the island mounted collapsed. No animation.                                                                                                    |
| multi-collapsed → single         | instant — island unmounts with its panel. No animation.                                                                                                                                          |
| multi-collapsed → multi-expanded | `CollapsePanel` height-morph (the precedent's standard treatment) — the ONLY animated pair.                                                                                                      |
| multi-expanded → multi-collapsed (user toggle)      | `CollapsePanel` height-morph (reverse).                                                                                                                     |
| multi-expanded → multi-collapsed (refresh remount)  | instant — if a refresh REMOUNTS the island it arrives in its default collapsed state with no transition rendered; if the refresh preserves island state, no collapse happens at all. The probe records which refresh path the live app takes (UNRATIFIED which, both in-contract). |
| single → multi-expanded          | unreachable in one step: a refresh mounts the island in its default collapsed state.                                                                                                             |
| multi-expanded → single          | instant — a refresh dropping `summaries.length` to 1 renders single mode: island and panel unmount, any open state discarded. No exit animation.                                                 |

Compound rows: (a) summaries CHANGE while expanded WITH the count staying above 1 (a reject converts one of three holds, `20260608000002_mi11_gate_rpcs.sql:70`) — list re-renders in place; if island state persisted, panel stays open; if not, it collapses — both correct (G3). When the change drops the count TO exactly 1 (a reject on a two-hold show), the MODE BOUNDARY governs instead: the multi-expanded to single row applies and the island unmounts (R7-L2 — the mode fork on summaries.length === 1 always wins over disclosure state); (b) the whole group clears while expanded (approve deletes rows, `20260608000002_mi11_gate_rpcs.sql:455`) — card unmounts on next refresh, no exit animation (consistent with every other inbox card's removal); (c) toggle clicked mid-refresh — island local state updates immediately; the refreshed payload re-renders around it per the same UNRATIFIED-persistence posture.

### Dimensional Invariants

No fixed-dimension parent/child relationships are introduced (flow-layout tile list), so the plan's layout-dimensions task is N/A (restated in §9). ONE spacing-topology contract exists (R6-K3), from the CollapsePanel usage contract — the always-mounted zero-height track means a parent flex gap would persist while collapsed (`components/admin/CollapsePanel.tsx:22-25`): the island root is flex-col with NO gap utility, and open-state separation lives INSIDE the panel children as a pt-3 wrapper (exact precedent: `components/admin/IgnoredSheetsDisclosure.tsx:100-104`). The card tile's own gap-2 is unaffected because the island is a single flex child.

**Mechanical gate pre-checks** (AGENTS.md "Pre-code mechanical UI gate"): no em-dashes or raw codes in the new copy strings (count line, `Show details` / `Hide details`, `and N more`, `Review →`, chip text, aria-labels, the nine copy-passage edits); tap targets `min-h-tap-min` (trigger AND footer link); type/token classes reuse the sync-problem card's exact classes plus `tabular-nums` on the two numeric lines (R5-J2); no new color tokens (existing warn treatment — no `DESIGN.md` change, no contrast pin needed).

## 8. Digest + error handling

**Digest changes** (`lib/notify/digest.ts`):

- New holds query on the existing `sql` tag (no text casts — G1; the inner join guarantees non-null `slug`):

  ```sql
  select sh.id, sh.show_id, sh.entity_key, sh.held_value, sh.proposed_value,
         sh.base_modified_time, sh.created_at, s.slug, s.title
  from sync_holds sh join shows s on s.id = sh.show_id
  where sh.kind = 'mi11_pending' and s.archived = false
  order by sh.created_at desc, sh.id asc
  ```

- Adapter maps each row to `IdentityHoldRow`, normalizing `created_at` and `base_modified_time` through the digest's existing `asIso` (`lib/notify/digest.ts:76-78`) — the same representation as the digest's other stream sort keys (D5/G1).
- Rows → `groupHoldRows` → `identityHolds` input; `totalCounts.identityHolds = groups.length`; `sourceTotals.holdShows = groups.length` (D9, including the two zero-literal constructors named there).
- The `buildNeedsAttention` call threads `cap: ingestions.length + syncs.length + identityHolds.length` (D8).
- `groupTitleFor` → `item.title`; `itemCopy` → `item.copy` (single-hold: the generated summary; multi: the count line); `slugFor` → `item.slug` (extends the switches at `lib/notify/digest.ts:82-108`).

**Error handling:**

| Surface                   | Holds-leg fault behavior                                                                                                                                                                                                                                                                          |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `loadNeedsAttention`      | Same contract as its other streams: any holds-leg fault → the whole call returns `{ kind: "infra_error", message }` (`lib/admin/loadNeedsAttention.ts:28`). No partial-stream success.                                                                                                            |
| `loadNeedsAttentionCount` | `loadOpenIdentityHolds` returning `infra_error` → `{ kind: "infra_error" }` (`lib/admin/needsAttentionCount.ts:9`). The cookie legs and the holds leg fail independently — a holds fault is not maskable by healthy pending counts (§9.11).                                                       |
| Digest                    | The holds query runs inside `buildDigestModel`'s existing try posture: any query fault resolves to the TYPED `{ kind: "infra_error" }` result (`lib/notify/digest.ts:238-239`). Scope note (R4): the typed-result contract covers QUERY faults — the paths outside it (`databaseUrl()` throw and default `postgres(...)` construction before the try, `lib/notify/digest.ts:55-61` + `digest.ts:130-139`, and the unguarded `finally` shutdown at `digest.ts:240-243`) predate this feature and are untouched; the holds query sits INSIDE the existing try, so it inherits exactly the typed posture. Never catch-and-continue to a partial model — a digest that silently omits holds would violate R2 (§9.14 pins the SELECTIVE holds-leg failure specifically). |

No new user-visible fault copy (existing surfaces own their infra rendering); no raw codes anywhere (invariant 5).

## 9. Tests

Unit (vitest):

1. **`groupHoldRows`** — grouping, order preservation (groups newest-first; summaries newest-first), empty input, single-row group, multi-show interleaved rows.
2. **`buildNeedsAttention` identity_hold** — single-hold copy = `summaries[0]`; multi-hold copy = count line with `summaries.length`; sort interleaving against other streams (fixture-derived timestamps — assert the merged ORDER derived from fixture values, never a hardcoded order); `totalCount`/`overflowCount`/`identityHoldTotal` arithmetic when the stream total exceeds rendered cards; defaults regression fence for D4: with the new keys omitted, `identityHoldTotal` is `0` and every OTHER field of the result deep-equals the expected pre-change shape built from the fixture (never compare a call against itself — that is tautological); empty-summaries group skipped.
3. **`loadOpenIdentityHolds`** — invariant-9 shape (returned error, thrown error, construction failure each → typed `infra_error`); embed flattening handles object AND array shapes; slug-less row skipped with warn; registry row in `tests/admin/_metaInfraContract.test.ts`.
4. **Count helper** — holds contribution = `groups.length` via the SAME `loadOpenIdentityHolds` (spy pins the call — no second query shape exists to drift); EXPAND the existing `loadNeedsAttentionCount` contract row in `tests/admin/_metaInfraContract.test.ts:263-267` to name the holds leg.
5. **Inbox component** — single-hold card: summary + link, NO island; multi-hold: count line collapsed, toggle expands to summaries + link; `aria-expanded`/`aria-controls`/panel id + `CollapsePanel` label wiring; the panel exposes NO `role="region"` (the repeated-landmark opt-out); the count line and the `and N more` line both carry `tabular-nums` (R5-J2); summaries cap line at 11+ holds (`and N more` derived from fixture length); title fallbacks: null → slug on the visible line, EMPTY STRING → accessible names still carry the slug via the truthy fork (G5); footer link present in both disclosure states.
6. **Summary card (G2)** — `identityHoldTotal` chip renders per the existing chips' zero-behavior; a holds-only state (other totals zero) yields a non-empty breakdown; chip text derived from fixture total.
7. **Archived exclusion (R6)** — an archived show's open hold produces no group (loader filter), no badge contribution, and no digest item (SQL filter). All three asserted.
8. **`undo_override` exclusion (R2)** — a present `undo_override` row for a show with no `mi11_pending` rows yields nothing on any surface (covers reject-conversion, not just approve-deletion).
9. **Cap boundary (D2/G7)** — exactly `HOLDS_ROW_CAP` rows: no warn, all grouped; `HOLDS_ROW_CAP + 1`: warn emitted, sentinel dropped, groups derived from the capped set; a fixture with `created_at` TIES spanning the boundary pins that membership follows the `id asc` secondary order deterministically.
10. **Tie-break** — two groups with equal `newestCreatedAt` order by `showId` ascending in the merged list.
11. **Independent fault injection** — cookie legs succeed while the service-role holds leg fails → `infra_error` (both helpers); service-role construction throw specifically covered.
12. **Digest representation compatibility (G1)** — the digest adapter delivers `asIso`-normalized strings; a fixture interleaving one hold BETWEEN two pending items by timestamp asserts the merged digest order is correct (would fail under a SQL-text-cast representation); a type-level pin keeps `IdentityHoldRow["created_at"]` as `string`.
13. **Digest uncapped + totals (D8/D9/G6)** — more than 20 merged items including an old hold: the hold is present in the model; `sourceTotals.holdShows` recorded; the invalid-recipient `no_send` literal and the `runNotify` synthesized model both carry `holdShows: 0` (compile-level pins with a runtime assertion each); holds-only digest sends (item count > 0 → not `no_send`, `lib/notify/digest.ts:228`).
14a. **Copy contract (G9/R3, sharpened R4)** — a source-walking copy test in the `tests/help/sheetChangesCopy.test.ts:11-29` idiom with NINE passage-anchored assertions, not one per file (the ninth: `app/help/tour/page.mdx` review-queues card, which ALSO asserts the stale sentence "Each queue is scoped to one show" is ABSENT — the phrase-presence half alone would leave the one-show claim unprotected, R7-L1): for the two dual-passage files, slice the source at a passage-local anchor first (dashboard mdx: the card-inventory list vs the "Changes and review" section; review-queues mdx: the two-queues table vs the live-show-changes bullet list) and assert the held-identity-change phrase (/held.{0,30}(identity|crew).{0,30}change/i, markdown-bold normalized) inside EACH slice; the other five sources get one anchored assertion each. Fails if ANY of the nine passages is omitted or reworded away. Supplementary dashboard-inventory assertions (R5-J1, sharpened R6-K2): the inventory slice matches /Five kinds of cards/; contains EXACTLY five bullet lines matching /^- \*\*/m; one bullet begins with the literal text - **Sync problem.**; the stale sentence "Nothing here clears itself." is ABSENT; and the replacement behavior sentence is PRESENT (match /sync problem cards clear on their own/i).
14. **Digest selective holds-leg fault (G8, corrected R3)** — ingestion/sync/shows queries succeed, the `sync_holds` query alone rejects → `buildDigestModel` RESOLVES to `{ kind: "infra_error" }` (never throws, never a partial model); complements the existing first-query-fails infra tests (`tests/notify/_metaInfraContract.test.ts:352-371`).

e2e (Playwright, extends the existing needs-attention spec files): seed `shows` + `sync_holds` rows DIRECTLY with the suite's prefix-namespaced insert idiom (`tests/e2e/needs-attention-page.spec.ts`) — one single-hold show and one 3-hold show. (The dev materializer is unsuitable here: its gate admits only tier-3 scenarios and both hold-bearing tier-3 scenarios carry exactly one hold — `lib/dev/materialize/plan.ts:56-62`, `lib/dev/attentionScenarios/tier3.ts:85-154`.) Assert: the page renders both cards with the correct copy fork; badge count includes both shows (expected number derived from the seeded fixture); ON `/admin` AT 390px the summary-card chip shows the seeded total (the summary card exists only on the dashboard, `components/admin/Dashboard.tsx:738-745` — not on the needs-attention page); expansion reveals the three summaries, the card link lands on `/admin?show={slug}` with approve/reject visible, and — follow-through — REJECTING the single hold then reloading shows the card GONE and the badge decremented (clear-through via the reject conversion, R2). Reject, not approve, is the e2e clear action (R4): approve requires a reachable Drive file whose live modifiedTime matches the hold (`lib/sync/holds/mi11GateActions.ts:89-102`) plus a live `crew_members` row (`20260608000002_mi11_gate_rpcs.sql:319-333`) — unseedable here — while `mi11_reject_hold` needs only the hold row and the byte-exact `base_modified_time` the feed already round-trips. Anti-tautology: scope card-copy assertions to `needs-attention-item-identity-hold-{showId}`, never page-wide text search (the per-show feed renders the same summaries).

Layout-dimensions task: N/A (§7 Dimensional Invariants). Transition-audit task: applies — lists every conditional render in the new card + island, asserts the single animated pair is the `CollapsePanel` morph and everything else is instant with no `AnimatePresence`, PROBES the refresh-persistence question in a real browser (G3) and records the observed behavior, and exercises compound row (a).

Meta-test / structural-registry inventory (declared):

- `tests/admin/_metaInfraContract.test.ts` — NEW row for `loadOpenIdentityHolds`; EXPANDED contract text on the existing `loadNeedsAttentionCount` row (`tests/admin/_metaInfraContract.test.ts:263-267`).
- `tests/admin/_metaBoundedReads.test.ts` — the new module path lib/admin/identityHolds.ts added to `READ_MODULES` (`_metaBoundedReads.test.ts:30`); `sync_holds` added to `UNBOUNDED_TABLES` (`_metaBoundedReads.test.ts:58`).
- NOT touched (declared): `AUDITABLE_MUTATIONS` (no mutation surfaces), advisory-lock topology tests (no writes), `PROTECTED_ROUTES` (no new routes), alert catalog (no new codes), DML-lockdown registries (no grant changes), schema manifest / validation parity (no migrations), sentinel-hiding registry (no optional-text tiles).

## 10. Documented limits

- **Row cap.** Beyond `HOLDS_ROW_CAP = 200` open hold rows, the read truncates: groups derive from the newest 200 rows (boundary membership deterministic via the `id asc` secondary order, G7), older holds temporarily invisible in the ROLLUP ONLY (the per-show feed remains complete), `log.warn` emitted. Page, inbox, AND badge consume the single `loadOpenIdentityHolds` read (D1), so for a given data state all three under-report identically; two reads straddling a WRITE can still differ transiently (next bullet). Unreachable at FXAV scale; never silent, never corrupting.
- **Badge/page skew window.** The badge and page read at different instants; a hold approved between the two reads can skew the badge by one until the next badge refresh. Identical to the existing pending-streams skew; not new.
- **Digest timing.** A hold created and cleared entirely between two digest sends never appears in any digest. Intended (R2).
- **Digest email length.** With the D8 uncapped model, a pathological item flood is bounded by the TEMPLATE's `DIGEST_MAX_SHOWS` slice with its honest overflow line (`lib/notify/templates/digest.ts:151`) — the bound moved from a silent model-layer slice to a visible template-layer one.
- **Expansion persistence.** Whether a multi-hold card's expanded state survives a `router.refresh()` is UNRATIFIED (G3); both outcomes are in-contract, and the transition-audit task records the observed behavior.

## 11. Completeness matrix (tier × layer)

Single domain (identity holds); every layer accounted:

| Layer                               | Action                                                                                                                                                             |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Table DDL / CHECKs / RLS / grants   | N/A — untouched; the feature reads the existing table under the existing service-role-only posture (R5).                                                            |
| RPC read/write (`mi11_*` gate RPCs) | N/A — untouched; approve/reject stay on the per-show surface (R4).                                                                                                  |
| PostgREST read (service-role)       | NEW `loadOpenIdentityHolds` (§4), consumed by both admin helpers (D1).                                                                                              |
| Raw-SQL read (digest transport)     | NEW holds query + `asIso` adapter (§8), same pure core.                                                                                                             |
| Cookie-client reads                 | Untouched (existing pending/alert legs).                                                                                                                            |
| Builder (pure)                      | `identityHolds` input + `identity_hold` variant + `identityHoldTotal` (§5).                                                                                         |
| UI — inbox                          | Card branch + `IdentityHoldDisclosure` island (§7); page/dashboard inherit via the shared component.                                                                |
| UI — mobile summary card            | `identityHoldTotal` prop + fourth chip; `Dashboard` threading (§7, G2).                                                                                             |
| UI — help/tooltip copy              | Nine passages across seven files updated (§7 copy surfaces, G9/K1).                                                                                                             |
| Badge                               | Count-helper holds leg via `loadOpenIdentityHolds` (§6).                                                                                                            |
| Email                               | Digest model (D8 cap, D9 `sourceTotals.holdShows` across ALL THREE constructors); template unchanged (its slice already honest).                                    |
| Durable telemetry                   | `sourceTotals` persisted at `lib/notify/deliver.ts:711` gains `holdShows` (additive).                                                                               |
| Tests / registries                  | §9 rows 1–14 + 14a (copy contract) + the e2e + the two registry touches.                                                                                                                  |

## 12. Implementation shape (for writing-plans)

<!-- spec-lint: ignore — names the planned new files lib/admin/identityHolds.ts and components/admin/IdentityHoldDisclosure.tsx -->

Single milestone, one branch (`feat/needs-attention-holds-rollup`), ~3 phases: (1) lib/admin/identityHolds.ts (pure grouping + reader) + builder variant + infra-contract/bounded-reads registry tests; (2) count helper + inbox card + IdentityHoldDisclosure island + summary card + component tests; (3) digest + copy surfaces + e2e + impeccable dual gate (invariant 8 — UI surface) + whole-diff review. Closeout also graduates the `BL-NEEDS-ATTENTION-HOLDS-ROLLUP` backlog row (ledger invariant 12: the graduation lands in the same PR, so no in-flight marker outlives the merge). UI throughout → Opus implements (routing rule); per the owner's 2026-08-03 instruction the implementation + closeouts run in a NEW Opus pane dispatched after plan approval, with this session's pipeline marker handed over per the AGENTS.md takeover protocol. Codex adversarial review per phase artifact (spec, plan) and whole-diff before merge; real CI green; merge.

## 13. Deferred

- Inline approve/reject in the rollup ("Reveal B") — declined R4; re-open only on an explicit owner request.
- Hold history / acknowledged states — declined R2.
- Digest per-hold detail beyond the shared item copy (the generated summaries already carry old/new values; richer per-hold email layout is not requested).
