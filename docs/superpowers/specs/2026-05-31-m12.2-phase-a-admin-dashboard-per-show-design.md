# M12.2 Phase A — Admin dashboard + per-show reskin (design / spec)

> **Milestone:** M12.2 — Admin redesign, **Phase A** (dashboard + per-show). Phase B (persistent
> nav + settings) is a separate plan under the same umbrella, deferred.
> **Implementer/owner:** Opus / Claude Code (UI hard rule, ROUTING.md).
> **Date:** 2026-05-31. **Spawned by:** the M12 UX-validation walk (admin IA/layout theme,
> `.validation-local/working-notes.md:125-146`).
> **Status:** design for owner review → writing-plans.

---

## 1. Authoritative sources + supersession order

1. `.validation-local/design-admin/RECONCILIATION.md` **§E** (owner decisions, 2026-05-30) — authoritative;
   supersedes any contradicting prototype or kickoff-brief detail.
2. This session's orchestrator rulings (2026-05-30/31): Live-now source (§3 below); archived UI deferred to B;
   global AlertBanner placement deferred to B; milestone label = M12.2 Phase A.
3. The Claude Design prototype at `.validation-local/design-admin/fxav-crew-pages/project/admin/*.jsx`
   + `project/Admin Redesign.html` — **visual target to recreate, not code to copy** (bundle README).
4. `DESIGN.md` (token surface + house rules) and `app/globals.css` `@theme` (executable tokens).

**Explicitly superseded:** the kickoff brief's "Live now = compute from `schedule_phases`; `dates jsonb` is the
WRONG comparator; do NOT fall back to dates" paragraph. Verified false against live code (see §3); §E D-2 wins.

---

## 2. Scope

**In Phase A:**
- Dashboard reskin (`components/admin/Dashboard.tsx` + new sub-components): stat strip, shows-as-table,
  needs-attention inbox consolidation.
- Per-show reskin (`app/admin/show/[slug]/page.tsx`): Crew ⟷ Share&access two-col, rotate/reset folded into
  the share panel, sync→quiet footer, Published pill, header share-link chip, keep `← Admin home`.
- D-1 status-token system: new `@theme` tokens + a **DESIGN.md amendment**.
- Carry-in bug: per-show `<sheet-name>` placeholder interpolation fix + class-sweep.

**Deferred to Phase B (out of scope here):** archived-shows display *and* archive/unarchive action;
persistent top-bar nav + mobile tabs + user menu; settings rebuild (Drive panel, preference toggles,
dev-tools, back-nav); the revoke-hang robustness fix; global AlertBanner placement/density/per-show
suppression (it stays mounted in `app/admin/layout.tsx:92-94` as-is for Phase A).

---

## 3. "Live now" — data layer

**Owner ruling (2026-05-30):** a published show is "live" when today (viewer TZ) falls anywhere in its
`travelIn → travelOut` span, inclusive (FULL active window). Computed from `dates`, **not** `schedule_phases`.

**Why `dates`, not `schedule_phases` (all verified live):**
- `shows` has no `schedule_phases` column and no event-date column other than `dates jsonb`
  (`supabase/migrations/20260501000000_initial_public_schema.sql:12`).
- `schedule_phases` is empty in prod (cron writes `{}` — `lib/sync/runScheduledCronSync.ts:502`) and is
  *derived from* `dates` via `deriveSchedulePhases` (`lib/parser/index.ts:272-307`; fallback at
  `lib/data/getShowForViewer.ts:267-273`). It deliberately omits `travelIn` (`lib/parser/index.ts:266`),
  so its key-range would under-count the full window.
- `dates.{travelIn,set,showDays,travelOut}` are ISO `YYYY-MM-DD` — they become `schedule_phases`'s ISO keys
  and feed the crew span gate `compareIso(todayIso, travelIn) >= 0 && compareIso(todayIso, travelOut) <= 0`
  (`lib/time/rightNow.ts:267-268`).

### 3.1 Shared span helper (orchestrator constraint 1 — pure extraction + negative regression)

New `lib/time/showSpan.ts`:

```
export function isTodayWithinShowSpan(
  todayIso: string,
  travelIn: string | null,
  travelOut: string | null,
): boolean
```
- **Guard (orchestrator constraint 2):** if `travelIn` OR `travelOut` is null/empty → return `false`
  (unbounded window ⇒ NOT live). Conservative default. (Plan-time: if §E or prototype implies a
  `showDays`-span fallback when travel bounds are missing, surface before building — default conservative.)
- Else: `compareIso(todayIso, travelIn) >= 0 && compareIso(todayIso, travelOut) <= 0` (reuse the existing
  `compareIso` from `rightNow.ts`).
- **Behavior-preserving extraction:** refactor `rightNow.ts:267-268` to call this helper. The existing
  `rightNow.ts` test suite MUST stay green unchanged — that green is the negative-regression proof that the
  crew-side `selectRightNowState` behavior is preserved.

### 3.2 Aggregate compute (server, in `fetchDashboardData`)

For each published show: resolve "today" ISO in that show's timezone using the **existing canonical resolver
`resolveTimezone(show)`** (`lib/visibility/packList.ts:72-76`), then `isTodayWithinShowSpan`. `liveCount` =
count of matches.

- **TZ resolution (verified, not hedged):** `venue.timezone` does **not** exist on `ShowRow.venue` today
  (no `timezone` field in `lib/parser/types.ts:87-93`). The codebase already reads it *defensively* for a
  future task: `resolveTimezone` returns `venue?.timezone` when populated, else `DEFAULT_TZ = "America/New_York"`
  (`packList.ts:60-76`); the crew `selectRightNowState` likewise defaults to `America/New_York`
  (`rightNow.ts:209`). So **today both crew and admin use `America/New_York` for every show**, and reusing
  `resolveTimezone` keeps "one notion of live" true now AND forward-compatible when venue tz lights up. Admin
  Live-now MUST reuse `resolveTimezone` (or its exact discipline) — do not invent a parallel tz path or a new
  field.
- **Plan-time verification V2:** `dates` is already selected (`Dashboard.tsx:59`); add `venue` (needed by
  `resolveTimezone`) and the per-show `last_synced_at`/`last_sync_status` (for the sync footer, §6) to the
  relevant `select(...)`.
- **Guard:** shows with empty/partial `dates` (e.g. `MI-3_NO_VALID_DATES`) → not live (helper returns false);
  never crash, never miscount.

---

## 4. Status-token system (D-1) + DESIGN.md amendment

The prototype's status colors (`Admin Redesign.html:296-303`): `.dot.live` = accent + ping; `.dot.ok` =
green `#2F7D4F`/`#6FCf95` (**the no-green violation**); `.dot.review` = accent; `.dot.warn` =
`#caa53a`/`#e9c45e`; `.dot.idle` = faint.

Per §E D-1, replace the green and define a named status set. New `@theme` tokens in `app/globals.css`
(light + dark runtime values + the `@theme` aliases, mirroring the existing `*-runtime` pattern at
`globals.css:38-64,185-232`), AA-verified both modes:

| Token | Meaning | Hue intent |
|---|---|---|
| `--color-status-live` | live (in active window) | FXAV accent orange (reuse `--color-accent`) |
| `--color-status-positive` (+ `-text` if contrast needs) | ok / synced / healthy | **NEW calm desaturated teal-leaning-neutral — NOT green** |
| `--color-status-review` | needs review | amber |
| `--color-status-warn` | stale / problem | amber, stronger than review |
| `--color-status-idle` | publishing / none | neutral/faint |

- Every status is **dot + text paired** (never color-only) — honors §9 color-blind floor.
- Live dot ping animation wrapped in `prefers-reduced-motion` (mirror `Admin Redesign.html:305`).
- **DESIGN.md amendment (required):** add a "§1.x Status-signal hues" subsection that (a) adds the token rows
  to the §1.1 table + §1.2 contrast figures, and (b) **justifies the single exception** to §1 "no competing
  accent hue (no blue, no purple, no teal)" / "orange stays alone": the status-positive hue is *narrowly
  scoped* to status dots/pills on sync/health state, always dot+text paired, never a CTA/brand/large surface,
  ≤ a few px dots + small pills. The accent stays orange; this is a status hue, not a second brand accent.
- New tokenized `components/admin/StatusIndicator.tsx` (dot + label); consumed via Tailwind utilities — **no
  inline hex/px** (§10).

---

## 5. Dashboard surface

Rework `components/admin/Dashboard.tsx`; `fetchDashboardData` extended to also return `liveCount`,
`crewTotal`, and the consolidated `needsAttention` list (reusing existing queries — no new tables).
Sub-components (new, all tokenized, no inline hex/px):

### 5.1 `StatStrip`
Props: `{ activeCount: number; liveCount: number; needReviewCount: number; crewTotal: number }`.
Renders 4 stat cells: **Active shows** / **Live now** (+ live dot when `liveCount > 0`) / **Need review** /
**Crew across all shows**. Tabular-nums on every number.
- **Guard conditions:** any count `0` renders "0" (not blank); the live dot is hidden when `liveCount === 0`
  (prototype `dashboard.jsx:84`); `NaN`/negative is impossible (counts derived from array lengths/sums) but
  the component coerces non-finite → `0` defensively. "Need review" number tinted `--color-status-review`
  only when `> 0`, neutral otherwise (prototype `dashboard.jsx:89`).
- **Definition — Need review count:** = `needsAttention.length` (the inbox item count, §5.3). Single source
  of truth; the stat and the inbox header count read the same value.

### 5.2 `ShowsTable` (replaces `ActiveShowsPanel` row list)
Props: `{ rows: ActiveShowRow[]; now: Date }`. Dense table: **Show** (title + client/meta) / **Dates** /
**Crew** / **Sync status** (`StatusIndicator`) / chevron. Whole row links to
`/admin/show/${encodeURIComponent(slug)}`.
- **Guard conditions:** empty `rows` → existing empty-state copy (preserve `ActiveShowsPanel`'s current empty
  treatment); `title` null → fallback label (preserve current `ActiveShowsPanel` behavior — verify at plan
  time); null `showDateStart`/`showDateEnd` → dash/"dates TBD"; `crewCount` 0 → "0".
- **Sync status mapping:** derive the `StatusIndicator` status from `lastSyncStatus`/`published`/live — exact
  mapping table specified in the plan (live → live; clean sync → positive; stale/failed → warn; publishing →
  idle; needs-review → review). Always dot+text.
- **Mobile:** collapse Dates/Crew/Sync into a stacked sub-line under the title (prototype `show-meta-mobile`,
  `dashboard.jsx:21`); hide the standalone columns < 720px.

### 5.3 `NeedsAttentionInbox`
Props: `{ items: NeedsAttentionItem[] }` where items consolidate the data `PendingPanel` already fetches:
`pending_ingestions` (couldn't-auto-apply, `Dashboard.tsx:160-191`) + first-seen `pending_syncs`
(`Dashboard.tsx:193-232`) + staged-review. One list, each item with a tone pill + actions.
- **Guard conditions:** empty `items` → `Empty` state "Nothing waiting on you" (prototype `dashboard.jsx:122`).
- **Cap/truncation:** the inbox can grow unbounded in principle; specify a max rendered count + "+N more"
  affordance in the plan (mirrors the AlertBanner queue-chip pattern) — pin the exact cap at plan time.
- No new data source; this is a re-grouping of existing fetched rows.

`DashboardFooter` retained (`Dashboard.tsx:302`).

---

## 6. Per-show surface (`app/admin/show/[slug]/page.tsx`)

- **Header:** keep `← Admin home` (`page.tsx:200-204`); title + **Published pill** (`shows.published`, real at
  `page.tsx:258`; no pill today); slug `<code>`; **share-link chip** — host-stripped display of the real URL
  `${NEXT_PUBLIC_SITE_ORIGIN}/show/${slug}/${token}` (`CurrentShareLinkPanel.tsx:77`), with copy. **Never** the
  prototype's fake `crew.fxav.co`.
  - **Guard:** when the share token is unavailable (`loadShowShareToken` null/throws — `CurrentShareLinkPanel.tsx:49-54`),
    the header chip is hidden (the in-panel "unavailable" notice already covers it); never render `/show/<slug>/null`.
- **Two-col split:** **Crew** ⟷ **Share & access**.
  - **Crew section:** "Open crew page" header action (opens the real share URL in a new tab; hidden when no
    token). Crew rows with avatar-initials; **Preview-as merged into each crew row** (the existing
    "Preview as a crew member" section, `page.tsx:255-353`, collapses into the Crew rows). Preserve every
    existing state: unpublished gate (`page.tsx:289-303`), `crewLookupFailed` (`page.tsx:304-311`), empty
    (`page.tsx:312-319`), per-member preview link (`page.tsx:341-347`).
  - **Share & access section:** `CurrentShareLinkPanel` (URL + copy) with **Rotate + Reset folded inside the
    panel** via dividers — move `RotateShareTokenButton` + `ResetPickerEpochButton` (`page.tsx:408-411`) inside
    the panel grouped with the URL card (prototype `showdetail.jsx:118-139`). Preserve each button's existing
    behavior + `router.refresh()` flow.
- **Parse warnings** (`ParsePanel`, `page.tsx:355-389`) kept as its own review section.
- **Sync health → quiet footer:** replace the standalone "Sync health" section (`page.tsx:219-253`, currently
  just a Re-sync button) with a footer status line — "Last synced `{relative}`" (+ `StatusIndicator`) and the
  `ReSyncButton` (prototype `showdetail.jsx:143-148`).
  - **Plan-time V2:** add `last_synced_at`/`last_sync_status` to the per-show `shows` select (`page.tsx:113`
    selects only `id, slug, title, drive_file_id, published`). **Guard:** null `last_synced_at` → "Not synced yet".
- **`PerShowAlertSection`** stays above the body, with the §7 fix.

---

## 7. `<sheet-name>` interpolation fix (carry-in bug)

`components/admin/PerShowAlertSection.tsx` renders alert copy via `safeDougFacing(code)` (`:52-55,161`) which
calls `messageFor(code)` with **no params** → catalog placeholders like `<sheet-name>` leak literally. The
global `AlertBanner` interpolates correctly by passing `alert.context` (`AlertBanner.tsx:205-218`).
`fetchPerShowAlerts` already selects `context` (`PerShowAlertSection.tsx:72`).

**Fix:** interpolate using the row's `context` — `messageFor(code, alert.context as MessageParams).dougFacing`
— and pass `context` as `params` to the per-show `HelpAffordance` (mirroring `AlertBanner.tsx:227-237`).
`lib/messages/lookup.ts:13-40` already accepts a params arg and normalizes hyphen↔underscore keys.

- **Contract note (brief exception check):** this only *calls* `messageFor` with its existing
  `(code, params)` signature — it does **not** change `lib/messages/lookup.ts`'s contract. Therefore the
  Phase-A no-Codex-diff-review rule holds; **no Codex leg** on `lookup.ts`. Re-confirm at implementation that
  the fix touches only `PerShowAlertSection.tsx` (call sites), not the helper's signature/behavior.
- **Class-sweep:** grep the catalog (`lib/messages/catalog.ts`) for every `<…>` placeholder token; for each,
  confirm every per-show render path passes the matching `context`. Report the sweep result in the handoff.
- **Negative-regression test:** render a per-show alert whose `context.sheet_name` is populated; assert the
  real name appears and the literal `<sheet-name>` does **not**. Stash the fix → test must fail (proves the
  contract is pinned, per the negative-regression rule).

---

## 8. Spec alignment (invariant 7)

Spec **§9.1** (dashboard) and **§9.2** (per-show) govern these surfaces. This plan supersedes:
- §9.1 flat active-shows list → stat strip + shows-table + needs-attention inbox IA.
- §9.2 flat stacked sections → two-col Crew⟷Share&access + folded share actions + sync footer.

The share/picker model follows the **2026-05-23 picker-pivot amendment**
(`docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-23-crew-auth-pivot-show-link-picker.md`), **not**
the stale signed-link spec. The writing-plans run MUST open an explicit spec-amendment note documenting each
supersession (invariant 7: open an amendment where the plan supersedes the spec; don't silently diverge).

---

## 9. Dimensional invariants (§7 — Tailwind v4 `items-stretch` is NOT default)

Every fixed-/equal-dimension parent→child relationship, with the guaranteeing class:

| Parent | Children | Relationship | Mechanism |
|---|---|---|---|
| StatStrip row | 4 stat cells | equal height | container `items-stretch`; each cell `h-full` |
| Dashboard split | ShowsTable col ⟷ NeedsAttention col | equal column height (desktop) | container `items-stretch` + `h-full` on each col |
| Per-show split | Crew col ⟷ Share&access col | equal column height (desktop) | container `items-stretch` + `h-full` on each col |
| ShowsTable header + rows | grid cells per row | shared column track widths | shared grid-template (prototype `.trow`/`.th`) |

**Plan MUST add a real-browser Playwright "Layout dimensions" task** asserting `getBoundingClientRect()`
child.height === parent.height within 0.5px for each `data-testid` above, on both the dashboard and per-show
renders, at desktop AND mobile widths. jsdom is insufficient.

---

## 10. Transition inventory

Surfaces are Server Components with thin client islands; client-side state transitions are minimal:

| Element | States | Treatment |
|---|---|---|
| Live status dot | static → ping loop | CSS ping animation; **disabled under `prefers-reduced-motion`** (mirror `Admin Redesign.html:305`) |
| Share-link chip / CopyBox | idle → copied → idle (~1.6-1.8s) | text/icon swap, instant (existing `ShareLinkCopyButton` pattern); no layout shift |
| Rotate / Reset buttons | idle → pending → success/error | preserve existing button-state behavior (`RotateShareTokenButton`/`ResetPickerEpochButton`); no change |
| Route enter / stagger (prototype `route-enter`/`stagger`) | mount | OPTIONAL subtle fade via `--duration-*` tokens, or instant; pick one in the plan, honor reduced-motion. Not load-bearing. |
| Needs-attention empty ↔ populated | server-rendered | instant — no client transition (full server re-render on navigation) |
| Published pill / sync footer | server-rendered | instant — no client transition |

Compound transition note: the copy-feedback timeout and the live-dot ping are independent and never interact
(different components); no compound-transition hazard in Phase A. The plan's "Transition audit" task confirms
each `AnimatePresence`/ternary/conditional has correct treatment.

---

## 11. Flag-lifecycle + tier×domain matrices

- **Flag lifecycle:** **N/A for Phase A.** No boolean config field or toggle is introduced (toggles are
  Phase B). No zombie flags created.
- **Tier × domain (DB) matrix:** **N/A for Phase A.** Phase A performs **no DB writes** and adds no
  columns/migrations/RPCs/CHECKs. It reads existing columns (`dates`, `venue`, `published`,
  `last_synced_at`/`last_sync_status`, existing pending/crew queries) and adds presentation + one derived
  count + a render-path bug fix.

---

## 12. Meta-test inventory

- **Supabase call-boundary** (`tests/admin/_metaInfraContract.test.ts`): `fetchDashboardData`
  (`Dashboard.tsx:38`) and `fetchPerShowAlerts` (`PerShowAlertSection.tsx:59`) are already registered. Phase A
  keeps them registered; any NEW server fetch helper added (e.g. if Live-now compute is factored into its own
  helper that calls Supabase) gets a registry row OR an inline `// not-subject-to-meta:` justification.
- **No new RPC-gated table** (no DB writes) → no PostgREST-DML-lockdown meta-test surface.
- **No new sentinel-hiding / admin-alert-catalog** surface.
- Declared: Phase A creates **no new** structural meta-test; it extends the existing infra-contract registry
  only if a new Supabase-calling helper is introduced.

---

## 13. Test plan (TDD)

- Per-task TDD; conventional-commit per task (invariant 6); scope `crew-page`/`admin` as appropriate.
- **Shared helper:** unit tests for `isTodayWithinShowSpan` — derive expectations from fixture dates (not
  hardcoded); edges: null travelIn, null travelOut, both null, today=travelIn, today=travelOut, today just
  outside each bound, MI-3 empty dates → false, dateless. Concrete failure mode each catches.
- **Negative regression (extraction):** `rightNow.ts` existing suite green after the refactor.
- **Live-now aggregate:** fixture with a mix of live/not-live/dateless shows; assert `liveCount` against the
  data source (the computed per-show booleans), **not** the rendered StatStrip container (anti-tautology).
- **`<sheet-name>` fix:** negative-regression as in §7.
- **Layout-dimensions Playwright** task per §9 (real browser).
- **Status-token contrast:** AA floor both modes (assert computed contrast or document calculated values in
  the DESIGN.md amendment).
- **Anti-tautology:** when scanning rendered DOM for a status label, clone + strip sibling nodes that
  independently render the same label before asserting.

---

## 14. Gates (close-out order)

1. TDD complete; all suites green; `pnpm` typecheck/lint green.
2. **UI invariant 8 — impeccable v3 dual-gate, BOTH external** (fresh subagent or user-invoked, NOT
   self-attested by the implementing session): `/impeccable critique` AND `/impeccable audit` on the Phase-A
   diff, with canonical preflight gates (PRODUCT.md / DESIGN.md / register / preflight). HIGH+CRITICAL fixed
   or `DEFERRED.md`-logged. Dispositions → handoff §12.
3. **Adversarial review (cross-model, Codex)** of the **spec + plan markdown** (this leg is mandatory for A).
   Iterate to APPROVE. Phase A does **not** additionally need a Codex review of the implementation diff (that
   leg is Phase B's migration/RPC territory) — UNLESS the `<sheet-name>` fix is found to change
   `lib/messages/lookup.ts`'s contract (§7), which would trigger a Codex leg on that file.
4. Whole-phase fresh-eyes pass + handoff doc; hand back to the M12 orchestrator.

---

## 15. Open plan-time verifications (carry into writing-plans)

- ~~**V1** venue timezone field existence/shape~~ — **RESOLVED in §3.2**: no `venue.timezone` today; reuse
  `resolveTimezone` (`packList.ts:72-76`), defaults `America/New_York` (matches crew). No field to invent.
- **V2** add `venue` + `last_synced_at`/`last_sync_status` to selects (§3.2, §6).
- **V3** preserve `ActiveShowsPanel` null-title + empty-state behavior in `ShowsTable` (§5.2).
- **V4** pin the needs-attention inbox max-render cap + overflow affordance (§5.3).
- **V5** confirm the `<sheet-name>` fix touches only `PerShowAlertSection.tsx` call sites (§7).
- **V6** decide route-enter animation: subtle token-driven fade vs instant (§10).
