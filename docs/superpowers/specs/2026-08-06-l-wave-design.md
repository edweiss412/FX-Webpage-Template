# L-wave — L-tier ledger convergence wave (classify, demote, decompose, ship)

**Date:** 2026-08-06 · **Arc branch:** `docs/l-wave-spec` · **Status:** DRAFT (pre-review)

## §0 Why this wave exists, and its baseline

The M-wave (`docs/superpowers/specs/2026-08-05-m-wave-design.md`, closed 2026-08-06 with PR #717/#718) cleared the READY mid-size queue. What remains oversized is the L tier: 29 open L entries, most of them not implementation-ready — stale premises, umbrella scopes, decision-fenced designs, and accepted limits filed as open work. This wave is the L-tier screen the user ratified on 2026-08-05 (AskUserQuestion batches, captured in `docs/superpowers/specs/2026-08-05-l-wave-decisions-brief.md`, committed on this branch): one SHIP, a set of probe-backed DEMOTEs, four DECOMPOSEs, one ENFORCE, one sentence-class DELETE, one PARKED stamp, one new honest filing, and a classification stamp for every newly-L entry the 2026-08-05 sizing sweep left unread. It runs fully autonomously under the AGENTS.md autonomous-ship gate.

Baseline census (`pnpm ledger:mass --json`, 2026-08-06 13:04 EDT, branch at post-#717 `a8b3a4128`): **80 open entries (XS 3 / S 16 / M 32 / L 29), mass 460, unsized 0, severity-unrecognized 0.** AC-PROG pins against these numbers. (The brief's 2026-08-05 baseline of 83/474 predates the M-wave W-UI close, which archived its remaining entries; `feat/m-wave-ui` merged as `a8b3a4128`, so the brief's staging rule for ledger-touching branches is satisfied.)

Wave pool: **29 claimed entries** — every EXISTING ledger entry the wave edits carries an invariant-12 marker: the 14 disposition entries (pushed at `f692f3cec`) plus the 15 stamp/classification targets (the 14 §2.1.6 entries and BL-WATCH-PROMOTION-ACTIVATION-RACE, pushed in the spec R1 repair commit — a stamp is an edit, so it is declared work; R1 F1). New filings (§2.1.4 children, §2.1.5) need no claim: a claim marks an existing entry in progress. Three themed units, each its own branch and PR.

| Unit | Branch | Work | Gate |
|---|---|---|---|
| W-LDOCS | `feat/l-wave-docs` | BL-COVERAGE-CLAIMS-CITE-SKIPPED-SUITES (delete class), BL-ATTENTION-PANEL-NAME-LEADING-SECTION + BL-RESYNC-STAGED-REVIEW-UI (archive), BL-ROLEFLAGSNOTICE-DROP-GUARD + BL-CI-OVERLAP-BOOT-WITH-SETUP + BL-CI-RECLASSIFY-PARALLEL-STABILITY + BL-ACCENT-BUTTON-ATOM-SWEEP (demote), BL-CI-PARALLEL-DB-FALLBACK-AUDIT (probe → demote or keep), BL-MUTATION-HARNESS-OPEN-HOLES + BL-E2E-LIFECYCLE-SPECS-CI-DARK + BL-OPS-LOG + BL-RESURRECT-MOBILE-SAFARI-E2E (decompose), BL-WATCH-PROMOTION-ACTIVATION-RACE (stamp), BL-PARSER-FIELD-PROVENANCE-MODEL (new filing), the 14 newly-L classification targets (§2.1.6; stamps + 3 executed archives + 1 resize — all claimed) | `impeccable-gate: N/A — no UI surface` |
| W-PUSH | `feat/l-wave-push` | BL-PUSH-NOTIFICATIONS — resize L→S + ship the Report-a-problem link in the notify templates | `impeccable-gate: N/A — no UI surface` (templates live in `lib/notify/`, not under invariant 8's UI definition; the copy rules still bind) |
| W-EMDASH | `feat/l-wave-emdash` | BL-EM-DASH-POLICY — lockstep sweep + structural guard (Opus, per the UI hard rule: touches `app/help/**` mdx, component copy, `DESIGN.md`) | impeccable dual-gate (critique + audit) |

## §1.1 Resolved scope — do not relitigate

All ratified 2026-08-05 by the user unless another source is cited. The brief at `docs/superpowers/specs/2026-08-05-l-wave-decisions-brief.md` is the capture of record; §4.5 below restates each answer.

1. **BL-COVERAGE-CLAIMS-CITE-SKIPPED-SUITES → DELETE the sentence class.** Prose coverage claims are unguardable; the sweep removes them. Deliberately NO new guard ships for this class (§4 limit 1). User ratification.
2. **BL-WATCH-PROMOTION-ACTIVATION-RACE → PARKED for its own future design session.** Not in this wave. Real defect per the 2026-08-04 screen ruling; the fix is a lock-topology redesign (M5-R20 nested-holder class). Stamp only — no archive, no patch.
3. **BL-EM-DASH-POLICY → ENFORCE** (the entry's resolution 2): sweep long dashes out of user-visible copy, guard prevents return. User ratification overrides the entry's own "(recommended)" tag on resolution 1.
4. **BL-ATTENTION-PANEL-NAME-LEADING-SECTION → KEEP the deliberate name, close.** Owner ruling recorded on the entry, archived. Zero code.
5. **BL-RESYNC-STAGED-REVIEW-UI → ARCHIVE.** The owner never asked for the diff-review workflow; re-open trigger is an operator asking for diff review.
6. **BL-PROJECTION-ALERT-VIEWER-INDEPENDENT-PROBE → LEAVE OPEN, untouched.** The user explicitly declined both archive and build. Not in the wave; no stamp beyond what exists.
7. **BL-STEP3-FULL-CREW-PREVIEW → LEAVE PARKED** for its own focused design conversation (adapter semantics). Untouched.
8. **NEW FILING: BL-PARSER-FIELD-PROVENANCE-MODEL,** an honest L filed by W-LDOCS, NOT implemented this wave (§2.1.5).
9. **The five reader DEMOTEs execute under the already-ratified filing bar** (AGENTS.md ledger filing bar + `docs/superpowers/specs/2026-08-04-backlog-convergence-design.md` §2): no per-entry user ask. BL-CI-PARALLEL-DB-FALLBACK-AUDIT is the one probe-GATED demote — probe first, then demote or keep per result (§2.1.3).
10. **The four DECOMPOSEs convert each umbrella L into sized children / an honest refile** per the brief's per-entry notes (§2.1.4).
11. **PREREQ fences stand:** BL-HEALTH-RESOLVE-DB-LOCKDOWN (closes only inside BL-ADMIN-POSTGREST-DML-LOCKDOWN), BL-HELP-NON-SHOW-REPORT-SURFACE (operator feedback), BL-TWO-WAY-SHEET-SYNC (Doug asks). Untouched.
12. **Fenced M-tier stays fenced (this spec's call, under the brief's "MAY settle" latitude):** BL-SERVER-ACTION-ORIGIN-GATE needs the deployment's trusted-proxy facts, which no session-local probe can establish; BL-SOURCE-ANCHORS-STALE-AFTER-FAILED-GID-FETCH's schema choice is better settled by the branch that implements it, with the migration matrix in front of it. Settling either here would be a probe-less analytical ruling — the exact class the round-economy rules exist to stop.
13. **Fully autonomous; both user review gates (spec, plan) WAIVED** (Call H, renewed by the 2026-08-06 kickoff). Stop only for a genuinely new question; an ambiguity that maps to a ratified answer is NOT a stop.
14. **Routing:** spec + plan authored in this Fable session; implementation + closeout in a NEW Opus pane (kickoff §4). UI work is Opus-owned per the AGENTS.md hard rule. W-EMDASH is the one dual-gated unit.
15. **All AGENTS.md invariants bind** — worktree-only (11), ledger claims + push (12), TDD (1), conventional commits (6), impeccable dual-gate on UI (8), mutation-surface observability (10), §12.4 lockstep triple, class-sweep discipline.
16. **This wave does not redesign ledger grammar, catalog grammar, or any guard beyond what an entry names.** The em-dash guard is the only new guard surface.

## §2 Per-entry contracts

Entry bodies in `BACKLOG.md`/`DEFERRED.md` are the spec-of-record for mechanics; this section states what the wave ADDS: the ratified decision, scope boundary, and acceptance shape. Every code claim below was grep-verified 2026-08-06 in the pre-draft citation pass; anchors are file + symbol (line numbers are drafting-time locators).

### §2.1 W-LDOCS (`feat/l-wave-docs`)

Not literally docs-only: the branch edits code COMMENTS (coverage-claims sweep) and runs the docs meta-suites plus the §2.1.3 probe, so preflight RUNS and the docs-only preflight exemption is NOT invoked.

**§2.1.1 BL-COVERAGE-CLAIMS-CITE-SKIPPED-SUITES — delete the sentence class.** Mechanics, per the entry's own methodological finding (the 12-site table is the SEED, not the bound):

1. Enumerate mechanically: every `describe.skip` / `test.skip` / `test.fixme` suite under `tests/` (12 files under `tests/e2e/` today, re-verified 2026-08-06: `grep -rln 'describe.skip' tests/e2e/ | wc -l` → 12).
2. For each, find citing prose repo-wide — by suite filename AND by the phrase families the entry measured escaping token-greps ("the compound tests", "the audit suite", "the helper covers", "exercised by e2e").
3. Each hit: DELETE the claim, or rewrite to honest present tense ("not exercised anywhere in CI; see <entry id>"). The two already-honest sites (`tests/visibility/capabilityTransitions.test.ts` — the M6-deferral note and the future-tense line the entry pins at :224/:272) are preserved verbatim, NOT "fixed".
4. The plan carries the exact sweep commands, their current output, and a per-hit disposition table (writing-plans reconciliation-sweep rule — authored AND RUN at plan time).
5. Archive the entry with the final site list and the ratified no-guard decision.

**§2.1.2 Zero-code closures + demotes** (archive convention: full body to `BACKLOG-archive.md` with dated resolution paragraph; flight markers stripped in the move; each archive's executable RED is the M-wave preamble pattern — move WITH marker, observe `tests/docs/_metaLedgerInProgress.test.ts` fail by name, strip, green):

- **BL-ATTENTION-PANEL-NAME-LEADING-SECTION.** Owner ruling recorded: the leading-section name is deliberate; the understatement is accepted. Trigger (screen-reader pass, or a redesign giving the panel a visible title) rides in the archive.
- **BL-RESYNC-STAGED-REVIEW-UI.** Archived per §1.1 item 5. The entry's own "promote it if the diff-review workflow is actually wanted" is the re-open trigger, preserved verbatim.
- **BL-ROLEFLAGSNOTICE-DROP-GUARD.** Demote: guards a hypothetical fifth site via an unbuilt static-analysis surface (recursive carrier tracking + exemption registry); all four known instances closed by the filing branch. The four refuted-cheaper-designs list and the locked-wrapper structural note (`lib/sync/runScheduledCronSync.ts`, emit attached to the locked wrapper) are the archive's preserved fix-shape.
- **BL-CI-OVERLAP-BOOT-WITH-SETUP.** Demote: built, MEASURED against real CI (96s → 102s leg-median, run pair 30783618781/30796409070), reverted under its own pre-ratified accept gate — an answered-negative question that never archived. Archive preserves: the measurement, the durable runner facts (probe run 29743206592), the validated `legfix`/`legwall` procedure pointer, and the named remaining lever (pre-baked Postgres image). One reconciliation note: the dormant spec-stage branch `origin/chore/ci-overlap-boot-with-setup` (no PR, claim inferred-only per `pnpm ledger:claims` 2026-08-06) is recorded in the archive entry as the spec's source branch; its final commit already says "stop the boot/setup overlap at spec stage".
- **BL-CI-RECLASSIFY-PARALLEL-STABILITY.** Demote: shelved by its own text (~17s contention-noisy win under its own 30s gate; flaking required-gate class). The two do-NOT-re-attempt-without conditions and the reusable-asset pointers (DB-touch probe, `DB_BINDING_SIGNALS`) ride in the archive.
- **BL-ACCENT-BUTTON-ATOM-SWEEP.** Demote at honest scope: "no correctness bug" (entry's words), census rotted (8 named baseline sites → 3 live in `MIGRATED_FILES`, `tests/styles/accent-button-atom.test.ts`, re-verified 2026-08-06 — the file's own comment tracks this entry). Archive preserves the per-site migration mechanics and the re-open trigger (a className-helper standardization pass or UI-consistency milestone); repo-wide token coverage remains `tests/styles/_metaBgAccentInventory.test.ts`'s job, stated in the archive so the demotion cannot read as coverage loss.

**§2.1.3 BL-CI-PARALLEL-DB-FALLBACK-AUDIT — probe-gated demote.** The entry is `**Reachability:** INFERRED, NOT PROBED` and names zero instances; its sizing self-contradicts (body "~691 files" vs the 2026-08-04 re-verification's 875). The probe IS the first scheduled step (the entry's own words):

- Run the `parallel` vitest project twice — once with every Supabase endpoint pointed at a CLOSED PORT, once with the local DB present — capturing per-file assertion counts (vitest JSON reporter; exact commands fixed at plan time and run per the reconciliation-sweep rule).
- **Validity gate first (R1 F3 — the decision rule is total only over VALID probes).** A probe run is VALID only if: both vitest invocations exit with a parseable JSON report; per-file results are present and the two runs cover the IDENTICAL file set; the DB-present run reports a nonzero total assertion count (executable premise); and the diff script validates its fields rather than defaulting absent ones to zero — the shipped precedent for exactly this class of misleading Vitest output is `scripts/run-excluded-test.mjs` (child-exit, numeric-field, `testResults`, exact-file-attribution validation). An aborted run, malformed JSON, or mismatched file set makes the probe INVALID: fix the harness and re-run; **an entry is NEVER archived on an invalid probe** — never silently wrong.
- Decision rule over a VALID probe, pre-ratified and total: **a file whose assertion count drops, that newly skips, or that reports all-skipped under the closed port is DEGRADING** (a skip under refusal IS the fallback shape the entry describes). **Zero degrading files → archive** the entry with the probe transcript (answered-negative, same class as CI-OVERLAP). **Any degrading file → the entry STAYS OPEN**, resized to the named instances with their count deltas; those files then move to serial or gain an explicit fallback-under-test note per the entry's original Work section.
- Either way the probe transcript is committed (in the archive entry or the resized body) so no future pass re-runs it to re-derive the same answer.

**§2.1.4 Decompositions.** Each umbrella converts to sized children or an honest refile; the umbrella archives with a decomposition record naming every child. New child entries satisfy the filing bar (each cites the umbrella's probe evidence) and are sized honestly.

- **BL-MUTATION-HARNESS-OPEN-HOLES → five standalone entries,** one per operator class already named as sub-ids in the body: `BL-MUTATION-REF-SUB`, `BL-MUTATION-UNICODE`, `BL-MUTATION-COLUMN-SHIFT`, `BL-MUTATION-MERGED-CELL`, `BL-MUTATION-SECTION-ORDER`. Each child carries: its corpus evidence (from the umbrella), its `OPERATOR_FINDING_MAP` linkage (`tests/parser/mutation/knownHoles.test.ts` pins that map covers every corrupting operator — the child ids must stay resolvable, which standalone entries satisfy), and the shrink-only ratchet contract (holes become `staleRows` on hardening; the nightly harness enforces). Sizing per child fixed at plan time (M default; honest per-class judgment).
- **BL-E2E-LIFECYCLE-SPECS-CI-DARK → honest refile.** The heading premise is stale: BOTH named specs are wired — `lifecycle-layout-e2e.yml` runs `admin-lifecycle-layout.spec.ts` AND `admin-lifecycle-transitions.spec.ts` on `--project=mobile-safari` (re-verified 2026-08-06, workflow lines re-checked). Residual = the ~60 app-dependent standalone-allowlist specs needing a dev server + seeded DB. Refile as a single honest entry (suggested id `BL-E2E-APP-DEPENDENT-SPECS-CI-DARK`, L) whose promotion path is the entry's own: land green batches incrementally, plus the recorded owner action (promoting e2e contexts into the required set is a GitHub-settings action, not repo code). Umbrella archives with the full wiring history.
- **BL-OPS-LOG → three sized children + archive.** The body contradicts its own scope: the durable sink is BUILT (`lib/log/persist.ts` writes `app_events`) and the redeem-link producer is MOOT (M11.5 banned surface). Children: (1) `BL-OPS-LOG-OAUTH-EMITS` (S) — durable `code:` emits on the `OAUTH_REDIRECT_INVALID` branches (anchor by token: `signInRedirect(request, "OAUTH_REDIRECT_INVALID", …)` in `app/auth/callback/route.ts` and `app/api/auth/google/start/route.ts`; the entry's line anchors have rotted — symbol anchors re-verified 2026-08-06); (2) `BL-OPS-LOG-ONBOARDING-EMIT` (S) — the `ONBOARDING_OPERATOR_ERROR` producer (`messageFor("ONBOARDING_OPERATOR_ERROR")` in `components/admin/OnboardingWizard.tsx` is render-only today); (3) `BL-OPS-LOG-DASHBOARD-BANNER` (M, Opus-owned UI, design-gated: the only current reader is the developer-gated `/admin/dev/telemetry` page — Doug needs a dashboard surface). Invariant-10 note carried on children 1–2: those emits are exactly the code-carrying emits the mutation-surface contract wants; the routes are already-instrumented-or-exempt today, and the children close the gap rather than widen it.
- **BL-RESURRECT-MOBILE-SAFARI-E2E → honest refile.** Premise partially refuted: CI DOES run the `mobile-safari` project for the two lifecycle specs (same workflow as above). Residual = the remaining ~20 M4 tile/crew specs, promoted per the entry's own incremental-green-batches path with `crew-e2e.yml`'s `CREW_E2E_ONLY` + `db:seed` pattern as the template. Refile at honest scope (id kept or `BL-MOBILE-SAFARI-TILE-SPECS-CI-DARK` — plan picks one, cross-referenced either way); umbrella text corrected in the same move.

**§2.1.5 New filing: BL-PARSER-FIELD-PROVENANCE-MODEL (L).** Files the remainder of the per-field provenance model as an honest entry: cites `docs/audits/e2e-real-world-variation-preparedness-2026-07-07.md` §7 item 5 (the "still the right long-term move" row) and the §10.2/§10.5 cross-flow P0-2 notes; records what already SHIPPED — ambiguity-warnings-v1 (#367: four judgment-call warn codes, `blockRef.field` anchors, wizard third state) and the `fast-check` property-fuzz layer (#379) — and that this row is the REMAINDER (the full provenance model for the P0-2 zero-signal residuals: MDY-monotone misread dates, wrong-but-`explicit` restrictions, heuristic-evading mis-splits, each named in the audit's §10.5 P0-2 row). Filing bar satisfied via the audit's named reachable surfaces; NOT implemented this wave.

**§2.1.6 Newly-L classification stamps** (the 2026-08-05 sizing sweep left 14 entries L with no reader classification; the brief directs this wave to classify them with the same screen). Each gets a dated `l-wave-screen 2026-08-06:` disposition line on its meta block; three also EXECUTE (archive) because the probe evidence is already in hand:

| Entry | Classification | Action this wave |
|---|---|---|
| BL-TASK-ENROLLMENT-SINGLE-DEPTH | PREREQ (design-gated multi-depth enrollment redesign; entry's own deferral-exception (c)) | stamp |
| BL-ADOPTION-PIN-REACHABILITY-BLIND | DEMOTE — a documented limit filed as open work; the guard header + five named behavioural backstops are the record; the entry's "act only if" triggers make it terminal | **archive** with cross-ref; triggers preserved verbatim (deleting a behavioural backstop converts the limit into a gap — stated in the archive) |
| BL-PUBLISHED-TOGGLE-CLIENT-COMMIT-WEDGE | PARKED-WATCH (measured upstream React replay-loss defect; mitigations gated on real-user reports or a vendored React fix; watch signals named in-body) | stamp |
| BL-PG-CRON-COVERAGE-UNRUN | KEEP at honest residual scope — per-job smoke residue only; resize L→M in the stamp commit (the wired-in-CI half closed 2026-07-26/27) | stamp + resize |
| BL-EXPORT-BLANK-ROW-SEGMENTATION | PREREQ-trigger (residuals have no corpus-clean discriminator; promote trigger in-body: a live mis-grouped show) | stamp |
| BL-TRANSPORT-ID-RESOLUTION | DEMOTE — the entry's REMAINS is refuted: id-based visibility SHIPPED as Flow 8.3b (#380; `transportTileVisible` Branch 0 "garble-proof id path" over `transportationOwnerIds`, `lib/visibility/scopeTiles.ts`, probed 2026-08-06; the preparedness audit's shipped-status table says "8.4 transport visibility fully closed") | **archive** with the probe; plan re-verifies the deferred regression pins landed with 8.3b before the move — if any pin is missing, the entry instead RESIZES to exactly that pin list |
| BL-NON-CREW-UNDO | PREREQ (operator explicitly wants non-crew undo; capture-widening cost judged then) | stamp |
| BL-PRIVATE-IMAGE-PIPELINE | PREREQ (scope floor; private-image-pipeline design session) | stamp |
| BL-ADMIN-PER-SHOW-HISTORY | PREREQ (scope floor; schema/data-model decision; possible bundle with BL-OPS-LOG-DASHBOARD-BANNER — cross-ref added) | stamp |
| BL-CREW-SHEET-TEMPLATE-V2 | PREREQ (scope floor; owner product decision on a standardized template) | stamp |
| SHARELINK-COPY-REF-ORDERING-PROOF (DEFERRED) | PREREQ (un-defer trigger: a scheduler harness that can resolve a promise between commit and passive effects) | stamp |
| SHARELINK-CUE-FORCED-COLORS-1 (DEFERRED) | PREREQ (repo-wide forced-colors pass sets the pattern) | stamp |
| ATTENTION-INDEX-JUMP-FOCUS-1 (DEFERRED) | PREREQ (own focus-orchestration spec; trigger: keyboard/SR report or next jump-handler change) | stamp |
| UNDO-FAILURE-REANNOUNCE-1 (DEFERRED) | DEMOTE — ratified documented limit (its own spec's §1.1 R8); the alternative is recorded as worse in-body | **archive** with cross-ref to the owning spec's ratification; un-defer trigger preserved |

The three executed archives use the same archive-RED pattern as §2.1.2. Stamps are prose lines; `tests/docs/` meta-suites green after every commit.

**§2.1.7 BL-WATCH-PROMOTION-ACTIVATION-RACE — stamp only.** One dated disposition line: "PARKED (ratified 2026-08-05) for its own lock-topology design session; not schedulable inside any wave (see the 2026-08-04 screen ruling in-body)." No archive, no patch, no other edit.

### §2.2 W-PUSH (`feat/l-wave-push`) — BL-PUSH-NOTIFICATIONS, resize + ship

The entry's prerequisites (a) and (c) are refuted by its own body: the design memo's provider decision is settled (Resend, `lib/notify/send.ts`) and delivery is LIVE (deliver/cron/digest shipped, verified in the entry's 2026-08-03 sweep). The residual is principle 6's PRIMARY mechanism: a "Report a problem" link in every push email (memo §120 form 1), absent from all four files in `lib/notify/templates/` (`autoPublishUndo.ts`, `digest.ts`, `realtimeProblem.ts`, plus the `escapeHtml.ts` helper — re-verified 2026-08-06: `grep -rn report lib/notify/templates/` → zero hits).

Scope, stated as an accept-set:

1. **Resize L→S in the same commit that ships the link** (the brief's instruction verbatim), with a staleness-repair note on the entry recording the two refuted prerequisites.
2. **Every email-body-producing template gains one footer "Report a problem" link — a NAVIGATIONAL entry point, and the spec says so plainly (R2 F2).** Probed 2026-08-06: the admin report affordance (`components/shared/ReportButton.tsx` → `ReportModal` → `POST /api/report`, show-scoped `showId`/`surfaceId`) is rendered by `PreviewBanner`, `DataQualityWarningControls`, and `StagedReviewCard` — all show-scoped contexts inside the show modal; NO admin surface unconditionally renders it, and no report-init GET route exists. The memo's full form-1 mechanism (one click from the email INTO a freeform report form with show/staging/parse context auto-attached) therefore requires a NEW surface — work the user's ratified resize ("residual is ONE Report-a-problem link in four notify templates … Resize L→S") deliberately does not carry, and whose non-show half is exactly PREREQ-fenced BL-HELP-NON-SHOW-REPORT-SURFACE (§1.1 item 11). What ships: shapes with a single show context link `${origin}/admin?show=<slug>` (the show modal — the surface that hosts the existing report controls; the per-show route's own redirect at `app/admin/show/[slug]/page.tsx` establishes this as the landed destination); shapes without one (digest and batch multi-show bodies, realtime `global`/`ingestion`) link `${origin}/admin`. **No new route, no new form, no `/api/report` contract change.** The delta between this navigational pointer and memo form 1 is recorded as a documented limit in the archive entry (§4 limit 6), with its un-archive trigger: operator feedback that the pointer is insufficient, or promotion of the fenced report-surface entry.
3. **Tests (R1 F4 — the accept-set is render SHAPES × body CHANNELS, not source files):** the current template API exposes five entry points producing seven distinct shapes — `renderAutoPublishUndo`, `renderAutoPublishUndoBatch`, `renderDigest`, `renderRealtimeProblem` with `kind` `show` | `global` | `ingestion`, and `renderRealtimeProblemBatch` — each returning `RenderedEmail = { subject, html, text }`. One unit row PER SHAPE, asserting the link in BOTH the `html` body (anchor with expected href) AND the `text` body (the labeled URL) — a batch path or plaintext channel missing the link fails by name. Hrefs fixture-derived per the anti-tautology rule; visible link text "Report a problem"; no em dash; no new §12.4 code (the link is navigation, not an error surface). If implementation adds or finds another shape, it gets a row — the shape list above is the census, the per-shape rule is the contract.
4. **Invariant-10:** template render is not a mutation surface; no registry row. Archive the entry (as the resized S) on merge with the memo cross-ref; the memo's forms 2–3 (reply-to ingest, apply-from-email) and the UNVERIFIED Doug-observation prerequisite stay recorded in the archive as explicitly NOT shipped.

### §2.3 W-EMDASH (`feat/l-wave-emdash`) — BL-EM-DASH-POLICY, enforce (Opus, dual gate)

Ratified: resolution 2, ENFORCE. `DESIGN.md` §9 ("No em dashes. … Also not `--`.", DESIGN.md:401) becomes enforced rather than aspirational, on a stated accept-set of covered surfaces.

**Covered surfaces (the guard's accept-set — keyed on structure: EXECUTABLE STRING LITERALS and rendered text, never comments; everything outside the set is out of scope by name, §4 limit 2). Corrected per the R1 F2 AST probe — the earlier raw-character censuses counted comments:**

1. `lib/messages/catalog.ts` — executable string literals. Census 2026-08-06 (AST): **0** copy strings carry U+2014 today; the file's 9 raw hits are all comments. The catalog is covered so regression is loud, and the §12.4 lockstep triple (master-spec §12.4 prose + `pnpm gen:spec-codes` regen + `catalog.ts`, same commit, x1 the proof) is a CONTINGENT obligation — it fires only if a row string actually changes, which the census says none will.
2. `app/help/**/*.mdx` prose, fenced code elided (2 files / 20 instances today).
3. **Every executable string literal and JSX text node** in `components/**`, `app/**` excluding `app/api/**`, **and ALL of `lib/**`** (R3 F1: the ratified contract is ALL user-visible copy, and an R3 AST probe found 66 executable em-dash literals outside the previous components/app/notify set, 19 of them live product copy — Step-3 tier strings in `lib/admin/step3ReviewItemTiers.ts`, watch-escalation operator emails in `lib/drive/watchEscalation.ts`, parser warning messages in `lib/parser/warnings.ts`, `lib/sync/blockDisappearance.ts`, `lib/sync/enrichTransportAssignees.ts`, generated schedule titles in `lib/parser/blocks/scheduleBookends.ts`, and admin change-log labels in `lib/sync/changeLog/fieldChanges.ts` rendered by `RecentAutoAppliedStrip` — those files join the seed inventory). The remaining ~47 non-copy literals the widened scan sweeps in are dispositioned per hit: repaired if user-visible, exempted by name with reason if internal (a directory-level registry row with reason is legal for wholly-internal trees such as `lib/dev/**`). `app/api/**` stays excluded WITH ITS REASON STATED: route handlers surface user-visible text exclusively through §12.4 catalog codes (invariant 5), so their literals are internal by contract — and the catalog itself is surface 1. Comment-stripped; this deliberately includes exported copy constants, return literals, and string expressions rendered as JSX children (the R1 probe's escaping class: `app/show/[slug]/unpublish/copy.ts`, `components/admin/roleRecognizeCopy.ts`, `PublishedToggle`, `ShowsTable`, `ShowReviewSurface`, `AttentionMenu`, `DriveConnectionPanel`, `step3ReviewSections`, `Gallery`, `app/admin/dev/page.tsx` — ~25 sites across 10 files, plus the template-fragment site `components/admin/nav/NotifBell.tsx` — the seed inventory). **"Executable string literal" is defined by NODE KINDS, not by a predicate name (R2 F3):** `StringLiteral`, `NoSubstitutionTemplateLiteral`, AND the interpolated template fragments `TemplateHead`/`TemplateMiddle`/`TemplateTail` (TypeScript's `isStringLiteralLike` excludes the fragments — probed; a scanner built on it alone silently misses live covered copy, including both notify hits below), plus `JsxText` and JSX attribute string values. Non-copy literals the scan sweeps in (test ids, class names) are handled by the reasons-required exemption registry, not by narrowing the scan — a denylist of "copy-looking" contexts is exactly the accept-set failure mode.
4. (Subsumed into item 3 by the R3 widening — `lib/notify/**` is part of `lib/**`. The two known notify copy hits, `lib/notify/templates/realtimeProblem.ts:121` and `lib/notify/templates/autoPublishUndo.ts:127`, are both `TemplateMiddle` fragments and stay in the seed inventory.)

**Guard = the enumerator.** A structural meta-test scans the covered surfaces for U+2014 (and, in MDX prose text and catalog/notify copy strings only, the literal `--` token — NOT in surfaces where `--` is load-bearing syntax: CSS custom properties, CLI text). It lands RED first; its failure list IS the sweep list (this satisfies the reconciliation-sweep rule mechanically — the plan includes the guard's current hit output, seeded by the R1 F2 probe inventory above but bounded by the guard's own scan, not by that list). Every hit is repaired (commas/colons/periods/parentheses per §9) or exempted by name in a reasons-required registry. The guard states its premise executably (`tests/_shared/premise.ts`): a planted-dash fixture proves it can fail.

**Ripple obligations, enumerated up front:**

- **Test pins.** Copy strings asserted verbatim in tests (the Held/not-published status string pinned in `ShowsTable`/`perShowPage` tests, the archive-confirm copy in `ArchiveShowButton` tests, and every peer the sweep's grep finds) update in the same commit as their source string — the class-sweep rule applied at round 0.
- **Screenshot baselines.** `public/help/screenshots/` holds 14 committed WebPs; if any changed help page is a captured surface, baselines regenerate FROM the pinned Playwright Docker image with `--platform linux/amd64` (byte-comparison discipline; never from the dev machine). The plan resolves which captures are affected.
- **`DESIGN.md` §9** gains one sentence naming the guard as the enforcement, in the same PR (a DESIGN.md edit — part of why this unit is dual-gated).

**Convergence contract (stated here because the guard is a detector surface):** consequence bound — every covered surface is dash-free or exempted by name; a new dash fails CI naming its file; nothing is ever silently rewritten, so the guard is never silently wrong. Threat-model fence — accidental authoring by an ordinary contributor; adversarial obfuscation (homoglyphs, HTML entities, U+2013 en dash substitution) is OUT of scope and files to §4's documented limits. The accept-set is keyed on surface + code point, not on spelling variants.

### Dimensional Invariants

This wave introduces NO new fixed-dimension parent with flex/grid children and NO new rendered component. W-EMDASH is copy-string replacement only (no box-model change on any surface); W-PUSH adds an inline anchor to email HTML (no fixed-height container); W-LDOCS renders nothing. If the plan or implementation introduces any fixed-dimension parent→child relationship after all, that task must add the relationship here plus the real-browser `getBoundingClientRect` assertion per the writing-plans layout-dimensions rule — absence of entries in this section is a claim, and the W-EMDASH dual-gate audit checks it.

### Transition Inventory

No new or changed visual STATE ships in any unit: copy-string swaps, an email footer link, and ledger prose have no state pairs. No `AnimatePresence`, no exit/initial/animate props, no conditional render changes. If a task adds one, the inventory gains its pairs first — same contract as the M-wave's §2.2 inventory.

## §3 Sequencing + claim-handoff protocol

1. `docs/l-wave-spec` (this branch) carries spec + plan and currently claims all 29 entries (Stage 0 at `f692f3cec` + the R1 repair commit).
2. HANDOFF-BY-OVERLAP, same order-binding protocol the M-wave ratified (its §3): FIRST the three unit branches are created off `origin/main`; each runs `pnpm ledger:claims --check <its ids>` FROM THE MAIN CHECKOUT expecting exit 1 naming `docs/l-wave-spec` and ONLY it (the planned-handoff signature; any other branch = real collision, stop). Each marks its subset `**Status:** IN PROGRESS · **Branch:** <unit branch>`, commits, pushes. Unit subsets: W-LDOCS takes 27 ids (the 12 docs-side disposition ids plus the 15 stamp/classification ids), W-PUSH takes BL-PUSH-NOTIFICATIONS, W-EMDASH takes BL-EM-DASH-POLICY — 29 total, matching §0's claimed set exactly; no entry the wave edits is ever undeclared on origin (R1 F1).
3. THEN this branch's last pre-merge commit removes all 29 markers. At no instant is any entry undeclared on origin; the transient dual-declaration is the designed handoff state.
4. This branch's PR (spec + plan + brief + marker handoff) merges first — docs-only, preflight skip declared in the PR body. Then units implement in order W-LDOCS → W-PUSH → W-EMDASH (largest copy churn last, on a quiet base), each: worktree, Stage-0 claims already held, TDD, cross-model review, real CI green → merge → `0 0`. Later units merge `origin/main` after each earlier unit lands (BACKLOG/archive conflicts resolve mechanically — both sides' edits stand).
5. Each unit strips its markers in its last pre-merge commit as its entries archive/restamp (invariant 12).

## §4 Documented limits (this wave's own)

1. **The coverage-claims class can recur.** Ratified: the sentence class is deleted, NOT guarded — prose coverage claims are unguardable (§1.1 item 1). A future claim is a review matter, not a CI matter. The archive entry says so.
2. **The em-dash guard covers exactly its §2.3 accept-set** — which, post-R3, spans `components/**`, `app/**` (minus the catalog-mediated `app/api/**`), `lib/**`, help MDX, and the catalog. Out of scope by name: code comments, `docs/**` prose, spec files, `tests/**`, `scripts/**`, en dashes, homoglyphs, and HTML entities. Widening further is a future decision, not drift.
3. **The §2.1.3 disposition is conditional by design.** "Archive" and "stays open, resized" are both compliant outcomes; the probe transcript is the record either way.
4. **Stamps are classifications, not closures.** The 11 stamp-only entries in §2.1.6 plus the BL-WATCH-PROMOTION-ACTIVATION-RACE stamp remain open; the wave's claim is that each now carries a reader ruling, not that the work happened.
5. **BL-PARSER-FIELD-PROVENANCE-MODEL is filed, not built.** Its implementation is explicitly outside this wave.
6. **W-PUSH ships a navigational entry point, not memo form 1's one-click report form.** Probed: no unconditional admin report affordance exists to link to (§2.2 item 2); the shipped link lands the operator on the show modal (or dashboard) where the existing show-scoped report flows live. The full form-1 mechanism (auto-attached context, report-init surface), reply-to ingest (form 2), and apply-from-email (form 3) all remain unshipped and are recorded as such in the archive entry, with the un-archive trigger named.
7. **Demotions leave durable records** reachable by grepping the entry id (archive + cross-ref); a wrong demotion is recoverable by construction.

## §4.5 Ratified answers (2026-08-05, capture of record: the decisions brief)

1. Coverage claims: **DELETE the sentence class** (docs-only, closes the L).
2. Watch-promotion race: **PARKED** for its own design session; stamp, no archive, no patch.
3. Em-dash policy: **ENFORCE** — lockstep sweep + structural guard; Opus under the dual gate where UI files are touched.
4. Attention-panel name: **KEEP + close** (owner ruling recorded, archive, zero code).
5. Resync staged-review UI: **ARCHIVE** (re-open: an operator asks for diff review).
6. Projection-alert probe: **LEAVE OPEN, untouched.**
7. Step-3 full crew preview: **LEAVE PARKED.**
8. Per-field provenance: **file as an honest L** (BL-PARSER-FIELD-PROVENANCE-MODEL), not implemented.
9. Reader demotes (ROLEFLAGSNOTICE, CI-OVERLAP, CI-PARALLEL probe-gated, CI-RECLASSIFY, ACCENT-BUTTON), decomposes (MUTATION-HARNESS, E2E-CI-DARK, OPS-LOG, RESURRECT-MOBILE-SAFARI), ship (PUSH-NOTIFICATIONS resize L→S), prereq fences — per the brief's per-entry notes, executed as §2.1–§2.2.
10. Newly-L entries: classified by this wave with the same screen (§2.1.6).
11. Fully autonomous (Call H); both user review gates waived; Fable authors spec + plan, a new Opus pane implements.

## §5 Meta-test / registry inventory (pre-declared for the plan)

- **CREATES:** the em-dash copy guard + its exemption registry (§2.3), with a planted-dash premise fixture; per-template link-presence unit rows (W-PUSH).
- **EXTENDS:** nothing structural — archives/stamps/refiles are exercised by the existing `tests/docs/` meta-suites (`_metaLedgerInProgress`, `_metaLedgerReferentialIntegrity`, sizing/claims checks), which walk the ledger from disk and cover new child entries by default. `tests/styles/accent-button-atom.test.ts` and `_metaBgAccentInventory` are unchanged (the demotion touches prose only). The x1 catalog-parity gate proves the W-EMDASH lockstep.
- **Invariant-9/10 registries:** no new Supabase call site, no new mutation surface in any unit (W-PUSH is template markup; W-EMDASH is copy + a test; W-LDOCS is prose + comments). A plan-time discovery to the contrary adds the registry row in the same commit.
- **§12.4 lockstep:** touched only by W-EMDASH catalog rows (§2.3 surface 1); W-PUSH adds no code and no row.

## §6 Acceptance criteria

- **AC-L1 (W-LDOCS):** every §2.1 disposition landed — the coverage-claims sweep executed grep-driven with its per-hit table; 2 closures + 4 demotes archived with records; the §2.1.3 probe ran and its disposition matches its result, transcript committed; 4 umbrellas decomposed with every child filed, sized, and cross-referenced (and `OPERATOR_FINDING_MAP`'s BL- refs still resolve); BL-PARSER-FIELD-PROVENANCE-MODEL filed citing the audit; all 14 newly-L entries stamped per the §2.1.6 table with the 3 executed archives done (or TRANSPORT-ID resized per its probe branch); WATCH-PROMOTION stamped PARKED; `tests/docs/` green on every commit.
- **AC-L2 (W-PUSH):** all seven render shapes (§2.2) carry the "Report a problem" link in BOTH `html` and `text` bodies, unit-proven per shape with fixture-derived hrefs; no new route/form/`/api/report` change; the entry resized L→S in the shipping commit and archived on merge with forms 2–3 recorded unshipped.
- **AC-L3 (W-EMDASH):** the guard covers all four §2.3 surfaces, failed RED pre-sweep, and fails by name on the planted fixture; zero unexempted U+2014 in covered surfaces at merge; every exemption carries a reason; §12.4 lockstep commits green through x1; every affected test pin updated in the same commit as its string; screenshot baselines regenerated from the pinned image iff a captured page changed; `DESIGN.md` §9 names the guard; impeccable dual-gate run with P0/P1 fixed or DEFERRED-entried.
- **AC-L4 (process):** claim handoff per §3 with no undeclared instant; markers stripped in each PR's last pre-merge commit; conventional commits; TDD per task; `impeccable-gate:` marker lines per unit per invariant 8.
- **AC-PROG:** at wave close, `pnpm ledger:mass` totals strictly below the §0 baseline — entries < 80 AND mass < 460 — recomputed and recorded in the closeout. (Exact deltas depend on the two probe-gated branches (§2.1.3, TRANSPORT-ID) and per-child sizing; the strict decrease is the pinned claim, the closeout records the arithmetic.)

## §7 Impeccable gate

Per-unit as the §0 table states: W-EMDASH carries the dual gate (its diff touches `app/help/**`, component copy, and `DESIGN.md`); W-LDOCS and W-PUSH are `impeccable-gate: N/A — no UI surface`. If any W-PUSH or W-LDOCS task unexpectedly touches an invariant-8 surface, that unit's gate flips to the dual gate before merge.
