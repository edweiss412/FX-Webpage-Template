# Design mocks — extend-role-scope-vocab

Fetched verbatim (2026-07-15) from Claude Design project "Role recognition control for FXAV"
(`https://claude.ai/design/p/8658e1ec-fa7d-4ff7-9776-9df9f10b7405`) via DesignSync.

- `Recognize Role Control.dc.html` — inline warning-attached control, all 6 states + trigger explorations (A neutral-outline chosen) + desktop two-up layout.
- `Roles You've Added.dc.html` — settings list page: rows with plain-language chips, inline edit (same checkbox set), two-step inline remove confirm, empty state, desktop grid rows.

`support.js` (Claude Design runtime) is deliberately not committed, matching every prior `*-mock/` snapshot (see `2026-07-06-telemetry-console-redesign-mock/`). The `.dc.html` markup + inline styles are the design source of truth for implementers; interactivity requires the Claude Design canvas.

Spec: `../2026-07-15-extend-role-scope-vocab.md` (§8 surfaces, §9 copy pins reference these mocks).
