# Flow 8.4 — Transport-Assignee No-Match Admin Warning

**Date:** 2026-07-09
**Branch / worktree:** `feat/flow8.4-transport-id` / `.claude/worktrees/flow8-transport-id`
**Status:** DRAFT (autonomous ship; spec + plan user-review gates waived per `/ship-feature`)
**Audit item:** `docs/audits/e2e-real-world-variation-preparedness-2026-07-07.md` §Flow 8, item 8.4
**Backlog item (partial closure):** `BACKLOG.md` → `BL-TRANSPORT-ID-RESOLUTION`

---

## 1. Problem

A crew member assigned as a show's driver should see their own ground-transport itinerary (pickup times, vehicle, parking) on their crew page. `transportTileVisible` (`lib/visibility/scopeTiles.ts:177-203`) decides "is this viewer the driver / an assignee?" by fuzzy name match — `namesRefer(transportation.driver_name, viewerName)` and `schedule[*].assigned_names` (`scopeTiles.ts:190-202`). `namesRefer` (`lib/data/nameMatch.ts:63-65`) tolerates first-name / nickname / legal-name / case / trim variance, so the common cases already resolve.

It does **not** tolerate a **hard mis-parse** — a merged-cell overflow that fuses an adjacent spreadsheet cell onto the name, e.g. a driver exported as `"Doug Larson Loadout"` against roster `"Doug Larson"`. `refersSingle` takes the both-multi-token branch (`nameMatch.ts:53`) and compares only the last tokens — `tokCompat("loadout", "larson")` (`nameMatch.ts:37-39`) is neither equal nor a prefix, so `namesRefer` returns `false`. Result: the driver-crew-member silently does not see their own transport block, **and no operator is warned anything is wrong.** The failure is silent — that is the harm this spec removes.

### What this spec is NOT

The audit/backlog headline ("resolve `driver_name`/`assigned_names` to `crew_member` ids at enrich, persist the id set, match visibility by id") rests on two premises the live code invalidates:

1. **No crew_member id exists at enrich time.** `ParseResult.crewMembers: CrewMemberRow[]` (`lib/parser/types.ts:410`) carries `{ name, email, phone, role, role_flags, date_restriction, stage_restriction, flight_info }` (`types.ts:84-93`) — **no `id`**. The stable uuid is DB-assigned at APPLY (`crew_members.id uuid ... default gen_random_uuid()`, `supabase/migrations/20260501000000_initial_public_schema.sql:32`; written by `upsertCrewMembers`, `lib/sync/applyParseResult.ts:129`). There is nothing to persist at enrich.
2. **Match-by-id at render is behavior-neutral.** Resolving `driver_name` → roster id at render would use the *same* `namesRefer` binding that already fails on `"Doug Larson Loadout"`, so it closes nothing.

Persisting ids would require APPLY-time resolution + a DB migration, and even then the mis-parsed name resolves to no id at apply too — the driver stays invisible until an operator fixes the source data. **The operator warning is the only mechanism that closes the silent gap in every design.** This spec ships exactly that mechanism and nothing else.

**Explicitly out of scope** (remain in `BL-TRANSPORT-ID-RESOLUTION`, targeted for the later 8.3 enrich spec): DB migration, persisted resolved-id columns, apply-time id resolution, any change to `transportTileVisible` or the crew-page render, any new `admin_alert` code, any new push/email/telemetry surface. Flow 8.3 (venue timezone) is a separate later spec.

---

## 2. Design

### 2.1 New enrich step — `lib/sync/enrichTransportAssignees.ts`

A pure, synchronous, best-effort enrichment step. It performs **no I/O** (no Drive, no Supabase — therefore not subject to the `_metaInfraContract` Supabase call-boundary meta-test), reads `result.crewMembers` + `result.transportation`, and mutates `result.warnings` only. It never throws.

```ts
export function enrichTransportAssignees(result: ParseResult): void
```

Called from `enrichWithDrivePins` (`lib/sync/enrichWithDrivePins.ts`) immediately after `await enrichVenueGeocode(result)` (`enrichWithDrivePins.ts:423`), before `return result` (`:425`). No `await` (the function is synchronous). Placing it in `enrichWithDrivePins` means all sync paths (onboarding scan + cron) inherit it, exactly like `enrichAgenda` / `enrichVenueGeocode` (`enrichWithDrivePins.ts:413-423`).

### 2.2 The predicate — near-miss AND not-cleanly-resolved

For each **candidate assignee name** — `transportation.driver_name` (when non-null) and every string in every `schedule[*].assigned_names` (`types.ts:187-192`) — first **split on `/`** into sub-names (mirroring `namesRefer`'s own `a.split("/")`, `nameMatch.ts:64`) so a slash-merged assignment `"Doug / Jane"` classifies each person independently instead of false-warning as an overflow. Classify each sub-name. Two primitives:

- **Significant tokens** of a name = normalized (lowercase, diacritic-folded, suffix-stripped like `nameMatch.ts:17` `SUFFIX_RE`) tokens of length ≥ 2. Computed by a helper **private to the enrich module** (§2.3) — the length ≥ 2 filter never touches `namesRefer`, which keeps one-letter tokens for initial matching.
- **`namesRefer(roster, candidate)`** — the unchanged fuzzy matcher (`nameMatch.ts:63`). Its multi-token rule is **surname-only** (`nameMatch.ts:53`), which is the lenient behavior that both (a) resolves nicknames/initials and (b) causes the overflow-mismatch this predicate must catch.

Two derived sets over the roster (each member's `significantTokens(name)`):

- **`covers(C, M)`** — every significant token of member `M` has a `tokCompat` match (equal-or-prefix, `nameMatch.ts:37`) among `C`'s significant tokens. I.e. `C` contains `M`'s whole name. (`covers("Doug Larson John Smith", "John Smith")` and `covers(…, "Doug Larson")` are both true; `covers("John Michael Smith", "John Smith")` is true; `covers("Doug", "Doug Larson")` is false — `larson` missing.)
- **`coveredCount(C)`** = number of **distinct** roster members `M` with `covers(C, M)`.

Classification of a non-sentinel candidate `C`:

1. **Skip (sentinel)** if `shouldHideGenericOptional(C)` is true (empty / `TBD` / `N/A` / `TBA` / `-` / `—` — `lib/visibility/emptyState.ts:79`, backed by `GENERIC_OPTIONAL_HIDE` at `:56`, the same helper Flow 8.1 used). Not a real assignment.
2. **Not a near-miss → skip** if `C` shares **no** significant token with **any** roster member. Unrelated external / charter driver (`"ABC Charters"`) — warning would be chronic noise.
3. **Fusion → warn** if `coveredCount(C) >= 2` — `C` contains the whole name of two-or-more distinct roster members, so it is a merged-cell fusion of multiple people (`"Doug Larson John Smith"`). This branch warns **even though `C` `namesRefer`-resolves** (by surname) to one of them, because the *other* member(s) it contains are silently hidden.
4. **Resolved → no warn** if `coveredCount(C) < 2` **and** some roster member `M` has `namesRefer(M.name, C)`. A single identity that the render predicate already shows — an abbreviation / initial / nickname / **middle-or-legal-name variant** (`"John Michael Smith"` for `"John Smith"`; `coveredCount = 1`).
5. **Otherwise → warn** (near-miss, unresolved, not a multi-person fusion): `C` shares a token with the roster but no member `namesRefer`-resolves it — a single garbled name (`"Doug Larson Loadout"`, where `"loadout"` breaks the surname match).

**Why coverage, not token count (R2 → R3).** R2 showed "resolved to anyone → safe" is wrong: a fusion `"Doug Larson John Smith"` surname-matches `"John Smith"` (verified `namesRefer(…,"John Smith") === true`, `namesRefer(…,"Doug Larson") === false`) yet hides Doug. A naïve fix — warn when the candidate has **more** significant tokens than the matched member — over-fires on legitimate longer variants: `"John Michael Smith"` (3 tokens) resolves to `"John Smith"` (2) and the crew member already sees the tile, so warning is a false data-gap. The correct signal is not token *count* but **how many distinct roster identities the candidate fully contains**: a fusion contains ≥2 (`Doug Larson` + `John Smith`); a middle name contains exactly 1 (`John Smith` — `"michael"` is nobody on the roster). Rule 3 keys on `coveredCount >= 2`, so the middle-name variant (`coveredCount = 1`) falls through to rule 4 and does not warn.

Worked examples (all `namesRefer` / coverage values verified against the live matcher; roster = the "Roster has" column):

| Candidate | Roster has | `namesRefer` resolves? | `coveredCount` | Outcome |
|---|---|---|---|---|
| `Doug Larson John Smith` | `Doug Larson`, `John Smith` | yes → `John Smith` | 2 | **warn** (fusion — rule 3) |
| `Doug Larson Loadout` | `Doug Larson` | no (`loadout`≠`larson`) | 1 (`covers` Doug Larson) but unresolved | **warn** (garbled — rule 5) |
| `John Michael Smith` | `John Smith` | yes → `John Smith` | 1 | no warn (middle name — rule 4) |
| `Doug` | `Doug Larson` | yes → `Doug Larson` | 0 | no warn (abbrev — rule 4) |
| `Douglas Larson` | `Doug Larson` | yes → `Doug Larson` | 1 | no warn (rule 4) |
| `Bill Werner` | `William Werner` | yes (surname) | 0 (`bill`≠`william`) | no warn (nickname — rule 4) |
| `J` | `John Larson` | yes (initial) | 0 | no warn (rule 4) |
| `  doug larson ` | `Doug Larson` | yes → `Doug Larson` | 1 | no warn (rule 4) |
| `ABC Charters` | `Doug Larson` | no | 0, shares 0 tokens | skip (external — rule 2) |
| `Doug / Jane` | `Doug Larson`, `Jane Doe` | split → each rule 4 | — | no warn (slash-split) |
| `` / `TBD` / `—` | any | — | — | skip (sentinel — rule 1) |

Note `Doug Larson Loadout` has `coveredCount = 1` (it contains all of `Doug Larson`) but is **unresolved** (surname `loadout` breaks `namesRefer`), so it warns via rule 5, not rule 3. Rules 3 and 5 are disjoint warn paths; rule 4 is the only no-warn-after-near-miss path.

### 2.3 Token primitive — private to the enrich module, `nameMatch.ts` untouched

**`lib/data/nameMatch.ts` is NOT modified.** `namesRefer` / `toks` / `refersSingle` keep their exact current behavior — critically, `toks` (`nameMatch.ts:22-33`) retains one-letter tokens so `namesRefer` still matches initials by prefix (an initial-only assignee resolves). Refactoring the shared matcher to drop one-letter tokens would silently change hotel AND transport visibility globally; this spec forbids that.

The near-miss check uses a **separate, private** helper inside `lib/sync/enrichTransportAssignees.ts` (not exported, not shared with `namesRefer`):

```ts
// private to enrichTransportAssignees.ts
function significantTokens(s: string): string[] // lowercase, strip SUFFIX_RE-style suffixes, keep tokens length >= 2
```

`enrichTransportAssignees` calls the unchanged `namesRefer` for the **resolved?** check (§2.2 rule 4) and its own `significantTokens` for both the **near-miss?** check (rule 2) and the **`covers` / `coveredCount`** computation (rules 3–4). The two concerns are deliberately decoupled: `namesRefer` decides visibility (initials included); `significantTokens` decides token overlap and identity-coverage (initials excluded, since a lone initial is not a meaningful overlap signal). An initial-only-matching regression test (§7 test 9) pins that `namesRefer` behavior is unchanged.

### 2.4 The warning — one aggregate per show

The step emits **at most one** warning per show (keeps the data-gap `total` / badge count to +1, one staged-review row):

```ts
result.warnings.push({
  severity: "warn",
  code: "TRAVEL_TRANSPORT_NAME_UNMATCHED",
  message: `Transport names people not on the crew list: ${quotedList} — check the spelling or add them to the crew.`,
});
```

- `quotedList` = the distinct **warned** names (the §2.2 rule-3 fusion ∪ rule-5 garbled set), de-duplicated by normalized form, in first-seen order, each wrapped in straight double-quotes, joined by `, `. **Capped at 5 names**; a 6th-and-beyond count is appended as `, and N more` (N = distinct-count − 5).
- The message is **plain language, never the raw code** (invariant 5). Warnings live in `shows_internal.parse_warnings` and are **admin-only** (spec `docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md:645-649`); the crew page renders zero warnings. Naming the assignee is safe — the operator already sees these names in their own sheet. No crew-facing copy (`crewFacing: null`).
- Emitted only when ≥ 1 warned name exists; zero warned names → no warning pushed (idempotent, silent-clean).

### 2.5 Guard conditions

| Condition | Behavior |
|---|---|
| `result.transportation === null` | return, no warning (nothing seeded) |
| `result.crewMembers.length === 0` | return, no warning (cannot resolve against an empty roster; every name would spuriously "not match") |
| `driver_name === null` | driver contributes no candidate |
| `assigned_names === []` (a leg) | that leg contributes no candidates |
| candidate contains `/` (e.g. `"Doug / Jane"`) | split into sub-names first; each classified independently |
| candidate is a sentinel (`shouldHideGenericOptional`) | skipped |
| candidate shares no significant token with any roster member | skipped (external driver) |
| `coveredCount(C) ≥ 2` (contains ≥2 whole roster names) | **warned** (fusion, rule 3) |
| `coveredCount(C) < 2` **and** some member `namesRefer`-resolves `C` | not warned (single identity, rule 4) |
| near-miss, `coveredCount < 2`, unresolved | **warned** (garbled single name, rule 5) |
| all candidates clean / external / sentinel | no warning pushed |
| any internal throw | swallowed (`try/catch`), no warning — best-effort, never breaks the sync |
| > 5 distinct warned names | message shows first 5 + `, and N more` |

### 2.6 Flag lifecycle — `gateExempt`

`TRAVEL_TRANSPORT_NAME_UNMATCHED` is registered in `GAP_CLASSES` (`lib/parser/dataGaps.ts:30-65`) with **`gateExempt: true`**.

| Column | Value |
|---|---|
| **storage** | `GAP_CLASSES` entry `{ code, label, gateExempt: true }` (`dataGaps.ts`) |
| **write path** | `result.warnings.push(...)` in `enrichTransportAssignees.ts` → persisted to `shows_internal.parse_warnings` on apply |
| **read paths** | `isDataQualityWarning` / `DATA_GAP_CODES` gate (`dataGaps.ts:88-90`) → staged-review card (`app/admin/show/staged/[stagedId]/page.tsx:164-176`, renders `.message`), wizard Step 3, changes feed, per-show panel, `summarizeDataGaps` count |
| **effect of `gateExempt`** | counts toward the data-gap `total` (badge/chip/digest-visible) but **never trips the push-alert regression gate** (`isQualityRegression` / `buildRegressionPayload`) — honored via the `gateExempt` flag, same as `VENUE_GEOCODE_UNRESOLVED` (`dataGaps.ts:56-60`) |

**Why `gateExempt: true`:** the user-selected shape is "staged-review page flags it" — a passive, operator-checked data-quality signal, not a push. Staged/badge visibility comes from `GAP_CLASSES` membership and is independent of `gateExempt`; `gateExempt` governs only push-regression participation. `true` keeps this spec's promise of **no new push/telemetry surface** (email delivery is config-blocked anyway — `lib/notify/config.ts:7`). See §6 Watchpoints for the reviewer preempt on this choice (it is a deliberate scope decision, not an oversight that this class is not transient).

---

## 3. §12.4 code lockstep — `TRAVEL_TRANSPORT_NAME_UNMATCHED`

A new persisted `warn` data-gap code touches every surface below; all land in the same PR (template = `VENUE_GEOCODE_UNRESOLVED`). **Never run prettier on the master spec** (`2026-04-30-fxav-crew-pages-v1.md`) — it mangles the §12.4 table and diverges the x1 gate.

| # | Surface | File | Action |
|---|---|---|---|
| 1 | Master spec §12.4 table row + long-form appendix | `docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md` (table ~`:2861-2909`; long-form ~`:3208`) | hand-add the row (no prettier) |
| 2 | Generated spec-codes | `lib/messages/__generated__/spec-codes.ts` | regen via `pnpm gen:spec-codes` |
| 3 | Runtime catalog | `lib/messages/catalog.ts` | add row `{ code, dougFacing, crewFacing: null, followUp, helpfulContext, title, longExplanation, helpHref }` |
| 4 | Data-gap class registry | `lib/parser/dataGaps.ts` `GAP_CLASSES` (`:30-65`) | add `{ code: "TRAVEL_TRANSPORT_NAME_UNMATCHED", label: "unmatched transport name", gateExempt: true }` |
| 5 | Internal code enums | `lib/messages/__generated__/*` | regen via `pnpm gen:internal-code-enums` (x2 no-raw-codes gate) |
| 6 | Help family taxonomy | **no `app/` edit** | The code's prefix is `TRAVEL` (`codePrefix` = leading token before the first `_`, `_families.ts:130`), which the `crew-schedule` family already lists (`_families.ts:74-88`, title "Crew, schedule & travel data"). `familyFor` therefore maps it to `crew-schedule` with **no change to `app/help/errors/_families.ts`** — deliberately keeping this milestone out of `app/` so invariant 8 (UI dual-gate) does not apply. helpHref `/help/errors#TRAVEL_TRANSPORT_NAME_UNMATCHED`. Pin the mapping in `tests/help/errors-grouping.test.tsx` (a `tests/` file, not `app/`). |
| 7 | Partition drift-guard | `tests/parser/dataGapsClassCompleteness.test.ts` | the new code enters `GAP_CLASSES` → the Layer-1 partition's gap-code count moves (see §5 numeric sweep); update the partition + any hard count |
| 8 | Emit site | `lib/sync/enrichTransportAssignees.ts` + call in `enrichWithDrivePins.ts` | new |

**Gates that must stay green:** x1 catalog-parity (`tests/cross-cutting/codes.test.ts` + `extract-spec-codes.test.ts`), x2 no-raw-codes (`tests/cross-cutting/no-raw-codes.test.ts`), `tests/help/errors-grouping.test.tsx` (every catalog code lands in a named family — empty OTHER fallback; add a pin `familyFor("TRAVEL_TRANSPORT_NAME_UNMATCHED").id === "crew-schedule"`), `tests/parser/dataGapsClassCompleteness.test.ts`, `catalogDocsValidator` (docs↔catalog). Run the **full** suite before push.

### Copy (draft — plain language, invariant 5)

- **dougFacing:** `A transport assignment in _<sheet-name>_ names someone who isn't on the crew list, so that person won't see their transport details. Check the spelling in the transport section, or add them to the crew.`
- **crewFacing:** `null` (admin-only warning).
- **followUp:** `Doug → fix in sheet or add to crew`
- **title:** `Transport names someone not on the crew`
- **helpfulContext / longExplanation:** explain that transport visibility is matched by name, a mismatched or mis-typed name hides that person's transport, and the fix is to correct the name or add the crew member.

---

## 4. Tier × domain matrix

No DB-touching change. Every layer is N/A except the enrich + message-catalog surfaces:

| Layer | Action |
|---|---|
| Table DDL / CHECK / migration | **N/A** — no schema change (warnings ride the existing `shows_internal.parse_warnings` JSONB) |
| RPC read / write | **N/A** — no RPC change |
| Propagation trigger / cleanup | **N/A** |
| Advisory-lock surface | **N/A** — `enrichTransportAssignees` mutates an in-memory `ParseResult`, holds no lock (invariant 2 untouched) |
| Supabase call boundary | **N/A** — pure function, no Supabase client (not a `_metaInfraContract` registry row) |
| admin_alert | **N/A** — no new `AdminAlertCode` |
| Telemetry / push | **N/A** — no `logAdminOutcome`, no new emit surface, no `admin_alerts` |
| Enrich | **new** — `lib/sync/enrichTransportAssignees.ts` + call site |
| Message catalog | **new code** — full §3 lockstep |
| Frontend / UI (invariant 8) | **N/A — and genuinely so** — this milestone touches **zero** files under `app/`. The help family mapping is achieved by naming the code with the already-mapped `TRAVEL` prefix (§3 row 6), NOT by editing `app/help/errors/_families.ts`. The staged-review card (`app/admin/show/staged/[stagedId]/page.tsx`) already renders any `isDataQualityWarning` message and is not edited. No `components/`, `globals.css`, `DESIGN.md`, or `tailwind.config.*` change. Therefore the impeccable critique + audit dual-gate does not apply. |

---

## 5. Numeric sweep (single-sourced constants)

| Value | Meaning | Where |
|---|---|---|
| **5** | max distinct unresolved names named in the message before `, and N more` | `enrichTransportAssignees.ts` (one named const, e.g. `MAX_NAMED = 5`) |
| **2** | minimum significant-token length for the near-miss + coverage checks | `significantTokens` filter, **private to `enrichTransportAssignees.ts`** (does not touch `namesRefer`) |
| **2** | `coveredCount` threshold — `≥ 2` distinct roster identities contained = fusion → warn (rule 3) | §2.2 |
| **+1** | data-gap `total` increment per show with ≥1 warned name (aggregate, not per-name) | §2.4 |
| `GAP_CLASSES` count **30 → 31** | adding `TRAVEL_TRANSPORT_NAME_UNMATCHED` | `dataGaps.ts:30-65`; the partition test comment cites "30" gap codes (`dataGapsClassCompleteness.test.ts:36`) — the plan re-derives the live count and updates the partition + any hard-coded number |

The GAP-class count is **not** hardcoded in this spec's prose beyond this row; the plan verifies the live count at implementation time (the partition test is authoritative).

---

## 6. Watchpoints (disagreement-loop preempts for the reviewer)

- **`gateExempt: true` is deliberate, not an oversight.** The reviewer may argue a non-transient data issue "should" trip the push-regression gate (unlike the transient geocode case). It is intentionally exempt because (a) the user-selected deliverable is staged-review visibility, not a push; (b) this spec's scope explicitly excludes any new push/telemetry surface; (c) staged/badge/digest visibility is preserved via `GAP_CLASSES` membership regardless. The `gateExempt` flag governs only push-regression participation (`dataGaps.ts:56-60`).
- **Match-by-id is intentionally NOT implemented.** §1 shows it is behavior-neutral against the fuzzy match already shipped, and id-persistence is architecturally infeasible at enrich (no id exists). Deferred to 8.3 in `BL-TRANSPORT-ID-RESOLUTION`. Do not relitigate as a gap.
- **The near-miss gate is required, not scope creep.** A plain "not-cleanly-resolved → warn" would fire on every legitimate external/charter driver (§2.2). The gate is what makes the alert trustworthy.
- **Predicate is coverage-based and fully case-enumerated (R2/R3 settled).** The discriminator is `coveredCount` (how many distinct roster identities the candidate fully contains), NOT token count — a fusion contains ≥2 (`Doug Larson John Smith`), a middle/legal name contains 1 (`John Michael Smith`), an abbreviation/nickname 0-1. The §2.2 worked-examples table and §7 tests (1, 2, 2b, 3, 3b, 4–9) are the exhaustive case matrix: external, sentinel, exact, abbreviation, initial, nickname, middle-name, garbled-single-unresolved, two-person-fusion, slash-merge. This matrix IS the structural defense (three predicate rounds R1→R3); a new proposed case must be shown absent from it before it is a finding.
- **Aggregate (one warning/show), not per-name.** Bounds the data-gap count to +1 and keeps a single staged row; the message still names up to 5 specific strings so each is fixable.
- **No `transportTileVisible` change.** The regression pins characterize the *shared* `namesRefer` tolerance via the new enrich classification (red-first, new code) — they do not add green-only tests to unchanged render code (invariant 1).
- **Code named `TRAVEL_TRANSPORT_NAME_UNMATCHED` deliberately** so its `TRAVEL` prefix maps to the existing `crew-schedule` help family with **no `app/help/errors/_families.ts` edit** — keeping the milestone entirely out of `app/` and therefore out of the invariant-8 UI dual-gate. Mirrors the sibling `TRAVEL_FLIGHT_NAME_UNMATCHED`. Do not relitigate as a "should be TRANSPORT_*" naming nit — the prefix choice is load-bearing.
- **`lib/data/nameMatch.ts` is untouched.** The near-miss check uses a private `significantTokens` helper in the enrich module; `namesRefer`/`toks`/`refersSingle` keep one-letter tokens for initial matching. An initial-only regression test (§7 test 9) pins no behavior change. Do not relitigate as a shared-matcher refactor.

---

## 7. Testing / acceptance

TDD per task. Concrete failure mode each test catches (anti-tautology):

1. **Known-gap fixture (headline):** transportation `driver_name: "Doug Larson Loadout"`, roster `["Doug Larson"]` → exactly one `TRAVEL_TRANSPORT_NAME_UNMATCHED` warning whose `.message` contains `"Doug Larson Loadout"`. Catches: the silent-driver regression re-appearing.
2. **Resolved tolerance (no false warning):** driver `"Doug"` / `"Douglas Larson"` / `"  doug larson "`, and assigned `["Bill Werner"]` vs roster `["William Werner"]` → **no** warning. Catches: over-warning on names `transportTileVisible` already shows (the BL tolerance matrix, red-first via the enrich classifier). Nickname `"Bill Werner"` vs `"William Werner"` (`coveredCount = 0`, `bill`≠`william`) pins the rule-4 clean side.
2b. **Middle / legal name (R3 finding):** driver `"John Michael Smith"`, roster `["John Smith"]` → **no** warning. Catches: the token-count false-positive — a longer legitimate variant (`coveredCount = 1`, resolves) must be clean, not a fusion. Contrast directly with test 3b (same 3-vs-2 shape but 2 roster identities).
3. **External driver (near-miss gate):** driver `"ABC Charters"`, roster `["Doug Larson"]` → **no** warning. Catches: chronic false-positive noise on legit external drivers.
3b. **Fusion / wrong-recipient (R2 finding):** driver `"Doug Larson John Smith"`, roster `["Doug Larson", "John Smith"]` → **exactly one** warning whose `.message` contains `"Doug Larson John Smith"`. Catches: the fusion that surname-`namesRefer`-resolves to `John Smith` (verified `true`) yet hides Doug's transport — `coveredCount = 2` (rule 3) must warn despite "resolving." This is the specific hole a "resolved-to-anyone → safe" gate would miss, and the counterpart to test 2b (which must NOT warn).
4. **Sentinel skip:** driver `""` / `"TBD"` / `"—"` → no warning. Catches: warning on non-assignments.
5. **Empty-roster / null-transport guards:** roster `[]` or `transportation: null` → no warning. Catches: spurious warnings when there is nothing to resolve against.
6. **Aggregate + cap:** 6 distinct warned names → one warning, message names 5 + `, and 1 more`. Catches: count inflation and unbounded message growth.
7. **assigned_names path:** a warned name only in `schedule[*].assigned_names` (driver clean) → warned. Catches: missing the leg-assignee code path.
7b. **Slash-merge split:** driver `"Doug / Jane"`, roster `["Doug Larson", "Jane Doe"]` → **no** warning (each sub-name cleanly resolves). Catches: a legit two-person slash assignment false-warning as an overflow because the un-split string carries 2 people's tokens.
8. **Lockstep gates:** x1, x2, `errors-grouping` (family pin), `dataGapsClassCompleteness` (partition), `catalogDocsValidator` all green with the new code.
9. **Initial-only matching unchanged (nameMatch untouched, non-tautological):** assert `namesRefer("J", "John Larson") === true` — a case that depends **solely** on the one-letter token `J` matching `John` by prefix (verified live; single-token branch `nameMatch.ts:47`), so it fails if a refactor drops one-letter tokens (unlike `"J Larson"`, which would still pass via the shared `Larson` surname path). Pair with an enrich test: assigned name `"J"`, roster `["John Larson"]` → **no** warning (`J` resolves; `significantTokens("J")` is empty → shares no significant token anyway → skip). Catches: a regression to `namesRefer`'s one-letter behavior AND proves `significantTokens` is genuinely private (dropping `J`) while `nameMatch.ts` is untouched.

Test files: new `tests/sync/enrichTransportAssignees.test.ts` (unit, the classification matrix + guards + cap); extend `tests/help/errors-grouping.test.tsx` (family pin); extend `tests/parser/dataGapsClassCompleteness.test.ts` (partition); `tests/cross-cutting/codes.test.ts` picks up the catalog row automatically.

## 8. Meta-test inventory

- **EXTENDS** `tests/parser/dataGapsClassCompleteness.test.ts` (partition gains the new gap code).
- **EXTENDS** `tests/help/errors-grouping.test.tsx` (family-coverage pin for the new code).
- **Does NOT touch** `_metaInfraContract` (no Supabase call), `advisoryLockRpcDeadlock` (no lock), `_auditableMutations` / mutation-surface observability (not an HTTP route or `"use server"` action), `no-inline-email-normalization` (no email handling), `_metaAdminAlertCatalog` (no admin_alert). Declared explicitly per the writing-plans meta-test-inventory rule.
