# Bug audit — merged PRs #38–#217 (2026-07-02)

> **Status: findings only — nothing in this document has been fixed.** Each finding lists the file:line on `main` @ `147bae46` where the defect lives.

## Context & method

Fable access lapsed around PR #37; PRs #38–#217 were implemented by Opus 4.8. This audit swept that range for bugs and structurally bad implementation **still present on current main** (`147bae46`, 2026-07-02).

- **Scope:** 66 load-bearing PRs reviewed individually (the 3 largest — #70, #103, #181 — dual-lens: logic + concurrency/state). 97 low-value PRs (help content, CI plumbing, lint/prettier, screenshots, UI polish, docs/tests-only) were skipped per-PR but their machinery was covered by 8 cross-PR subsystem sweeps: typo-tolerance, advisory-locks, onboarding state machine, agenda pipeline, observability, viewer matching, cache invalidation, source-anchors chain.
- **Verification:** every P1-severity claim was adversarially verified by an independent agent instructed to refute it (reachability, existing guards, documented intent). P2/P3 claims are **reported as-is, unverified** — expect some to be refuted on closer inspection (the one P1 that was verified-refuted is documented in §3 as a template for what refutation looks like).
- Findings already documented in DEFERRED.md / BACKLOG.md, or fixed by later PRs in the range, were excluded.

**Severity:** P0 = data loss / security / crash / deadlock · P1 = user-visible functional bug · P2 = edge-case bug · P3 = structural bad implementation. No P0-severity findings were confirmed.

**Totals:** 12 confirmed (8 P1 / 4 P2) · 1 refuted · 107 unverified (66 P2 / 41 P3).

---

## 1. Confirmed findings (adversarially verified)

### C1. [P1] #48 — `components/crew/sections/VenueSection.tsx:169`

**The Venue 'Facilities' loading-dock row hardcodes the fabricated sub-caption "Service entrance" (mock fixture copy) under every real loading-dock value, regardless of what the data says.**

*Failure scenario:* Any show whose sheet fills the loading-dock field with something that is not a service entrance (e.g. "No dock — load through main lobby freight elevator") renders "Loading dock / No dock — load through main lobby freight elevator" with a data-styled secondary line reading "Service entrance" beneath it, presenting false way-finding information to crew. The PR's own plan (plan.md Step 5) required a sub only "where the data provides one", and the spec bans invented content; no data path ever supplies this string — it is unconditional for every venue with a dock value.

*Suggested fix:* Delete the `sub: "Service entrance"` line from the loading-dock factRows.push in VenueSection.tsx (no data source supplies a dock sub-caption), and update the two pinning tests (tests/components/crew/sections/VenueSection.test.tsx:117 to assert the string is ABSENT; tests/components/crew/factRows.test.tsx may keep a generic sub fixture since FactRows itself is data-agnostic).

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): components/crew/sections/VenueSection.tsx:165-172 unconditionally pushes `sub: "Service entrance"` whenever `venue.loadingDock` is non-sentinel, and FactRows.tsx:80-87 renders any defined `sub` with no gate — so the string appears under every real dock value (reachable today: real fixtures carry docks like "Viramar Street Dock has scissor lift..." in fixtures/shows/raw/2024-05-east-coast-family-office.md:11, parsed via lib/parser/blocks/venue.ts:174-175/313; no data path ever supplies "Service entrance"). It is not documented-intentional: the PR's own plan (docs/superpowers/plans/2026-06-19-crew-mock-fidelity/plan.md:496, Task 6 Step 5) requires a sub only "where the data provides one", there is no DEFERRED/BACKLOG entry, and the only other occurrences are tests pinning the implementation (tests/components/crew/sections/VenueSection.test.tsx:117 asserts it against fixture value "Dock at rear").

</details>

### C2. [P1] #180 — `lib/onboarding/rescanWizardSheet.ts:339`

**Clean re-scan of a CHECKED sheet force-resets its manifest to 'staged' while re-stamping wizard_approved=true, so the Step-3 checkbox and publish counts show it unchecked/Held but finalize still publishes it Live.**

*Failure scenario:* Doug checks a sheet in Step 3 (approve sets manifest 'applied' + wizard_approved=true), then edits a typo in Drive and clicks 'Re-scan this sheet'. The heal step (line 337-342) sets manifest status='staged' and the clean branch (line 384-391) re-stamps wizard_approved=true; the UI derives checked-state and publishCount solely from manifest status (Step3SheetCard initialChecked={row.status==="applied"}, OnboardingWizard.tsx:398), so the box renders UNCHECKED and 'Publish N shows' excludes it — yet finalize keys publish_intent off wizard_approved (finalize/route.ts:364,383) and publishes the show Live. If Doug leaves it 'unchecked' expecting a Held draft, crew pages go Live silently and one more show publishes than the confirm label stated. The db test T-A1 masks this by seeding the manifest as 'staged' instead of 'applied' for its previously-approved fixture.

*Suggested fix:* In the CLEAN + priorReady branch of rescanWizardSheet.ts, restore the manifest alongside the approval re-stamp (update onboarding_scan_manifest set status='applied', transitioned_at=now() for the row) so the Step-3 checkbox and publish counts match the kept approval. Fix T-A1 to seed the previously-approved fixture's manifest as 'applied' and assert it remains 'applied' after a clean rescan.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): rescanWizardSheet.ts:337-342 force-heals the manifest to 'staged' while the clean branch (:384-391) re-stamps wizard_approved=true, but the real approve route had set manifest 'applied' (approve/route.ts:179-180) and the Step-3 UI derives checked-state and publish counts solely from manifest status (Step3SheetCard.tsx:1565, OnboardingWizard.tsx:398, Step3Review.tsx:745-746 — wizard_approved is never even queried). Finalize selects the row anyway (manifest 'staged' allowed, finalize/route.ts:382-383) and stamps publish_intent from wizard_approved (:1014-1020), which the CAS flips Live (finalize-cas/route.ts:505,531) — so a sheet rendering unchecked/Held publishes Live and the confirm count understates by one. The rescan button is mounted on checked cards (Step3SheetCard.tsx:1699-1701), the spec expects the card to stay approved-checked (spec §6.2 "stays publish-ready" + transition row :218), and T-A1 masks the divergence by seeding manifest 'staged' instead of 'applied' (rescanWizardSheet.db.test.ts:349,378).

</details>

### C3. [P1] #180 — `lib/onboarding/rescanWizardSheet.ts:293`

**The hard_failed re-scan branch leaves the stale APPROVED pending_syncs row (old parse + reviewer choices keyed to old item ids) untouched, creating a state where the pre-existing Retry path re-stages new item ids under the retained approval and finalize then throws a batch-wedging internal error.**

*Failure scenario:* Doug approves sheet S, then breaks it in Drive (e.g. deletes all crew rows -> MI-4 hard fail) and clicks Re-scan: phase1 writes pending_ingestions + manifest 'hard_failed' but never touches the approved pending_syncs row (phase1.ts:279-299), and this branch only deletes the shadow. Doug fixes the sheet and clicks the Step-3 Retry affordance: retrySingleFile's upsert (runOnboardingScan.ts:421-434) replaces triggered_review_items with fresh randomUUID sentinel ids while retaining wizard_approved=true and the OLD wizard_reviewer_choices. At Publish, validateReviewerChoices returns EXTRA_REVIEWER_CHOICE -> core.outcome 'invalid_request' -> finalize throws (finalize/route.ts:1001-1009), a route-level ONBOARDING_FINALIZE_INTERNAL_ERROR that wedges the whole batch with no per-row recovery; structurally, the approval also survives an arbitrary content change (including crew-email edits) without ever passing the PR's own clean/dirty rule. No pre-PR-180 sequence could put an approved row into the retryable hard_failed state.

*Suggested fix:* In rescanWizardSheet's hard_failed branch (lib/onboarding/rescanWizardSheet.ts:293-310), also demote the retained Flow-A approval before returning: run the same pending_syncs UPDATE as the isDirty branch (wizard_approved=false, null approval payload/choices, last_finalize_failure_code=RESCAN_REVIEW_REQUIRED) scoped to (wizard_session_id, drive_file_id). For structural closure of the class, additionally have upsertLivePendingSync's ON CONFLICT clause (runOnboardingScan.ts:421-434) clear wizard_approved/wizard_reviewer_choices whenever it replaces triggered_review_items.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): rescanWizardSheet.ts:293-310 returns from the hard_failed branch without touching the approved pending_syncs row (phase1.ts:279-299 writes only pending_ingestions), and the Step-3 Retry path (Step3Review.tsx:213-218 → retrySingleFile → runOnboardingScan.ts:421-434 upsert) replaces triggered_review_items with fresh randomUUID sentinels while retaining wizard_approved=true and the old wizard_reviewer_choices. At Publish the approved branch feeds the stored choices to validateReviewerChoices (finalize/route.ts:955-956 → applyStagedCore.ts:139-141), which deterministically returns EXTRA_REVIEWER_CHOICE → invalid_request → the throw at finalize/route.ts:1001-1009, uncaught by the per-row catch (route.ts:1174, FirstSeenProvenanceRaceError only) → batch-wide 500 ONBOARDING_FINALIZE_INTERNAL_ERROR (route.ts:1264) that recurs on every Publish; the WM-R6 structural guard (route.ts:889-900) checks only shape, not id membership, and pre-PR-180 the approved+retryable-hard_failed state was unreachable (scan/route.ts:186-195 wipes rows on a new session).

</details>

### C4. [P1] #217 — `lib/drive/unknownFieldAnchors.ts:46`

**The details-block anchor scan's TERMINATORS set includes "DIAGRAMS" (and "DRESS"), which are known EVENT DETAILS field labels per the parser (lib/parser/blocks/event.ts:56 explicitly documents diagrams as a field, not a terminator), so the scan aborts mid-block and produces no anchors for rows below those labels.**

*Failure scenario:* Standard v4 template sheets have 'DIagrams' as the FIRST row of the EVENT DETAILS block (fixtures/shows/raw/2026-04-asset-mgmt-cfo-coo-waldorf.md:36). extractUnknownFieldAnchors finds the header, then hits 'DIAGRAMS' in TERMINATORS on the very next row and breaks, yielding zero details anchors. Any UNKNOWN_FIELD warning from that block resolves to null, and since the PR removed the region fallback in attachSourceCellAnchors, the operator gets NO 'Open in Sheet' link at all — a regression from the pre-PR block-header link and a silent no-op of the PR's headline feature on the dominant template (the live test passed only because the repro show's block starts with 'Floor Plan'/'Room Diagram').

*Suggested fix:* Make the terminator set per-kind: for kind "details", exclude "DIAGRAMS" and "DRESS" (known field labels per event.ts CANONICAL_KEY_MAP) while keeping them for "venue" — over-inclusion past a real DIAGRAMS section is safe by the module's own exactly-one-match design. Add a regression test with a DIagrams-first EVENT DETAILS block mirroring the fintech/rpas fixtures asserting anchors resolve for rows below it.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): TERMINATORS at lib/drive/unknownFieldAnchors.ts:46,50 includes DIAGRAMS/DRESS, which event.ts:56 and CANONICAL_KEY_MAP (event.ts:73,99) document as details-block FIELD labels, and 3 of 7 production exporter-xlsx fixtures (fintech.md:92, fixed-income.md:79, rpas.md:96 — the v4 "EVENT DETAILS" variant) have DIagrams as the block's first row, so the scan breaks at headerRow+1 (line 134) yielding zero details anchors. attachSourceCellAnchors (lib/drive/showDayTimeAnchors.ts:136-144) dispatches UNKNOWN_FIELD with an explicit "no region fallback", so every details-block warning loses its pre-PR block-header link entirely; both unit and live tests only exercise Floor Plan/Room Diagram-first layouts, so the suite structurally misses it and no DEFERRED entry documents it.

</details>

### C5. [P1] #70 — `lib/crew/resolveKeyTimes.ts:141`

**The anchor candidate loop re-promotes a terminal-titled entry's clock as the day's Show anchor, defeating the tokenizer's terminal-event guard: a day whose only entry is a wrap/conclusion time displays that END time as the show's key time.**

*Failure scenario:* Live fixed-income shape (fixtures/shows/exporter-xlsx/fixed-income.md:23): SHOW DAY 2 TIME cell '4:15pm - Meeting Concludes' parses to entries=[{start:'4:15PM', title:'Meeting Concludes'}] with showStart:null (TERMINAL_RE guard, scheduleTimes.ts:195). resolveKeyTimes candidate row 3 (entries.find(...)?.start — only kind:'strike'/'loadout' synthetics are skipped, parsed terminal entries are not) then picks 4:15PM anyway, so KeyTimesStrip/TodaySection/RightNowHero show 'Day 2 · Tue 10/21 — 4:15PM' as the Show time when 4:15pm is when the meeting ENDS. The design doc's own terminal-guard section (docs/superpowers/specs/2026-06-22-per-day-schedule-keytimes-design.md:107,111, closing adversarial R5 finding 9) says this day's anchor must fall to the date-safe room fallback (here: omit), not the terminal clock.

*Suggested fix:* In the resolveKeyTimes row-3 candidate, also skip entries whose title matches the terminal-event pattern — export TERMINAL_RE from lib/parser/blocks/scheduleTimes.ts and change the find predicate to `e.kind !== "strike" && e.kind !== "loadout" && !TERMINAL_RE.test(e.title)` — so terminal-only days fall through to the date-safe room fallback/omit (rows 4-6). Reconcile spec §5.1 decision-table row 3 with §4.1 step 6 and add a resolve-level test for the "4:15pm - Meeting Concludes" day.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): resolveKeyTimes.ts:141 promotes the first non-synthetic entry's start, and parsed terminal entries carry no kind (types.ts:355), so the live fixed-income SHOW DAY 2 cell "4:15pm - Meeting Concludes" (fixtures/shows/exporter-xlsx/fixed-income.md, DATES block; parser behavior pinned at tests/parser/blocks/scheduleTimes.test.ts:59-64 with showStart:null per scheduleTimes.ts:194-195) yields ShowAnchor time "4:15PM" for Day 2. The design doc's terminal-guard section (2026-06-22-per-day-schedule-keytimes-design.md:107,111) explicitly requires this day's anchor to fall to the date-safe room show_time fallback, not the terminal clock, so the behavior defeats a ratified guard rather than implementing a documented choice; no test, DEFERRED.md, or BACKLOG.md entry covers or defers the terminal case at resolve level.

</details>

### C6. [P1] sweep — `lib/onboarding/rescanWizardSheet.ts:338`

**[onboarding-state] Clean per-sheet re-scan of a checked row re-stamps pending_syncs.wizard_approved=true but leaves onboarding_scan_manifest at 'staged', desyncing the checkbox UI, the finalize checked/unchecked branch, and the Phase-D publish flip which all key off opposite halves of the broken manifest⟺approval invariant.**

*Failure scenario:* Operator checks a Step-3 card (approve route sets manifest 'applied' + wizard_approved=true), then clicks the card's always-present 'Re-scan this sheet' (Step3SheetCard.tsx:1700) on an unchanged sheet: rescanWizardSheet resets the manifest to 'staged' (line 338) then its §6.2 clean+priorReady branch re-approves only pending_syncs (line 384), while the button reports 'Updated. Still ready to publish.' After router.refresh() the checkbox renders UNCHECKED (Step3Review isChecked = status==='applied'), the finalize label/soft-confirm count it as 'won't be published' — yet finalize's selectFinishableCleanRows/processApprovedRow re-read wizard_approved=true and run the CHECKED branch: for a re-onboarded existing show the staged edits are shadow-staged and applied to the live show at Phase D despite the UI's unchecked/no-op claim, and for a first-seen row publish_intent=true is stamped but publishAppliedWizardShows requires m.status='applied' (finalize-cas/route.ts:524) so the show is silently stranded Held with publish_intent=true. Every other approval writer pairs the two writes (approve route markManifestApplied; applyStaged.ts defaultMarkWizardManifestApplied) — this branch is the sole outlier.

*Suggested fix:* In rescanWizardSheet's §6.2 clean+priorReady branch, restore the manifest⟺approval pairing by executing `update public.onboarding_scan_manifest set status = 'applied', transitioned_at = now() where wizard_session_id = $1::uuid and drive_file_id = $2` in the same locked tx as the wizard_approved=true re-stamp (mirroring the approve route's markManifestApplied), leaving the line-338 'staged' reset in place for the dirty and not-previously-ready branches. Update tests/onboarding/rescanWizardSheet.db.test.ts T-A1 (line 378) to expect manifestStatus()==='applied', seeding the manifest at 'applied' to model the real approve-route state.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): rescanWizardSheet.ts:337-342 unconditionally resets the manifest to 'staged', then the clean+priorReady branch (lines 377-392) re-stamps only pending_syncs.wizard_approved=true, while the UI checkbox keys off manifest status (Step3Review.tsx:745-746, sourced from onboarding_scan_manifest in OnboardingWizard.tsx:203-318) and finalize keys off wizard_approved (finalize/route.ts:382-383, 902-914) — so a checked→rescan-clean row renders unchecked, is not reconciled by any flush (Step3Review.tsx:805-825 only POSTs on overlay divergence), yet runs the CHECKED branch (existing-show shadow applied at Phase D; first-seen show created with publish_intent=true but skipped by publishAppliedWizardShows' m.status='applied' predicate at finalize-cas/route.ts:500/524, stranding it Held after finalize_complete since unresolvedManifestCount at finalize-cas/route.ts:297-300 ignores a 'staged' row whose pending row was consumed). The scenario is reachable from the always-rendered RescanSheetButton on non-dirty cards (Step3SheetCard.tsx:1699-1701), and this branch is the sole approval writer that doesn't pair the manifest write — approve/route.ts:246-257 (markManifestApplied) and applyStaged.ts:569/1154 (defaultMarkWizardManifestApplied) both do; the T-A1 test pins manifest='staged' but seeds the manifest at 'staged' via direct SQL rather than the approve route, so it doesn't document the desync as intentional.

</details>

### C7. [P1] sweep — `lib/sync/enrichWithDrivePins.ts:327`

**[agenda-pipeline] Cron re-sync silently erases a published show's agenda schedule on any transient Drive fault because the prior extraction is never seeded into the fresh parse, making enrichAgenda's 'unknown → leave-existing' protection vacuous.**

*Failure scenario:* A published show has a high-confidence extraction in shows.agenda_links[i].extracted (crew page renders the schedule). Doug edits any cell in the sheet; cron re-parses, and enrichAgenda's getFile for the agenda PDF hits a Drive 5xx/429/403-throttle → verdict 'unknown' with NO warning (enrichAgenda.ts:180-186) and link.extracted stays absent (the fresh parse has no prior payload to preserve, unlike the admin path whose parse_result comes from pending_syncs). The sync succeeds, phase-2 wholesale-overwrites shows.agenda_links (applyStaged.ts:1128) and advances last_seen_modified_time, so perFileProcessor's watermark gate skips the show on every later tick — the crew page drops to embed-only with no admin signal until the sheet is edited again.

*Suggested fix:* In the cron/push/manual prepare stage (runScheduledCronSync.ts, right before enrichWithDrivePins at :2423), read the existing shows.agenda_links row and copy each stored link's `extracted` onto the fresh parsed.show.agenda_links entry matched by fileId (fallback: label), so enrichAgenda's leave-existing and revision-cache paths become effective. Do NOT merge at applyShowSnapshot instead — that would mask intentional known_stale clears.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): The cron pipeline never seeds prior extractions into the fresh parse (parseAgendaLinks emits no `extracted`, lib/parser/index.ts:286-309; priorParseResult at runScheduledCronSync.ts:611 feeds only runInvariants), so enrichAgenda's "unknown → leave-existing" guard (enrichAgenda.ts:183-201) preserves nothing while phase2's applyShowSnapshot wholesale-writes agenda_links from the fresh parse (runScheduledCronSync.ts:1128/1154 — the claim's applyStaged.ts:1128 citation is the wrong file, but the code exists) and advances the watermark so perFileProcessor.ts:214 skips the show until the next sheet edit. This contradicts the ratified spec intent "any pre-existing extracted is preserved, never cleared" (docs/superpowers/specs/2026-06-26-agenda-pdf-schedule-design.md:240) and merge-spec :747, and the crew page reads shows.agenda_links[i].extracted (lib/crew/agendaDayForToday.ts:54), so the published schedule silently drops to embed-only with only a plain-log forensic code (AGENDA_GETFILE_FAULT, not an admin alert per tests/log/_metaAdminOutcomeContract.test.ts:82).

</details>

### C8. [P1] sweep — `app/api/admin/onboarding/finalize-cas/route.ts:888`

**[cache-invalidation] revalidateShow calls inside the streaming finalize/finalize-cas ReadableStream bodies are silent no-ops — Next flushes pendingRevalidatedTags when the route handler resolves, which happens before the streamed batch commits — so the wizard's (always-streaming) publish path never busts show-<id> and existing published crew pages serve stale cached data.**

*Failure scenario:* Admin re-onboards a sheet backed by an existing published show (crew name/role identity change staged as a shadow row) and clicks Publish in wizard Step 3; FinalizeButton.tsx:341-343 sends Accept: application/x-ndjson, so POST /api/admin/onboarding/finalize-cas takes handleOnboardingFinalizeCasStream, which returns new Response(stream) immediately — Next's app-route module (dist/server/route-modules/app-route/module.js:494) then executes the still-empty pendingRevalidatedTags queue, and the revalidateShow(showId) calls at finalize-cas/route.ts:888 (and finalize/route.ts:1239 via the stream at :1305) only enqueue tags AFTER that flush, with no later flush point existing for route handlers. The live show's crew/hotel/rooms/transport data was durably mutated (applyShadow, route.ts:370/462) but show-<id> is never revalidated; crew members' pages keep serving the old unstable_cache fan-out, and the ShowRealtimeBridge cannot rescue it because the stale render carries a fresh live-read viewerVersionToken (token match → no router.refresh — the stuck-stale mode showCacheTag.ts:22-27 warns about), so the identity change is invisible to crew for the full 300s TTL + one stale-while-revalidate request. The non-streaming handlers (route.ts:828, finalize/route.ts:1239 via handleOnboardingFinalize) flush correctly, but no production caller uses them — FinalizeButton always streams; only RunFinalCASButton.tsx:67 still hits the non-streaming path.

*Suggested fix:* In both streaming handlers, replace the in-stream revalidateShow loops with `after(() => { for (const id of affectedShowIds) revalidateShow(id); })` from next/server, registered post-commit inside the stream — after() callbacks run in withExecuteRevalidates (next/dist/server/after/after-context.js:107), which diffs and executes tags enqueued inside the callback after the response closes. Extend the showCacheRevalidateCoverage meta-test (or a sibling structural test) to forbid bare revalidateShow calls inside ReadableStream start() bodies so the class is pinned.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P1): Next 16.2.4's app-route module flushes pending tag revalidations exactly once at handler resolution (module.js:494 → executeRevalidates, which returns false on an empty queue with no later flush), so the revalidateShow calls inside the ReadableStream bodies at finalize-cas/route.ts:888 and finalize/route.ts:1238-1240 (via the stream at :1305) enqueue tags after the flush and are never executed. The wizard publish path always streams (FinalizeButton.tsx:298, :341-343 send Accept: application/x-ndjson; dispatch at finalize-cas/route.ts:925-930), and applyShadow durably mutates existing published shows whose crew pages are served from unstable_cache tagged show-<id> with a 300s TTL (getShowForViewer.ts:783-791), so the stale window is real and unrescued — ShowRealtimeBridge's router.refresh re-reads the same valid cache entry. Comments at finalize-cas/route.ts:822-828/:885-888 and the call-site-presence meta-test (tests/db/showCacheRevalidateCoverage.test.ts:96-101) show this is believed to work, not documented-intentional.

</details>

### C9. [P2] #57 — `.github/workflows/dev-gate-e2e.yml:69`

**The dev-gate-e2e workflow omits HASH_FOR_LOG_PEPPER from the runner env, so every run fails deterministically at the first webServer build — the PR's B1-D4 CI gate has never passed and is still broken on main.**

*Failure scenario:* gh workflow run dev-gate-e2e.yml → Playwright boots the :3001 webServer → `pnpm build` collects page data for /api/auth/picker-bootstrap → lib/email/hashForLog.ts throws 'HASH_FOR_LOG_PEPPER env var must be set' at module evaluation (bare CI runners have no .env.local; the :3001-:3003 webServer commands carry no env fallback, unlike :3004) → 'Process from config.webServer was not able to start. Exit code: 1'. Verified in real CI: run 27928396750 on main failed exactly here; all 4 runs ever fired are failures and none has run since 2026-06-22, so the build-vs-runtime /admin/dev security gate this workflow exists to verify has zero working CI coverage. help-affordances.yml:78 documents this exact requirement for the :3000-:3003 builds, but the 'mirrored' dev-gate workflow dropped it.

*Suggested fix:* Add HASH_FOR_LOG_PEPPER: "fxav-r41-test-pepper-32-chars-min-deterministic" to the "Run dev-gate projects" step env in dev-gate-e2e.yml (mirroring help-affordances.yml:78 / closed PR #62), AND per PR #62's dispatch verification also supply SUPABASE_URL + NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY to the :3001-:3003 webServer envs in playwright.config.ts — the pepper alone still leaves the workflow red. Then re-dispatch to green and correct the B1-D4 DEFERRED.md entry.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P2): dev-gate-e2e.yml:69-85 sets no HASH_FOR_LOG_PEPPER and the :3001-:3003 webServer commands (playwright.config.ts:248-307) carry no fallback (only :3004 does, lines 333-334), while lib/email/hashForLog.ts:6-14 throws at module eval and is imported by app/api/auth/picker-bootstrap/route.ts, which `next build` evaluates; real CI run 27928396750 on main failed with exactly that error and all 4 runs ever are failures. The defect is unfixed on main — fix PR #62 was closed without merge and no in-tree deferral exists (DEFERRED.md B1-D4 instead falsely says RESOLVED with real-CI-green checked off). Severity lowered to P2 because the workflow is workflow_dispatch-only, gates no PR merges, and the build-vs-runtime contract retains structural unit-test coverage (build-artifact-gate.test.ts et al.).

</details>

### C10. [P2] #90 — `components/crew/CrewSections.tsx:51`

**The syncedInitial prop-sync only fires when initialSection changes value, but syncedInitial is never updated on shallow tab taps or popstate, so a server-driven navigation back to the last-synced section (e.g. the Today section's SectionChipLink to ?s=schedule) is silently ignored, leaving the URL and the rendered section desynced.**

*Failure scenario:* Crew member opens the show page (Today, syncedInitial="today"), taps the "Run of show → Agenda" chip (Next Link nav to ?s=schedule; sync sets active=syncedInitial="schedule"), taps the Today tab (shallow pushState ?s=today; active="today", syncedInitial stays "schedule"), then taps the chip again: the full server nav re-renders CrewShell with initialSection="schedule", but since it equals syncedInitial the adjust at line 51 never fires — the body and nav stay on Today while the URL shows ?s=schedule. The chip appears dead, and reloading/sharing the URL opens Schedule, unlike what is displayed. The same root cause can stomp a user's tab selection when a router.refresh() that snapshotted a pre-tap ?s= URL resolves after a shallow tap (initialSection arrives differing from syncedInitial and force-resets active to the stale URL's section).

*Suggested fix:* Keep syncedInitial mirroring the current URL-derived section: call setSyncedInitial(id) alongside setActive in onSelect and in the popstate handler, so any server nav delivering a section different from the client's current one re-fires the adjust at line 51.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P2): CrewSections.tsx:51-54 only adjusts when initialSection differs from syncedInitial, but onSelect (:56-67) and the popstate handler (:69-76) update only `active`, never `syncedInitial`, so after chip-nav→shallow-Today-tap→chip-again the server nav delivers initialSection="schedule"===syncedInitial and active stays "today" while the URL shows ?s=schedule. The chip is a real next/link server nav (SectionChipLink.tsx:47-49, rendered at TodaySection.tsx:623) and initialSection is URL-derived (_CrewShell.tsx:193,412) with no remount key, so the desync is reachable; no test or DEFERRED entry covers it (crewSections.test.tsx:190 only tests a changed initialSection). Downgraded to P2: the chip silently no-ops and the URL desyncs, but tapping the tab recovers instantly and no data is lost.

</details>

### C11. [P2] #210 — `components/admin/FinalizeButton.tsx:304`

**A mid-stream network failure rejects reader.read() inside readFinalizeBatch/readFinalizeCas, and the rejection escapes runLoop (only the fetch() calls are try/caught) as an unhandled promise rejection, leaving the UI permanently stuck on the 'running' progress panel with no error state and no retry affordance.**

*Failure scenario:* Operator clicks 'Publish N shows & finish setup'; 20s into the 30-60s streamed batch the connection drops (Wi-Fi blip, laptop sleep, proxy idle-timeout, or the 300s maxDuration kill severing the response). reader.read() at FinalizeButton.tsx:203 rejects with a TypeError, which propagates out of the bare 'await readFinalizeBatch(response)' at :304 (and :349 for CAS) through 'void runLoop()' — state stays kind:'running' forever: frozen bar, button unmounted, no error copy, operator cannot tell whether their shows published and has no recovery short of a full page reload. The 'interrupted' sentinel only covers clean EOF without a terminal event, and both test harnesses (mockNdjsonResponse/controllableNdjson) only close streams cleanly, so this path is untested; Step2Verify, the pattern this PR claims to mirror, wraps its entire reader loop in try/catch (Step2Verify.tsx:226-271).

*Suggested fix:* Wrap the `await readFinalizeBatch(response)` (:304) and `await readFinalizeCas(casResponse)` (:349) calls in try/catch mapping rejection to the existing `setState({ kind: "error", copy: GENERIC_ERROR, code: null }); return;` used by the interrupted sentinel (mirroring Step2Verify.tsx:226-271). This also covers the unguarded non-stream `response.json()` paths at :167/:237.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P2): FinalizeButton.tsx:202-216/:258-272 await reader.read() with no try/catch, and runLoop guards only the fetch() calls (:295-303, :340-348) — the bare awaits at :304/:349 let a mid-body rejection escape `void runLoop()` (:403), leaving state stuck at kind:'running' (:291) with the button unmounted; the `interrupted` sentinel (:220-224) covers only clean done:true EOF, GlobalErrorListener.tsx:39-53 merely logs telemetry, and the mirrored pattern Step2Verify.tsx:226-271 wraps its whole reader loop in try/catch, confirming this is an omission. Downgraded to P2: transient trigger, admin-only surface, and a page reload recovers (server idempotency-gated, wizard resumable).

</details>

### C12. [P2] sweep — `instrumentation-client.ts:3`

**[observability] Sentry captures crew-page URLs containing the share token (the crew auth credential) and raw error messages with zero scrubbing — none of the three Sentry.init configs (instrumentation-client.ts, sentry.server.config.ts, sentry.edge.config.ts) sets beforeSend, so the email-redaction/hash discipline enforced on the app_events path (lib/log/sanitize.ts, hashForLog) is bypassed entirely on the third-party path.**

*Failure scenario:* With NEXT_PUBLIC_SENTRY_DSN set, a crew member opens /show/east-coast/<shareToken> and any client render error fires app/show/[slug]/[shareToken]/error.tsx:14 -> captureBoundaryError -> Sentry.captureException; the browser SDK attaches request.url = location.href, so the live share token (rotatable credential granting crew-page access, M11.5 pivot) is stored on sentry.io and propagated through Sentry notification emails/integrations. Server-side, instrumentation.ts:8 onRequestError = Sentry.captureRequestError does the same for crew-route server errors, and error.message strings containing crew emails reach Sentry unredacted even though the same message would be email-redacted before entering app_events.

*Suggested fix:* Add a shared scrub helper (e.g., lib/observe/scrubSentryEvent.ts) registered as beforeSend in all three Sentry.init configs that (a) replaces the shareToken path segment in event.request.url, transaction names, and breadcrumb URLs with a placeholder, and (b) runs redactEmails from lib/log/sanitize.ts over event.message and exception.values[].value. Pin it with a structural test asserting each of the three config files passes beforeSend.

<details><summary>Verifier evidence</summary>

- CONFIRMED(P2): All three Sentry.init configs (instrumentation-client.ts:3-8, sentry.server.config.ts:4-9, sentry.edge.config.ts:4-9) lack beforeSend, and the live paths app/show/[slug]/[shareToken]/error.tsx:14 → lib/observe/captureBoundaryError.ts:13 (Sentry.captureException) and instrumentation.ts:8 (captureRequestError) attach the share-token URL via SDK defaults, bypassing the email-redaction applied on the app_events path (lib/log/logger.ts:35 → lib/log/sanitize.ts:57); no spec/plan/DEFERRED entry documents this as intentional. Downgraded P1→P2 because the leak is DSN-config-gated, the Sentry org audience is the operators who already hold admin access, tokens are rotatable, and the project's own first-party log path already stores the identical token-bearing URL by design (lib/observe/clientErrorTransport.ts:43), so only the email-redaction contract is genuinely violated.

</details>

---

## 2. Refuted claims

One P1 claim was killed by verification — recorded so it isn't re-reported:

- **[P1 claimed] sweep — `lib/sync/applyStaged.ts`** — [anchors-chain] Live staged-review applies update show content but never refresh shows.source_anchors: the cron's freshly computed anchors are dropped at staging (live upsertLivePendingSync at lib/sync/runScheduledCronSync.ts:725 has no source_anchors column; runPhase1 is called without them at :2553) and applyStaged.ts:1288 passes no sourceAnchors to applyStagedCore, so the shows UPDATE's coalesce(NULL, source_anchors) keeps pre-change rects forever.

*Refutation:* the live staging path is retired for existing-show changes — phase1 routes existing-show diffs to `auto_apply_with_holds`, which flows through `runPhase2_unlocked` **with** `pipeline.sourceAnchors`, so anchors are refreshed in the claimed scenario. The residual gap (first-seen staged applies inserting `{}` anchors at `runScheduledCronSync.ts:1113`) is a lesser, self-healing defect and appears in §3 under PR #64/#69.

---

## 3. Unverified findings (P2/P3 — not yet adversarially verified)

Reviewer claims that have **not** been through the refutation pass. Treat each as "credible, needs a look" — batch-verify before fixing.

### 3.1 Parser — hotels & guests (11)

**U1. [P2] #40 — `lib/parser/blocks/hotels.ts:761`**
resolveDate treats ANY 4-digit 20xx token in the inline hotel cell as the reservation year, and lets it override the reliable show-context year (cellYear takes precedence over contextYear).
*Scenario:* Inline cell `| Hotel Reservations | Hyatt Place 2040 N Clark St Check In: 6/23 Check Out: 6/26 Doug --- 104461566 |` on a show whose DATES say 2025: /\b(20\d\d)\b/ matches the street number 2040, so check_in/check_out resolve to 2040-06-23/2040-06-26 instead of 2025 — wrong ISO dates rendered on the crew page and fed to date filters. Any 20xx street number, suite number, ZIP+4 tail (60611-2033) or phone fragment in the cell triggers it; PR #40 widened the old 202\d match to 20\d\d and made the previously-dead back-fill path live for all yearless M/D dates.

**U2. [P2] #40 — `lib/parser/blocks/hotels.ts:488`**
buildInlineReservations keys the multi-stay split on a raw count of /check\s+in/gi substrings, so a prose mention of 'check in' in a single-reservation cell triggers the split path and its fallback NULLs valid check-in/check-out dates.
*Scenario:* Cell `| Hotel Reservations | Westin Michigan Ave Check In: 3/23 Check Out: 3/27 Doug Larson - 7414 late check in for Eric |`: checkInCount=2 → splitInlineReservationGroups cuts at 'Check Out: 3/27'; segment 1 has no guest names (guests follow the checkout), so rows.every(names>0) fails and the fallback sets single.check_in=null/check_out=null — the cell's unambiguous 3/23–3/27 dates are silently dropped from the crew page. Also matches 'check into', 'early check in requested', etc.

**U3. [P2] #40 — `lib/parser/blocks/hotels.ts:542`**
sanitizeHotelName strips every 'TitleCase-words <dash> digits' span from the shared hotel name in the multi-stay path, which eats dash-separated street addresses (hotel-name + '- <street number>') along with the intended guest—conf# tokens.
*Scenario:* Multi-stay cell `| Hotel Reservations | Hotel Zachary - 3630 N Clark St Doug Larson - 12345 Check In: 10/8 Check Out: 10/10 Eric Weiss - 23456 Check In: 10/8 Check Out: 10/9 |`: the regex /[A-Z][a-z]+(…)*\s*[-–—]{1,3}\s*#?\d+/ matches 'Hotel Zachary - 3630' (not just 'Doug Larson - 12345'), so baseName becomes 'N Clark St' and every reservation renders that remnant as the hotel name. The 'Name - number' hotel/address shape is real in this codebase (buildInlineHotel's no-guest path at hotels.ts:684-689 explicitly protects 'Hyatt Regency - 1515 Broadway'), but the multi-group path has no such protection.

**U4. [P2] #42 — `lib/parser/blocks/hotels.ts:159`**
stripConfTokens' dash rule (\s*[-–—]{1,3}\s*#?\s*\d{4,}) false-positively deletes legitimate dash-prefixed 4+ digit street numbers, ZIP+4 suffixes, and phone fragments from persisted, crew-rendered hotel_name/hotel_address at the table path (hotels.ts:372/377) and the inline final pass (stripHotelNameConf, hotels.ts:531).
*Scenario:* An inline 'Hotel Reservations' cell "Hyatt Regency - 2233 Martin Luther King Jr Ave Check In: 6/1 Check Out: 6/3 Doug Larson - 12345" goes through stripHotelNameConf → stripConfTokens, which eats "- 2233", persisting hotel_name "Hyatt Regency Martin Luther King Jr Ave" — the house number is silently gone, so the crew page renders an un-navigable address (verified against the live regex). Same rule truncates a table-path address cell "1601 Market St Philadelphia, PA 19103-2929" to "…19103" and mangles "(312) 787-2200" to "(312) 787". A later PR shipped the noSepDash workaround for exactly this class but only on the guest-less no-Check-In branch (hotels.ts:688); the dated inline path and table path remain exposed.

**U5. [P2] #42 — `lib/parser/blocks/hotels.ts:142`**
parseGuestCell silently drops any leading guest that precedes the first '<name> <dash> <conf#>' token when separated by a character outside the name class (comma, digit, parenthesis) — the trailing un-numbered guest is captured (the tail branch) but the leading prefix before the first regex match is never pushed into names[].
*Scenario:* A v4 'Names on Reservation' cell "Doug Larson, John Carleo - #2069855" parses to names: ["John Carleo"] (verified against the live regex) — Doug Larson vanishes from the persisted names[], so hotelVisibleToViewer (lib/data/getShowForViewer.ts:102-104, surname-matching namesRefer) hides the lodging card from Doug entirely and his name is missing from every roster/lodging render. A 'Last, First' cell "Larson, Doug - #2069854" likewise persists only "Doug".

**U6. [P2] #126 — `lib/parser/blocks/hotels.ts:627`**
The learn-K guest extractor drops all cell content after the last conf# delimiter, so a trailing un-numbered guest vanishes from both names[] and hotel_name.
*Scenario:* Hotel Stays cell "Four Seasons Fort Lauderdale Doug--- 103317 Carl--- 103316 Eric Weiss" (last guest has no conf#, a shape the structured-table twin parseGuestCell explicitly handles at hotels.ts:143) → segs only collects text before each delim, text.slice(prev) after the final conf# is never examined → names ['Doug','Carl'], 'Eric Weiss' nowhere in the output (verified against an exact replica of the shipped logic). getShowForViewer's hotelVisibleToViewer (getShowForViewer.ts:102) then hides the reservation card from viewer Eric Weiss; pre-PR his name at least remained visible in the glued hotel_name.

**U7. [P2] #126 — `lib/parser/blocks/hotels.ts:589`**
The new branch is gated on !checkInMatch only, so a cell with 'Check Out:' but no matching 'Check In' takes the guest-less shortcut and loses both its Pattern-3 guests and its check-out date (a regression vs pre-PR).
*Scenario:* Cell "The Drake Hotel Checkin: 5/11 Check Out: 5/15 Eric Carroll Eric Weiss Connor Hester" (the 2025-05 Drake corpus shape with 'Check In' omitted or typo'd — 'Checkin:'/'Check-in:' fail the /check\s+in/ regex while 'Check Out:' still matches) → the guest-less shortcut at lines 680-702 returns names:[] and check_out:null unconditionally, bypassing legacy Pattern 3 (names after Check Out, lines 726-743) and the checkOut resolution; pre-PR output was names ['Eric Carroll','Eric Weiss','Connor Hester'] + check_out 5/15 (verified via replica). The hotel card is hidden from all three named crew viewers and the checkout date is silently lost.

**U8. [P2] #126 — `lib/parser/blocks/hotels.ts:612`**
delimRe ([-–—]{1,3}\s*#?\s*(\d{4,})\b) false-positives on non-conf hyphens — dash runs of 4+ and ZIP+4 hyphens — persisting garbage guest names and re-gluing/corrupting hotel data.
*Scenario:* (a) "Four Seasons Fort Lauderdale Doug ---- 103317 Carl --- 103316" (4-dash run; the live east-coast cell already mixes '---' and '–-' styles) → the {1,3} cap makes the match start inside the run, leaving a stray '-' token in seg0 → names ['-','Carl'] with a literal '-' persisted as a guest, 'Doug' re-glued into hotel_name ('Four Seasons Fort Lauderdale Doug') and dropped from names[] so his hotel card is hidden — pre-PR Pattern 2 (-{2,}) extracted 'Doug'. (b) "Four Seasons 123 Main St Miami, FL 33131-1234 Doug--- 103317 Carl--- 103316" → the ZIP+4 hyphen '-1234' is classified as a conf delimiter (streetStartsAt fails at '1234') → bogus numeric guest '33131' persisted in names[] and hotel_address truncated to '123 Main St Miami, FL'. Both verified against an exact replica of the shipped logic.

**U9. [P2] sweep — `lib/parser/blocks/hotels.ts:727`**
[viewer-matching] When a no-Check-In inline hotel cell has bare (dash-less) conf#s, Pattern 3's replace() is a no-op and its title-case pair scanner runs over the ENTIRE cell, fabricating guest names out of the hotel name and splitting a real guest across the hotel/guest boundary so namesRefer can never match them.
*Scenario:* The corpus cell fixtures/shows/raw/2025-04-asset-mgmt-cfo-coo.md:119 ('| Hotel Reservations | Four Seasons Chicago Eric Weiss 2004173 In on the 6th out on the 10th Jeffrey Justice 2004172 ... |') has no dash-conf and no 'Check Out:', so text.replace(/.*?check\s+out.../) matches nothing and the whole cell is scanned, persisting names = ['Four Seasons', 'Chicago Eric', 'Jeffrey Justice'] and hotel_name = 'Four Seasons Chicago Eric Weiss'. Viewer 'Eric Weiss' then fails hotelVisibleToViewer (namesRefer('Chicago Eric','Eric Weiss') compares surnames eric vs weiss -> false), so Eric's own reservation is hidden from his Tonight card and Travel Hotels list — the exact under-match harm PR #133 was built to prevent — while Jeffrey sees a hotel literally named 'Four Seasons Chicago Eric Weiss'. The in-code comment at hotels.ts:679-684 claims this shape 'surfaces the guest', which is false for the bare-conf form.

**U10. [P3] #42 — `lib/parser/blocks/hotels.ts:113`**
parseGuestCell flattens the &#10; guest delimiter to a space BEFORE any splitting, so a confirmation-number-less &#10;-delimited multi-guest cell collapses into one glued name — the function's own docstring claims &#10;-delimited guests 'yield clean names', but that only holds when every guest carries a conf# token (the only remaining split boundaries are conf# tokens and '/').
*Scenario:* A table cell "Eric Carroll&#10;Eric Weiss" (no conf#s) yields names: ["Eric Carroll Eric Weiss"] (verified). namesRefer's multi-token rule compares last tokens only, so viewer "Eric Carroll" (carroll ≠ weiss) fails hotelVisibleToViewer and his own hotel card is hidden — the exact under-match harm lib/data/nameMatch.ts documents as the failure to avoid. Filed P3 because the pre-PR code glued the same cell (with a raw entity), so this is an incomplete fix with a misleading contract in new code rather than a regression.

**U11. [P3] sweep — `lib/parser/blocks/hotels.ts:372`**
parseHotelTable runs stripConfTokens on the 'Hotel Name / Address' cell before splitHotelNameAddress, so a dash-separated 4-5 digit street number is deleted as a fake conf#; the sibling inline no-guest path was given a protective dash-collapse (hotels.ts:688) but this parallel structured path was not, leaving the two paths diverged.
*Scenario:* A structured HOTEL cell written as 'Hilton Garden Inn - 1500 Lake Shore Dr Chicago, IL' (dash separator instead of the usual flattened newline): stripConfTokens' /[-–—]{1,3}\s*#?\s*\d{4,}/ rule strips '- 1500', the split then finds no street number, and the show persists hotel_name 'Hilton Garden Inn Lake Shore Dr Chicago, IL' with hotel_address null — the house number is silently and unrecoverably deleted from crew-facing data. The comment at :369-370 concedes conf# never appears in this cell, so the defensive strip on this path can only cause harm; the same hazard also eats suite numbers like '#4500' via the /#\s*\d{4,}/ rule.

### 3.2 Parser — rooms (8)

**U12. [P2] #40 — `lib/parser/blocks/rooms.ts:601`**
extractGsBlock anchors the 'GS block' at the FIRST and LAST 'GS '-prefixed row anywhere in the sheet and its trailing extension skips blank lines with a stop list of only 7 headers, so the block provably swallows DETAILS rows (via 'GS Podium Type') and can swallow whole later sections, re-enabling the Digital-Signage misattribution the PR claimed to fix.
*Scenario:* On ria/redefining/consultants fixtures the block already starts at the DETAILS row '| GS Podium Type | … |' (ria.md:79 vs GS Setup at :89), pulling 10+ DETAILS rows inside the scoped block; if an operator places the DETAILS 'Digital Signage' row (consultants.md:288 shows DETAILS does carry one) after 'GS Podium Type', dsRe (rooms.ts:586-588) copies that unrelated sentence onto the GS room — the exact leak class PR #40's R14 fix targeted. Separately, on a GS-only v2 sheet with no adjacent bare DS row, the trailing scan (blank lines skipped, stop list lacking ROLE/TRANSPORTATION/HOTEL/intake-form banners) extends into later tables until the intake form's '| Digital Signage | FALSE |' row and sets GS digital_signage='FALSE'; the swallowed span also feeds the empty-col0 gear-continuation loop (rooms.ts:557-578).

**U13. [P2] #106 — `lib/parser/blocks/rooms.ts:692`**
parseBoRooms' dedup key changed from the full header line to the bare split venue name, so distinct breakout/lunch blocks that share a venue room are silently dropped with all their fields.
*Scenario:* A v2 sheet with 'BREAKOUT 1 SALON D' (Setup 'Theater for 60', Set 8:00 AM) and 'BREAKOUT 2 SALON D' (Setup 'Rounds for 40', Set 1:00 PM) — same physical room reused morning/afternoon — now both split to name 'SALON D'; `seen.has(headerKey)` at line 694 skips the second block before applyBoFields runs, so the afternoon room's times/AV vanish from the crew page (verified by executing parseRooms on current main). Same collapse drops a 'LUNCH ROOM BALLROOM C' block entirely when any breakout is also in BALLROOM C (lunch loop, line 723-724). Pre-#106 keys were 'BREAKOUT 1 SALON D' vs 'BREAKOUT 2 SALON D' / 'LUNCH ROOM BALLROOM C', so all blocks survived; the project's own GS/breakout reconciliation (rooms.ts:95-108) ratified that same-venue reuse must be lossless.

**U14. [P2] #106 — `lib/parser/blocks/rooms.ts:376`**
parseV4RoomBlock adopts splitRoomHeader's empty name with no fallback while parseBoRooms falls back to the raw first line, producing divergent mergeRooms dedup keys and a duplicated room (one nameless) for a content-bearing breakout whose venue name is unfilled.
*Scenario:* A v4 sheet mid-edit with header 'BREAKOUT 1 Dimensions Floor' (venue name not yet typed) but filled Setup/Set Time rows: parseV4RoomBlock emits {kind:'breakout', name:''} and parseBoRooms re-matches the same block via its bare-label plainFieldRe, emitting {name:'BREAKOUT 1 Dimensions Floor'} via the line-691 fallback; mergeRooms keys ('breakout::' vs 'breakout::BREAKOUT 1 DIMENSIONS FLOOR') differ, so the SAME room renders twice on the crew page and Step-3 modal — one card with a blank name heading (verified by executing parseRooms on current main). This defeats the exact double-emission the PR's own comment at line 866-868 says the shared splitter prevents; shows sync continuously, so half-filled sheet states publish.

**U15. [P2] #106 — `lib/parser/blocks/rooms.ts:933`**
splitRoomHeader strips template placeholder words ('Dimensions'/'Floor') only from the name (step 4), not from the dimensions slice (step 3), so an unfilled trailing 'Floor' placeholder leaks into the crew-visible dimensions value.
*Scenario:* Header cell 'BREAKOUT 1\nSALON D\n43' x 24' x 12'\nFloor' (dims filled, floor placeholder left — placeholders surviving next to filled values is proven by consultants' committed fixture 'LUNCH ROOM BALLROOM C Dimensions Floor') is exporter-flattened to 'BREAKOUT 1 SALON D 43' x 24' x 12' Floor'; step 2 finds no ordinal floor, step 3 takes everything from the first dim token to end-of-string, and the room's dimensions render as "43' x 24' x 12' Floor" on the crew Room-details card and Step-3 modal (verified by executing parseRooms on current main).

**U16. [P2] #106 — `lib/parser/blocks/rooms.ts:919`**
The ordinal-only floor regex plus the unconditional global 'Floor' word-strip mangles non-ordinal floor designations: the floor is lost and its qualifier is glued onto the venue name.
*Scenario:* Header 'BREAKOUT 1\nDELAWARE\nMezzanine Floor' (or 'Ground Floor'/'Lobby Floor') parses to floor:null and name:'DELAWARE Mezzanine' — step 2 matches only /\d+(st|nd|rd|th)\s+floor/, then step 4 (line 945) deletes the bare word 'Floor' from the name, leaving the orphaned qualifier fused into the room name (verified by executing parseRooms on current main). Pre-#106 the v2 multi-line header loop captured any '<X> floor' line as floor ('Mezzanine'), so this path regressed.

**U17. [P2] #114 — `lib/parser/blocks/rooms.ts:931`**
PR #114 made the colon optional for the TOTAL/A\/B dims prefixes (`\s*:?\s*` instead of `\s*:\s*`) while adding APPROXIMATELY, so an 'A/B'-suffixed room name directly followed by unprefixed dimensions gets its 'A/B' pulled out of the name and into the dimensions.
*Scenario:* A GS or breakout header cell 'GENERAL SESSION GRAND BALLROOM A/B 82' x 63' x 14'' (real room name in the corpus; unprefixed name-then-dims is the dominant header shape in fintech/fixed-income/rpas) now parses to name 'GRAND BALLROOM' + dimensions "A/B 82' x 63' x 14'" — pre-PR code produced the correct 'GRAND BALLROOM A/B' + "82' x 63' x 14'" (verified by running both regexes through splitRoomHeader's steps). The truncated name renders wrong on the crew page and changes the kind+name dedup/MI-7b preservation key, re-staging the show on a spurious rename.

**U18. [P3] #114 — `lib/parser/blocks/rooms.ts:38`**
RECONCILE_FIELDS is an exact duplicate of the FIELDS list already inlined in mergeBoFields (rooms.ts:774-789) in the same file; the PR added a second enumeration of 'all RoomRow data fields' instead of extracting one shared constant, and the two will silently drift.
*Scenario:* When a new data field is added to RoomRow (and to mergeBoFields' list or buildEmptyRoom) but not to RECONCILE_FIELDS, the same-name breakout reconciliation judges a breakout with a DISTINCT value in that field as a 'lossless subset', absorbs it into the GS room, and drops the breakout's value for the new field entirely — silent crew-visible data loss, the exact bug class the subset gate was built to prevent.

**U19. [P3] #173 — `lib/parser/blocks/rooms.ts:572`**
The new GS continuation-row loop claims to be 'scoped to the GS block', but its scope (extractGsBlock, first->last 'GS *'-prefixed row anywhere in the document) spans hundreds of unrelated lines on real sheets because 'GS Podium Type' sits in the standard DETAILS template, and the discipline-classified branch appends any empty-col0 row's value to the GS room regardless of prevGsCol.
*Scenario:* On raw/2025-06-ria-investment-forum.md the effective 'GS block' is lines 123-227 (the entire ~80-row VENUES table); on raw/2025-03-dci-rpas-central.md it is lines 183-306 (crew roster, dress, hotel, agenda rows). Any `| | <value> |` row in such a span whose value contains a discipline keyword — e.g. a DETAILS 'Backdrop / Scenic' value wrapped onto a continuation row ('| | (1) II Logo Spandex ... |', SPANDEX -> scenic) — is appended onto the GS room's discipline column, duplicating non-room content on the crew page. Current fixtures survive only because their in-span empty-col0 rows happen to be keyword-free (verified by parser run), so the documented guard is illusory.

### 3.3 Parser — schedule / dates / bookends (7)

**U20. [P2] #44 — `lib/parser/blocks/dates.ts:75`**
The datesEmpty predicate checks only the original four fields (travelIn/set/showDays/travelOut) and has diverged from the dates shape, so a DATES block that yields loadIn/setupTime/setAgendaRaw but no normalizable dates emits a false 'parsed zero fields — section dropped' warning.
*Scenario:* A v2/v4 5-col DATES block where every DATE cell fails normalizeDate (e.g. 'TBD' or the PR's own '5/13 - AFTER 8PM' qualifier case) but the SET row's TIME cell carries clocks ('8:00 AM Load In 10:00 AM Setup') populates result.loadIn/setupTime/setAgendaRaw (dates.ts:214-229) yet leaves all four checked fields empty → SECTION_HEADER_NO_FIELDS fires claiming zero fields were parsed. Meanwhile dates.loadIn renders crew-side as the bare Set clock via lib/crew/resolveKeyTimes.ts:106-109 (explicitly set-date-independent, wp-23) and the RightNow load-in line (components/right-now/buildRightNowContext.ts:86), so the operator-facing warning asserts a section drop for data that is live on crew pages.

**U21. [P2] #172 — `lib/parser/blocks/scheduleBookends.ts:110`**
tokenizeSetSchedule's label-before mode guard only checks the FIRST clock's lead, so a SET cell with a vocab colon lead followed by a corpus-style time-first list bypasses the R9-R14 trailing-label rejection and mis-attributes each trailing label to the NEXT clock (and drops the last label) — a regression versus the pre-PR position-default synthesis that handled these cells correctly.
*Scenario:* SET TIME cell "Setup: 9:00PM - LOAD IN 10:00PM - SETUP" (verified by executing the live module): lead "Setup:" is colon-terminated and in SET_LABEL_VOCAB, so the whole cell is tokenized in label-before mode; the slice " - LOAD IN " between the clocks is recognized and attached to the 10:00PM clock. Crew run-of-show renders "Setup 9:00PM" + "LOAD IN 10:00PM" — Load In shown an hour late and the Setup label dropped — whereas pre-PR (and for the same cell without the lead, which returns []) it correctly rendered "Load In 9:00PM" + "Setup 10:00PM". Single-clock variant "Set: 11:00 AM LOAD IN" likewise loses the operator's LOAD IN label (renders "Set 11:00 AM").

**U22. [P2] sweep — `lib/parser/blocks/scheduleBookends.ts:149`**
[set-strike-loadout] Same-day multi-time strike groups are ordered by a.time.localeCompare(b.time) — a lexicographic compare of verbatim clock strings — so a 10:00 PM strike sorts before a 9:00 PM strike, contradicting the feature spec's ratified 'multiple strikes sort by time ascending' and rendering out of chronological order on every sheet-order surface.
*Scenario:* Rooms grid: GS strike_time '10/9 @ 10:00 PM', breakout strike_time '10/9 @ 9:00 PM'. Groups sort '10:00 PM' before '9:00 PM' ('1' < '9' lexicographically), so appendEntry pushes the 10 PM strike first; RunOfShowList (RunOfShowList.tsx renders the synthetic group in array order, no time sort) shows 'Strike — General Session 10:00 PM' ABOVE 'Strike — Breakout 9:00 PM' on the crew Schedule day card, crew Today (no-agenda path), and admin Step-3. Same class for '12:00 PM' vs '9:00 AM'. Only the agenda-merged Today timeline (which re-sorts by minutes) shows the correct order.

**U23. [P2] #70 — `lib/parser/blocks/scheduleTimes.ts:122`**
readShowDayTimeCells requires /^SHOW\s+DAY\b/i on the row label while parseDates' classifyLabel treats any /^SHOW/-prefixed label as a show day, so label variants produce a show day whose TIME column is silently dropped with zero observability.
*Scenario:* A DATES row labeled 'SHOW 1', 'SHOW', 'SHOWDAY 2', or 'SHOW NIGHT' (classifyLabel at lib/parser/blocks/dates.ts:36 accepts all of these — the corpus already contains bare 'Show' labels in the east-coast sheet) enters dates.showDays and renders as a show day on the crew page, but readShowDayTimeCells skips the row entirely: no ScheduleDay is built AND no SCHEDULE_TIME_UNPARSED fires (the row never enters the scan), so the per-day run-of-show/key-time silently vanishes — the exact 'TIME column dropped wholesale' gap this PR was meant to close. The deep-link anchor scanner (lib/drive/showDayTimeAnchors.ts:48) mirrors the same narrow regex.

**U24. [P2] #70 — `lib/parser/blocks/scheduleTimes.ts:146`**
The SHOW DAY tokenizer never entity-decodes the TIME cell, so exporter-emitted '&#10;' newlines produce phantom clock tokens (the semicolon-typo tolerance matches inside '&#10;NN') and leak raw '&#' entity residue into crew-visible entry titles.
*Scenario:* The exporter emits multi-line TIME cells with literal '&#10;' (proven live: consultants TRAVEL/SET cell '9:00PM - LOAD IN&#10;10:00PM - SETUP', fixtures/shows/exporter-xlsx/consultants.md:17). If Doug writes a SHOW DAY cell the same way, e.g. 'GS: 9:00AM&#10;10:00PM - Awards Dinner', CLOCK_RE (line 21, [:;] separator tolerance) matches '10;10' inside '&#10;10' → phantom entry start '10:10'; the real clock's leading digits are consumed leaving '00PM' → second phantom start '0PM'; titles carry '&#' fragments. Crew page renders garbage entries ('0PM Awards Dinner', '9:00AM &#'). The sibling SET-row path was later fixed with decodeEntities(clean()) in extractClockTimes (lib/parser/blocks/dates.ts:298) but this PR-70 tokenizer was never swept.

**U25. [P3] #172 — `lib/parser/blocks/dates.ts:229`**
The explicit-SET-row setAgendaRaw capture is an unconditional clobber (`result.setAgendaRaw = clean(setCell) ? setCell : null`) while the loadIn/setupTime precedence it claims to mirror is conditional (`if (times[0]) ...`), so an empty explicit SET TIME cell nulls a travel_set-captured label-before cell while keeping that same cell's times — silently disabling the cell-derived labels this PR exists to produce.
*Scenario:* DATES table with `TRAVEL / SET | Mon | 3/22/26 | Load In: 8:00 AM Room Access: 9:00 AM` followed by `SET | Tue | 3/23/26 | ` (empty TIME cell) — verified by executing parseDates: loadIn="8:00 AM" and setupTime="9:00 AM" survive from the travel_set row, but setAgendaRaw is clobbered to null by the empty SET row. deriveScheduleBookends therefore falls through to the generic synthesis and renders "Setup 9:00 AM" instead of the operator's "Room Access 9:00 AM", from the very cell whose times are still being used. Output degrades to pre-PR behavior (not a regression), and the inline comments ("mirrors loadIn precedence" / "explicit SET overrides") document a symmetry the code doesn't have.

**U26. [P3] #70 — `lib/parser/blocks/scheduleTimes.ts:122`**
PR 70 added a second, independent DATES-block SHOW-DAY walker (readShowDayTimeCells) whose row predicate (/^SHOW\s+DAY\b/i, row.length >= 5) is stricter than the sibling walker parseV2V4Dates uses to build showDays (classifyLabel /^SHOW/ at dates.ts:36, row.length >= 4), so the two scans of the same rows have already diverged.
*Scenario:* A sheet row labeled 'SHOW DAY1', 'SHOWDAY 2', or bare 'SHOW' (label variants classifyLabel accepts) puts the date into dates.showDays — the day renders a DayCard, counts in the hero's 'Show day n of total', and gets a KeyTimes room fallback — but readShowDayTimeCells never scans its TIME cell, so its schedule content is silently dropped with NO ScheduleDay and NO SCHEDULE_TIME_UNPARSED warning, even though that warning exists precisely to flag contentful-but-unparsed TIME cells.

### 3.4 Parser — other blocks & infrastructure (14)

**U27. [P2] #125 — `lib/parser/blocks/crew.ts:278`**
Email-unreadable predicate gates on canonicalize() (trim+lowercase only) instead of a presence()-style check, so exporter whitespace entities (&#10;/&#9;) in an EMAIL cell count as content and fire a spurious FIELD_UNREADABLE warning — asymmetric with the phone path, which decodes those entities and treats the cell as absent.
*Scenario:* A crew EMAIL cell containing only an in-cell newline (Alt+Enter or a pasted value with a stray LF) exports as "&#10;"; canonicalize returns "&#10;" (non-null, no @) so every sync emits 'Crew email for row N couldn't be read as an email address ("&#10;") — check the sheet' on the admin Data-quality panel, even though the cell looks empty in Sheets and the same content in the PHONE cell is silently treated as absent (presence() decodes entities per lib/parser/blocks/_helpers.ts:66-74). This violates the feature's own guard condition ('whitespace-only → no warning') and the warning persists until the invisible newline is deleted.

**U28. [P2] #191 — `lib/parser/blocks/dress.ts:37`**
parseDress collects continuation-row values with no per-line sentinel filtering, so a multi-row sentinel DRESS block joins into a value that defeats both the sentinel-aware merge and the UI's sentinel hiding.
*Scenario:* A sheet whose DRESS block rows are each a sentinel (e.g. `| DRESS | TBD |` + continuation `|  | TBD |`, a plausible mid-planning state on the standard two-row template) yields dress='TBD\nTBD'. shouldHideGenericOptional only matches whole-string sentinels, so mergeDressCode treats it as real and overwrites a real DETAILS `Dress Code`/`Attire` value (the exact clobber the spec's R6 sentinel-aware merge was built to prevent), and TodaySection.tsx:318's gate fails to hide it — the crew dress card renders 'TBD TBD'. Verified mechanically: 'TBD\nTBD' is not in GENERIC_OPTIONAL_HIDE and replaces an existing 'Black Tie'.

**U29. [P2] #191 — `lib/parser/blocks/dress.ts:36`**
parseDress stores dress values via clean() only, skipping the decodeEntities step every other event_details writer applies at the value-storage boundary, so raw `&#10;`/`&#9;` entity text can reach the crew dress card.
*Scenario:* A DRESS value cell containing an in-cell newline (Alt+Enter — common in this corpus; the exporter's escapeCell/normalizeNewlines emits it as `&#10;`, e.g. 'Set/Strike: X&#10;Show: Y') is stored verbatim into event_details.dress_code. The sibling writer parseEventDetails stores through presence() (event.ts:320) which decodes entities per the documented contract at lib/parser/blocks/_helpers.ts:55-64 ('final field values never surface a raw entity to crew'), and no crew component decodes at render — so the crew dress card displays the literal text 'Set/Strike: X&#10;Show: Y'.

**U30. [P2] sweep — `lib/parser/blocks/event.ts:265`**
[typo-tolerance] Form-layout harvest fuzzy-corrects field labels with no FIELD_LABEL_AUTOCORRECTED warning — harvestFormLayout/resolveKnownCanon never receive the aggregator, violating the spec's 'field-label corrections all warn+deep-link' contract that the classic path honors.
*Scenario:* On a form-layout show (classic EVENT DETAILS block absent, e.g. the consultants/rpas class), a row `| Keynote Requirments | need confidence monitor |` inside an anchored >=3-known-label run is fuzzy-corrected (Damerau-1) and written to event_details.keynote_requirements, but harvestFormLayout(markdown, result) has no agg parameter, so no FIELD_LABEL_AUTOCORRECTED warning is emitted and the operator-actionable review list (dataGaps.ts OPERATOR_ACTIONABLE_ANCHORED) shows nothing. The identical typo in a classic EVENT DETAILS block emits the warning (event.ts:230-237), so the same correction is visible on one path and silent on the other.

**U31. [P2] #44 — `lib/parser/blocks/transport.ts:146`**
isEmptyTransport() hand-enumerates TransportationRow fields and omits loadout_name/loadout_phone/loadout_email, so a transport section whose only parsed content is the 'Load Out:' secondary transporter is falsely reported as 'parsed zero fields — section dropped'.
*Scenario:* A v4 sheet with the plain exporter header '| TRANSPORTATION | TRANSPORTATION | PHONE | EMAIL |', a blank Equipment Transporter row, and a populated '| Load Out: | Bob | 555-1234 | bob@x.com |' row returns a row where only loadout_* are non-null (transport.ts:272-277). isEmptyTransport ignores those three fields, returns true, and emitEmptySection fires a SECTION_HEADER_NO_FIELDS warning into pending_syncs.warning_summary / the StagedReviewCard claiming the transportation section was dropped — while the load-out contact is actually persisted and rendered on the crew TravelSection (components/crew/sections/TravelSection.tsx:195) and Step3SheetCard (line 332), sending the operator to 'fix' a sheet that parsed fine.

**U32. [P2] #47 — `lib/parser/blocks/travelFlights.ts:21`**
The TRAVEL flight path never decodes the exporter's &#10; in-cell-newline entity (it bypasses the codebase's decodeEntities/presence() storage boundary used by the crew.ts TECH flight path), so any exactly-2-line FLIGHT DETAILS cell is either silently dropped or stored with a literal entity that garbles the crew Travel card.
*Scenario:* A source cell 'GEUZAB\n3/22 AA3002 LGA - ORD 7:23am - 9:15am' (conf + one-way leg, 2 lines) is exported as 'GEUZAB&#10;3/22 ...' because shouldPreserveNewlines (lib/drive/exportSheetToMarkdown.ts:32) only space-flattens cells with >=3 lines; normalizeTravelCell tokenizes on \s+ so 'GEUZAB&#10;3/22' fails DATE_RE, zero dates are found, and the crew member's real flight is dropped with a TRAVEL_FLIGHT_UNPARSEABLE warning that renders on no admin surface (not in DATA_GAP_CODES or OPERATOR_ACTIONABLE_ANCHORED). For a 2-line two-leg cell ('5/2 ... 1:00pm\n5/7 ... 1:17pm'), flight_info is stored containing '1:00pm&#10;5/7', and parseFlightItinerary (lib/crew/flightDisplay.ts:163) splits only on '|' or '\n', so the crew card shows one merged segment with the outbound route but the RETURN leg's dep/arr times, and the return flight disappears entirely. The PR's docstring claim that 'the exporter flattens the source cell to one space-separated line' is false for 2-line cells; the fixtures pass only because both real flyers happened to have 3-line cells.

**U33. [P2] sweep — `lib/parser/blocks/venue.ts:146`**
[typo-tolerance] The FIELD_LABEL_AUTOCORRECTED warning for a fuzzy-corrected venue label is gated on inVenueFieldScope, which is still false when the block's FIRST field row is the misspelled one — so the most common typo position (VENUE NAME, the opening row of the v4 2-column venue block) is autocorrected silently.
*Scenario:* A v4 sheet whose venue block opens with `| VENU NAME | Waldorf Astoria Chicago |` (the 2-column shape at fixtures/shows/raw/2026-04-asset-mgmt-cfo-coo-waldorf.md:75 with a 1-edit typo): resolveAliasScoped corrects col0Canon to venue.name and the assignment at line 240-248 sets name, but the warning check at line 146 ran earlier in the same iteration while inVenueFieldScope was still false (it only becomes true via a field assignment), so no FIELD_LABEL_AUTOCORRECTED is pushed — contradicting the adjacent comment 'preserving venue's operator visibility (NOT the silent info downgrade)'. Any misspelled label on a LATER venue row does warn, making the gap first-row-specific.

**U34. [P2] #154 — `lib/parser/dataGaps.ts:163`**
operatorActionableWarnings' dedup key (code, gid, a1) conflates same-CELL with same-REGION anchors, so multiple distinct FIELD_UNREADABLE warnings (different crew rows/fields, all sharing the one crew-region range anchor) collapse to a single item — and since PR 154 also removed these codes from the per-show flat message digest (app/admin/show/[slug]/page.tsx:310), all but the first warning become completely invisible on /admin/show/[slug].
*Scenario:* A show sheet has an unreadable phone in crew row 2 and an unreadable email in crew row 4: both FIELD_UNREADABLE warnings get the identical region anchor (same gid + a1 range from sources.region['crew']), so selectActionableForDisplay returns only the first; the panel renders one card showing only the phone message (FIELD_UNREADABLE's catalog title is null, catalog.ts:1373-1381, so the card falls back to the first warning's .message). The row-4 email problem is silently hidden nowhere else on the page — pre-PR-154 both messages were listed in the digest. This contradicts the function's own contract ('no actionable row is ever hidden', dataGaps.ts:150) and the page comment's 'strictly better' claim.

**U35. [P2] #173 — `lib/parser/index.ts:405`**
mergeGearIntoRooms fill-don't-clobber tests `match[col] == null` only, so an INFO-stored sentinel placeholder ("-", "—", "N/A", "TBD") blocks the GEAR-tab fill while the render layer hides that sentinel — the GEAR-parsed gear is silently invisible on the crew page.
*Scenario:* INFO breakout row `BO Audio | -` (exactly what fixtures/shows/exporter-xlsx/ria.md stores today: DRAWING ROOM A audio="-", lighting="-") combined with a GEAR date-grid carrying audio for the same (kind, name-token) room: verified by repro — merged room keeps audio="-", shouldHideGenericOptional("-") is true (the '-' sentinel PR #173 itself added to GENERIC_OPTIONAL_HIDE), so the crew Audio scope card omits the room and the parsed GEAR audio is silently dropped. Inconsistent with the same PR's fillIfAbsentOrSentinel rule in lib/parser/blocks/event.ts:278.

**U36. [P2] #173 — `lib/parser/pull-sheet.ts:151`**
collectDataBlock scans arbitrarily far past the pull-sheet case's own table (stopping only at the next PULL SHEET header or EOF), so when the case's table is empty or flagless it adopts the first later block containing TRUE/FALSE in col0/col4 — e.g. the internal ops CHECKLIST tab — as the case's items.
*Scenario:* A show whose PULL SHEET tab exists but whose item table is still empty (created via the 'Create Pull Sheet' checklist step, not yet filled) and whose workbook carries the standard internal checklist tab later (exact shape at fixtures/shows/exporter-xlsx/rpas.md:271-306 / fintech.md:367): verified by repro — parsePullSheet returns the checklist rows ('| TRUE | Confirm Venue | Client |' etc.) as the case's items via the 3-col raw fallback, so the crew Gear pack-list card renders the internal ops checklist raw, pipes and all. Pre-PR behavior returned an empty case.

**U37. [P3] #123 — `lib/parser/blocks/crew.ts:269`**
The Class-A unreadable-phone detector runs digitsOnly() on the raw cell but presence() on the entity-decoded cell, so the exporter's in-cell whitespace entities (&#10;/&#9;) contribute phantom digits ('10'/'9') that defeat the zero-digits check.
*Scenario:* A crew phone cell containing an in-cell line break and no real digits (e.g. raw markdown cell `TBD&#10;ask Doug` — the exporter emits &#10; for in-cell newlines per lib/parser/blocks/_helpers.ts:55-68) yields presence()='TBD ask Doug' (non-null) but digitsOnly(raw)='10' (length 2), so phoneUnreadable is false: no FIELD_UNREADABLE warning is emitted and the phone is not nulled at crew.ts:348. The exact silent-drop Class A exists to surface (Data-quality panel, Step-3 chips, first-published digest) is skipped, and the junk string persists as the member's phone. The check should test the decoded value, e.g. digitsOnly(presence(phoneRaw) ?? '').

**U38. [P3] sweep — `lib/parser/blocks/crew.ts:100`**
[typo-tolerance] detectColumns lets a LATER fuzzy-matched header segment overwrite an EARLIER exact column assignment (assign(fix.match, i) is unconditional), diverging from the exact-always-beats-typo two-pass topology that detectPassengersColIdx (transport.ts:548-559) explicitly implements for the same bug class.
*Scenario:* A v4 crew header carrying a stray annotation cell within Damerau-1 of a vocab word AFTER the real column — e.g. `| CREW | NAME | ROLE | PHONE | EMAIL | E MAIL |` (duplicated/merged-cell artifact) — remaps colMap.email from index 4 to index 5; every crew row's email is then read from the trailing TRUE/FALSE checkbox column (the 6th column in the exporter v4 crew layout, see fixtures/shows/exporter-xlsx/fintech.md crew rows), canonicalize('TRUE') has no '@', so every member gets FIELD_UNREADABLE and a null email while the real email column is ignored. This gate also has no minLen/tieAbort, unlike every other PR-A surface.

**U39. [P3] #173 — `lib/parser/gearClassification.ts:86`**
classifyGearItem matches allow-list keywords as raw substrings (u.includes(k)) with audio checked first, so short tokens like "MIC", "K8", "K10" shadow more-specific video/lighting/scenic keywords inside longer words; the gearClassificationRegistry collision meta-test only checks exact-literal duplication across lists and cannot catch this.
*Scenario:* Verified by repro: "MICROSOFT SURFACE LAPTOP" -> audio (MIC ⊂ MICROSOFT beats LAPTOP/video), "4K80 LASER PROJECTOR" -> audio (K8 ⊂ 4K80 beats PROJECTOR/video), "DYNAMIC UPLIGHT WASH" -> audio (MIC ⊂ DYNAMIC beats UPLIGHT/lighting). Any such GEAR-grid item or GS continuation row lands in the wrong discipline card on the crew Gear page; no word-boundary matching and no test can catch it.

**U40. [P3] sweep — `lib/parser/typoGate.ts:37`**
[typo-tolerance] tieAbort counts tied vocab MEMBERS instead of distinct canonical TARGETS, so alias families that deliberately include known-typo spellings (VIRTUAL/VIRTAUL AUDIENCE, GOOSENECK/GOOSNECK/GOOSENECKS, DRESS CODE/DRESS_CODE, hotel/hotal contact info) abort recovery for exactly the labels the feature exists to recover, even when both tied candidates resolve to the same canonical key.
*Scenario:* EVENT DETAILS row `| Virtul Audience | Yes - 200 remote |`: Damerau distance is 1 to BOTH 'VIRTUAL AUDIENCE' and 'VIRTAUL AUDIENCE' (verified), both mapping to canonical virtual_audience; tieAbort returns null, the label falls to the normalize-and-keep fallback and is stored as unknown key 'virtul_audience' with UNKNOWN_FIELD, so event_details.virtual_audience stays absent and the crew page's 'Virtual audience' field (lib/crew/eventDetailsSpecs.ts) doesn't render. Same self-defeat for 'Goosnecks' (ties GOOSNECK/GOOSENECKS) and 'Hotl Contact Info' (ties hotel/hotal contact info on the venue.contact_info path). Fix shape: count distinct REVERSE_MAP/CANONICAL_KEY_MAP targets at bestDist, not members.

### 3.5 Sync / cron / Drive (15)

**U41. [P2] #132 — `lib/drive/fetch.ts:249`**
Passing `retry: false` to drive.files.get/files.list disabled gaxios's built-in no-response network-error retry (previously ON via googleapis-common's `retry: true` default, noResponseRetries=2) while the replacement classifier `driveErrorStatus` only retries `TimeoutError`/`ETIMEDOUT`/`ECONNABORTED` — codes gaxios-7 native fetch never emits for real socket failures (undici throws `TypeError: fetch failed` with the code at cause.cause.code, which GaxiosError does not copy), so transient network blips that used to be silently absorbed now fail the whole pass on the first attempt.
*Scenario:* During an onboarding Step-2 scan, one per-sheet `files.get` (or the folder `files.list` in lib/drive/list.ts:100-106) hits a transient TCP reset / EAI_AGAIN DNS blip on a reused connection -> GaxiosError{code: undefined, no status} -> driveErrorStatus returns null -> withDriveRetry rethrows immediately -> fail-fast mapWithConcurrency aborts the entire scan; a cron/manual sync pass hitting the same blip marks the show SYNC_INFRA_ERROR. Pre-#132, gaxios's internal layer retried these up to 2x and the pass continued — the exact 'single transient Drive failure aborts the whole pass' class the BL-ONBOARDING-SCAN-TRANSIENT-THROTTLE-RETRY comment at lib/drive/fetch.ts:110-114 says must be retried (408 also lost retry coverage). DXT-3 later copied the same `{timeout, retry:false}` pattern to sheetGids/verifyReelOnApply/cron/applyStaged, spreading the gap.

**U42. [P2] #77 — `lib/sync/applyStaged.ts:1601`**
The wizard revision-race restage's injected fetchMarkdownWithBinding discards the xlsx bytes (uses fetchSheetAsMarkdownAtRevision, returns no bytes), so the restaged row is staged with source_anchors={} and link-less parse warnings, and after finalize the published show's 'Open in Sheet' deep links fall back to the wrong tab (#gid=0).
*Scenario:* Doug edits a sheet after the wizard scan stages it; admin clicks apply in Step 3 -> revision_race -> inline restage runs prepareOnboardingFiles with bytes undefined, so attachWarningAnchors early-returns (attachWarningAnchors.ts:30) and sourceAnchors={} is persisted to pending_syncs.source_anchors (runOnboardingScan.ts:957,990,604). Finalize reads persisted anchors and skips empty ones (finalize/route.ts:816-820,984 — it no longer re-exports the XLSX), so shows.source_anchors stays {} and every region/warning deep link for that show degrades to the wrong tab until a later cron sync recomputes anchors. Fix is one line-class: use fetchSheetMarkdownAndBytesAtRevision and return bytes (the export already downloads them).

**U43. [P2] #166 — `lib/sync/enrichWithDrivePins.ts:332`**
Best-effort venue geocoding (worst-case ~12.4s of Google latency plus two un-time-bounded Supabase cache round-trips) was added inside the cron sync's single hard 30s enrichment budget, so a slow/hanging Google Geocoding API can now fail an entire show's sync instead of silently falling back.
*Scenario:* Cron sync processes a changed image-heavy sheet whose embedded-image/linked-folder/agenda enrichment legitimately takes ~18-25s (passed pre-PR); the venue is a cache miss and Google Geocoding hangs (2 attempts x 6s + backoff = ~12.2s, breaker not yet open — this hits the first 3 changed shows of an outage and one probe per 60s cooldown thereafter). The shared withStepTimeout("enrichWithDrivePins", ...) at lib/sync/runScheduledCronSync.ts:2421 (DRIVE_SYNC_STEP_TIMEOUT_MS=30_000) fires, processOneFile returns fetch_failure with SYNC_STEP_TIMEOUT, and the whole show sync errors (last_sync_error/admin-alert surface) — contradicting enrichVenueGeocode's own stated contract that a geocode outage only leaves venue.city unset.

**U44. [P2] #80 — `lib/sync/retrySingleFile.ts:271`**
Running the whole onboarding scan on the single Lock#2 transaction (`{ tx: scanTx, withShowLock: passthrough }`) breaks the scan's live-row-conflict recovery protocol, which requires a FRESH transaction after the 23505/42P10 abort — so a retry that hits a live-row conflict throws OnboardingScanInfraError (25P02) and the route 500s instead of returning the typed live_row_conflict outcome.
*Scenario:* Admin retries a hard-failed wizard pending_ingestion whose drive_file_id collides with a live pending_syncs row under the legacy-PK drift (the exact production class tests/onboarding/onboardingScanLiveRowConflictDb.test.ts defends): runPhase1's wizard insert raises 23505, scanPreparedFileWithTx throws OnboardingScanLiveRowConflictRollbackError, and scanPreparedFiles' recovery (runOnboardingScan.ts:873, 'FRESH transaction') re-enters withTx — but with deps.tx injected, withScanTx (runOnboardingScan.ts:1018-1021) hands back the SAME already-aborted Lock#2 transaction, so recordLiveRowConflict's first statement fails with 25P02 in_failed_sql_transaction and callTx wraps it as OnboardingScanInfraError. The route's catch only handles WizardSessionSupersededRollbackError, so the request returns an unhandled HTTP 500; no live_row_conflict manifest transition, no LIVE_ROW_CONFLICT admin alert, no sync_log row is recorded, and since the tx rolls back the pending row survives — every subsequent retry 500s identically instead of the intended 200 {status:"live_row_conflict"} that statusFromScan (retrySingleFile.ts:97-99) and retryResponse (route.ts:142) explicitly support. The retry's real-DB regression tests only cover the clean-staged and superseded branches, so this branch is untested.

**U45. [P2] #64 — `lib/sync/runScheduledCronSync.ts:2452`**
A transient/exhausted-retry failure of the pre-fetched listSpreadsheetSheets call is silently swallowed and makes extractSourceAnchors return a defined `{}`, which the always-spread cron path persists through coalesce($::jsonb, source_anchors), durably wiping all previously good shows.source_anchors.
*Scenario:* Doug edits a show sheet; during the cron pass, Sheets `spreadsheets.get` fails after withDriveRetry exhaustion (429 burst/5xx) and is swallowed at lines 2412-2415, so titleToGid is empty and sourceAnchors becomes `{}` (defined, not undefined). Phase 2 applies normally, `source_anchors = coalesce('{}'::jsonb, source_anchors)` overwrites the good anchors, last_seen_modified_time advances and later passes skip via deferred_modtime — every crew "In sheet" link on all 7 sections degrades to the bare `#gid=0` INFO link until the sheet is next edited, with no warning, alert, or log (the enrich step's own fallback re-call of the same API can succeed, so the pass looks fully healthy).

**U46. [P2] #69 — `lib/sync/runScheduledCronSync.ts:1163`**
The coalesce-preserve introduced for staged applies leaves shows.source_anchors indefinitely stale: an approved staged apply writes the new revision's content while keeping anchors extracted from the pre-edit revision, and nothing on the staged live path ever re-extracts them.
*Scenario:* Live show has anchors from cron auto-apply at revision R1 (e.g. details -> A55:B70). Doug inserts rows above DETAILS and edits crew at R2; the cron computes fresh R2 anchors (runScheduledCronSync.ts:2451) but the staging path discards them (phase1 persists pending_syncs.source_anchors only for the onboarding scan, lib/sync/phase1.ts:44-46) and applyStaged (lib/sync/applyStaged.ts:1288) passes no sourceAnchors, so the UPDATE's coalesce($18::jsonb, source_anchors) retains R1 anchors against R2 content. Crew/admin 'In sheet' deep links now highlight the wrong cells, and because the staged apply advances last_seen_modified_time to R2 the cron never re-extracts until the NEXT sheet edit — shows whose edits always trigger review (crew changes) keep misdirected anchors across every subsequent staged apply.

**U47. [P2] #102 — `lib/sync/runScheduledCronSync.ts:2565`**
The hard_fail sync outcome commits shows.last_sync_status='parse_error' (via runPhase1's updateShowParseError) but carries no showId, so every caller's showId-presence revalidate gate skips the cache bust and crew pages serve a stale sync status.
*Scenario:* A live published show's sheet is edited so parsing hard-fails an invariant (e.g., structural block deleted). Cron runs processOneFile: phase1 writes last_sync_status='parse_error' + last_synced_at=now() to public.shows (phase1.ts:283, runScheduledCronSync.ts:770) and commits, but the result is { outcome:'hard_fail', code } with no showId (even though show.showId is read at line 2567 for the admin alert), so revalidateShowFromResult no-ops. Crew who load the page keep seeing the cached last_sync_status='ok' fan-out — StaleFooter never shows the red PARSE_ERROR_LAST_GOOD banner (StaleFooter.tsx:60) — for up to the 300s TTL on fresh loads, and indefinitely on an already-open page until the next covered write.

**U48. [P2] sweep — `lib/sync/runScheduledCronSync.ts:2421`**
[agenda-pipeline] The cron path wraps enrichWithDrivePins — which since the agenda merge includes up to 6 agenda-PDF downloads (≤25MB each, per-download deadline 120s) plus main-thread pdf.js extraction — in the pre-agenda 30s DRIVE_SYNC_STEP_TIMEOUT_MS, so aggregate agenda work >30s fails the ENTIRE show sync, repeatedly.
*Scenario:* A sheet links several large agenda PDFs (e.g. two 20MB scanned decks; AGENDA_PDF_MAX_BYTES allows 25MB) whose download+getTextContent exceeds 30s combined with embedded-image capture. withStepTimeout('enrichWithDrivePins') throws SyncStepTimeoutError → the whole sync returns parse_error (not just a missing agenda, defeating enrichAgenda's never-throws best-effort contract), last_seen_modified_time does NOT advance, so every cron tick re-downloads everything and fails again — the show is wedged (no crew-page updates of ANY kind) and Drive quota burns each tick. The admin extract route gives this same work 250s+300s; the cron inherits 30s.

**U49. [P2] sweep — `lib/sync/runScheduledCronSync.ts:2451`**
[anchors-chain] A transient listSpreadsheetSheets failure during a cron auto-apply silently wipes a show's existing good anchors to {}: sheets=undefined (:2410-2415) yields an empty titleToGid, extractSourceAnchors then returns {} (defined, not omitted), and updateOrInsertShow's coalesce('{}'::jsonb, source_anchors) persists the empty object — violating the documented 'omitted (not {}) so coalesce never wipes existing anchors' contract at lib/sync/applyStagedCore.ts:435-436.
*Scenario:* A show has full anchors from weeks of syncs. On one cron tick the Drive XLSX export succeeds but the Sheets v4 spreadsheets.get metadata call times out (it is untimed per DEFERRED.md DXT). The sync applies 'ok' but shows.source_anchors is overwritten with {}; every crew 'In sheet' link regresses to #gid=0 (INFO tab), so schedule/gear links land on the wrong tab, and nothing repairs it until the operator edits the sheet again AND that sync auto-applies.

**U50. [P3] sweep — `lib/drive/unknownFieldAnchors.ts:135`**
[anchors-chain] The two halves of the UNKNOWN_FIELD (kind,label,value) correlation key use divergent value-column semantics: the anchor extractor takes the first non-blank cell after the label (nextNonBlankAfter, :77-83/:135) while the venue parser's warning value is strictly the second column (`presence(row[1] ?? '') ?? ''`, lib/parser/blocks/venue.ts:299), so any row where they disagree can never produce the exactly-one match.
*Scenario:* A VENUE-block row like 'Loading Dock | <blank> | Yes' (label in col A, value in col C, col B empty and unmerged): the parser emits UNKNOWN_FIELD with value '' while the extractor records value 'yes' for the same row; resolveUnknownFieldCell finds zero matches and returns null, so the warning renders link-less on every admin surface even though exactly one unambiguous source row exists — a systematic, silent anchor miss rather than the designed ambiguity degradation.

**U51. [P3] #166 — `lib/geocoding/cache.ts:57`**
readGeocodeCache/writeGeocodeCache have no timeout/AbortSignal, yet enrichVenueGeocode's header claims the enrichment "never hangs the sync"; on the onboarding scan path there is no outer step-timeout at all, and this PR introduced the first (unbounded, side-effecting) DB I/O into a prepare phase documented as "pre-lock, side-effect-free Drive read".
*Scenario:* During an onboarding wizard scan, prepareOne (lib/sync/runOnboardingScan.ts:947) awaits enrich → enrichVenueGeocode → readGeocodeCache with no time bound anywhere on that path (only the Google fetch has AbortSignal.timeout); if the Supabase REST connection stalls without erroring (undici default headers/body timeouts are ~300s), the wizard scan wedges until the route's maxDuration=300s kills it instead of degrading within the documented ~12s per-venue budget — the exact untimed-read-hangs-the-sync class the module claims to have closed.

**U52. [P3] #181 — `lib/sync/enrichAgenda.ts:307`**
For a PDF whose Drive metadata has a missing/empty headRevisionId, the verdict layer can never emit "fresh" (revStable requires a non-empty currentRev), so the schedule is re-extracted on every pass but never stored, and the admin endpoint's known_stale merge actively deletes any previously stored extraction — including on transient download failures where staleness is not proven.
*Scenario:* Agenda link points at a PDF for which files.get returns no headRevisionId (defaultDriveClient coerces it to "" at runScheduledCronSync.ts:1563; the PR's own cache-check fix treats empty-rev as a real input). Admin opens Step-3: enrichAgenda downloads+extracts, revStable=false, verdict known_stale; the extract-agenda route's merge (route.ts:171) deletes link.extracted and discards the new extraction, so the card shows "No schedule detected" and the published crew page is embed-only forever, while every subsequent sync re-downloads (up to 25MB) and re-extracts the same PDF with the same discarded result. Same known_stale-clear fires on a transient download 5xx/stall for such a file, where the inline comment's "proves stale-by-revision" justification (lines 243-251) does not hold because no revision was ever readable.

**U53. [P3] #181 — `lib/sync/enrichAgenda.ts:307`**
For a PDF whose Drive metadata omits headRevisionId (normalized to "" by toDriveFileMeta), enrichAgenda downloads and pdf.js-parses the file but then unconditionally discards the extraction — revStable requires a non-empty payload.sourceRevision which is never set when currentRev is empty — contradicting the inline contract at line 212 ("Missing revision → re-extract (deterministic)") and producing a known_stale verdict that makes the extract endpoint delete any stored extraction.
*Scenario:* A staged sheet links an application/pdf whose files.get response lacks headRevisionId (DriveClient.getFile maps it to "" at lib/sync/runScheduledCronSync.ts:1563). Every cron sync and every admin Step-3 extract POST re-downloads up to 25MB and re-parses the PDF, then throws the result away (cache at :213 can never hit, revStable at :307-312 is always false), so the schedule can never surface for that link; in the extract endpoint the resulting known_stale verdict additionally deletes any previously stored link.extracted via mergeReportIntoParseResult. Unbounded repeat work with a guaranteed-unreachable success path.

**U54. [P3] #64 — `lib/sync/runScheduledCronSync.ts:729`**
The live staged partition drops the freshly scanned source anchors — upsertLivePendingSync does not persist pending_syncs.source_anchors (the column the wizard partition writes and finalize reads) and applyStaged.ts:1288 calls applyStagedCore without sourceAnchors — so approving a staged sync applies new parse data against the pre-edit layout's stale A1 rects.
*Scenario:* A sheet edit inserts rows above the CREW grid and renames a crew member (dirty diff → staged via pending_syncs); prepareProcessOneFile computed correct new anchors but they are discarded at staging; the admin approves, coalesce(null, source_anchors) retains the old rects, and the crew page's "Show crew" In-sheet link now range-highlights rows that belong to a different block (e.g., HOTEL) until the next clean auto-applied sync. Successive dirty-staged edits keep the anchors drifting staler indefinitely.

**U55. [P3] #128 — `lib/sync/runScheduledCronSync.ts:2378`**
The 45s-per-attempt xlsx-export stall guard (up to ~180s with retries) added at the cron export call site is dead code there: the call sits under the pre-existing 30s withStepTimeout Promise.race (DRIVE_SYNC_STEP_TIMEOUT_MS, line 99), which always preempts the guard, and the orphaned promise now actively retries the export in the background after the step has already failed.
*Scenario:* During a cron sync, one sheet's Drive xlsx export stalls. withStepTimeout rejects at 30s (SYNC_STEP_TIMEOUT fetch_failure — the guard's advertised 'stalled attempt recovers on retry with a fresh budget' contract, documented for both export sites in lib/drive/fetch.ts:175-185, never engages because the outer race fires 15s before the guard's first abort). Meanwhile the abandoned fetchSheetMarkdownAndBytesAtRevision promise keeps running the retry ladder detached: up to 3 more 45s export fetch attempts plus an after-get files.get if one succeeds — ~180s of discarded Drive requests per stalled sheet consuming quota/sockets behind the rest of the same cron pass (pre-PR the abandoned promise idled passively on one socket and issued no further requests).

### 3.6 Onboarding wizard & admin routes (25)

**U56. [P2] #217 — `app/admin/show/staged/[stagedId]/page.tsx:191`**
The Part D legacy-anchor shim is not applied on the two staged read surfaces: stagedRowFromLiveFirstSeen (this file, line 191) and the wizard failed-reapply page (app/admin/onboarding/staged/[wizardSessionId]/[driveFileId]/page.tsx:235) call operatorActionableWarnings directly on persisted parse_result.warnings instead of selectActionableForDisplay, contradicting the dataGaps.ts:199-200 claim that "Both persisted-read call sites use this one function".
*Scenario:* A pending_syncs first-seen row (or wizard failed-reapply row) whose parse_result was persisted BEFORE this deploy carries UNKNOWN_FIELD warnings with legacy block-RANGE anchors sharing one a1. On /admin/show/staged/[stagedId], operatorActionableWarnings dedups the distinct rows down to one (under-count) and StagedReviewCard renders an 'Open in Sheet' link to the block header — exactly the reported bug the PR set out to fix, persisting on the staged review surfaces until the sheet changes and is re-scanned (never, for an unchanged sheet).

**U57. [P2] sweep — `app/admin/show/staged/[stagedId]/page.tsx:191`**
[anchors-chain] Two persisted-read surfaces bypass the #217 legacy shim: app/admin/show/staged/[stagedId]/page.tsx:191 and app/admin/onboarding/staged/[wizardSessionId]/[driveFileId]/page.tsx:234 call operatorActionableWarnings directly on persisted pending_syncs.parse_result warnings, skipping stripLegacyUnknownFieldAnchors — contradicting selectActionableForDisplay's 'both persisted-read call sites use this one function' contract (lib/parser/dataGaps.ts:199-206).
*Scenario:* A live first-seen pending_syncs row staged before #217 deployed sits awaiting admin review; its UNKNOWN_FIELD warnings carry legacy block-RANGE sourceCell anchors (a1 contains ':'). On /admin/show/staged/[stagedId], operatorActionableWarnings dedups them by (code,gid,a1), collapsing N distinct unknown-field rows into 1 and rendering a deep link to the block header instead of any row — the exact undercount+wrong-link behavior the #217 shim was shipped to neutralize on the per-show panel.

**U58. [P2] #102 — `app/api/admin/onboarding/finalize-cas/route.ts:827`**
The post-commit revalidateShow loop over affectedShowIds runs only when deps.withTx resolves; an unexpected throw mid-batch skips it, leaving already-committed per-row shadow applies serving stale crew data.
*Scenario:* runFinalizeCas applies shadow rows to existing live shows via independent per-row withRowTx commits (the R1 comment at lines 822-826 states a mixed batch durably applies early rows). If a later row's shadow-payload parse or DB work throws (the catch at line 833 exists for exactly this), the handler returns ONBOARDING_FINALIZE_INTERNAL_ERROR without executing the revalidate loop — the early-committed shows' show-${id} tags are never busted, so crew pages serve the pre-apply fan-out for up to 300s (and open pages that broadcast-refresh re-read the stale-but-fresh-tagged entry with a matching token, so they stay stale until the next write). The streaming sibling (line 888 inside try, catch at 897) has the identical gap; the loop belongs in a finally.

**U59. [P2] #210 — `app/api/admin/onboarding/finalize-cas/route.ts:897`**
handleOnboardingFinalizeCasStream's catch emits the typed internal-error terminal but drops the log.error that the non-streaming sibling performs, so unexpected finalize-cas failures on the default production path are silently unlogged.
*Scenario:* The shipped FinalizeButton always sends Accept: application/x-ndjson, so every real final-CAS runs through the streaming handler. If runFinalizeCas throws (shadow-payload parse fault, DB error) or subscribeToWatchedFolder throws post-commit, the bare 'catch { emit(...) }' at :897-898 swallows the error object entirely — no server-side diagnostics exist for the failure, unlike the non-streaming handler (log.error at :837, added specifically as the M12 'never leak an empty 500' structural defense) and the scan stream it claims to mirror (scan/route.ts:267-271, which logs before emitting). Doug sees generic error copy and the developer has nothing to debug from.

**U60. [P2] #103 — `app/api/admin/onboarding/finalize/route.ts:908`**
A checked (publish-intent) sheet whose show already exists as a Held show is never published at finish: the existing-show branch stamps publish_intent=true but the finalize-cas publish flip only flips rows with created_show_id IS NOT NULL and wizard_created_session_id = current session, and the shadow apply deliberately preserves published=false, so the operator's explicit 'Check to publish' intent is silently dropped.
*Scenario:* Run setup, leave sheet X unchecked → Held show created (published=false). Re-run setup (the flow the wizard itself instructs for conflict recovery): X now hits showExists=true, renders a normal Step-3 card with the 'Publish this show' checkbox. Doug checks it and clicks 'Publish N shows & finish setup' (N includes X); finalize routes X through the existing-show-checked branch (shadow + stampManifestPublishIntent true), finalize-cas applies content but excludes X from publishAppliedWizardShows (created_show_id NULL, session mismatch — finalize-cas/route.ts:501,527) and 'the payload never carries published' (finalize-cas/route.ts:488). The wizard reports success but X stays Held/invisible to crew; Doug believes it went live. Spec §7.4 only covers existing-LIVE ('stays published') and existing-unchecked (D10); the existing-HELD-checked cell was never specified, and it is not in DEFERRED.md/BACKLOG.md.

**U61. [P2] #112 — `app/api/admin/onboarding/manifest/[wizardSessionId]/[driveFileId]/ignore/route.ts:77`**
In-wizard "Permanently ignore" for live_row_conflict writes a LIVE-partition permanent_ignore for a drive_file_id the live sync pipeline owns, silently killing all future automatic syncs of that sheet and stranding its still-pending live pending_syncs inbox row — contradicting the spec's "the live show is untouched" contract for this exit.
*Scenario:* live_row_conflict by definition means a live pending_syncs row exists for the same drive_file_id (LIVE_ROW_CONFLICT copy: "already being processed by the live folder sync"; the wizard scan's 23505/42P10 collision). Doug clicks the new Ignore button: the route writes deferred_ingestions (wizard_session_id IS NULL, permanent_ignore) and deliberately never touches the live pending_syncs row, so the dashboard inbox still offers Apply — and whether the sheet is already a live show or Doug applies that pending row, every subsequent cron/push pass hits perFileProcessor's permanent_ignore gate (lib/sync/perFileProcessor.ts:176-178, invoked for every folder file via runScheduledCronSync.ts:2295) and skips the file before even reading the shows row. The live show's crew page then goes permanently stale with no warning; recovery requires discovering /admin/ignored-sheets unignore or a manual per-show sync.

**U62. [P2] sweep — `app/api/admin/onboarding/scan/route.ts:167`**
[onboarding-state] reserveWizardSession reuses the existing pending_wizard_session_id on every re-scan, so an abandoned in-flight scan of a DIFFERENT folder is never superseded — its per-file staging guards (pending_wizard_session_id = $wsid) still pass and it interleaves the old folder's rows into the new scan's manifest.
*Scenario:* Operator scans folder A (15-25s for ~19 sheets), realizes it is the wrong folder mid-scan, and clicks Back or the step-1 pill — Step2Verify unmounts but neither the fetch nor the server scan is aborted (the route deliberately runs the scan to completion). They return to Step 2, paste folder B, and scan: reserveWizardSession keeps the SAME wizardSessionId (line 167, mints only when null), deletes the session's rows, and sets pending_folder_id=B; scan A's still-running staging loop (runOnboardingScan upsertManifest/upsertLivePendingSync guards check only pending_wizard_session_id, which never changed) then re-inserts folder-A rows alongside folder-B rows. Step 3 lists sheets from both folders; checking a folder-A sheet and clicking Publish demotes it with STAGED_PARSE_SOURCE_OUT_OF_SCOPE (metadata.parents no longer includes pending_folder_id), blocking finish until each phantom row is manually resolved or the operator uses Start Over (which DOES rotate the id — the rotation-only supersession detection is exactly the hole). Same interleaving occurs with two admin tabs scanning different folders concurrently.

**U63. [P2] sweep — `app/api/admin/pending-ingestions/[id]/retry/route.ts:147`**
[anchors-chain] The pending-ingestion retry first-seen path materializes shows with source_anchors={} and unanchored warnings: defaultPrepareFirstSeenStage fetches markdown only (no bytes, no gid lookup, no extractSourceAnchors/attachWarningAnchors), and runManualStageForFirstSeen's phase2 call (lib/sync/runManualStageForFirstSeen.ts:96-109) omits sourceAnchors, hitting the first-seen INSERT's `args.sourceAnchors ?? {}` arm (lib/sync/runScheduledCronSync.ts:1113).
*Scenario:* A first-seen sheet hard-fails ingestion, admin clicks Retry on /admin/pending-ingestions, and the auto-publish branch applies. The created show has empty source_anchors: every crew section's 'In sheet' link falls back to #gid=0 (INFO) — schedule cards that should deep-link to AGENDA and pack-list cards that should hit PULL SHEET/GEAR all open the wrong tab — and per-warning deep links are absent, until a later sheet edit happens to auto-apply through the cron pipeline (a reviewed edit instead hits the staged-apply drop and stays {}).

**U64. [P2] #87 — `app/api/admin/staged/[fileId]/apply/route.ts:139`**
PR #87's route-topology audit missed the legacy staged-apply route: it calls applyStaged with sourceScope "wizard" but has no catch for WizardSessionSupersededRollbackError (and no catch-all around the call), so the new in-apply-supersession throw escapes the handler as a generic body-less 500 with no WIZARD_SESSION_SUPERSEDED_RACE alert, where pre-#87 the same race returned a typed 409 WIZARD_SESSION_SUPERSEDED.
*Scenario:* Admin API client POSTs {source_scope:"wizard", wizard_session_id, staged_id} to /api/admin/staged/[fileId]/apply (a supported contract — the route validates and maps wizard_applied, pinned by tests/api/admin-staged-apply-route.test.ts:84) while the wizard session flips mid-apply; applyStaged hits any of the three throw sites (applyStaged.ts:1133/1163/1654) and the uncaught error yields a raw 500 instead of the cataloged 409, and the race alert is never written. The tx still rolls back (integrity holds), but the response contract violates the project's explicit never-leak-a-body-less-500 rule that the sibling wizard route enforces, and BACKLOG.md:381 now falsely documents that these throws are reachable only via the onboarding wizard apply route, which will mislead future audits.

**U65. [P2] #217 — `components/admin/PerShowActionableWarnings.tsx:50`**
labelFromRawSnippet is applied unconditionally to EVERY rendered warning (also Step3SheetCard.tsx:856), but the '<label> | <value>' rawSnippet contract only holds for UNKNOWN_FIELD; other codes' rawSnippets containing " | " get a garbled or misleading fragment rendered as the muted 'row label' line.
*Scenario:* A show with a <5-column pull-sheet row emits PULL_SHEET_AMBIGUOUS_FORMAT (warn, in OPERATOR_ACTIONABLE_ANCHORED) with rawSnippet set to the raw markdown row (lib/parser/pull-sheet.ts:257, e.g. '| TRUE | 2 | Mixer |') → the admin Data-quality panel and staged card render '| TRUE' as the row label; COLUMN_HEADER_AUTOCORRECTED (rawSnippet = crew header line, lib/parser/blocks/crew.ts:129) renders '| Name'; on the Step-3 breakdown (which renders ALL warnings) DAY_RESTRICTION_DOUBLE_LOCATION (lib/parser/personalization.ts:106, 'name: X | role: Y') renders 'name: X' as if it were a sheet row label.

**U66. [P2] #180 — `components/admin/wizard/Step3Review.tsx:650`**
selectableRows excludes rows with ANY non-null lastFinalizeFailureCode, but Step3SheetCard suppresses the checkbox only for RESCAN_REVIEW_REQUIRED, so finalize-demoted rows (DRIVE_FETCH_FAILED, STAGED_REVIEW_ITEMS_CORRUPT, etc.) render an enabled checkbox that Select-all silently skips and the 'N of M' count omits.
*Scenario:* A finalize run demotes one sheet with DRIVE_FETCH_FAILED (demotePending: wizard_approved=false, manifest back to 'staged', code set). On Step 3 the card shows a normal enabled checkbox (isDirtyRescan is false, Step3SheetCard.tsx:1471), and per the approve route's own contract 'EVERY other demotion code keeps the one-click checkbox recovery' — but this filter drops the row from selectableRows, so clicking 'Select all' never POSTs for it, the header count excludes it, and finishable stays false (OnboardingWizard.tsx:368) while the resolution status line still reads 'You can finish setup whenever you are ready' (blockingCount only counts hard-fail statuses). Doug gets a disabled Finish with no indication which visible checkbox he must click individually.

**U67. [P2] #204 — `components/admin/wizard/Step3ReviewWithFinalize.tsx:58`**
The finalize soft-confirm gate and the "Publish N shows" promise now consume optimistic checkbox counts, so Finish can fire with no warning while approve POSTs are still in flight, and finalize silently Holds rows whose approval hasn't committed server-side.
*Scenario:* Operator checks all staged rows (e.g. Select-all: overlay flips instantly, /approve POSTs still in flight via Step3Review's flush()) and immediately clicks the button now reading "Publish 2 shows & finish setup". FinalizeButton.onPrimaryClick sees optimistic uncheckedCleanCount=0 (FinalizeButton.tsx:399) so it skips the soft confirm and POSTs /finalize at once; finalize's per-show locked re-read (the BL-FINALIZE-APPROVAL-DECISION-RACE fix) only honors approvals already COMMITTED, so any row whose approve POST lands after finalize takes its show lock is silently created as Held — the operator was promised "Publish 2 shows", got no confirm, and one show quietly lands under Unpublished. Pre-#204 this window was incidentally guarded: the server-derived count kept the confirm firing and the label reading "Publish 0 shows" (a visible lag signal) until server truth caught up; Step3Review already tracks in-flight writes (sendingDfids, Step3Review.tsx:697) but never exposes them, so the wrapper cannot disable Finish during convergence — the exact client defense-in-depth BACKLOG item 3 declined as unnecessary before this PR made the gate optimistic.

**U68. [P2] #103 — `lib/admin/loadHeldShows.ts:188`**
loadHeldShows hardcodes crewCount: null, and ShowsTable renders `${row.crewCount ?? 0} crew` (ShowsTable.tsx:442), so /admin/unpublished displays a false '0 crew' for every Held show even though finalize's full first-seen apply creates real crew_members rows for them.
*Scenario:* Leave a cleanly-parsed sheet with 12 crew unchecked during setup → finalize creates the Held show with all 12 crew_members rows. Open /admin/unpublished: the Crew column reads '0 crew' for that show (and every Held show), and sorting by Crew is meaningless — Doug can reasonably conclude the parse dropped the whole roster before publishing. The code comment claims 'Crew count is not surfaced on this view', but ShowsTable unconditionally renders the Crew column, so a fabricated 0 is surfaced instead of the real count or a neutral placeholder. Not documented in DEFERRED.md/BACKLOG.md.

**U69. [P2] #103 — `lib/admin/loadHeldShows.ts:188`**
The /admin/unpublished Held-shows loader hardcodes crewCount: null, which ShowsTable renders as a fabricated "0 crew" label (and sorts as 0) for every Held show regardless of its actual roster.
*Scenario:* Doug finishes onboarding leaving a cleanly-parsed sheet with a 15-person crew unchecked; it lands on /admin/unpublished, where ShowsTable.tsx:442 renders `${row.crewCount ?? 0} crew` -> the row shows "0 crew" on both the mobile meta line and the desktop Crew column. At the exact publish-decision moment the UI falsely implies the parse found no crew (every other ShowsTable caller, e.g. Dashboard.tsx:370, supplies real per-show counts), so the operator may re-scan or distrust a perfectly good show. Not documented in DEFERRED.md or BACKLOG.md.

**U70. [P2] #180 — `lib/onboarding/rescanWizardSheet.ts:267`**
The manifest-membership guard admits rows already CONSUMED by a finalize batch (manifest 'applied' with created_show_id, no pending_syncs row, no shadow), and re-scanning such a first-seen row re-enters it unapproved so the next Publish hits the D10 no-op, discarding the re-scanned content and overwriting publish_intent=false / created_show_id=null.
*Scenario:* Doug checks a first-seen sheet and clicks Publish; the batch creates the Held show, stamps publish_intent=true and deletes the pending row, so on refresh the Step-3 card renders as a no-details card whose primary CTA is the new 'Re-scan this sheet' button (Step3SheetCard.tsx:1502-1509). Doug clicks it before final CAS: capturePriorState finds neither pending row nor shadow (first-seen rows never get shadows) -> priorReady=false -> clean path leaves the fresh row unapproved and reopens the checkpoint. The next Publish sees showExists + wizard_approved=false -> the D10 no-op (finalize/route.ts:916-931) sets publish_intent=false and created_show_id=null and consumes the row, so the refreshed parse never reaches the created show, the show is severed from its manifest provenance, and Doug's originally-checked show silently stays Held (the CAS flip only publishes publish_intent=true rows with created_show_id).

**U71. [P2] sweep — `lib/onboarding/sessionLifecycle.ts:197`**
[advisory-locks] cleanupAbandonedFinalize takes FOR UPDATE row locks (onboarding_scan_manifest + shows_pending_changes in lockCleanupDriveFiles, lines 172-190) BEFORE acquiring the per-show advisory locks at line 197, inverting the advisory-before-row order every other surface uses — an AB-BA deadlock with any show-lock-first holder that touches the same rows.
*Scenario:* Session S is pending but >24h stale (reserveWizardSession never refreshes pending_wizard_session_at, so a re-scanned old session stays cleanup-eligible). Admin 1 POSTs /api/admin/onboarding/cleanup-abandoned-finalize/S: it acquires finalize:S, then FOR UPDATE-locks S's applied manifest row for sheet X, then blocks acquiring show:X. Concurrently Admin 2's scan re-stage (lib/sync/runOnboardingScan.ts per-file tx) or the manifest-ignore route (app/api/admin/onboarding/manifest/.../ignore/route.ts:200 — its readLockedManifestRow FOR UPDATE fires before the status gate) already holds show:X via withPostgresSyncPipelineLock and blocks on the manifest row lock → AB-BA; Postgres's deadlock detector aborts one tx with 40P01 after ~1s, surfacing a 500 on cleanup or the scan/ignore action. This is exactly the R15/PF11 class the reap (line 524-533) and the RPC lock-order test pin — but cleanup's JS body predates that rule and is unpinned.

**U72. [P3] #65 — `app/admin/settings/_actions/validationReset.ts:86`**
Both destructive admin actions swallow all diagnostic detail — four bare `catch {}` blocks plus discarded RPC `error.message` mean a failed reset/reseed leaves zero server-side record, while the VALIDATION_RESET_FAILED catalog copy explicitly tells Doug 'the developer needs to investigate the underlying database error' that the code never captures (unlike sibling actions, whose infra throws propagate to the cataloged 500 boundary, and unlike the logAdminOutcome/app_events trail every other admin mutation now gets).
*Scenario:* reset_validation_data() fails with any unexpected DB error (e.g., a schema change breaking the RPC on the validation project) -> action returns { ok:false, code:'VALIDATION_RESET_FAILED' }, the underlying message is discarded, nothing is logged -> developer must reproduce blind. This exact blindness already caused the documented two-hotfix misdiagnosis chain: the swallowed PostgREST '21000 DELETE requires a WHERE clause' error was misread as an 8s statement_timeout (see migration 20260622000003 header, 'the earlier 8s-statement_timeout diagnosis was a red herring').

**U73. [P3] #67 — `app/admin/settings/_actions/validationReset.ts:40`**
The resetValidationDataAction section-header contract still reads "Calls the reset_validation_data() RPC (session client so RLS + is_admin() apply at the DB layer too)" — both claims are false after PR 67, which moved the wipe to the service-role client and dropped is_admin() from the RPC entirely.
*Scenario:* A maintainer auditing or refactoring the security posture trusts the header and believes DB-layer is_admin() still guards the wipe; re-granting EXECUTE to authenticated (e.g., to "simplify" back to one client, as the comment describes) would expose the wipe to ANY authenticated non-admin user via PostgREST on the validation project, where destructive_reset_gate.enabled=true so the only remaining in-function fence passes. The misleading contract survives because PR 67 updated the inline comments but not the section header directly above the function it changed.

**U74. [P3] #56 — `app/admin/settings/admins/RevokeRowButton.tsx:131`**
PR #56's remap of every last_admin_lockout producer to self_revoke_forbidden left RevokeRowButton's entire LAST_ADMIN_LOCKOUT_REFUSED rendering path (lockoutMessage + two duplicated render blocks at lines 225-237 and 299-311) as unreachable zombie code, with stale docstrings (RevokeRowButton.tsx:22-28, actions.ts:9-11) still claiming that contract is live.
*Scenario:* revokeAdminAction (app/admin/settings/admins/actions.ts:174-176 and 199-203) converts both its own self-revoke guard and the data-layer last_admin_lockout outcome to { kind: 'self_revoke_forbidden' }, and the only other producer (addAdminAction:139-142) is unreachable from the upsert RPC — so result.kind === 'last_admin_lockout' can never be true in RevokeRowButton in any deploy window, including the documented transitional one. A maintainer reading the component docstring ('a forged submit ... surfaces LAST_ADMIN_LOCKOUT_REFUSED inline') or the actions.ts header ('refused inside the RPC (LAST_ADMIN_LOCKOUT_REFUSED catalog code)') will edit or rely on the dead lockout branch/copy believing it renders, while the actual refusal renders SELF_REVOKE_FORBIDDEN — a misleading contract plus two already-duplicated dead render blocks.

**U75. [P3] #154 — `app/admin/show/staged/[stagedId]/page.tsx:167`**
The first-seen staged card double-renders FIELD_UNREADABLE: warningSummaryFor still includes all three DQ codes' .message text while PR 154 added the operatorActionable card list to the same card — the exact double-render the PR's impeccable gate flagged and fixed on the per-show page only (no class-sweep to the sibling surface).
*Scenario:* A cron first-seen sheet with one unreadable crew phone lands on /admin/show/staged/[stagedId]: the identical sentence ('Crew phone for row 2 couldn't be read as a phone number ("...") — check the sheet.') renders twice on one card — once in the staged-warning-summary paragraph (line 167 filter includes FIELD_UNREADABLE) and again as the PerShowActionableWarnings item title (catalog title is null so it falls back to the same .message), plus a third representation in the dataGaps count chip. The per-show page excludes OPERATOR_ACTIONABLE_ANCHORED codes from its digest for precisely this reason; the staged derivation (line 191) applies no such exclusion.

**U76. [P3] #115 — `app/api/admin/onboarding/scan/route.ts:267`**
The stream's catch-all around runOnboardingScan discards the caught error object entirely (no catch binding), so the cause of any mid-run scan failure never reaches logs or Sentry — diverging from the finalize route's established convention of passing `error` into log.error meta.
*Scenario:* A scan fails mid-run (Drive 429/500 during prepare, Postgres connection failure during staging, or a parser TypeError): the client is shown only the generic '{ ok:false, code:null }' copy, and the server records just 'onboarding scan failed' + requestId with no message or stack (catch has no error binding, unlike app/api/admin/onboarding/finalize/route.ts:1260-1263 which passes `error` via lib/log's serializeError path). Pre-#115 the throw propagated and Next logged the unhandled route error; now repeated production scan failures are undiagnosable without reproducing locally.

**U77. [P3] #38 — `components/admin/FinalizeButton.tsx:457`**
The PR's catalog-emphasis wiring is inconsistent inside the three finalize buttons: `state.copy` is wrapped with renderEmphasis but the per-row failure copy (`lookupDougFacing(failure.code)` at FinalizeButton.tsx:457 and `lookupDougFacing(row.code)` at :489, plus ResumeFinalizeButton.tsx:139 and RunFinalCASButton.tsx:123) still renders raw catalog dougFacing, while the PR's fail-closed guard (tests/messages/_metaEmphasisRenderContract.test.ts) only sniffs for an import at FILE granularity — so these files are certified emphasis-aware despite containing unwrapped catalog-copy render sites.
*Scenario:* Any of the per-row failure codes (DRIVE_FETCH_FAILED, STAGED_PARSE_OUTDATED_AT_PHASE_D, ONBOARDING_LEGACY_ROW_AMBIGUOUS, ...) gets its dougFacing edited into the catalog's dominant house style — 30+ sibling codes wrap the sheet name as `_<sheet-name>_` — and Doug then sees literal underscores/asterisks (and an unresolved `<sheet-name>` token, since these call sites pass no params) in the wizard finalize failure list, with the meta-test, x1 parity, and the whole suite staying green. The contract the PR shipped ('every catalog-copy render site is emphasis-aware, pinned fail-closed') is misleading for exactly the surfaces the PR itself half-wired.

**U78. [P3] #210 — `components/admin/FinalizeButton.tsx:230`**
The PR added two verbatim in-file copies of the ~35-line NDJSON pump loop (readFinalizeBatch :160, readFinalizeCas :230) alongside Step2Verify's pre-existing third copy, instead of extracting a shared reader next to the shared wire-contract module — and the copies have already diverged on error handling.
*Scenario:* Three implementations of the same NDJSON line-pump protocol now exist with divergent failure semantics: Step2Verify.tsx:243-268 runs inside a try/catch and recovers from read() rejections; the two new FinalizeButton copies do not (producing the P1 frozen-UI bug) and add tail-flush + reader.cancel() that Step2Verify lacks. Any future protocol fix (e.g., decoder final-flush, abort support) must be applied in three places and will predictably miss one — the PR created lib/onboarding/finalizeProgress.ts precisely to prevent server/client drift but left the consumer logic triplicated.

**U79. [P3] #65 — `lib/admin/validationDeployment.ts:12`**
isValidationDeployment() is a zombie export retaining PR 65's NEXT_PUBLIC_SUPABASE_URL fallback (the design that caused the original wipe-guard/wipe-target mismatch), with zero production callers, while the adjacent FIX 3 comment falsely claims 'it's used by other callers that legitimately want the fallback'.
*Scenario:* A future contributor needing an 'is this the validation deployment?' check grabs the obvious-named exported helper (the false comment affirms it is the sanctioned fallback-tolerant variant) and gates a destructive or env-sensitive operation with it -> on a host where SUPABASE_URL is unset but NEXT_PUBLIC_SUPABASE_URL points at validation, the gate passes while server-side service-role clients (which read SUPABASE_URL) target a different database — reintroducing the exact mismatch destructiveResetAllowed() was later patched (FIX 3) to avoid. Verified: rg shows no callers outside this file, its unit test, and docs.

**U80. [P3] #68 — `lib/admin/validationDeployment.ts:22`**
PR 68 left isValidationDeployment() as a zombie export justified by a false comment — "it's used by other callers that legitimately want the fallback" — when it had (and still has) zero production callers, preserving a weaker NEXT_PUBLIC_SUPABASE_URL-fallback guard that invites exactly the guard-vs-target mismatch FIX 3 closed.
*Scenario:* A developer adding a new validation-only destructive surface sees two exported guards and, trusting the comment that other callers sanction the fallback semantics, gates a service-role write with isValidationDeployment(); on a deployment where only NEXT_PUBLIC_SUPABASE_URL is the validation ref and SUPABASE_URL is unset, the guard passes while createSupabaseServiceRoleClient() (lib/supabase/server.ts:80) targets http://127.0.0.1:54321 — the wrong-target class PR 68's own FIX 3 was written to eliminate. Grep confirms only lib/admin/validationDeployment.ts itself, tests/admin/validationDeployment.test.ts, and docs reference the function on current main.

### 3.7 Crew page & viewer (10)

**U81. [P2] #169 — `components/crew/sections/ScheduleSection.tsx:288`**
Fragment-day DayCard meta gate checks raw sd.entries.length === 0, so a synthetic strike/load-out entry appended to a showStart-only day silently suppresses the day's call-time meta.
*Scenario:* A show day's DATES TIME cell contains only a clock (e.g. "8:00 AM"), parsing to {entries: [], showStart: "8:00 AM"}; a room strike_time dated that same day (the surveyed-corpus norm — 7/7 shows carry strikes, usually on the last show day) makes deriveScheduleBookends append a kind:"strike" entry to that day. entries.length becomes 1, the fragment branch is skipped, and the crew Schedule DayCard drops its "8:00 AM" meta entirely — the day renders only the "Strike — …" row, hiding the show-start time from the left column (it survives only in the right-column key-times strip). Pre-PR the meta rendered; the gate should count non-synthetic entries, as the sibling SET-day meta (line 284, dayEntries) and resolveKeyTimes (line 141) already do.

**U82. [P2] #48 — `components/crew/sections/TravelSection.tsx:271`**
The Travel vehicle-row cascade (`vehicleFields[0]` / `slice(1,-1)` / last-element-as-conf) mislabels promoted fields under the bold "Vehicle" eyebrow and mis-slots the last surviving field into the unlabeled faint confirmation line whenever any of the four fields is absent.
*Scenario:* With transportation.vehicle = "TBD" but parking = "Lot 4 — North Garage, permit on dash" (a realistic sheet state: van unassigned, parking known), the Getting-there card renders eyebrow "VEHICLE" with the parking instructions as the bold 15px primary — parking data labeled as the vehicle. Separately, with vehicle="Sprinter" and license_plate="ABC-123" only, slice(1,-1) is empty so the plate lands in the 11.5px faint tabular-nums conf slot instead of the meta line the code contract documents ("license plate + color fall to meta; parking is the conf line") — the conf slot is positional, not semantic.

**U83. [P2] sweep — `lib/crew/flightDisplay.ts:121`**
[flight-display] Dep/arr times written with a space before the meridiem ('7:23 am') can never be parsed — TIME_RE is tested per whitespace-split token so its \s*(am|pm) tail is dead code — and because the segment is still 'structured', the raw line containing the times is not rendered either, silently dropping the flight times.
*Scenario:* crew_members.flight_info = '3/22 AA3002 LGA - ORD 7:23 AM - 9:15 AM' tokenizes to [...,'7:23','AM','-','9:15','AM']; no token matches TIME_RE (^\d{1,2}:\d{2}\s*(am|pm)$), so depTime/arrTime stay null while date/flightNo/route parse. TravelSection's showStructured branch (TravelSection.tsx ~572-627) then renders date + 'AA3002 · LGA → ORD' with NO time line and never falls back to seg.raw (hasContent is true because carrier/route parsed) — the crew member sees a complete-looking card missing their departure time, violating the component's own 'operator's text is never dropped' contract.

**U84. [P2] sweep — `lib/crew/showDayTimeline.ts:46`**
[timeline] Past-midnight entries (12:xx AM) in a show day's run-of-show sort to the TOP of the unified Today timeline (clockToMinutes maps 12:30 AM to minute 30), placing an end-of-night closer/strike above the morning registration, and inconsistently with the Schedule page which keeps it last in sheet order.
*Scenario:* A show-day cell ending '...8:00pm - Awards Dinner 12:30am - Concludes' (or a rooms-grid strike '10/9 @ 12:00 AM') produces an entry with minutes 30 (or 0). On a day where the agenda merge is active, buildShowDayTimeline's ascending minute sort renders 'Concludes 12:30am' (or the synthesized 'Strike — all rooms 12:00 AM', which ShowDayTimelineList deliberately keeps 'in its chronological position') as the FIRST row of the day, above '7:15am Registration' — while ScheduleSection's RunOfShowList shows the same entry last, so the two surfaces disagree and the Today ordering contradicts operator intent.

**U85. [P2] #102 — `lib/data/getShowForViewer.ts:803`**
getShowForViewer now reads the live viewer_version_token strictly AFTER the cached data fan-out, so a write committing during the render window produces 'stale data + fresh token', defeating the realtime bridge's token-compare catch-up and leaving the page stuck stale.
*Scenario:* Crew member opens their show page while a cron sync applies a sheet change: SSR reads the cached (pre-write) fan-out, the write commits + busts the tag + broadcasts (missed — the client hasn't subscribed yet), then readViewerVersionToken returns the POST-write token. The page renders old data with the new token; the bridge's post-subscribe catch-up (ShowRealtimeBridge.tsx:319) fetches the live token, finds it equal to the rendered one, and skips router.refresh — the change stays invisible until another write or a manual reload. Pre-PR the token was read in the same Promise.all as the live data (single RPC, typically resolving first), so the safe token-not-newer-than-data ordering generally held; reading the token FIRST would close the window entirely.

**U86. [P3] #70 — `components/crew/RightNowHero.tsx:161`**
The show_day_n 'legacy single-anchor' fallback `(ctx.showAnchors.length === 0 ? ctx.callTime : null)` is dead code: the same PR redefined callTime as `showAnchors[0]?.time ?? null` (buildRightNowContext.ts:90), so callTime is definitionally null whenever showAnchors is empty, yet a 15-line comment and a dedicated test pin the fallback as live behavior.
*Scenario:* tests/components/crew/rightNowHero.test.tsx:434 ('legacy fallback preserved') passes only by fabricating a context ({showAnchors: [], callTime: '7:00 PM'}) that the sole production builder can never produce; in production every showAnchors-empty show_day_n render yields showTime null (Show stat omitted). A maintainer relying on the documented/tested contract — e.g. re-adding a room-sourced single anchor path — will wire it through ctx.callTime and it will never render.

**U87. [P3] #48 — `components/crew/sections/TodaySection.tsx:646`**
Today Mode A renders the 1.6fr/1fr split-wide grid unconditionally with `{quickCardsStack}` in the right track even when quickCardsStack is null, leaving a dead blank ~38% right column at >=720px — the one-sided-collapse guard the same PR added to Crew (bothColumns), Travel (useSplit), Schedule (rightHasContent) and Venue (useSplit) was never applied to Mode A, and later PRs added it only to Mode B (hasLeft/hasRight).
*Scenario:* A show-day viewer on a show with displayable runOfShow entries but no hotel reservations, a null show.venue, and no selectable primary contact (firstHotel, venue, primaryContact all falsy) gets modeA=true with quickCardsStack=null; on desktop the Run of show card is squeezed into the left 1.6fr track while the 1fr right track renders an empty <div class="min-w-0"> — a permanently blank column, contradicting the PR's own 'no blank track' contract applied to every sibling section.

**U88. [P3] #49 — `components/crew/sections/TodaySection.tsx:380`**
The Tonight card's status-positive "Booked" pill is unconditional hardcoded chrome — it asserts a booking-confirmed status for any hotelReservations[0] row without reading any data field (the nullable confirmation_no in the projection is ignored entirely).
*Scenario:* A show sheet lists a hotel row that is tentative/pending (confirmation_no empty, or notes say 'HOLD — awaiting confirmation'); the crew page still renders the authoritative teal 'Booked' pill on the Tonight card, telling crew their room is confirmed when the sheet never said so. The pill's truth condition is 'a hotel row exists', so it can never reflect an unbooked/pending state — the exact zombie-chrome class the repo's own flag-lifecycle discipline targets (storage/read/effect: none).

**U89. [P3] #51 — `components/crew/sections/TodaySection.tsx:495`**
keyTimesCard hardcodes KeyTimesStrip layout="row" but is rendered in two postures — Mode B's bounded 1.6fr column (intended) and the full-page-width no-quick-cards fallback (line 667), which reproduces exactly the "anchors spread 50/50 across the full page with a mid-page divider" layout the PR's own review classified as a defect and claimed to have fixed by reserving row for the bounded column.
*Scenario:* A show with Set/Strike key times but no hotel reservation, no venue, and no primary contact (plausible sparse early-onboarding sheet) makes hasRight false, so the final else branch renders keyTimesCard as a direct full-width child at >=720px: the row-mode strip stretches its flex-1 cells across the entire content column with mid-page hairline dividers — the code comment at line 604 states this posture is reserved for the bounded 1.6fr column, directly contradicting the render at line 667.

**U90. [P3] #102 — `lib/data/showCacheTag.ts:51`**
revalidateOnApplied is dead code (zero production callers) whose applied-only gate the same file documents as defective, yet the module header still advertises it as a write-site helper and the coverage meta-test's regex accepts it as valid revalidate proof.
*Scenario:* A future write site follows the header instruction at showCacheTag.ts:9 ('every Next-runtime write ... calls revalidateShow(showId) / revalidateOnApplied(result) POST-COMMIT') and uses revalidateOnApplied; tests/db/showCacheRevalidateCoverage.test.ts's REVALIDATE_CALL_RE matches 'revalidateOnApplied(' so the structural guard passes, but any non-'applied' outcome that still committed a projected write (the exact parse_error/source_gone class the file's own docblock at lines 57-63 says this gate MISSES) silently skips the cache bust — reintroducing the whole-diff-R2 staleness bug while CI stays green.

### 3.8 Agenda pipeline (3)

**U91. [P2] sweep — `lib/agenda/extractAgendaSchedule.ts:388`**
[agenda-pipeline] A day-opening session with a bare start and explicit-PM end ("7:00 – 8:30 p.m.") is resolved to a 7:00 AM start at high confidence: start-meridiem inference ignores the end token's explicit meridiem, and the §4.4 guard's hasExplicit test (line 510) treats the END's 'pm' as disambiguating the START.
*Scenario:* Agenda PDF opens with "Sunday, June 7, 2026 / 7:00 – 8:30 p.m. Welcome Reception" followed by full conference days (a standard format: meridiem only on the range's end token). parts=['7:00','8:30pm']; sp.ap=null and prevStart=null → seedAp(7)='AM' → startMin 420; end explicit PM (1230) > start so no §4.3.2 repair fires; the ambiguous-first guard is skipped because /AM|PM/i matches the whole raw token. Output "7:00 AM – 8:30 PM" passes monotonic+confidence gates, so the crew page and unified show-day timeline (clockToMinutes uses the start) place the evening reception at 7:00 AM.

**U92. [P2] sweep — `lib/agenda/extractAgendaSchedule.ts:600`**
[agenda-pipeline] The catch-all converts a transient infra throw (pdfjs/memory/worker fault) into a plain low-confidence extraction, which enrichAgenda then stamps with sourceRevision+EXTRACTOR_VERSION and caches as 'fresh' — permanently pinning "No schedule detected" for that PDF revision with no retry path.
*Scenario:* During wizard Step-3 extraction, pdfjs rejects once (e.g. memory pressure at 4 concurrent 25MB extractions, or an environment fault like #184's fake-worker failure) → LOW() payload gets sourceRevision stamped, passes the rev-stability fence, persists to pending_syncs. Every subsequent dialog-open/POST cache-hits at enrichAgenda.ts:213-217 (confidence is not part of the key and the comment's 'extraction is deterministic' rationale doesn't hold for infra throws) and serves the poisoned LOW without re-downloading — the operator's retries are no-ops, finalize publishes embed-only to crew, and only a PDF re-upload or the next sheet edit (cron fresh parse) can heal it. Precedent: exactly this mechanism made #184's failure sticky, requiring PR #190's version bump to purge v1 caches.

**U93. [P3] #181 — `lib/agenda/extractAgendaLease.ts:172`**
The in-memory slot store (per-instance concurrency cap + per-key inFlight dedupe) has no TTL/self-heal — its only release is the extract route's finally — while the sibling durable lease layer deliberately self-heals via a 330s TTL, so an invocation that dies before finally permanently poisons the warm instance's agenda capacity.
*Scenario:* The route's timeout path (route.ts:352-354) aborts and then awaits extraction settlement, but extractAgendaSchedule takes no abort signal and enrichAgenda checks the signal only between links (enrichAgenda.ts:134), so a slow pdf.js parse can push settlement past the 50s margin between AGENDA_EXTRACT_DEADLINE_MS (250s) and maxDuration (300s); when Vercel terminates the invocation before finally runs, the durable lease expires at 330s but defaultSlotStore's count/inFlight never restore on that warm instance. Every subsequent POST for that staged row served by the instance returns 202 in_progress forever (the admin card ends in error after its 330s poll budget on every retry), and four such events pin count >= AGENDA_MAX_CONCURRENT_EXTRACTIONS so the instance answers every agenda extraction "queued" until it recycles.

### 3.9 Tests, CI & scripts (10)

**U94. [P2] #115 — `app/globals.css:436`**
The :indeterminate shimmer for the scan progress bar is painted on the <progress> element's own background, where it is occluded by the opaque ::-webkit-progress-bar track in Chromium/WebKit, and in Firefox the full-width indeterminate ::-moz-progress-bar renders a misleading 100%-full accent bar instead.
*Scenario:* Operator submits a folder URL in Chrome: during the connecting and finishing (staging) phases the bar is indeterminate, but the authored shimmer (and the reduced-motion static hint at line 459) sits behind the opaque ::-webkit-progress-bar background (lines 424-426, --color-surface-raised is opaque #ffffff/#1c1d23), so the bar is a dead-still empty pill with no 'active' signal — the exact hung-vs-progressing ambiguity the PR set out to fix. In Firefox, an indeterminate <progress>'s ::-moz-progress-bar spans the whole track, so the accent override at lines 432-434 shows a completely FULL bar while merely connecting, which then snaps to near-empty once the 'listed' event arrives.

**U95. [P2] #178 — `scripts/backfill-validation-source-anchors.ts:70`**
Backfill script UPDATEs the `shows` table (a per-show advisory-lock-set table per AGENTS.md invariant 2) with zero lock holders — no pg_advisory_xact_lock and no transaction — unlike every sibling validation write path (mint_validation_fixture_atomic.sql:69, validation_reset_rpc.sql:76 both lock in-RPC).
*Scenario:* The backfill loop is slow (two Drive API calls per show). If `pnpm validation:reseed` or the admin validation-reset action runs concurrently — the PR body itself says to re-run the backfill around reseeds — the mint RPC can re-mint a show (resetting source_anchors={} and re-stamping the fixture) between the backfill's initial `select id, slug, drive_file_id` and its unlocked UPDATE; the backfill then writes anchors computed from the pre-reseed sheet state onto the post-reseed row with no error, leaving deep links pointing at stale/wrong gids — the exact bug class the PR was fixing. The advisory lock would serialize this against the mint/reset RPCs.

**U96. [P2] #51 — `tests/e2e/crew-layout-dimensions.spec.ts:398`**
The replacement regression gate asserts getComputedStyle(grid).alignItems === "start", but Tailwind's items-start utility compiles to align-items: flex-start, which computes to "flex-start" in both Chromium and WebKit — the assertion can never pass, so the gate false-fails on the correct implementation and the split-wide alignment contract (which the PR's own Codex review required protection for, after the equal-height assertions were deleted) has zero working enforcement.
*Scenario:* Run `pnpm exec playwright test --project=mobile-safari tests/e2e/crew-layout-dimensions.spec.ts` against a seeded server: the always-two-column Schedule test forces the assertSplitWide branch (expectTwoColumns=true) and fails with `got align-items=flex-start` despite min-[720px]:items-start being correctly applied; the same guaranteed failure exists at lines 570 (Today Mode A) and 688 (Mode B). Verified empirically with the repo's installed Playwright (chromium/webkit both return "flex-start" for align-items:flex-start) and the installed tailwindcss dist (items-start → [["align-items","flex-start"]]). Conversely, a regression back to items-stretch is caught by nothing, since no CI workflow runs this spec (crew-e2e.yml runs only crew-section-toggle.spec.ts) and inv2/inv7 in crew-page.spec.ts deliberately delegate the contract here.

**U97. [P2] #60 — `tests/e2e/picker-flow.spec.ts:68`**
PR body claims the three app bugs it discovered were 'filed as follow-ups in BACKLOG.md', but no BACKLOG/DEFERRED entries were ever added — the only record is // SKIP: comments in a spec file whose Playwright project is not run in any CI job, leaving live user-visible bugs untracked.
*Scenario:* The three bugs are all confirmed still present on main: (1) app/show/[slug]/[shareToken]/_PickerInterstitial.tsx:154 — the claimed-row control is <form action="/auth/sign-in?next=…" method="GET"> with no hidden inputs, and GET submission discards the action URL's query, so a claimed crew member tapping their locked row lands on bare /auth/sign-in and is never returned to the show; (2) Mode-B 'Continue as guest' is a dead-end loop — clearIdentityAndSkip (lib/auth/picker/clearIdentity.ts:59) redirects to ?gate=skip but page.tsx:193 honors gate=skip only for first_contact, so a signed-in non-roster session re-resolves to google_mismatch and the same gate re-renders forever; (3) picker-bootstrap route.ts:181/199 redirects via new URL(path, request.url) whose host canonicalization (127.0.0.1→localhost) drops host-scoped auth cookies. Because the promised BACKLOG entries don't exist and crew-e2e.yml runs only crew-section-toggle.spec.ts, nothing in the repo's tracking or CI surfaces these — they will only be rediscovered by end users.

**U98. [P3] #178 — `scripts/backfill-validation-source-anchors.ts:42`**
The write-safety guard claims to "refuse anything that isn't the remote validation pooler" but only checks `host.includes("pooler.supabase.com")`, which is true of EVERY hosted Supabase project's pooler (project identity lives in the connection username, not the hostname) — instead of reusing/porting the repo's purpose-built wrong-project binding guard (scripts/lib/validation-target.ts:83 assertSupabaseTargetMatchesProjectRef, added specifically to close this "F2 wrong-project" class and used by all four sibling validation scripts).
*Scenario:* Operator's .env.local TEST_DATABASE_URL gets pointed at a different Supabase project's pooler URL (e.g., a future production project or another project pasted while debugging — all pooler URLs share aws-N-<region>.pooler.supabase.com hostnames); the guard passes, and without --dry-run the script immediately overwrites shows.source_anchors across every show in the wrong project, with no project-ref check and no advisory lock against that project's live sync.

**U99. [P3] #57 — `scripts/with-admin-dev-flag.mjs:157`**
The acquireLockWithRetry default bump 240s→600s ('so three serial cold builds fit') is unreachable headroom: every dev-gate build runs inside a Playwright webServer with timeout 300_000, so Playwright kills the process before the raised lock wait can ever matter.
*Scenario:* On a slow CI runner (cold pnpm/Next caches), each serialized build takes ~100-150s; the :3003 prod-runtime-flip server must wait through two builds (~200-300s) plus its own build, exceeding the 300s webServer timeout at playwright.config.ts:306 → Playwright kills the command with 'Timed out waiting 300000ms' and the workflow fails — exactly the B1-D4 timeout class the PR claimed to fix. The window where the 240→600s bump could help (lock wait >240s) always implies total readiness >300s, so the fix is at the wrong layer and the comment's 'ample headroom' contract is false.

**U100. [P3] #67 — `tests/auth/advisoryLockRpcDeadlock.test.ts:159`**
PR 67 fixed the superseded-body drift for only one of three call sites: the PF11 lock-order scan list (and the lockTakingRpcNames list at line 38) still hardcodes migration 20260622000002 with a comment asserting it "is the LATEST definition", which is already false on current main where 20260622000003 is the shipped reset_validation_data body.
*Scenario:* A future migration that does `create or replace function public.reset_validation_data` adding a `SELECT ... FOR UPDATE` before the advisory-lock loop (the exact M5-R20 deadlock class the PF11 test exists to catch) passes CI: the PF11 scan audits the stale 20260622000002 body instead of the shipped one, so the invariant-2 structural guard silently validates a superseded artifact. This is the precise failure mode PR 67's own helper `latestResetValidationDataBody()` was written to prevent, but the helper was wired into only the body-inspection test at line 205 — the two hardcoded file lists diverged one PR later (20260622000003) and remain diverged today.

**U101. [P3] #72 — `tests/db/resetValidationDataPostgrest.test.ts:73`**
The PR's regression test flips destructive_reset_gate.enabled=true in beforeEach but never restores it (the only afterAll just closes the SQL pool), violating the explicit repo invariant 'never persist enabled=true past a test' (tests/db/destructiveResetGate.test.ts:68-69) that every sibling reset test honors via finally/afterEach restores.
*Scenario:* Run the unit suite (or this file alone) against the shared local Supabase DB: after the run, destructive_reset_gate stays permanently enabled=true, so the DB-side fence that is supposed to gate the wipe RPC is left open — any later reset_validation_data() invocation (admin button in local dev, another test file that assumes the documented enabled=false default, or a stray RPC call) succeeds and wipes all shows without the gate raising. It also creates order-dependent state: a future test asserting the gate's default-off behavior passes or fails depending on whether this file ran first in the serial phase.

**U102. [P3] #123 — `tests/parser/_metaKnownSectionsRegistry.test.ts:27`**
The 'registry drift guard' meta-test is a second hand-maintained copy of the header list, not a walker over the block parsers, so it cannot detect the drift it claims to pin — contradicting the documented contract at lib/parser/knownSections.ts:12-15 ('adding a future block parser without registering its header fails CI').
*Scenario:* A developer adds a new block parser (e.g. a CATERING section) without touching knownSections.ts: REQUIRED_HEADERS in the meta-test is unchanged, so CI stays green, and the Class-B scan in lib/parser/index.ts:593-611 then emits a false 'Unrecognized section "CATERING" — its rows were not parsed' warning on every sheet that carries the now-parsed section, inflating the Step-3/staged/unpublished/per-show data-gap counts with a wrong claim. Both the registry and its 'guard' require the same manual update, violating the project's own meta-test rule (structural meta-tests must walk the relevant subtree, not lexically scan a hand-named list) while the feature's false-positive safety story rests on it.

**U103. [P3] #80 — `tests/sync/_advisoryLockSingleHolderContract.test.ts:103`**
Both structural meta-test registries (and the retry route's comments) still describe PR #80's abandoned intermediate design — 'own-connection scan BETWEEN' the two locks — while the shipped code runs the scan INSIDE Lock#2 on the locked connection, leaving the lock-topology contract pins factually wrong.
*Scenario:* The advisory-lock single-holder registry row (tests/sync/_advisoryLockSingleHolderContract.test.ts:103: 'pre-lock Drive prepare + own-connection scan BETWEEN them') and the infra-contract row for retrySingleFileFinalize (tests/sync/_metaInfraContract.test.ts:264: 'interprets the (own-connection) scan result'), plus the route comments (app/api/admin/onboarding/pending_ingestions/[id]/retry/route.ts:32 and :401), all state the scan runs on its own connection outside/between the locks — directly contradicting retrySingleFile.ts:143-146 ('the scan runs on the SAME locked connection/transaction ... it has NOT committed independently'), which is load-bearing: finalize's supersession THROW only rolls the staging back BECAUSE the scan shares the locked tx. AGENTS.md invariant 2 requires plans editing lock surfaces to enumerate holders from exactly these registries; an engineer following the registry text would conclude the scan commits independently (wrong residue/rollback analysis) or could reintroduce an own-connection scan while a show lock is held — the two-connection same-key deadlock class this PR existed to fix.

### 3.10 Other (4)

**U104. [P3] #70 — `components/right-now/buildRightNowContext.ts:90`**
PR 70 redefined RightNowContext.callTime as showAnchors[0]?.time ('Day-1 anchor') for 'back-compat consumers', but the RightNowHero fallback that reads it is unreachable and the only other consumer, the still-exported RightNowCard, inherits the Day-1 cross-label bug the PR fixed only in RightNowHero.
*Scenario:* Since callTime is derived FROM showAnchors, RightNowHero.tsx:161's fallback '(ctx.showAnchors.length === 0 ? ctx.callTime : null)' can never yield a non-null value in production (showAnchors empty ⇒ callTime null) — dead code with a comment claiming a 'legacy single-anchor case' that no longer exists. Meanwhile components/right-now/RightNowCard.tsx:276-279 (unmounted but exported and test-covered) renders 'Call: ${ctx.callTime}' for every show_day_n, so any remount/reuse of RightNowCard mislabels Day 1's call time as today's on days 2..N — the exact cross-label defect the PR's whole-branch review flagged and fixed in RightNowHero only.

**U105. [P3] #55 — `components/shared/AccentButton.tsx:33`**
The atom's documented className escape-hatch contract ('appended LAST so per-site overrides win in cascade order') is based on a false CSS premise — class-attribute order has no effect on the cascade — and the sole shipped override (disabled:hover:bg-accent) only works by coincidental specificity, while the unit test (tests/components/atoms/AccentButton.test.tsx:187-188) and BL-ACCENT-BUTTON-ATOM-SWEEP (docs/superpowers/plans/BACKLOG.md:427) both codify and propagate the misconception.
*Scenario:* A developer follows BL-ACCENT-BUTTON-ATOM-SWEEP's instruction to migrate a `selected ? "bg-accent…" : "bg-surface…"` ternary site via the escape hatch, e.g. `<AccentButton className={selected ? "" : "bg-surface"}>`. `.bg-accent` (always emitted by BASE_CLASS) and `.bg-surface` are equal-specificity utilities in the same Tailwind layer, so which fill renders is decided solely by Tailwind's stylesheet emission order, not the documented 'appended last wins' — the unselected button silently stays accent-orange (or the selected one loses its fill), and the atom's contract plus its passing unit test tell the developer the override should have won.

**U106. [P3] #57 — `docs/superpowers/plans/v1-pre-deployment-amendments/2026-05-30-m12.2-admin-redesign/DEFERRED.md:51`**
B1-D4 is marked 'STATUS: RESOLVED' (and the plan doc checks off '[x] Real CI green (dev-gate)' plus Steps 3-4) even though the workflow has only ever failed in CI, and the resolution text describes a 'workflow_dispatch + scoped pull_request' trigger that does not match the merged dispatch-only file.
*Scenario:* A future orchestrator consulting the tracked DEFERRED.md (line 51) or M12.2-phase-b1-admin-nav-settings.md:1621 ('[x] Real CI green (dev-gate) — B1-D4 RESOLVED') concludes the /admin/dev build-artifact gate is CI-verified and relies on it as a release blocker, when in reality every dev-gate-e2e run failed (last attempt 2026-06-22, never re-fired) and the workflow cannot pass on main; the promised negative-regression red-step (Step 3) was also never demonstrated since it was deferred to this red CI run. The 'scoped pull_request' claim is stale — commit 89e279f2 made the workflow dispatch-only before merge, so nothing fires it automatically.

**U107. [P3] sweep — `lib/db/advisoryLock.ts:32`**
[advisory-locks] withShowAdvisoryLock is now a zombie lock wrapper — zero production call sites (only its own unit tests reference it) — while tests/auth/advisoryLockRpcDeadlock.test.ts:59-119 still scans a curated 8-file list for its callbacks, so that guard check matches nothing and enforces nothing today.
*Scenario:* The first deadlock-guard test greps 8 named files (showInvalidation.ts, dev/actions.ts, picker files, feed.ts, ...) for withShowAdvisoryLock callback bodies; since every listed file migrated to bare self-locking RPCs, matchAll returns empty everywhere and the nested-lock-taker assertion never runs. A future developer who reaches for the still-exported, still-documented wrapper (its doc comments and the test's own comments present it as the canonical JS-side holder) and calls a self-locking RPC like archive_show or reset_picker_epoch_atomic inside it from any file NOT on the list reproduces the M5 R20 two-connection self-deadlock (holder connection A, RPC connection B waiting forever on the same show: key — undetectable by Postgres since A waits on nothing) with no structural test failing.

---

## 4. Reviewer notes — already-fixed bugs & coverage caveats

Per-PR reviewer notes recording bugs that later PRs already fixed, intentional tradeoffs, and coverage caveats. Kept for context; no action implied.

<details><summary>Show all notes</summary>

- PR is otherwise clean. Verified empirically (tsx script over the live catalog): stripEmphasis and renderEmphasis produce identical textContent for every catalog string; `***` literals, single `*` (e.g. \"X.* checks\"), snake_case underscores, and `_<placeholder>_` wrapping all round-trip; renderCatalogEmphasis's per-node interpolation is equivalent to messageFor's whole-string interpolation because placeholder tokens cannot span emphasis boundaries. The MI-7_SECTION_SHRINKAGE `_<prior*count>*` corruption was a pre-existing catalog typo this PR fixed. AlertBanner's collapsed line still strips params-then-markers (can mangle a sheet name containing `*...*`), but that path predates PR #38 and was not touched by it. BL-EM-DASH-POLICY (docs) is already filed in BACKLOG.md.
- No still-present bugs introduced by PR #39. One cosmetic residue: lib/parser/blocks/event.ts:16-19 still documents the disproven variant-2 contract ("Values do not appear in this block; return empty record") that PR #39's DEF-1 resolution overturned at the exporter layer — comment-only, and still accurate for the legacy fixtures/shows/raw single-column shape, so not reported. All fidelity gaps the PR knowingly shipped (rooms/hotels/transport/fail-loud) are pre-existing parser defects documented in docs/superpowers/plans/2026-04-30-fxav-crew-pages-v1/DEFERRED.md and were largely fixed in later rounds (e.g., D1 warning in PR #44, rounds 2/3 fixes).
- Transport changes verified clean: the v4 plain-header driver scan is exact-anchored (`load in:?$`) so schedule stages like 'Load In at Venue' don't match, and the v1 vehicle back-look matches the corpus shape. The cellYear-overrides-context bug (finding 1) was later copied into transport.ts parseV2DateTime:680 by a subsequent PR ('Mirrors hotels.ts'), so a fix should cover both sites. Much of PR #40's other hotel logic (guest splitting, name/address splitting) has been substantially rewritten by later PRs and was audited only as it exists on current main.
- PR #42's diff-time table path (presence(stripConfTokens(col1)) leaving name+address glued in hotel_name) was later superseded by the splitHotelNameAddress work (BL-PARSER #3), which kept stripConfTokens in front — so finding 1 anchors to current-main call sites, not the original ones. parseGuestCell's `confs` return value is computed but consumed by zero callers (documented parse-not-persist intent), a minor dead-data wart not filed separately. The entity-decode half (#8) is clean and corpus-guarded by the meta-test.
- Both findings share the structural root PR #44 introduced — 'emptiness' predicates that hand-enumerate shape fields with no exhaustiveness guard — and materialized when later PRs extended the shapes (loadIn/setupTime/setAgendaRaw via the schedule-inference work; loadout_* via PR #212) without updating them. Not reported as bugs: the contacts '| In House AV | FALSE |' warn is a deliberate, test-pinned design decision; kinds 'dates'/'event_details' not being RegionIds is harmless since SECTION_HEADER_NO_FIELDS is not in OPERATOR_ACTIONABLE_ANCHORED and renders message-only.
- PR #46's UI rendering (the " | " split + stripAgendaUrls card in TravelSection.tsx) was fully superseded on main by the structured flight card (commits 8c7788cb/5a9e7e94, lib/crew/flightDisplay.ts), which inherited the split/strip/filter pipeline — verified no concrete failure remains in it. The surviving projection in lib/data/getShowForViewer.ts is clean (own-row scoped, error-checked, viewer-keyed cache). The PR's known gaps (cross-crew flight_info/confirmation-code DB readability; unparsed TRAVEL-tab flights; unlabeled legs) are all pre-documented in BACKLOG.md (BL-CREW-PII-DB-LOCKDOWN, BL-FLIGHT-LEG-ORIENTATION) and the milestone DEFERRED.md (DEF-FLIGHT-1), so they are excluded per the rules.
- Duplicate same-name TRAVEL rows silently keep the FIRST row's flight (stale-row-wins, no warning) because the TECH-precedence null-check at travelFlights.ts:139 can't distinguish TECH-set from earlier-TRAVEL-set — but this exact behavior is explicitly test-pinned as intended (tests/parser/travelFlights.test.ts 'precedence' case), so it is reported here as a watchpoint, not a finding. The raw/ fixture family's split two-table TRAVEL layout never matches the header signature, but that family is the retired MCP converter, not the prod renderer. PR is parser-only: advisory-lock, Supabase call-boundary, email-canonicalization, and getShowForViewer cache-tag classes have no surface in this diff.
- The Today Mode A privacy gate, shared agendaDisplay single-sourcing, and sentinel meta-test relaxation all verified sound on main. PR #48's 2-line venue address split was later replaced (whereRows form), so its comma-edge quirks no longer exist; the Mode-B one-sided layout gap #48 also had was fixed by the later hasLeft/hasRight branches (only Mode A remains, reported above). The known unknown_asterisk Tonight/Where date leak is pre-documented as BL-CREW-UNKNOWN-ASTERISK-TODAY-DATES in docs/superpowers/plans/BACKLOG.md and therefore not reported.
- PR 49's Mode A arm rendered raw data.runOfShow![todayIso]! entries while gating Mode A on the filtered displayableEntries list (filter/render mismatch could show sentinel/URL-only rows) — pre-existing before #49 and since fixed on main, which now renders the filtered `todays` (TodaySection.tsx:642). The later client-section-toggle refactor (#90) initially conflicted with SectionChipLink's real <Link> nav but CrewSections' prop-sync pattern (CrewSections.tsx:50-54) explicitly resolved it. Everything else in the PR (buildSectionHref param-dropping, anchorsPresent/KeyTimesStrip null-rule mirroring, prefetch={false} meta-test, Mode B degenerate fallbacks) verified clean on current main.
- PR #50 is purely presentational (icon props on SectionCard + a headingIcon chip on the Section-based diagrams tile). All call sites verified on current main; icons are aria-hidden decorative SVGs alongside text titles, RSC-safe, no data/lock/cache surface touched. The one borderline item — the sunken-square chip class duplicated between DiagramsBlock.tsx:122 and SectionCard.tsx:51 — has been maintained in lockstep (both updated size-[15px]→size-3.75 together) and has not diverged, so it does not meet the structural-finding bar.
- Product-code changes in the PR (KeyValueRows columns/span, items-start swap on the five split-wide grids, row-mode gating to min-[720px]) verified clean on current main; KeyTimesStrip was substantially rewritten by a later PR (per-day show rows, 5-row cap) but the PR-51 row-layout plumbing survives intact and its stack posture below 720px is preserved. Neither finding is documented in DEFERRED.md or BACKLOG.md.
- PR #53's known big gap — the standard exporter's DRESS block sits ABOVE the DETAILS header so the new dress/attire aliases never fired on real shows — is documented as BL-PARSER-DRESS-DROP in BACKLOG.md and already fixed on main via lib/parser/blocks/dress.ts (parseDress + mergeDressCode). Two remaining tradeoffs are intentional and documented: distinct real "Dress Code" + "Attire" values collapse to one key (last-real-wins, pinned by tests/parser/blocks/event.test.ts), and pre-#53 persisted `attire` keys render nothing until re-sync (stated in the PR body). A theoretical fuzzy collision (unknown 5-char label like "PRESS" autocorrecting into dress_code) is a product of the later typo-tolerance PR's EVENT_LABEL_VOCAB derivation, has no corpus grounding, and emits an operator-visible FIELD_LABEL_AUTOCORRECTED warning.
- Test-only PR; verified non-tautological on current main (production applyShowSnapshot UPDATE arms at lib/sync/runScheduledCronSync.ts:1140/:1167 still clear requires_resync, and the test still exercises them). Minor non-findings: header comments cite drifted line numbers (:1096/:1122), and the probe catch's `if (sql) sql.end()` guard is dead code so a failed probe pool is never ended (harmless in a vitest worker). The skipIf-no-DB silent skip is a tradeoff disclosed in the PR body, and the test does run in CI (serial vitest project, not env-bound-excluded).
- All 8 migrated sites were verified token-by-token against their pre-PR class strings; only PublishShowButton diverges (original py-2 dropped by the sm size tier), which is visually inert because min-h-tap-min (44px) + inline-flex centering dominates for its single-line labels. ringOffset="surface" is a never-used variant value (speculative API surface, too small to report). The meta-test's same-line-only scan can be evaded by multi-line cn() compositions, but that is guard strength, not a product bug.
- Core PR verified clean on main: DB self-check and UPDATE key share the same lower(btrim()) canonicalization (refusal can't be dodged), advisory-lock stays single-holder in-RPC with the upsert's key, R5 status-whitelist/infra discipline preserved, §12.4 three-lockstep present, and the permanent acceptance of the old last_admin_lockout RPC status is a tradeoff documented in the PR body. lib/data/adminEmails.ts was later touched by f9f4284d (infra-emission) without altering PR #56's translation logic.
- The PR-head CI runs also failed with the same pepper error, so this was observable pre-merge. An unmerged follow-up branch (chore/dev-gate-e2e-pepper, run 27928866762) added the pepper and STILL failed — dev-build's admin-dev-tools-row never rendered (likely a further missing runner/webServer Supabase env for signInAs on bare runners) — so the pepper fix alone is insufficient. The M11-A-D3 MDX crawl fix and the M12 docs walker are clean on current main.
- Clean PR. Only candidate issues were explicitly-stated tradeoffs in the PR body (title truncates with hover-only tooltip recovery, inaccessible on touch) or non-observable (sub-pixel descender clipping from truncate's overflow:hidden with leading-[1.1]). Coverage caveat: the admin-lifecycle-layout e2e suite that guards this fix does not run in PR CI (per project routing), so a future testid rename in app/admin/show/[slug]/page.tsx would silently orphan the regression test.
- The three underlying app bugs predate PR 60 (introduced by earlier M11.5 picker PRs), so per the introduced-by-this-PR rule they are folded into the tracking-loss finding rather than reported as standalone pr=60 findings — but bugs (1) and (2) are P1-grade user-visible defects on current main that deserve their own tickets. The helpers themselves are clean: invariant-9 call boundaries, envelope shape, claim-comparison direction, testids, and baseURL were all verified against live code. Minor caveat: seedPickerCookie's secure-cookie-over-http justification cites Chromium behavior while its only consumer project runs WebKit (iPhone 14), an unverified assumption that only matters if the skipped guest-flow test is re-enabled.
- PR #64 bugs already fixed by later work (not reported): (1) applyShowSnapshot originally wrote `source_anchors = $::jsonb` with `args.sourceAnchors ?? {}`, so any caller omitting the field (staged apply, wizard, manual) wiped anchors to {} — later converted to `?? null` + coalesce-preserve (runScheduledCronSync.ts:1065-1067); (2) today-dress was mapped to the `details` region — PR #211 added the dedicated `dress` region; (3) an anchor-less link opened the sheet's last-active tab — later pinned to `#gid=0`; (4) the untimed cron listSpreadsheetSheets read was bounded by DXT-3 (DEFERRED.md:148, not reported per rules). The prefix-only DETAILS header regex in REGION_ANCHOR_SPEC was explicitly ratified as acceptable for whole-block rects (commit bffc3a71's comment), so it was not reported.
- PR 65 shipped four functional bugs that later PRs fixed: (1) the reset ran via the session client under authenticated's 8s statement_timeout, so any advisory-lock wait cancelled the wipe (fixed by 74e9f804 + migration 20260622000002); (2) unqualified DELETEs were rejected by Supabase's session-preloaded safeupdate over PostgREST, so the button always returned VALIDATION_RESET_FAILED (fixed in PR #72 / migration 20260622000003); (3) the reseed action recorded seededProjectRef 'local' and let buildFixtures throws escape the fault boundary (fixed in PR #68); (4) destructiveResetAllowed() fell back to NEXT_PUBLIC_SUPABASE_URL, allowing guard/target divergence (FIX 3, PR #68). Coverage caveat (not PR 65's defect): the advisoryLockRpcDeadlock guard now pins the reset-RPC topology to migration 20260622000002 with a comment calling it 'the LATEST definition', but 20260622000003 superseded it — introduced by the #72 hotfix; the FK/drive-keyed audits correctly use latestResetValidationDataBody() and were properly maintained (agenda_extract_leases, app_events dispositions added by later PRs).
- PR 67 shipped with its headline fix still broken: the recreated function kept bare `delete from` statements, which the safeupdate extension (session-preloaded on PostgREST's authenticator role, applying even to service_role) rejects with 21000 in ~0.5s — so the Reset button still returned VALIDATION_RESET_FAILED after merge; PR 67's \"verified\" reproduction used a direct psql connection that bypasses safeupdate. Fixed same day by migration 20260622000003 (commit b11b8136), which also identified the 8s-statement_timeout diagnosis as partially a red herring, so not reported as a finding. The unbounded advisory-lock wait under service_role (no statement_timeout) is a tradeoff explicitly documented in the PR body and was not reported.
- All three headline fixes verified correct on current main (mint 5th-arg matches reseedFixtures.ts:38-43 and check-seed predicate (d); buildFixtures/client construction inside typed fault boundaries; guard env-source now matches createSupabaseServiceRoleClient). Minor caveat, not reported as a finding: the bare catch blocks in validationReset.ts swallow error details with no server-side logging, so a production SUPABASE_SECRET_KEY misconfig surfaces only as a generic VALIDATION_RESET/RESEED_FAILED — consistent with the file's documented typed-code contract and admin-only validation scope, and partially pre-existing before PR 68.
- The wipe-to-{} bug the PR targeted is genuinely fixed and still correct on main; PR #214 later added fresh-anchor persistence (pending_syncs.source_anchors) but wired it only for the onboarding scan/finalize path, so the finding above is confined to the cron-staged live-show apply path. The PR body's claim that stale anchors last 'until the next cron re-parse' understates the window — the staged apply advances last_seen_modified_time, so no re-parse occurs until the sheet is edited again and that edit auto-applies cleanly.
- PR #71's original unit-suite.yml had real weaknesses — supabase/setup-cli `version: latest` (violating the repo pin discipline), unconditional cancel-in-progress (could cancel a push-to-main gate run), and CLI `--exclude` flags that vitest ignores under project-level excludes — but the CI-speedup arc (PR #121 et al.) rewrote the workflow (pinned 2.107.0, PR-scoped cancel, VITEST_EXCLUDE_ENV_BOUND), so none survive. The three test-file exclusions are documented in BACKLOG.md (BL-CI-UNIT-GATE-EXCLUSIONS) and email-canonicalization remains gated by x5 in x-audits.yml. The MaintenanceResetButtons safe-plaintext registry row was verified accurate (only 4 marker-free VALIDATION_* codes can flow), and the remaining changes are comment-only waivers and exact-count test exemptions that are self-enforcing.
- The migration delta itself (WHERE ctid IS NOT NULL on every DELETE) is semantics-preserving and correct; it is still the latest reset_validation_data() definition on main, lock topology/key format is unchanged, and table-list drift is structurally guarded by resetValidationDataDriveKeyedAudit.test.ts. Minor caveat (not reported as a finding): the same test hardcodes REST_URL to 127.0.0.1:54321 while seeding/asserting via TEST_DATABASE_URL, so a dual-local-stack setup (worktree Supabase on nonstandard ports) would pass the localhost safety guard yet fire the destructive RPC at a different instance than the one seeded/verified. A class-sweep found no sibling unqualified DELETE/UPDATE reachable via PostgREST.
- No surviving bugs from PR #73. Its two real tradeoffs were documented at merge and later fixed on main: (a) fail-fast abort on a single transient Drive throttle was filed as BL-ONBOARDING-SCAN-TRANSIENT-THROTTLE-RETRY in the same PR and since resolved by withDriveRetry bounded backoff in lib/drive/fetch.ts; (b) a silently-stalled export wedging the whole parallel scan was closed by the DXT-1 stall-guard timeouts (DRIVE_EXPORT_TIMEOUT_MS / DRIVE_FILES_GET_TIMEOUT_MS). The PR body's stated cap (8) never matched the shipped constant (6, now 12 with an updated rationale comment) — description-only drift, no code defect.
- PR 75 is clean on current main. Coverage caveats: (a) the PR's duplicated deps.tx/default branches in runOnboardingScan were later unified into withScanTx (lib/sync/runOnboardingScan.ts:1012), so that structural nit is already gone; (b) the Drive-export-under-lock issue the PR documented was later fixed and BL-WIZARD-RESTAGE-FETCH-BEFORE-LOCK is marked fully closed in docs/superpowers/plans/BACKLOG.md:354; (c) idle_timeout:1 means the "one reused connection" actually re-handshakes once after the >1s Drive prepare phase (2 handshakes vs the old N+1) — postgres.js reconnects transparently and reserved-in-transaction connections are exempt from idle timeout, so this is a minor perf nuance, not a bug.
- Attribution caveat: PR #77 authored the bytes-less override when the prepare contract was {binding, markdown}; the anchors PRs (#211/#214) later added the optional bytes/sourceAnchors flow and missed this call site, so the defect only became user-visible then — it lives in #77's seam and is live on main. PR #77's other known tradeoff (retrySingleFile Drive-under-lock, exempted via knownDriveUnderLockPaths) was since fixed in the follow-up PR (exemption removed, guard now runs with no exemptions per tests/sync/_advisoryLockSingleHolderContract.test.ts:518 and BACKLOG BL-WIZARD-RESTAGE-FETCH-BEFORE-LOCK marked fully closed), so it is not reported.
- PR #80's five claimed fixes (deadlock, false 409, defer/ignore race, live-partition corruption, in-scan supersession rollback) are all verified correct on main and pinned by real-DB tests. The same 25P02 recovery defect (finding 1) also exists in stageWizardRestageInline (lib/sync/applyStaged.ts:1643-1646), but that path's tx-injection predates #80 (commit 37bd1fb5, PR #77). The hard_failed-retry + post-scan-supersession partial-commit residue window is the already-documented BL-APPLYSTAGED-SUPERSESSION-ROLLBACK class in BACKLOG.md, so it is not re-reported.
- Two non-finding observations: (1) The PR body itself discloses that is_session_live() is a function and therefore outside the tables/columns-only validation-schema-parity gate (supabase/__generated__/schema-manifest.json has no function entries) — an environment re-provisioned without migration 20260622000004 would lock all admins out with the cataloged 500; this was a disclosed tradeoff handled by manual apply, not a code bug. (2) getSettingsPageFlags treats a missing app_settings row as infra_error while the four single getters return {kind:"value", false}; the per-toggle fallback makes end-user behavior converge, and the row is seeded/never deleted, so it is not user-visible. PR-85's in-wave viewer_version_token read was later restructured by PR #102 (moved outside unstable_cache), superseding any token-ordering concerns.
- Sibling pre-existing gap (NOT introduced by #87): the legacy discard route app/api/admin/staged/[fileId]/discard/route.ts:100 also calls discardStaged wizard-scoped without catching WizardSessionSupersededRollbackError; those throws were added by the earlier class-sweep commit 523d00ae, so it is the same bug class from a different PR. The three throw sites themselves, the leave-as-return audit (single-statement guarded UPDATEs, read-only preflight), the rollback mechanics, and the new wizard-route 409+alert catch all verified correct on current main.
- Two gaps #88 shipped with were fixed by later PRs: (1) the xlsx-export body read (`arrayBuffer()`) sat OUTSIDE `withDriveRetry` with no time bound, so a stalled/dropped body read hung the scan un-retried — fixed by the stall-guard PR (commit d18a15a9, now `fetchXlsxExportBytes` in lib/drive/fetch.ts:186); (2) despite the "every caller benefits"/BL-RESOLVED claim, `drive.files.list` (the scan's first Drive call) and the cron/apply metadata reads were left un-retried — closed by DXT-1 (commit 2a08602e, lib/drive/list.ts:100) and DXT-3. Coverage caveat still on main but pre-existing (NOT introduced by #88): `defaultVerifyFolder` (app/api/admin/onboarding/scan/route.ts:106) still issues a raw untimed, un-retried `files.get`, so a transient 429/5xx at the wizard's verify-folder step falls through to `OPERATOR_ERROR_INCOMPLETE_FOLDER_METADATA` (its catalog copy does say "usually transient — try again"), and DEFERRED.md DXT-1's "only files.watch/channels.stop remain untimed" enumeration misses this call.
- PR #90's product surface is small (CrewSections.tsx, _CrewShell.tsx, CrewSubNav.tsx, playwright.config.ts); all three components are unchanged on current main since the PR merge (e3e8f548). The mobile-safari e2e project being dead-in-CI is already documented in BACKLOG.md (and addressed by crew-e2e.yml in PR #93), so it is not reported. Unit tests pin the prop-change sync case only, which is why the change-back-to-last-synced desync survived four review rounds.
- PR #91 is clean on current main: the cap bump is pure tuning with no wave-count/DB/lock assumptions elsewhere, and the files.list retry is a bounded, transient-only retry of an idempotent per-page read (retries the same pageToken, no dup/loss). Coverage caveat: the ONBOARDING_PREPARE_CONCURRENCY docblock's "does NOT issue Sheets-API reads / ~6 Drive calls per sheet" claim is now stale, but that drift was introduced by PR #214's eager per-sheet gid fetch, not by #91.
- Coverage of the revalidate registry is otherwise sound on current main (raw-SQL discovery counts match; no multi-line or non-public-qualified writes escape the regex; all writing RPCs on the 7 read tables are registered or genuinely exempt). One inherent, spec-acknowledged residual: unstable_cache stores whatever an in-flight fan-out read, so a write committing while a cache-miss fan-out is running can store a pre-write entry AFTER revalidateTag fired (entry then treated fresh) — bounded by the documented revalidate:300 backstop, so treated as accepted design rather than a finding.
- All four findings verified by executing parseRooms on current main (splitRoomHeader lines blame to PR #106 commit fb26d6868). The fixture-pinned paths (all 7 exporter-xlsx shows) are correct; every finding is an off-fixture edge state. A same-day follow-up commit (9ae7b526c) already extended the dims-prefix regex with APPROXIMATELY, fixing the PR's original name-retains-APPROXIMATELY gap — not reported.
- Route/UI/tests otherwise verified clean on current main: single advisory-lock holder, TS-side canonicalize bound as SQL param, deferral-before-transition with rollback-throw on CAS miss, catalog-mediated error copy, non-tautological mocked-tx + real-DB tests. The \"permanently ignored sheet resurfaces on wizard re-run\" gap (onboarding scan never reads the live deferral partition) is pre-existing shared behavior of the earlier C1 hard_failed writer, not introduced by #112. transitionManifestRow's export predates the PR (commit 8a03213f), so the route-to-route import compiles as intended.
- The PR's two earlier data-loss designs (unconditional same-name-breakout drop, then fill-null merge) were caught by cross-model review and fixed within the PR itself before merge (commits a1892df5/6fca79e0), so they are not present on main. Venue-header false-positive risk on merged multi-line non-header cells above GS Setup is mitigated by the strong-evidence gate and has no corpus instance; not reported.
- PR #115's catch was originally a fully silent `catch {}`; a later observability PR (commit 52d1a270, 'tap non-auth silent fault sites') added the message-only log.error and adf99aed added requestId, but the discarded-error-object gap introduced by #115 remains. DEFERRED.md D13 (real-browser dimension probe, since resolved) covers only the bar's width/height, not the indeterminate-state rendering. The 'N of M sheets' copy is accurate (listFolder filters to the Sheets MIME type) and was ratified in the plan's R3 note.
- Several PR-123 rough edges were already fixed by later PRs: the FIELD_UNREADABLE message's false 'no call link will appear' claim and the missing email-unreadable check were reworked (now outcome-neutral + canonicalized, lib/parser/warnings.ts:74-95), and the per-show panel gained the FIELD_UNREADABLE de-dup vs. actionable rows. The one-shot BLOCK_DISAPPEARED lifecycle (warning cleared by any subsequent apply even while the block is still absent) and the prefix-matched KNOWN_SUB_LABELS over-suppression (e.g. 'AV CREW'/'SET CREW' bands never flagged) are ratified design tradeoffs in docs/superpowers/specs/v1-pre-deployment-amendments/2026-06-23-parse-data-quality-warnings-design.md, so not reported; DEFERRED.md DQ-1 covers the jsdom-only layout test.
- PR #124 itself verified clean on current main: its documented heuristic tradeoffs (numeric-brand hotel names, suffixless streets stay glued) were hardened in-PR (Codex R1/R2) and further by the follow-up PR 67e587f1..87f6ff5a (ZIP-tail street discriminator, learn-K guest extraction), and the east-coast v1 guest-glue residual is documented in BACKLOG.md under BL-PARSER-PRODUCTION-FIDELITY-RESIDUAL #3. The single P3 is filed as a sweep finding (pr=0) because the stripConfTokens call on that cell predates #124 (AR R6, 8827908a) and the path divergence was created by the follow-up PR fixing only the inline path. The hotel-local zero-width strip is now redundant with clean() (2e8dfa27) but coverage is identical, so it was not reported.
- Sentinel email values (TBD, N/A, -) also fire FIELD_UNREADABLE despite the project's sentinel-means-absence contract (lib/visibility/emptyState.ts), but this mirrors the phone bar deliberately set by PR #123 ('has content, no @/digits'), so it is inherited design, not a #125 defect. Minor non-findings: tests/parser/blocks/crew.test.ts:512 comment still cites the pre-R2 'no email link will appear' copy that the same PR replaced, and the invariant-3 structural regex test only catches direct emailRaw.method() calls — both doc/test-quality nits, not runtime bugs.
- PR code is byte-identical on current main (hotels.ts:197-220, 579-703) — nothing was later fixed. The PR's documented limitations (single-guest multi-word fall-through, bare-conf# fall-through, suffixless-street gluing, viewer-name matching — since shipped as namesRefer in PR #133) are excluded per BACKLOG.md. Structural note folded into finding 1: the new inline extractor duplicates parseGuestCell's '<name> <dash> conf' parsing but has already diverged from it (no trailing-un-numbered-guest handling, no slash-separated-guest split).
- Guard implementation itself is correct (fresh AbortController per attempt, timedOut set before abort, timer cleared in finally, 504 in the transient set, tests self-proving). The companion gaps the PR knowingly deferred (untimed files.get/list, asset/revision streams, Sheets metadata) were all closed by later PRs per DEFERRED.md DXT-1/2/3. The sibling mismatch — files.get budgets (8s×4≈34s) also exceeding the same 30s cron step race — was introduced by the DXT-1 PR (#132), not #128.
- Core timeout mechanics verified correct against gaxios 7.1.4 + googleapis-common 8.0.1 source (per-call options merged, DOMException cause -> code "TimeoutError", no non-idempotent withDriveRetry callers). The ETIMEDOUT/ECONNABORTED arm of driveErrorStatus (fetch.ts:132) is dead code under gaxios-7 native fetch (codes nest two causes deep) — folded into the finding above as its mechanism rather than reported separately. PR-body claim that exhausted retries throw "a typed error" is inaccurate (raw GaxiosError propagates), but all consumers map it to typed SYNC_INFRA_ERROR/infra_error handling, so no user-visible defect; DXT-2/DXT-3 follow-ups the PR deferred were since completed by later PRs per DEFERRED.md.
- No surviving findings. Verified on current main (147bae46): stall-guard semantics, abort/destroy wiring incl. already-aborted race, timedOut()-based fail-soft vs error propagation, onChunk total-budget forwarding, and bytesFromWebStream->readBoundedWebStream equivalence are all correct; helpers unchanged since the PR. The stall-vs-absent-asset observability gap and the cron 30s total-step-timeout overlap are documented in DEFERRED.md / lib/drive/stallGuard.ts:66-73 respectively, so excluded. Caveat: a stalled google-auth token refresh inside drive.revisions.get is not bound by the guard signal and could still hang — pre-existing, not introduced by this PR.
- No surviving bugs from PR #140. Minor non-findings: (1) the { maxRetries: 1 } apply-path default in verifyReelOnApply.ts:90 / applyStaged.ts:942 is replaced wholesale if a caller injects any partial DriveRetryOptions (latent footgun, no production caller affected); (2) the "holds the per-show advisory xact lock" comment at applyStaged.ts:933 is stale — the sole call site (applyStaged.ts:1721) now runs pre-lock between the two withPipelineLock blocks, apparently due to a later refactor.
- Verified the anchor pipeline dynamically against all four exporter-xlsx fixtures (both CREW and TECH geometries resolve correct cells; name-key normalization matches parser blockRef on both sides; terminator/ambiguity degradation is link-less, never wrong-cell). Adjacent but out-of-scope: PR #217's selectActionableForDisplay legacy-anchor strip is applied only on the per-show page, while both staged pages still call operatorActionableWarnings directly on persisted parse_result (contradicting dataGaps.ts:199-201's 'both persisted-read call sites' claim) — that gap was introduced by PR #217, not #154. The untimed fetchSheetTitleToGid on the onboarding path is already documented in DEFERRED.md:150 and was not reported.
- Later commits extended the feature without fixing PR-166 bugs (b7c0f31b added structured cache-fault logging; b73cfe1e surfaced city on crew cards — city survives apply since parse_result jsonb passes through wholesale). Not reported as a bug: after the 30-day cache TTL expires, a re-scan during a Google outage silently drops a city the crew page previously showed — this is the PR's documented best-effort/offline-fallback design. Breaker half-open races under the onboarding scan's 12-way concurrency were examined and judged benign.
- Coverage caveats: (1) yearless strike dates resolve via inferShowYear (first date in the sheet), so a Dec→Jan cross-year show misdates a January strike into the prior January (false SCHEDULE_STRIKE_DATE_OFF_SCHEDULE + strike hidden from crew) — but this mirrors the pre-existing hotels/transport year-backfill convention, so not attributed to PR 169. (2) Verbatim time grouping means format variants (\"4:30 PM\" vs \"4:30pm\") don't collapse into one strike entry — already documented in DEFERRED.md (meridiem-normalizer entry). (3) Most other risky-looking choices (dateless strikes skipped silently, off-schedule load-out unwarned, load-out transport gating, aggregate-day widening) are explicitly ratified in the 17-round-reviewed design spec.
- Both findings verified by executing the live modules read-only (npx tsx) against main @ 147bae46; neither appears in DEFERRED.md or BACKLOG.md. Rest of the PR is clean: setAgendaRaw strip is sound (parseSheet is the only ShowRow composer; parseAgenda/parseScheduleTimes read fields only), the decodeEntities addition to extractClockTimes cannot create/destroy colon-clock matches, and the change touches no lock/Supabase/UI/cache surface.
- PR #173 also shipped the lunch-room duplication bug (GEAR 'GRAND BALLROOM C'/additional vs INFO 'BALLROOM C'/breakout double-rendering as two cards) — already fixed by PR #191 per BACKLOG.md BL-ROOM-GEAR-MERGE-DEDUP, so not reported. Full-corpus parser runs (rooms, GS fields, event_details, pull sheets over all 17 fixtures) otherwise match expected output; the event-form harvest leaks no PII/financial keys on the corpus.
- No surviving defects. The coalesce is guarded by the Phase B Drive-modifiedTime equality check (route.ts:714) that runs under the same per-show advisory lock before staging, so it cannot mask genuine staleness; the Phase D equality preflight still refuses any post-staging live advance. Copy three-way lockstep (spec §12.4 / spec-codes.ts / catalog.ts) is intact on current main and the code renders only via lib/messages lookup.
- The buildSheetDeepLink #gid=0 fallback half of the PR is clean on current main: all consumers tolerate the new fragment, the whole-sheet title-link landing change is a documented tradeoff with updated contract tests, and the finalize route now relies on the fallback. The script's direct DB write also skips show-cache-tag invalidation, but the 300s unstable_cache TTL backstop (spec §4.3, same posture as validationReset.ts) makes that self-healing, so it is not reported.
- PR #179's finalize-time mechanism (pre-lock Drive XLSX export per approved row — wastefully run even for existing-show rows that ignored the result — and Drive failures swallowed via bare console.warn) was fully replaced on 2026-07-01 by the persist-at-scan rewrite (commits 56746156/0370a299/3487101c): finalize now reads pending_syncs.source_anchors under the lock with no Drive export. The surviving pieces (ApplyStagedCoreArgs.sourceAnchors at lib/sync/applyStagedCore.ts:437/536 and the route spread at app/api/admin/onboarding/finalize/route.ts:984) are correct; all callers honor the never-pass-{} contract so the UPDATE-arm coalesce cannot wipe anchors. The known finalize-cas sibling gap persists but is documented at docs/superpowers/plans/BACKLOG.md:28 (BL-ONBOARDING-CAS-SOURCE-ANCHORS), so it is excluded per the audit rules.
- The folder-scope guard also re-stages files Doug trashed in Drive (parents still include the setup folder; the whole-folder scan filters trashed=false at lib/drive/list.ts:78) — not reported because it deliberately mirrors the pre-existing retrySingleFile.ts:232-234 pattern. DEFERRED.md RESCAN-1 (raw drive_file_id labels on blocker rows) is already documented and excluded.
- PR 188's logic is intact on main (later PRs only added source_anchors to the same locked re-read). Two adjacent issues were verified as NOT introduced by this PR: (a) the freshRead 0-row demote path (demotePending keyed only on drive_file_id+session, which can reset a discarded sheet's manifest to 'staged') pre-dates 188 from the agenda-feature re-read; (b) the skipped client-side finish/checkbox serialization is an explicitly documented scope deferral in BACKLOG.md (BL-FINALIZE-APPROVAL-DECISION-RACE resolution).
- The banner #0 partial-duplicate/leading-empty holes were already fixed in-PR (commit 6cc40bbf, Codex whole-diff R1). Not reported: escaped `#` in a banner title leaking a backslash into show.title is a pre-existing class shared by the #1/#6 title paths that #0 merely inherits; and the GEAR-only GRAND strip not merging an INFO lunch room itself named 'GRAND …' is not a regression (the kind mismatch prevented that merge pre-PR too) and matches the spec's accepted lunch-scoped design.
- Current main matches the PR verbatim (Step3Review.tsx's last commit is the PR itself), so nothing was later fixed. BACKLOG's BL-FINALIZE-APPROVAL-DECISION-RACE documents only the server-side commit-after-select race (resolved, PR #188); the reported finding is the residual in-browser window, whose sole incidental guard (server-derived confirm + lagging label) this PR removed — it is not documented in DEFERRED.md or BACKLOG.md. Seed/optimistic count parity, wrapper state-sync after router.refresh(), hydration, and the effect wiring were all verified clean.
- PR 206 as merged had one real bug, since fixed: it routed the new event-details UNKNOWN_FIELD warnings through the then-current region-level anchoring (git show 5465aa45:lib/drive/showDayTimeAnchors.ts:138), so every unknown label in a show shared one block-range (code,gid,a1) dedup key — operatorActionableWarnings collapsed N distinct unknown-label warnings into one row whose deep link pointed at the block header, not the row. PR 217 (per-row anchors in lib/drive/unknownFieldAnchors.ts + stripLegacyUnknownFieldAnchors shim + bffc3a71 exact-match header regex) fully repaired this on current main. Remaining caveat, judged by-design rather than a defect: unknown EVENT-DETAILS labels with an empty value are still neither kept nor flagged (lib/parser/blocks/event.ts:209 val guard), a deliberate defense against flagging every row of the v2 label-only DETAILS list variant (e.g. Floor Plan / Room Diagram in fixtures/shows/raw/2025-03-dci-rpas-central.md:295-314).
- Advisory-lock topology, listed/row reconciliation math, cache revalidation parity, raw-code rendering, and the Accept-negotiation impact on legacy callers (ResumeFinalizeButton/RunFinalCASButton) were all checked and are clean. The finalize route's own streaming catch (finalize/route.ts:1310) lacks logging too but is near-unreachable since executeFinalizeBatch catches and logs internally, so only the cas route is reported. The running→complete focus drop to <body> is a documented tradeoff in the PR body and was not reported.
- No surviving defects found. The PR's one real edge bug (region-extract failure stripping the valid gid map from warning-anchor attachment) was fixed inside the PR branch itself (commit 7c191db6, ancestor of merge 24133643) with a regression test. Remaining degradations — pre-ship wizard sessions reading '{}' (#gid=0 fallback) and the eager per-sheet gid fetch at scan — are intentional, documented tradeoffs in the PR body, and the gid fetch is already timed/retried per DEFERRED.md DXT-3.
- The in-branch fix bffc3a71 already closed a false-early DETAILS-header match (exact-$ regex) before merge, so it is not reported. The other safe-degradation paths I checked (v1 'DETAILS/Room Diagram' and 'GS DETAILS (FOR BOTH)' headers never matching the scanner, parser-vs-scanner value-column mismatches, applyParseResult re-running attachSourceCellAnchors with unknownField:[]) all degrade to a null link rather than a wrong cell and match the PR's documented correct-cell-or-null tradeoff, so they were not reported as separate findings.
- The PR's known-HIGH (RightNowHero cross-labeling Day-1 onto an omitted Day-2) was fixed pre-merge (9070da18). AGENDA_DAY_EMPTIED was deliberately narrowed to fully-empty days (lib/sync/applyParseResult.ts:160-168) — a grid day emptied while a DATES-column showStart survives now loses its stored entries without any warning; treated as a ratified design choice per in-code/spec comments, not reported. KeyTimesStrip overflow UX and mixed time-format copy are already documented in DEFERRED.md (D2/D3) and were excluded.
- Lens coverage caveat: this pass covered concurrency/error-handling/state; parser-tokenization display defects were left to the sibling lens (e.g. the window branch at scheduleTimes.ts:169-170 stores raw cell slices, bypassing normClock, so an accepted semicolon-typo clock like '7;30am' persists and renders verbatim). PR 70's original ScheduleSection fragment-day meta arm was later reworked by the scheduleBookends PR (viewer-gated dayEntries), so raw-vs-gated entry-count divergence there is not attributable to #70.
- Bugs PR 103 shipped that later PRs already fixed (excluded from findings): (1) Step-3 optimistic-state desync — Step3PublishHeader's optimisticAll was never reconciled after router.refresh() and PublishCheckbox's useState(initialChecked) ignored refreshed props, so Select-all left per-card boxes/count lying; fixed by commit f3226ae9 plus the shared-overlay rewrite now in Step3Review.tsx:664-746. (2) UI/server finishable mismatch for demoted rows — PR 103's UI blocking set ignored last_finalize_failure_code while the server's unresolvedManifestCount counted demoted 'staged' rows, yielding an unexplained ONBOARDING_NOT_RESOLVED 409 dead-end; current OnboardingWizard.tsx:368 and Step3Review.tsx:650/Step3SheetCard.tsx:1419+ now thread lastFinalizeFailureCode. Also excluded per instructions: the finalize select-time stale wizard_approved race (documented at BACKLOG.md:61-70).
- Two PR-103 bugs were fixed by later PRs and are not reported: (1) the Step-3 select-all optimistic state never reconciled with server truth and per-card PublishCheckbox useState was decoupled from refresh (the \"select-all-doesn't-stick\" bug) — rewritten into a lifted overlay + per-row coalescing flush in Step3Review.tsx; (2) finalize acting on select-time wizard_approved before taking the per-show lock — a later PR added a locked re-read forward-defense and the residual race is tracked in BACKLOG.md:61-70, so it is excluded per instructions. Coverage caveat: test files were only skimmed; UI/visual aspects left to the sibling lens.
- The one pre-existing race the PR surfaced (finalize select-time wizard_approved vs approve) was deferred to BACKLOG/DEFERRED at merge time and later fixed by PR #188, so it is excluded. PR-era console.error calls were later upgraded to structured log.* by follow-up PRs. All other candidates (staged_modified_time Date round-trip, report-ordinal merge mapping, client slot-throttle/abort paths, lease lifecycle, advisory-lock topology) were verified safe against current main.
- The destructive known_stale clearing on download failure after an EXTRACTOR_VERSION bump (transient Drive fault deletes a still-revision-current stored schedule) is ratified in the spec's KNOWN-STALE definition (docs/superpowers/specs/2026-06-27-agenda-pdf-schedule-merge.md:367-372), so it was not reported despite the misleading in-code proof comment at enrichAgenda.ts:243-251 claiming cache-miss implies revision mismatch. The finalize approval-decision race the PR surfaced is documented in BACKLOG.md and was fixed by PR #188; the PR's finalize parse_result re-read was subsumed by #188's widened locked re-read. Lock topology, lease lifecycle, fences, Supabase call boundaries, and the client 5-state machine (late-response suppression, throttle drain, budgets) all verified clean under this lens.
- DEFERRED.md/BACKLOG.md contain no typo-tolerance entries. Cross-vocab false-positive sweep (all gates simulated against the union of every real label table: FIELD_ALIASES, KNOWN_SUB_LABELS, KNOWN_SECTION_HEADERS, terminators, role codes, stage words) found no corpus-grounded meaning-changing rewrite — the registry meta-test plus exact-first/exclude/minLen gates hold; risky-looking hits ('DIAGRAM'→'DIAGRAMS', 'PASSENGER'→'PASSENGERS', 'in-house av') are same-meaning or blocked by the exact-alias-out-of-scope check. parseOps/parseClientV2 fuzz whole-document rows (no block scoping), but no Damerau-1 collision exists among real corpus labels, so reported nothing there.
- Coverage caveats: (1) lockTakingRpcNames in the guard test scans a curated 10-migration list; I manually verified the ~9 unlisted lock-taking migrations (redeem_link, mint_bootstrap_nonce, recheck_link_session, revoke_leaked_link, signed_link_admin_rpcs, b2 lifecycle/def1/r4/r8) all take the advisory lock before any FOR UPDATE and none is called under a JS-side lock — no live violation, only future-drift exposure. (2) Session-reset purges (reserveWizardSession in scan/route.ts:186-189 and purgeWizardRows via purgeAndRotate*/cleanup) mutate pending_syncs/pending_ingestions with zero per-show advisory locks, but are serialized against the finalize family via the app_settings row lock and this lock-free topology is spec-ratified (2026-06-22 nav-perf phase1 spec advisory-lock note); residual races vs show-lock-only actions self-heal, so not reported. All five named recent surfaces (#210 streaming shares executeFinalizeBatch with one finalize: holder + per-row defaultWithRowTx show: holder; #180 rescan finalize:→app_settings→show: pinned; #80 retry two sequential non-nested pipeline-lock windows; #188 full decision-row re-read under the held row lock; #214 anchors computed pre-lock) verified clean.
- BL-FINALIZE-APPROVAL-DECISION-RACE (BACKLOG.md) is shipped and verified in code (locked full-decision-row re-read at finalize/route.ts:746-845), and RESCAN-1 (DEFERRED.md) is resolved — both skipped per instructions. Finding 1's manifest-'staged' end-state is pinned by tests/onboarding/rescanWizardSheet.db.test.ts T-A1, but that test seeds approval via raw SQL that never sets manifest 'applied', so it pins the desync rather than proving it intended; the Flow-B (shadow-recovery) 'staged' pin is harmless since those rows have no created_show_id. Streaming error paths (scan + finalize NDJSON), optimistic checkbox coalescing (#204), first-seen provenance race (#176), select-all (#130/#143/#145), and back-nav/resume gating all checked clean.
- Cache-staleness axis (b) is clean: only the console→lib/log refactor touched the extractor after the v1→v2 bump, and PR #192 changed consumers only. Confidence gating (e) is correctly enforced at all three render boundaries. The #186 guard's show-first-session scoping (bare-evening later days misread as AM) is an explicitly ratified fixture-driven tradeoff documented in code comments, so not reported.
- DEFERRED.md/BACKLOG.md contain no Sentry/PII/share-token entry, and the phase-3 spec/plan considered crew PII only for the deferred Replay feature, never for error-event URLs. All other hunted vectors (logger throw/recursion, persist await semantics, capture loops, cron per-row persistence, console-migration silent drops, unbounded /admin/observability queries) verified clean — persistence is level/code-gated, queries are keyset-paginated with LIMIT, and app_events has a 60-day prune cron.
- namesRefer itself is clean within its ratified bounds (surname-share/prefix over-match and the bare cross-initial-nickname under-match are explicitly documented in the 2026-06-26 spec and file header); the rpas 'Lunch / Strike' sheet-listed strike alongside the synthesized strike entry was checked and judged ratified-faithful per the inference spec's corpus survey (RD1/RD5), so no double-entry finding was filed. Flight cross-year rollover, timeline crew-vs-agenda dedup, cap partitioning, and D12 run-of-show day gating all verified clean.
- The identical defect exists in app/api/admin/onboarding/finalize/route.ts:1294-1309 (stream start awaits executeFinalizeBatch whose revalidate loop at :1238-1240 runs post-handler-resolution) but is benign today: that batch only creates first-seen shows with firstSeenPublished:false (route.ts:963/977), which have no cache entries to bust. All other post-#102 write paths (#103/#112/#166/#178/#179/#180/#188/#204/#214/#217, cron/manual/push sync, mi11/undo actions, promoteSnapshot, assetRecovery, discardStaged) were verified covered or exempt, and the cache key/viewer isolation (role, share token, cross-viewer) is clean. tests/db/showCacheRevalidateCoverage.test.ts only counts revalidate call sites per file, so it structurally cannot catch this flush-timing class.
- DEFERRED.md's DXT entry covers only missing timeouts/retries on the gid-fetch calls, not the anchor-wipe or drop semantics above, and BACKLOG.md has no anchor entries. The #217 range-':' shim itself never misclassifies new-format refs (per-row anchors are encode_cell, never ':'), and finalize/onboarding re-scan paths correctly refresh or intentionally clear wizard-partition anchors. Read-only audit; no tests were run.

</details>

---

## Appendix — audit scope

**PRs reviewed individually (66):** #38, #39, #40, #42, #44, #46, #47, #48, #49, #50, #51, #53, #54, #55, #56, #57, #58, #60, #64, #65, #67, #68, #69, #70, #71, #72, #73, #75, #77, #80, #85, #87, #88, #90, #91, #102, #103, #106, #112, #114, #115, #123, #124, #125, #126, #128, #132, #136, #140, #154, #166, #169, #172, #173, #176, #178, #179, #180, #181, #188, #191, #204, #206, #210, #214, #217

**PRs skipped as low-value (97)** (machinery covered by subsystem sweeps where applicable): #41, #45, #52, #59, #61, #74, #76, #79, #81, #82, #83, #84, #86, #89, #92, #93, #94, #95, #96, #97, #98, #100, #104, #105, #107, #109, #110, #111, #113, #116, #117, #118, #121, #122, #127, #130, #131, #133, #134, #135, #137, #138, #139, #141, #142, #143, #144, #145, #146, #147, #148, #149, #150, #151, #152, #153, #155, #156, #157, #158, #159, #160, #161, #162, #163, #164, #165, #167, #168, #170, #171, #174, #175, #177, #182, #183, #184, #185, #186, #187, #190, #192, #193, #194, #195, #197, #199, #201, #203, #205, #208, #209, #211, #212, #213, #215, #216
