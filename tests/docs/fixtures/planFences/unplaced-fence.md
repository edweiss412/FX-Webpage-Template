# Planted fixture — an unplaceable fence

The fence below never closes. The gate must REPORT it by path and line rather
than skip it, which is what makes the demotion a documented limit instead of a
hole (spec limit 3b).

In `lib/example.ts`:

```ts
const value = rows[0].name;
