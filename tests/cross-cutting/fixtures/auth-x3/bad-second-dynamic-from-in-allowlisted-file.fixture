export class SomeClass {
  async lookup(tableName: string) {
    return supabase.from(tableName).select("*");
  }
}

export async function resolveSecondaryTable(otherTable: string) {
  return supabase.from(otherTable).select("*");
}
