export async function report(db: { from(table: string): { insert(row: unknown): Promise<void> } }, crewEmail: string) {
  await db.from("reports").insert({
    reported_by_kind: "crew",
    reported_by: crewEmail,
  });
}
