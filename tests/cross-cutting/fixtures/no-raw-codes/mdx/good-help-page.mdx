# Fixture help page (M11-A-D3, clean)

This fixture is the negative control for the MDX runtime-crawl proof: an
MDX-authored help surface whose prose uses only plain-language copy, never a
raw §12.4 catalog code. The runtime crawl must pass it (no leaks).

If the schedule sheet cannot be read, the banner explains the problem in
plain language and links to the help article — no raw error code is shown.

<abbr title="What you can do next">missing sheet</abbr>
