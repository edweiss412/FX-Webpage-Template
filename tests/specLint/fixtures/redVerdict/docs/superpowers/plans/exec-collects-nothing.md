# Verdict fixture — live red that collects nothing

<!-- tasks: depth=2 red-contract -->

## Task A

<!-- task: red=`pnpm vitest run --config fixtures/specLint/redVerdict/vitest.config.ts fixtures/specLint/redVerdict/excluded.test.ts` red-state=live why=`the file is tracked but the fixture config excludes it, so the non-zero exit is a collection artifact` ac=AC-1 -->

- [ ] Step 1: satisfy AC-1.

<!-- tasks: end -->
