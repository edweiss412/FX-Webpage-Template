# Verdict fixture — authored red naming an uncollectible suite

<!-- tasks: depth=2 red-contract -->

## Task A

<!-- task: red=`pnpm vitest run --config fixtures/specLint/redVerdict/vitest.config.ts fixtures/specLint/redVerdict/excluded.test.ts` red-state=authored red-target=`lib/specLint/redContract.ts:1` why=`the named suite is tracked but the fixture config can never collect it` ac=AC-1 -->

- [ ] Step 1: satisfy AC-1.

<!-- tasks: end -->
