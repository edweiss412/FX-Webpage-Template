# Verdict fixture — live unprobeable red that exits zero

<!-- tasks: depth=2 red-contract -->

## Task A

<!-- task: red=`sh -c "true"` red-state=live why=`the red exits zero so the exec arm reports it already green, and the collection arm independently reports that no probe could be derived for it` ac=AC-1 -->

- [ ] Step 1: satisfy AC-1.

<!-- tasks: end -->
