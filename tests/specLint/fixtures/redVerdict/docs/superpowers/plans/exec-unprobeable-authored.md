# Verdict fixture — authored red that is unprobeable without any wrapper

<!-- tasks: depth=2 red-contract -->

## Task A

<!-- task: red=`sh -c "grep -q collected fixtures/specLint/redVerdict/collected.test.ts"` red-state=authored why=`a grep is not a runner, so no collection probe exists to derive and the arm reports that its capability is unverified rather than dropping the entry` ac=AC-1 -->

- [ ] Step 1: satisfy AC-1.

<!-- tasks: end -->
