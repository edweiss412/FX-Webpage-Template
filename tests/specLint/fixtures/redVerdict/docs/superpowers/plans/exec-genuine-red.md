# Verdict fixture — live red genuinely observed

<!-- tasks: depth=2 red-contract -->

## Task A

<!-- task: red=`pnpm vitest run --config fixtures/specLint/redVerdict/vitest.config.ts fixtures/specLint/redVerdict/collected.test.ts` red-state=live why=`the fixture suite fails for its own reason and the probe collects it` ac=AC-1 -->

- [ ] Step 1: satisfy AC-1.

<!-- tasks: end -->
