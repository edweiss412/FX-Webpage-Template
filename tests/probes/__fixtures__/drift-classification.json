{
  "_comment": "Synthetic classification used ONLY by regen-db-free.mjs under REGEN_DB_FREE_STUB=drift, to prove `--check` fails on a mismatch (Codex plan R1). Deliberately DIFFERS from the committed lists; kept minimal + synthetic (no real repo paths) so a source scan finds nothing real here.",
  "movable": ["tests/__fabricated__/drift-marker.test.ts"],
  "dbTouching": ["tests/__fabricated__/drift-db-marker.test.ts"]
}
