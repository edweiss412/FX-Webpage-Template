# Dependencies / lockfiles
node_modules/
pnpm-lock.yaml
package-lock.json
yarn.lock

# Build output
.next/
out/
build/
dist/

# Test artifacts
coverage/
test-results/
playwright-report/
blob-report/

# Generated
next-env.d.ts
*.tsbuildinfo
# schema-manifest.json is byte-compared by tests/db/validation-schema-parity.test.ts
# (layer 3) and must stay in gen-script format (JSON.stringify 2-space); keep
# prettier from re-flowing it.
supabase/__generated__/

# The master spec is gen:spec-codes INPUT — x1-catalog-parity byte-compares the
# regenerated manifest against the committed one, and prettier's markdown
# emphasis normalization (_x_ -> *x*, asterisk escaping) inside §12.4 table
# cells changes the generated copy. Never format it.
docs/superpowers/specs/2026-04-30-fxav-crew-pages-v1.md

# Generated TS — gen:* scripts emit raw (NOT piped through prettier), so a
# reformat here would permanently drift vs the next gen run (dirty tree / would
# fail format:check after any regen). Keep them in gen-script format.
lib/messages/__generated__/
lib/audit/*.generated.ts
lib/admin/__generated__/
# tests/db/__generated__/postgresExecutionMethods.ts is emitted raw by
# gen:execution-methods and byte-compared two ways: its --check mode and the
# x-audits "Verify generated execution-methods module is fresh" step
# (git diff --exit-code). A prettier reflow of the array literals drifts the
# committed bytes from the next gen run and both gates go permanently red.
tests/db/__generated__/

# Parser input fixtures — markdown table column whitespace is load-bearing input
# read by tests/parser/*; prettier table reflow would churn the test corpus.
fixtures/shows/

# Cross-cutting audit fixtures: intentional-malformation / controlled-input
# corpora whose EXACT content (table shape, multiline messageFor, controlled
# inputs, semantic cursor patterns) is asserted by the audit tests
# (extract-spec-codes, no-raw-codes, no-global-cursor, email-canonicalization).
# Prettier reflow would silently change what those tests detect. eslint already
# special-cases several of these dirs; mirror that for prettier.
tests/cross-cutting/fixtures/

# Directive-resolver contract fixtures (PR-C): several are UNPARSEABLE by design
# (octal-escape directive, trailing garbage) so prettier errors on them, and the
# single-quote directive fixture must keep its exact quotes (prettier would
# rewrite 'use server' -> "use server" and destroy the case-e distinction). Same
# posture as tsconfig + eslint.config.mjs, which already exclude this dir.
tests/e2e/helpers/__fixtures__/directive/

# Prose docs (plans, specs, handoffs, project docs). Prettier's markdown table +
# emphasis normalization is the same hazard that exempts the master spec above;
# several are asserted on by doc-guard tests and actively edited (merge-conflict
# risk). The format gate covers code, not prose.
docs/

# DESIGN.md is a UI-surface gate trigger (AGENTS.md invariant 8 — changes require
# the impeccable critique+audit pair). Keep it out of the mechanical format pass
# so a reformat does not silently trip that gate.
DESIGN.md

# declared-limit-pin arm fixtures: these plan documents ARE the recognizer's input,
# and the arm reads structure prettier normalizes — `**Files:**` block shape, the
# blank line after a header that makes a declaration DECLINE, list markers, and
# indentation. Measured, not assumed: a prettier pass over plan-pre-step3b.md made
# the PRE plan draw 0 advisories where the corpus test requires exactly 1
# (tests/specLint/declaredLimitPinsCorpus.test.ts). Same posture as the parser and
# cross-cutting fixture corpora above.
tests/specLint/__fixtures__/declaredLimitPins/

# Editor / OS
.DS_Store

# Supabase managed
supabase/.branches/
supabase/.temp/

# Tool dirs
.serena/
.superpowers/
.claude/
.vercel/

# Captured-baseline fixtures are byte-compared against a live render
# (tests/components/admin/review/reviewModalShell.test.tsx, T-STEP3-INVARIANT).
# Prettier would re-flow the captured markup and break the comparison; the
# generator (scripts/captureStep3HeaderBaseline.ts) owns this file’s formatting.
tests/components/admin/review/__fixtures__/

# The captured VenueSection raw-Wi-Fi baseline is byte-compared against a live
# render (tests/components/crew/sections/VenueSection.wifiRoom.test.tsx, the
# fail-soft regression pin). Prettier would re-flow the captured markup and the
# comparison would then assert against reformatted bytes rather than the bytes
# the component actually emitted.
tests/fixtures/venueSectionRawWifi.html

# The claim sweep's historical re-enactment fixtures are FROZEN BLOBS of the
# incident arc's documents, checked out verbatim from `fede5f084` and
# `c272ebed3` (spec §6). Every assertion over them is a
# `(document, line, COLUMN, token)` set taken from the probe record, so
# re-flowing a markdown table moves the columns and the replay stops
# reproducing the acceptance criterion it exists to pin. An artifact whose
# value is EVIDENTIARY must not be updated to stay current: freshness and
# fidelity are opposite requirements here. Byte-identity against the two blobs
# is asserted by tests/specLint/claimSweepFixtures.ts's own integrity case.
tests/specLint/fixtures/claimSweep/
