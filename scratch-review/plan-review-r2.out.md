- [HIGH] Task 1 does not actually test the §10.1 contract. The spec requires “drive to `error`, call `run.dismiss()`, assert `state.kind === 'idle'`” (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:199`). The planned test starts from the hook’s initial idle state (`components/admin/FinalizeButton.tsx:169`) and clicks dismiss while already idle (`docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:72-83`). A no-op `dismiss` would pass once the method exists, so this is tautological coverage.

- [HIGH] Task 5’s compound test is not runnable for the `error` branch. The plan uses `test.each(["cas_per_row", "error"])`, fires Escape, then always clicks `wizard-finalize-blocker-dismiss` (`docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:227-238`). But both spec and plan say Escape dismisses the blocker for `error` (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:105`, `:157`), so the dismiss button is gone before the click.

- [HIGH] Task 7’s Playwright harness does not prove the required production stacking/layout case. The spec requires the modal mounted through `FinalizeStatusRegion` inside real `WizardFooter`, with an open `Step3ReviewModal`, proving footer height stability and top-of-stack behavior (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:174-179`). The plan instead mounts `<FinalizeBlockerModal run={stubRun}/>` beside a “WizardFooter-shaped container” with no review modal (`docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:267-269`). That bypasses the footer-center slot and the z-40/z-50 conflict the spec is trying to pin.

- [HIGH] Task 7 also depends on an unexported/private component. The plan only says `FinalizeBlockerModal` is “co-located” (`docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:99`) and current exports are `FinalizeRun` and `FinalizeStatusRegion` (`components/admin/FinalizeButton.tsx:498`, `:572`). A standalone esbuild entry cannot import `<FinalizeBlockerModal>` unless the implementation adds an export the plan never specifies. Testing via exported `FinalizeStatusRegion` would match the single-lever contract.

- [MEDIUM] Task 5 still contains a placeholder for the hardest harness: “render Step3ReviewWithFinalize with one card; open Step3ReviewModal via the card’s More button; drive finalize” (`docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:228`). The live fixture exists in `tests/components/admin/wizard/step3Page.transitions.test.tsx:36-40` and `:190-208`, but the plan does not cite or inline it despite claiming no placeholders (`docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:293`).

VERDICT: NEEDS-ATTENTION