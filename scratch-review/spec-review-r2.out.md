- [HIGH] Compound Escape handling is still wrong. Spec says blocker `event.stopPropagation()` prevents `Step3ReviewModal` from closing underneath, but both handlers are native `document` listeners; `stopPropagation()` does not stop other listeners on the same target. Since the review modal listener is registered first, it can still run first and call `onClose()` (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:141`, `components/admin/wizard/Step3ReviewModal.tsx:299-307`).

- [HIGH] `cas_per_row` re-scan behavior is invented. Spec says `RescanSheetButton` resolves by calling `run.runLoop()` and transitions to `running`, but live props have no callback, the finalize callsite passes only `driveFileId`/`wizardSessionId`, and the button only `router.refresh()`es on success (`docs/...design.md:97`, `components/admin/FinalizeButton.tsx:638-641`, `components/admin/RescanSheetButton.tsx:35-53`, `components/admin/RescanSheetButton.tsx:123-135`). Test §10.4 only covers `BlockedRowResolver`, so the re-scan path remains underspecified.

- [MEDIUM] Portal rationale misstates the production host. Spec says the production host is inside `PageTransition`, making portal required, but the production footer is already rendered through `WizardFooter`, which portals to `document.body` (`docs/...design.md:81`, `docs/...design.md:156`, `components/admin/wizard/Step3ReviewWithFinalize.tsx:141-153`, `components/admin/wizard/WizardFooter.tsx:62-92`). The proposed Playwright proof under a transformed ancestor does not prove the real production layout condition.

- [MEDIUM] Scrim animation names are not grounded. Spec requires `motion-safe:animate-[scrim-fade_…]`, but `app/globals.css` defines/uses `step3-details-scrim-in`, not `scrim-fade`; §10.8 only asserts the class, not that the keyframe exists (`docs/...design.md:130`, `docs/...design.md:183`, `app/globals.css:726-728`, `app/globals.css:747-748`).

- [LOW] §2 citation is imprecise: `Step3ReviewModal.tsx:557-587` covers the rendered shell, but not `useDialogFocus`, document Escape, or body scroll lock; those are at `components/admin/wizard/Step3ReviewModal.tsx:282-308` (`docs/...design.md:38`).

VERDICT: NEEDS-ATTENTION