- [HIGH] Spec misses a real compound modal state: the Step 3 review modal is explicitly reachable while publish is running (`tests/components/admin/wizard/step3Page.transitions.test.tsx:183-209`), and it is already an `aria-modal` dialog (`components/admin/wizard/Step3ReviewModal.tsx:557-563`). If finalize then returns `race_row` / `cas_per_row` / `error`, §4/§7 would open a second portaled modal with its own focus trap. Spec §7 only covers dismiss-during-enter (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:119-130`) and does not define stacking, closing, suppression, focus ownership, or z-index behavior.

- [HIGH] Portaling breaks existing no-raw-error-code test coverage by making `container.textContent` assertions vacuous. The spec claims existing RTL queries keep passing via `document.body` (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:147-153`), but several invariant-5 assertions inspect `container.textContent`, not `baseElement`/`document.body` (`tests/components/admin/FinalizeButton.test.tsx:467-478`, `550-578`, `607-620`, `649-662`, `1109-1118`, `1126-1140`, `1153-1167`, `1211-1218`). After `createPortal(..., document.body)`, those negative assertions would no longer inspect the visible modal content.

- [MEDIUM] Transition inventory is incomplete under the repo’s spec rules. The spec declares six states (`idle`, `running`, `race_row`, `cas_per_row`, `error`, `complete`) but only enumerates selected modal-relevant transitions (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:117-130`), not all state pairs or the review-modal-open compound above. It also says “scrim fades in” but the test plan only asserts panel classes (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:123`, `166`).

- [LOW] §2’s “component props” citation is imprecise/incomplete: `BlockedRowResolver` actually has `disabled?: boolean` in its prop type (`components/admin/BlockedRowResolver.tsx:40-48`), but the spec lists `{ driveFileId, wizardSessionId, code, displayName?, rebuildExhausted?, onResolved }` and cites only the current callsite (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:42-45`, `components/admin/FinalizeButton.tsx:643-652`). Same pattern for `HelpAffordance`, whose actual props include `params?` and `route?` (`components/admin/HelpAffordance.tsx:45-64`).

- [LOW] The spec artifact contains stray closing tags at the end (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:197-198`), which should not be part of the tracked spec.

VERDICT: NEEDS-ATTENTION