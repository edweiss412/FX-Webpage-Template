- [CRITICAL] Task 2 plans to commit with known failing tests. `docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:126-127` says to run full `FinalizeButton.test.tsx`, accept legacy failures, then commit. That violates the plan’s own green-per-task/commit invariant at `docs/superpowers/plans/2026-07-17-wizard-blocker-modal.md:17` and repo TDD rule.

- [CRITICAL] Task 4’s resolver-race test will not run as written and asserts the wrong thing. It clicks invented `/resolve-blocker-action/` once (`docs/...modal.md:190`), but the real testid is `blocked-row-resolver-${driveFileId}` (`components/admin/BlockedRowResolver.tsx:210`) and the resolver is two-tap (`components/admin/BlockedRowResolver.tsx:184-198`; existing proof `tests/components/admin/FinalizeButton.test.tsx:752-759`). It also asserts no `/finalize` call ever occurred (`docs/...modal.md:193-194`), but entering `cas_per_row` already required `/api/admin/onboarding/finalize` and `/finalize-cas` (`components/admin/FinalizeButton.tsx:331`, `components/admin/FinalizeButton.tsx:397`). It must snapshot finalize-call count before late resolve, not expect zero.

- [HIGH] Task 2’s mount gate is inverted/ambiguous. The plan says the modal “Returns `null` unless state.kind ∈ blockers OR `!useHasMounted()`” (`docs/...modal.md:94`), but the spec requires `createPortal(... document.body)` gated until mounted (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:81`). `useHasMounted()` is false for SSR/hydration (`lib/a11y/useHasMounted.ts:21-26`); treating `!useHasMounted()` as a render condition risks touching `document.body` before mount.

- [HIGH] The compound modal test targets the wrong accessible name and wrong inert node. The plan uses `screen.getByRole("dialog", { name: /review/i })` and expects that dialog to get `aria-hidden` (`docs/...modal.md:221-226`). The real `Step3ReviewModal` dialog is labelled by the show title (`components/admin/wizard/Step3ReviewModal.tsx:558-563`, `components/admin/wizard/Step3ReviewModal.tsx:624-630`; tested at `tests/components/admin/wizard/Step3ReviewModal.test.tsx:227-239`). The planned inert algorithm applies to direct `document.body` children (`docs/...modal.md:242-243`), so in RTL the `aria-hidden` will likely be on the render container ancestor, not the dialog element.

- [HIGH] Task 8’s Playwright plan does not match the real runner/harness. The plan says `pnpm test:e2e <spec>` (`docs/...modal.md:21`) and to mirror `step3-review-page.layout.spec.ts` while rendering/driving React state (`docs/...modal.md:276-280`). The referenced file is a static HTML transcription harness, not a live React state harness (`tests/e2e/step3-review-page.layout.spec.ts:9-13`, `tests/e2e/step3-review-page.layout.spec.ts:143-165`). Standalone layout specs run through `tests/e2e/standalone.config.ts` (`tests/e2e/step3-review-modal.layout.spec.ts:63-65`), and that config’s allowlist does not include `wizard-blocker-modal.layout` (`tests/e2e/standalone.config.ts:23-25`). As written, the real-browser acceptance gate is likely not runnable and cannot drive `cas_per_row`.

- [HIGH] Spec §10.4 is not actually mapped to a task. The spec requires normal `BlockedRowResolver` resolve → `runLoop`/modal unmount and `RescanSheetButton` success → modal remains mounted (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:202`). The plan’s self-review claims coverage via Task 2 + Task 4 (`docs/...modal.md:300`), but Task 2 only sketches error/complete tests (`docs/...modal.md:102-121`) and Task 4 only covers late resolve after Back (`docs/...modal.md:178-196`). Existing tests prove some pre-modal resolver behavior (`tests/components/admin/FinalizeButton.test.tsx:706-771`), but the plan does not add the modal-specific rescan-stays-mounted proof.

- [MEDIUM] The plan references reusable fetch helpers that are not exported. `docs/...modal.md:105-106` says to import or reuse helpers from `FinalizeButton.test.tsx`, but `fetchMock`, `mockJsonResponse`, `mockNdjsonResponse`, and `controllableNdjson` are local to that file (`tests/components/admin/FinalizeButton.test.tsx:33-42`, `tests/components/admin/FinalizeButton.test.tsx:930-976`). Importing them will fail; the plan must require local replication or extraction.

- [MEDIUM] Several “failing test” snippets are placeholders, not executable plan steps: examples include “drive to error” (`docs/...modal.md:108`), “drive to cas_per_row” (`docs/...modal.md:151`), and “assert focus wraps” without code (`docs/...modal.md:216-219`). This violates the writing-plan requirement for runnable, bite-sized TDD steps and will push design decisions into implementation.

- [MEDIUM] Coverage for dismiss/blocking states is incomplete. Spec §5 includes both `race_row` and `cas_per_row` as action-only blockers (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:103-113`), but Task 3 only tests `cas_per_row` Escape/backdrop inertness (`docs/...modal.md:150-160`). `race_row` has distinct re-apply links and should get its own blocking-dismiss proof.

- [MEDIUM] Spec §10.11 includes the compound `error` case, where Escape dismisses the blocker but not the underlying review modal (`docs/superpowers/specs/2026-07-17-wizard-blocker-modal-design.md:209`). Task 5 only sketches `cas_per_row` with Escape swallowed and Back close (`docs/...modal.md:221-232`); Task 3 tests error without an underlying review modal (`docs/...modal.md:138-148`).

- [LOW] Task 1’s hook test depends on `useRouter()` but the new test skeleton does not mock `next/navigation` (`docs/...modal.md:46-52`). Existing `FinalizeButton.test.tsx` does mock it (`tests/components/admin/FinalizeButton.test.tsx:27-31`), and `useFinalizeRun` calls `useRouter()` (`components/admin/FinalizeButton.tsx:168`), so the new file needs the same mock before it can even reach the expected `dismiss` failure.

VERDICT: NEEDS-ATTENTION